<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://kohsheen1234.github.io/kohshh-portfolio/feed.xml" rel="self" type="application/atom+xml"/><link href="https://kohsheen1234.github.io/kohshh-portfolio/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-09T11:51:36+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Chapter 8: Memory Management</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/memory/" rel="alternate" type="text/html" title="Chapter 8: Memory Management"/><published>2026-02-02T00:00:00+00:00</published><updated>2026-02-02T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/memory</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/memory/"><![CDATA[<h2 id="the-stateless-agent-problem">The Stateless Agent Problem</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Stateless:</strong> A system with no memory between requests. Each call is independent — the system doesn't remember previous interactions. A basic LLM API call is stateless: call it twice with the same prompt and it has no idea the first call happened.</p> <p style="margin-top:0.5rem"><strong>Persistent vs ephemeral:</strong> Persistent data survives when the program stops (saved to disk/database). Ephemeral data exists only while the program runs (in RAM) and disappears when it stops. The context window is ephemeral — conversation history in a database is persistent.</p> <p style="margin-top:0.5rem"><strong>Vector database:</strong> A database that stores text as vectors (lists of numbers representing meaning) and supports "semantic search" — finding entries similar in <em>meaning</em>, not just matching keywords. Used for long-term agent memory because you want to find relevant past info by topic, not by exact words.</p> <p style="margin-top:0.5rem"><strong>Semantic search:</strong> Finding things by meaning rather than keywords. Searching "car" would semantically find "automobile" and "vehicle" too. Vector databases do this by comparing the mathematical distance between word/sentence embeddings.</p> <p style="margin-top:0.5rem"><strong>Session:</strong> One complete conversation thread between a user and the agent. Like a phone call — starts when the user first messages, ends when they're done. Multiple sessions with the same user build up long-term memory over time.</p> </div> <p>Every agent pattern in this series — <a href="/kohshh-portfolio/blog/2026/prompt-chaining/">chaining</a>, <a href="/kohshh-portfolio/blog/2026/routing/">routing</a>, <a href="/kohshh-portfolio/blog/2026/planning/">planning</a>, <a href="/kohshh-portfolio/blog/2026/multi-agent/">multi-agent</a> — has one silent assumption: the agent has context. It knows what the user said, what it did before, what tools it called.</p> <p>But where does that context live?</p> <p>Without explicit memory management, agents are stateless. Each call to the LLM starts fresh. Ask it “what’s my order status?” and it has no idea who you are, what you ordered, or what you discussed five messages ago. That’s not an agent — it’s a very expensive autocomplete.</p> <p><strong>Memory</strong> is what transforms a stateless LLM call into an agent that can maintain context, track progress, personalize responses, and learn from past interactions.</p> <p>There are two fundamentally different problems:</p> <ol> <li><strong>Short-term</strong>: remember what was said <em>in this conversation</em></li> <li><strong>Long-term</strong>: remember what matters <em>across conversations</em></li> </ol> <p>Both require different mechanisms. Neither is optional for production agents.</p> <hr/> <h2 id="two-types-of-agent-memory">Two Types of Agent Memory</h2> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">MEMORY ARCHITECTURE</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">User Message</div> <div class="ns-node-sub">Arrives at the Runner · new conversation turn</div> </div> <div class="ns-arrow"></div> <div class="ns-phase"> <div class="ns-phase-title">Short-Term Memory — Session Scope</div> <div class="ns-phase-sub">Ephemeral · lost when session ends · lives in the LLM context window</div> <div class="ns-row"> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Events</div> <div class="ns-node-sub">Full message history for this conversation</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">State</div> <div class="ns-node-sub">Key-value scratchpad · task progress, preferences</div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">Agent Processes · Responds</div> <div class="ns-node-sub">LLM has full session context · generates reply</div> </div> <div class="ns-arrow"></div> <div class="ns-phase"> <div class="ns-phase-title">Long-Term Memory — Cross-Session Scope</div> <div class="ns-phase-sub">Persistent · survives session end · retrieved via semantic search</div> <div class="ns-row"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">MemoryService</div> <div class="ns-node-sub">Extracts facts from sessions · stores persistently</div> </div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Vector Database</div> <div class="ns-node-sub">Semantic search · retrieved into next session's context</div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Persistent Knowledge</div> <div class="ns-node-sub">User preferences · past interactions · learned behaviors</div> </div> </div> </div> <h3 id="short-term-memory">Short-Term Memory</h3> <p>Short-term memory is the <strong>context window</strong> — everything the model sees on a single call: the system prompt, the conversation history, tool results, agent thoughts.</p> <p>It’s fast, has zero retrieval cost, and is always 100% relevant to the current conversation. But it’s <strong>ephemeral</strong>: when the session ends, it’s gone. And it has capacity limits — even a 1M token window fills up eventually, and processing the full history on every call is expensive.</p> <h3 id="long-term-memory">Long-Term Memory</h3> <p>Long-term memory is <strong>external storage</strong> — databases, vector stores, knowledge graphs — that persists across sessions. When an agent needs something from the past, it queries the store, retrieves the relevant data, and injects it into the current context.</p> <p>Vector databases are the dominant storage type here because they support <strong>semantic search</strong>: the agent can find relevant memories by <em>meaning</em>, not just exact keyword match.</p> <hr/> <h2 id="interactive-watch-memory-work">Interactive: Watch Memory Work</h2> <div class="mem-demo-wrapper"> <div class="mem-demo-header"> <span class="mem-demo-title">MEMORY TRACKER — 3-TURN CONVERSATION</span> <button class="mem-demo-btn" id="memDemoPlayBtn">▶ Run Conversation</button> </div> <div class="mem-demo-body"> <div class="mem-demo-turns" id="memTurns"></div> <div class="mem-demo-state-panel" id="memStatePanel" style="display:none"> <div class="mem-panel mem-panel-short"> <div class="mem-panel-label">SHORT-TERM STATE (session.state)</div> <div class="mem-panel-content" id="memShortContent"></div> </div> <div class="mem-panel mem-panel-long"> <div class="mem-panel-label">LONG-TERM MEMORY (MemoryService)</div> <div class="mem-panel-content" id="memLongContent"></div> </div> </div> </div> </div> <style>.mem-demo-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.mem-demo-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.mem-demo-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.mem-demo-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.mem-demo-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.mem-demo-body{padding:1rem 1.1rem;display:flex;flex-direction:column;gap:.85rem}.mem-demo-turns{display:flex;flex-direction:column;gap:.6rem}.mem-turn{border:1px solid var(--global-divider-color);border-radius:7px;padding:.75rem .9rem;background:rgba(128,128,128,0.03);animation:memTurnIn .3s ease}@keyframes memTurnIn{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}.mem-turn-header{display:flex;align-items:center;gap:.6rem;margin-bottom:.45rem}.mem-turn-icon{font-size:.9rem}.mem-turn-role{font-size:.68rem;font-weight:700;letter-spacing:.07em;text-transform:uppercase;color:var(--global-text-color-light)}.mem-turn-text{font-size:.82rem;color:var(--global-text-color);line-height:1.55}.mem-turn-agent .mem-turn-text{color:#2698ba}.mem-turn-tags{display:flex;flex-wrap:wrap;gap:.35rem;margin-top:.4rem}.mem-tag{font-size:.62rem;font-family:monospace;padding:.1em .45em;border-radius:3px;border:1px solid var(--global-divider-color);color:var(--global-text-color-light)}.mem-tag-short{border-color:rgba(201,122,242,0.3);color:#c97af2;background:rgba(201,122,242,0.07)}.mem-tag-long{border-color:rgba(79,201,126,0.3);color:#4fc97e;background:rgba(79,201,126,0.07)}.mem-demo-state-panel{display:flex;gap:.75rem;flex-wrap:wrap}.mem-panel{flex:1;min-width:200px;border:1px solid var(--global-divider-color);border-radius:7px;overflow:hidden}.mem-panel-short{border-color:rgba(201,122,242,0.3)}.mem-panel-long{border-color:rgba(79,201,126,0.3)}.mem-panel-label{font-size:.6rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;padding:.4rem .75rem;border-bottom:1px solid var(--global-divider-color)}.mem-panel-short .mem-panel-label{color:#c97af2;background:rgba(201,122,242,0.06)}.mem-panel-long .mem-panel-label{color:#4fc97e;background:rgba(79,201,126,0.05)}.mem-panel-content{padding:.6rem .75rem;font-size:.73rem;color:var(--global-text-color-light);line-height:1.65;font-family:monospace}</style> <script>
var MEM_TURNS = [
  {
    role: 'user', icon: '👤',
    text: 'Hi! My name is Sarah and I work in Tokyo as a data scientist.',
    tags: [
      { label: 'stored in session.state → user:name = "Sarah"', type: 'short' },
      { label: 'LTM: extracted → name=Sarah, role=data scientist, location=Tokyo', type: 'long' },
    ],
    shortState: '{\n  "user:name": "Sarah",\n  "user:location": "Tokyo",\n  "user:role": "data scientist"\n}',
    longMem: '• Name: Sarah\n• Role: Data scientist\n• Location: Tokyo\n• (extracted from Turn 1)',
  },
  {
    role: 'user', icon: '👤',
    text: 'Can you recommend some AI meetups I should attend?',
    tags: [
      { label: 'short-term: full Turn 1 in context window', type: 'short' },
      { label: 'LTM retrieved: location=Tokyo → scopes recommendation', type: 'long' },
    ],
    shortState: '{\n  "user:name": "Sarah",\n  "user:location": "Tokyo",\n  "user:role": "data scientist",\n  "task:active": "meetup_search"\n}',
    longMem: '• Retrieved: location=Tokyo (similarity search)\n• Retrieved: role=data scientist\n→ Agent recommends Tokyo AI/ML meetups specifically',
  },
  {
    role: 'agent', icon: '🤖',
    text: 'Based on your location in Tokyo, Sarah, I\'d recommend the Tokyo AI Meetup, ML Tokyo, and the Data Science Japan group — all very active for data scientists in 2025.',
    tags: [
      { label: 'used session.state[user:name] for personalization', type: 'short' },
      { label: 'used LTM location recall to scope recommendations', type: 'long' },
    ],
    shortState: '{\n  "user:name": "Sarah",\n  "user:location": "Tokyo",\n  "user:role": "data scientist",\n  "task:active": "meetup_search",\n  "last_recommendation": "Tokyo AI Meetup"\n}',
    longMem: '• Updated: user interested in AI meetups\n• Updated: task_type=recommendation\n• Stored for future sessions',
  }
];

document.addEventListener('DOMContentLoaded', function(){
  var playBtn = document.getElementById('memDemoPlayBtn');
  if (!playBtn) return;
  var running = false;

  playBtn.addEventListener('click', async function(){
    if (running) return;
    running = true;
    playBtn.textContent = '⏳ Running…';
    playBtn.disabled = true;

    var turns = document.getElementById('memTurns');
    var statePanel = document.getElementById('memStatePanel');
    turns.innerHTML = '';
    statePanel.style.display = 'none';

    for (var i = 0; i < MEM_TURNS.length; i++) {
      await new Promise(function(r){ setTimeout(r, 600); });
      var t = MEM_TURNS[i];
      var div = document.createElement('div');
      div.className = 'mem-turn mem-turn-' + t.role;
      var tagsHTML = t.tags.map(function(tg){
        return '<span class="mem-tag mem-tag-'+tg.type+'">'+tg.label+'</span>';
      }).join('');
      div.innerHTML =
        '<div class="mem-turn-header">' +
          '<span class="mem-turn-icon">'+t.icon+'</span>' +
          '<span class="mem-turn-role">'+t.role+'</span>' +
        '</div>' +
        '<div class="mem-turn-text">'+t.text+'</div>' +
        '<div class="mem-turn-tags">'+tagsHTML+'</div>';
      turns.appendChild(div);

      // update state panels
      document.getElementById('memShortContent').textContent = t.shortState;
      document.getElementById('memLongContent').textContent  = t.longMem;
      statePanel.style.display = 'flex';
    }

    running = false;
    playBtn.textContent = '↺ Replay';
    playBtn.disabled = false;
  });
});
</script> <hr/> <h2 id="google-adk-session-state-and-memoryservice">Google ADK: Session, State, and MemoryService</h2> <p>The ADK structures memory into three explicit components with distinct responsibilities.</p> <h3 id="session-the-conversation-thread">Session: The Conversation Thread</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.sessions</span> <span class="kn">import</span> <span class="n">InMemorySessionService</span>

<span class="n">session_service</span> <span class="o">=</span> <span class="nc">InMemorySessionService</span><span class="p">()</span>
</code></pre></div></div> <blockquote> <p>A <strong><code class="language-plaintext highlighter-rouge">Session</code></strong> is one conversation thread. It holds:</p> <ul> <li><code class="language-plaintext highlighter-rouge">id</code> — unique identifier for this thread</li> <li><code class="language-plaintext highlighter-rouge">events</code> — ordered list of all messages, agent replies, and tool calls</li> <li><code class="language-plaintext highlighter-rouge">state</code> — temporary key-value data for this conversation</li> <li><code class="language-plaintext highlighter-rouge">last_update_time</code> — timestamp of the most recent activity</li> </ul> <p>You never create <code class="language-plaintext highlighter-rouge">Session</code> objects directly — the <code class="language-plaintext highlighter-rouge">SessionService</code> manages their lifecycle: create, retrieve, append events, delete.</p> </blockquote> <h3 id="three-sessionservice-implementations">Three SessionService Implementations</h3> <div class="mem-backends-grid"> <div class="mem-backend-card mem-card-dev"> <div class="mem-backend-tier">DEVELOPMENT</div> <div class="mem-backend-name">InMemorySessionService</div> <div class="mem-backend-code"> <code>from google.adk.sessions import InMemorySessionService<br/>session_service = InMemorySessionService()</code> </div> <ul class="mem-backend-props"> <li>✓ Zero setup — no database needed</li> <li>✓ Fast for testing</li> <li>✗ Data lost on app restart</li> <li>✗ Single process only</li> </ul> <span class="mem-backend-use">Use for: local development, unit tests</span> </div> <div class="mem-backend-card mem-card-prod"> <div class="mem-backend-tier">PRODUCTION</div> <div class="mem-backend-name">DatabaseSessionService</div> <div class="mem-backend-code"> <code>from google.adk.sessions import DatabaseSessionService<br/>session_service = DatabaseSessionService(db_url="sqlite:///./agent.db")</code> </div> <ul class="mem-backend-props"> <li>✓ Persistent across restarts</li> <li>✓ Supports SQLite, PostgreSQL, MySQL</li> <li>✓ You control the database</li> <li>✗ Requires infra management</li> </ul> <span class="mem-backend-use">Use for: production apps, self-hosted</span> </div> <div class="mem-backend-card mem-card-cloud"> <div class="mem-backend-tier">CLOUD SCALE</div> <div class="mem-backend-name">VertexAiSessionService</div> <div class="mem-backend-code"> <code>from google.adk.sessions import VertexAiSessionService<br/>session_service = VertexAiSessionService(project=PROJECT_ID, location="us-central1")</code> </div> <ul class="mem-backend-props"> <li>✓ Fully managed by Google Cloud</li> <li>✓ Scales automatically</li> <li>✓ Integrated with Reasoning Engine</li> <li>✗ Requires GCP setup</li> </ul> <span class="mem-backend-use">Use for: enterprise, high-traffic production</span> </div> </div> <style>.mem-backends-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:.85rem;margin:1.5rem 0}.mem-backend-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.5rem}.mem-card-dev{border-left:3px solid #e6a817}.mem-card-prod{border-left:3px solid #2698ba}.mem-card-cloud{border-left:3px solid #4fc97e}.mem-backend-tier{font-size:.58rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color-light)}.mem-backend-name{font-size:.85rem;font-weight:700;color:var(--global-text-color)}.mem-backend-code{background:rgba(0,0,0,0.2);border-radius:5px;padding:.5rem .6rem;font-size:.67rem;line-height:1.55;color:#7dcfff;overflow-x:auto;white-space:pre}.mem-backend-code code{background:0;border:0;color:inherit;font-size:inherit;padding:0}.mem-backend-props{list-style:none;padding:0;margin:0;font-size:.74rem;color:var(--global-text-color-light);display:flex;flex-direction:column;gap:.2rem;line-height:1.45}.mem-backend-use{font-size:.68rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <h3 id="state-the-sessions-scratchpad">State: The Session’s Scratchpad</h3> <p><code class="language-plaintext highlighter-rouge">session.state</code> is a dictionary that lives within a <code class="language-plaintext highlighter-rouge">Session</code>. Think of it as a whiteboard for one conversation — agents read from it and write to it throughout the session.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># The state has 4 key namespaces:
</span>
<span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">preference</span><span class="sh">"</span><span class="p">]</span>        <span class="c1"># session-scoped (cleared when session ends)
</span><span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">user:name</span><span class="sh">"</span><span class="p">]</span>         <span class="c1"># user-scoped (persists across this user's sessions)
</span><span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">app:config</span><span class="sh">"</span><span class="p">]</span>        <span class="c1"># app-scoped (shared across all users)
</span><span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">temp:validation</span><span class="sh">"</span><span class="p">]</span>   <span class="c1"># turn-scoped (cleared after each processing turn)
</span></code></pre></div></div> <blockquote> <p><strong>Key prefixes determine persistence.</strong> Without a prefix, data lives only in the current session. With <code class="language-plaintext highlighter-rouge">user:</code>, it follows the user across all their sessions. With <code class="language-plaintext highlighter-rouge">app:</code>, it’s global to the application. With <code class="language-plaintext highlighter-rouge">temp:</code>, it’s cleared after each processing turn — useful for ephemeral flags.</p> </blockquote> <p><strong>The right way to update state:</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ✓ Method 1: output_key on the agent (simplest — for agent text replies)
</span><span class="n">greeting_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">Greeter</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>      <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Generate a short, friendly greeting.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">output_key</span> <span class="o">=</span> <span class="sh">"</span><span class="s">last_greeting</span><span class="sh">"</span><span class="p">,</span>   <span class="c1"># runner auto-saves the reply to state["last_greeting"]
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">output_key</code>?</strong> The Runner intercepts the agent’s final response and writes it to <code class="language-plaintext highlighter-rouge">session.state["last_greeting"]</code> through the normal <code class="language-plaintext highlighter-rouge">append_event</code> flow. This means the change is recorded in the event history, properly persisted by the SessionService, and timestamped. It’s the least error-prone option.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ✓ Method 2: tool that updates state via ToolContext (for complex updates)
</span><span class="kn">from</span> <span class="n">google.adk.tools.tool_context</span> <span class="kn">import</span> <span class="n">ToolContext</span>
<span class="kn">import</span> <span class="n">time</span>

<span class="k">def</span> <span class="nf">log_user_login</span><span class="p">(</span><span class="n">tool_context</span><span class="p">:</span> <span class="n">ToolContext</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Tracks a user login. Updates login count, status, and timestamp.</span><span class="sh">"""</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">tool_context</span><span class="p">.</span><span class="n">state</span>

    <span class="n">login_count</span> <span class="o">=</span> <span class="n">state</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">user:login_count</span><span class="sh">"</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span>
    <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">user:login_count</span><span class="sh">"</span><span class="p">]</span>    <span class="o">=</span> <span class="n">login_count</span>
    <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">task_status</span><span class="sh">"</span><span class="p">]</span>          <span class="o">=</span> <span class="sh">"</span><span class="s">active</span><span class="sh">"</span>
    <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">user:last_login_ts</span><span class="sh">"</span><span class="p">]</span>   <span class="o">=</span> <span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">()</span>
    <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">temp:validation_needed</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span>

    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">success</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">message</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Login #</span><span class="si">{</span><span class="n">login_count</span><span class="si">}</span><span class="s"> tracked.</span><span class="sh">"</span><span class="p">}</span>
</code></pre></div></div> <blockquote> <p><strong>Why use a tool for state updates?</strong> Tools have access to <code class="language-plaintext highlighter-rouge">ToolContext</code>, which wraps <code class="language-plaintext highlighter-rouge">session.state</code> with proper event-tracking. Changes made through <code class="language-plaintext highlighter-rouge">tool_context.state</code> get recorded in the event log and properly persisted.</p> <p><strong>Never do this:</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">session</span> <span class="o">=</span> <span class="n">session_service</span><span class="p">.</span><span class="nf">get_session</span><span class="p">(</span><span class="n">app_name</span><span class="p">,</span> <span class="n">user_id</span><span class="p">,</span> <span class="n">session_id</span><span class="p">)</span>
<span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">key</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">value</span><span class="sh">"</span>  <span class="c1"># ← bypasses event tracking, may not persist
</span></code></pre></div> </div> <p>Direct dictionary writes bypass the <code class="language-plaintext highlighter-rouge">append_event</code> mechanism. They won’t be persisted by <code class="language-plaintext highlighter-rouge">DatabaseSessionService</code> or <code class="language-plaintext highlighter-rouge">VertexAiSessionService</code> and won’t appear in the event history.</p> </blockquote> <h3 id="memoryservice-long-term-knowledge">MemoryService: Long-Term Knowledge</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Development
</span><span class="kn">from</span> <span class="n">google.adk.memory</span> <span class="kn">import</span> <span class="n">InMemoryMemoryService</span>
<span class="n">memory_service</span> <span class="o">=</span> <span class="nc">InMemoryMemoryService</span><span class="p">()</span>

<span class="c1"># Production (Vertex AI RAG)
</span><span class="kn">from</span> <span class="n">google.adk.memory</span> <span class="kn">import</span> <span class="n">VertexAiRagMemoryService</span>
<span class="n">memory_service</span> <span class="o">=</span> <span class="nc">VertexAiRagMemoryService</span><span class="p">(</span>
    <span class="n">rag_corpus</span>              <span class="o">=</span> <span class="sh">"</span><span class="s">projects/my-project/locations/us-central1/ragCorpora/my-corpus</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">similarity_top_k</span>        <span class="o">=</span> <span class="mi">5</span><span class="p">,</span>      <span class="c1"># retrieve top 5 most relevant memories
</span>    <span class="n">vector_distance_threshold</span> <span class="o">=</span> <span class="mf">0.7</span><span class="p">,</span>  <span class="c1"># minimum similarity score to include
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">VertexAiRagMemoryService</code></strong> stores memories as vector embeddings in a Vertex AI RAG Corpus. When an agent searches memory, it sends a semantic query and gets back the most similar stored facts — not exact keyword matches, but conceptually related information.</p> <p><strong><code class="language-plaintext highlighter-rouge">similarity_top_k=5</code></strong>: retrieve at most 5 memories per query. Higher numbers give more context but consume more of the context window.</p> <p><strong><code class="language-plaintext highlighter-rouge">vector_distance_threshold=0.7</code></strong>: only return memories with at least 70% similarity to the query. This prevents irrelevant memories from polluting the context.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># After a session ends: extract facts and store in long-term memory
</span><span class="k">await</span> <span class="n">memory_service</span><span class="p">.</span><span class="nf">add_session_to_memory</span><span class="p">(</span><span class="n">session</span><span class="p">)</span>

<span class="c1"># When a new session starts: retrieve relevant past memories
</span><span class="n">relevant_memories</span> <span class="o">=</span> <span class="k">await</span> <span class="n">memory_service</span><span class="p">.</span><span class="nf">search_memory</span><span class="p">(</span>
    <span class="n">app_name</span> <span class="o">=</span> <span class="sh">"</span><span class="s">my_app</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">user_id</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">sarah_123</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">query</span>    <span class="o">=</span> <span class="sh">"</span><span class="s">user preferences and past interactions</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <h3 id="session-lifecycle-in-adk">Session lifecycle in ADK</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ADK SESSION LIFECYCLE — what happens on every message, start to finish</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:260px;"><div class="ns-node-title">New Message Arrives</div><div class="ns-node-sub">user sends a message to the agent</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:280px;"><div class="ns-node-title">Runner</div><div class="ns-node-sub">the main executor · retrieves or creates the session for this user</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:300px;"><div class="ns-node-title">SessionService.get_or_create</div><div class="ns-node-sub">loads full conversation history + state so agent has full context</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:300px;"><div class="ns-node-title">Agent Processes with Session Context</div><div class="ns-node-sub">LLM sees all past messages + current state · generates response</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:300px;"><div class="ns-node-title">SessionService.append_event</div><div class="ns-node-sub">saves the agent's response + any state changes to persistent storage</div></div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:480px;"> <div class="ns-branch"> <span class="ns-label-green">Conversation continues</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Wait for Next Message</div><div class="ns-node-sub">session stays open · history grows each turn</div></div> </div> <div class="ns-branch"> <span class="ns-label-red">Session ends</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"><div class="ns-node-title">MemoryService extracts facts</div><div class="ns-node-sub">key info saved to long-term store for future sessions</div></div> </div> </div> </div> </div> <hr/> <h2 id="langchain--langgraph-memory">LangChain &amp; LangGraph Memory</h2> <h3 id="short-term-conversationbuffermemory">Short-Term: ConversationBufferMemory</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain.memory</span> <span class="kn">import</span> <span class="n">ConversationBufferMemory</span>

<span class="n">memory</span> <span class="o">=</span> <span class="nc">ConversationBufferMemory</span><span class="p">(</span>
    <span class="n">memory_key</span>      <span class="o">=</span> <span class="sh">"</span><span class="s">chat_history</span><span class="sh">"</span><span class="p">,</span>   <span class="c1"># must match the variable name in your prompt
</span>    <span class="n">return_messages</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>              <span class="c1"># return list of message objects, not a string
</span><span class="p">)</span>

<span class="n">memory</span><span class="p">.</span><span class="nf">save_context</span><span class="p">(</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">input</span><span class="sh">"</span><span class="p">:</span>  <span class="sh">"</span><span class="s">What</span><span class="sh">'</span><span class="s">s the weather like?</span><span class="sh">"</span><span class="p">},</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">output</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">It</span><span class="sh">'</span><span class="s">s sunny today.</span><span class="sh">"</span><span class="p">},</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">memory_key="chat_history"</code></strong> — this string must exactly match the placeholder in your <code class="language-plaintext highlighter-rouge">ChatPromptTemplate</code>. If your prompt has <code class="language-plaintext highlighter-rouge">MessagesPlaceholder(variable_name="chat_history")</code>, LangChain injects the conversation history there automatically.</p> <p><strong><code class="language-plaintext highlighter-rouge">return_messages=True</code></strong> — returns a list of <code class="language-plaintext highlighter-rouge">HumanMessage</code> / <code class="language-plaintext highlighter-rouge">AIMessage</code> objects instead of a single formatted string. Use this with chat models (ChatOpenAI, ChatGemini). The raw string format works for non-chat LLMs but loses role metadata.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>
<span class="kn">from</span> <span class="n">langchain.chains</span> <span class="kn">import</span> <span class="n">LLMChain</span>
<span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">ChatPromptTemplate</span><span class="p">,</span> <span class="n">MessagesPlaceholder</span>

<span class="n">prompt</span> <span class="o">=</span> <span class="nc">ChatPromptTemplate</span><span class="p">(</span><span class="n">messages</span><span class="o">=</span><span class="p">[</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">You are a friendly travel assistant.</span><span class="sh">"</span><span class="p">),</span>
    <span class="nc">MessagesPlaceholder</span><span class="p">(</span><span class="n">variable_name</span><span class="o">=</span><span class="sh">"</span><span class="s">chat_history</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">human</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">{question}</span><span class="sh">"</span><span class="p">),</span>
<span class="p">])</span>

<span class="n">memory</span> <span class="o">=</span> <span class="nc">ConversationBufferMemory</span><span class="p">(</span><span class="n">memory_key</span><span class="o">=</span><span class="sh">"</span><span class="s">chat_history</span><span class="sh">"</span><span class="p">,</span> <span class="n">return_messages</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="n">conversation</span> <span class="o">=</span> <span class="nc">LLMChain</span><span class="p">(</span><span class="n">llm</span><span class="o">=</span><span class="nc">ChatOpenAI</span><span class="p">(),</span> <span class="n">prompt</span><span class="o">=</span><span class="n">prompt</span><span class="p">,</span> <span class="n">memory</span><span class="o">=</span><span class="n">memory</span><span class="p">)</span>

<span class="n">response</span> <span class="o">=</span> <span class="n">conversation</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">question</span><span class="o">=</span><span class="sh">"</span><span class="s">I want to book a flight.</span><span class="sh">"</span><span class="p">)</span>   <span class="c1"># turn 1
</span><span class="n">response</span> <span class="o">=</span> <span class="n">conversation</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">question</span><span class="o">=</span><span class="sh">"</span><span class="s">My name is Sam, by the way.</span><span class="sh">"</span><span class="p">)</span> <span class="c1"># turn 2
</span><span class="n">response</span> <span class="o">=</span> <span class="n">conversation</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">question</span><span class="o">=</span><span class="sh">"</span><span class="s">What was my name again?</span><span class="sh">"</span><span class="p">)</span>    <span class="c1"># turn 3 — knows it's Sam
</span></code></pre></div></div> <blockquote> <p>The <code class="language-plaintext highlighter-rouge">memory</code> object maintains the conversation buffer. On each <code class="language-plaintext highlighter-rouge">predict()</code> call, it: 1) loads the existing history into <code class="language-plaintext highlighter-rouge">{chat_history}</code>, 2) generates the response, 3) saves the new exchange to the buffer. Turn 3 works because turns 1 and 2 are in the buffer.</p> </blockquote> <h3 id="long-term-langgraph-inmemorystore">Long-Term: LangGraph InMemoryStore</h3> <p>LangGraph provides three long-term memory types, each mapped to a different human memory analogy:</p> <div class="mem-types-grid"> <div class="mem-type-card"> <div class="mem-type-icon">📖</div> <div class="mem-type-name">Semantic Memory</div> <div class="mem-type-sub">Remembering Facts</div> <div class="mem-type-desc">Stores facts and user preferences — a continuously updated profile or collection of factual documents. Used to ground responses with user-specific context.</div> <div class="mem-type-ex">user:name, location, preferences, domain knowledge</div> </div> <div class="mem-type-card"> <div class="mem-type-icon">🎬</div> <div class="mem-type-name">Episodic Memory</div> <div class="mem-type-sub">Remembering Experiences</div> <div class="mem-type-desc">Stores past events and successful interaction sequences. Implemented via few-shot examples — the agent learns from past task completions to do them better next time.</div> <div class="mem-type-ex">successful tool call patterns, past task solutions</div> </div> <div class="mem-type-card"> <div class="mem-type-icon">⚙</div> <div class="mem-type-name">Procedural Memory</div> <div class="mem-type-sub">Remembering Rules</div> <div class="mem-type-desc">The agent's core instructions and behaviors — often in the system prompt. Agents can update their own procedural memory through Reflection: reviewing past interactions and rewriting their own instructions.</div> <div class="mem-type-ex">system prompt rules, behavioral guidelines, self-updated instructions</div> </div> </div> <style>.mem-types-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:.85rem;margin:1.5rem 0}.mem-type-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.mem-type-icon{font-size:1.1rem}.mem-type-name{font-size:.85rem;font-weight:700;color:var(--global-text-color)}.mem-type-sub{font-size:.68rem;font-family:monospace;color:#c97af2}.mem-type-desc{font-size:.78rem;color:var(--global-text-color-light);line-height:1.5;margin:0}.mem-type-ex{font-size:.67rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <h3 id="langgraph-store-put-get-search">LangGraph Store: put, get, search</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langgraph.store.memory</span> <span class="kn">import</span> <span class="n">InMemoryStore</span>

<span class="k">def</span> <span class="nf">embed</span><span class="p">(</span><span class="n">texts</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">list</span><span class="p">[</span><span class="nb">float</span><span class="p">]]:</span>
    <span class="k">return</span> <span class="p">[[</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">2.0</span><span class="p">]</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="n">texts</span><span class="p">]</span>   <span class="c1"># replace with real embedding model in production
</span>
<span class="n">store</span> <span class="o">=</span> <span class="nc">InMemoryStore</span><span class="p">(</span><span class="n">index</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">embed</span><span class="sh">"</span><span class="p">:</span> <span class="n">embed</span><span class="p">,</span> <span class="sh">"</span><span class="s">dims</span><span class="sh">"</span><span class="p">:</span> <span class="mi">2</span><span class="p">})</span>

<span class="c1"># Namespace: (user_id, context_type) — like a folder structure
</span><span class="n">namespace</span> <span class="o">=</span> <span class="p">(</span><span class="sh">"</span><span class="s">sarah_123</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">preferences</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Store a memory
</span><span class="n">store</span><span class="p">.</span><span class="nf">put</span><span class="p">(</span>
    <span class="n">namespace</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">a-memory</span><span class="sh">"</span><span class="p">,</span>           <span class="c1"># key — like a filename
</span>    <span class="p">{</span>
        <span class="sh">"</span><span class="s">rules</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="sh">"</span><span class="s">User likes short, direct responses</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">User speaks English and Python</span><span class="sh">"</span><span class="p">],</span>
        <span class="sh">"</span><span class="s">timezone</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Asia/Tokyo</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">},</span>
<span class="p">)</span>

<span class="c1"># Retrieve by key
</span><span class="n">item</span> <span class="o">=</span> <span class="n">store</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">namespace</span><span class="p">,</span> <span class="sh">"</span><span class="s">a-memory</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Semantic search within the namespace
</span><span class="n">results</span> <span class="o">=</span> <span class="n">store</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span>
    <span class="n">namespace</span><span class="p">,</span>
    <span class="nb">filter</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">timezone</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Asia/Tokyo</span><span class="sh">"</span><span class="p">},</span>    <span class="c1"># metadata filter
</span>    <span class="n">query</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">language preferences</span><span class="sh">"</span><span class="p">,</span>        <span class="c1"># semantic similarity query
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Namespace = <code class="language-plaintext highlighter-rouge">(user_id, context_type)</code></strong> — think of it as a two-level folder structure. The first level is the user, the second is the type of memory (preferences, episodes, instructions). This prevents memory from one user bleeding into another’s.</p> <p><strong><code class="language-plaintext highlighter-rouge">store.put(namespace, key, data)</code></strong> — the <code class="language-plaintext highlighter-rouge">key</code> is like a filename. If a memory with that key already exists, it’s overwritten. Use descriptive keys like <code class="language-plaintext highlighter-rouge">"user-profile"</code> or <code class="language-plaintext highlighter-rouge">"booking-preferences"</code>.</p> <p><strong><code class="language-plaintext highlighter-rouge">store.search()</code> with both <code class="language-plaintext highlighter-rouge">filter</code> and <code class="language-plaintext highlighter-rouge">query</code></strong> — <code class="language-plaintext highlighter-rouge">filter</code> does exact metadata matching (deterministic); <code class="language-plaintext highlighter-rouge">query</code> does vector similarity search (semantic). Using both together is the most precise retrieval.</p> </blockquote> <h3 id="procedural-memory-self-updating-instructions">Procedural Memory: Self-Updating Instructions</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">update_instructions</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">State</span><span class="p">,</span> <span class="n">store</span><span class="p">:</span> <span class="n">BaseStore</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Reflection node: agent rewrites its own system prompt based on conversation.</span><span class="sh">"""</span>
    <span class="n">namespace</span> <span class="o">=</span> <span class="p">(</span><span class="sh">"</span><span class="s">instructions</span><span class="sh">"</span><span class="p">,)</span>
    <span class="n">current_instructions</span> <span class="o">=</span> <span class="n">store</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="n">namespace</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>

    <span class="c1"># Ask the LLM to review its behavior and propose improvements
</span>    <span class="n">prompt</span> <span class="o">=</span> <span class="n">prompt_template</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span>
        <span class="n">instructions</span> <span class="o">=</span> <span class="n">current_instructions</span><span class="p">.</span><span class="n">value</span><span class="p">[</span><span class="sh">"</span><span class="s">instructions</span><span class="sh">"</span><span class="p">],</span>
        <span class="n">conversation</span> <span class="o">=</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">],</span>
    <span class="p">)</span>
    <span class="n">output</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
    <span class="n">new_instructions</span> <span class="o">=</span> <span class="n">output</span><span class="p">[</span><span class="sh">"</span><span class="s">new_instructions</span><span class="sh">"</span><span class="p">]</span>

    <span class="c1"># Overwrite stored instructions with the improved version
</span>    <span class="n">store</span><span class="p">.</span><span class="nf">put</span><span class="p">((</span><span class="sh">"</span><span class="s">agent_instructions</span><span class="sh">"</span><span class="p">,),</span> <span class="sh">"</span><span class="s">agent_a</span><span class="sh">"</span><span class="p">,</span> <span class="p">{</span><span class="sh">"</span><span class="s">instructions</span><span class="sh">"</span><span class="p">:</span> <span class="n">new_instructions</span><span class="p">})</span>
</code></pre></div></div> <blockquote> <p>This is the <a href="/kohshh-portfolio/blog/2026/reflection/">Reflection</a> pattern (Chapter 4) applied to procedural memory. The agent reads its own instructions, sees how the conversation went, and rewrites its prompt to do better next time. Each session, it gets slightly smarter.</p> </blockquote> <hr/> <h2 id="vertex-memory-bank">Vertex Memory Bank</h2> <p>For teams using Google Cloud, <strong>Memory Bank</strong> (part of Vertex AI Agent Engine) is a fully managed long-term memory service that works across frameworks — ADK, LangGraph, and CrewAI.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.memory</span> <span class="kn">import</span> <span class="n">VertexAiMemoryBankService</span>

<span class="n">memory_service</span> <span class="o">=</span> <span class="nc">VertexAiMemoryBankService</span><span class="p">(</span>
    <span class="n">project</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">my-gcp-project</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">location</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">us-central1</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">agent_engine_id</span> <span class="o">=</span> <span class="n">agent_engine_id</span><span class="p">,</span>
<span class="p">)</span>

<span class="c1"># After a session completes: extract and store memories
</span><span class="n">session</span> <span class="o">=</span> <span class="k">await</span> <span class="n">session_service</span><span class="p">.</span><span class="nf">get_session</span><span class="p">(</span><span class="n">app_name</span><span class="o">=</span><span class="n">app_name</span><span class="p">,</span> <span class="n">user_id</span><span class="o">=</span><span class="sh">"</span><span class="s">USER_ID</span><span class="sh">"</span><span class="p">,</span> <span class="n">session_id</span><span class="o">=</span><span class="n">session</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span>
<span class="k">await</span> <span class="n">memory_service</span><span class="p">.</span><span class="nf">add_session_to_memory</span><span class="p">(</span><span class="n">session</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>How Memory Bank works:</strong> After <code class="language-plaintext highlighter-rouge">add_session_to_memory()</code>, Gemini asynchronously analyzes the conversation history, extracts key facts and user preferences, and stores them persistently. On the next session, the agent retrieves relevant memories via similarity search — getting only the facts that matter for the current conversation, not the entire history.</p> <p><strong>What makes it different from <code class="language-plaintext highlighter-rouge">VertexAiRagMemoryService</code>:</strong> Memory Bank actively uses Gemini to <em>understand</em> and <em>consolidate</em> memories — resolving contradictions, merging related facts, and updating existing preferences. It’s not just a vector store; it’s a managed memory intelligence layer.</p> </blockquote> <hr/> <h2 id="storage-backend-comparison">Storage Backend Comparison</h2> <div class="mem-compare-wrapper"> <div class="mem-compare-header"> <span class="mem-compare-title">STORAGE BACKENDS — CAPABILITY COMPARISON</span> <span class="mem-compare-sub">Hover a bar for details</span> </div> <div style="overflow-x:auto;padding:0.75rem;"> <canvas id="memCompareChart" width="620" height="260"></canvas> </div> <div class="mem-compare-legend"> <div class="mem-leg-item"><span class="mem-leg-dot" style="background:#e6a817"></span>InMemory (dev)</div> <div class="mem-leg-item"><span class="mem-leg-dot" style="background:#2698ba"></span>Database (prod)</div> <div class="mem-leg-item"><span class="mem-leg-dot" style="background:#4fc97e"></span>Vertex AI (cloud)</div> </div> <div class="mem-compare-tooltip" id="memTooltip" style="display:none"></div> </div> <style>.mem-compare-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0;position:relative}.mem-compare-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.4rem}.mem-compare-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.mem-compare-sub{font-size:.65rem;color:var(--global-text-color-light);opacity:.7}.mem-compare-legend{display:flex;gap:1.25rem;padding:.6rem 1.1rem;border-top:1px solid var(--global-divider-color);flex-wrap:wrap}.mem-leg-item{display:flex;align-items:center;gap:.4rem;font-size:.72rem;color:var(--global-text-color-light)}.mem-leg-dot{width:9px;height:9px;border-radius:2px;flex-shrink:0}.mem-compare-tooltip{position:fixed;background:var(--global-bg-color);border:1px solid var(--global-divider-color);border-radius:6px;padding:.5rem .75rem;font-size:.73rem;color:var(--global-text-color);pointer-events:none;z-index:200;max-width:240px;line-height:1.5;box-shadow:0 4px 16px rgba(0,0,0,0.35)}</style> <script>
(function(){
  var canvas = document.getElementById('memCompareChart');
  if (!canvas) return;
  var ctx = canvas.getContext('2d');
  var tooltip = document.getElementById('memTooltip');

  var dims    = ['Setup ease', 'Persistence', 'Scalability', 'Semantic search', 'Cost'];
  var backends = ['InMemory', 'Database', 'Vertex AI'];
  var colors   = ['#e6a817', '#2698ba', '#4fc97e'];
  var scores   = [
    [5, 3, 1], // Setup ease
    [1, 4, 5], // Persistence
    [1, 3, 5], // Scalability
    [2, 2, 5], // Semantic search
    [5, 3, 2], // Cost (inverse — free = 5)
  ];
  var descs = [
    ['Zero config, no dependencies.', 'Need DB URL, driver install.', 'Requires GCP project + auth.'],
    ['Lost on restart — dev only.', 'Survives restarts, SQL-backed.', 'Fully managed, durable.'],
    ['Single process only.', 'Vertical scale, connection pooling.', 'Auto-scales with GCP infra.'],
    ['None — exact key lookup only.', 'None natively — add pgvector.', 'Built-in RAG + embeddings.'],
    ['Free — no infra cost.', 'Your DB hosting cost.', 'GCP billing per query.'],
  ];

  var dpr=window.devicePixelRatio||1, W=Math.min(620,canvas.parentElement.getBoundingClientRect().width-24), H=260;
  canvas.width=W*dpr; canvas.height=H*dpr; canvas.style.width=W+'px'; canvas.style.height=H+'px';
  ctx.scale(dpr,dpr);

  var ML=90,MR=20,MT=22,MB=36, PW=W-ML-MR, PH=H-MT-MB;
  var nD=dims.length,nB=backends.length, gW=PW/nD, bW=(gW*0.7)/nB, bGap=(gW*0.3)/(nB+1);
  var hitRects=[];

  function getTheme(){ var s=getComputedStyle(document.documentElement); return {text:s.getPropertyValue('--global-text-color').trim()||'#e0e0e0',muted:s.getPropertyValue('--global-text-color-light').trim()||'#888',div:s.getPropertyValue('--global-divider-color').trim()||'#333'}; }

  function draw(hD,hB){
    ctx.clearRect(0,0,W,H); hitRects=[];
    var th=getTheme();
    for(var v=1;v<=5;v++){
      var gy=MT+(1-(v-1)/4)*PH;
      ctx.strokeStyle=th.div; ctx.lineWidth=0.5; ctx.setLineDash([3,3]);
      ctx.beginPath(); ctx.moveTo(ML,gy); ctx.lineTo(ML+PW,gy); ctx.stroke();
      ctx.setLineDash([]); ctx.fillStyle=th.muted; ctx.font='10px monospace'; ctx.textAlign='right';
      ctx.fillText(v,ML-5,gy+3.5);
    }
    for(var di=0;di<nD;di++){
      var gx=ML+di*gW;
      for(var bi=0;bi<nB;bi++){
        var bx=gx+bGap*(bi+1)+bW*bi, sc=scores[di][bi];
        var bh=(sc-1)/4*PH, by=MT+PH-bh;
        var isH=(hD===di&&hB===bi), alpha=(hD===null)?1:(isH?1:0.22);
        ctx.globalAlpha=alpha; ctx.fillStyle=colors[bi];
        ctx.fillRect(bx,by,bW,bh); ctx.globalAlpha=1;
        hitRects.push({x:bx,y:by,w:bW,h:bh,di:di,bi:bi,sc:sc});
      }
      ctx.fillStyle=th.muted; ctx.font='9px monospace'; ctx.textAlign='center';
      ctx.fillText(dims[di],gx+gW/2,H-MB+14);
    }
  }
  draw(null,null);

  canvas.addEventListener('mousemove',function(e){
    var rect=canvas.getBoundingClientRect(), mx=e.clientX-rect.left, my=e.clientY-rect.top, hit=null;
    for(var i=0;i<hitRects.length;i++){ var r=hitRects[i]; if(mx>=r.x&&mx<=r.x+r.w&&my>=r.y&&my<=r.y+r.h){hit=r;break;} }
    if(hit){
      draw(hit.di,hit.bi);
      tooltip.style.display='block'; tooltip.style.left=(e.clientX+12)+'px'; tooltip.style.top=(e.clientY-10)+'px';
      tooltip.innerHTML='<strong>'+backends[hit.bi]+' — '+dims[hit.di]+'</strong><br><span style="color:var(--global-text-color-light)">'+descs[hit.di][hit.bi]+'</span>';
      canvas.style.cursor='pointer';
    } else { draw(null,null); tooltip.style.display='none'; canvas.style.cursor='default'; }
  });
  canvas.addEventListener('mouseleave',function(){ draw(null,null); tooltip.style.display='none'; });
})();
</script> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="mem-summary-card"> <div class="mem-summary-col"> <div class="mem-summary-label">WHAT</div> <p>A dual-component system: short-term memory (session context window) for the current conversation, and long-term memory (external vector store) for knowledge that persists across sessions.</p> </div> <div class="mem-summary-divider"></div> <div class="mem-summary-col"> <div class="mem-summary-label">WHY</div> <p>Without memory, every conversation starts from zero. Agents can't maintain context, track progress, personalize responses, or learn from past interactions — making them useless for anything beyond single-turn Q&amp;A.</p> </div> <div class="mem-summary-divider"></div> <div class="mem-summary-col"> <div class="mem-summary-label">RULE OF THUMB</div> <p>Use short-term memory (Session + State) for any agent handling multi-turn conversations. Add long-term memory (MemoryService) when users expect personalization or continuity across sessions.</p> </div> </div> <style>.mem-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.mem-summary-card{flex-direction:column}}.mem-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.mem-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.mem-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.mem-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li><strong>Memory has two fundamentally different problems:</strong> short-term (within a conversation) and long-term (across conversations). They require different storage mechanisms and have different access patterns.</li> <li><strong>ADK’s three primitives:</strong> <code class="language-plaintext highlighter-rouge">Session</code> tracks the conversation thread. <code class="language-plaintext highlighter-rouge">State</code> is the session’s temporary scratchpad (with namespace prefixes for scope). <code class="language-plaintext highlighter-rouge">MemoryService</code> is the searchable long-term knowledge store.</li> <li><strong>Update state through <code class="language-plaintext highlighter-rouge">append_event</code>, not direct dict writes.</strong> <code class="language-plaintext highlighter-rouge">session.state["key"] = value</code> bypasses event tracking and may not persist. Use <code class="language-plaintext highlighter-rouge">output_key</code> on agents or <code class="language-plaintext highlighter-rouge">EventActions.state_delta</code> in events.</li> <li><strong>State key prefixes determine persistence:</strong> <code class="language-plaintext highlighter-rouge">user:</code> persists across sessions for that user. <code class="language-plaintext highlighter-rouge">app:</code> is global. <code class="language-plaintext highlighter-rouge">temp:</code> clears each turn. No prefix = session-only.</li> <li><strong>Long-term memory uses vector search.</strong> Semantic retrieval finds relevant memories by meaning, not keyword. <code class="language-plaintext highlighter-rouge">similarity_top_k</code> and <code class="language-plaintext highlighter-rouge">vector_distance_threshold</code> control how much you retrieve and how relevant it must be.</li> <li><strong>LangChain’s <code class="language-plaintext highlighter-rouge">ConversationBufferMemory</code></strong> handles short-term automatically — loads history into the prompt, saves each turn. Use <code class="language-plaintext highlighter-rouge">return_messages=True</code> for chat models.</li> <li><strong>LangGraph’s <code class="language-plaintext highlighter-rouge">InMemoryStore</code></strong> supports three long-term memory types: Semantic (facts), Episodic (past task solutions), and Procedural (self-updating instructions via Reflection).</li> <li><strong>Vertex Memory Bank</strong> is the fully managed option — Gemini analyzes conversations, extracts facts, resolves contradictions, and provides personalized retrieval across ADK, LangGraph, and CrewAI.</li> </ul> <hr/> <p><em>Next up — Chapter 9: Human-in-the-Loop, where agents pause, ask for approval, and incorporate human judgment at critical decision points.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="memory"/><category term="session"/><category term="state"/><summary type="html"><![CDATA[Without memory, every conversation starts from zero. Memory management gives agents the ability to remember — short-term context within a session, long-term knowledge across sessions.]]></summary></entry><entry><title type="html">Chapter 7: Multi-Agent Collaboration</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/multi-agent/" rel="alternate" type="text/html" title="Chapter 7: Multi-Agent Collaboration"/><published>2026-01-29T00:00:00+00:00</published><updated>2026-01-29T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/multi-agent</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/multi-agent/"><![CDATA[<h2 id="why-one-agent-isnt-enough">Why One Agent Isn’t Enough</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Specialization:</strong> Focusing on a narrow domain to become very good at it. A specialist cardiologist is better at heart problems than a general practitioner, even though the GP knows more topics overall. The same principle applies to AI agents.</p> <p style="margin-top:0.5rem"><strong>Orchestrator:</strong> The agent that manages and coordinates other agents. It receives the high-level goal, decomposes it, assigns sub-tasks, and aggregates results. Think of it as the project manager in a team.</p> <p style="margin-top:0.5rem"><strong>Sequential vs parallel:</strong> Sequential = one after another (like a queue at a counter). Parallel = simultaneously (like multiple checkout lanes open at once). Sequential is simpler to reason about; parallel is faster for independent tasks.</p> <p style="margin-top:0.5rem"><strong>Why does more roles = worse LLM performance?</strong> When you stuff too many instructions into one system prompt ("be a researcher AND a writer AND a fact-checker"), the model splits its "attention" across all goals. It tends to do each poorly. Separate, focused system prompts produce better results for each role.</p> </div> <p>Every chapter up to now has been about making a single agent smarter — chain it, route it, run it in parallel, give it tools, make it reflect, make it plan. All of these improve a single agent’s performance.</p> <p>But there’s a ceiling.</p> <p>A single agent handling a complex research project needs to be: a domain expert, a search specialist, a statistical analyst, a fact-checker, and a polished writer — simultaneously. The more roles you pile onto one system prompt, the worse it performs at each of them.</p> <p>The same problem that prompted humans to build teams applies to agents: <strong>specialization beats generalism at scale</strong>.</p> <p>Multi-agent collaboration breaks a complex goal into sub-tasks, assigns each to an agent built specifically for that task, and coordinates their outputs into a unified result. The researcher does the research. The analyst runs the numbers. The writer assembles the draft. The reviewer catches errors. Each agent excels at its role. The system as a whole exceeds what any single agent could produce.</p> <hr/> <h2 id="what-multi-agent-collaboration-is">What Multi-Agent Collaboration Is</h2> <p>Multi-agent collaboration is a system design pattern where:</p> <ol> <li>A <strong>complex goal</strong> is decomposed into discrete sub-tasks</li> <li>Each sub-task is assigned to a <strong>specialized agent</strong> with the right tools, knowledge, or reasoning approach</li> <li>Agents <strong>coordinate</strong> through defined communication protocols — passing outputs, delegating tasks, sharing state</li> <li>A <strong>final output</strong> emerges from the coordinated work of the team</li> </ol> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">MULTI-AGENT COLLABORATION PATTERN</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">Complex Goal</div> <div class="ns-node-sub">Too large for any single agent</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Orchestrator Agent</div> <div class="ns-node-sub">Decomposes goal · delegates to specialists</div> </div> <div class="ns-arrow"></div> <div class="ns-phase"> <div class="ns-phase-title">Specialist Agents — each owns a distinct role</div> <div class="ns-phase-sub">Separate tools, knowledge, and system prompts per agent</div> <div class="ns-row"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">Research Agent</div> <div class="ns-node-sub">Search · retrieve</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">Analysis Agent</div> <div class="ns-node-sub">Process · compute</div> </div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Writer Agent</div> <div class="ns-node-sub">Draft · format</div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">Synthesis Agent</div> <div class="ns-node-sub">Merges all specialist outputs · resolves conflicts</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Final Result</div> <div class="ns-node-sub">Exceeds what any single agent could produce</div> </div> </div> </div> <p>The critical ingredient is <strong>inter-agent communication</strong>: a standardized way for agents to exchange data, delegate work, and signal completion. Without it, you just have isolated agents running in parallel — not a team.</p> <hr/> <h2 id="the-six-interaction-models">The Six Interaction Models</h2> <p>Agent teams can be structured in fundamentally different ways. The choice of structure changes autonomy, fault tolerance, complexity, and scalability.</p> <div class="mas-models-wrapper"> <div class="mas-models-tabs"> <button class="mas-tab active" data-model="0" onclick="masSelect(0)">Single</button> <button class="mas-tab" data-model="1" onclick="masSelect(1)">Network</button> <button class="mas-tab" data-model="2" onclick="masSelect(2)">Supervisor</button> <button class="mas-tab" data-model="3" onclick="masSelect(3)">Supervisor as Tool</button> <button class="mas-tab" data-model="4" onclick="masSelect(4)">Hierarchical</button> <button class="mas-tab" data-model="5" onclick="masSelect(5)">Custom</button> </div> <div class="mas-models-body"> <div class="mas-model-content" id="masContent"> <div class="mas-model-left"> <div class="mas-model-name" id="masName">Single Agent</div> <div class="mas-model-desc" id="masDesc">One agent handles everything autonomously. Simple to build and manage, but constrained by a single agent's scope and resources. No inter-agent communication overhead. Suitable only when the task is fully self-contained.</div> <div class="mas-model-props" id="masProps"> <div class="mas-prop"><span class="mas-prop-label">Autonomy</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:20%"></div></div></div> <div class="mas-prop"><span class="mas-prop-label">Fault tolerance</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:10%"></div></div></div> <div class="mas-prop"><span class="mas-prop-label">Scalability</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:15%"></div></div></div> <div class="mas-prop"><span class="mas-prop-label">Complexity</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:10%"></div></div></div> </div> <div class="mas-model-usecase" id="masUsecase">Best for: simple, self-contained tasks with no need for specialization</div> </div> <div class="mas-model-right"> <svg id="masSvg" viewBox="0 0 240 180" width="240" height="180"></svg> </div> </div> </div> </div> <style>.mas-models-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.mas-models-tabs{display:flex;overflow-x:auto;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.04)}.mas-tab{flex-shrink:0;padding:.5rem .85rem;font-family:monospace;font-size:.68rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s,color .15s;white-space:nowrap}.mas-tab:last-child{border-right:0}.mas-tab.active{background:rgba(38,152,186,0.1);color:#2698ba;font-weight:700}.mas-tab:hover:not(.active){background:rgba(128,128,128,0.08)}.mas-models-body{padding:1.1rem}.mas-model-content{display:flex;gap:1.5rem;align-items:flex-start;flex-wrap:wrap}.mas-model-left{flex:1;min-width:220px;display:flex;flex-direction:column;gap:.75rem}.mas-model-name{font-size:1rem;font-weight:700;color:var(--global-text-color)}.mas-model-desc{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6}.mas-model-props{display:flex;flex-direction:column;gap:.45rem}.mas-prop{display:flex;align-items:center;gap:.75rem}.mas-prop-label{font-size:.68rem;font-family:monospace;color:var(--global-text-color-light);width:100px;flex-shrink:0}.mas-prop-bar{flex:1;height:4px;background:rgba(128,128,128,0.15);border-radius:2px;overflow:hidden}.mas-prop-fill{height:100%;background:#2698ba;border-radius:2px;transition:width .4s ease}.mas-model-usecase{font-size:.7rem;font-family:monospace;color:#4fc97e;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}.mas-model-right{flex-shrink:0}#masSvg{display:block}</style> <script>
var MAS_MODELS = [
  {
    name: "Single Agent",
    desc: "One agent handles everything autonomously. Simple to build and manage, but constrained by a single agent's scope and resources. No inter-agent communication overhead. Suitable only when the task is fully self-contained.",
    props: [20, 10, 15, 10],
    usecase: "Best for: simple, self-contained tasks with no need for specialization",
    draw: function(svg) {
      svg.innerHTML = '';
      addNode(svg, 120, 90, 'Agent', '#2698ba', 40, 20);
    }
  },
  {
    name: "Network",
    desc: "Multiple agents interact peer-to-peer in a decentralized mesh. Agents share information, resources, and tasks directly. Resilient — no single point of failure. Challenging to coordinate at scale without explicit orchestration.",
    props: [80, 80, 60, 70],
    usecase: "Best for: distributed tasks where agents need to share information freely",
    draw: function(svg) {
      svg.innerHTML = '';
      var pts = [[120,40],[60,100],[180,100],[80,155],[160,155]];
      // Edges
      var edges = [[0,1],[0,2],[1,3],[2,4],[1,2],[3,4],[0,3],[0,4]];
      edges.forEach(function(e){
        addEdge(svg,pts[e[0]][0],pts[e[0]][1],pts[e[1]][0],pts[e[1]][1],'#4a5a6a');
      });
      pts.forEach(function(p,i){
        addNode(svg,p[0],p[1],'A'+(i+1),'#c97af2',26,14);
      });
    }
  },
  {
    name: "Supervisor",
    desc: "A central supervisor agent coordinates and delegates to subordinate workers. Clear authority and task allocation. The supervisor is a single point of failure and can become a bottleneck with many subordinates.",
    props: [50, 30, 70, 45],
    usecase: "Best for: tasks with clear decomposition and one central coordinator",
    draw: function(svg) {
      svg.innerHTML = '';
      var workers = [[50,140],[100,140],[150,140],[200,140]];
      workers.forEach(function(p){
        addEdge(svg,120,50,p[0],p[1],'#4a5a6a');
      });
      addNode(svg,120,50,'Supervisor','#2698ba',42,18);
      workers.forEach(function(p,i){
        addNode(svg,p[0],p[1],'W'+(i+1),'#c97af2',28,14);
      });
    }
  },
  {
    name: "Supervisor as Tool",
    desc: "The supervisor provides resources, guidance, or analytical capabilities to other agents rather than commanding them. Workers can query the supervisor for support without rigid top-down control.",
    props: [60, 40, 65, 50],
    usecase: "Best for: teams that need shared expertise without strict hierarchy",
    draw: function(svg) {
      svg.innerHTML = '';
      var workers = [[40,140],[120,140],[200,140]];
      workers.forEach(function(p){
        addEdgeDashed(svg,120,55,p[0],p[1],'#e6a817');
      });
      addNode(svg,120,50,'Supervisor\n(Tool)','#e6a817',52,20);
      workers.forEach(function(p,i){
        addNode(svg,p[0],p[1],'Agent '+(i+1),'#c97af2',36,14);
      });
    }
  },
  {
    name: "Hierarchical",
    desc: "Multiple layers of supervisors, each managing the layer below. Higher supervisors oversee lower ones; operational agents sit at the bottom. Handles complexity at scale. Requires careful boundary design at each layer.",
    props: [70, 50, 90, 80],
    usecase: "Best for: large-scale systems with natural sub-problem decomposition",
    draw: function(svg) {
      svg.innerHTML = '';
      addNode(svg,120,28,'Root','#2698ba',36,14);
      addEdge(svg,120,42,70,80,'#4a5a6a');
      addEdge(svg,120,42,170,80,'#4a5a6a');
      addNode(svg,70,85,'Mgr A','#e6a817',36,14);
      addNode(svg,170,85,'Mgr B','#e6a817',36,14);
      [[40,145],[100,145]].forEach(function(p){addEdge(svg,70,99,p[0],p[1],'#4a5a6a');addNode(svg,p[0],p[1],'W','#c97af2',22,12);});
      [[140,145],[200,145]].forEach(function(p){addEdge(svg,170,99,p[0],p[1],'#4a5a6a');addNode(svg,p[0],p[1],'W','#c97af2',22,12);});
    }
  },
  {
    name: "Custom",
    desc: "A bespoke structure combining elements of multiple models — or an entirely novel design. Optimized for specific metrics, highly dynamic environments, or domain-specific constraints. Requires the deepest understanding of multi-agent design principles.",
    props: [90, 70, 85, 95],
    usecase: "Best for: complex, unique domains where no standard model fits",
    draw: function(svg) {
      svg.innerHTML = '';
      // Mix of hierarchical + peer links
      addNode(svg,120,28,'Orch','#2698ba',34,14);
      addEdge(svg,120,42,60,90,'#4a5a6a');
      addEdge(svg,120,42,180,90,'#4a5a6a');
      addNode(svg,60,95,'Expert A','#c97af2',38,14);
      addNode(svg,180,95,'Expert B','#4fc97e',38,14);
      addEdgeDashed(svg,60,111,180,111,'#e6a817');
      addEdge(svg,120,42,120,150,'#4a5a6a');
      addNode(svg,120,155,'Tool Agent','#e6a817',42,14);
      addEdgeDashed(svg,60,111,120,143,'#4a5a6a');
      addEdgeDashed(svg,180,111,120,143,'#4a5a6a');
    }
  }
];

function addNode(svg, cx, cy, label, color, rx, ry) {
  rx = rx || 36; ry = ry || 16;
  var rect = document.createElementNS('http://www.w3.org/2000/svg','rect');
  rect.setAttribute('x', cx - rx); rect.setAttribute('y', cy - ry);
  rect.setAttribute('width', rx*2); rect.setAttribute('height', ry*2);
  rect.setAttribute('rx', 6); rect.setAttribute('fill', '#141b2d');
  rect.setAttribute('stroke', color); rect.setAttribute('stroke-width', '1.5');
  svg.appendChild(rect);
  var lines = label.split('\n');
  lines.forEach(function(line, i) {
    var text = document.createElementNS('http://www.w3.org/2000/svg','text');
    text.setAttribute('x', cx); text.setAttribute('y', cy + (i - (lines.length-1)/2) * 13 + 4);
    text.setAttribute('text-anchor','middle'); text.setAttribute('fill','#e0e0e0');
    text.setAttribute('font-size','10'); text.setAttribute('font-family','monospace');
    text.textContent = line;
    svg.appendChild(text);
  });
}
function addEdge(svg, x1, y1, x2, y2, color) {
  var line = document.createElementNS('http://www.w3.org/2000/svg','line');
  line.setAttribute('x1',x1);line.setAttribute('y1',y1);line.setAttribute('x2',x2);line.setAttribute('y2',y2);
  line.setAttribute('stroke',color);line.setAttribute('stroke-width','1.2');
  line.setAttribute('marker-end','url(#arr)');
  svg.appendChild(line);
  ensureArrow(svg);
}
function addEdgeDashed(svg, x1, y1, x2, y2, color) {
  var line = document.createElementNS('http://www.w3.org/2000/svg','line');
  line.setAttribute('x1',x1);line.setAttribute('y1',y1);line.setAttribute('x2',x2);line.setAttribute('y2',y2);
  line.setAttribute('stroke',color);line.setAttribute('stroke-width','1.2');
  line.setAttribute('stroke-dasharray','4 3');
  svg.appendChild(line);
}
function ensureArrow(svg) {
  if (svg.querySelector('#arr')) return;
  var defs = document.createElementNS('http://www.w3.org/2000/svg','defs');
  defs.innerHTML = '<marker id="arr" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path d="M0,0 L0,6 L6,3 z" fill="#4a5a6a"/></marker>';
  svg.insertBefore(defs, svg.firstChild);
}

function masSelect(idx) {
  document.querySelectorAll('.mas-tab').forEach(function(t){t.classList.remove('active');});
  document.querySelector('[data-model="'+idx+'"]').classList.add('active');
  var m = MAS_MODELS[idx];
  document.getElementById('masName').textContent = m.name;
  document.getElementById('masDesc').textContent = m.desc;
  document.getElementById('masUsecase').textContent = m.usecase;
  var fills = document.querySelectorAll('.mas-prop-fill');
  var labels = ['Autonomy','Fault tolerance','Scalability','Complexity'];
  var propContainer = document.getElementById('masProps');
  propContainer.innerHTML = '';
  labels.forEach(function(lbl, i) {
    var div = document.createElement('div'); div.className = 'mas-prop';
    div.innerHTML = '<span class="mas-prop-label">'+lbl+'</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:'+m.props[i]+'%"></div></div>';
    propContainer.appendChild(div);
  });
  var svg = document.getElementById('masSvg');
  m.draw(svg);
}
document.addEventListener('DOMContentLoaded', function(){ masSelect(0); });
</script> <hr/> <h2 id="the-five-collaboration-patterns">The Five Collaboration Patterns</h2> <p>Within multi-agent systems, agents interact through five fundamental patterns:</p> <div class="mas-patterns-grid"> <div class="mas-pattern-card"> <div class="mas-pattern-icon">→</div> <h4>Sequential Handoff</h4> <p>Agent A completes its task and passes its output directly to Agent B. Output of A becomes input to B. Clean pipeline with clear dependencies.</p> <span class="mas-pattern-ex">Research → Write → Edit → Publish</span> </div> <div class="mas-pattern-card"> <div class="mas-pattern-icon">⇉</div> <h4>Parallel Workstreams</h4> <p>Multiple agents work on independent sub-tasks simultaneously. Results are merged by a synthesizer. Reduces total latency for independent work.</p> <span class="mas-pattern-ex">News + Weather + Stocks → Report</span> </div> <div class="mas-pattern-card"> <div class="mas-pattern-icon">⇄</div> <h4>Debate &amp; Consensus</h4> <p>Agents with different perspectives evaluate options and discuss. The group reaches a more informed decision than any single agent could.</p> <span class="mas-pattern-ex">Pro + Con agents → Moderator → Verdict</span> </div> <div class="mas-pattern-card"> <div class="mas-pattern-icon">↕</div> <h4>Hierarchical Delegation</h4> <p>A manager agent dynamically assigns sub-tasks to worker agents based on their tool access. Workers report back, manager synthesizes.</p> <span class="mas-pattern-ex">Coordinator → Specialist A / B / C → Result</span> </div> <div class="mas-pattern-card"> <div class="mas-pattern-icon">✓✗</div> <h4>Critic-Reviewer</h4> <p>A creator agent produces output. A critic agent evaluates it for quality, compliance, or correctness. Creator revises based on critique.</p> <span class="mas-pattern-ex">Generator → Critic → Revised Output</span> </div> </div> <style>.mas-patterns-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:.85rem;margin:1.5rem 0}.mas-pattern-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.mas-pattern-icon{font-size:1.1rem;color:#2698ba;font-weight:700}.mas-pattern-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.mas-pattern-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.mas-pattern-ex{font-size:.68rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="watch-a-two-agent-team-work">Watch a Two-Agent Team Work</h2> <p>A Researcher and a Writer collaborate on a blog post. Click <strong>Run Team</strong> to watch the sequential handoff:</p> <div class="mas-team-wrapper"> <div class="mas-team-header"> <span class="mas-team-title">MULTI-AGENT TEAM DEMO — Research + Write Pipeline</span> <button class="mas-team-btn" id="masTeamRunBtn">▶ Run Team</button> </div> <div class="mas-team-body"> <div class="mas-team-goal"> <span class="mas-tg-label">SHARED GOAL</span> <span class="mas-tg-text">Write a blog post on the top 3 AI trends in 2025</span> </div> <div class="mas-agents-row"> <div class="mas-agent-panel" id="masAgentA"> <div class="mas-agent-header"> <span class="mas-agent-icon">🔍</span> <div> <div class="mas-agent-name">Research Agent</div> <div class="mas-agent-role">Senior Research Analyst</div> </div> <span class="mas-agent-badge" id="masAgentABadge">Idle</span> </div> <div class="mas-agent-track"><div class="mas-agent-fill cyan" id="masAgentAFill"></div></div> <div class="mas-agent-output" id="masAgentAOut" style="display:none"> <strong>Research findings:</strong><br/> 1. <strong>Multimodal AI</strong> — models handling text, image, audio, video simultaneously (GPT-4o, Gemini 1.5).<br/> 2. <strong>Agentic systems</strong> — AI moving from Q&amp;A to autonomous multi-step task execution.<br/> 3. <strong>On-device inference</strong> — compact models running locally on phones and laptops (Phi-3, Gemma). </div> </div> <div class="mas-handoff-arrow" id="masHandoff">→</div> <div class="mas-agent-panel" id="masAgentB" style="opacity:0.35"> <div class="mas-agent-header"> <span class="mas-agent-icon">✍</span> <div> <div class="mas-agent-name">Writer Agent</div> <div class="mas-agent-role">Technical Content Writer</div> </div> <span class="mas-agent-badge" id="masAgentBBadge">Waiting</span> </div> <div class="mas-agent-track"><div class="mas-agent-fill purple" id="masAgentBFill"></div></div> <div class="mas-agent-output" id="masAgentBOut" style="display:none"> <strong>Blog post draft:</strong><br/><br/> <em>The AI landscape in 2025 is defined by three transformative forces. First, multimodal models now process text, images, and audio as a unified stream — eliminating the boundary between media types. Second, agentic AI has crossed from demo to deployment: systems now autonomously plan, use tools, and course-correct across multi-step workflows. Third, on-device inference is making capable AI private and offline-ready, removing the cloud as a prerequisite for intelligence.</em> </div> </div> </div> <div class="mas-team-result" id="masTeamResult" style="display:none"> <span class="mas-result-icon">✓</span> Two specialized agents. One coherent output. <strong>Sequential handoff complete.</strong> </div> </div> </div> <style>.mas-team-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.mas-team-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.mas-team-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.mas-team-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.mas-team-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.mas-team-body{padding:1rem 1.1rem;display:flex;flex-direction:column;gap:.85rem}.mas-team-goal{border:1px solid var(--global-divider-color);border-radius:6px;padding:.6rem .9rem;display:flex;align-items:center;gap:.75rem;background:rgba(128,128,128,0.04)}.mas-tg-label{font-size:.6rem;font-weight:700;letter-spacing:.1em;color:#2698ba;flex-shrink:0}.mas-tg-text{font-size:.82rem;color:var(--global-text-color);font-family:monospace}.mas-agents-row{display:flex;align-items:center;gap:.75rem;flex-wrap:wrap}.mas-agent-panel{flex:1;min-width:220px;border:1.5px solid var(--global-divider-color);border-radius:8px;padding:.85rem;background:rgba(128,128,128,0.03);display:flex;flex-direction:column;gap:.5rem;transition:border-color .3s,opacity .4s}.mas-agent-panel.active{border-color:#2698ba}.mas-agent-panel.done{border-color:#4fc97e;opacity:1!important}.mas-agent-header{display:flex;align-items:center;gap:.6rem}.mas-agent-icon{font-size:1.1rem}.mas-agent-name{font-size:.82rem;font-weight:700;color:var(--global-text-color)}.mas-agent-role{font-size:.65rem;color:var(--global-text-color-light);font-family:monospace}.mas-agent-badge{font-size:.6rem;font-family:monospace;padding:.1em .4em;border-radius:3px;border:1px solid var(--global-divider-color);color:var(--global-text-color-light);margin-left:auto;transition:all .3s}.mas-agent-badge.running{color:#2698ba;border-color:#2698ba;background:rgba(38,152,186,0.1)}.mas-agent-badge.done{color:#4fc97e;border-color:#4fc97e;background:rgba(79,201,126,0.1)}.mas-agent-track{height:5px;background:rgba(128,128,128,0.12);border-radius:3px;overflow:hidden}.mas-agent-fill{height:100%;width:0;border-radius:3px;transition:width .05s linear}.mas-agent-fill.cyan{background:#2698ba}.mas-agent-fill.purple{background:#c97af2}.mas-agent-output{font-size:.77rem;color:var(--global-text-color-light);line-height:1.6;border-top:1px solid var(--global-divider-color);padding-top:.5rem;animation:masOutIn .3s ease}@keyframes masOutIn{from{opacity:0}to{opacity:1}}.mas-handoff-arrow{font-size:1.4rem;color:var(--global-text-color-light);flex-shrink:0;transition:color .4s}.mas-handoff-arrow.active{color:#2698ba}.mas-team-result{display:flex;align-items:center;gap:.6rem;padding:.6rem .85rem;background:rgba(79,201,126,0.07);border:1px solid rgba(79,201,126,0.2);border-radius:6px;font-size:.8rem;color:var(--global-text-color);animation:masOutIn .3s ease}.mas-result-icon{color:#4fc97e;font-weight:700}</style> <script>
document.addEventListener('DOMContentLoaded', function(){
  var btn = document.getElementById('masTeamRunBtn');
  if (!btn) return;
  var running = false;

  btn.addEventListener('click', async function(){
    if (running) return;
    running = true;
    btn.textContent = '⏳ Running…';
    btn.disabled = true;

    // Reset
    ['masAgentA','masAgentB'].forEach(function(id){
      var p = document.getElementById(id);
      p.classList.remove('active','done');
      p.style.opacity = id === 'masAgentB' ? '0.35' : '1';
    });
    document.getElementById('masAgentAFill').style.width = '0%';
    document.getElementById('masAgentBFill').style.width = '0%';
    document.getElementById('masAgentAOut').style.display = 'none';
    document.getElementById('masAgentBOut').style.display = 'none';
    document.getElementById('masTeamResult').style.display = 'none';
    document.getElementById('masHandoff').classList.remove('active');
    ['masAgentABadge','masAgentBBadge'].forEach(function(id,i){
      var el = document.getElementById(id);
      el.textContent = i===0 ? 'Idle' : 'Waiting';
      el.className = 'mas-agent-badge';
    });

    await new Promise(function(r){setTimeout(r,300)});

    // Agent A: Research
    var pA = document.getElementById('masAgentA');
    var badgeA = document.getElementById('masAgentABadge');
    var fillA = document.getElementById('masAgentAFill');
    pA.classList.add('active');
    badgeA.textContent = 'Researching…'; badgeA.className = 'mas-agent-badge running';

    var startA = null;
    await new Promise(function(resolve){
      function animA(ts){ if(!startA) startA=ts; var pct=Math.min((ts-startA)/1600*100,100); fillA.style.width=pct+'%'; if(pct<100) requestAnimationFrame(animA); else resolve(); }
      requestAnimationFrame(animA);
    });
    pA.classList.remove('active'); pA.classList.add('done');
    badgeA.textContent='Done ✓'; badgeA.className='mas-agent-badge done';
    document.getElementById('masAgentAOut').style.display='block';

    await new Promise(function(r){setTimeout(r,400)});

    // Handoff arrow
    document.getElementById('masHandoff').classList.add('active');
    await new Promise(function(r){setTimeout(r,500)});

    // Agent B: Write
    var pB = document.getElementById('masAgentB');
    var badgeB = document.getElementById('masAgentBBadge');
    var fillB = document.getElementById('masAgentBFill');
    pB.style.opacity = '1';
    pB.classList.add('active');
    badgeB.textContent='Writing…'; badgeB.className='mas-agent-badge running';

    var startB = null;
    await new Promise(function(resolve){
      function animB(ts){ if(!startB) startB=ts; var pct=Math.min((ts-startB)/1800*100,100); fillB.style.width=pct+'%'; if(pct<100) requestAnimationFrame(animB); else resolve(); }
      requestAnimationFrame(animB);
    });
    pB.classList.remove('active'); pB.classList.add('done');
    badgeB.textContent='Done ✓'; badgeB.className='mas-agent-badge done';
    document.getElementById('masAgentBOut').style.display='block';
    document.getElementById('masTeamResult').style.display='flex';

    running = false;
    btn.textContent = '↺ Replay';
    btn.disabled = false;
  });
});
</script> <hr/> <h2 id="use-cases-across-domains">Use Cases Across Domains</h2> <div class="mas-usecases-grid"> <div class="mas-uc-card"><span class="mas-uc-num">01</span><h4>Complex Research</h4><p>Searcher agent finds sources, summarizer agent digests them, trend-spotter identifies patterns, synthesizer writes the final report.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">02</span><h4>Software Development</h4><p>Requirements analyst, code generator, test writer, and documentation agent collaborate — each handing off to the next in sequence.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">03</span><h4>Financial Analysis</h4><p>Agents fetch stock data, analyze news sentiment, run technical analysis, and generate investment recommendations in parallel.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">04</span><h4>Customer Support</h4><p>Front-line agent handles general queries. Specialist agents (billing, technical, escalation) are activated only when needed.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">05</span><h4>Supply Chain</h4><p>Agents representing suppliers, manufacturers, and distributors collaborate to optimize inventory and logistics in real time.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">06</span><h4>Network Remediation</h4><p>Multiple triage agents work concurrently to pinpoint failures. Each integrates with existing ML tooling while contributing to a coordinated remediation plan.</p></div> </div> <style>.mas-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(185px,1fr));gap:.85rem;margin:1.5rem 0}.mas-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.mas-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#2698ba}.mas-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.mas-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}</style> <hr/> <h2 id="crewai-researcher--writer-team">CrewAI: Researcher + Writer Team</h2> <p>CrewAI is designed for exactly this — defining a team of agents with distinct roles, assigning tasks, and orchestrating collaboration.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">crewai</span> <span class="kn">import</span> <span class="n">Agent</span><span class="p">,</span> <span class="n">Task</span><span class="p">,</span> <span class="n">Crew</span><span class="p">,</span> <span class="n">Process</span>
<span class="kn">from</span> <span class="n">langchain_google_genai</span> <span class="kn">import</span> <span class="n">ChatGoogleGenerativeAI</span>

<span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatGoogleGenerativeAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="defining-the-agents">Defining the Agents</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">researcher</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">role</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Senior Research Analyst</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">goal</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Find and summarize the latest trends in AI.</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">backstory</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">You are an experienced research analyst with a knack for identifying key trends and synthesizing information.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">verbose</span>          <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
    <span class="n">allow_delegation</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">writer</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">role</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Technical Content Writer</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">goal</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Write a clear and engaging blog post based on research findings.</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">backstory</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">You are a skilled writer who can translate complex technical topics into accessible content.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">verbose</span>          <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
    <span class="n">allow_delegation</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Two agents, two roles, zero overlap.</strong> The researcher knows nothing about writing; the writer knows nothing about searching. This clean separation means each agent’s system prompt is focused — no role confusion, no competing instructions.</p> <p><strong><code class="language-plaintext highlighter-rouge">allow_delegation=False</code></strong> — prevents each specialist from spawning sub-agents on their own. In a controlled pipeline, you want the delegation logic to live in the Crew definition, not in individual agents.</p> </blockquote> <h3 id="defining-the-tasks-with-dependency">Defining the Tasks (with dependency)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">research_task</span> <span class="o">=</span> <span class="nc">Task</span><span class="p">(</span>
    <span class="n">description</span>      <span class="o">=</span> <span class="sh">"</span><span class="s">Research the top 3 emerging AI trends in 2024-2025. Focus on practical applications and impact.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">expected_output</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">A detailed summary of the top 3 AI trends with key points and sources.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">agent</span>            <span class="o">=</span> <span class="n">researcher</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">writing_task</span> <span class="o">=</span> <span class="nc">Task</span><span class="p">(</span>
    <span class="n">description</span>     <span class="o">=</span> <span class="sh">"</span><span class="s">Write a 500-word blog post based on the research findings. Engaging, accessible to a general audience.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">expected_output</span> <span class="o">=</span> <span class="sh">"</span><span class="s">A complete 500-word blog post about the latest AI trends.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">agent</span>           <span class="o">=</span> <span class="n">writer</span><span class="p">,</span>
    <span class="n">context</span>         <span class="o">=</span> <span class="p">[</span><span class="n">research_task</span><span class="p">],</span>   <span class="c1"># ← explicit dependency
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">context=[research_task]</code></strong> is the sequential handoff mechanism. It tells CrewAI: “the writing task cannot begin until research_task is complete, and the research output should be available to the writer as context.”</p> <p>Without this, the writer would run without access to the researcher’s findings — generating generic content rather than building on real research.</p> </blockquote> <h3 id="assembling-the-crew">Assembling the Crew</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">blog_creation_crew</span> <span class="o">=</span> <span class="nc">Crew</span><span class="p">(</span>
    <span class="n">agents</span>  <span class="o">=</span> <span class="p">[</span><span class="n">researcher</span><span class="p">,</span> <span class="n">writer</span><span class="p">],</span>
    <span class="n">tasks</span>   <span class="o">=</span> <span class="p">[</span><span class="n">research_task</span><span class="p">,</span> <span class="n">writing_task</span><span class="p">],</span>
    <span class="n">process</span> <span class="o">=</span> <span class="n">Process</span><span class="p">.</span><span class="n">sequential</span><span class="p">,</span>
    <span class="n">llm</span>     <span class="o">=</span> <span class="n">llm</span><span class="p">,</span>
    <span class="n">verbose</span> <span class="o">=</span> <span class="mi">2</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">result</span> <span class="o">=</span> <span class="n">blog_creation_crew</span><span class="p">.</span><span class="nf">kickoff</span><span class="p">()</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">Process.sequential</code></strong> ensures tasks run in the order they’re listed — researcher first, writer second. The <code class="language-plaintext highlighter-rouge">context</code> dependency enforces this even if the process allows parallelism.</p> <p><strong><code class="language-plaintext highlighter-rouge">llm</code> at the Crew level</strong> sets a default model for all agents. Individual agents can still override with their own <code class="language-plaintext highlighter-rouge">llm=</code> parameter if specialization is needed — e.g., a more powerful model for the researcher, a faster one for the writer.</p> </blockquote> <h3 id="crewai-data-flow">CrewAI Data Flow</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">CREWAI DATA FLOW — research output becomes writer input via context=[task]</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="flex-direction:row;align-items:center;gap:0.75rem;padding:1.1rem 1.25rem;flex-wrap:nowrap;"> <div class="ns-node ns-node-cyan" style="flex-shrink:0;max-width:180px;"><div class="ns-node-title">Topic</div><div class="ns-node-sub">"Top 3 AI trends 2025"</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-purple" style="flex-shrink:0;max-width:200px;"><div class="ns-node-title">Researcher Agent</div><div class="ns-node-sub">Senior Research Analyst · finds sources · summarizes trends</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-amber" style="flex-shrink:0;max-width:200px;"><div class="ns-node-title">Crew Context</div><div class="ns-node-sub">research output stored · context=[research_task] passes it automatically</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-cyan" style="flex-shrink:0;max-width:200px;"><div class="ns-node-title">Writer Agent</div><div class="ns-node-sub">Technical Content Writer · sees research output · writes blog post</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-green" style="flex-shrink:0;max-width:180px;"><div class="ns-node-title">500-word Blog Post</div></div> </div> </div> <hr/> <h2 id="google-adk-four-orchestration-patterns">Google ADK: Four Orchestration Patterns</h2> <p>The ADK provides explicit primitives for each collaboration structure. Here are the four most important.</p> <h3 id="1-hierarchical-parent-child-agents">1. Hierarchical: Parent-Child Agents</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LlmAgent</span><span class="p">,</span> <span class="n">BaseAgent</span>
<span class="kn">from</span> <span class="n">google.adk.agents.invocation_context</span> <span class="kn">import</span> <span class="n">InvocationContext</span>
<span class="kn">from</span> <span class="n">google.adk.events</span> <span class="kn">import</span> <span class="n">Event</span>
<span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">AsyncGenerator</span>

<span class="k">class</span> <span class="nc">TaskExecutor</span><span class="p">(</span><span class="n">BaseAgent</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">A specialized agent with custom, non-LLM behavior.</span><span class="sh">"""</span>
    <span class="n">name</span><span class="p">:</span>        <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">TaskExecutor</span><span class="sh">"</span>
    <span class="n">description</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Executes a predefined task.</span><span class="sh">"</span>

    <span class="k">async</span> <span class="k">def</span> <span class="nf">_run_async_impl</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> <span class="n">context</span><span class="p">:</span> <span class="n">InvocationContext</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">AsyncGenerator</span><span class="p">[</span><span class="n">Event</span><span class="p">,</span> <span class="bp">None</span><span class="p">]:</span>
        <span class="k">yield</span> <span class="nc">Event</span><span class="p">(</span><span class="n">author</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">content</span><span class="o">=</span><span class="sh">"</span><span class="s">Task finished successfully.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">BaseAgent</code> is the extension point</strong> for non-LLM agents. When you need deterministic logic, API calls, or custom execution — instead of LLM reasoning — subclass <code class="language-plaintext highlighter-rouge">BaseAgent</code> and implement <code class="language-plaintext highlighter-rouge">_run_async_impl</code>. It yields <code class="language-plaintext highlighter-rouge">Event</code> objects as output, integrating cleanly with the ADK event stream.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">greeter</span>     <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Greeter</span><span class="sh">"</span><span class="p">,</span> <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span> <span class="n">instruction</span><span class="o">=</span><span class="sh">"</span><span class="s">You are a friendly greeter.</span><span class="sh">"</span><span class="p">)</span>
<span class="n">task_doer</span>   <span class="o">=</span> <span class="nc">TaskExecutor</span><span class="p">()</span>

<span class="n">coordinator</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">Coordinator</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">When asked to greet, delegate to the Greeter. When asked to perform a task, delegate to the TaskExecutor.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">sub_agents</span>  <span class="o">=</span> <span class="p">[</span><span class="n">greeter</span><span class="p">,</span> <span class="n">task_doer</span><span class="p">],</span>
<span class="p">)</span>

<span class="c1"># ADK sets parent_agent automatically
</span><span class="k">assert</span> <span class="n">greeter</span><span class="p">.</span><span class="n">parent_agent</span>   <span class="o">==</span> <span class="n">coordinator</span>
<span class="k">assert</span> <span class="n">task_doer</span><span class="p">.</span><span class="n">parent_agent</span> <span class="o">==</span> <span class="n">coordinator</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">sub_agents</code></strong> establishes the parent-child relationship. The coordinator’s LLM reads both agents’ descriptions and decides which to delegate to. The <code class="language-plaintext highlighter-rouge">parent_agent</code> attribute is set automatically by the ADK framework — you don’t need to wire it manually.</p> </blockquote> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ADK HIERARCHICAL AGENTS — parent delegates to children via sub_agents</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:300px;"><div class="ns-node-title">Coordinator LlmAgent</div><div class="ns-node-sub">reads user query · decides which child to delegate to based on each agent's description</div></div> <div class="ns-arrow"></div> <div class="ns-row" style="max-width:440px;"> <div class="ns-node ns-node-purple"><div class="ns-node-title">Greeter LlmAgent</div><div class="ns-node-sub">activated when greeting is needed · LLM-powered · uses Gemini</div></div> <div class="ns-node ns-node-amber"><div class="ns-node-title">TaskExecutor BaseAgent</div><div class="ns-node-sub">custom logic · non-LLM · runs deterministic code</div></div> </div> </div> </div> <h3 id="2-loopagent-iterative-workflows">2. LoopAgent: Iterative Workflows</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LoopAgent</span><span class="p">,</span> <span class="n">LlmAgent</span><span class="p">,</span> <span class="n">BaseAgent</span>
<span class="kn">from</span> <span class="n">google.adk.events</span> <span class="kn">import</span> <span class="n">Event</span><span class="p">,</span> <span class="n">EventActions</span>

<span class="k">class</span> <span class="nc">ConditionChecker</span><span class="p">(</span><span class="n">BaseAgent</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Stops the loop when session state signals completion.</span><span class="sh">"""</span>
    <span class="n">name</span><span class="p">:</span>        <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">ConditionChecker</span><span class="sh">"</span>
    <span class="n">description</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Checks if a process is complete and signals the loop to stop.</span><span class="sh">"</span>

    <span class="k">async</span> <span class="k">def</span> <span class="nf">_run_async_impl</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">context</span><span class="p">:</span> <span class="n">InvocationContext</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">AsyncGenerator</span><span class="p">[</span><span class="n">Event</span><span class="p">,</span> <span class="bp">None</span><span class="p">]:</span>
        <span class="n">status</span>  <span class="o">=</span> <span class="n">context</span><span class="p">.</span><span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">pending</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">is_done</span> <span class="o">=</span> <span class="p">(</span><span class="n">status</span> <span class="o">==</span> <span class="sh">"</span><span class="s">completed</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">is_done</span><span class="p">:</span>
            <span class="k">yield</span> <span class="nc">Event</span><span class="p">(</span><span class="n">author</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">actions</span><span class="o">=</span><span class="nc">EventActions</span><span class="p">(</span><span class="n">escalate</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span>  <span class="c1"># stop loop
</span>        <span class="k">else</span><span class="p">:</span>
            <span class="k">yield</span> <span class="nc">Event</span><span class="p">(</span><span class="n">author</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">content</span><span class="o">=</span><span class="sh">"</span><span class="s">Condition not met, continuing loop.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">EventActions(escalate=True)</code></strong> is the loop-termination signal. When <code class="language-plaintext highlighter-rouge">ConditionChecker</code> yields this, the <code class="language-plaintext highlighter-rouge">LoopAgent</code> stops regardless of remaining iterations. Without it, the loop runs to <code class="language-plaintext highlighter-rouge">max_iterations</code>.</p> <p><strong><code class="language-plaintext highlighter-rouge">context.session.state</code></strong> is the shared key-value store accessible to all agents within a session. <code class="language-plaintext highlighter-rouge">ProcessingStep</code> writes <code class="language-plaintext highlighter-rouge">"status": "completed"</code> there; <code class="language-plaintext highlighter-rouge">ConditionChecker</code> reads it. This is how agents communicate state in ADK without direct message passing.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">process_step</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">ProcessingStep</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Perform your task. If you are the final step, set session state </span><span class="sh">'</span><span class="s">status</span><span class="sh">'</span><span class="s"> to </span><span class="sh">'</span><span class="s">completed</span><span class="sh">'</span><span class="s">.</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">poller</span> <span class="o">=</span> <span class="nc">LoopAgent</span><span class="p">(</span>
    <span class="n">name</span>           <span class="o">=</span> <span class="sh">"</span><span class="s">StatusPoller</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">max_iterations</span> <span class="o">=</span> <span class="mi">10</span><span class="p">,</span>
    <span class="n">sub_agents</span>     <span class="o">=</span> <span class="p">[</span><span class="n">process_step</span><span class="p">,</span> <span class="nc">ConditionChecker</span><span class="p">()],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">LoopAgent</code></strong> runs its <code class="language-plaintext highlighter-rouge">sub_agents</code> sequentially on each iteration. Each iteration: <code class="language-plaintext highlighter-rouge">ProcessingStep</code> executes → <code class="language-plaintext highlighter-rouge">ConditionChecker</code> evaluates → if not done, repeat. Maximum 10 iterations as a safety cap. Use this for polling, retry workflows, and iterative refinement loops.</p> </blockquote> <h3 id="3-sequentialagent-linear-pipelines">3. SequentialAgent: Linear Pipelines</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">SequentialAgent</span><span class="p">,</span> <span class="n">Agent</span>

<span class="n">step1</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Step1_Fetch</span><span class="sh">"</span><span class="p">,</span>   <span class="n">output_key</span><span class="o">=</span><span class="sh">"</span><span class="s">data</span><span class="sh">"</span><span class="p">)</span>
<span class="n">step2</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Step2_Process</span><span class="sh">"</span><span class="p">,</span> <span class="n">instruction</span><span class="o">=</span><span class="sh">"</span><span class="s">Analyze the information in state[</span><span class="sh">'</span><span class="s">data</span><span class="sh">'</span><span class="s">] and provide a summary.</span><span class="sh">"</span><span class="p">)</span>

<span class="n">pipeline</span> <span class="o">=</span> <span class="nc">SequentialAgent</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">MyPipeline</span><span class="sh">"</span><span class="p">,</span> <span class="n">sub_agents</span><span class="o">=</span><span class="p">[</span><span class="n">step1</span><span class="p">,</span> <span class="n">step2</span><span class="p">])</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">output_key="data"</code></strong> — when <code class="language-plaintext highlighter-rouge">Step1_Fetch</code> completes, its output is automatically stored in <code class="language-plaintext highlighter-rouge">session.state["data"]</code>. <code class="language-plaintext highlighter-rouge">Step2_Process</code> reads it from state via its instruction. This is the ADK equivalent of CrewAI’s <code class="language-plaintext highlighter-rouge">context=[task]</code> — explicit state-passing between sequential steps.</p> </blockquote> <h3 id="4-parallelagent-concurrent-workers">4. ParallelAgent: Concurrent Workers</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span><span class="p">,</span> <span class="n">ParallelAgent</span>

<span class="n">weather_fetcher</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">weather_fetcher</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Fetch the weather for the given location and return only the report.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">output_key</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">weather_data</span><span class="sh">"</span><span class="p">,</span>   <span class="c1"># → session.state["weather_data"]
</span><span class="p">)</span>

<span class="n">news_fetcher</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">news_fetcher</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Fetch the top news story for the given topic and return only that story.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">output_key</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">news_data</span><span class="sh">"</span><span class="p">,</span>      <span class="c1"># → session.state["news_data"]
</span><span class="p">)</span>

<span class="n">data_gatherer</span> <span class="o">=</span> <span class="nc">ParallelAgent</span><span class="p">(</span>
    <span class="n">name</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">data_gatherer</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">sub_agents</span> <span class="o">=</span> <span class="p">[</span><span class="n">weather_fetcher</span><span class="p">,</span> <span class="n">news_fetcher</span><span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">ParallelAgent</code></strong> fires both sub-agents concurrently. Each writes to a different <code class="language-plaintext highlighter-rouge">output_key</code> in session state — <code class="language-plaintext highlighter-rouge">weather_data</code> and <code class="language-plaintext highlighter-rouge">news_data</code> — with no conflict since the keys are distinct. After both complete, a downstream agent (or your application code) can read both from state.</p> </blockquote> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ADK PARALLEL AGENT — two workers fire simultaneously, results in shared state</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-purple" style="max-width:260px;"><div class="ns-node-title">ParallelAgent</div><div class="ns-node-sub">fires both sub-agents at the exact same time — doesn't wait for one before starting the other</div></div> <div class="ns-arrow"></div> <div class="ns-row" style="max-width:440px;"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">weather_fetcher</div> <div class="ns-node-sub">output_key: "weather_data" → written to Session State</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">news_fetcher</div> <div class="ns-node-sub">output_key: "news_data" → written to Session State</div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:300px;"><div class="ns-node-title">Session State</div><div class="ns-node-sub">shared key-value store · both results land here · no conflict since keys are different</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:260px;"><div class="ns-node-title">Synthesizer reads both</div><div class="ns-node-sub">downstream agent combines weather_data + news_data into final response</div></div> </div> </div> <h3 id="5-agent-as-a-tool">5. Agent as a Tool</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LlmAgent</span>
<span class="kn">from</span> <span class="n">google.adk.tools</span> <span class="kn">import</span> <span class="n">agent_tool</span>

<span class="k">def</span> <span class="nf">generate_image</span><span class="p">(</span><span class="n">prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Generates an image from a textual prompt. Returns image bytes.</span><span class="sh">"""</span>
    <span class="n">mock_bytes</span> <span class="o">=</span> <span class="sa">b</span><span class="sh">"</span><span class="s">mock_image_data_for_a_cat_wearing_a_hat</span><span class="sh">"</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">success</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">image_bytes</span><span class="sh">"</span><span class="p">:</span> <span class="n">mock_bytes</span><span class="p">,</span> <span class="sh">"</span><span class="s">mime_type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">image/png</span><span class="sh">"</span><span class="p">}</span>

<span class="n">image_generator_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">ImageGen</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Generates an image based on a detailed text prompt.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Take the user</span><span class="sh">'</span><span class="s">s request and use the generate_image tool to create the image.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span>       <span class="o">=</span> <span class="p">[</span><span class="n">generate_image</span><span class="p">],</span>
<span class="p">)</span>

<span class="n">image_tool</span> <span class="o">=</span> <span class="n">agent_tool</span><span class="p">.</span><span class="nc">AgentTool</span><span class="p">(</span>
    <span class="n">agent</span>       <span class="o">=</span> <span class="n">image_generator_agent</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Use this to generate an image. Input should be a descriptive prompt.</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">artist_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">Artist</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Invent a creative image prompt. Then use the ImageGen tool to generate it.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span>       <span class="o">=</span> <span class="p">[</span><span class="n">image_tool</span><span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">AgentTool</code></strong> wraps a sub-agent and makes it callable as a tool from a parent agent’s perspective. The <code class="language-plaintext highlighter-rouge">artist_agent</code> sees <code class="language-plaintext highlighter-rouge">image_tool</code> the same way it sees <code class="language-plaintext highlighter-rouge">generate_image</code> — as a tool it can call with arguments. The tool’s <code class="language-plaintext highlighter-rouge">description</code> is what the parent’s LLM reads to decide when to invoke it.</p> <p><strong>Why “agent as a tool” instead of <code class="language-plaintext highlighter-rouge">sub_agents</code>?</strong> With <code class="language-plaintext highlighter-rouge">sub_agents</code>, the coordinator’s LLM decides routing. With <code class="language-plaintext highlighter-rouge">AgentTool</code>, the parent agent explicitly invokes the sub-agent like a function call — passing specific arguments and receiving a return value. More precise, less autonomous.</p> </blockquote> <hr/> <h2 id="framework-comparison">Framework Comparison</h2> <table> <thead> <tr> <th>Feature</th> <th>CrewAI</th> <th>Google ADK</th> </tr> </thead> <tbody> <tr> <td><strong>Team definition</strong></td> <td><code class="language-plaintext highlighter-rouge">Agent</code> + <code class="language-plaintext highlighter-rouge">Task</code> + <code class="language-plaintext highlighter-rouge">Crew</code></td> <td><code class="language-plaintext highlighter-rouge">LlmAgent</code> / <code class="language-plaintext highlighter-rouge">BaseAgent</code> + orchestrators</td> </tr> <tr> <td><strong>Sequential flow</strong></td> <td><code class="language-plaintext highlighter-rouge">Process.sequential</code> + <code class="language-plaintext highlighter-rouge">context=[]</code></td> <td><code class="language-plaintext highlighter-rouge">SequentialAgent</code> + <code class="language-plaintext highlighter-rouge">output_key</code></td> </tr> <tr> <td><strong>Parallel flow</strong></td> <td>Multiple tasks with no <code class="language-plaintext highlighter-rouge">context</code> dependency</td> <td><code class="language-plaintext highlighter-rouge">ParallelAgent</code></td> </tr> <tr> <td><strong>Loop flow</strong></td> <td>Custom code / <code class="language-plaintext highlighter-rouge">while</code> loop</td> <td><code class="language-plaintext highlighter-rouge">LoopAgent</code> + <code class="language-plaintext highlighter-rouge">EventActions(escalate=True)</code></td> </tr> <tr> <td><strong>Hierarchical</strong></td> <td><code class="language-plaintext highlighter-rouge">allow_delegation=True</code></td> <td><code class="language-plaintext highlighter-rouge">sub_agents=[]</code> on <code class="language-plaintext highlighter-rouge">LlmAgent</code></td> </tr> <tr> <td><strong>Agent as tool</strong></td> <td>Not native (use LangChain tools)</td> <td><code class="language-plaintext highlighter-rouge">AgentTool</code> wrapper</td> </tr> <tr> <td><strong>State passing</strong></td> <td><code class="language-plaintext highlighter-rouge">context=[task]</code> in task definition</td> <td><code class="language-plaintext highlighter-rouge">output_key</code> → <code class="language-plaintext highlighter-rouge">session.state</code></td> </tr> <tr> <td><strong>Custom agent logic</strong></td> <td>Not supported natively</td> <td><code class="language-plaintext highlighter-rouge">BaseAgent._run_async_impl()</code></td> </tr> </tbody> </table> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="mas-summary-card"> <div class="mas-summary-col"> <div class="mas-summary-label">WHAT</div> <p>A system of specialized agents, each owning a distinct role and set of tools, coordinating to achieve a complex shared goal that no single agent could handle effectively alone.</p> </div> <div class="mas-summary-divider"></div> <div class="mas-summary-col"> <div class="mas-summary-label">WHY</div> <p>Specialization beats generalism at scale. Decomposing a complex task into focused sub-tasks — each assigned to an agent built for it — produces higher quality, more reliable, and more scalable outcomes.</p> </div> <div class="mas-summary-divider"></div> <div class="mas-summary-col"> <div class="mas-summary-label">RULE OF THUMB</div> <p>Use when a task requires diverse expertise, multiple distinct phases, or benefits from parallel workstreams. If a single agent's system prompt is trying to do too many jobs — split it into a team.</p> </div> </div> <style>.mas-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.mas-summary-card{flex-direction:column}}.mas-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.mas-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.mas-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.mas-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li><strong>Specialization wins.</strong> A focused agent with a clear role outperforms a generalist agent trying to do everything. Split roles aggressively.</li> <li><strong>The six interaction models</strong> — Single, Network, Supervisor, Supervisor-as-Tool, Hierarchical, Custom — each trade off autonomy vs. control vs. complexity. Most production systems use Hierarchical or Supervisor.</li> <li><strong>CrewAI wires tasks, not agents.</strong> The <code class="language-plaintext highlighter-rouge">context=[task]</code> parameter is the key to sequential handoffs — it passes one agent’s output as context to the next without any manual wiring.</li> <li><strong>ADK provides explicit primitives:</strong> <code class="language-plaintext highlighter-rouge">SequentialAgent</code> for linear pipelines, <code class="language-plaintext highlighter-rouge">ParallelAgent</code> for concurrent work, <code class="language-plaintext highlighter-rouge">LoopAgent</code> for iterative workflows, <code class="language-plaintext highlighter-rouge">sub_agents</code> for hierarchy, <code class="language-plaintext highlighter-rouge">AgentTool</code> for invoking an agent like a function.</li> <li><strong><code class="language-plaintext highlighter-rouge">output_key</code> is ADK’s state bus.</strong> Every agent in an ADK session shares <code class="language-plaintext highlighter-rouge">session.state</code>. Writing to a key with <code class="language-plaintext highlighter-rouge">output_key</code> and reading from it in another agent’s instruction is the standard inter-agent communication mechanism.</li> <li><strong><code class="language-plaintext highlighter-rouge">EventActions(escalate=True)</code></strong> is how a <code class="language-plaintext highlighter-rouge">LoopAgent</code> stops. Always provide a clear stopping condition — and <code class="language-plaintext highlighter-rouge">max_iterations</code> as a safety cap.</li> <li><strong>Failure isolation is a feature.</strong> When one agent in a multi-agent system fails, the others can continue. Compare this to a monolithic agent where one failed step can derail the entire task.</li> </ul> <hr/> <p><em>Next up — Chapter 8: Memory, where agents stop starting from scratch on every turn and begin building persistent context across conversations and sessions.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="multi-agent"/><category term="collaboration"/><summary type="html"><![CDATA[One agent hits walls. A team of specialized agents doesn't. Multi-agent collaboration lets you decompose complex problems into coordinated workstreams — each agent doing what it does best.]]></summary></entry><entry><title type="html">Chapter 6: Planning</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/planning/" rel="alternate" type="text/html" title="Chapter 6: Planning"/><published>2026-01-25T00:00:00+00:00</published><updated>2026-01-25T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/planning</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/planning/"><![CDATA[<h2 id="when-reacting-isnt-enough">When Reacting Isn’t Enough</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Decomposition:</strong> Breaking a complex problem into smaller, more manageable sub-problems. "Plan a conference" → book venue, invite speakers, arrange catering, set up registration. Each sub-task is simpler than the whole.</p> <p style="margin-top:0.5rem"><strong>Constraint:</strong> A condition that limits possible solutions. "Keep it under $15k" and "everyone must be able to attend" are constraints. A good planner finds solutions that satisfy all constraints.</p> <p style="margin-top:0.5rem"><strong>Fixed workflow vs planning agent:</strong> A fixed workflow is like a recipe — you follow the same steps every time. A planning agent is like hiring a chef — it figures out the steps itself based on what's available. Use fixed when you know the exact steps. Use planning when you don't.</p> </div> <p>Every pattern so far — <a href="/kohshh-portfolio/blog/2026/prompt-chaining/">chaining</a>, <a href="/kohshh-portfolio/blog/2026/routing/">routing</a>, <a href="/kohshh-portfolio/blog/2026/parallelization/">parallelization</a>, <a href="/kohshh-portfolio/blog/2026/reflection/">reflection</a>, <a href="/kohshh-portfolio/blog/2026/tool-use/">tool use</a> — processes one input and produces one output. The “how” is determined by the developer at design time. The agent executes what was pre-wired.</p> <p>That works when you know exactly what needs to happen. But real goals are often messier:</p> <blockquote> <p><em>“Organize a team offsite for 30 people in Q3, keep it under $15k, and make sure everyone can attend.”</em></p> </blockquote> <p>There’s no fixed sequence here. You don’t know in advance whether you’ll need to try three venues before one is available, renegotiate the catering, or push the dates by a week. The “how” needs to be <strong>discovered</strong> from the goal — not hard-coded before execution begins.</p> <p>That’s <strong>planning</strong>: the agent first generates the route, then follows it — adapting when the terrain changes.</p> <hr/> <h2 id="what-planning-is">What Planning Is</h2> <p>A planning agent receives a high-level objective and does two things before acting:</p> <ol> <li><strong>Decomposes</strong> the goal into a sequence of smaller, actionable steps</li> <li><strong>Adapts</strong> when steps fail, constraints change, or new information arrives</li> </ol> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">PLANNING PATTERN</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">Complex Goal</div> <div class="ns-node-sub">High-level objective · solution unknown in advance</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Planning Agent</div> <div class="ns-node-sub">Decomposes goal · generates step sequence</div> </div> <div class="ns-arrow"></div> <div class="ns-phase"> <div class="ns-phase-title">Execute Plan Steps</div> <div class="ns-phase-sub">Sequential or parallel · each step may call tools or sub-agents</div> <div class="ns-row"> <div class="ns-node"> <div class="ns-node-title">Step 1</div> <div class="ns-node-sub">Search / Gather</div> </div> <div class="ns-node"> <div class="ns-node-title">Step 2</div> <div class="ns-node-sub">Analyse</div> </div> <div class="ns-node"> <div class="ns-node-title">Step N</div> <div class="ns-node-sub">Draft / Act</div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">Synthesize Results</div> <div class="ns-node-sub">Merge step outputs into coherent answer</div> </div> <div class="ns-arrow"></div> <div class="ns-decision"> <div class="ns-node-title">Goal met?</div> </div> <div class="ns-arrow"></div> <div class="ns-branch-row"> <div class="ns-branch"> <span class="ns-label-red">Obstacle / Gap</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"> <div class="ns-node-title">Re-plan</div> <div class="ns-node-sub">Adapt to new constraint · revise steps</div> </div> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-dim"> <div class="ns-node-title">↑ Back to Planning Agent</div> </div> </div> <div class="ns-branch"> <span class="ns-label-green">Complete</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Final Output</div> <div class="ns-node-sub">Goal achieved · deliver result</div> </div> </div> </div> </div> </div> <p>The critical loop is the feedback arc from “Goal met?” back to the Planning Agent. When an obstacle blocks a step — a venue is booked, a search returns nothing useful, a dependency fails — a capable planner doesn’t stop. It re-evaluates and generates a revised plan.</p> <hr/> <h2 id="static-vs-dynamic-the-core-decision">Static vs Dynamic: The Core Decision</h2> <p>Before choosing planning, ask one question: <strong>does the “how” need to be discovered, or is it already known?</strong></p> <div class="plan-matrix-wrapper"> <div class="plan-matrix-header"> <span class="plan-matrix-title">WHEN TO USE PLANNING — DECISION MATRIX</span> </div> <div class="plan-matrix-grid"> <div class="plan-matrix-cell plan-cell-tl"> <div class="plan-cell-label">Complex + Known Solution</div> <div class="plan-cell-rec plan-rec-fixed">Use Fixed Workflow</div> <div class="plan-cell-ex">e.g. Invoice processing, payroll runs, CI/CD pipelines</div> <div class="plan-cell-why">Solution is understood and repeatable. Hard-coding it reduces risk and guarantees consistency.</div> </div> <div class="plan-matrix-cell plan-cell-tr plan-cell-highlight"> <div class="plan-cell-label">Complex + Unknown Solution</div> <div class="plan-cell-rec plan-rec-plan">Use Planning Agent</div> <div class="plan-cell-ex">e.g. Research report, onboarding a new hire, competitive analysis</div> <div class="plan-cell-why">The "how" must be discovered. Planning handles dependencies, unknowns, and adaptation.</div> </div> <div class="plan-matrix-cell plan-cell-bl"> <div class="plan-cell-label">Simple + Known Solution</div> <div class="plan-cell-rec plan-rec-fixed">Single Prompt / Chain</div> <div class="plan-cell-ex">e.g. Summarize this article, translate this text</div> <div class="plan-cell-why">One step, known output. No planning overhead needed.</div> </div> <div class="plan-matrix-cell plan-cell-br"> <div class="plan-cell-label">Simple + Unknown Solution</div> <div class="plan-cell-rec plan-rec-route">Routing + Fallback</div> <div class="plan-cell-ex">e.g. Customer support triage, intent classification</div> <div class="plan-cell-why">Classify and dispatch. Routing (Chapter 2) handles this cleanly.</div> </div> </div> <div class="plan-matrix-axes"> <div class="plan-axis-x">← Known &nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Solution &nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Unknown →</div> <div class="plan-axis-y">↑ Complex &nbsp;/&nbsp; Simple ↓</div> </div> </div> <style>.plan-matrix-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.plan-matrix-header{padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.plan-matrix-grid{display:grid;grid-template-columns:1fr 1fr;gap:0}@media(max-width:560px){.plan-matrix-grid{grid-template-columns:1fr}}.plan-matrix-cell{padding:1.1rem;border-right:1px solid var(--global-divider-color);border-bottom:1px solid var(--global-divider-color);display:flex;flex-direction:column;gap:.4rem}.plan-matrix-cell:nth-child(even){border-right:0}.plan-matrix-cell:nth-child(3),.plan-matrix-cell:nth-child(4){border-bottom:0}.plan-cell-highlight{background:rgba(38,152,186,0.05)}.plan-cell-label{font-size:.68rem;font-weight:700;letter-spacing:.07em;color:var(--global-text-color-light);text-transform:uppercase}.plan-cell-rec{font-size:.82rem;font-weight:700;padding:.2rem 0}.plan-rec-plan{color:#2698ba}.plan-rec-fixed{color:#4fc97e}.plan-rec-route{color:#e6a817}.plan-cell-ex{font-size:.72rem;font-family:monospace;color:var(--global-text-color-light)}.plan-cell-why{font-size:.75rem;color:var(--global-text-color-light);line-height:1.5;margin-top:.2rem}.plan-matrix-axes{display:flex;justify-content:space-between;align-items:center;padding:.5rem 1.1rem;border-top:1px solid var(--global-divider-color);background:rgba(128,128,128,0.03)}.plan-axis-x,.plan-axis-y{font-size:.65rem;font-family:monospace;color:var(--global-text-color-light);opacity:.6}</style> <hr/> <h2 id="watch-an-agent-build-a-plan">Watch an Agent Build a Plan</h2> <p>Pick a complex task. Click <strong>Generate Plan</strong> to watch the agent decompose it into executable steps.</p> <div class="plan-gen-wrapper"> <div class="plan-gen-header"> <span class="plan-gen-title">PLAN GENERATION DEMO</span> <div class="plan-gen-controls"> <button class="plan-gen-run" id="planRunBtn" disabled="">▶ Generate Plan</button> <span class="plan-gen-status" id="planStatus"></span> </div> </div> <div class="plan-gen-tasks"> <button class="plan-task-btn active" data-idx="0" onclick="planSelect(0)">📄 Research Report</button> <button class="plan-task-btn" data-idx="1" onclick="planSelect(1)">👤 Employee Onboarding</button> <button class="plan-task-btn" data-idx="2" onclick="planSelect(2)">🏢 Team Offsite</button> <button class="plan-task-btn" data-idx="3" onclick="planSelect(3)">📊 Competitive Analysis</button> </div> <div class="plan-gen-body"> <div class="plan-gen-goal" id="planGoal"> <span class="plan-gen-goal-label">GOAL</span> <span class="plan-gen-goal-text" id="planGoalText">Write a research report on AI in healthcare</span> </div> <div class="plan-steps-container" id="planSteps"></div> </div> </div> <style>.plan-gen-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.plan-gen-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.5rem}.plan-gen-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.plan-gen-controls{display:flex;align-items:center;gap:.75rem}.plan-gen-run{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.plan-gen-run:not(:disabled):hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.plan-gen-run:disabled{opacity:.4;cursor:default}.plan-gen-status{font-size:.68rem;font-family:monospace;color:var(--global-text-color-light);min-width:80px}.plan-gen-tasks{display:flex;border-bottom:1px solid var(--global-divider-color);overflow-x:auto}.plan-task-btn{flex:1;min-width:120px;padding:.55rem .5rem;font-family:monospace;font-size:.68rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s;white-space:nowrap}.plan-task-btn:last-child{border-right:0}.plan-task-btn.active{background:rgba(38,152,186,0.1);color:#2698ba;font-weight:700}.plan-task-btn:hover:not(.active){background:rgba(128,128,128,0.08)}.plan-gen-body{padding:1rem 1.1rem}.plan-gen-goal{border:1px solid var(--global-divider-color);border-radius:6px;padding:.6rem .9rem;display:flex;align-items:center;gap:.75rem;background:rgba(128,128,128,0.04);margin-bottom:1rem}.plan-gen-goal-label{font-size:.6rem;font-weight:700;letter-spacing:.1em;color:#2698ba;flex-shrink:0}.plan-gen-goal-text{font-size:.85rem;color:var(--global-text-color);font-family:monospace}.plan-steps-container{display:flex;flex-direction:column;gap:.5rem;min-height:40px}.plan-step-item{display:flex;align-items:flex-start;gap:.75rem;padding:.7rem .85rem;border:1px solid var(--global-divider-color);border-radius:7px;background:rgba(128,128,128,0.03);animation:planStepIn .35s ease}@keyframes planStepIn{from{opacity:0;transform:translateX(-8px)}to{opacity:1;transform:none}}.plan-step-num{font-family:monospace;font-size:.65rem;font-weight:700;color:#2698ba;flex-shrink:0;padding-top:.1rem;min-width:1.5rem}.plan-step-body{flex:1}.plan-step-title{font-size:.82rem;font-weight:700;color:var(--global-text-color);margin-bottom:.2rem}.plan-step-desc{font-size:.73rem;color:var(--global-text-color-light);line-height:1.45}.plan-step-badge{font-size:.6rem;font-family:monospace;padding:.1em .4em;border-radius:3px;flex-shrink:0;margin-top:.05rem}.badge-tool{background:rgba(230,168,23,0.12);color:#e6a817;border:1px solid rgba(230,168,23,0.2)}.badge-llm{background:rgba(201,122,242,0.12);color:#c97af2;border:1px solid rgba(201,122,242,0.2)}.badge-action{background:rgba(79,201,126,0.12);color:#4fc97e;border:1px solid rgba(79,201,126,0.2)}</style> <script>
var PLAN_DATA = [
  {
    goal: "Write a research report on AI in healthcare",
    steps: [
      { num:"01", title:"Define scope and research questions", desc:"Identify key themes: diagnostics, drug discovery, patient care, ethics. Set 5 core research questions.", badge:"llm", badgeTxt:"LLM" },
      { num:"02", title:"Search academic and industry sources", desc:"Query PubMed, arXiv, and industry reports. Retrieve top 15 relevant papers and studies.", badge:"tool", badgeTxt:"Search Tool" },
      { num:"03", title:"Extract key findings per theme", desc:"For each of the 5 themes, pull the most significant statistics and conclusions.", badge:"llm", badgeTxt:"LLM" },
      { num:"04", title:"Identify gaps and cross-reference", desc:"Check for conflicting data. Search for additional sources where evidence is thin.", badge:"tool", badgeTxt:"Search Tool" },
      { num:"05", title:"Structure and draft the report", desc:"Generate sections: Introduction, Findings per theme, Limitations, Conclusion.", badge:"llm", badgeTxt:"LLM" },
      { num:"06", title:"Add citations and final review", desc:"Attach inline citations to all claims. Self-review for coherence and completeness.", badge:"action", badgeTxt:"Reflect" },
    ]
  },
  {
    goal: "Onboard a new software engineer",
    steps: [
      { num:"01", title:"Create system accounts", desc:"Provision GitHub, Slack, Jira, and email accounts. Set correct team permissions.", badge:"tool", badgeTxt:"API Tool" },
      { num:"02", title:"Send welcome package", desc:"Email onboarding guide, team handbook, and first-week schedule.", badge:"action", badgeTxt:"Email Tool" },
      { num:"03", title:"Schedule intro meetings", desc:"Book 1-on-1s with manager, team leads, and cross-functional partners for week 1.", badge:"tool", badgeTxt:"Calendar API" },
      { num:"04", title:"Assign training modules", desc:"Enroll in required security, compliance, and codebase training tracks.", badge:"action", badgeTxt:"LMS API" },
      { num:"05", title:"Set up dev environment", desc:"Run setup script, verify CI/CD access, confirm local build works end-to-end.", badge:"tool", badgeTxt:"Code Tool" },
      { num:"06", title:"Create 30/60/90 day goals", desc:"Draft milestone targets in collaboration with the manager. Store in the performance system.", badge:"llm", badgeTxt:"LLM" },
    ]
  },
  {
    goal: "Plan a company team offsite for 30 people",
    steps: [
      { num:"01", title:"Survey team availability and preferences", desc:"Send availability poll for Q3 window. Collect preferences for format and activities.", badge:"action", badgeTxt:"Form Tool" },
      { num:"02", title:"Set budget and constraints", desc:"Confirm $500/person cap, dietary requirements, travel limits, and accessibility needs.", badge:"llm", badgeTxt:"LLM" },
      { num:"03", title:"Research and shortlist venues", desc:"Search venue databases for 30-person capacity within budget. Shortlist top 3.", badge:"tool", badgeTxt:"Search Tool" },
      { num:"04", title:"Check availability and book", desc:"Contact venues. If first choice is unavailable, fall back to shortlist — adapt plan.", badge:"action", badgeTxt:"Book API" },
      { num:"05", title:"Plan agenda and activities", desc:"Draft day-by-day schedule: workshops, team activities, meals, free time.", badge:"llm", badgeTxt:"LLM" },
      { num:"06", title:"Send invitations and logistics", desc:"Email itinerary, travel instructions, hotel options, and Slack event channel link.", badge:"action", badgeTxt:"Email Tool" },
    ]
  },
  {
    goal: "Conduct a competitive analysis of the market",
    steps: [
      { num:"01", title:"Identify top competitors", desc:"Define the competitive set: 5 direct + 3 adjacent competitors. Confirm with stakeholders.", badge:"llm", badgeTxt:"LLM" },
      { num:"02", title:"Gather product and feature data", desc:"Scrape product pages, changelogs, and feature lists. Extract structured comparison data.", badge:"tool", badgeTxt:"Search Tool" },
      { num:"03", title:"Collect pricing information", desc:"Retrieve public pricing tiers. Flag where pricing is hidden for follow-up.", badge:"tool", badgeTxt:"Web Tool" },
      { num:"04", title:"Analyze positioning and messaging", desc:"Summarize each competitor's value proposition, ICP, and differentiators.", badge:"llm", badgeTxt:"LLM" },
      { num:"05", title:"Review customer sentiment", desc:"Scan G2, Capterra, and Reddit reviews. Extract recurring praise and complaints.", badge:"tool", badgeTxt:"Search Tool" },
      { num:"06", title:"Synthesize into comparison matrix", desc:"Build a structured table: features, pricing, strengths, weaknesses, opportunities.", badge:"llm", badgeTxt:"LLM" },
    ]
  }
];

var planIdx = 0;
var planRunning = false;

function planSelect(idx) {
  planIdx = idx;
  document.querySelectorAll('.plan-task-btn').forEach(function(b){ b.classList.remove('active'); });
  var chosen = document.querySelector('.plan-task-btn[data-idx="'+idx+'"]');
  if (chosen) chosen.classList.add('active');
  document.getElementById('planGoalText').textContent = PLAN_DATA[idx].goal;
  document.getElementById('planSteps').innerHTML = '';
  document.getElementById('planStatus').textContent = '';
  var runBtn = document.getElementById('planRunBtn');
  runBtn.disabled = false;
  runBtn.textContent = '▶ Generate Plan';
}

document.addEventListener('DOMContentLoaded', function(){
  planSelect(0);
  var runBtn = document.getElementById('planRunBtn');
  runBtn.addEventListener('click', async function(){
    if (planRunning) return;
    planRunning = true;
    runBtn.disabled = true;
    runBtn.textContent = '⏳ Planning…';
    var statusEl = document.getElementById('planStatus');
    statusEl.textContent = 'Decomposing goal…';

    var container = document.getElementById('planSteps');
    container.innerHTML = '';
    var steps = PLAN_DATA[planIdx].steps;

    for (var i = 0; i < steps.length; i++) {
      await new Promise(function(r){ setTimeout(r, 400); });
      var s = steps[i];
      var div = document.createElement('div');
      div.className = 'plan-step-item';
      div.innerHTML =
        '<div class="plan-step-num">' + s.num + '</div>' +
        '<div class="plan-step-body">' +
          '<div class="plan-step-title">' + s.title + '</div>' +
          '<div class="plan-step-desc">' + s.desc + '</div>' +
        '</div>' +
        '<span class="plan-step-badge badge-' + s.badge + '">' + s.badgeTxt + '</span>';
      container.appendChild(div);
      statusEl.textContent = 'Step ' + (i+1) + ' / ' + steps.length;
    }

    statusEl.textContent = '✓ Plan ready';
    runBtn.textContent = '↺ Regenerate';
    runBtn.disabled = false;
    planRunning = false;
  });
});
</script> <p>Notice the badges on each step — some call <strong>tools</strong> (search, APIs), others are pure <strong>LLM</strong> reasoning steps, and some trigger <strong>actions</strong>. Planning isn’t a monolith; it’s an orchestration of all the prior patterns working in sequence.</p> <hr/> <h2 id="use-cases-where-planning-is-essential">Use Cases Where Planning Is Essential</h2> <div class="plan-usecases-grid"> <div class="plan-uc-card"> <span class="plan-uc-num">01</span> <h4>Research &amp; Synthesis</h4> <p>Multi-phase tasks: gather sources, extract findings, identify gaps, refine, write. Each phase depends on the previous one's output.</p> <span class="plan-uc-ex">Research reports, literature reviews</span> </div> <div class="plan-uc-card"> <span class="plan-uc-num">02</span> <h4>Workflow Automation</h4> <p>Business processes with ordered, interdependent steps — each must complete successfully before the next begins.</p> <span class="plan-uc-ex">Employee onboarding, invoice processing</span> </div> <div class="plan-uc-card"> <span class="plan-uc-num">03</span> <h4>Robotics &amp; Navigation</h4> <p>Physical or virtual agents must plan a path through state space — optimizing for constraints while avoiding obstacles.</p> <span class="plan-uc-ex">Autonomous vehicles, warehouse robots</span> </div> <div class="plan-uc-card"> <span class="plan-uc-num">04</span> <h4>Competitive Intelligence</h4> <p>Gather data from multiple sources, cross-reference, identify gaps, synthesize into a structured report with citations.</p> <span class="plan-uc-ex">Market analysis, product benchmarking</span> </div> <div class="plan-uc-card"> <span class="plan-uc-num">05</span> <h4>Customer Support (Multi-step)</h4> <p>Complex issues requiring diagnosis, solution search, implementation, and verification — not a single-turn response.</p> <span class="plan-uc-ex">Technical troubleshooting, escalations</span> </div> <div class="plan-uc-card"> <span class="plan-uc-num">06</span> <h4>Content Production</h4> <p>Plan → outline → draft → research → refine → publish. Each phase is distinct and builds on the previous output.</p> <span class="plan-uc-ex">Blog posts, documentation, reports</span> </div> </div> <style>.plan-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(185px,1fr));gap:.85rem;margin:1.5rem 0}.plan-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.plan-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#c97af2}.plan-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.plan-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.plan-uc-ex{font-size:.68rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="crewai-plan-then-execute">CrewAI: Plan-then-Execute</h2> <p>CrewAI implements planning by having a single agent receive a two-part task: first produce the plan, then execute it. The key is in how the task description and <code class="language-plaintext highlighter-rouge">expected_output</code> are structured.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">crewai</span> <span class="kn">import</span> <span class="n">Agent</span><span class="p">,</span> <span class="n">Task</span><span class="p">,</span> <span class="n">Crew</span><span class="p">,</span> <span class="n">Process</span>
<span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>

<span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4-turbo</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">gpt-4-turbo</code> for planning?</strong> Planning requires multi-step reasoning, coherent goal decomposition, and the ability to maintain context across a long task. More capable models produce more reliable, logical plans. Use the strongest model your budget allows for the planning stage — the cost is justified by the quality improvement.</p> </blockquote> <h3 id="the-planning-agent">The Planning Agent</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">planner_writer_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">role</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Article Planner and Writer</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">goal</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Plan and then write a concise, engaging summary on a specified topic.</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">backstory</span>  <span class="o">=</span> <span class="p">(</span>
        <span class="sh">'</span><span class="s">You are an expert technical writer and content strategist. </span><span class="sh">'</span>
        <span class="sh">'</span><span class="s">Your strength lies in creating a clear, actionable plan before writing, </span><span class="sh">'</span>
        <span class="sh">'</span><span class="s">ensuring the final summary is both informative and easy to digest.</span><span class="sh">'</span>
    <span class="p">),</span>
    <span class="n">verbose</span>          <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
    <span class="n">allow_delegation</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
    <span class="n">llm</span>              <span class="o">=</span> <span class="n">llm</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>The backstory shapes the plan.</strong> In CrewAI, the <code class="language-plaintext highlighter-rouge">backstory</code> is injected into the system prompt. Saying “your strength lies in creating a clear, actionable plan <strong>before</strong> writing” directly nudges the model to produce a plan first rather than diving straight into content. This is prompt engineering embedded in the agent definition.</p> </blockquote> <h3 id="the-two-stage-task">The Two-Stage Task</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">topic</span> <span class="o">=</span> <span class="sh">"</span><span class="s">The importance of Reinforcement Learning in AI</span><span class="sh">"</span>

<span class="n">task</span> <span class="o">=</span> <span class="nc">Task</span><span class="p">(</span>
    <span class="n">description</span> <span class="o">=</span> <span class="p">(</span>
        <span class="sa">f</span><span class="sh">"</span><span class="s">1. Create a bullet-point plan for a summary on: </span><span class="sh">'</span><span class="si">{</span><span class="n">topic</span><span class="si">}</span><span class="sh">'</span><span class="s">.</span><span class="se">\n</span><span class="sh">"</span>
        <span class="sa">f</span><span class="sh">"</span><span class="s">2. Write the summary based on your plan, keeping it around 200 words.</span><span class="sh">"</span>
    <span class="p">),</span>
    <span class="n">expected_output</span> <span class="o">=</span> <span class="p">(</span>
        <span class="sh">"</span><span class="s">A final report with two distinct sections:</span><span class="se">\n\n</span><span class="sh">"</span>
        <span class="sh">"</span><span class="s">### Plan</span><span class="se">\n</span><span class="sh">"</span>
        <span class="sh">"</span><span class="s">- A bulleted list of the main points.</span><span class="se">\n\n</span><span class="sh">"</span>
        <span class="sh">"</span><span class="s">### Summary</span><span class="se">\n</span><span class="sh">"</span>
        <span class="sh">"</span><span class="s">- A concise, well-structured summary.</span><span class="sh">"</span>
    <span class="p">),</span>
    <span class="n">agent</span> <span class="o">=</span> <span class="n">planner_writer_agent</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>The two-part task description is the planning mechanism.</strong> Step 1 forces the agent to create a plan. Step 2 forces it to execute the plan it just created. This isn’t magic — it’s a structured prompt that makes the “plan first” behavior explicit and verifiable.</p> <p><strong><code class="language-plaintext highlighter-rouge">expected_output</code> with section headers.</strong> Specifying <code class="language-plaintext highlighter-rouge">### Plan</code> and <code class="language-plaintext highlighter-rouge">### Summary</code> as required headings makes the output machine-readable. Downstream agents, parsers, or quality checks can verify that both sections are present and non-empty.</p> </blockquote> <h3 id="execution-flow">Execution Flow</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">crew</span> <span class="o">=</span> <span class="nc">Crew</span><span class="p">(</span>
    <span class="n">agents</span>  <span class="o">=</span> <span class="p">[</span><span class="n">planner_writer_agent</span><span class="p">],</span>
    <span class="n">tasks</span>   <span class="o">=</span> <span class="p">[</span><span class="n">task</span><span class="p">],</span>
    <span class="n">process</span> <span class="o">=</span> <span class="n">Process</span><span class="p">.</span><span class="n">sequential</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">result</span> <span class="o">=</span> <span class="n">crew</span><span class="p">.</span><span class="nf">kickoff</span><span class="p">()</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">Process.sequential</code></strong>: ensures tasks run in defined order. Relevant when a multi-agent crew has a planner agent followed by an executor agent — the plan must exist before execution begins.</p> </blockquote> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">CREWAI PLAN-THEN-EXECUTE — the same agent does two steps in sequence</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="flex-direction:row;align-items:center;gap:0.75rem;padding:1.1rem 1.25rem;flex-wrap:nowrap;"> <div class="ns-node ns-node-cyan" style="flex-shrink:0;max-width:160px;"><div class="ns-node-title">Topic</div><div class="ns-node-sub">"Reinforcement Learning in AI"</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-purple" style="flex-shrink:0;max-width:200px;"><div class="ns-node-title">Planner-Writer Agent</div><div class="ns-node-sub">one agent, two-part task</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-amber" style="flex-shrink:0;max-width:180px;"><div class="ns-node-title">Step 1: Generate Plan</div><div class="ns-node-sub">bullet list of main points</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node" style="flex-shrink:0;max-width:180px;"><div class="ns-node-title">Step 2: Write Summary</div><div class="ns-node-sub">200-word blog post based on plan</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-green" style="flex-shrink:0;max-width:180px;"><div class="ns-node-title">Final Report</div><div class="ns-node-sub">### Plan + ### Summary</div></div> </div> </div> <hr/> <h2 id="google-deep-research-planning-at-scale">Google Deep Research: Planning at Scale</h2> <p>Google’s Deep Research is a production planning agent built on Gemini. It doesn’t just generate a plan and execute it once — it <strong>continuously re-plans</strong> based on what each search returns.</p> <h3 id="the-architecture">The Architecture</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">GOOGLE DEEP RESEARCH ARCHITECTURE — the iterative search-plan-analyze cycle</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:260px;"><div class="ns-node-title">User Query</div><div class="ns-node-sub">"Research the impact of AI on healthcare"</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:280px;"><div class="ns-node-title">Plan Generation</div><div class="ns-node-sub">Gemini breaks query into 5-10 research sub-questions</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:280px;"><div class="ns-node-title">User Reviews Plan</div><div class="ns-node-sub">you can edit or approve — human stays in the loop before search starts</div></div> <div class="ns-arrow"></div> <div class="ns-phase" style="max-width:440px;"> <div class="ns-phase-title">Iterative Search Loop — repeats until coverage is complete</div> <div class="ns-phase-sub">each iteration searches the web, analyzes, finds gaps, searches again</div> <div class="ns-row"> <div class="ns-node"><div class="ns-node-title">Search: sub-q 1</div></div> <div class="ns-node"><div class="ns-node-title">Search: sub-q 2</div></div> <div class="ns-node"><div class="ns-node-title">Search: sub-q N</div></div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:280px;"><div class="ns-node-title">Analyze + Find Gaps</div><div class="ns-node-sub">are there unanswered questions? → loop again. All covered? → proceed</div></div> </div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:260px;"><div class="ns-node-title">Synthesize</div><div class="ns-node-sub">combines all search results into logical sections</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:280px;"><div class="ns-node-title">Structured Report + Citations</div><div class="ns-node-sub">every claim linked to a source · fully verifiable</div></div> </div> </div> <h3 id="what-makes-it-different">What Makes It Different</h3> <div class="deep-research-pipeline"> <div class="dr-stage" id="drStage0"> <div class="dr-stage-icon">🎯</div> <div class="dr-stage-content"> <div class="dr-stage-title">Plan Generation</div> <div class="dr-stage-desc">Decomposes the query into a multi-point research plan. Presents it to the user for review and modification before any search begins.</div> <div class="dr-stage-tag">User-in-the-loop</div> </div> </div> <div class="dr-connector">↓</div> <div class="dr-stage" id="drStage1"> <div class="dr-stage-icon">🔍</div> <div class="dr-stage-content"> <div class="dr-stage-title">Iterative Search Loop</div> <div class="dr-stage-desc">Dynamically formulates search queries based on what it has already found. Doesn't just run the plan — adapts it as new information arrives. Can analyze hundreds of sources.</div> <div class="dr-stage-tag">Asynchronous — resilient to single-point failures</div> </div> </div> <div class="dr-connector">↓</div> <div class="dr-stage" id="drStage2"> <div class="dr-stage-icon">🧠</div> <div class="dr-stage-content"> <div class="dr-stage-title">Gap Analysis + Re-planning</div> <div class="dr-stage-desc">After each search round, evaluates coverage gaps and corroborates conflicting data points. Triggers new searches to fill holes before moving to synthesis.</div> <div class="dr-stage-tag">Reflection (Chapter 4) + Planning combined</div> </div> </div> <div class="dr-connector">↓</div> <div class="dr-stage dr-stage-final" id="drStage3"> <div class="dr-stage-icon">📋</div> <div class="dr-stage-content"> <div class="dr-stage-title">Structured Report</div> <div class="dr-stage-desc">Synthesizes findings into a multi-page report with logical sections, inline citations, and links to all sources. Optionally includes audio overview and interactive charts.</div> <div class="dr-stage-tag">Citations · Verifiable · Interactive</div> </div> </div> </div> <style>.deep-research-pipeline{display:flex;flex-direction:column;gap:0;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}.dr-stage{display:flex;align-items:flex-start;gap:1rem;padding:1rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.03);transition:background .2s}.dr-stage:hover{background:rgba(38,152,186,0.05)}.dr-stage-final{border-bottom:0}.dr-stage-icon{font-size:1.25rem;flex-shrink:0;padding-top:.1rem}.dr-stage-content{flex:1}.dr-stage-title{font-size:.85rem;font-weight:700;color:var(--global-text-color);margin-bottom:.3rem}.dr-stage-desc{font-size:.78rem;color:var(--global-text-color-light);line-height:1.55;margin-bottom:.35rem}.dr-stage-tag{font-size:.65rem;font-family:monospace;color:#2698ba}.dr-connector{text-align:center;padding:0;font-size:1rem;color:var(--global-text-color-light);line-height:1;border-left:1px solid var(--global-divider-color);margin-left:2.35rem;height:0;overflow:visible}</style> <h3 id="key-properties">Key Properties</h3> <table> <thead> <tr> <th>Property</th> <th>Detail</th> </tr> </thead> <tbody> <tr> <td><strong>User review step</strong></td> <td>Plan is shown to the user before execution. You can edit research questions before the agent searches.</td> </tr> <tr> <td><strong>Adaptive queries</strong></td> <td>Doesn’t run predefined searches — formulates queries dynamically based on what’s been found so far</td> </tr> <tr> <td><strong>Gap detection</strong></td> <td>After each round, identifies what’s missing and generates targeted follow-up searches</td> </tr> <tr> <td><strong>Asynchronous</strong></td> <td>Long-running; user can disengage and is notified on completion</td> </tr> <tr> <td><strong>Source transparency</strong></td> <td>Returns full citation list with direct links to all consulted sources</td> </tr> <tr> <td><strong>Private doc integration</strong></td> <td>Can combine uploaded documents with web research in a single synthesis</td> </tr> </tbody> </table> <hr/> <h2 id="openai-deep-research-api">OpenAI Deep Research API</h2> <p>The OpenAI Deep Research API gives programmatic access to the same plan → search → synthesize pipeline, with full visibility into every intermediate step.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>

<span class="n">client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">(</span><span class="n">api_key</span><span class="o">=</span><span class="sh">"</span><span class="s">YOUR_OPENAI_API_KEY</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="the-api-call">The API Call</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">system_message</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">You are a professional researcher preparing a structured report.
Focus on data-rich insights, use reliable sources, and include inline citations.</span><span class="sh">"""</span>

<span class="n">user_query</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Research the economic impact of semaglutide on global healthcare systems.</span><span class="sh">"</span>

<span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">responses</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="n">model</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">o3-deep-research-2025-06-26</span><span class="sh">"</span><span class="p">,</span>
    <span class="nb">input</span>  <span class="o">=</span> <span class="p">[</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">developer</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="p">[{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">input_text</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span> <span class="n">system_message</span><span class="p">}]},</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span>      <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="p">[{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">input_text</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span> <span class="n">user_query</span><span class="p">}]},</span>
    <span class="p">],</span>
    <span class="n">reasoning</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">summary</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">auto</span><span class="sh">"</span><span class="p">},</span>
    <span class="n">tools</span>     <span class="o">=</span> <span class="p">[{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">web_search_preview</span><span class="sh">"</span><span class="p">}],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">o3-deep-research-2025-06-26</code></strong> — the model trained specifically for long-horizon research tasks. It internally manages: query decomposition, web search invocation, result analysis, gap identification, and synthesis. The <code class="language-plaintext highlighter-rouge">o4-mini-deep-research-2025-06-26</code> variant trades quality for speed.</p> <p><strong><code class="language-plaintext highlighter-rouge">reasoning={"summary": "auto"}</code></strong> — tells the API to include summaries of the model’s internal reasoning steps in the response. This exposes the agent’s “thinking” — what it was trying to figure out before each search — making the process auditable.</p> <p><strong><code class="language-plaintext highlighter-rouge">tools=[{"type": "web_search_preview"}]</code></strong> — enables the model to call web search as a tool. Without this, it’s just a standard completion. With it, the model can issue multiple search queries and incorporate live web results.</p> </blockquote> <h3 id="accessing-the-report">Accessing the Report</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Final synthesized report
</span><span class="n">final_report</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="n">output</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">].</span><span class="n">content</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">text</span>
<span class="nf">print</span><span class="p">(</span><span class="n">final_report</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">response.output[-1]</code></strong> — the output is a list of events (reasoning steps, search calls, code executions, final response). The last item is always the final response. This event-based structure is what enables transparency into intermediate steps.</p> </blockquote> <h3 id="extracting-citations">Extracting Citations</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">annotations</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="n">output</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">].</span><span class="n">content</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">annotations</span>

<span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">citation</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">annotations</span><span class="p">):</span>
    <span class="n">cited_text</span> <span class="o">=</span> <span class="n">final_report</span><span class="p">[</span><span class="n">citation</span><span class="p">.</span><span class="n">start_index</span> <span class="p">:</span> <span class="n">citation</span><span class="p">.</span><span class="n">end_index</span><span class="p">]</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Citation </span><span class="si">{</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="si">}</span><span class="s">:</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Cited Text: </span><span class="si">{</span><span class="n">cited_text</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Title:      </span><span class="si">{</span><span class="n">citation</span><span class="p">.</span><span class="n">title</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  URL:        </span><span class="si">{</span><span class="n">citation</span><span class="p">.</span><span class="n">url</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Position:   chars </span><span class="si">{</span><span class="n">citation</span><span class="p">.</span><span class="n">start_index</span><span class="si">}</span><span class="s">–</span><span class="si">{</span><span class="n">citation</span><span class="p">.</span><span class="n">end_index</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Inline citations</strong> are the critical feature for enterprise use. Every factual claim in the report has <code class="language-plaintext highlighter-rouge">start_index</code> and <code class="language-plaintext highlighter-rouge">end_index</code> pointing to the exact text span in the report. <code class="language-plaintext highlighter-rouge">citation.url</code> links to the original source. This means every claim is verifiable — not just a confident-sounding hallucination.</p> </blockquote> <h3 id="inspecting-intermediate-steps">Inspecting Intermediate Steps</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># The agent's internal reasoning (what it was planning)
</span><span class="n">reasoning_step</span> <span class="o">=</span> <span class="nf">next</span><span class="p">(</span>
    <span class="n">item</span> <span class="k">for</span> <span class="n">item</span> <span class="ow">in</span> <span class="n">response</span><span class="p">.</span><span class="n">output</span> <span class="k">if</span> <span class="n">item</span><span class="p">.</span><span class="nb">type</span> <span class="o">==</span> <span class="sh">"</span><span class="s">reasoning</span><span class="sh">"</span>
<span class="p">)</span>
<span class="k">for</span> <span class="n">part</span> <span class="ow">in</span> <span class="n">reasoning_step</span><span class="p">.</span><span class="n">summary</span><span class="p">:</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Reasoning: </span><span class="si">{</span><span class="n">part</span><span class="p">.</span><span class="n">text</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># The exact search queries it executed
</span><span class="n">search_step</span> <span class="o">=</span> <span class="nf">next</span><span class="p">(</span>
    <span class="n">item</span> <span class="k">for</span> <span class="n">item</span> <span class="ow">in</span> <span class="n">response</span><span class="p">.</span><span class="n">output</span> <span class="k">if</span> <span class="n">item</span><span class="p">.</span><span class="nb">type</span> <span class="o">==</span> <span class="sh">"</span><span class="s">web_search_call</span><span class="sh">"</span>
<span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Query executed: </span><span class="sh">'</span><span class="si">{</span><span class="n">search_step</span><span class="p">.</span><span class="n">action</span><span class="p">[</span><span class="sh">'</span><span class="s">query</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="sh">'"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Status: </span><span class="si">{</span><span class="n">search_step</span><span class="p">.</span><span class="n">status</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Any code it ran (if code_interpreter tool was included)
</span><span class="n">code_step</span> <span class="o">=</span> <span class="nf">next</span><span class="p">(</span>
    <span class="n">item</span> <span class="k">for</span> <span class="n">item</span> <span class="ow">in</span> <span class="n">response</span><span class="p">.</span><span class="n">output</span> <span class="k">if</span> <span class="n">item</span><span class="p">.</span><span class="nb">type</span> <span class="o">==</span> <span class="sh">"</span><span class="s">code_interpreter_call</span><span class="sh">"</span>
<span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Code ran:</span><span class="se">\n</span><span class="si">{</span><span class="n">code_step</span><span class="p">.</span><span class="nb">input</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  Output: </span><span class="si">{</span><span class="n">code_step</span><span class="p">.</span><span class="n">output</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>This is the key advantage over ChatGPT Deep Research.</strong> The API exposes every intermediate step:</p> <ul> <li><strong>Reasoning steps</strong>: the model’s planning narrative — what it was trying to determine</li> <li><strong>Search calls</strong>: the exact query strings submitted to the web, and their status</li> <li><strong>Code calls</strong>: any Python it executed for data analysis or computation</li> </ul> <p>This makes debugging possible. If the report gets something wrong, you can trace exactly which search query returned bad data.</p> </blockquote> <h3 id="full-response-structure">Full Response Structure</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>response.output = [
  { type: "reasoning",        summary: [{text: "I'll start by..."}] },
  { type: "web_search_call",  action: {query: "semaglutide cost analysis"}, status: "completed" },
  { type: "web_search_call",  action: {query: "GLP-1 drugs healthcare budget 2024"}, status: "completed" },
  { type: "reasoning",        summary: [{text: "I found conflicting data on..."}] },
  { type: "web_search_call",  action: {query: "..."}, status: "completed" },
  ... (more searches as needed) ...
  { type: "message",          content: [{text: "## Economic Impact of Semaglutide\n\n..."}] }
]
</code></pre></div></div> <hr/> <h2 id="planning-systems-compared">Planning Systems Compared</h2> <div class="plan-compare-wrapper"> <div class="plan-compare-header"> <span class="plan-compare-title">PLANNING SYSTEMS — CAPABILITY COMPARISON</span> <span class="plan-compare-sub">Hover a bar for details</span> </div> <div style="overflow-x:auto;padding:0.75rem;"> <canvas id="planCompareChart" width="620" height="280"></canvas> </div> <div class="plan-compare-legend"> <div class="pcl-item"><span class="pcl-dot" style="background:#2698ba"></span>CrewAI (Plan-then-Execute)</div> <div class="pcl-item"><span class="pcl-dot" style="background:#c97af2"></span>Google Deep Research</div> <div class="pcl-item"><span class="pcl-dot" style="background:#4fc97e"></span>OpenAI Deep Research API</div> </div> <div class="plan-compare-tooltip" id="planTooltip" style="display:none"></div> </div> <style>.plan-compare-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0;position:relative}.plan-compare-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.4rem}.plan-compare-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.plan-compare-sub{font-size:.65rem;color:var(--global-text-color-light);opacity:.7}.plan-compare-legend{display:flex;gap:1.25rem;padding:.6rem 1.1rem;border-top:1px solid var(--global-divider-color);flex-wrap:wrap}.pcl-item{display:flex;align-items:center;gap:.4rem;font-size:.72rem;color:var(--global-text-color-light)}.pcl-dot{width:9px;height:9px;border-radius:2px;flex-shrink:0}.plan-compare-tooltip{position:fixed;background:var(--global-bg-color);border:1px solid var(--global-divider-color);border-radius:6px;padding:.5rem .75rem;font-size:.73rem;color:var(--global-text-color);pointer-events:none;z-index:200;max-width:240px;line-height:1.5;box-shadow:0 4px 16px rgba(0,0,0,0.35)}</style> <script>
(function(){
  var canvas = document.getElementById('planCompareChart');
  if (!canvas) return;
  var ctx = canvas.getContext('2d');
  var tooltip = document.getElementById('planTooltip');

  var dims = ['Adaptability', 'Transparency', 'Customizable', 'Speed', 'Scalability'];
  var systems = ['CrewAI', 'Google DR', 'OpenAI API'];
  var colors  = ['#2698ba', '#c97af2', '#4fc97e'];
  var scores  = [
    [4, 3, 4], // Adaptability
    [3, 3, 5], // Transparency
    [5, 2, 4], // Customizable
    [4, 3, 3], // Speed
    [3, 5, 5], // Scalability
  ];
  var descs = [
    ['Plan adapts if tasks fail; re-planning is manual.', 'Fully adaptive — re-plans mid-research based on findings.', 'Adaptive — model manages re-planning internally.'],
    ['Verbose output shows agent steps.', 'Shows plan and sources, not internal queries.', 'Full access: reasoning, queries, code, citations.'],
    ['Fully customizable: agents, tools, prompts.', 'Limited — uses Gemini + Google Search natively.', 'Customizable via system prompt + MCP tools.'],
    ['Fast — bounded by task complexity.', 'Slow — long-running async process.', 'Slow — designed for thoroughness over speed.'],
    ['Scales within a single Crew.', 'Google infrastructure scale — hundreds of sources.', 'API-grade scalability for production apps.'],
  ];

  var dpr = window.devicePixelRatio || 1;
  var W = Math.min(620, canvas.parentElement.getBoundingClientRect().width - 24);
  var H = 280;
  canvas.width = W*dpr; canvas.height = H*dpr;
  canvas.style.width = W+'px'; canvas.style.height = H+'px';
  ctx.scale(dpr, dpr);

  var ML=100, MR=20, MT=24, MB=40;
  var PW=W-ML-MR, PH=H-MT-MB;
  var nD=dims.length, nS=systems.length;
  var gW=PW/nD, bW=(gW*0.68)/nS, bGap=(gW*0.32)/(nS+1);
  var hitRects=[];

  function getTheme(){ var s=getComputedStyle(document.documentElement); return { text:s.getPropertyValue('--global-text-color').trim()||'#e0e0e0', muted:s.getPropertyValue('--global-text-color-light').trim()||'#888', div:s.getPropertyValue('--global-divider-color').trim()||'#333' }; }

  function draw(hD,hS){
    ctx.clearRect(0,0,W,H); hitRects=[];
    var th=getTheme();
    for(var v=1;v<=5;v++){
      var gy=MT+(1-(v-1)/4)*PH;
      ctx.strokeStyle=th.div; ctx.lineWidth=0.5; ctx.setLineDash([3,3]);
      ctx.beginPath(); ctx.moveTo(ML,gy); ctx.lineTo(ML+PW,gy); ctx.stroke();
      ctx.setLineDash([]);
      ctx.fillStyle=th.muted; ctx.font='10px monospace'; ctx.textAlign='right';
      ctx.fillText(v,ML-6,gy+3.5);
    }
    for(var di=0;di<nD;di++){
      var gx=ML+di*gW;
      for(var si=0;si<nS;si++){
        var bx=gx+bGap*(si+1)+bW*si;
        var sc=scores[di][si];
        var bh=(sc-1)/4*PH; var by=MT+PH-bh;
        var isH=(hD===di&&hS===si);
        var alpha=(hD===null)?1:(isH?1:0.22);
        ctx.globalAlpha=alpha; ctx.fillStyle=colors[si];
        ctx.fillRect(bx,by,bW,bh);
        ctx.globalAlpha=1;
        hitRects.push({x:bx,y:by,w:bW,h:bh,di:di,si:si,sc:sc});
      }
      ctx.fillStyle=th.muted; ctx.font='9px monospace'; ctx.textAlign='center';
      var lx=gx+gW/2;
      ctx.fillText(dims[di],lx,H-MB+14);
    }
  }
  draw(null,null);

  canvas.addEventListener('mousemove',function(e){
    var rect=canvas.getBoundingClientRect();
    var mx=e.clientX-rect.left, my=e.clientY-rect.top;
    var hit=null;
    for(var i=0;i<hitRects.length;i++){ var r=hitRects[i]; if(mx>=r.x&&mx<=r.x+r.w&&my>=r.y&&my<=r.y+r.h){hit=r;break;} }
    if(hit){
      draw(hit.di,hit.si);
      tooltip.style.display='block';
      tooltip.style.left=(e.clientX+12)+'px'; tooltip.style.top=(e.clientY-10)+'px';
      tooltip.innerHTML='<strong>'+systems[hit.si]+' — '+dims[hit.di]+'</strong><br><span style="color:var(--global-text-color-light)">'+descs[hit.di][hit.si]+'</span>';
      canvas.style.cursor='pointer';
    } else { draw(null,null); tooltip.style.display='none'; canvas.style.cursor='default'; }
  });
  canvas.addEventListener('mouseleave',function(){ draw(null,null); tooltip.style.display='none'; });
})();
</script> <table> <thead> <tr> <th> </th> <th>CrewAI Plan-then-Execute</th> <th>Google Deep Research</th> <th>OpenAI Deep Research API</th> </tr> </thead> <tbody> <tr> <td><strong>Access</strong></td> <td>Open source</td> <td>Gemini app (UI)</td> <td>REST API</td> </tr> <tr> <td><strong>Transparency</strong></td> <td>Agent steps verbose</td> <td>Plan + citations</td> <td>Full: reasoning + queries + code</td> </tr> <tr> <td><strong>Adaptability</strong></td> <td>Manual re-plan</td> <td>Fully autonomous</td> <td>Autonomous</td> </tr> <tr> <td><strong>Customization</strong></td> <td>Full control</td> <td>Minimal</td> <td>System prompt + MCP tools</td> </tr> <tr> <td><strong>Use case</strong></td> <td>Custom workflows</td> <td>Ad-hoc research</td> <td>Production apps</td> </tr> <tr> <td><strong>Output</strong></td> <td>Task result</td> <td>Structured report</td> <td>Structured report + metadata</td> </tr> </tbody> </table> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="plan-summary-card"> <div class="plan-summary-col"> <div class="plan-summary-label">WHAT</div> <p>An agent receives a high-level goal and generates a sequence of steps to achieve it — before acting. The plan is not known in advance; it's created in response to the request and adapted when obstacles arise.</p> </div> <div class="plan-summary-divider"></div> <div class="plan-summary-col"> <div class="plan-summary-label">WHY</div> <p>Complex goals have unknown "how." Planning transforms a reactive system into a strategic executor — capable of handling multi-step tasks, dependencies, and dynamic obstacles.</p> </div> <div class="plan-summary-divider"></div> <div class="plan-summary-col"> <div class="plan-summary-label">RULE OF THUMB</div> <p>Use planning when the solution to a problem needs to be discovered, not just executed. If the steps are already known and repeatable — use a fixed workflow instead.</p> </div> </div> <style>.plan-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.plan-summary-card{flex-direction:column}}.plan-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.plan-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.plan-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.plan-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#c97af2}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li><strong>Planning separates goal from method.</strong> The user defines <em>what</em>. The agent discovers <em>how</em>. This is what enables autonomous behavior on complex, open-ended tasks.</li> <li><strong>Static vs dynamic</strong> is the central decision. Known, repeatable processes → fixed workflow. Unknown, context-dependent processes → planning agent.</li> <li><strong>Plans are starting points, not scripts.</strong> A capable planning agent re-plans when obstacles appear — venue is booked, search returns nothing, a step fails. Adaptability is the feature.</li> <li><strong>CrewAI implements planning through task structure.</strong> The two-part <code class="language-plaintext highlighter-rouge">description</code> (plan, then execute) and the <code class="language-plaintext highlighter-rouge">expected_output</code> with explicit section headers force explicit plan-then-act behavior.</li> <li><strong>Google Deep Research is an iterative planner.</strong> It presents the plan for user review, then runs a search loop that continuously re-formulates queries based on what it finds — combining planning, parallelization, and reflection.</li> <li><strong>The OpenAI Deep Research API exposes every step.</strong> Unlike the UI, the API returns reasoning summaries, search queries, and code executions as inspectable objects — enabling debugging, auditing, and downstream integration.</li> <li><strong>Citations are non-negotiable for trust.</strong> Both deep research systems link every factual claim to a source. For planning agents in enterprise use, verifiability is a requirement, not a feature.</li> </ul> <hr/> <p><em>Next up — Chapter 7: Multi-Agent Systems, where individual agents become coordinated teams — each specialized, each accountable, working in parallel toward shared goals.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="planning"/><category term="deep-research"/><summary type="html"><![CDATA[Reacting to a single input is easy. Achieving a complex goal across many unknown steps is not. Planning is how agents develop foresight — decomposing a destination into a route before they move.]]></summary></entry><entry><title type="html">Chapter 5: Tool Use (Function Calling)</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/tool-use/" rel="alternate" type="text/html" title="Chapter 5: Tool Use (Function Calling)"/><published>2026-01-21T00:00:00+00:00</published><updated>2026-01-21T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/tool-use</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/tool-use/"><![CDATA[<h2 id="the-wall-every-llm-hits">The Wall Every LLM Hits</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Function calling:</strong> A specific feature of modern LLMs where instead of generating prose, the model outputs a structured request to call a specific function. The model says "call get_weather with city=London" as structured data (JSON), not free text.</p> <p style="margin-top:0.5rem"><strong>JSON (JavaScript Object Notation):</strong> A simple text format for structured data. Example: <code>{"name": "get_weather", "city": "London"}</code>. It's language-agnostic and used everywhere APIs send data. LLMs output JSON for function calls because it's machine-parseable.</p> <p style="margin-top:0.5rem"><strong>Python decorator (@):</strong> A function that wraps another function to add behavior. <code>@langchain_tool</code> before a function definition tells Python: "convert this function into a LangChain tool object." The <code>@</code> is syntactic sugar — it's equivalent to writing <code>my_func = langchain_tool(my_func)</code>.</p> <p style="margin-top:0.5rem"><strong>Type hints:</strong> Optional annotations in Python that say what type a variable should be: <code>def get_weather(city: str) -&gt; dict:</code>. The LangChain <code>@tool</code> decorator reads these hints to automatically build the tool's parameter schema.</p> <p style="margin-top:0.5rem"><strong>Schema:</strong> A formal description of what data should look like. A tool's schema describes what parameters it accepts and their types. The LLM reads this schema to know how to call the tool correctly.</p> </div> <p>Every pattern in this series — <a href="/kohshh-portfolio/blog/2026/prompt-chaining/">chaining</a>, <a href="/kohshh-portfolio/blog/2026/routing/">routing</a>, <a href="/kohshh-portfolio/blog/2026/parallelization/">parallelization</a>, <a href="/kohshh-portfolio/blog/2026/reflection/">reflection</a> — operates entirely inside the model’s head. Input goes in, text comes out. The model reasons over what it already knows.</p> <p>That’s a wall.</p> <p>The model’s knowledge is frozen at its training cutoff. It can’t check today’s stock price. It can’t query your company’s database. It can’t send an email. It can’t run your Python code. For all its sophistication, without external connections it’s an encyclopedia — impressively complete, but static.</p> <p><strong>Tool use</strong> is what breaks the wall. Instead of generating a text answer, the model generates a <em>function call</em> — a structured request to execute an external piece of code. The framework runs the function, returns the result, and the model incorporates it into its response.</p> <p>This is how an LLM becomes an <strong>agent</strong> that can sense, reason, and act.</p> <hr/> <h2 id="how-function-calling-works-the-6-steps">How Function Calling Works: The 6 Steps</h2> <div class="tool-lifecycle-wrapper"> <div class="tool-lifecycle-header"> <span class="tool-lifecycle-title">TOOL CALL LIFECYCLE</span> <button class="tool-lifecycle-btn" id="lifecycleBtn">▶ Animate</button> </div> <div class="tool-lifecycle-body"> <div class="tool-lifecycle-grid"> <div class="tl-step" id="tlStep0"> <div class="tl-step-num">01</div> <div class="tl-step-name">User Query</div> <div class="tl-step-detail">"What's the weather in London?"</div> </div> <div class="tl-arrow" id="tlArr01">→</div> <div class="tl-step" id="tlStep1"> <div class="tl-step-num">02</div> <div class="tl-step-name">LLM Decides</div> <div class="tl-step-detail">Reads query + tool definitions. Picks <code>get_weather</code>.</div> </div> <div class="tl-arrow" id="tlArr12">→</div> <div class="tl-step" id="tlStep2"> <div class="tl-step-num">03</div> <div class="tl-step-name">JSON Generated</div> <div class="tl-step-detail"><code>{"name":"get_weather","args":{"city":"London"}}</code></div> </div> <div class="tl-arrow tl-arrow-down" id="tlArrDown">↓</div> <div class="tl-step" id="tlStep5"> <div class="tl-step-num">06</div> <div class="tl-step-name">Final Response</div> <div class="tl-step-detail">"It's 15°C and cloudy in London right now."</div> </div> <div class="tl-arrow tl-arrow-rev" id="tlArr45">←</div> <div class="tl-step" id="tlStep4"> <div class="tl-step-num">05</div> <div class="tl-step-name">LLM Processes</div> <div class="tl-step-detail">Receives tool result, formulates response.</div> </div> <div class="tl-arrow tl-arrow-rev" id="tlArr34">←</div> <div class="tl-step" id="tlStep3"> <div class="tl-step-num">04</div> <div class="tl-step-name">Tool Executes</div> <div class="tl-step-detail">Framework calls actual weather API. Returns JSON result.</div> </div> </div> </div> </div> <style>.tool-lifecycle-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.tool-lifecycle-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.tool-lifecycle-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.tool-lifecycle-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.tool-lifecycle-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.tool-lifecycle-body{padding:1.25rem 1rem}.tool-lifecycle-grid{display:grid;grid-template-columns:1fr auto 1fr auto 1fr;grid-template-rows:auto auto auto;gap:.5rem .4rem;align-items:center}.tl-step{border:1.5px solid var(--global-divider-color);border-radius:8px;padding:.85rem .75rem;background:rgba(128,128,128,0.03);text-align:center;transition:border-color .3s,background .3s}.tl-step.active{border-color:#2698ba;background:rgba(38,152,186,0.08)}.tl-step.done{border-color:rgba(38,152,186,0.4)}.tl-step-num{font-family:monospace;font-size:.6rem;font-weight:700;color:var(--global-text-color-light);margin-bottom:.3rem;letter-spacing:.1em}.tl-step.active .tl-step-num{color:#2698ba}.tl-step-name{font-size:.78rem;font-weight:700;color:var(--global-text-color);margin-bottom:.3rem}.tl-step-detail{font-size:.66rem;color:var(--global-text-color-light);line-height:1.45}.tl-step-detail code{font-size:.63rem;background:rgba(38,152,186,0.1);border-radius:3px;padding:.05em .3em;color:#7dcfff;border:none!important}.tl-arrow{font-size:1.1rem;color:var(--global-text-color-light);text-align:center;transition:color .3s}.tl-arrow.active{color:#2698ba}.tl-arrow-down{grid-column:5;grid-row:1 / span 2;align-self:center;justify-self:center;writing-mode:horizontal-tb;font-size:1.4rem;padding:.2rem}.tl-arrow-rev{font-size:1.1rem}@media(max-width:600px){.tool-lifecycle-grid{grid-template-columns:1fr}.tl-arrow-down,.tl-arrow-rev{display:none}.tl-arrow{display:none}}</style> <script>
(function(){
  var btn = document.getElementById('lifecycleBtn');
  if (!btn) return;
  var steps = [0,1,2,3,4,5].map(function(i){ return document.getElementById('tlStep'+i); });
  var arrs  = ['01','12','Down','34','45'].map(function(s){ return document.getElementById('tlArr'+s); });

  function reset() {
    steps.forEach(function(s){ if(s){ s.classList.remove('active','done'); }});
    arrs.forEach(function(a){ if(a){ a.classList.remove('active'); }});
  }

  var ORDER = [0,1,2,3,4,5];
  var ARR_ORDER = [0,1,4,3,2]; // which arrow index lights up after each step

  async function run() {
    reset();
    btn.disabled = true;
    btn.textContent = '⏳ Running…';

    for (var i = 0; i < ORDER.length; i++) {
      var s = steps[ORDER[i]];
      if (s) s.classList.add('active');
      if (ARR_ORDER[i] !== undefined && arrs[ARR_ORDER[i]]) arrs[ARR_ORDER[i]].classList.add('active');
      await new Promise(function(r){ setTimeout(r, 650); });
      if (s) { s.classList.remove('active'); s.classList.add('done'); }
    }

    btn.textContent = '↺ Replay';
    btn.disabled = false;
  }

  btn.addEventListener('click', function(){ if (!btn.disabled) run(); });
})();
</script> <p>The critical insight is in <strong>step 3</strong>: the LLM does not run the tool — it <em>describes</em> which tool to run and with what arguments, as structured JSON. The framework does the actual execution. This separation keeps the model stateless and the tool execution safe and auditable.</p> <hr/> <h2 id="the-three-categories-of-tools">The Three Categories of Tools</h2> <div class="tool-categories"> <div class="tool-cat tool-cat-blue"> <div class="tool-cat-icon">🔍</div> <h4>Information Retrieval</h4> <p>Pull live data the model doesn't have. APIs, databases, search engines, knowledge bases.</p> <div class="tool-cat-examples"> <span>Weather API</span><span>Stock prices</span><span>Google Search</span><span>Company DB</span> </div> </div> <div class="tool-cat tool-cat-purple"> <div class="tool-cat-icon">⚡</div> <h4>Action Execution</h4> <p>Trigger real-world effects. Send messages, update records, control systems.</p> <div class="tool-cat-examples"> <span>Send email</span><span>Post Slack msg</span><span>Smart home</span><span>Payment API</span> </div> </div> <div class="tool-cat tool-cat-green"> <div class="tool-cat-icon">💻</div> <h4>Code Execution</h4> <p>Run code in a sandboxed interpreter. Get deterministic results for calculations, data processing.</p> <div class="tool-cat-examples"> <span>Python sandbox</span><span>SQL runner</span><span>Math eval</span><span>Data analysis</span> </div> </div> </div> <style>.tool-categories{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:.85rem;margin:1.5rem 0}.tool-cat{border-radius:8px;padding:1.1rem;border:1px solid var(--global-divider-color);display:flex;flex-direction:column;gap:.5rem}.tool-cat-blue{border-left:3px solid #2698ba}.tool-cat-purple{border-left:3px solid #c97af2}.tool-cat-green{border-left:3px solid #4fc97e}.tool-cat-icon{font-size:1.2rem}.tool-cat h4{font-size:.9rem;font-weight:700;margin:0;color:var(--global-text-color)}.tool-cat p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.tool-cat-examples{display:flex;flex-wrap:wrap;gap:.35rem;margin-top:.25rem}.tool-cat-examples span{font-size:.65rem;font-family:monospace;padding:.15em .5em;border-radius:3px;background:rgba(128,128,128,0.1);color:var(--global-text-color-light);border:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="watch-it-happen-live-function-call-simulator">Watch It Happen: Live Function Call Simulator</h2> <p>Pick a query. Watch the agent decide which tool to call, generate the JSON, get a result, and respond.</p> <div class="tool-sim-wrapper"> <div class="tool-sim-header"> <span class="tool-sim-title">FUNCTION CALL SIMULATOR</span> <button class="tool-sim-run" id="simRunBtn" disabled="">▶ Run</button> </div> <div class="tool-sim-queries"> <button class="tool-sim-q active" data-idx="0" onclick="simSelect(0)">🌤 Weather</button> <button class="tool-sim-q" data-idx="1" onclick="simSelect(1)">📈 Stock Price</button> <button class="tool-sim-q" data-idx="2" onclick="simSelect(2)">📧 Send Email</button> <button class="tool-sim-q" data-idx="3" onclick="simSelect(3)">🔢 Calculator</button> </div> <div class="tool-sim-body"> <div class="tool-sim-input" id="simInput"> <span class="sim-badge">USER QUERY</span> <span class="sim-query-text" id="simQueryText">What's the weather in Tokyo right now?</span> </div> <div class="tool-sim-steps" id="simSteps"> <div class="sim-step" id="simStep0" style="display:none"> <div class="sim-step-label">TOOL SELECTED</div> <div class="sim-step-content" id="simToolName"></div> </div> <div class="sim-step" id="simStep1" style="display:none"> <div class="sim-step-label">FUNCTION CALL — JSON</div> </div> <div class="sim-step" id="simStep2" style="display:none"> <div class="sim-step-label">TOOL RESULT</div> </div> <div class="sim-step sim-step-final" id="simStep3" style="display:none"> <div class="sim-step-label">FINAL RESPONSE</div> <div class="sim-final-text" id="simFinal"></div> </div> </div> </div> </div> <style>.tool-sim-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.tool-sim-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.tool-sim-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.tool-sim-run{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.tool-sim-run:not(:disabled):hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.tool-sim-run:disabled{opacity:.4;cursor:default}.tool-sim-queries{display:flex;border-bottom:1px solid var(--global-divider-color);overflow-x:auto}.tool-sim-q{flex:1;min-width:100px;padding:.55rem .5rem;font-family:monospace;font-size:.7rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s,color .15s;white-space:nowrap}.tool-sim-q:last-child{border-right:0}.tool-sim-q.active{background:rgba(38,152,186,0.1);color:#2698ba;font-weight:700}.tool-sim-q:hover:not(.active){background:rgba(128,128,128,0.08);color:var(--global-text-color)}.tool-sim-body{padding:1rem 1.1rem;display:flex;flex-direction:column;gap:.75rem}.tool-sim-input{border:1px solid var(--global-divider-color);border-radius:6px;padding:.6rem .9rem;display:flex;align-items:center;gap:.75rem;background:rgba(128,128,128,0.04)}.sim-badge{font-size:.6rem;font-weight:700;letter-spacing:.1em;color:#2698ba;flex-shrink:0}.sim-query-text{font-family:monospace;font-size:.82rem;color:var(--global-text-color)}.tool-sim-steps{display:flex;flex-direction:column;gap:.65rem}.sim-step{border:1px solid var(--global-divider-color);border-radius:7px;overflow:hidden;animation:simFadeIn .3s ease}@keyframes simFadeIn{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}.sim-step-final{border-color:#4fc97e}.sim-step-label{font-size:.6rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;padding:.35rem .75rem;background:rgba(128,128,128,0.06);color:var(--global-text-color-light);border-bottom:1px solid var(--global-divider-color)}.sim-step-content{padding:.6rem .75rem;font-size:.82rem;font-family:monospace;color:#c97af2;font-weight:700}.sim-json{background:transparent!important;border:none!important;border-radius:0!important;padding:.7rem .75rem!important;margin:0!important;font-size:.76rem!important;color:#cdd6f4!important;line-height:1.6!important;position:static!important}.sim-json::before{display:none!important}.sim-result{color:#4fc97e!important}.sim-final-text{padding:.7rem .75rem;font-size:.85rem;color:var(--global-text-color);line-height:1.6}</style> <script>
var SIM_DATA = [
  {
    query:    "What's the weather in Tokyo right now?",
    tool:     "get_weather",
    json:     '{\n  "name": "get_weather",\n  "arguments": {\n    "city": "Tokyo",\n    "units": "celsius"\n  }\n}',
    result:   '{\n  "temperature": 22,\n  "condition": "Partly cloudy",\n  "humidity": 68,\n  "wind_kph": 14\n}',
    response: "The current weather in Tokyo is 22°C and partly cloudy, with 68% humidity and light winds at 14 km/h."
  },
  {
    query:    "What's the current stock price of AAPL?",
    tool:     "get_stock_price",
    json:     '{\n  "name": "get_stock_price",\n  "arguments": {\n    "ticker": "AAPL",\n    "exchange": "NASDAQ"\n  }\n}',
    result:   '{\n  "ticker": "AAPL",\n  "price": 187.42,\n  "change": "+1.23%",\n  "volume": "54.2M"\n}',
    response: "Apple (AAPL) is currently trading at $187.42 on the NASDAQ, up 1.23% today with a volume of 54.2 million shares."
  },
  {
    query:    "Send an email to sarah@co.com about the 3pm meeting",
    tool:     "send_email",
    json:     '{\n  "name": "send_email",\n  "arguments": {\n    "to": "sarah@co.com",\n    "subject": "Meeting at 3pm today",\n    "body": "Hi Sarah, just a reminder about our meeting today at 3pm. See you then!"\n  }\n}',
    result:   '{\n  "status": "sent",\n  "message_id": "msg_8f2a1b3c",\n  "timestamp": "2026-04-12T09:14:02Z"\n}',
    response: "Done — email sent to sarah@co.com with subject 'Meeting at 3pm today'. Message ID: msg_8f2a1b3c."
  },
  {
    query:    "Calculate a 15% tip on a $87.50 bill",
    tool:     "calculator",
    json:     '{\n  "name": "calculator",\n  "arguments": {\n    "expression": "87.50 * 0.15",\n    "precision": 2\n  }\n}',
    result:   '{\n  "result": 13.13,\n  "expression": "87.50 * 0.15",\n  "total_with_tip": 100.63\n}',
    response: "A 15% tip on $87.50 is $13.13. Your total bill including the tip would be $100.63."
  }
];

var simIdx = 0;
var simRunning = false;

function simSelect(idx) {
  simIdx = idx;
  document.querySelectorAll('.tool-sim-q').forEach(function(b){ b.classList.remove('active'); });
  var chosen = document.querySelector('[data-idx="'+idx+'"]');
  if (chosen) chosen.classList.add('active');
  document.getElementById('simQueryText').textContent = SIM_DATA[idx].query;
  [0,1,2,3].forEach(function(i){
    var el = document.getElementById('simStep'+i);
    if (el) el.style.display = 'none';
  });
  document.getElementById('simRunBtn').disabled = false;
  document.getElementById('simRunBtn').textContent = '▶ Run';
}

function delay(ms){ return new Promise(function(r){ setTimeout(r, ms); }); }

document.addEventListener('DOMContentLoaded', function(){
  var runBtn = document.getElementById('simRunBtn');
  if (!runBtn) return;

  simSelect(0);

  runBtn.addEventListener('click', async function(){
    if (simRunning) return;
    simRunning = true;
    runBtn.disabled = true;
    runBtn.textContent = '⏳ Calling…';

    var d = SIM_DATA[simIdx];
    [0,1,2,3].forEach(function(i){
      var el = document.getElementById('simStep'+i);
      if (el) el.style.display = 'none';
    });

    await delay(400);

    // Step 0: tool selected
    document.getElementById('simToolName').textContent = d.tool + '()';
    var s0 = document.getElementById('simStep0');
    s0.style.display = 'block';
    await delay(600);

    // Step 1: JSON call
    document.getElementById('simJSON').textContent = d.json;
    var s1 = document.getElementById('simStep1');
    s1.style.display = 'block';
    await delay(700);

    // Step 2: result
    document.getElementById('simResult').textContent = d.result;
    var s2 = document.getElementById('simStep2');
    s2.style.display = 'block';
    await delay(700);

    // Step 3: final response
    document.getElementById('simFinal').textContent = d.response;
    var s3 = document.getElementById('simStep3');
    s3.style.display = 'block';

    simRunning = false;
    runBtn.textContent = '↺ Run Again';
    runBtn.disabled = false;
  });
});
</script> <p>The JSON in step 3 is the LLM’s actual output — not prose, but a structured object your code can parse. The framework reads <code class="language-plaintext highlighter-rouge">"name"</code> to know which function to call, and <code class="language-plaintext highlighter-rouge">"arguments"</code> to know what to pass it.</p> <hr/> <h2 id="six-use-cases">Six Use Cases</h2> <div class="tool-usecases-grid"> <div class="tool-uc-card"> <span class="tool-uc-num">01</span> <h4>Real-Time Data</h4> <p>Weather, stock prices, sports scores, news — anything the model's training data can't contain.</p> <span class="tool-uc-tool">Tool: external REST API</span> </div> <div class="tool-uc-card"> <span class="tool-uc-num">02</span> <h4>Database Queries</h4> <p>Query company-specific data — orders, inventory, customer records — that will never be in training data.</p> <span class="tool-uc-tool">Tool: SQL / NoSQL query</span> </div> <div class="tool-uc-card"> <span class="tool-uc-num">03</span> <h4>Calculations</h4> <p>Complex math, statistics, currency conversions — offload to deterministic code, not probabilistic generation.</p> <span class="tool-uc-tool">Tool: code interpreter</span> </div> <div class="tool-uc-card"> <span class="tool-uc-num">04</span> <h4>Sending Communications</h4> <p>Email, Slack, SMS — the agent generates the content, the tool actually sends it.</p> <span class="tool-uc-tool">Tool: messaging API</span> </div> <div class="tool-uc-card"> <span class="tool-uc-num">05</span> <h4>Code Execution</h4> <p>Run user-provided code in a sandbox, debug errors, analyze outputs — beyond what the model can simulate.</p> <span class="tool-uc-tool">Tool: sandboxed interpreter</span> </div> <div class="tool-uc-card"> <span class="tool-uc-num">06</span> <h4>System Control</h4> <p>Smart home, IoT, browser automation — the agent instructs, the tool acts in the physical or digital world.</p> <span class="tool-uc-tool">Tool: device / browser API</span> </div> </div> <style>.tool-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(185px,1fr));gap:.85rem;margin:1.5rem 0}.tool-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.tool-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#2698ba}.tool-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.tool-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.tool-uc-tool{font-size:.68rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="langchain-tool--agentexecutor">LangChain: <code class="language-plaintext highlighter-rouge">@tool</code> + <code class="language-plaintext highlighter-rouge">AgentExecutor</code></h2> <p>LangChain makes tool definition as simple as a Python decorator. The framework handles everything else: tool registration, LLM binding, execution, and response formatting.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>
<span class="kn">import</span> <span class="n">asyncio</span>
<span class="kn">from</span> <span class="n">langchain_google_genai</span> <span class="kn">import</span> <span class="n">ChatGoogleGenerativeAI</span>
<span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">ChatPromptTemplate</span>
<span class="kn">from</span> <span class="n">langchain_core.tools</span> <span class="kn">import</span> <span class="n">tool</span> <span class="k">as</span> <span class="n">langchain_tool</span>
<span class="kn">from</span> <span class="n">langchain.agents</span> <span class="kn">import</span> <span class="n">create_tool_calling_agent</span><span class="p">,</span> <span class="n">AgentExecutor</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">langchain_core.tools.tool</code>?</strong> The <code class="language-plaintext highlighter-rouge">@tool</code> decorator inspects the function and converts it into a <code class="language-plaintext highlighter-rouge">StructuredTool</code> object that contains the function name, the parameters schema (extracted from type hints), and the description (extracted from the docstring). The LLM reads all three to decide when and how to call it.</p> </blockquote> <h3 id="defining-a-tool">Defining a Tool</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@langchain_tool</span>
<span class="k">def</span> <span class="nf">search_information</span><span class="p">(</span><span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Provides factual information on a given topic.
    Use this tool to answer questions like </span><span class="sh">'</span><span class="s">capital of France</span><span class="sh">'</span><span class="s">
    or </span><span class="sh">'</span><span class="s">weather in London</span><span class="sh">'</span><span class="s">.
    </span><span class="sh">"""</span>
    <span class="n">simulated_results</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">weather in london</span><span class="sh">"</span><span class="p">:</span>   <span class="sh">"</span><span class="s">Cloudy, 15°C.</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">capital of france</span><span class="sh">"</span><span class="p">:</span>   <span class="sh">"</span><span class="s">The capital of France is Paris.</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">population of earth</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Approximately 8 billion people.</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">simulated_results</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span>
        <span class="n">query</span><span class="p">.</span><span class="nf">lower</span><span class="p">(),</span>
        <span class="sa">f</span><span class="sh">"</span><span class="s">No specific data for </span><span class="sh">'</span><span class="si">{</span><span class="n">query</span><span class="si">}</span><span class="sh">'</span><span class="s">.</span><span class="sh">"</span>
    <span class="p">)</span>

<span class="n">tools</span> <span class="o">=</span> <span class="p">[</span><span class="n">search_information</span><span class="p">]</span>
</code></pre></div></div> <blockquote> <p><strong>The docstring is critical.</strong> When the LLM receives the tool definition, it sees:</p> <ul> <li><code class="language-plaintext highlighter-rouge">name</code>: <code class="language-plaintext highlighter-rouge">"search_information"</code> — what to put in <code class="language-plaintext highlighter-rouge">"name"</code> of the JSON call</li> <li><code class="language-plaintext highlighter-rouge">description</code>: the full docstring — the LLM reads this to decide <em>when</em> to use the tool</li> <li><code class="language-plaintext highlighter-rouge">parameters</code>: <code class="language-plaintext highlighter-rouge">{"query": {"type": "string"}}</code> — extracted from type hints</li> </ul> <p>A vague docstring → wrong tool usage. A precise docstring → accurate routing. The docstring IS the tool’s interface contract with the LLM.</p> </blockquote> <h3 id="building-the-agent">Building the Agent</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatGoogleGenerativeAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>

<span class="n">agent_prompt</span> <span class="o">=</span> <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_messages</span><span class="p">([</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span>      <span class="sh">"</span><span class="s">You are a helpful assistant.</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">human</span><span class="sh">"</span><span class="p">,</span>       <span class="sh">"</span><span class="s">{input}</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">placeholder</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">{agent_scratchpad}</span><span class="sh">"</span><span class="p">),</span>   <span class="c1"># ← the agent's working memory
</span><span class="p">])</span>

<span class="n">agent</span> <span class="o">=</span> <span class="nf">create_tool_calling_agent</span><span class="p">(</span><span class="n">llm</span><span class="p">,</span> <span class="n">tools</span><span class="p">,</span> <span class="n">agent_prompt</span><span class="p">)</span>
<span class="n">agent_executor</span> <span class="o">=</span> <span class="nc">AgentExecutor</span><span class="p">(</span><span class="n">agent</span><span class="o">=</span><span class="n">agent</span><span class="p">,</span> <span class="n">tools</span><span class="o">=</span><span class="n">tools</span><span class="p">,</span> <span class="n">verbose</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>What is <code class="language-plaintext highlighter-rouge">{agent_scratchpad}</code>?</strong> This placeholder is where the agent writes its intermediate steps — the tool calls it made, the results it received, and its reasoning about what to do next. Without it, the agent has no place to “think” between steps. It’s the equivalent of scratch paper for multi-step reasoning.</p> <p><strong><code class="language-plaintext highlighter-rouge">AgentExecutor</code> is the loop.</strong> It repeatedly calls the agent until either:</p> <ol> <li>The agent produces a final answer (no more tool calls needed)</li> <li>A max iteration limit is hit</li> </ol> <p>Each iteration: agent runs → decides tool → executor calls tool → result fed back → agent runs again.</p> </blockquote> <h3 id="the-execution-loop-visualized">The Execution Loop Visualized</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">TOOL CALL LIFECYCLE</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">01 · User Query</div> <div class="ns-node-sub">"What's the weather in London?"</div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">02 · LLM Receives Query + Tool Definitions</div> <div class="ns-node-sub">Model reads available tools and their descriptions</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">03 · LLM Decides: Tool Needed</div> <div class="ns-node-sub">Selects <code>get_weather</code> based on docstring match</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">04 · JSON Call Generated</div> <div class="ns-node-sub">&#123;"name":"get_weather","args":&#123;"city":"London"&#125;&#125;</div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">05 · Framework Executes Tool</div> <div class="ns-node-sub">Calls actual API · returns structured result</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">06 · LLM Formulates Response</div> <div class="ns-node-sub">"It's 15°C and cloudy in London right now."</div> </div> </div> </div> <h3 id="running-queries">Running Queries</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">run_agent</span><span class="p">(</span><span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
    <span class="n">response</span> <span class="o">=</span> <span class="k">await</span> <span class="n">agent_executor</span><span class="p">.</span><span class="nf">ainvoke</span><span class="p">({</span><span class="sh">"</span><span class="s">input</span><span class="sh">"</span><span class="p">:</span> <span class="n">query</span><span class="p">})</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">output</span><span class="sh">"</span><span class="p">])</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="k">await</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span>
        <span class="nf">run_agent</span><span class="p">(</span><span class="sh">"</span><span class="s">What is the capital of France?</span><span class="sh">"</span><span class="p">),</span>
        <span class="nf">run_agent</span><span class="p">(</span><span class="sh">"</span><span class="s">What</span><span class="sh">'</span><span class="s">s the weather like in London?</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">)</span>

<span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">main</span><span class="p">())</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">ainvoke</code> and <code class="language-plaintext highlighter-rouge">gather</code>?</strong> <code class="language-plaintext highlighter-rouge">ainvoke</code> is the async version of <code class="language-plaintext highlighter-rouge">invoke</code>. Using <code class="language-plaintext highlighter-rouge">asyncio.gather</code> fires multiple agent queries concurrently — each agent call is I/O-bound (waiting for the LLM API), so they run in parallel without blocking each other. This is the <a href="/kohshh-portfolio/blog/2026/parallelization/">parallelization</a> pattern applied to tool-using agents.</p> </blockquote> <hr/> <h2 id="crewai-tool--role-based-agents">CrewAI: <code class="language-plaintext highlighter-rouge">@tool</code> + Role-Based Agents</h2> <p>CrewAI takes a different philosophy: agents are defined by <strong>role</strong>, <strong>goal</strong>, and <strong>backstory</strong> — almost like hiring a specialist. Tools are assigned to an agent’s persona rather than to a chain.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">crewai</span> <span class="kn">import</span> <span class="n">Agent</span><span class="p">,</span> <span class="n">Task</span><span class="p">,</span> <span class="n">Crew</span>
<span class="kn">from</span> <span class="n">crewai.tools</span> <span class="kn">import</span> <span class="n">tool</span>
</code></pre></div></div> <h3 id="defining-the-tool">Defining the Tool</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@tool</span><span class="p">(</span><span class="sh">"</span><span class="s">Stock Price Lookup Tool</span><span class="sh">"</span><span class="p">)</span>
<span class="k">def</span> <span class="nf">get_stock_price</span><span class="p">(</span><span class="n">ticker</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Fetches the latest simulated stock price for a given ticker.
    Returns the price as a float. Raises ValueError if not found.
    </span><span class="sh">"""</span>
    <span class="n">prices</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">AAPL</span><span class="sh">"</span><span class="p">:</span> <span class="mf">178.15</span><span class="p">,</span> <span class="sh">"</span><span class="s">GOOGL</span><span class="sh">"</span><span class="p">:</span> <span class="mf">1750.30</span><span class="p">,</span> <span class="sh">"</span><span class="s">MSFT</span><span class="sh">"</span><span class="p">:</span> <span class="mf">425.50</span><span class="p">}</span>
    <span class="n">price</span> <span class="o">=</span> <span class="n">prices</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">ticker</span><span class="p">.</span><span class="nf">upper</span><span class="p">())</span>
    <span class="k">if</span> <span class="n">price</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
        <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Ticker </span><span class="sh">'</span><span class="si">{</span><span class="n">ticker</span><span class="si">}</span><span class="sh">'</span><span class="s"> not found.</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">price</span>
</code></pre></div></div> <blockquote> <p><strong>Why raise <code class="language-plaintext highlighter-rouge">ValueError</code> instead of returning a string?</strong> Returning a string like <code class="language-plaintext highlighter-rouge">"Not found"</code> makes the agent think it has a valid answer — it might confidently report a price of “Not found”. Raising an exception forces the agent to handle the failure explicitly, leading to better error acknowledgment in the final response.</p> </blockquote> <h3 id="the-agent-task-and-crew">The Agent, Task, and Crew</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">financial_analyst</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">role</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Senior Financial Analyst</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">goal</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Analyze stock data and report key prices accurately.</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">backstory</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Experienced analyst adept at using data sources to find stock information.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">verbose</span>   <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
    <span class="n">tools</span>     <span class="o">=</span> <span class="p">[</span><span class="n">get_stock_price</span><span class="p">],</span>
    <span class="n">allow_delegation</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">role</code>, <code class="language-plaintext highlighter-rouge">goal</code>, <code class="language-plaintext highlighter-rouge">backstory</code>?</strong> CrewAI injects these into the agent’s system prompt. The role frames what the agent IS (“Senior Financial Analyst”). The goal tells it what it’s trying to achieve. The backstory adds professional context that influences how it reasons and writes. Together they create a coherent persona — not just a generic assistant.</p> <p><strong><code class="language-plaintext highlighter-rouge">allow_delegation=False</code></strong>: prevents the agent from spawning sub-agents. Keep it <code class="language-plaintext highlighter-rouge">False</code> for simple single-agent tasks.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">task</span> <span class="o">=</span> <span class="nc">Task</span><span class="p">(</span>
    <span class="n">description</span> <span class="o">=</span> <span class="p">(</span>
        <span class="sh">"</span><span class="s">Find the simulated stock price for Apple (ticker: AAPL). </span><span class="sh">"</span>
        <span class="sh">"</span><span class="s">Use the Stock Price Lookup Tool. </span><span class="sh">"</span>
        <span class="sh">"</span><span class="s">If the ticker is not found, report that clearly.</span><span class="sh">"</span>
    <span class="p">),</span>
    <span class="n">expected_output</span> <span class="o">=</span> <span class="sh">"</span><span class="s">A single sentence stating the AAPL price. e.g. </span><span class="sh">'</span><span class="s">The simulated stock price for AAPL is $178.15.</span><span class="sh">'"</span><span class="p">,</span>
    <span class="n">agent</span> <span class="o">=</span> <span class="n">financial_analyst</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">crew</span> <span class="o">=</span> <span class="nc">Crew</span><span class="p">(</span>
    <span class="n">agents</span> <span class="o">=</span> <span class="p">[</span><span class="n">financial_analyst</span><span class="p">],</span>
    <span class="n">tasks</span>  <span class="o">=</span> <span class="p">[</span><span class="n">task</span><span class="p">],</span>
    <span class="n">verbose</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">result</span> <span class="o">=</span> <span class="n">crew</span><span class="p">.</span><span class="nf">kickoff</span><span class="p">()</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">expected_output</code></strong>: This is a key CrewAI feature. It tells the agent exactly what format the output should take. The agent evaluates its own output against this expectation — essentially a built-in reflection step.</p> <p><strong><code class="language-plaintext highlighter-rouge">crew.kickoff()</code></strong>: Starts the execution. CrewAI handles the agent loop internally, including tool invocation, result parsing, and task completion detection.</p> </blockquote> <h3 id="crewai-agent-task-crew-relationship">CrewAI Agent-Task-Crew Relationship</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">CREWAI AGENT-TASK-CREW — how the three pieces fit together</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:220px;"><div class="ns-node-title">User Request</div><div class="ns-node-sub">"What is AAPL's stock price?"</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:260px;"><div class="ns-node-title">Crew</div><div class="ns-node-sub">orchestrates agents and tasks · calls kickoff()</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:260px;"><div class="ns-node-title">Task: Find AAPL Price</div><div class="ns-node-sub">description + expected_output · assigned to the Financial Analyst agent</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:300px;"><div class="ns-node-title">Agent — Senior Financial Analyst</div><div class="ns-node-sub">role + goal + backstory define the persona · has access to get_stock_price tool</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-cyan" style="max-width:280px;"><div class="ns-node-title">get_stock_price tool</div><div class="ns-node-sub">agent calls it with ticker="AAPL" · tool returns 178.15 · agent formats the response</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:260px;"><div class="ns-node-title">Final Result</div><div class="ns-node-sub">"The simulated stock price for AAPL is $178.15"</div></div> </div> </div> <hr/> <h2 id="google-adk-built-in-tools--code-execution">Google ADK: Built-in Tools + Code Execution</h2> <p>The ADK provides <strong>pre-built tools</strong> that require zero custom code. You just import them and pass them to an agent.</p> <h3 id="google-search-zero-configuration">Google Search (Zero Configuration)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span>
<span class="kn">from</span> <span class="n">google.adk.tools</span> <span class="kn">import</span> <span class="n">google_search</span>

<span class="n">root_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">basic_search_agent</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Answers questions by searching the internet.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Answer questions by searching the web.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span>       <span class="o">=</span> <span class="p">[</span><span class="n">google_search</span><span class="p">],</span>   <span class="c1"># one line — full search capability
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">google_search</code> is a pre-built ADK tool</strong> — it connects to the Google Search API automatically, handles authentication, pagination, and result formatting. No API key configuration, no schema definition, no docstring to write. You get production-quality web search in one import.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">call_agent</span><span class="p">(</span><span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
    <span class="n">session_service</span> <span class="o">=</span> <span class="nc">InMemorySessionService</span><span class="p">()</span>
    <span class="n">session</span> <span class="o">=</span> <span class="k">await</span> <span class="n">session_service</span><span class="p">.</span><span class="nf">create_session</span><span class="p">(</span>
        <span class="n">app_name</span><span class="o">=</span><span class="n">APP_NAME</span><span class="p">,</span> <span class="n">user_id</span><span class="o">=</span><span class="n">USER_ID</span><span class="p">,</span> <span class="n">session_id</span><span class="o">=</span><span class="n">SESSION_ID</span>
    <span class="p">)</span>
    <span class="n">runner</span>  <span class="o">=</span> <span class="nc">Runner</span><span class="p">(</span><span class="n">agent</span><span class="o">=</span><span class="n">root_agent</span><span class="p">,</span> <span class="n">app_name</span><span class="o">=</span><span class="n">APP_NAME</span><span class="p">,</span> <span class="n">session_service</span><span class="o">=</span><span class="n">session_service</span><span class="p">)</span>
    <span class="n">content</span> <span class="o">=</span> <span class="n">types</span><span class="p">.</span><span class="nc">Content</span><span class="p">(</span><span class="n">role</span><span class="o">=</span><span class="sh">'</span><span class="s">user</span><span class="sh">'</span><span class="p">,</span> <span class="n">parts</span><span class="o">=</span><span class="p">[</span><span class="n">types</span><span class="p">.</span><span class="nc">Part</span><span class="p">(</span><span class="n">text</span><span class="o">=</span><span class="n">query</span><span class="p">)])</span>

    <span class="k">for</span> <span class="n">event</span> <span class="ow">in</span> <span class="n">runner</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="n">user_id</span><span class="o">=</span><span class="n">USER_ID</span><span class="p">,</span> <span class="n">session_id</span><span class="o">=</span><span class="n">SESSION_ID</span><span class="p">,</span> <span class="n">new_message</span><span class="o">=</span><span class="n">content</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">event</span><span class="p">.</span><span class="nf">is_final_response</span><span class="p">():</span>
            <span class="nf">print</span><span class="p">(</span><span class="n">event</span><span class="p">.</span><span class="n">content</span><span class="p">.</span><span class="n">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">text</span><span class="p">)</span>

<span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">call_agent</span><span class="p">(</span><span class="sh">"</span><span class="s">What</span><span class="sh">'</span><span class="s">s the latest AI news?</span><span class="sh">"</span><span class="p">))</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">InMemorySessionService</code></strong>: stores conversation state in RAM. Every <code class="language-plaintext highlighter-rouge">run()</code> call has a <code class="language-plaintext highlighter-rouge">session_id</code> — this is how the agent maintains multi-turn context. Sessions are covered in depth in Chapter 8.</p> <p><strong><code class="language-plaintext highlighter-rouge">event.is_final_response()</code></strong>: the ADK streams events as the agent works. Most events are intermediate (tool calls, reasoning steps). Only one event marks the final answer. Always filter for this before reading the output.</p> </blockquote> <h3 id="code-execution-built-in-sandboxed-interpreter">Code Execution (Built-in Sandboxed Interpreter)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LlmAgent</span>
<span class="kn">from</span> <span class="n">google.adk.code_executors</span> <span class="kn">import</span> <span class="n">BuiltInCodeExecutor</span>

<span class="n">code_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>          <span class="o">=</span> <span class="sh">"</span><span class="s">calculator_agent</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>         <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">code_executor</span> <span class="o">=</span> <span class="nc">BuiltInCodeExecutor</span><span class="p">(),</span>    <span class="c1"># sandboxed Python
</span>    <span class="n">instruction</span>   <span class="o">=</span> <span class="sh">"""</span><span class="s">You are a calculator agent.
When given a math problem, write Python code to solve it.
Return only the final numerical result as plain text.</span><span class="sh">"""</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">BuiltInCodeExecutor</code></strong>: gives the agent a sandboxed Python interpreter. The agent writes Python code, the executor runs it securely, and the result is returned. This is how the agent handles:</p> <ul> <li>Exact arithmetic (no rounding errors from the LLM)</li> <li>Data manipulation (sorting, filtering, aggregation)</li> <li>Problems that require deterministic computation</li> </ul> <p>The LLM is good at reasoning <em>about</em> code. <code class="language-plaintext highlighter-rouge">BuiltInCodeExecutor</code> makes it actually <em>run</em> code. The combination is far more powerful than either alone.</p> </blockquote> <h3 id="adk-tool-execution-flow">ADK Tool Execution Flow</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ADK CODE EXECUTION — agent writes code, sandbox runs it, agent reports the answer</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:260px;"><div class="ns-node-title">User Query</div><div class="ns-node-sub">"What is 10 factorial?"</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:240px;"><div class="ns-node-title">Runner</div><div class="ns-node-sub">manages the session · passes query to the agent</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:300px;"><div class="ns-node-title">LlmAgent (Calculator)</div><div class="ns-node-sub">reasons: "I should write Python code to calculate this precisely"</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:300px;"><div class="ns-node-title">BuiltInCodeExecutor</div><div class="ns-node-sub">receives Python code from agent · runs it in a safe sandbox · cannot harm your system</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-cyan" style="max-width:300px;"><div class="ns-node-title">Python Sandbox</div><div class="ns-node-sub">executes: import math; math.factorial(10) → returns 3628800</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:260px;"><div class="ns-node-title">Final Response</div><div class="ns-node-sub">"10! = 3,628,800" — exact answer, no guessing</div></div> </div> </div> <hr/> <h2 id="framework-comparison">Framework Comparison</h2> <div class="tool-fw-compare-wrapper"> <div class="tool-fw-compare-header"> <span class="tool-fw-compare-title">FRAMEWORK CAPABILITY COMPARISON</span> <span class="tool-fw-compare-sub">Hover a bar to see details</span> </div> <div style="overflow-x:auto;padding:0.75rem;"> <canvas id="fwCompareChart" width="640" height="300"></canvas> </div> <div class="tool-fw-legend"> <div class="tool-fw-leg-item"><span class="tool-fw-dot" style="background:#2698ba"></span>LangChain</div> <div class="tool-fw-leg-item"><span class="tool-fw-dot" style="background:#c97af2"></span>CrewAI</div> <div class="tool-fw-leg-item"><span class="tool-fw-dot" style="background:#4fc97e"></span>Google ADK</div> </div> <div class="tool-fw-tooltip" id="fwTooltip" style="display:none"></div> </div> <style>.tool-fw-compare-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0;position:relative}.tool-fw-compare-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.4rem}.tool-fw-compare-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.tool-fw-compare-sub{font-size:.65rem;color:var(--global-text-color-light);opacity:.7}.tool-fw-legend{display:flex;gap:1.25rem;padding:.6rem 1.1rem;border-top:1px solid var(--global-divider-color);flex-wrap:wrap}.tool-fw-leg-item{display:flex;align-items:center;gap:.4rem;font-size:.72rem;color:var(--global-text-color-light)}.tool-fw-dot{width:9px;height:9px;border-radius:2px;flex-shrink:0}.tool-fw-tooltip{position:fixed;background:var(--global-bg-color);border:1px solid var(--global-divider-color);border-radius:6px;padding:.5rem .75rem;font-size:.73rem;color:var(--global-text-color);pointer-events:none;z-index:200;max-width:230px;line-height:1.5;box-shadow:0 4px 16px rgba(0,0,0,0.35)}</style> <script>
(function(){
  var canvas = document.getElementById('fwCompareChart');
  if (!canvas) return;
  var ctx = canvas.getContext('2d');
  var tooltip = document.getElementById('fwTooltip');

  var dims = ['Custom Tools', 'Built-in Tools', 'Multi-Agent', 'Async Support', 'Enterprise Scale'];
  var frameworks = ['LangChain', 'CrewAI', 'Google ADK'];
  var colors = ['#2698ba', '#c97af2', '#4fc97e'];

  // scores[dim][framework]: 1-5
  var scores = [
    [5, 4, 4],  // Custom Tools
    [3, 2, 5],  // Built-in Tools
    [4, 5, 5],  // Multi-Agent
    [5, 3, 4],  // Async Support
    [3, 3, 5],  // Enterprise Scale
  ];

  var descriptions = [
    ['Excellent — @tool decorator, custom schemas, flexible binding.', 'Good — @tool decorator with role-aware context.', 'Good — FunctionTool with docstring schema.'],
    ['Moderate — community tool integrations, not pre-packaged.', 'Limited — mostly relies on LangChain tools.', 'Excellent — google_search, code executor, VSearch built-in.'],
    ['Good — LangGraph for complex graphs, native agent chaining.', 'Excellent — Crew/Agent/Task model built for teams of agents.', 'Excellent — ParallelAgent, SequentialAgent, LlmAgent natively.'],
    ['Excellent — full async/await, ainvoke, astream throughout.', 'Moderate — some async support, primarily sync kickoff.', 'Good — async runner, run_async for streaming events.'],
    ['Moderate — requires custom infra for production scale.', 'Moderate — growing enterprise adoption.', 'Excellent — Google Cloud native, Vertex AI integration.'],
  ];

  var dpr = window.devicePixelRatio || 1;
  var W = Math.min(640, canvas.parentElement.getBoundingClientRect().width - 24);
  var H = 300;
  canvas.width  = W * dpr;
  canvas.height = H * dpr;
  canvas.style.width  = W + 'px';
  canvas.style.height = H + 'px';
  ctx.scale(dpr, dpr);

  var ML = 110, MR = 20, MT = 28, MB = 40;
  var PW = W - ML - MR, PH = H - MT - MB;
  var nDims = dims.length, nFW = frameworks.length;
  var groupW = PW / nDims;
  var barW = (groupW * 0.7) / nFW;
  var barGap = (groupW * 0.3) / (nFW + 1);
  var hitRects = [];

  function getTheme() {
    var s = getComputedStyle(document.documentElement);
    return {
      text:  s.getPropertyValue('--global-text-color').trim()       || '#e0e0e0',
      muted: s.getPropertyValue('--global-text-color-light').trim() || '#888',
      div:   s.getPropertyValue('--global-divider-color').trim()    || '#333',
    };
  }

  function draw(hoverDim, hoverFW) {
    ctx.clearRect(0, 0, W, H);
    hitRects = [];
    var th = getTheme();

    // Y grid
    for (var v = 1; v <= 5; v++) {
      var gy = MT + (1 - (v-1)/4) * PH;
      ctx.strokeStyle = th.div; ctx.lineWidth = 0.5;
      ctx.setLineDash([3,3]);
      ctx.beginPath(); ctx.moveTo(ML, gy); ctx.lineTo(ML+PW, gy); ctx.stroke();
      ctx.setLineDash([]);
      ctx.fillStyle = th.muted; ctx.font = '10px monospace'; ctx.textAlign = 'right';
      ctx.fillText(v, ML-6, gy + 3.5);
    }

    // Bars
    for (var di = 0; di < nDims; di++) {
      var gx = ML + di * groupW;
      for (var fi = 0; fi < nFW; fi++) {
        var bx = gx + barGap*(fi+1) + barW*fi;
        var score = scores[di][fi];
        var bh = (score-1)/4 * PH;
        var by = MT + PH - bh;
        var isH = (hoverDim === di && hoverFW === fi);
        var alpha = (hoverDim === null) ? 1 : (isH ? 1 : 0.25);
        ctx.globalAlpha = alpha;
        ctx.fillStyle = colors[fi];
        ctx.fillRect(bx, by, barW, bh);
        ctx.globalAlpha = 1;
        hitRects.push({ x: bx, y: by, w: barW, h: bh, di: di, fi: fi, score: score });
      }
      // X label
      ctx.fillStyle = th.muted; ctx.font = '9px monospace'; ctx.textAlign = 'center';
      var labelX = gx + groupW/2;
      var words = dims[di].split(' ');
      if (words.length > 1) {
        ctx.fillText(words[0], labelX, H - MB + 14);
        ctx.fillText(words.slice(1).join(' '), labelX, H - MB + 25);
      } else {
        ctx.fillText(dims[di], labelX, H - MB + 14);
      }
    }
  }

  draw(null, null);

  canvas.addEventListener('mousemove', function(e) {
    var rect = canvas.getBoundingClientRect();
    var mx = e.clientX - rect.left, my = e.clientY - rect.top;
    var hit = null;
    for (var i = 0; i < hitRects.length; i++) {
      var r = hitRects[i];
      if (mx >= r.x && mx <= r.x+r.w && my >= r.y && my <= r.y+r.h) { hit = r; break; }
    }
    if (hit) {
      draw(hit.di, hit.fi);
      tooltip.style.display = 'block';
      tooltip.style.left = (e.clientX + 12) + 'px';
      tooltip.style.top  = (e.clientY - 10) + 'px';
      tooltip.innerHTML = '<strong>' + frameworks[hit.fi] + ' — ' + dims[hit.di] + '</strong><br>' +
        '<span style="color:var(--global-text-color-light)">' + descriptions[hit.di][hit.fi] + '</span>';
      canvas.style.cursor = 'pointer';
    } else {
      draw(null, null);
      tooltip.style.display = 'none';
      canvas.style.cursor = 'default';
    }
  });
  canvas.addEventListener('mouseleave', function(){
    draw(null, null);
    tooltip.style.display = 'none';
  });
})();
</script> <table> <thead> <tr> <th> </th> <th>LangChain</th> <th>CrewAI</th> <th>Google ADK</th> </tr> </thead> <tbody> <tr> <td><strong>Tool definition</strong></td> <td><code class="language-plaintext highlighter-rouge">@tool</code> decorator</td> <td><code class="language-plaintext highlighter-rouge">@tool("Name")</code> decorator</td> <td><code class="language-plaintext highlighter-rouge">FunctionTool</code> or built-ins</td> </tr> <tr> <td><strong>Built-in tools</strong></td> <td>Community integrations</td> <td>Relies on LangChain tools</td> <td><code class="language-plaintext highlighter-rouge">google_search</code>, <code class="language-plaintext highlighter-rouge">BuiltInCodeExecutor</code>, <code class="language-plaintext highlighter-rouge">VSearchAgent</code></td> </tr> <tr> <td><strong>Agent model</strong></td> <td>Chain / graph</td> <td>Role + goal + backstory</td> <td><code class="language-plaintext highlighter-rouge">LlmAgent</code> + orchestrators</td> </tr> <tr> <td><strong>Best for</strong></td> <td>Composable pipelines</td> <td>Role-based multi-agent teams</td> <td>Google Cloud / enterprise</td> </tr> <tr> <td><strong>Async</strong></td> <td>Full (<code class="language-plaintext highlighter-rouge">ainvoke</code>, <code class="language-plaintext highlighter-rouge">astream</code>)</td> <td>Partial</td> <td><code class="language-plaintext highlighter-rouge">run_async</code> streaming</td> </tr> </tbody> </table> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="tool-summary-card"> <div class="tool-summary-col"> <div class="tool-summary-label">WHAT</div> <p>LLMs can't call APIs, query databases, or run code on their own. Tool use lets them generate a structured JSON request — which the framework executes — bridging language reasoning with real-world action.</p> </div> <div class="tool-summary-divider"></div> <div class="tool-summary-col"> <div class="tool-summary-label">WHY</div> <p>Training data is frozen. Real-world tasks require live data, exact computation, and system interaction. Tool use is the mechanism that makes agents actually useful — not just fluent.</p> </div> <div class="tool-summary-divider"></div> <div class="tool-summary-col"> <div class="tool-summary-label">RULE OF THUMB</div> <p>Use tool calling whenever the agent needs to break out of its training data: real-time info, private data, exact math, code execution, or triggering real-world actions.</p> </div> </div> <style>.tool-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.tool-summary-card{flex-direction:column}}.tool-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.tool-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.tool-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.tool-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li><strong>The LLM doesn’t run the tool.</strong> It generates a structured JSON object specifying which tool to call and with what arguments. The framework executes it. This separation is what makes tool use safe and auditable.</li> <li><strong>The docstring is the interface.</strong> In LangChain and CrewAI, the function’s docstring is what the LLM reads to decide when to use a tool. A clear, specific docstring = accurate tool routing.</li> <li><strong>ADK built-in tools are production-ready.</strong> <code class="language-plaintext highlighter-rouge">google_search</code>, <code class="language-plaintext highlighter-rouge">BuiltInCodeExecutor</code>, and <code class="language-plaintext highlighter-rouge">VSearchAgent</code> require zero configuration. For custom tools, all three frameworks use decorator-based definitions.</li> <li><strong><code class="language-plaintext highlighter-rouge">AgentExecutor</code> (LangChain) is an execution loop</strong> — it keeps calling the agent until no more tool calls are needed. The <code class="language-plaintext highlighter-rouge">{agent_scratchpad}</code> placeholder is the agent’s scratch pad for intermediate thoughts.</li> <li><strong>CrewAI’s role/goal/backstory</strong> creates an agent persona that influences reasoning style and output formatting — not just a system prompt, but a professional identity.</li> <li><strong>Code execution is qualitatively different</strong> from text generation — it gives agents deterministic, exact answers for math, data manipulation, and computation. The LLM reasons about <em>what</em> code to write; the executor runs it.</li> </ul> <hr/> <p><em>Next up — Chapter 6: Planning, where agents stop reacting to individual inputs and start building structured multi-step plans to achieve complex goals.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="tool-use"/><category term="function-calling"/><summary type="html"><![CDATA[LLMs are frozen in time and disconnected from the world. Tool use breaks those walls — letting agents call APIs, run code, query databases, and trigger real actions.]]></summary></entry><entry><title type="html">Chapter 4: Reflection</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/reflection/" rel="alternate" type="text/html" title="Chapter 4: Reflection"/><published>2026-01-17T00:00:00+00:00</published><updated>2026-01-17T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/reflection</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/reflection/"><![CDATA[<h2 id="the-problem-with-first-drafts">The Problem with First Drafts</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>System prompt:</strong> A set of instructions sent to the LLM <em>before</em> the user's message. It defines the AI's persona, rules, and behavior. e.g., "You are a senior code reviewer. Be critical and look for bugs." The same model behaves very differently with different system prompts.</p> <p style="margin-top:0.5rem"><strong>Feedback loop:</strong> A cycle where the output of one step becomes input to the same (or earlier) step. Reflection is a feedback loop: generate → evaluate → the evaluation feeds back into the generation.</p> <p style="margin-top:0.5rem"><strong>Deterministic vs probabilistic:</strong> Deterministic means the same input always gives the same output (like a calculator). Probabilistic means the same input might give different outputs (like an LLM). Temperature 0 makes LLMs more deterministic but never fully so.</p> <p style="margin-top:0.5rem"><strong>Iteration:</strong> One complete cycle of "try → evaluate → try again." Three iterations means the agent attempted to improve the output three times.</p> </div> <p>In <a href="/kohshh-portfolio/blog/2026/prompt-chaining/">Chapter 1</a> through <a href="/kohshh-portfolio/blog/2026/parallelization/">Chapter 3</a>, every pattern shares one assumption: once a step produces output, that output moves forward. The chain trusts it. The router acts on it. The synthesizer combines it.</p> <p>But what if the output is wrong?</p> <p>LLMs make mistakes. They miss edge cases, hallucinate facts, generate code with bugs, write summaries that lose key details. A single-pass pipeline has no way to catch any of this. It just passes the mistake downstream — and by the time it reaches the user, the error is baked in.</p> <p>The fix is to add a feedback loop: <strong>make the agent evaluate its own output before declaring it done</strong>.</p> <p>That’s reflection.</p> <h2 id="what-reflection-is">What Reflection Is</h2> <p>Reflection is the pattern where an agent:</p> <ol> <li><strong>Executes</strong> — produces an initial output</li> <li><strong>Evaluates</strong> — critiques that output against specific criteria</li> <li><strong>Refines</strong> — generates an improved version based on the critique</li> <li><strong>Repeats</strong> — until the output meets a quality bar or a max iteration count is hit</li> </ol> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">REFLECTION FEEDBACK LOOP</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">Task</div> <div class="ns-node-sub">Initial prompt or goal</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Execute — Producer Agent</div> <div class="ns-node-sub">Generates initial output · code, text, plan</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">Evaluate — Critic Agent</div> <div class="ns-node-sub">Reviews against criteria · finds bugs, gaps, inaccuracies</div> </div> <div class="ns-arrow"></div> <div class="ns-decision"> <div class="ns-node-title">Satisfactory?</div> </div> <div class="ns-arrow"></div> <div class="ns-branch-row"> <div class="ns-branch"> <span class="ns-label-red">Needs work</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"> <div class="ns-node-title">Refine</div> <div class="ns-node-sub">Producer rewrites using critique</div> </div> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-dim"> <div class="ns-node-title">↑ Back to Evaluate</div> </div> </div> <div class="ns-branch"> <span class="ns-label-green">Approved</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Final Output</div> <div class="ns-node-sub">Quality bar met · done</div> </div> </div> </div> </div> </div> <p>The key difference from a simple chain: <strong>the arrow goes backward</strong>. Output becomes input again. This is a feedback loop, not a pipeline.</p> <blockquote> <p><strong>The core insight:</strong> A model reviewing its own work with a different system prompt — “you are a senior code reviewer” vs “you are a code generator” — behaves fundamentally differently. The second prompt surfaces errors the first one wouldn’t.</p> </blockquote> <h2 id="the-feedback-loop-step-by-step">The Feedback Loop, Step by Step</h2> <div class="refl-loop-wrapper"> <div class="refl-loop-header"> <span class="refl-loop-title">REFLECTION FEEDBACK LOOP</span> <button class="refl-loop-btn" id="reflLoopBtn">▶ Animate</button> </div> <div class="refl-loop-body"> <div class="refl-stage-row"> <div class="refl-stage" id="reflStage0"> <div class="refl-stage-num">01</div> <div class="refl-stage-label">EXECUTE</div> <div class="refl-stage-desc">Producer generates initial output (code, text, plan…)</div> </div> <div class="refl-arrow" id="reflArrow01">→</div> <div class="refl-stage" id="reflStage1"> <div class="refl-stage-num">02</div> <div class="refl-stage-label">EVALUATE</div> <div class="refl-stage-desc">Critic checks output against criteria — finds bugs, gaps, inaccuracies</div> </div> <div class="refl-arrow" id="reflArrow12">→</div> <div class="refl-stage" id="reflStage2"> <div class="refl-stage-num">03</div> <div class="refl-stage-label">REFINE</div> <div class="refl-stage-desc">Producer rewrites using the critique as a guide</div> </div> </div> <div class="refl-feedback-arc"> <div class="refl-feedback-line" id="reflFeedback"> <span class="refl-feedback-label">feedback loop → back to EXECUTE</span> </div> </div> <div class="refl-iter-row" id="reflIterRow"> <div class="refl-iter-dot" id="reflDot0"></div> <div class="refl-iter-dot" id="reflDot1"></div> <div class="refl-iter-dot" id="reflDot2"></div> <div class="refl-iter-dot" id="reflDot3"></div> <span class="refl-iter-label" id="reflIterLabel">Iteration: 0 / 3</span> </div> </div> </div> <style>.refl-loop-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.refl-loop-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.refl-loop-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.refl-loop-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.refl-loop-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.refl-loop-body{padding:1.5rem 1.1rem 1rem}.refl-stage-row{display:flex;align-items:center;gap:.5rem;justify-content:center;flex-wrap:wrap}.refl-stage{border:1.5px solid var(--global-divider-color);border-radius:8px;padding:1rem;width:160px;text-align:center;transition:border-color .3s,background .3s;background:rgba(128,128,128,0.03);flex-shrink:0}.refl-stage.active{border-color:#2698ba;background:rgba(38,152,186,0.08)}.refl-stage.active .refl-stage-num{color:#2698ba}.refl-stage-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:var(--global-text-color-light);margin-bottom:.35rem;transition:color .3s}.refl-stage-label{font-size:.78rem;font-weight:700;color:var(--global-text-color);margin-bottom:.4rem}.refl-stage-desc{font-size:.68rem;color:var(--global-text-color-light);line-height:1.5}.refl-arrow{font-size:1.2rem;color:var(--global-text-color-light);transition:color .3s;flex-shrink:0}.refl-arrow.active{color:#2698ba}.refl-feedback-arc{margin-top:.75rem;display:flex;justify-content:center}.refl-feedback-line{border:1px dashed var(--global-divider-color);border-top:0;width:80%;height:24px;border-radius:0 0 8px 8px;display:flex;align-items:flex-end;justify-content:center;padding-bottom:2px;transition:border-color .3s}.refl-feedback-line.active{border-color:#c97af2}.refl-feedback-label{font-size:.62rem;font-family:monospace;color:var(--global-text-color-light);transition:color .3s}.refl-feedback-line.active .refl-feedback-label{color:#c97af2}.refl-iter-row{display:flex;align-items:center;gap:.5rem;justify-content:center;margin-top:1rem}.refl-iter-dot{width:8px;height:8px;border-radius:50%;background:rgba(128,128,128,0.2);transition:background .3s}.refl-iter-dot.done{background:#4fc97e}.refl-iter-dot.active{background:#2698ba}.refl-iter-label{font-size:.68rem;font-family:monospace;color:var(--global-text-color-light);margin-left:.5rem}</style> <script>
(function(){
  var btn = document.getElementById('reflLoopBtn');
  if (!btn) return;

  var stages   = [0,1,2].map(function(i){ return document.getElementById('reflStage'+i); });
  var arrows   = ['01','12'].map(function(s){ return document.getElementById('reflArrow'+s); });
  var feedback = document.getElementById('reflFeedback');
  var dots     = [0,1,2,3].map(function(i){ return document.getElementById('reflDot'+i); });
  var label    = document.getElementById('reflIterLabel');

  var MAX_ITER = 3;
  var running = false;

  function reset() {
    stages.forEach(function(s){ if(s) s.classList.remove('active'); });
    arrows.forEach(function(a){ if(a) a.classList.remove('active'); });
    if(feedback) feedback.classList.remove('active');
    dots.forEach(function(d){ if(d) { d.classList.remove('active','done'); }});
    if(label) label.textContent = 'Iteration: 0 / ' + MAX_ITER;
  }

  function delay(ms){ return new Promise(function(r){ setTimeout(r, ms); }); }

  async function runLoop() {
    reset();
    running = true;
    btn.textContent = '⏳ Running…';
    btn.disabled = true;

    for (var iter = 0; iter < MAX_ITER; iter++) {
      // Mark completed dots
      for (var d = 0; d < iter; d++) { dots[d].classList.remove('active'); dots[d].classList.add('done'); }
      dots[iter].classList.add('active');
      if(label) label.textContent = 'Iteration: ' + (iter+1) + ' / ' + MAX_ITER;

      // EXECUTE
      stages[0].classList.add('active');
      await delay(700);

      // → arrow
      arrows[0].classList.add('active');
      await delay(300);

      // EVALUATE
      stages[0].classList.remove('active');
      stages[1].classList.add('active');
      await delay(800);

      // → arrow
      arrows[1].classList.add('active');
      await delay(300);

      // REFINE
      stages[1].classList.remove('active');
      stages[2].classList.add('active');
      await delay(700);

      if (iter < MAX_ITER - 1) {
        // feedback loop arc
        stages[2].classList.remove('active');
        arrows[0].classList.remove('active');
        arrows[1].classList.remove('active');
        feedback.classList.add('active');
        await delay(600);
        feedback.classList.remove('active');
      }
    }

    // Final state
    dots[MAX_ITER-1].classList.remove('active');
    dots[MAX_ITER-1].classList.add('done');
    stages[2].classList.remove('active');
    if(label) label.textContent = '✓ Done — output approved';
    running = false;
    btn.textContent = '↺ Replay';
    btn.disabled = false;
  }

  btn.addEventListener('click', function(){
    if (!running) runLoop();
  });
})();
</script> <h2 id="self-reflection-vs-producer-critic">Self-Reflection vs Producer-Critic</h2> <p>There are two ways to implement reflection. The choice changes both the quality of the output and the architecture of the system.</p> <h3 id="approach-1-self-reflection">Approach 1: Self-Reflection</h3> <p>A single agent generates output, then switches roles to critique it.</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">SELF-REFLECTION — one agent, two system prompts, one loop</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="flex-direction:row;align-items:center;gap:0.75rem;padding:1.1rem 1.25rem;flex-wrap:nowrap;"> <div class="ns-node ns-node-cyan" style="flex-shrink:0;max-width:120px;"><div class="ns-node-title">Task</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-purple" style="flex-shrink:0;max-width:200px;"> <div class="ns-node-title">Same Agent — Generator</div> <div class="ns-node-sub">system prompt: "write code"</div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-amber" style="flex-shrink:0;max-width:200px;"> <div class="ns-node-title">Same Agent — Critic</div> <div class="ns-node-sub">system prompt: "review code"</div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div style="display:flex;flex-direction:column;gap:0.4rem;flex-shrink:0;"> <div class="ns-node ns-node-red"><div class="ns-node-title">Needs work → loops back</div></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Approved → Output</div></div> </div> </div> </div> <p><strong>Simpler to implement.</strong> One model, two system prompts. But the same model that generated the output is also evaluating it — it tends to be less critical of its own work.</p> <h3 id="approach-2-producer-critic">Approach 2: Producer-Critic</h3> <p>Two distinct agents with separate roles and personas.</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">PRODUCER-CRITIC — two separate agents, objective review</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:200px;"><div class="ns-node-title">Task</div><div class="ns-node-sub">what needs to be created</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:260px;"><div class="ns-node-title">Producer Agent</div><div class="ns-node-sub">generates: code, text, or plan — first draft</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:220px;"><div class="ns-node-title">Draft Output</div><div class="ns-node-sub">passed to the critic for review</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:260px;"><div class="ns-node-title">Critic Agent</div><div class="ns-node-sub">different system prompt · fresh perspective · finds issues</div></div> <div class="ns-arrow"></div> <div class="ns-decision" style="max-width:180px;"><div class="ns-node-title">Approved?</div></div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:440px;"> <div class="ns-branch"> <span class="ns-label-red">No — iterate</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"><div class="ns-node-title">Back to Producer</div><div class="ns-node-sub">critique used to improve next draft</div></div> </div> <div class="ns-branch"> <span class="ns-label-green">Yes</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Final Output</div><div class="ns-node-sub">quality bar met</div></div> </div> </div> </div> </div> <p><strong>More powerful.</strong> The Critic has a completely different system prompt — “You are a senior software engineer”, “You are a meticulous fact-checker” — and approaches the output with a fresh lens. It doesn’t have the generator’s blind spots.</p> <table> <thead> <tr> <th> </th> <th>Self-Reflection</th> <th>Producer-Critic</th> </tr> </thead> <tbody> <tr> <td><strong>Agents</strong></td> <td>1 (two roles)</td> <td>2 (dedicated)</td> </tr> <tr> <td><strong>Objectivity</strong></td> <td>Lower — same model bias</td> <td>Higher — separate perspective</td> </tr> <tr> <td><strong>Cost</strong></td> <td>Lower</td> <td>Higher</td> </tr> <tr> <td><strong>Critique quality</strong></td> <td>General</td> <td>Specialized</td> </tr> <tr> <td><strong>Best for</strong></td> <td>Quick refinement</td> <td>High-stakes, quality-critical tasks</td> </tr> </tbody> </table> <h2 id="quality-improves-with-each-iteration">Quality Improves with Each Iteration</h2> <div class="refl-quality-wrapper"> <div class="refl-quality-header"> <span class="refl-quality-title">OUTPUT QUALITY vs REFLECTION ITERATIONS</span> <button class="refl-quality-btn" id="qualityPlayBtn">▶ Animate</button> </div> <canvas id="qualityChart" style="display:block;width:100%;"></canvas> <div class="refl-quality-footer" id="qualityFooter" style="display:none"> Reflection cycles stopped at iteration 3 — quality crossed the acceptable threshold </div> </div> <style>.refl-quality-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.refl-quality-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.refl-quality-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.refl-quality-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.refl-quality-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.refl-quality-footer{padding:.6rem 1.1rem;border-top:1px solid var(--global-divider-color);background:rgba(79,201,126,0.06);font-size:.75rem;color:#4fc97e;font-family:monospace}</style> <script>
(function(){
  var canvas  = document.getElementById('qualityChart');
  var playBtn = document.getElementById('qualityPlayBtn');
  var footer  = document.getElementById('qualityFooter');
  if (!canvas || !playBtn) return;

  var dataPoints = [
    { label: 'v0 — Initial',  score: 44, note: 'Bug in loop range, no docstring, no edge case handling' },
    { label: 'v1 — Iter 1',   score: 67, note: 'Bug fixed, docstring added — still no ValueError for negatives' },
    { label: 'v2 — Iter 2',   score: 83, note: 'Error handling added — type hints missing, could be cleaner' },
    { label: 'v3 — Iter 3',   score: 91, note: 'Type hints added, code clean — critic approves' },
    { label: 'v4 — Iter 4',   score: 95, note: 'Minor style polish — diminishing returns' },
  ];
  var THRESHOLD = 80;
  var PAD = { top: 28, right: 24, bottom: 52, left: 52 };
  var H = 240;

  function getTheme() {
    var s = getComputedStyle(document.documentElement);
    return {
      text:  s.getPropertyValue('--global-text-color').trim()       || '#e0e0e0',
      muted: s.getPropertyValue('--global-text-color-light').trim() || '#888',
      div:   s.getPropertyValue('--global-divider-color').trim()    || '#333',
    };
  }

  var tooltip = null;
  function ensureTooltip() {
    if (!tooltip) {
      tooltip = document.createElement('div');
      tooltip.style.cssText = 'position:fixed;background:var(--global-bg-color,#111);border:1px solid var(--global-divider-color,#333);border-radius:6px;padding:0.45rem 0.7rem;font-size:0.72rem;color:var(--global-text-color,#e0e0e0);pointer-events:none;z-index:200;max-width:240px;line-height:1.5;display:none;box-shadow:0 4px 16px rgba(0,0,0,0.4);';
      document.body.appendChild(tooltip);
    }
    return tooltip;
  }

  var hitPoints = [];

  function draw(progress) {
    var dpr = window.devicePixelRatio || 1;
    var W = canvas.parentElement ? canvas.parentElement.getBoundingClientRect().width : 560;
    if (W < 10) W = 560;
    canvas.width  = W * dpr;
    canvas.height = H * dpr;
    canvas.style.height = H + 'px';
    var ctx = canvas.getContext('2d');
    ctx.scale(dpr, dpr);
    var th = getTheme();

    var PW = W - PAD.left - PAD.right;
    var PH = H - PAD.top - PAD.bottom;

    hitPoints = [];

    // Y grid + axis
    ctx.font = '10px monospace';
    [0,20,40,60,80,100].forEach(function(v){
      var y = PAD.top + (1 - v/100) * PH;
      ctx.strokeStyle = th.div; ctx.lineWidth = 0.5;
      ctx.setLineDash([3,3]);
      ctx.beginPath(); ctx.moveTo(PAD.left, y); ctx.lineTo(PAD.left+PW, y); ctx.stroke();
      ctx.setLineDash([]);
      ctx.fillStyle = th.muted; ctx.textAlign = 'right';
      ctx.fillText(v, PAD.left - 6, y + 3.5);
    });

    // Y label
    ctx.save(); ctx.translate(12, PAD.top + PH/2); ctx.rotate(-Math.PI/2);
    ctx.textAlign = 'center'; ctx.fillStyle = th.muted; ctx.font = '10px monospace';
    ctx.fillText('Quality Score', 0, 0); ctx.restore();

    // Threshold line
    var ty = PAD.top + (1 - THRESHOLD/100) * PH;
    ctx.strokeStyle = 'rgba(79,201,126,0.4)'; ctx.lineWidth = 1;
    ctx.setLineDash([5,4]);
    ctx.beginPath(); ctx.moveTo(PAD.left, ty); ctx.lineTo(PAD.left+PW, ty); ctx.stroke();
    ctx.setLineDash([]);
    ctx.fillStyle = '#4fc97e'; ctx.font = '9px monospace'; ctx.textAlign = 'left';
    ctx.fillText('acceptable (' + THRESHOLD + ')', PAD.left + 4, ty - 4);

    // Data points to draw (based on progress)
    var n = dataPoints.length;
    var step = PW / (n - 1);
    var totalSegments = n - 1;
    var drawn = progress * totalSegments;

    // Line
    ctx.beginPath();
    ctx.strokeStyle = '#2698ba'; ctx.lineWidth = 2.5; ctx.lineJoin = 'round';
    for (var i = 0; i < n; i++) {
      var seg = drawn - (i - 1);
      if (i === 0) {
        var x0 = PAD.left;
        var y0 = PAD.top + (1 - dataPoints[0].score/100) * PH;
        ctx.moveTo(x0, y0);
      } else {
        var frac = Math.min(Math.max(drawn - (i-1), 0), 1);
        var xPrev = PAD.left + (i-1)*step;
        var yPrev = PAD.top + (1 - dataPoints[i-1].score/100)*PH;
        var xCurr = PAD.left + i*step;
        var yCurr = PAD.top + (1 - dataPoints[i].score/100)*PH;
        ctx.lineTo(xPrev + (xCurr-xPrev)*frac, yPrev + (yCurr-yPrev)*frac);
      }
    }
    ctx.stroke();

    // Gradient fill under line
    var grad = ctx.createLinearGradient(0, PAD.top, 0, PAD.top+PH);
    grad.addColorStop(0, 'rgba(38,152,186,0.25)');
    grad.addColorStop(1, 'rgba(38,152,186,0)');
    ctx.beginPath();
    ctx.moveTo(PAD.left, PAD.top + PH);
    for (var j = 0; j < n; j++) {
      var segJ = Math.min(Math.max(drawn - (j > 0 ? j-1 : 0), 0), 1);
      if (j === 0) {
        ctx.lineTo(PAD.left, PAD.top + (1 - dataPoints[0].score/100)*PH);
      } else {
        var frac2 = Math.min(Math.max(drawn-(j-1), 0), 1);
        var xP = PAD.left + (j-1)*step;
        var yP = PAD.top + (1-dataPoints[j-1].score/100)*PH;
        var xC = PAD.left + j*step;
        var yC = PAD.top + (1-dataPoints[j].score/100)*PH;
        ctx.lineTo(xP + (xC-xP)*frac2, yP + (yC-yP)*frac2);
      }
    }
    var lastDrawnX = PAD.left + Math.min(drawn, n-1) * step;
    ctx.lineTo(lastDrawnX, PAD.top + PH);
    ctx.closePath();
    ctx.fillStyle = grad;
    ctx.fill();

    // Dots + X labels
    dataPoints.forEach(function(pt, i) {
      if (drawn < i) return;
      var frac = i === 0 ? 1 : Math.min(drawn - (i-1), 1);
      if (frac <= 0) return;
      var px = PAD.left + i * step;
      var py = PAD.top + (1 - pt.score/100)*PH;

      ctx.beginPath();
      ctx.arc(px, py, 5, 0, Math.PI*2);
      ctx.fillStyle = pt.score >= THRESHOLD ? '#4fc97e' : '#2698ba';
      ctx.fill();
      ctx.strokeStyle = 'rgba(0,0,0,0.4)'; ctx.lineWidth = 1;
      ctx.stroke();

      // Score label
      ctx.fillStyle = pt.score >= THRESHOLD ? '#4fc97e' : '#2698ba';
      ctx.font = '700 10px monospace'; ctx.textAlign = 'center';
      ctx.fillText(pt.score, px, py - 9);

      // X label
      ctx.fillStyle = th.muted; ctx.font = '9px monospace'; ctx.textAlign = 'center';
      ctx.fillText(pt.label, px, H - PAD.bottom + 16);

      hitPoints.push({ x: px, y: py, pt: pt });
    });
  }

  draw(0);

  // Hover tooltip
  canvas.addEventListener('mousemove', function(e) {
    var rect = canvas.getBoundingClientRect();
    var mx = e.clientX - rect.left;
    var my = e.clientY - rect.top;
    var tt = ensureTooltip();
    var hit = null;
    hitPoints.forEach(function(h) {
      var dx = mx - h.x, dy = my - h.y;
      if (Math.sqrt(dx*dx+dy*dy) < 14) hit = h;
    });
    if (hit) {
      tt.innerHTML = '<strong>' + hit.pt.label + '</strong><br>Score: ' + hit.pt.score + '/100<br><span style="color:var(--global-text-color-light,#888);font-size:0.68rem">' + hit.pt.note + '</span>';
      tt.style.display = 'block';
      tt.style.left = (e.clientX + 12) + 'px';
      tt.style.top  = (e.clientY - 10) + 'px';
      canvas.style.cursor = 'pointer';
    } else {
      tt.style.display = 'none';
      canvas.style.cursor = 'default';
    }
  });
  canvas.addEventListener('mouseleave', function() {
    ensureTooltip().style.display = 'none';
  });

  var animId = null, running = false;
  playBtn.addEventListener('click', function(){
    if (running) return;
    footer.style.display = 'none';
    running = true;
    playBtn.textContent = '⏳ Running…';
    playBtn.disabled = true;
    var DURATION = 2400, startTs = null;
    function frame(ts) {
      if (!startTs) startTs = ts;
      var p = Math.min((ts - startTs) / DURATION, 1);
      draw(p * (dataPoints.length - 1));
      if (p < 1) { animId = requestAnimationFrame(frame); }
      else {
        running = false;
        playBtn.textContent = '↺ Replay';
        playBtn.disabled = false;
        footer.style.display = 'block';
      }
    }
    requestAnimationFrame(frame);
  });

  window.addEventListener('resize', function(){ draw(0); footer.style.display='none'; });
})();
</script> <p>Hover over any data point to see what the critique found at that iteration. Notice the <strong>diminishing returns</strong> past iteration 3 — this is why reflection loops always need a stopping condition.</p> <h2 id="the-live-demo-a-bug-found-and-fixed">The Live Demo: A Bug Found and Fixed</h2> <p>This is a concrete reflection cycle. The initial code has a real bug. Click through to watch the critic find it and the producer fix it.</p> <div class="refl-demo-wrapper"> <div class="refl-demo-header"> <span class="refl-demo-title">PRODUCER-CRITIC LIVE DEMO — Factorial Function</span> </div> <div class="refl-demo-tabs"> <button class="refl-tab active" data-tab="v1" onclick="reflTab('v1')">Draft v1</button> <button class="refl-tab" data-tab="critique" onclick="reflTab('critique')">Critique</button> <button class="refl-tab" data-tab="v2" onclick="reflTab('v2')">Refined v2</button> </div> <div class="refl-tab-content active" id="reflTab-v1"> <div class="refl-code-label">PRODUCER OUTPUT — initial draft</div> <pre class="refl-code"><code><span class="refl-bug">def calculate_factorial(n):
    result = 1
    for i in range(1, n):  # ← bug: misses n itself
        result *= i
    return result</span></code></pre> <div class="refl-code-note">5 lines. No docstring. No edge cases. Hidden off-by-one bug in <code>range(1, n)</code> — <code>calculate_factorial(5)</code> returns 24 instead of 120.</div> <button class="refl-next-btn" onclick="reflTab('critique')">Send to Critic →</button> </div> <div class="refl-tab-content" id="reflTab-critique"> <div class="refl-code-label">CRITIC OUTPUT — structured review</div> <div class="refl-critique-list"> <div class="refl-critique-item refl-crit-error"> <span class="refl-crit-icon">✗</span> <div><strong>Off-by-one error</strong> — <code>range(1, n)</code> excludes <code>n</code>. For n=5: produces 1×2×3×4=24, not 120. Must be <code>range(1, n+1)</code>.</div> </div> <div class="refl-critique-item refl-crit-error"> <span class="refl-crit-icon">✗</span> <div><strong>Missing edge case</strong> — <code>calculate_factorial(0)</code> returns 1 only by coincidence (empty loop). This is mathematically correct but undocumented.</div> </div> <div class="refl-critique-item refl-crit-warn"> <span class="refl-crit-icon">!</span> <div><strong>No input validation</strong> — negative input (e.g., n=-3) runs silently and returns 1. Should raise <code>ValueError</code>.</div> </div> <div class="refl-critique-item refl-crit-warn"> <span class="refl-crit-icon">!</span> <div><strong>No docstring</strong> — function contract, args, return value, and exceptions are undocumented.</div> </div> <div class="refl-critique-item refl-crit-info"> <span class="refl-crit-icon">→</span> <div><strong>No type hints</strong> — consider <code>def calculate_factorial(n: int) -&gt; int</code> for clarity.</div> </div> </div> <div class="refl-critique-verdict refl-verdict-fail">✗ NEEDS REVISION — 2 critical errors, 2 warnings found</div> <button class="refl-next-btn" onclick="reflTab('v2')">Apply critique → Refine</button> </div> <div class="refl-tab-content" id="reflTab-v2"> <div class="refl-code-label">PRODUCER OUTPUT — refined v2</div> <pre class="refl-code"><code>def calculate_factorial(n: int) -&gt; int:
    <span class="refl-added">"""Calculate the factorial of a non-negative integer n.

    Args:
        n: A non-negative integer.
    Returns:
        The factorial of n (n!). Returns 1 when n is 0.
    Raises:
        ValueError: If n is negative.
    """
    if n &lt; 0:
        raise ValueError(f"Input must be non-negative, got {n}.")
    if n == 0:
        return 1</span>
    result = 1
    for i in range(1, <span class="refl-added">n + 1</span>):  <span class="refl-added"># ← fixed</span>
        result *= i
    return result</code></pre> <div class="refl-critique-verdict refl-verdict-pass">✓ APPROVED — all issues addressed</div> <div class="refl-code-note">All 5 critique points addressed. Bug fixed. Docstring complete. Edge cases handled. Type hints added.</div> </div> </div> <style>.refl-demo-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.refl-demo-header{padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.refl-demo-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.refl-demo-tabs{display:flex;border-bottom:1px solid var(--global-divider-color)}.refl-tab{flex:1;padding:.55rem .75rem;font-family:monospace;font-size:.72rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s,color .15s}.refl-tab:last-child{border-right:0}.refl-tab.active{background:rgba(38,152,186,0.1);color:#2698ba;font-weight:700}.refl-tab:hover:not(.active){background:rgba(128,128,128,0.08);color:var(--global-text-color)}.refl-tab-content{display:none;padding:1.1rem}.refl-tab-content.active{display:block}.refl-code-label{font-size:.62rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:var(--global-text-color-light);margin-bottom:.6rem}.refl-code{background:rgba(0,0,0,0.25);border:1px solid var(--global-divider-color);border-radius:6px;padding:.9rem 1rem;font-size:.82rem;overflow-x:auto;margin-bottom:.75rem}.refl-code code{background:0;padding:0;font-family:monospace}.refl-bug{color:#ff6b6b}.refl-added{color:#4fc97e}.refl-code-note{font-size:.75rem;color:var(--global-text-color-light);line-height:1.55;margin-bottom:.75rem}.refl-critique-list{display:flex;flex-direction:column;gap:.5rem;margin-bottom:.75rem}.refl-critique-item{display:flex;align-items:flex-start;gap:.6rem;padding:.6rem .75rem;border-radius:5px;font-size:.8rem;line-height:1.5}.refl-crit-error{background:rgba(255,107,107,0.08);border:1px solid rgba(255,107,107,0.2)}.refl-crit-warn{background:rgba(230,168,23,0.08);border:1px solid rgba(230,168,23,0.2)}.refl-crit-info{background:rgba(38,152,186,0.06);border:1px solid rgba(38,152,186,0.15)}.refl-crit-icon{font-size:.75rem;flex-shrink:0;padding-top:.1rem;font-weight:700}.refl-crit-error .refl-crit-icon{color:#ff6b6b}.refl-crit-warn .refl-crit-icon{color:#e6a817}.refl-crit-info .refl-crit-icon{color:#2698ba}.refl-critique-verdict{font-size:.75rem;font-family:monospace;font-weight:700;padding:.45rem .75rem;border-radius:4px;margin-bottom:.75rem}.refl-verdict-fail{background:rgba(255,107,107,0.1);color:#ff6b6b;border:1px solid rgba(255,107,107,0.2)}.refl-verdict-pass{background:rgba(79,201,126,0.1);color:#4fc97e;border:1px solid rgba(79,201,126,0.2)}.refl-next-btn{font-family:monospace;font-size:.72rem;padding:.35rem .9rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.refl-next-btn:hover{background:rgba(38,152,186,0.12);border-color:#2698ba;color:#2698ba}</style> <script>
function reflTab(name) {
  document.querySelectorAll('.refl-tab').forEach(function(t){ t.classList.remove('active'); });
  document.querySelectorAll('.refl-tab-content').forEach(function(t){ t.classList.remove('active'); });
  var btn = document.querySelector('[data-tab="'+name+'"]');
  var content = document.getElementById('reflTab-'+name);
  if (btn) btn.classList.add('active');
  if (content) content.classList.add('active');
}
</script> <h2 id="six-situations-where-reflection-pays-off">Six Situations Where Reflection Pays Off</h2> <div class="refl-usecases-grid"> <div class="refl-usecase-card"> <span class="refl-uc-num">01</span> <h4>Code Generation</h4> <p>Write code, run static analysis or tests, feed results back — the agent fixes its own bugs before you see the output.</p> <span class="refl-uc-gain">Catches runtime errors, logic bugs, style issues</span> </div> <div class="refl-usecase-card"> <span class="refl-uc-num">02</span> <h4>Long-Form Content</h4> <p>Generate a draft, critique for tone, flow, and clarity, rewrite. Repeat until the piece reads like something an editor approved.</p> <span class="refl-uc-gain">Polished prose without human editing rounds</span> </div> <div class="refl-usecase-card"> <span class="refl-uc-num">03</span> <h4>Summarization</h4> <p>Generate a summary, compare against the source document for missed key points, refine until complete and accurate.</p> <span class="refl-uc-gain">Reduces hallucinations and key-point omissions</span> </div> <div class="refl-usecase-card"> <span class="refl-uc-num">04</span> <h4>Planning</h4> <p>Propose a plan, evaluate feasibility and constraint violations, revise. Don't hand over a plan that fails on day one.</p> <span class="refl-uc-gain">More realistic, executable plans</span> </div> <div class="refl-usecase-card"> <span class="refl-uc-num">05</span> <h4>Fact-Checking</h4> <p>A Critic agent with a "fact-checker" persona reviews every claim in the draft and flags anything that needs sourcing or correction.</p> <span class="refl-uc-gain">Structural defense against hallucination</span> </div> <div class="refl-usecase-card"> <span class="refl-uc-num">06</span> <h4>Complex Reasoning</h4> <p>Propose a reasoning step, evaluate whether it leads closer to the solution or introduces contradictions, backtrack if needed.</p> <span class="refl-uc-gain">Enables multi-step problem solving</span> </div> </div> <style>.refl-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:.85rem;margin:1.5rem 0}.refl-usecase-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.refl-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#c97af2}.refl-usecase-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.refl-usecase-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.refl-uc-gain{font-size:.7rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <h2 id="the-langchain-way-lcel-reflection-loop">The LangChain Way: LCEL Reflection Loop</h2> <p>The LangChain implementation uses <strong>conversation history</strong> as the state that carries context between generation and critique cycles. Each iteration appends messages to a growing list, so the model always has full context on what it produced and what the critique said.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>
<span class="kn">from</span> <span class="n">dotenv</span> <span class="kn">import</span> <span class="n">load_dotenv</span>
<span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>
<span class="kn">from</span> <span class="n">langchain_core.messages</span> <span class="kn">import</span> <span class="n">SystemMessage</span><span class="p">,</span> <span class="n">HumanMessage</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">messages</code> instead of <code class="language-plaintext highlighter-rouge">ChatPromptTemplate</code>?</strong> This example uses raw message lists rather than prompt templates. That’s because the conversation history needs to grow dynamically — each iteration appends a new critique and a new refinement. Template-based prompts have fixed slots; message lists are append-only, which is exactly what a growing feedback loop needs.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4o</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mf">0.1</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><code class="language-plaintext highlighter-rouge">temperature=0.1</code> — very low. For code generation and code review, you want deterministic, consistent outputs. Creativity hurts here; precision helps.</p> </blockquote> <h3 id="the-task-definition">The Task Definition</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">task_prompt</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">
Your task is to create a Python function named `calculate_factorial`.
Requirements:
1. Accept a single integer `n` as input.
2. Calculate its factorial (n!).
3. Include a clear docstring.
4. Handle edge cases: factorial of 0 is 1.
5. Raise ValueError for negative input.
</span><span class="sh">"""</span>
</code></pre></div></div> <blockquote> <p>This is the source of truth that both the Generator and the Critic reference. The Critic compares every output against this spec — so the spec must be complete and unambiguous from the start.</p> </blockquote> <h3 id="the-reflection-loop">The Reflection Loop</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">max_iterations</span> <span class="o">=</span> <span class="mi">3</span>
<span class="n">current_code</span>   <span class="o">=</span> <span class="sh">""</span>
<span class="n">message_history</span> <span class="o">=</span> <span class="p">[</span><span class="nc">HumanMessage</span><span class="p">(</span><span class="n">content</span><span class="o">=</span><span class="n">task_prompt</span><span class="p">)]</span>   <span class="c1"># starts with just the task
</span>
<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_iterations</span><span class="p">):</span>

    <span class="c1"># ── STAGE 1: GENERATE (or REFINE) ──────────────────────────────────
</span>    <span class="k">if</span> <span class="n">i</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
        <span class="c1"># First pass: just the task prompt
</span>        <span class="n">response</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">message_history</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="c1"># Subsequent passes: task + previous code + critique
</span>        <span class="n">message_history</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span>
            <span class="nc">HumanMessage</span><span class="p">(</span><span class="n">content</span><span class="o">=</span><span class="sh">"</span><span class="s">Refine the code using the critiques provided.</span><span class="sh">"</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="n">response</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">message_history</span><span class="p">)</span>

    <span class="n">current_code</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="n">content</span>
    <span class="n">message_history</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">response</span><span class="p">)</span>        <span class="c1"># add generated code to history
</span></code></pre></div></div> <blockquote> <p><strong>Why append to history?</strong> The model receives the entire conversation on each call. By appending both the generated code and the critique, the model knows:</p> <ol> <li>The original task (what was asked)</li> <li>What it previously generated (so it doesn’t repeat it)</li> <li>What the critic said (so it knows what to fix)</li> </ol> <p>Without this, each iteration would generate from scratch with no awareness of previous attempts.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># ── STAGE 2: REFLECT (CRITIQUE) ────────────────────────────────────
</span>    <span class="n">reflector_messages</span> <span class="o">=</span> <span class="p">[</span>
        <span class="nc">SystemMessage</span><span class="p">(</span><span class="n">content</span><span class="o">=</span><span class="sh">"""</span><span class="s">
You are a senior software engineer. Perform a meticulous code review.
Evaluate the code against the original task requirements.
Check for: bugs, missing edge cases, style issues, incomplete docstrings.
If the code is perfect, respond with exactly: CODE_IS_PERFECT
Otherwise, provide a bulleted list of specific critiques.
</span><span class="sh">"""</span><span class="p">),</span>
        <span class="nc">HumanMessage</span><span class="p">(</span><span class="n">content</span><span class="o">=</span><span class="sa">f</span><span class="sh">"</span><span class="s">Task:</span><span class="se">\n</span><span class="si">{</span><span class="n">task_prompt</span><span class="si">}</span><span class="se">\n\n</span><span class="s">Code:</span><span class="se">\n</span><span class="si">{</span><span class="n">current_code</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="p">]</span>
    <span class="n">critique_response</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">reflector_messages</span><span class="p">)</span>
    <span class="n">critique</span> <span class="o">=</span> <span class="n">critique_response</span><span class="p">.</span><span class="n">content</span>
</code></pre></div></div> <blockquote> <p><strong>Why a separate system prompt for the Critic?</strong> This is the Producer-Critic split within a single LangChain call. By giving the same model a completely different system prompt (“senior software engineer performing a code review”), you get a different reasoning stance. The model is no longer generating — it’s scrutinizing.</p> <p><strong>Why not add the Critic’s system prompt to the main conversation history?</strong> Because you want the Critic to always evaluate from a fresh perspective, not a perspective shaped by the previous generation attempts. Each critique call is independent.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># ── STOPPING CONDITION ───────────────────────────────────────────────
</span>    <span class="k">if</span> <span class="sh">"</span><span class="s">CODE_IS_PERFECT</span><span class="sh">"</span> <span class="ow">in</span> <span class="n">critique</span><span class="p">:</span>
        <span class="k">break</span>                                   <span class="c1"># stop early — quality bar met
</span>
    <span class="c1"># Add critique to history so next iteration can fix it
</span>    <span class="n">message_history</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span>
        <span class="nc">HumanMessage</span><span class="p">(</span><span class="n">content</span><span class="o">=</span><span class="sa">f</span><span class="sh">"</span><span class="s">Critique:</span><span class="se">\n</span><span class="si">{</span><span class="n">critique</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Early stopping matters.</strong> Without a stopping condition, the loop runs all <code class="language-plaintext highlighter-rouge">max_iterations</code> even when the output is already good — wasting API calls. The <code class="language-plaintext highlighter-rouge">CODE_IS_PERFECT</code> sentinel lets the Critic signal satisfaction explicitly.</p> </blockquote> <h3 id="message-history-at-iteration-2">Message History at Iteration 2</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>message_history after 2 iterations:
┌──────────────────────────────────────────────────────────┐
│ [0] Human: "Write a function that calculates factorial…" │  ← task
│ [1] AI:    "def calculate_factorial(n):\n    result=1…"  │  ← v1 code
│ [2] Human: "Critique:\n• Bug in range(1,n)…\n• No doc…" │  ← critique 1
│ [3] Human: "Refine the code using the critiques."        │  ← trigger
│ [4] AI:    "def calculate_factorial(n: int) -&gt; int:\n…"  │  ← v2 code
└──────────────────────────────────────────────────────────┘
</code></pre></div></div> <p>The model at iteration 2 sees the full thread and knows exactly what changed and why.</p> <h3 id="full-reflection-data-flow">Full Reflection Data Flow</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">LANGCHAIN REFLECTION DATA FLOW — how code and critique pass between roles</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:280px;"><div class="ns-node-title">task_prompt + message_history</div><div class="ns-node-sub">growing conversation thread — task, code, critique, refinement</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:260px;"><div class="ns-node-title">LLM — Generator Role</div><div class="ns-node-sub">writes or improves the code</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:220px;"><div class="ns-node-title">current_code</div><div class="ns-node-sub">added back to message_history for context</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:260px;"><div class="ns-node-title">LLM — Critic Role</div><div class="ns-node-sub">different system prompt · reviews code · produces critique</div></div> <div class="ns-arrow"></div> <div class="ns-decision" style="max-width:220px;"><div class="ns-node-title">CODE_IS_PERFECT?</div></div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:440px;"> <div class="ns-branch"> <span class="ns-label-red">No — has issues</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"><div class="ns-node-title">Add critique to history</div><div class="ns-node-sub">generator reads it on next iteration</div></div> </div> <div class="ns-branch"> <span class="ns-label-green">Yes — approved</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Final Output</div><div class="ns-node-sub">loop exits</div></div> </div> </div> </div> </div> <h2 id="the-google-adk-way-generator-critic">The Google ADK Way: Generator-Critic</h2> <p>The ADK version uses <strong>session state</strong> (key-value store) instead of message history for passing data between agents. The architecture is simpler to read but less flexible for complex multi-turn loops.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">SequentialAgent</span><span class="p">,</span> <span class="n">LlmAgent</span>
</code></pre></div></div> <blockquote> <p><strong>Why only <code class="language-plaintext highlighter-rouge">SequentialAgent</code> and <code class="language-plaintext highlighter-rouge">LlmAgent</code>?</strong> The ADK has a <code class="language-plaintext highlighter-rouge">LoopAgent</code> for true iterative loops, but the core reflection concept is demonstrated here in a single generate → critique cycle using <code class="language-plaintext highlighter-rouge">SequentialAgent</code>. Two agents, run in order, sharing state via <code class="language-plaintext highlighter-rouge">output_key</code>.</p> </blockquote> <h3 id="the-producer-agent">The Producer Agent</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">generator</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">DraftWriter</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Generates initial draft content on a given subject.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Write a short, informative paragraph about the user</span><span class="sh">'</span><span class="s">s subject.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">output_key</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">draft_text</span><span class="sh">"</span><span class="p">,</span>    <span class="c1"># stores output in session state
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">output_key</code></strong>: When <code class="language-plaintext highlighter-rouge">DraftWriter</code> completes, its output is stored as <code class="language-plaintext highlighter-rouge">session_state["draft_text"]</code>. Any subsequent agent can read this by referencing <code class="language-plaintext highlighter-rouge">{draft_text}</code> in its instruction template.</p> <p><strong>Why not just pass the output directly?</strong> ADK agents are independent workers. They don’t pass return values to each other — they communicate through shared session state. This decouples the producer from the consumer; you can add more critics, rearrange them, or branch without modifying the producer.</p> </blockquote> <h3 id="the-critic-agent">The Critic Agent</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reviewer</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">FactChecker</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Reviews text for factual accuracy and provides structured critique.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">
You are a meticulous fact-checker.
1. Read the text provided in the state key </span><span class="sh">'</span><span class="s">draft_text</span><span class="sh">'</span><span class="s">.
2. Carefully verify the factual accuracy of all claims.
3. Your final output must be a dictionary with two keys:
   - </span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="s">:    </span><span class="sh">"</span><span class="s">ACCURATE</span><span class="sh">"</span><span class="s"> or </span><span class="sh">"</span><span class="s">INACCURATE</span><span class="sh">"</span><span class="s">
   - </span><span class="sh">"</span><span class="s">reasoning</span><span class="sh">"</span><span class="s">: A clear explanation citing specific issues if any.
</span><span class="sh">"""</span><span class="p">,</span>
    <span class="n">output_key</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">review_output</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># stores critique in session state
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">{draft_text}</code> in the instruction?</strong> The ADK automatically fills <code class="language-plaintext highlighter-rouge">{key}</code> placeholders from session state before calling the model. So the Critic’s actual prompt at runtime contains the full text that <code class="language-plaintext highlighter-rouge">DraftWriter</code> produced — without any manual wiring.</p> <p><strong>Why structured output (dict with <code class="language-plaintext highlighter-rouge">status</code> + <code class="language-plaintext highlighter-rouge">reasoning</code>)?</strong> Structured output is machine-readable. A downstream agent or your own application code can parse <code class="language-plaintext highlighter-rouge">session_state["review_output"]["status"]</code> to decide whether to trigger another iteration — without parsing free-form text.</p> </blockquote> <h3 id="the-pipeline">The Pipeline</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">review_pipeline</span> <span class="o">=</span> <span class="nc">SequentialAgent</span><span class="p">(</span>
    <span class="n">name</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">WriteAndReviewPipeline</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">sub_agents</span> <span class="o">=</span> <span class="p">[</span><span class="n">generator</span><span class="p">,</span> <span class="n">reviewer</span><span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><code class="language-plaintext highlighter-rouge">SequentialAgent</code> guarantees <code class="language-plaintext highlighter-rouge">generator</code> completes before <code class="language-plaintext highlighter-rouge">reviewer</code> starts. This matters because <code class="language-plaintext highlighter-rouge">reviewer</code> reads from <code class="language-plaintext highlighter-rouge">session_state["draft_text"]</code> — which must already exist.</p> </blockquote> <h3 id="execution-state-at-each-step">Execution State at Each Step</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Session State Evolution:
┌─────────────────────────────────────────────────────┐
│ Before:  {}                                         │
│                                                     │
│ After DraftWriter:                                  │
│   { "draft_text": "Solar panels convert sunlight…" }│
│                                                     │
│ After FactChecker:                                  │
│   { "draft_text": "Solar panels convert sunlight…", │
│     "review_output": {                              │
│       "status":    "INACCURATE",                    │
│       "reasoning": "Claim about 40% efficiency      │
│                     is incorrect — max ~26%…"       │
│     }                                               │
│   }                                                 │
└─────────────────────────────────────────────────────┘
</code></pre></div></div> <h3 id="adk-orchestration">ADK Orchestration</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ADK REFLECTION — SequentialAgent passing state between writer and checker</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:200px;"><div class="ns-node-title">User Input</div><div class="ns-node-sub">e.g. "Write a paragraph about Mars"</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:240px;"><div class="ns-node-title">SequentialAgent</div><div class="ns-node-sub">runs DraftWriter first, then FactChecker</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:260px;"><div class="ns-node-title">DraftWriter LlmAgent</div><div class="ns-node-sub">writes paragraph · output_key: "draft_text" → saved to Session State</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:260px;"><div class="ns-node-title">Session State</div><div class="ns-node-sub">shared memory between agents · FactChecker reads draft_text from here</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-cyan" style="max-width:260px;"><div class="ns-node-title">FactChecker LlmAgent</div><div class="ns-node-sub">reads draft_text · outputs: status "ACCURATE" or "INACCURATE" + reasoning</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:240px;"><div class="ns-node-title">Critique Result</div><div class="ns-node-sub">status + reasoning saved to review_output key</div></div> </div> </div> <h2 id="side-by-side-langchain-vs-adk">Side by Side: LangChain vs ADK</h2> <table> <thead> <tr> <th> </th> <th>LangChain (LCEL)</th> <th>Google ADK</th> </tr> </thead> <tbody> <tr> <td><strong>State mechanism</strong></td> <td>Growing <code class="language-plaintext highlighter-rouge">message_history</code> list</td> <td>Key-value session state (<code class="language-plaintext highlighter-rouge">output_key</code>)</td> </tr> <tr> <td><strong>Iteration control</strong></td> <td>Explicit <code class="language-plaintext highlighter-rouge">for</code> loop + early-break</td> <td><code class="language-plaintext highlighter-rouge">LoopAgent</code> (or manual SequentialAgent chains)</td> </tr> <tr> <td><strong>Critique format</strong></td> <td>Free-form string in conversation</td> <td>Structured dict (better for programmatic use)</td> </tr> <tr> <td><strong>Role switching</strong></td> <td>New <code class="language-plaintext highlighter-rouge">SystemMessage</code> per call</td> <td>Separate <code class="language-plaintext highlighter-rouge">LlmAgent</code> with dedicated instruction</td> </tr> <tr> <td><strong>Context window risk</strong></td> <td>Higher — history grows each iteration</td> <td>Lower — only relevant state keys passed</td> </tr> <tr> <td><strong>Stopping condition</strong></td> <td>Sentinel string (<code class="language-plaintext highlighter-rouge">CODE_IS_PERFECT</code>)</td> <td>Status field in structured output</td> </tr> <tr> <td><strong>Best for</strong></td> <td>Iterative loops where history matters</td> <td>Single-cycle generate → critique → act</td> </tr> </tbody> </table> <h2 id="the-trade-offs-you-cant-ignore">The Trade-offs You Can’t Ignore</h2> <p>Reflection is not free. Every iteration adds:</p> <div class="refl-tradeoffs-grid"> <div class="refl-tradeoff-card refl-tradeoff-cost"> <div class="refl-tradeoff-icon">💸</div> <h4>Cost</h4> <p>Each iteration is at least 2 LLM calls (generator + critic). A 3-iteration loop = 6 API calls. At GPT-4o pricing, this adds up fast for high-volume tasks.</p> </div> <div class="refl-tradeoff-card refl-tradeoff-latency"> <div class="refl-tradeoff-icon">⏱</div> <h4>Latency</h4> <p>Iterations are sequential — each must complete before the next begins. A 3-iteration loop with 3s per call = 9s minimum. Unsuitable for real-time use cases.</p> </div> <div class="refl-tradeoff-card refl-tradeoff-context"> <div class="refl-tradeoff-icon">📋</div> <h4>Context Growth</h4> <p>Each iteration appends to the conversation history. After 4-5 cycles on a long document, you may approach the model's context window limit.</p> </div> <div class="refl-tradeoff-card refl-tradeoff-loop"> <div class="refl-tradeoff-icon">∞</div> <h4>Infinite Loops</h4> <p>Without a firm <code>max_iterations</code> cap and a clear stopping condition, the agent can loop indefinitely — especially if the Critic keeps finding minor issues.</p> </div> </div> <style>.refl-tradeoffs-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(185px,1fr));gap:.85rem;margin:1.5rem 0}.refl-tradeoff-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.refl-tradeoff-cost{border-left:3px solid #ff6b6b}.refl-tradeoff-latency{border-left:3px solid #e6a817}.refl-tradeoff-context{border-left:3px solid #c97af2}.refl-tradeoff-loop{border-left:3px solid #2698ba}.refl-tradeoff-icon{font-size:1.2rem}.refl-tradeoff-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.refl-tradeoff-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}</style> <blockquote> <p><strong>Rule of thumb:</strong> Use reflection when quality and accuracy matter more than speed and cost. Don’t use it as the default — use it selectively for the parts of your pipeline where errors are expensive.</p> </blockquote> <h2 id="at-a-glance">At a Glance</h2> <div class="refl-summary-card"> <div class="refl-summary-col"> <div class="refl-summary-label">WHAT</div> <p>A feedback loop where an agent evaluates its own output — or a dedicated Critic agent evaluates it — and uses the critique to generate an improved version.</p> </div> <div class="refl-summary-divider"></div> <div class="refl-summary-col"> <div class="refl-summary-label">WHY</div> <p>First-pass outputs from LLMs are often incomplete, inaccurate, or non-compliant. Reflection adds a self-correction layer — catching errors before they reach the user.</p> </div> <div class="refl-summary-divider"></div> <div class="refl-summary-col"> <div class="refl-summary-label">RULE OF THUMB</div> <p>Use when output quality matters more than speed. Use a separate Critic agent (not self-reflection) when the task requires specialized evaluation — code review, fact-checking, compliance.</p> </div> </div> <style>.refl-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.refl-summary-card{flex-direction:column}}.refl-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.refl-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.refl-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.refl-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#c97af2}</style> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li><strong>Reflection = feedback loop.</strong> The output doesn’t move forward until it passes evaluation. Generate → critique → refine → repeat.</li> <li><strong>Producer-Critic beats self-reflection</strong> for quality-critical tasks. A separate system prompt creates a genuinely different perspective; the same model reviewing its own work has blind spots.</li> <li><strong>The stopping condition is mandatory.</strong> Always pair a <code class="language-plaintext highlighter-rouge">max_iterations</code> cap with an explicit quality signal (sentinel string, structured status field). Without both, loops run forever.</li> <li><strong>LangChain uses growing message history</strong> — the full conversation thread (task, code, critique, refinement) is passed on each call. Natural for iterative loops; risks context window overflow at scale.</li> <li><strong>ADK uses session state</strong> — agents write to named keys, other agents read from them. Cleaner data handoff; structured output is easier to act on programmatically.</li> <li><strong>Cost scales with iterations.</strong> Each reflection cycle is 2+ LLM calls. Budget for it or gate reflection behind a quality pre-check.</li> <li><strong>Connections forward:</strong> Reflection pairs naturally with memory (Chapter 8) — a Critic that remembers what it critiqued before can give progressively sharper feedback. It also anchors to goal-setting (Chapter 11) — the goal is the benchmark the Critic evaluates against.</li> </ul> <p><em>Next up — Chapter 5: Tool Use, where agents stop reasoning in text and start taking real actions in the world.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="reflection"/><summary type="html"><![CDATA[First drafts are rarely final. Reflection gives agents the ability to critique their own outputs, find what's wrong, and iterate toward better results — before returning anything to you.]]></summary></entry><entry><title type="html">Chapter 3: Parallelization</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/parallelization/" rel="alternate" type="text/html" title="Chapter 3: Parallelization"/><published>2026-01-13T00:00:00+00:00</published><updated>2026-01-13T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/parallelization</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/parallelization/"><![CDATA[<h2 id="the-problem-with-waiting">The Problem with Waiting</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Latency:</strong> The time it takes for a response to arrive. If an API takes 2 seconds to respond, its latency is 2 seconds. In agents, total latency = sum of all sequential steps. Parallelization reduces this.</p> <p style="margin-top:0.5rem"><strong>I/O-bound vs CPU-bound:</strong> "I/O bound" means the code spends most of its time <em>waiting</em> for input/output (network responses, disk reads). "CPU bound" means it's actively computing. Async (asyncio) helps I/O-bound tasks — it's useless for CPU-bound work.</p> <p style="margin-top:0.5rem"><strong>async/await:</strong> Python keywords for writing code that can pause while waiting (e.g., for an API response) and let other code run in the meantime. <code>async def</code> marks a function as async. <code>await</code> pauses that function until a result is ready. This is NOT the same as running on multiple CPU cores.</p> <p style="margin-top:0.5rem"><strong>Event loop:</strong> The engine that powers asyncio. It keeps a list of tasks, runs one until it hits an <code>await</code> (waiting point), then switches to another task. It's like a chef managing multiple dishes — not cooking two things simultaneously, but switching attention efficiently.</p> <p style="margin-top:0.5rem"><strong>GIL (Global Interpreter Lock):</strong> A Python rule that only allows one thread to execute Python code at a time. This is why Python threads don't give true CPU parallelism. But for I/O-bound work (like LLM API calls), the GIL barely matters because the thread is just <em>waiting</em>, not executing.</p> </div> <p>In <a href="/kohshh-portfolio/blog/2026/prompt-chaining/">Chapter 1</a> we chained steps sequentially. In <a href="/kohshh-portfolio/blog/2026/routing/">Chapter 2</a> we added decision-making. Both assume the same thing: <strong>one step runs, finishes, then the next begins</strong>.</p> <p>That’s the right model when each step genuinely needs the previous step’s output. But often, it isn’t necessary — it’s just the default.</p> <p>Imagine your agent needs to research a company. It pulls:</p> <table> <thead> <tr> <th>Task</th> <th>Simulated latency</th> </tr> </thead> <tbody> <tr> <td>Search recent news</td> <td>1.2 s</td> </tr> <tr> <td>Fetch stock price data</td> <td>0.9 s</td> </tr> <tr> <td>Check social media mentions</td> <td>0.7 s</td> </tr> <tr> <td>Query internal company database</td> <td>1.5 s</td> </tr> <tr> <td><strong>Synthesize all findings</strong></td> <td>0.8 s</td> </tr> </tbody> </table> <p><strong>Sequential total: 5.1 seconds.</strong> Every task waits for the one before it — even though none of them depend on each other.</p> <p>Now think about it differently. News search doesn’t need stock data to start. Social media check doesn’t need news results. All four lookups are completely independent. So fire them all at once. Wait for the slowest (1.5 s), then synthesize.</p> <p><strong>Parallel total: 2.3 seconds.</strong> Same answer. <strong>2.2× faster.</strong></p> <p>That’s <strong>parallelization</strong>: identify the independent tasks, fire them concurrently, wait for everything to land, then continue.</p> <h2 id="the-core-concept">The Core Concept</h2> <p>Parallelization rests on one rule:</p> <blockquote> <p><strong>If Task B doesn’t need Task A’s output to start, Task B can start the moment Task A starts.</strong></p> </blockquote> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">PARALLELIZATION PATTERN</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">Input Query</div> <div class="ns-node-sub">Single request · fans out to independent workers</div> </div> <div class="ns-arrow"></div> <div class="ns-phase"> <div class="ns-phase-title">Parallel Execution — all start simultaneously</div> <div class="ns-phase-sub">Independent tasks run concurrently · no waiting</div> <div class="ns-row"> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Task A</div> <div class="ns-node-sub">Search News · 1.2s</div> </div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Task B</div> <div class="ns-node-sub">Fetch Stock · 0.9s</div> </div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Task C</div> <div class="ns-node-sub">Query DB · 1.5s</div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">Wait for slowest · 1.5s</div> <div class="ns-node-sub">All parallel results collected before proceeding</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Synthesize</div> <div class="ns-node-sub">Merge all outputs into one final answer · 0.8s</div> </div> </div> </div> <p>The input fans out to independent tasks. They run simultaneously. Their outputs converge at a single synthesis step.</p> <p>Notice: the <strong>synthesis step is still sequential</strong> — it must wait for all parallel tasks before it can begin. You’re not removing sequential dependencies; you’re removing <em>unnecessary</em> ones.</p> <h2 id="the-time-difference-visualized">The Time Difference, Visualized</h2> <div class="par-timeline-wrapper"> <div class="par-timeline-header"> <span class="par-timeline-title">SEQUENTIAL vs PARALLEL — EXECUTION TIMELINE</span> <div class="par-timeline-controls"> <button class="par-timeline-btn" id="timelinePlayBtn">▶ Animate</button> </div> </div> <canvas id="parallelTimeline" style="display:block;width:100%;"></canvas> <div class="par-timeline-footer" id="timelineBadge" style="display:none"> <span class="par-badge-highlight">2.2×</span> faster — same result <span class="par-badge-sub">Sequential: 5.1s &nbsp;·&nbsp; Parallel: 2.3s</span> </div> </div> <style>.par-timeline-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.par-timeline-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.par-timeline-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.par-timeline-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s,color .15s}.par-timeline-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.par-timeline-footer{padding:.65rem 1.1rem;border-top:1px solid var(--global-divider-color);background:rgba(79,201,126,0.07);font-size:.78rem;color:var(--global-text-color);display:flex;align-items:center;gap:.75rem}.par-badge-highlight{font-size:1.2rem;font-weight:800;color:#4fc97e;letter-spacing:-0.01em}.par-badge-sub{margin-left:auto;font-size:.68rem;color:var(--global-text-color-light);letter-spacing:.05em}</style> <script>
(function(){
  var canvas = document.getElementById('parallelTimeline');
  var playBtn = document.getElementById('timelinePlayBtn');
  var badge   = document.getElementById('timelineBadge');
  if (!canvas || !playBtn) return;

  var tasks = [
    { label: 'Search News',  dur: 1.2, color: '#2698ba' },
    { label: 'Fetch Stock',  dur: 0.9, color: '#4fc97e' },
    { label: 'Check Social', dur: 0.7, color: '#e6a817' },
    { label: 'Query DB',     dur: 1.5, color: '#c97af2' },
    { label: 'Synthesize',   dur: 0.8, color: '#ff6b6b' },
  ];

  // Sequential start times
  var seqStarts = [0, 1.2, 2.1, 2.8, 4.3]; // cumulative
  var SEQ_TOTAL = 5.1;

  // Parallel start times
  var parStarts  = [0, 0, 0, 0, 1.5]; // first 4 at t=0, synth at t=1.5
  var PAR_TOTAL  = 2.3;

  var SCALE = SEQ_TOTAL; // timeline goes 0..5.1 for both sections

  var LABEL_W = 112;
  var PAD_L   = 12;
  var PAD_R   = 16;
  var ROW_H   = 22;
  var ROW_GAP = 5;
  var SEC_HEAD = 28;
  var SEC_PAD  = 14;
  var SEC_H    = SEC_HEAD + tasks.length * (ROW_H + ROW_GAP);
  var CANVAS_H = SEC_H * 2 + 24;

  function setupCanvas() {
    var dpr = window.devicePixelRatio || 1;
    var W = canvas.parentElement ? canvas.parentElement.getBoundingClientRect().width : 600;
    if (W < 10) W = 600;
    canvas.width  = W * dpr;
    canvas.height = CANVAS_H * dpr;
    canvas.style.height = CANVAS_H + 'px';
    var ctx = canvas.getContext('2d');
    ctx.scale(dpr, dpr);
    return { ctx: ctx, W: W };
  }

  function getTheme() {
    var s = getComputedStyle(document.documentElement);
    return {
      text:  s.getPropertyValue('--global-text-color').trim()       || '#e0e0e0',
      muted: s.getPropertyValue('--global-text-color-light').trim() || '#888',
      div:   s.getPropertyValue('--global-divider-color').trim()    || '#333',
      bg:    s.getPropertyValue('--global-bg-color').trim()         || '#111',
    };
  }

  function drawTimeline(progress) {
    var setup = setupCanvas();
    var ctx = setup.ctx, W = setup.W;
    var th = getTheme();
    var tlW = W - LABEL_W - PAD_L - PAD_R;

    ctx.clearRect(0, 0, W, CANVAS_H);

    // ── Section A: Sequential ──
    var secAY = 0;
    ctx.font = '700 10px monospace';
    ctx.fillStyle = th.muted;
    ctx.textAlign = 'left';
    ctx.fillText('SEQUENTIAL', PAD_L, secAY + 14);
    var seqDoneTime = progress * SCALE;
    var seqLabel = seqDoneTime < SEQ_TOTAL
      ? (seqDoneTime.toFixed(1) + 's')
      : 'TOTAL: 5.1s';
    ctx.font = '10px monospace';
    ctx.fillStyle = seqDoneTime >= SEQ_TOTAL ? '#ff6b6b' : th.muted;
    ctx.textAlign = 'right';
    ctx.fillText(seqLabel, W - PAD_R, secAY + 14);

    tasks.forEach(function(t, i) {
      var rowY = secAY + SEC_HEAD + i * (ROW_H + ROW_GAP);
      var tStart = seqStarts[i], tEnd = tStart + t.dur;
      var xStart = LABEL_W + (tStart / SCALE) * tlW;
      var xEnd   = LABEL_W + (Math.min(tEnd, seqDoneTime) / SCALE) * tlW;
      var filled  = seqDoneTime >= tStart;

      // Row bg
      ctx.fillStyle = 'rgba(128,128,128,0.06)';
      ctx.fillRect(LABEL_W, rowY, tlW, ROW_H);

      // Filled bar
      if (filled) {
        var drawEnd = Math.min(xEnd, LABEL_W + tlW);
        if (drawEnd > xStart) {
          ctx.globalAlpha = 0.85;
          ctx.fillStyle = t.color;
          ctx.fillRect(xStart, rowY + 2, drawEnd - xStart, ROW_H - 4);
          ctx.globalAlpha = 1;
        }
      }

      // Label
      ctx.font = '11px monospace';
      ctx.fillStyle = filled && seqDoneTime >= tEnd ? t.color : th.muted;
      ctx.textAlign = 'right';
      ctx.fillText(t.label, LABEL_W - 8, rowY + ROW_H / 2 + 4);
    });

    // Time axis (seq)
    var axisY = secAY + SEC_HEAD + tasks.length * (ROW_H + ROW_GAP);
    ctx.strokeStyle = th.div; ctx.lineWidth = 0.5;
    ctx.beginPath(); ctx.moveTo(LABEL_W, axisY); ctx.lineTo(LABEL_W + tlW, axisY); ctx.stroke();
    [0,1,2,3,4,5].forEach(function(v) {
      var ax = LABEL_W + (v / SCALE) * tlW;
      ctx.fillStyle = th.muted; ctx.font = '9px monospace'; ctx.textAlign = 'center';
      ctx.fillText(v + 's', ax, axisY + 10);
      ctx.strokeStyle = th.div; ctx.lineWidth = 0.5;
      ctx.beginPath(); ctx.moveTo(ax, axisY); ctx.lineTo(ax, axisY + 3); ctx.stroke();
    });

    // ── Divider ──
    var divY = SEC_H + 8;
    ctx.strokeStyle = th.div; ctx.lineWidth = 0.5;
    ctx.setLineDash([4,4]);
    ctx.beginPath(); ctx.moveTo(PAD_L, divY); ctx.lineTo(W - PAD_R, divY); ctx.stroke();
    ctx.setLineDash([]);

    // ── Section B: Parallel ──
    var secBY = SEC_H + 16;
    ctx.font = '700 10px monospace';
    ctx.fillStyle = th.muted;
    ctx.textAlign = 'left';
    ctx.fillText('PARALLEL', PAD_L, secBY + 14);
    var parDoneTime = seqDoneTime; // same clock
    var parLabel = parDoneTime < PAR_TOTAL
      ? (parDoneTime.toFixed(1) + 's')
      : 'TOTAL: 2.3s';
    ctx.font = '10px monospace';
    ctx.fillStyle = parDoneTime >= PAR_TOTAL ? '#4fc97e' : th.muted;
    ctx.textAlign = 'right';
    ctx.fillText(parLabel, W - PAD_R, secBY + 14);

    tasks.forEach(function(t, i) {
      var rowY = secBY + SEC_HEAD + i * (ROW_H + ROW_GAP);
      var tStart = parStarts[i], tEnd = tStart + t.dur;
      var xStart = LABEL_W + (tStart / SCALE) * tlW;
      var xEnd   = LABEL_W + (Math.min(tEnd, parDoneTime) / SCALE) * tlW;
      var filled  = parDoneTime >= tStart;

      ctx.fillStyle = 'rgba(128,128,128,0.06)';
      ctx.fillRect(LABEL_W, rowY, tlW, ROW_H);

      if (filled) {
        var drawEnd = Math.min(xEnd, LABEL_W + tlW);
        if (drawEnd > xStart) {
          ctx.globalAlpha = 0.85;
          ctx.fillStyle = t.color;
          ctx.fillRect(xStart, rowY + 2, drawEnd - xStart, ROW_H - 4);
          ctx.globalAlpha = 1;
        }
      }

      ctx.font = '11px monospace';
      ctx.fillStyle = filled && parDoneTime >= tEnd ? t.color : th.muted;
      ctx.textAlign = 'right';
      ctx.fillText(t.label, LABEL_W - 8, rowY + ROW_H / 2 + 4);
    });

    // Playhead (only during animation, up to PAR_TOTAL)
    if (progress > 0 && progress < 1) {
      var phX = LABEL_W + (parDoneTime / SCALE) * tlW;
      ctx.strokeStyle = 'rgba(255,255,255,0.5)';
      ctx.lineWidth = 1;
      ctx.setLineDash([3,3]);
      ctx.beginPath();
      ctx.moveTo(phX, secBY + SEC_HEAD);
      ctx.lineTo(phX, secBY + SEC_HEAD + tasks.length * (ROW_H + ROW_GAP));
      ctx.stroke();
      ctx.setLineDash([]);
    }

    // Parallel axis
    var axisY2 = secBY + SEC_HEAD + tasks.length * (ROW_H + ROW_GAP);
    ctx.strokeStyle = th.div; ctx.lineWidth = 0.5;
    ctx.beginPath(); ctx.moveTo(LABEL_W, axisY2); ctx.lineTo(LABEL_W + tlW, axisY2); ctx.stroke();
    [0,1,2,3,4,5].forEach(function(v) {
      var ax = LABEL_W + (v / SCALE) * tlW;
      ctx.fillStyle = th.muted; ctx.font = '9px monospace'; ctx.textAlign = 'center';
      ctx.fillText(v + 's', ax, axisY2 + 10);
      ctx.strokeStyle = th.div; ctx.lineWidth = 0.5;
      ctx.beginPath(); ctx.moveTo(ax, axisY2); ctx.lineTo(ax, axisY2 + 3); ctx.stroke();
    });

    // "Done" markers
    if (progress >= 1) {
      var seqDoneX = LABEL_W + (SEQ_TOTAL / SCALE) * tlW;
      var parDoneX = LABEL_W + (PAR_TOTAL / SCALE) * tlW;
      [
        { x: seqDoneX, y: SEC_HEAD + secAY, color: '#ff6b6b' },
        { x: parDoneX, y: SEC_HEAD + secBY, color: '#4fc97e' },
      ].forEach(function(m) {
        ctx.strokeStyle = m.color; ctx.lineWidth = 1.5;
        ctx.setLineDash([]);
        ctx.beginPath();
        ctx.moveTo(m.x, m.y);
        ctx.lineTo(m.x, m.y + tasks.length * (ROW_H + ROW_GAP));
        ctx.stroke();
      });
    }
  }

  // Draw initial state
  drawTimeline(0);

  var animId = null;
  var running = false;

  playBtn.addEventListener('click', function() {
    if (running) return;
    badge.style.display = 'none';
    running = true;
    playBtn.textContent = '⏳ Running…';
    playBtn.disabled = true;

    var ANIM_DURATION = 2800; // ms
    var startTime = null;

    function frame(ts) {
      if (!startTime) startTime = ts;
      var elapsed = ts - startTime;
      var progress = Math.min(elapsed / ANIM_DURATION, 1);
      drawTimeline(progress);
      if (progress < 1) {
        animId = requestAnimationFrame(frame);
      } else {
        running = false;
        playBtn.textContent = '↺ Replay';
        playBtn.disabled = false;
        badge.style.display = 'flex';
      }
    }
    animId = requestAnimationFrame(frame);
  });

  window.addEventListener('resize', function() { drawTimeline(0); badge.style.display = 'none'; });
})();
</script> <h2 id="when-to-use-it-seven-scenarios">When to Use It: Seven Scenarios</h2> <div class="par-usecases-grid"> <div class="par-usecase-card"> <span class="par-usecase-num">01</span> <h4>Information Gathering</h4> <p>Query multiple APIs simultaneously — news, stock data, social feeds, databases — instead of fetching them one by one.</p> <span class="par-usecase-gain">↑ 3–5× faster research agents</span> </div> <div class="par-usecase-card"> <span class="par-usecase-num">02</span> <h4>Data Analysis</h4> <p>Run sentiment analysis, keyword extraction, categorization, and urgency scoring on the same batch of text — all at once.</p> <span class="par-usecase-gain">↑ Multi-faceted output in one pass</span> </div> <div class="par-usecase-card"> <span class="par-usecase-num">03</span> <h4>Multi-API Orchestration</h4> <p>A travel agent checking flights, hotels, events, and restaurants simultaneously. Four calls, not four round-trips.</p> <span class="par-usecase-gain">↑ Complete plan, not a drip feed</span> </div> <div class="par-usecase-card"> <span class="par-usecase-num">04</span> <h4>Content Generation</h4> <p>Generate subject line, body copy, image prompt, and CTA text for an email — in parallel, then assemble.</p> <span class="par-usecase-gain">↑ Faster creative pipelines</span> </div> <div class="par-usecase-card"> <span class="par-usecase-num">05</span> <h4>Input Validation</h4> <p>Check email format, phone validity, address lookup, and profanity filter simultaneously — return all issues at once.</p> <span class="par-usecase-gain">↑ Sub-second validation feedback</span> </div> <div class="par-usecase-card"> <span class="par-usecase-num">06</span> <h4>Multi-Modal Processing</h4> <p>Analyze the text and the image in a social post at the same time. Merge insights from both modalities at the end.</p> <span class="par-usecase-gain">↑ No wasted latency on modalities</span> </div> <div class="par-usecase-card"> <span class="par-usecase-num">07</span> <h4>A/B Option Generation</h4> <p>Generate three different headlines simultaneously using slightly varied prompts. Pick the best one automatically.</p> <span class="par-usecase-gain">↑ More options, same wall-clock time</span> </div> </div> <style>.par-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:.85rem;margin:1.5rem 0}.par-usecase-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.par-usecase-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#2698ba}.par-usecase-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.par-usecase-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.par-usecase-gain{font-size:.7rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <h2 id="how-it-actually-works-asyncio">How It Actually Works: asyncio</h2> <p>Before writing any code, one important nuance needs to be addressed — because it trips up almost everyone.</p> <p><strong><code class="language-plaintext highlighter-rouge">asyncio</code> does not run code in parallel on multiple CPU cores.</strong> It runs on a <strong>single thread</strong>, using Python’s event loop.</p> <p>Here’s how it works:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Event Loop (single thread):
┌────────────────────────────────────────────────────────┐
│                                                        │
│  1. Start Task A (send HTTP request)                   │
│  2. While waiting for A's response:                    │
│     → Start Task B (send HTTP request)                 │
│     → Start Task C (send HTTP request)                 │
│  3. A's response arrives → resume Task A               │
│  4. B's response arrives → resume Task B               │
│  5. C's response arrives → resume Task C               │
│  6. All three done → proceed                           │
│                                                        │
└────────────────────────────────────────────────────────┘
</code></pre></div></div> <p>The key word is <strong>waiting</strong>. When Task A is waiting for a network response, that’s idle time — the CPU is doing nothing for Task A. The event loop fills that idle time by starting Task B and C.</p> <p>This means:</p> <table> <thead> <tr> <th>Scenario</th> <th>asyncio helps?</th> </tr> </thead> <tbody> <tr> <td>Multiple API calls / network requests</td> <td><strong>Yes</strong> — I/O bound, lots of waiting</td> </tr> <tr> <td>Multiple LLM calls (external API)</td> <td><strong>Yes</strong> — network I/O dominates</td> </tr> <tr> <td>Heavy CPU computation (matrix ops)</td> <td><strong>No</strong> — CPU bound, no idle time to exploit</td> </tr> <tr> <td>Reading many files</td> <td><strong>Yes</strong> — disk I/O has wait time</td> </tr> </tbody> </table> <p>For agentic AI — where tasks are overwhelmingly LLM API calls and web requests — <strong>asyncio is exactly the right tool</strong>. The Python GIL (Global Interpreter Lock) is largely irrelevant here because the threads aren’t fighting for CPU; they’re waiting for network.</p> <h2 id="watch-it-run-a-live-demo">Watch It Run: A Live Demo</h2> <p>Click <strong>Run</strong> to see three researcher agents fire simultaneously, then converge into a synthesis step.</p> <div class="par-demo-wrapper"> <div class="par-demo-header"> <span class="par-demo-title">LIVE PARALLEL EXECUTION DEMO</span> <button class="par-demo-btn" id="parDemoRunBtn">▶ Run</button> </div> <div class="par-demo-body"> <div class="par-demo-query" id="parDemoQuery"> <span class="par-query-label">INPUT TOPIC</span> <span class="par-query-text">"Sustainable Technology Advancements"</span> </div> <div class="par-demo-arrow">▼ Fan-out: all three start simultaneously</div> <div class="par-demo-agents"> <div class="par-agent-lane" id="parLane0"> <div class="par-agent-header"> <span class="par-agent-icon">⚡</span> <span class="par-agent-name">Renewable Energy</span> <span class="par-agent-badge" id="parBadge0">Idle</span> </div> <div class="par-agent-track"> <div class="par-agent-fill" id="parFill0" style="width:0%"></div> </div> <div class="par-agent-output" id="parOut0" style="display:none"> "Solar and wind capacity grew 40% YoY. Offshore wind now cost-competitive with gas in 12 markets." </div> </div> <div class="par-agent-lane" id="parLane1"> <div class="par-agent-header"> <span class="par-agent-icon">🚗</span> <span class="par-agent-name">EV Technology</span> <span class="par-agent-badge" id="parBadge1">Idle</span> </div> <div class="par-agent-track"> <div class="par-agent-fill" id="parFill1" style="width:0%;background:#4fc97e"></div> </div> <div class="par-agent-output" id="parOut1" style="display:none"> "Solid-state batteries on track for 2027 mass production. EV range now averages 380 km per charge." </div> </div> <div class="par-agent-lane" id="parLane2"> <div class="par-agent-header"> <span class="par-agent-icon">🌿</span> <span class="par-agent-name">Carbon Capture</span> <span class="par-agent-badge" id="parBadge2">Idle</span> </div> <div class="par-agent-track"> <div class="par-agent-fill" id="parFill2" style="width:0%;background:#e6a817"></div> </div> <div class="par-agent-output" id="parOut2" style="display:none"> "Direct air capture costs fell 30% in 2025. Three new gigaton-scale facilities began operations." </div> </div> </div> <div class="par-demo-synth" id="parDemoSynth"> <div class="par-synth-header"> <span class="par-synth-icon">⚙</span> <span class="par-synth-name">Synthesis Agent</span> <span class="par-agent-badge" id="parSynthBadge">Waiting for all researchers…</span> </div> <div class="par-synth-output" id="parSynthOut" style="display:none"> <strong>## Summary of Sustainable Technology Advancements</strong><br/><br/> <strong>Renewable Energy:</strong> Solar and wind installations accelerated globally, with offshore wind becoming commercially viable across 12 new markets, driven by 40% annual capacity growth.<br/><br/> <strong>Electric Vehicles:</strong> Solid-state battery technology is approaching mass production readiness, with current EV fleets achieving an average range of 380 km — closing the gap with combustion vehicles.<br/><br/> <strong>Carbon Capture:</strong> Significant cost reductions in direct air capture technology have enabled industrial-scale deployments, with three gigaton-class facilities now operational as of 2025. </div> </div> <div class="par-demo-timing" id="parDemoTiming" style="display:none"> <span>Sequential estimate: <strong>4.8s</strong></span> <span class="par-timing-divider">·</span> <span>Parallel actual: <strong>2.1s</strong></span> <span class="par-timing-divider">·</span> <span class="par-timing-win">2.3× faster</span> </div> </div> </div> <style>.par-demo-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.par-demo-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;background:rgba(128,128,128,0.05);border-bottom:1px solid var(--global-divider-color)}.par-demo-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.par-demo-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.par-demo-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.par-demo-body{padding:1rem 1.1rem;display:flex;flex-direction:column;gap:.9rem}.par-demo-query{border:1px solid var(--global-divider-color);border-radius:6px;padding:.6rem .9rem;display:flex;align-items:center;gap:.75rem;background:rgba(128,128,128,0.04)}.par-query-label{font-size:.62rem;font-weight:700;letter-spacing:.1em;color:#2698ba;flex-shrink:0}.par-query-text{font-family:monospace;font-size:.8rem;color:var(--global-text-color)}.par-demo-arrow{font-size:.72rem;color:var(--global-text-color-light);text-align:center;letter-spacing:.04em}.par-demo-agents{display:grid;grid-template-columns:repeat(3,1fr);gap:.75rem}@media(max-width:580px){.par-demo-agents{grid-template-columns:1fr}}.par-agent-lane{border:1px solid var(--global-divider-color);border-radius:7px;padding:.75rem;background:rgba(128,128,128,0.03);display:flex;flex-direction:column;gap:.5rem;transition:border-color .3s}.par-agent-lane.active{border-color:#2698ba}.par-agent-lane.done{border-color:#4fc97e}.par-agent-header{display:flex;align-items:center;gap:.4rem;flex-wrap:wrap}.par-agent-icon{font-size:.9rem}.par-agent-name{font-size:.75rem;font-weight:700;color:var(--global-text-color);flex:1}.par-agent-badge{font-size:.6rem;font-family:monospace;padding:.1rem .4rem;border-radius:3px;border:1px solid var(--global-divider-color);color:var(--global-text-color-light);background:transparent;transition:all .3s}.par-agent-badge.running{color:#2698ba;border-color:#2698ba;background:rgba(38,152,186,0.1)}.par-agent-badge.done{color:#4fc97e;border-color:#4fc97e;background:rgba(79,201,126,0.1)}.par-agent-track{height:6px;background:rgba(128,128,128,0.12);border-radius:3px;overflow:hidden}.par-agent-fill{height:100%;width:0;background:#2698ba;border-radius:3px;transition:width .05s linear}.par-agent-output{font-size:.73rem;color:var(--global-text-color-light);line-height:1.5;border-top:1px solid var(--global-divider-color);padding-top:.45rem;margin-top:.1rem}.par-demo-synth{border:1px solid var(--global-divider-color);border-radius:7px;padding:.75rem;background:rgba(128,128,128,0.03);opacity:.4;transition:opacity .4s,border-color .4s}.par-demo-synth.active{opacity:1;border-color:#c97af2}.par-demo-synth.done{opacity:1;border-color:#4fc97e}.par-synth-header{display:flex;align-items:center;gap:.4rem;margin-bottom:.5rem;flex-wrap:wrap}.par-synth-icon{font-size:.9rem}.par-synth-name{font-size:.75rem;font-weight:700;color:var(--global-text-color);flex:1}.par-synth-output{font-size:.76rem;color:var(--global-text-color);line-height:1.65;border-top:1px solid var(--global-divider-color);padding-top:.6rem;margin-top:.4rem}.par-demo-timing{display:flex;align-items:center;gap:.75rem;font-size:.78rem;color:var(--global-text-color-light);border-top:1px solid var(--global-divider-color);padding-top:.65rem;margin-top:.2rem;flex-wrap:wrap}.par-timing-divider{color:var(--global-divider-color)}.par-timing-win{color:#4fc97e;font-weight:700}</style> <script>
(function(){
  var runBtn = document.getElementById('parDemoRunBtn');
  if (!runBtn) return;

  var lanes = [
    { fill: 'parFill0', badge: 'parBadge0', out: 'parOut0', lane: 'parLane0', dur: 1500 },
    { fill: 'parFill1', badge: 'parBadge1', out: 'parOut1', lane: 'parLane1', dur: 1200 },
    { fill: 'parFill2', badge: 'parBadge2', out: 'parOut2', lane: 'parLane2', dur: 900  },
  ];
  var synth = {
    el:    document.getElementById('parDemoSynth'),
    badge: document.getElementById('parSynthBadge'),
    out:   document.getElementById('parSynthOut'),
  };
  var timing = document.getElementById('parDemoTiming');

  function reset() {
    lanes.forEach(function(l) {
      document.getElementById(l.fill).style.width  = '0%';
      document.getElementById(l.badge).textContent = 'Idle';
      document.getElementById(l.badge).className   = 'par-agent-badge';
      document.getElementById(l.out).style.display = 'none';
      document.getElementById(l.lane).className    = 'par-agent-lane';
    });
    synth.el.className     = 'par-demo-synth';
    synth.badge.textContent = 'Waiting for all researchers…';
    synth.badge.className   = 'par-agent-badge';
    synth.out.style.display = 'none';
    timing.style.display    = 'none';
  }

  runBtn.addEventListener('click', function() {
    reset();
    runBtn.disabled = true;
    runBtn.textContent = '⏳ Running…';

    var allDone = 0;
    var maxDur = 0;
    lanes.forEach(function(l) { if (l.dur > maxDur) maxDur = l.dur; });

    lanes.forEach(function(l) {
      var fillEl  = document.getElementById(l.fill);
      var badgeEl = document.getElementById(l.badge);
      var outEl   = document.getElementById(l.out);
      var laneEl  = document.getElementById(l.lane);

      laneEl.className = 'par-agent-lane active';
      badgeEl.textContent = 'Running…';
      badgeEl.className   = 'par-agent-badge running';

      var startTs = null;
      function animFill(ts) {
        if (!startTs) startTs = ts;
        var pct = Math.min((ts - startTs) / l.dur * 100, 100);
        fillEl.style.width = pct + '%';
        if (pct < 100) {
          requestAnimationFrame(animFill);
        } else {
          laneEl.className    = 'par-agent-lane done';
          badgeEl.textContent = 'Done ✓';
          badgeEl.className   = 'par-agent-badge done';
          outEl.style.display = 'block';
          allDone++;
          if (allDone === lanes.length) startSynth();
        }
      }
      requestAnimationFrame(animFill);
    });

    function startSynth() {
      synth.el.className = 'par-demo-synth active';
      synth.badge.textContent = 'Synthesizing…';
      synth.badge.className   = 'par-agent-badge running';
      setTimeout(function() {
        synth.el.className = 'par-demo-synth done';
        synth.badge.textContent = 'Done ✓';
        synth.badge.className   = 'par-agent-badge done';
        synth.out.style.display  = 'block';
        timing.style.display     = 'flex';
        runBtn.disabled   = false;
        runBtn.textContent = '↺ Replay';
      }, 900);
    }
  });
})();
</script> <h2 id="the-langchain-way-runnableparallel">The LangChain Way: <code class="language-plaintext highlighter-rouge">RunnableParallel</code></h2> <p>LangChain implements parallelization through <strong><code class="language-plaintext highlighter-rouge">RunnableParallel</code></strong> — a construct that takes a dictionary of named chains and runs all of them at once, returning a dictionary of results.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>
<span class="kn">import</span> <span class="n">asyncio</span>
<span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>
<span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">ChatPromptTemplate</span>
<span class="kn">from</span> <span class="n">langchain_core.output_parsers</span> <span class="kn">import</span> <span class="n">StrOutputParser</span>
<span class="kn">from</span> <span class="n">langchain_core.runnables</span> <span class="kn">import</span> <span class="n">RunnableParallel</span><span class="p">,</span> <span class="n">RunnablePassthrough</span>
</code></pre></div></div> <blockquote> <p><strong>Why these imports?</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">asyncio</code> — Python’s built-in library for writing concurrent code using <code class="language-plaintext highlighter-rouge">async</code>/<code class="language-plaintext highlighter-rouge">await</code></li> <li><code class="language-plaintext highlighter-rouge">ChatOpenAI</code> — the LangChain wrapper for OpenAI’s chat models (swappable for any other provider)</li> <li><code class="language-plaintext highlighter-rouge">ChatPromptTemplate</code> — structures messages into <code class="language-plaintext highlighter-rouge">system</code> + <code class="language-plaintext highlighter-rouge">user</code> roles (what the model expects)</li> <li><code class="language-plaintext highlighter-rouge">StrOutputParser</code> — converts the raw message object from the LLM into a plain Python string</li> <li><code class="language-plaintext highlighter-rouge">RunnableParallel</code> — the key component that executes multiple chains <em>simultaneously</em></li> <li><code class="language-plaintext highlighter-rouge">RunnablePassthrough</code> — passes the input through unchanged, so downstream steps can still access the original value</li> </ul> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4o-mini</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mf">0.7</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><code class="language-plaintext highlighter-rouge">temperature=0.7</code> — a mid-range value. Lower (0.0) makes outputs deterministic and consistent; higher (1.0) adds creativity. For research summaries we want some flexibility, so 0.7 is appropriate.</p> </blockquote> <h3 id="defining-three-independent-chains">Defining Three Independent Chains</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">summarize_chain</span> <span class="o">=</span> <span class="p">(</span>
    <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_messages</span><span class="p">([</span>
        <span class="p">(</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Summarize the following topic concisely:</span><span class="sh">"</span><span class="p">),</span>
        <span class="p">(</span><span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span>   <span class="sh">"</span><span class="s">{topic}</span><span class="sh">"</span><span class="p">)</span>
    <span class="p">])</span>
    <span class="o">|</span> <span class="n">llm</span>
    <span class="o">|</span> <span class="nc">StrOutputParser</span><span class="p">()</span>
<span class="p">)</span>

<span class="n">questions_chain</span> <span class="o">=</span> <span class="p">(</span>
    <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_messages</span><span class="p">([</span>
        <span class="p">(</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Generate three interesting questions about the following topic:</span><span class="sh">"</span><span class="p">),</span>
        <span class="p">(</span><span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span>   <span class="sh">"</span><span class="s">{topic}</span><span class="sh">"</span><span class="p">)</span>
    <span class="p">])</span>
    <span class="o">|</span> <span class="n">llm</span>
    <span class="o">|</span> <span class="nc">StrOutputParser</span><span class="p">()</span>
<span class="p">)</span>

<span class="n">terms_chain</span> <span class="o">=</span> <span class="p">(</span>
    <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_messages</span><span class="p">([</span>
        <span class="p">(</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Identify 5–10 key terms from the following topic, separated by commas:</span><span class="sh">"</span><span class="p">),</span>
        <span class="p">(</span><span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span>   <span class="sh">"</span><span class="s">{topic}</span><span class="sh">"</span><span class="p">)</span>
    <span class="p">])</span>
    <span class="o">|</span> <span class="n">llm</span>
    <span class="o">|</span> <span class="nc">StrOutputParser</span><span class="p">()</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p>Each chain is a complete pipeline: <code class="language-plaintext highlighter-rouge">prompt → LLM → string output</code>. They all take <code class="language-plaintext highlighter-rouge">{topic}</code> as input and return a string. None of them depend on each other’s output — which makes them perfect candidates for parallel execution.</p> </blockquote> <h3 id="building-the-parallel-block">Building the Parallel Block</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">map_chain</span> <span class="o">=</span> <span class="nc">RunnableParallel</span><span class="p">({</span>
    <span class="sh">"</span><span class="s">summary</span><span class="sh">"</span><span class="p">:</span>    <span class="n">summarize_chain</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">questions</span><span class="sh">"</span><span class="p">:</span>  <span class="n">questions_chain</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">key_terms</span><span class="sh">"</span><span class="p">:</span>  <span class="n">terms_chain</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">topic</span><span class="sh">"</span><span class="p">:</span>      <span class="nc">RunnablePassthrough</span><span class="p">(),</span>   <span class="c1"># ← keep the original input available downstream
</span><span class="p">})</span>
</code></pre></div></div> <blockquote> <p><strong>How <code class="language-plaintext highlighter-rouge">RunnableParallel</code> works:</strong> When you call <code class="language-plaintext highlighter-rouge">map_chain.invoke("space exploration")</code>:</p> <ol> <li>LangChain sends <code class="language-plaintext highlighter-rouge">"space exploration"</code> to <code class="language-plaintext highlighter-rouge">summarize_chain</code>, <code class="language-plaintext highlighter-rouge">questions_chain</code>, <code class="language-plaintext highlighter-rouge">terms_chain</code>, and <code class="language-plaintext highlighter-rouge">RunnablePassthrough()</code> <strong>at the same time</strong></li> <li>Each chain runs concurrently (as async tasks in the event loop)</li> <li>Once all four return, <code class="language-plaintext highlighter-rouge">RunnableParallel</code> packages their outputs into a single dictionary: <code class="language-plaintext highlighter-rouge">{"summary": "...", "questions": "...", "key_terms": "...", "topic": "space exploration"}</code></li> </ol> <p><strong>Why <code class="language-plaintext highlighter-rouge">RunnablePassthrough()</code>?</strong> The synthesis step needs the original topic text — not just the processed outputs. Without it, the original string would be consumed and discarded by the parallel step. <code class="language-plaintext highlighter-rouge">RunnablePassthrough()</code> passes the input through unchanged so the next step can reference it.</p> </blockquote> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Data flow through map_chain:

Input: "space exploration"
        │
        ├──→ summarize_chain  ──→ "A summary of space exploration..."
        │
        ├──→ questions_chain  ──→ "1. What year... 2. Who... 3. Why..."
        │
        ├──→ terms_chain      ──→ "NASA, Apollo, orbit, rocket..."
        │
        └──→ RunnablePassthrough() ──→ "space exploration"
        
Output: { "summary": ..., "questions": ..., "key_terms": ..., "topic": ... }
</code></pre></div></div> <h3 id="the-synthesis-step">The Synthesis Step</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">synthesis_prompt</span> <span class="o">=</span> <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_messages</span><span class="p">([</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"""</span><span class="s">Based on the following information:
Summary: {summary}
Related Questions: {questions}
Key Terms: {key_terms}

Synthesize a comprehensive answer.</span><span class="sh">"""</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Original topic: {topic}</span><span class="sh">"</span><span class="p">)</span>
<span class="p">])</span>

<span class="n">full_parallel_chain</span> <span class="o">=</span> <span class="n">map_chain</span> <span class="o">|</span> <span class="n">synthesis_prompt</span> <span class="o">|</span> <span class="n">llm</span> <span class="o">|</span> <span class="nc">StrOutputParser</span><span class="p">()</span>
</code></pre></div></div> <blockquote> <p>The <code class="language-plaintext highlighter-rouge">|</code> pipe connects <code class="language-plaintext highlighter-rouge">map_chain</code>’s dictionary output directly into <code class="language-plaintext highlighter-rouge">synthesis_prompt</code>. LangChain automatically fills <code class="language-plaintext highlighter-rouge">{summary}</code>, <code class="language-plaintext highlighter-rouge">{questions}</code>, <code class="language-plaintext highlighter-rouge">{key_terms}</code>, and <code class="language-plaintext highlighter-rouge">{topic}</code> from the dictionary keys. This is why the dictionary keys in <code class="language-plaintext highlighter-rouge">RunnableParallel</code> must match the variable names in the synthesis prompt exactly.</p> </blockquote> <h3 id="running-it-asynchronously">Running It Asynchronously</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">run_parallel_example</span><span class="p">(</span><span class="n">topic</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="n">response</span> <span class="o">=</span> <span class="k">await</span> <span class="n">full_parallel_chain</span><span class="p">.</span><span class="nf">ainvoke</span><span class="p">(</span><span class="n">topic</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">response</span><span class="p">)</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">run_parallel_example</span><span class="p">(</span><span class="sh">"</span><span class="s">The history of space exploration</span><span class="sh">"</span><span class="p">))</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">ainvoke</code> vs <code class="language-plaintext highlighter-rouge">invoke</code>:</strong> <code class="language-plaintext highlighter-rouge">ainvoke</code> is the async version. It allows the event loop to switch between the parallel tasks while they’re waiting for API responses. Using the synchronous <code class="language-plaintext highlighter-rouge">invoke</code> would block the entire thread during each LLM call, serializing the “parallel” chains and defeating the purpose.</p> <p><strong><code class="language-plaintext highlighter-rouge">asyncio.run()</code>:</strong> This is the standard entry point for running async code from a synchronous context (like a script’s <code class="language-plaintext highlighter-rouge">__main__</code> block). It creates an event loop, runs the coroutine, and then closes the loop.</p> </blockquote> <h3 id="full-data-flow">Full Data Flow</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">LANGCHAIN PARALLEL DATA FLOW — one input, four simultaneous chains, one output</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="flex-direction:row;align-items:center;gap:0.75rem;padding:1.1rem 1.25rem;flex-wrap:nowrap;"> <div class="ns-node ns-node-cyan" style="flex-shrink:0;max-width:140px;"> <div class="ns-node-title">Topic String</div> <div class="ns-node-sub">"space exploration"</div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-purple" style="flex-shrink:0;max-width:160px;"> <div class="ns-node-title">RunnableParallel</div> <div class="ns-node-sub">fans out to all chains at once</div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div style="display:flex;flex-direction:column;gap:0.4rem;flex:1;min-width:140px;"> <div class="ns-node"><div class="ns-node-title">summarize_chain</div></div> <div class="ns-node"><div class="ns-node-title">questions_chain</div></div> <div class="ns-node"><div class="ns-node-title">terms_chain</div></div> <div class="ns-node ns-node-dim"><div class="ns-node-title">RunnablePassthrough</div><div class="ns-node-sub">passes original topic through</div></div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-amber" style="flex-shrink:0;max-width:160px;"> <div class="ns-node-title">Merged Dict</div> <div class="ns-node-sub">summary, questions, key_terms, topic</div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div style="display:flex;flex-direction:column;gap:0.4rem;flex-shrink:0;min-width:120px;"> <div class="ns-node"><div class="ns-node-title">synthesis_prompt</div></div> <div class="ns-node"><div class="ns-node-title">LLM</div></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Final Output</div></div> </div> </div> </div> <h2 id="the-google-adk-way-parallelagent">The Google ADK Way: <code class="language-plaintext highlighter-rouge">ParallelAgent</code></h2> <p>The Google ADK takes a different approach. Instead of wiring chains together, you define <strong>agents</strong> and declare their relationships using <code class="language-plaintext highlighter-rouge">ParallelAgent</code> and <code class="language-plaintext highlighter-rouge">SequentialAgent</code>. The framework handles the scheduling.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LlmAgent</span><span class="p">,</span> <span class="n">ParallelAgent</span><span class="p">,</span> <span class="n">SequentialAgent</span>
<span class="kn">from</span> <span class="n">google.adk.tools</span> <span class="kn">import</span> <span class="n">google_search</span>

<span class="n">GEMINI_MODEL</span> <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span>
</code></pre></div></div> <blockquote> <p><strong>Why these imports?</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">LlmAgent</code> — a single agent powered by an LLM. You give it an instruction and optional tools.</li> <li><code class="language-plaintext highlighter-rouge">ParallelAgent</code> — an orchestrator that runs its <code class="language-plaintext highlighter-rouge">sub_agents</code> concurrently, waiting until all complete before proceeding.</li> <li><code class="language-plaintext highlighter-rouge">SequentialAgent</code> — an orchestrator that runs its <code class="language-plaintext highlighter-rouge">sub_agents</code> one after another. Used to chain the <code class="language-plaintext highlighter-rouge">ParallelAgent</code> with the synthesis agent.</li> <li><code class="language-plaintext highlighter-rouge">google_search</code> — a built-in ADK tool that gives agents access to live web search.</li> </ul> </blockquote> <h3 id="three-researcher-agents-the-parallel-workers">Three Researcher Agents (the parallel workers)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">researcher_agent_1</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">RenewableEnergyResearcher</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="n">GEMINI_MODEL</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">You are a research assistant specializing in energy.
Research the latest advancements in </span><span class="sh">'</span><span class="s">renewable energy sources</span><span class="sh">'</span><span class="s">.
Use the Google Search tool provided.
Summarize your key findings concisely (1–2 sentences).
Output *only* the summary.</span><span class="sh">"""</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Researches renewable energy sources.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span>       <span class="o">=</span> <span class="p">[</span><span class="n">google_search</span><span class="p">],</span>
    <span class="n">output_key</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">renewable_energy_result</span><span class="sh">"</span><span class="p">,</span>   <span class="c1"># ← stores result in session state
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why docstring-style instructions?</strong> The ADK uses the <code class="language-plaintext highlighter-rouge">instruction</code> field as the agent’s <em>system prompt</em>. Being explicit about:</p> <ul> <li>What tool to use (<code class="language-plaintext highlighter-rouge">Use the Google Search tool</code>)</li> <li>How much to write (<code class="language-plaintext highlighter-rouge">1–2 sentences</code>)</li> <li>What to output (<code class="language-plaintext highlighter-rouge">Output *only* the summary</code>) …prevents the agent from adding preamble, caveats, or asking clarifying questions.</li> </ul> <p><strong>Why <code class="language-plaintext highlighter-rouge">output_key</code>?</strong> This is how parallel agents share results. When <code class="language-plaintext highlighter-rouge">researcher_agent_1</code> finishes, it stores its output string into the session state under the key <code class="language-plaintext highlighter-rouge">"renewable_energy_result"</code>. The synthesis agent can then read from <code class="language-plaintext highlighter-rouge">{renewable_energy_result}</code> in its instruction template. Without <code class="language-plaintext highlighter-rouge">output_key</code>, the parallel agents’ outputs would be lost.</p> </blockquote> <p>Researchers 2 and 3 are identical in structure, covering EV technology (<code class="language-plaintext highlighter-rouge">output_key="ev_technology_result"</code>) and carbon capture (<code class="language-plaintext highlighter-rouge">output_key="carbon_capture_result"</code>).</p> <h3 id="the-parallelagent-runs-all-three-at-once">The ParallelAgent (runs all three at once)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">parallel_research_agent</span> <span class="o">=</span> <span class="nc">ParallelAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">ParallelWebResearchAgent</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">sub_agents</span>  <span class="o">=</span> <span class="p">[</span><span class="n">researcher_agent_1</span><span class="p">,</span> <span class="n">researcher_agent_2</span><span class="p">,</span> <span class="n">researcher_agent_3</span><span class="p">],</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Runs multiple research agents in parallel to gather information.</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p>This is the entire parallelization mechanism in ADK — just declare <code class="language-plaintext highlighter-rouge">sub_agents</code> inside a <code class="language-plaintext highlighter-rouge">ParallelAgent</code>. The framework:</p> <ol> <li>Starts all three <code class="language-plaintext highlighter-rouge">LlmAgent</code>s concurrently</li> <li>Each agent performs its search and writes its result to session state via <code class="language-plaintext highlighter-rouge">output_key</code></li> <li><code class="language-plaintext highlighter-rouge">ParallelAgent</code> completes once <strong>all</strong> sub-agents have finished</li> </ol> <p>No async code, no event loop management, no callback hell — the framework handles all of it.</p> </blockquote> <h3 id="the-synthesis-agent">The Synthesis Agent</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">merger_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">SynthesisAgent</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span> <span class="o">=</span> <span class="n">GEMINI_MODEL</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">You are responsible for combining research findings into a structured report.

**Input Summaries:**
* Renewable Energy: {renewable_energy_result}
* Electric Vehicles: {ev_technology_result}
* Carbon Capture:   {carbon_capture_result}

**CRITICAL RULE:** Base your entire response *exclusively* on the Input Summaries above.
Do NOT add external knowledge not present in these summaries.

**Output Format:**
## Summary of Recent Sustainable Technology Advancements

### Renewable Energy Findings
[Synthesize only the renewable energy input summary]

### Electric Vehicle Findings
[Synthesize only the EV input summary]

### Carbon Capture Findings
[Synthesize only the carbon capture input summary]

### Overall Conclusion
[1–2 sentences connecting only the findings above]

Output *only* the structured report.</span><span class="sh">"""</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Combines research findings into a structured, cited report.</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">{renewable_energy_result}</code> in the instruction?</strong> The ADK automatically fills these <code class="language-plaintext highlighter-rouge">{key}</code> placeholders from the session state. Since the three researcher agents stored their outputs under exactly these keys, the synthesis agent receives all three summaries injected directly into its prompt.</p> <p><strong>Why the “CRITICAL RULE”?</strong> Without it, LLMs will use their pre-trained world knowledge to supplement the research, making the output non-deterministic and potentially inconsistent with what was actually found in the search. The explicit constraint forces the agent to stay grounded.</p> </blockquote> <h3 id="the-sequentialagent-orchestrates-everything">The SequentialAgent (orchestrates everything)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">sequential_pipeline_agent</span> <span class="o">=</span> <span class="nc">SequentialAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">ResearchAndSynthesisPipeline</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">sub_agents</span>  <span class="o">=</span> <span class="p">[</span><span class="n">parallel_research_agent</span><span class="p">,</span> <span class="n">merger_agent</span><span class="p">],</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Coordinates parallel research and synthesizes the results.</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">root_agent</span> <span class="o">=</span> <span class="n">sequential_pipeline_agent</span>
</code></pre></div></div> <blockquote> <p>The <code class="language-plaintext highlighter-rouge">SequentialAgent</code> runs <code class="language-plaintext highlighter-rouge">parallel_research_agent</code> first (which internally runs the three researchers in parallel), waits for it to complete, then runs <code class="language-plaintext highlighter-rouge">merger_agent</code>. This gives you <strong>parallelism where possible, sequencing where necessary</strong> — exactly the right structure for fan-out / fan-in workflows.</p> </blockquote> <h3 id="adk-orchestration-flow">ADK Orchestration Flow</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ADK ORCHESTRATION FLOW</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:200px;"><div class="ns-node-title">User Input</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:200px;"><div class="ns-node-title">SequentialAgent</div><div class="ns-node-sub">orchestrates the whole flow</div></div> <div class="ns-arrow"></div> <div class="ns-phase" style="max-width:480px;"> <div class="ns-phase-title">ParallelAgent — all three fire simultaneously</div> <div class="ns-phase-sub">output_key writes each result to Session State</div> <div class="ns-row"> <div class="ns-node ns-node-cyan"><div class="ns-node-title">Renewable Energy</div><div class="ns-node-sub">→ renewable_energy_result</div></div> <div class="ns-node ns-node-cyan"><div class="ns-node-title">EV Researcher</div><div class="ns-node-sub">→ ev_technology_result</div></div> <div class="ns-node ns-node-cyan"><div class="ns-node-title">Carbon Capture</div><div class="ns-node-sub">→ carbon_capture_result</div></div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:260px;"><div class="ns-node-title">Session State</div><div class="ns-node-sub">fills template placeholders in Synthesis Agent's instruction</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:200px;"><div class="ns-node-title">Synthesis Agent</div><div class="ns-node-sub">merges all results into final report</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:200px;"><div class="ns-node-title">Structured Report</div></div> </div> </div> <h2 id="side-by-side-langchain-vs-adk">Side by Side: LangChain vs ADK</h2> <table> <thead> <tr> <th> </th> <th>LangChain (LCEL)</th> <th>Google ADK</th> </tr> </thead> <tbody> <tr> <td><strong>Parallelism primitive</strong></td> <td><code class="language-plaintext highlighter-rouge">RunnableParallel</code> dict</td> <td><code class="language-plaintext highlighter-rouge">ParallelAgent</code></td> </tr> <tr> <td><strong>Sequencing primitive</strong></td> <td><code class="language-plaintext highlighter-rouge">\|</code> pipe operator</td> <td><code class="language-plaintext highlighter-rouge">SequentialAgent</code></td> </tr> <tr> <td><strong>How results are shared</strong></td> <td>Dict keys flow through the pipeline</td> <td><code class="language-plaintext highlighter-rouge">output_key</code> writes to session state</td> </tr> <tr> <td><strong>Async model</strong></td> <td><code class="language-plaintext highlighter-rouge">asyncio</code> via <code class="language-plaintext highlighter-rouge">ainvoke</code> / <code class="language-plaintext highlighter-rouge">astream</code></td> <td>Managed by ADK framework</td> </tr> <tr> <td><strong>Code verbosity</strong></td> <td>Lower — functional chain composition</td> <td>Higher — agent class definitions</td> </tr> <tr> <td><strong>Observability</strong></td> <td>LangSmith tracing</td> <td>ADK built-in tracing</td> </tr> <tr> <td><strong>Best for</strong></td> <td>Tight, composable chains where you control the data flow</td> <td>Multi-agent systems where agents are independent workers</td> </tr> </tbody> </table> <p>The fundamental difference: <strong>LangChain is data-flow</strong> (inputs pipe through transforms), <strong>ADK is agent-flow</strong> (agents communicate via shared state). Both achieve parallelism, but the mental model is different.</p> <h2 id="at-a-glance">At a Glance</h2> <div class="par-summary-card"> <div class="par-summary-col"> <div class="par-summary-label">WHAT</div> <p>Independent tasks that don't need each other's output are executed simultaneously instead of one at a time.</p> </div> <div class="par-summary-divider"></div> <div class="par-summary-col"> <div class="par-summary-label">WHY</div> <p>Sequential execution adds all latencies together. Parallel execution takes only the longest. For I/O-bound work (API calls, LLM requests), this is a 2–5× speedup with zero additional cost.</p> </div> <div class="par-summary-divider"></div> <div class="par-summary-col"> <div class="par-summary-label">RULE OF THUMB</div> <p>Use when a workflow contains multiple independent lookups, computations, or content-generation tasks that each produce a piece of a larger whole.</p> </div> </div> <style>.par-summary-card{display:flex;gap:0;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.par-summary-card{flex-direction:column}}.par-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.par-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.par-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.par-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}</style> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li><strong>The core rule:</strong> Tasks that don’t depend on each other’s output can run in parallel. Tasks that do must remain sequential.</li> <li><strong>The gain:</strong> For I/O-bound work (LLM calls, API requests, database queries), parallelism reduces total time from <code class="language-plaintext highlighter-rouge">sum of all durations</code> to <code class="language-plaintext highlighter-rouge">max of parallel durations + sequential tail</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">asyncio</code> is concurrency, not CPU parallelism.</strong> It works by filling idle network-wait time with other tasks. This is exactly what agentic workflows need.</li> <li><strong>LangChain uses <code class="language-plaintext highlighter-rouge">RunnableParallel</code></strong> — wrap a dictionary of chains and the LCEL runtime fires them all concurrently, collecting results into a dict for the next step.</li> <li><strong>ADK uses <code class="language-plaintext highlighter-rouge">ParallelAgent</code></strong> — declare sub-agents in a <code class="language-plaintext highlighter-rouge">ParallelAgent</code>, use <code class="language-plaintext highlighter-rouge">output_key</code> to write results to session state, and a downstream synthesis agent reads from state via <code class="language-plaintext highlighter-rouge">{key}</code> placeholders in its instruction.</li> <li><strong>The synthesis step is always sequential.</strong> Parallelization is a fan-out / fan-in pattern: spread out, work in parallel, reconverge.</li> <li><strong>Added complexity is real.</strong> Parallel workflows are harder to debug, log, and reason about than sequential ones. Use it when the latency gain is significant — not as a default architecture.</li> </ul> <p><em>Next up — Chapter 4: Orchestration, where we combine chaining, routing, and parallelization into full multi-agent systems.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="parallelization"/><summary type="html"><![CDATA[Sequential is clean. Parallel is fast. The art is knowing which tasks can run at the same time — and wiring the plumbing to make it happen.]]></summary></entry><entry><title type="html">Chapter 2: Routing</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/routing/" rel="alternate" type="text/html" title="Chapter 2: Routing"/><published>2026-01-09T00:00:00+00:00</published><updated>2026-01-09T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/routing</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/routing/"><![CDATA[<h2 id="the-limitation-of-chains">The Limitation of Chains</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Intent classification:</strong> Figuring out what a user actually *wants* from their message. "My package hasn't arrived" → intent is "delivery complaint". This is the first step in any routing system.</p> <p style="margin-top:0.5rem"><strong>Embeddings / Vectors:</strong> A way to convert text into a list of numbers (a vector) that captures its *meaning*. Similar sentences get similar vectors. This lets us find "semantically related" content without exact keyword matching. e.g., "what's the weather?" and "is it raining?" would have similar vectors even though they share no words.</p> <p style="margin-top:0.5rem"><strong>Cosine similarity:</strong> A math formula that measures how similar two vectors are. Two identical vectors = similarity of 1. Completely unrelated vectors ≈ 0. Used in embedding-based routing to find the closest-matching route.</p> <p style="margin-top:0.5rem"><strong>Docstring:</strong> A comment at the top of a Python function (inside triple quotes) that explains what it does. In LangChain and ADK, the LLM <em>reads</em> the docstring to decide when to call that function — so the docstring is actually part of the interface.</p> </div> <p>In <a href="/kohshh-portfolio/blog/2026/prompt-chaining/">Chapter 1</a>, we saw how breaking a task into sequential steps makes LLMs more reliable. Every step has one job. Output of step N feeds step N+1. Clean, predictable.</p> <p>But here’s the problem: <strong>what if you don’t know which sequence to run until you see the input?</strong></p> <p>Imagine a customer support bot. Every incoming message is different:</p> <ul> <li><em>“Where’s my order?”</em> → check the database</li> <li><em>“Your product broke after one use”</em> → escalate to a human</li> <li><em>“How do I reset my password?”</em> → search the knowledge base</li> <li><em>“blah blah blah gibberish”</em> → ask for clarification</li> </ul> <p>You can’t write a fixed chain for this. The right action depends on what the user actually said. You need the system to <em>decide</em> before it <em>acts</em>.</p> <p>That’s <strong>routing</strong>.</p> <h2 id="what-routing-is">What Routing Is</h2> <p>Routing adds <strong>conditional logic</strong> to an agent’s execution. Instead of always following the same path, the agent first evaluates the input — then chooses which path to take.</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ROUTING PATTERN</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">User Query</div> <div class="ns-node-sub">Unclassified input arrives</div> </div> <div class="ns-arrow"></div> <div class="ns-decision"> <div class="ns-node-title">Router</div> <div class="ns-node-sub">Classifies intent</div> </div> <div class="ns-arrow"></div> <div class="ns-row"> <div class="ns-node"> <div class="ns-node-title">Database Agent</div> <div class="ns-node-sub">Order status</div> </div> <div class="ns-node ns-node-red"> <div class="ns-node-title">Escalation</div> <div class="ns-node-sub">Complaint → human</div> </div> <div class="ns-node"> <div class="ns-node-title">Knowledge Base</div> <div class="ns-node-sub">How-to questions</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">Clarify</div> <div class="ns-node-sub">Unclear input</div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Response</div> <div class="ns-node-sub">Right handler, right answer</div> </div> </div> </div> <p>The router is the decision-maker. Everything downstream is a <strong>handler</strong> — a function, tool, sub-agent, or prompt chain that handles one specific type of request.</p> <blockquote> <p><strong>The key insight:</strong> Routing separates <em>what to do</em> from <em>how to do it</em>. The router decides. The handler executes. Neither knows about the other’s internals.</p> </blockquote> <h2 id="the-four-types-of-routing">The Four Types of Routing</h2> <p>Not all routers work the same way. There are four distinct mechanisms — each with different trade-offs in speed, flexibility, and accuracy.</p> <div class="routing-compare-wrapper"> <div class="routing-compare-header"> <span class="routing-compare-title">ROUTING METHOD COMPARISON</span> <span class="routing-compare-sub">Hover a bar to see details</span> </div> <div class="routing-compare-chart-area"> <canvas id="routingCompareChart" width="680" height="320"></canvas> </div> <div class="routing-compare-legend" id="routingLegend"> <div class="rcl-item"><span class="rcl-dot" style="background:#2698ba"></span> Flexibility</div> <div class="rcl-item"><span class="rcl-dot" style="background:#4fc97e"></span> Speed</div> <div class="rcl-item"><span class="rcl-dot" style="background:#e6a817"></span> Novel-input accuracy</div> <div class="rcl-item"><span class="rcl-dot" style="background:#c97af2"></span> Cost-efficiency</div> </div> <div class="routing-compare-tooltip" id="rcTooltip" style="display:none"></div> </div> <style>.routing-compare-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0;position:relative}.routing-compare-header{padding:.85rem 1.25rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.routing-compare-title{font-size:.7rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color);display:block}.routing-compare-sub{font-size:.65rem;color:var(--global-text-color-light);opacity:.7}.routing-compare-chart-area{padding:.75rem;overflow-x:auto}#routingCompareChart{display:block;max-width:100%}.routing-compare-legend{display:flex;gap:1.25rem;padding:.6rem 1.25rem;border-top:1px solid var(--global-divider-color);flex-wrap:wrap;background:rgba(128,128,128,0.03)}.rcl-item{display:flex;align-items:center;gap:.4rem;font-size:.72rem;color:var(--global-text-color-light)}.rcl-dot{width:9px;height:9px;border-radius:2px;flex-shrink:0}.routing-compare-tooltip{position:fixed;background:var(--global-bg-color);border:1px solid var(--global-divider-color);border-radius:6px;padding:.5rem .75rem;font-size:.75rem;color:var(--global-text-color);pointer-events:none;z-index:100;max-width:220px;line-height:1.5;box-shadow:0 4px 16px rgba(0,0,0,0.3)}</style> <script>
(function(){
  var methods = ['LLM-based','Rule-based','Embedding-based','ML classifier'];
  var dims = ['Flexibility','Speed','Novel accuracy','Cost efficiency'];
  var colors = ['#2698ba','#4fc97e','#e6a817','#c97af2'];
  // scores[method][dim]: 1-5
  var scores = [
    [5, 2, 4, 2], // LLM-based
    [2, 5, 1, 5], // Rule-based
    [4, 4, 4, 3], // Embedding-based
    [3, 4, 3, 3], // ML classifier
  ];
  var descriptions = [
    'The LLM itself classifies the input at runtime. Handles novel and nuanced queries well. Slow and expensive per-call. Best when flexibility matters most.',
    'Explicit if/else on keywords or regex patterns. Blazing fast, zero API cost. Breaks on anything outside the predefined rules.',
    'Converts input to a vector and finds the nearest route by cosine similarity. Fast after setup, handles semantic variation well. Needs embeddings infra.',
    'A small classifier fine-tuned on labeled examples. Fast at inference, very accurate for known categories. Requires training data and retraining to extend.',
  ];

  document.addEventListener('DOMContentLoaded', function(){
    var canvas = document.getElementById('routingCompareChart');
    if (!canvas) return;
    var ctx = canvas.getContext('2d');
    var tooltip = document.getElementById('rcTooltip');

    var dpr = window.devicePixelRatio || 1;
    var W = Math.min(680, canvas.parentElement.getBoundingClientRect().width - 16);
    var H = 320;
    canvas.width = W * dpr; canvas.height = H * dpr;
    canvas.style.width = W+'px'; canvas.style.height = H+'px';
    ctx.scale(dpr, dpr);

    var ML = 110, MR = 20, MT = 30, MB = 44;
    var PW = W - ML - MR, PH = H - MT - MB;
    var barGroups = methods.length, nDims = dims.length;
    var groupW = PW / barGroups;
    var barW = (groupW * 0.7) / nDims;
    var barGap = (groupW * 0.3) / (nDims + 1);

    var hitRects = [];

    function getTheme() {
      var s = getComputedStyle(document.documentElement);
      return {
        text:  s.getPropertyValue('--global-text-color').trim()       || '#e0e0e0',
        muted: s.getPropertyValue('--global-text-color-light').trim() || '#888',
        div:   s.getPropertyValue('--global-divider-color').trim()    || '#333',
      };
    }

    function drawChart(hoverGroup, hoverDim) {
      ctx.clearRect(0, 0, W, H);
      hitRects = [];
      var th = getTheme();

      // Y grid + labels
      ctx.font = '10px monospace';
      [1,2,3,4,5].forEach(function(v){
        var y = MT + (1-(v-1)/4)*PH;
        ctx.strokeStyle = th.div; ctx.lineWidth = 0.5;
        ctx.setLineDash([3,3]);
        ctx.beginPath(); ctx.moveTo(ML,y); ctx.lineTo(ML+PW,y); ctx.stroke();
        ctx.setLineDash([]);
        ctx.fillStyle = th.muted; ctx.textAlign = 'right';
        ctx.fillText(v, ML-6, y+3.5);
      });

      // Y axis label
      ctx.save(); ctx.translate(12, MT+PH/2); ctx.rotate(-Math.PI/2);
      ctx.textAlign = 'center'; ctx.fillStyle = th.muted;
      ctx.font = '10px monospace'; ctx.fillText('score (1–5)', 0, 0); ctx.restore();

      // Bars
      methods.forEach(function(method, gi){
        var gx = ML + gi * groupW;
        dims.forEach(function(dim, di){
          var bx = gx + barGap*(di+1) + barW*di;
          var score = scores[gi][di];
          var barH = (score-1)/4 * PH;
          var by = MT + PH - barH;
          var isHover = (hoverGroup===gi && hoverDim===di);
          var alpha = (hoverGroup===null) ? 1 : (isHover ? 1 : 0.3);
          ctx.globalAlpha = alpha;
          ctx.fillStyle = colors[di];
          ctx.beginPath();
          if (ctx.roundRect) {
            ctx.roundRect(bx, by, barW, barH, [3,3,0,0]);
          } else {
            ctx.rect(bx, by, barW, barH);
          }
          ctx.fill();
          ctx.globalAlpha = 1;
          hitRects.push({x:bx,y:by,w:barW,h:barH,gi:gi,di:di,score:score,method:method,dim:dim});
        });
        // Group label
        ctx.fillStyle = th.muted; ctx.textAlign = 'center';
        ctx.font = '10px monospace';
        ctx.fillText(method, gx + groupW/2, H - MB + 16);
      });
    }

    drawChart(null, null);

    canvas.addEventListener('mousemove', function(e){
      var rect = canvas.getBoundingClientRect();
      var mx = (e.clientX - rect.left) * (W / rect.width);
      var my = (e.clientY - rect.top)  * (H / rect.height);
      var found = null;
      hitRects.forEach(function(r){ if(mx>=r.x && mx<=r.x+r.w && my>=r.y && my<=r.y+r.h) found=r; });
      if (found) {
        drawChart(found.gi, found.di);
        tooltip.style.display = 'block';
        tooltip.style.left = (e.clientX + 12) + 'px';
        tooltip.style.top  = (e.clientY - 10) + 'px';
        tooltip.innerHTML = '<strong>' + found.method + '</strong><br>' + found.dim + ': ' + found.score + '/5<br><small style="opacity:0.7">' + descriptions[found.gi] + '</small>';
      } else {
        drawChart(null, null);
        tooltip.style.display = 'none';
      }
    });
    canvas.addEventListener('mouseleave', function(){
      drawChart(null,null); tooltip.style.display='none';
    });
  });
})();
</script> <p><strong>Quick summary of each method:</strong></p> <p><strong>LLM-based routing</strong> — You ask the LLM itself: <em>“Read this query. Output exactly one word: <code class="language-plaintext highlighter-rouge">booking</code>, <code class="language-plaintext highlighter-rouge">info</code>, or <code class="language-plaintext highlighter-rouge">unclear</code>.”</em> The most flexible approach. Handles nuanced or unusual inputs. Trade-off: one extra API call per request, which adds latency and cost.</p> <p><strong>Rule-based routing</strong> — Pure code. <code class="language-plaintext highlighter-rouge">if "flight" in query or "hotel" in query → booking</code>. Zero API cost, sub-millisecond speed. Falls apart the moment a user says something you didn’t explicitly anticipate.</p> <p><strong>Embedding-based routing</strong> — Convert the query into a vector (a list of numbers capturing its meaning). Compare to pre-computed vectors for each route. Route to the closest match. Handles semantic variation well (<em>“get me a room”</em> matches <em>“book a hotel”</em>). Needs embedding infrastructure.</p> <p><strong>ML classifier routing</strong> — A small discriminative model fine-tuned on labelled examples. <em>“Here are 500 examples of booking requests, 500 info requests…”</em> Fast at inference, very accurate for known categories. Needs training data, and retraining every time you add a new route.</p> <h2 id="how-it-works-step-by-step">How It Works: Step by Step</h2> <p>Let’s walk through exactly what happens when a routing system processes a request.</p> <div class="routing-demo-wrapper"> <div class="routing-demo-header"> <span class="routing-demo-label">INTERACTIVE — click a query to route it</span> </div> <div class="routing-demo-queries"> <button class="rq-btn" data-query="Book me a flight to Tokyo next Friday." data-route="booking">✈ Book flight to Tokyo</button> <button class="rq-btn" data-query="What is the capital of France?" data-route="info">? Capital of France</button> <button class="rq-btn" data-query="I need help with something important." data-route="unclear">⚠ Vague request</button> <button class="rq-btn" data-query="Find me a hotel in Paris for 3 nights." data-route="booking">🏨 Hotel in Paris</button> </div> <div class="routing-demo-pipeline" id="rdPipeline"> <div class="rdp-node rdp-node--input" id="rdInput"> <div class="rdp-node-label">QUERY</div> <div class="rdp-node-text" id="rdInputText">Select a query above</div> </div> <div class="rdp-arrow rdp-arrow--down" id="rdArrow1"> <div class="rdp-arrow-line"></div> <div class="rdp-arrow-tip">↓</div> <div class="rdp-arrow-label">LLM analyses intent</div> </div> <div class="rdp-node" id="rdRouter"> <div class="rdp-node-label">ROUTER</div> <div class="rdp-node-text" id="rdRouterText">Waiting…</div> </div> <div class="rdp-arrow rdp-arrow--down" id="rdArrow2"> <div class="rdp-arrow-line"></div> <div class="rdp-arrow-tip">↓</div> <div class="rdp-arrow-label">routes to correct handler</div> </div> <div class="rdp-handlers"> <div class="rdp-handler" id="rdH-booking"> <div class="rdp-handler-icon">✈</div> <div class="rdp-handler-name">Booking Agent</div> <div class="rdp-handler-desc">Interacts with flight/hotel APIs</div> </div> <div class="rdp-handler" id="rdH-info"> <div class="rdp-handler-icon">📚</div> <div class="rdp-handler-name">Info Agent</div> <div class="rdp-handler-desc">Searches knowledge base</div> </div> <div class="rdp-handler" id="rdH-unclear"> <div class="rdp-handler-icon">❓</div> <div class="rdp-handler-name">Clarifier</div> <div class="rdp-handler-desc">Asks for more information</div> </div> </div> </div> <div class="routing-demo-status" id="rdStatus">Pick a query to see how it flows through the router.</div> </div> <style>.routing-demo-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.routing-demo-header{padding:.75rem 1.25rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);font-size:.65rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:var(--global-text-color-light)}.routing-demo-queries{display:flex;flex-wrap:wrap;gap:.5rem;padding:1rem 1.25rem;border-bottom:1px solid var(--global-divider-color)}.rq-btn{background:rgba(128,128,128,0.08);border:1px solid var(--global-divider-color);border-radius:6px;padding:.4rem .85rem;font-size:.78rem;color:var(--global-text-color-light);cursor:pointer;transition:border-color .2s,color .2s}.rq-btn:hover,.rq-btn.rq-btn--active{border-color:var(--global-theme-color);color:var(--global-theme-color)}.routing-demo-pipeline{display:flex;flex-direction:column;align-items:center;padding:1.5rem 1.25rem 1rem;gap:0}.rdp-node{width:100%;max-width:480px;border:1px solid var(--global-divider-color);border-radius:8px;padding:.85rem 1.1rem;transition:border-color .3s,box-shadow .3s}.rdp-node--input{background:rgba(128,128,128,0.04)}.rdp-node.rdp-active{border-color:var(--global-theme-color);box-shadow:0 0 0 1px var(--global-theme-color)}.rdp-node-label{font-size:.58rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color-light);margin-bottom:.4rem}.rdp-node-text{font-size:.875rem;color:var(--global-text-color);min-height:1.2rem}.rdp-arrow{display:flex;flex-direction:column;align-items:center;gap:.1rem;padding:.25rem 0}.rdp-arrow-line{width:2px;height:20px;background:var(--global-divider-color);border-radius:1px;transition:background .3s}.rdp-arrow.rdp-active .rdp-arrow-line{background:var(--global-theme-color)}.rdp-arrow-tip{font-size:.9rem;color:var(--global-text-color-light);transition:color .3s;line-height:1}.rdp-arrow.rdp-active .rdp-arrow-tip{color:var(--global-theme-color)}.rdp-arrow-label{font-size:.6rem;font-weight:600;letter-spacing:.08em;text-transform:uppercase;color:var(--global-text-color-light);opacity:.55;transition:opacity .3s}.rdp-arrow.rdp-active .rdp-arrow-label{opacity:1;color:var(--global-theme-color)}.rdp-handlers{display:flex;gap:.75rem;width:100%;max-width:480px;flex-wrap:wrap}.rdp-handler{flex:1;min-width:120px;border:1px solid var(--global-divider-color);border-radius:8px;padding:.75rem .85rem;text-align:center;transition:border-color .3s,box-shadow .3s,opacity .3s;opacity:.45}.rdp-handler.rdp-handler--active{border-color:var(--global-theme-color);box-shadow:0 0 0 1px var(--global-theme-color);opacity:1}.rdp-handler-icon{font-size:1.2rem;margin-bottom:.3rem}.rdp-handler-name{font-size:.78rem;font-weight:700;color:var(--global-text-color);margin-bottom:.2rem}.rdp-handler-desc{font-size:.7rem;color:var(--global-text-color-light);line-height:1.3}.routing-demo-status{padding:.65rem 1.25rem;border-top:1px solid var(--global-divider-color);font-size:.78rem;color:var(--global-text-color-light);background:rgba(128,128,128,0.04)}</style> <script>
(function(){
  var routerMessages = {
    booking: 'Output: <strong style="color:var(--global-theme-color)">"booker"</strong> — intent is flight or hotel booking',
    info:    'Output: <strong style="color:var(--global-theme-color)">"info"</strong> — intent is a factual question',
    unclear: 'Output: <strong style="color:var(--global-theme-color)">"unclear"</strong> — intent is ambiguous',
  };
  var statusMessages = {
    booking: 'Routed to <strong>Booking Agent</strong> — will call flight/hotel APIs to fulfil the request.',
    info:    'Routed to <strong>Info Agent</strong> — will search the knowledge base and return an answer.',
    unclear: 'Routed to <strong>Clarifier</strong> — will ask the user for more detail before proceeding.',
  };

  function resetAll() {
    document.getElementById('rdInput').classList.remove('rdp-active');
    document.getElementById('rdRouter').classList.remove('rdp-active');
    document.getElementById('rdArrow1').classList.remove('rdp-active');
    document.getElementById('rdArrow2').classList.remove('rdp-active');
    document.getElementById('rdRouterText').textContent = 'Waiting…';
    document.querySelectorAll('.rdp-handler').forEach(function(h){ h.classList.remove('rdp-handler--active'); });
    document.querySelectorAll('.rq-btn').forEach(function(b){ b.classList.remove('rq-btn--active'); });
  }

  function animateRoute(query, route, btn) {
    resetAll();
    btn.classList.add('rq-btn--active');

    var inputNode = document.getElementById('rdInput');
    var routerNode = document.getElementById('rdRouter');
    var arrow1 = document.getElementById('rdArrow1');
    var arrow2 = document.getElementById('rdArrow2');
    var inputText = document.getElementById('rdInputText');
    var routerText = document.getElementById('rdRouterText');
    var status = document.getElementById('rdStatus');

    status.innerHTML = 'Sending query to router…';
    inputText.textContent = '"' + query + '"';
    inputNode.classList.add('rdp-active');

    setTimeout(function(){
      arrow1.classList.add('rdp-active');
      routerText.innerHTML = '<em style="opacity:0.5">Analysing intent…</em>';
    }, 400);

    setTimeout(function(){
      routerNode.classList.add('rdp-active');
      routerText.innerHTML = routerMessages[route];
    }, 900);

    setTimeout(function(){
      arrow2.classList.add('rdp-active');
    }, 1300);

    setTimeout(function(){
      var handler = document.getElementById('rdH-' + route);
      if (handler) handler.classList.add('rdp-handler--active');
      status.innerHTML = statusMessages[route];
    }, 1700);
  }

  document.addEventListener('DOMContentLoaded', function(){
    document.querySelectorAll('.rq-btn').forEach(function(btn){
      btn.addEventListener('click', function(){
        animateRoute(btn.dataset.query, btn.dataset.route, btn);
      });
    });
  });
})();
</script> <h2 id="practical-applications">Practical Applications</h2> <h3 id="customer-service-bots">Customer Service Bots</h3> <p>The most obvious use case. A single entry point receives all user messages. The router classifies intent and dispatches to the right sub-agent:</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">CUSTOMER SERVICE ROUTING</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="flex-direction:row;align-items:center;gap:1rem;padding:1.25rem 1.5rem;flex-wrap:nowrap;"> <div class="ns-node ns-node-cyan" style="flex-shrink:0;"><div class="ns-node-title">Incoming Message</div></div> <div style="font-size:1.5rem;color:#4a5a6a;flex-shrink:0;">→</div> <div class="ns-decision" style="flex-shrink:0;"><div class="ns-node-title">Router</div><div class="ns-node-sub">classifies intent</div></div> <div style="font-size:1.5rem;color:#4a5a6a;flex-shrink:0;">→</div> <div style="display:flex;flex-direction:column;gap:0.5rem;flex:1;min-width:160px;"> <div class="ns-node"><div class="ns-node-title">Order DB Agent</div><div class="ns-node-sub">order status</div></div> <div class="ns-node ns-node-amber"><div class="ns-node-title">Refund Workflow</div><div class="ns-node-sub">refund request</div></div> <div class="ns-node ns-node-purple"><div class="ns-node-title">Troubleshooting Chain</div><div class="ns-node-sub">technical issue</div></div> <div class="ns-node ns-node-red"><div class="ns-node-title">Human Escalation</div><div class="ns-node-sub">complaint</div></div> <div class="ns-node"><div class="ns-node-title">Clarification Prompt</div><div class="ns-node-sub">unclear intent</div></div> </div> </div> </div> <p>Without routing, you’d need separate endpoints for each query type — and users would have to know which one to use.</p> <h3 id="document--email-pipelines">Document &amp; Email Pipelines</h3> <p>Incoming emails get classified before any processing happens:</p> <ul> <li><strong>Sales lead</strong> → CRM ingestion workflow</li> <li><strong>Support ticket</strong> → ticketing system + priority score</li> <li><strong>Invoice</strong> → accounts payable extraction chain</li> <li><strong>Spam</strong> → discard</li> </ul> <p>The router is the first step. Everything after it is specialised.</p> <h3 id="multi-agent-research-systems">Multi-Agent Research Systems</h3> <p>A research system with separate agents for <em>web search</em>, <em>paper summarisation</em>, <em>data analysis</em>, and <em>report writing</em> needs a router to decide which agent gets each sub-task. The router looks at the current objective and dispatches accordingly.</p> <h3 id="ai-coding-assistants">AI Coding Assistants</h3> <p>Before passing a code snippet to any tool, the assistant routes based on <strong>language</strong> × <strong>intent</strong>:</p> <ul> <li>Python + debug → Python linter + error explainer</li> <li>TypeScript + explain → TypeScript-aware explainer</li> <li>SQL + optimise → query plan analyser</li> </ul> <p>Two routing dimensions at once: language detection and intent classification.</p> <h2 id="langchain-implementation">LangChain Implementation</h2> <p>Here’s a working routing system using LangChain and Google’s Gemini. A coordinator LLM classifies the intent, then <code class="language-plaintext highlighter-rouge">RunnableBranch</code> dispatches to the right handler.</p> <h3 id="install">Install</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>langchain langgraph langchain-google-genai python-dotenv
</code></pre></div></div> <h3 id="the-full-code-explained-line-by-line">The Full Code, Explained Line by Line</h3> <p><strong>1. Imports</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_google_genai</span> <span class="kn">import</span> <span class="n">ChatGoogleGenerativeAI</span>
<span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">ChatPromptTemplate</span>
<span class="kn">from</span> <span class="n">langchain_core.output_parsers</span> <span class="kn">import</span> <span class="n">StrOutputParser</span>
<span class="kn">from</span> <span class="n">langchain_core.runnables</span> <span class="kn">import</span> <span class="n">RunnablePassthrough</span><span class="p">,</span> <span class="n">RunnableBranch</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">ChatGoogleGenerativeAI</code> — LangChain’s wrapper around Gemini. Swap for <code class="language-plaintext highlighter-rouge">ChatOpenAI</code> if using OpenAI.</li> <li><code class="language-plaintext highlighter-rouge">ChatPromptTemplate</code> — builds reusable prompts with named placeholders.</li> <li><code class="language-plaintext highlighter-rouge">StrOutputParser</code> — strips the LLM’s response object down to a plain Python string.</li> <li><code class="language-plaintext highlighter-rouge">RunnablePassthrough</code> — passes input through unchanged. Used to keep the original request available downstream.</li> <li><code class="language-plaintext highlighter-rouge">RunnableBranch</code> — the routing mechanism. Takes a list of <code class="language-plaintext highlighter-rouge">(condition, runnable)</code> pairs. Runs the first branch whose condition is <code class="language-plaintext highlighter-rouge">True</code>.</li> </ul> <p><strong>2. Initialise the model</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>
<span class="kn">from</span> <span class="n">dotenv</span> <span class="kn">import</span> <span class="n">load_dotenv</span>
<span class="nf">load_dotenv</span><span class="p">()</span>  <span class="c1"># reads GOOGLE_API_KEY from .env
</span>
<span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatGoogleGenerativeAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
<span class="c1"># temperature=0 → deterministic output
# Critical for routing: you need a consistent, single-word answer every time
</span></code></pre></div></div> <p><strong>3. Define the handler functions</strong></p> <p>These simulate what each specialised sub-agent would actually do:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">booking_handler</span><span class="p">(</span><span class="n">request</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Called when the router decides this is a booking request.</span><span class="sh">"""</span>
    <span class="c1"># In production: call a flight/hotel API, check availability, etc.
</span>    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">→ DELEGATING TO: Booking Handler</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Booking confirmed for: </span><span class="sh">'</span><span class="si">{</span><span class="n">request</span><span class="si">}</span><span class="sh">'"</span>

<span class="k">def</span> <span class="nf">info_handler</span><span class="p">(</span><span class="n">request</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Called when the router decides this is an information request.</span><span class="sh">"""</span>
    <span class="c1"># In production: search a knowledge base, fetch from a database, etc.
</span>    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">→ DELEGATING TO: Info Handler</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Information retrieved for: </span><span class="sh">'</span><span class="si">{</span><span class="n">request</span><span class="si">}</span><span class="sh">'"</span>

<span class="k">def</span> <span class="nf">unclear_handler</span><span class="p">(</span><span class="n">request</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Fallback for anything the router can</span><span class="sh">'</span><span class="s">t classify.</span><span class="sh">"""</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">→ DELEGATING TO: Clarification Handler</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Could not process: </span><span class="sh">'</span><span class="si">{</span><span class="n">request</span><span class="si">}</span><span class="sh">'</span><span class="s">. Please clarify your request.</span><span class="sh">"</span>
</code></pre></div></div> <p><strong>4. Build the router chain</strong></p> <p>This is the classification step — the LLM reads the request and outputs exactly one word:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">router_prompt</span> <span class="o">=</span> <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_messages</span><span class="p">([</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"""</span><span class="s">Analyze the user</span><span class="sh">'</span><span class="s">s request and output the correct category.

Rules:
- If the request involves booking flights or hotels → output: booker
- If the request is a general information question  → output: info
- If the request is unclear or doesn</span><span class="sh">'</span><span class="s">t fit either   → output: unclear

IMPORTANT: Output exactly ONE word. No punctuation, no explanation.</span><span class="sh">"""</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">{request}</span><span class="sh">"</span><span class="p">)</span>
<span class="p">])</span>

<span class="c1"># Chain: fill the prompt → call LLM → strip to plain string
# Result is a single word: "booker", "info", or "unclear"
</span><span class="n">router_chain</span> <span class="o">=</span> <span class="n">router_prompt</span> <span class="o">|</span> <span class="n">llm</span> <span class="o">|</span> <span class="nc">StrOutputParser</span><span class="p">()</span>
</code></pre></div></div> <p>Why <code class="language-plaintext highlighter-rouge">temperature=0</code> matters here: if temperature &gt; 0, the model might output <code class="language-plaintext highlighter-rouge">"Booker"</code> (capitalised), <code class="language-plaintext highlighter-rouge">"booking"</code>, or <code class="language-plaintext highlighter-rouge">"I think this is a booking request..."</code> — all of which break the downstream string comparison.</p> <p><strong>5. Build the routing branch</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># RunnableBranch takes:
#   [(condition_fn, runnable), (condition_fn, runnable), ..., default_runnable]
# It evaluates conditions in order and runs the FIRST matching branch.
# The default (no condition) runs if nothing matches.
</span>
<span class="n">delegation</span> <span class="o">=</span> <span class="nc">RunnableBranch</span><span class="p">(</span>
    <span class="c1"># condition: is the decision "booker"?
</span>    <span class="c1"># x is the dict {"decision": "booker", "request": {...}}
</span>    <span class="p">(</span>
        <span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">decision</span><span class="sh">"</span><span class="p">].</span><span class="nf">strip</span><span class="p">().</span><span class="nf">lower</span><span class="p">()</span> <span class="o">==</span> <span class="sh">"</span><span class="s">booker</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">RunnablePassthrough</span><span class="p">.</span><span class="nf">assign</span><span class="p">(</span>
            <span class="n">output</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="nf">booking_handler</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">][</span><span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">])</span>
        <span class="p">)</span>
    <span class="p">),</span>
    <span class="p">(</span>
        <span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">decision</span><span class="sh">"</span><span class="p">].</span><span class="nf">strip</span><span class="p">().</span><span class="nf">lower</span><span class="p">()</span> <span class="o">==</span> <span class="sh">"</span><span class="s">info</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">RunnablePassthrough</span><span class="p">.</span><span class="nf">assign</span><span class="p">(</span>
            <span class="n">output</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="nf">info_handler</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">][</span><span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">])</span>
        <span class="p">)</span>
    <span class="p">),</span>
    <span class="c1"># Default branch — runs if neither condition matched
</span>    <span class="n">RunnablePassthrough</span><span class="p">.</span><span class="nf">assign</span><span class="p">(</span>
        <span class="n">output</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="nf">unclear_handler</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">][</span><span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">])</span>
    <span class="p">),</span>
<span class="p">)</span>
</code></pre></div></div> <p><strong>6. Combine into a single runnable</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Step 1: run router_chain → get decision
# Step 2: keep original request alongside the decision
# Step 3: delegation branch picks the right handler
# Step 4: extract just the output string
</span>
<span class="n">coordinator</span> <span class="o">=</span> <span class="p">(</span>
    <span class="p">{</span>
        <span class="sh">"</span><span class="s">decision</span><span class="sh">"</span><span class="p">:</span> <span class="n">router_chain</span><span class="p">,</span>       <span class="c1"># ← runs classifier, stores result
</span>        <span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">:</span> <span class="nc">RunnablePassthrough</span><span class="p">()</span> <span class="c1"># ← passes original input through unchanged
</span>    <span class="p">}</span>
    <span class="o">|</span> <span class="n">delegation</span>
    <span class="o">|</span> <span class="p">(</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">[</span><span class="sh">"</span><span class="s">output</span><span class="sh">"</span><span class="p">])</span>           <span class="c1"># ← extract the handler's response string
</span><span class="p">)</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">RunnablePassthrough()</code> here is critical: without it, the <code class="language-plaintext highlighter-rouge">delegation</code> branch would only see the router’s decision (a single word), and couldn’t pass the original request to the handler.</p> <p><strong>7. Run it</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Each invoke() call: classify → route → handle → return
</span>
<span class="nf">print</span><span class="p">(</span><span class="n">coordinator</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Book me a flight to London.</span><span class="sh">"</span><span class="p">}))</span>
<span class="c1"># → DELEGATING TO: Booking Handler
# → "Booking confirmed for: 'Book me a flight to London.'"
</span>
<span class="nf">print</span><span class="p">(</span><span class="n">coordinator</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">What is the capital of Italy?</span><span class="sh">"</span><span class="p">}))</span>
<span class="c1"># → DELEGATING TO: Info Handler
# → "Information retrieved for: 'What is the capital of Italy?'"
</span>
<span class="nf">print</span><span class="p">(</span><span class="n">coordinator</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">request</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Do the thing.</span><span class="sh">"</span><span class="p">}))</span>
<span class="c1"># → DELEGATING TO: Clarification Handler
# → "Could not process: 'Do the thing.'. Please clarify your request."
</span></code></pre></div></div> <h3 id="what-the-data-flow-looks-like">What the Data Flow Looks Like</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">LANGCHAIN DATA FLOW — How input travels through the routing chain</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="flex-direction:row;align-items:center;gap:0.75rem;padding:1.1rem 1.25rem;flex-wrap:nowrap;"> <div class="ns-node ns-node-cyan" style="flex-shrink:0;max-width:180px;"> <div class="ns-node-title">User Request</div> <div class="ns-node-sub">"Book flight to London"</div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-amber" style="flex-shrink:0;max-width:160px;"> <div class="ns-node-title">router_chain</div> <div class="ns-node-sub">LLM outputs: "booker" or "info"</div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-decision" style="flex-shrink:0;"> <div class="ns-node-title">RunnableBranch</div> <div class="ns-node-sub">checks decision string</div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div style="display:flex;flex-direction:column;gap:0.4rem;flex:1;min-width:150px;"> <div class="ns-node ns-node-green"><div class="ns-node-title">booking_handler</div><div class="ns-node-sub">decision == "booker"</div></div> <div class="ns-node ns-node-cyan"><div class="ns-node-title">info_handler</div><div class="ns-node-sub">decision == "info"</div></div> <div class="ns-node"><div class="ns-node-title">unclear_handler</div><div class="ns-node-sub">default fallback</div></div> </div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-green" style="flex-shrink:0;"><div class="ns-node-title">Final Output</div><div class="ns-node-sub">string response to user</div></div> </div> </div> <h2 id="google-adk-implementation">Google ADK Implementation</h2> <p>The Agent Development Kit (ADK) takes a fundamentally different approach. Instead of building explicit routing logic in code, you define <strong>agents with descriptions</strong> and <strong>tools with docstrings</strong>, and let the framework figure out routing.</p> <h3 id="the-philosophy">The Philosophy</h3> <table> <thead> <tr> <th>LangChain approach</th> <th>ADK approach</th> </tr> </thead> <tbody> <tr> <td>You write the routing logic explicitly</td> <td>The framework routes via the LLM’s understanding of agent descriptions</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">RunnableBranch</code> with explicit conditions</td> <td><code class="language-plaintext highlighter-rouge">sub_agents=</code> list — ADK Auto-Flow handles dispatch</td> </tr> <tr> <td>Full control over routing criteria</td> <td>More automatic, less code</td> </tr> <tr> <td>Better for complex conditional logic</td> <td>Better for simple delegation patterns</td> </tr> </tbody> </table> <h3 id="install-1">Install</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>google-adk google-generativeai python-dotenv
</code></pre></div></div> <h3 id="the-full-code-explained">The Full Code, Explained</h3> <p><strong>1. Define tool functions</strong></p> <p>In ADK, a <strong>tool</strong> is just a Python function with a descriptive docstring. The docstring is critical — ADK’s LLM reads it to decide when to call this tool.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.tools</span> <span class="kn">import</span> <span class="n">FunctionTool</span>

<span class="k">def</span> <span class="nf">booking_handler</span><span class="p">(</span><span class="n">request</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Handles booking requests for flights and hotels.
    Call this tool when the user wants to book, reserve, or schedule
    a flight, hotel room, or accommodation.

    Args:
        request: The user</span><span class="sh">'</span><span class="s">s booking request in natural language.
    Returns:
        Confirmation that the booking action was simulated.
    </span><span class="sh">"""</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">→ Booking Handler called</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Booking action simulated for: </span><span class="sh">'</span><span class="si">{</span><span class="n">request</span><span class="si">}</span><span class="sh">'"</span>

<span class="k">def</span> <span class="nf">info_handler</span><span class="p">(</span><span class="n">request</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Handles general information and factual questions.
    Call this tool when the user asks a factual question, wants
    an explanation, or is looking for general knowledge.

    Args:
        request: The user</span><span class="sh">'</span><span class="s">s information request.
    Returns:
        The result of the simulated information retrieval.
    </span><span class="sh">"""</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">→ Info Handler called</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Information retrieved for: </span><span class="sh">'</span><span class="si">{</span><span class="n">request</span><span class="si">}</span><span class="sh">'"</span>

<span class="c1"># Wrap functions as FunctionTool objects
</span><span class="n">booking_tool</span> <span class="o">=</span> <span class="nc">FunctionTool</span><span class="p">(</span><span class="n">booking_handler</span><span class="p">)</span>
<span class="n">info_tool</span>    <span class="o">=</span> <span class="nc">FunctionTool</span><span class="p">(</span><span class="n">info_handler</span><span class="p">)</span>
</code></pre></div></div> <p><strong>2. Define specialised sub-agents</strong></p> <p>Each sub-agent has a <code class="language-plaintext highlighter-rouge">description</code> — used by the coordinator to decide which agent to delegate to:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span>

<span class="n">booking_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Booker</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">description</span><span class="o">=</span><span class="sh">"</span><span class="s">Specialist agent for all flight and hotel booking requests.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span><span class="o">=</span><span class="p">[</span><span class="n">booking_tool</span><span class="p">]</span>  <span class="c1"># this agent can call booking_handler
</span><span class="p">)</span>

<span class="n">info_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Info</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">description</span><span class="o">=</span><span class="sh">"</span><span class="s">Specialist agent for general information and factual questions.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span><span class="o">=</span><span class="p">[</span><span class="n">info_tool</span><span class="p">]</span>  <span class="c1"># this agent can call info_handler
</span><span class="p">)</span>
</code></pre></div></div> <p><strong>3. Define the coordinator</strong></p> <p>The coordinator’s <code class="language-plaintext highlighter-rouge">instruction</code> tells it what its job is. The <code class="language-plaintext highlighter-rouge">sub_agents</code> list enables ADK’s <strong>Auto-Flow</strong>: the coordinator reads the user message and the sub-agent descriptions, then delegates automatically.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">coordinator</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Coordinator</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span><span class="o">=</span><span class="sh">"""</span><span class="s">
    You are a coordinator. Your ONLY job is to route incoming requests.
    Do NOT answer the user directly. Delegate every request to a sub-agent.

    - Flight/hotel booking requests  → delegate to Booker
    - All information/factual questions → delegate to Info
    </span><span class="sh">"""</span><span class="p">,</span>
    <span class="n">description</span><span class="o">=</span><span class="sh">"</span><span class="s">Routes user requests to the appropriate specialist.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">sub_agents</span><span class="o">=</span><span class="p">[</span><span class="n">booking_agent</span><span class="p">,</span> <span class="n">info_agent</span><span class="p">]</span>  <span class="c1"># enables Auto-Flow routing
</span><span class="p">)</span>
</code></pre></div></div> <p><strong>4. Run it</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">asyncio</span><span class="p">,</span> <span class="n">uuid</span>
<span class="kn">from</span> <span class="n">google.adk.runners</span> <span class="kn">import</span> <span class="n">InMemoryRunner</span>
<span class="kn">from</span> <span class="n">google.genai</span> <span class="kn">import</span> <span class="n">types</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">run</span><span class="p">(</span><span class="n">runner</span><span class="p">:</span> <span class="n">InMemoryRunner</span><span class="p">,</span> <span class="n">request</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">user_id</span>    <span class="o">=</span> <span class="sh">"</span><span class="s">user_001</span><span class="sh">"</span>
    <span class="n">session_id</span> <span class="o">=</span> <span class="nf">str</span><span class="p">(</span><span class="n">uuid</span><span class="p">.</span><span class="nf">uuid4</span><span class="p">())</span>

    <span class="c1"># Create a fresh session for this request
</span>    <span class="k">await</span> <span class="n">runner</span><span class="p">.</span><span class="n">session_service</span><span class="p">.</span><span class="nf">create_session</span><span class="p">(</span>
        <span class="n">app_name</span><span class="o">=</span><span class="n">runner</span><span class="p">.</span><span class="n">app_name</span><span class="p">,</span>
        <span class="n">user_id</span><span class="o">=</span><span class="n">user_id</span><span class="p">,</span>
        <span class="n">session_id</span><span class="o">=</span><span class="n">session_id</span><span class="p">,</span>
    <span class="p">)</span>

    <span class="n">final</span> <span class="o">=</span> <span class="sh">""</span>
    <span class="k">for</span> <span class="n">event</span> <span class="ow">in</span> <span class="n">runner</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span>
        <span class="n">user_id</span><span class="o">=</span><span class="n">user_id</span><span class="p">,</span>
        <span class="n">session_id</span><span class="o">=</span><span class="n">session_id</span><span class="p">,</span>
        <span class="n">new_message</span><span class="o">=</span><span class="n">types</span><span class="p">.</span><span class="nc">Content</span><span class="p">(</span>
            <span class="n">role</span><span class="o">=</span><span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span>
            <span class="n">parts</span><span class="o">=</span><span class="p">[</span><span class="n">types</span><span class="p">.</span><span class="nc">Part</span><span class="p">(</span><span class="n">text</span><span class="o">=</span><span class="n">request</span><span class="p">)]</span>
        <span class="p">),</span>
    <span class="p">):</span>
        <span class="k">if</span> <span class="n">event</span><span class="p">.</span><span class="nf">is_final_response</span><span class="p">()</span> <span class="ow">and</span> <span class="n">event</span><span class="p">.</span><span class="n">content</span><span class="p">:</span>
            <span class="c1"># Extract the text from the final response event
</span>            <span class="k">if</span> <span class="n">event</span><span class="p">.</span><span class="n">content</span><span class="p">.</span><span class="n">parts</span><span class="p">:</span>
                <span class="n">final</span> <span class="o">=</span> <span class="sh">""</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">p</span><span class="p">.</span><span class="n">text</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">event</span><span class="p">.</span><span class="n">content</span><span class="p">.</span><span class="n">parts</span> <span class="k">if</span> <span class="n">p</span><span class="p">.</span><span class="n">text</span><span class="p">)</span>
            <span class="k">break</span>
    <span class="k">return</span> <span class="n">final</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="n">runner</span> <span class="o">=</span> <span class="nc">InMemoryRunner</span><span class="p">(</span><span class="n">coordinator</span><span class="p">)</span>

    <span class="n">result</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">run</span><span class="p">(</span><span class="n">runner</span><span class="p">,</span> <span class="sh">"</span><span class="s">Book me a hotel in Paris.</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Result:</span><span class="sh">"</span><span class="p">,</span> <span class="n">result</span><span class="p">)</span>
    <span class="c1"># → Booking Handler called
</span>    <span class="c1"># → "Booking action simulated for: 'Book me a hotel in Paris.'"
</span>
    <span class="n">result</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">run</span><span class="p">(</span><span class="n">runner</span><span class="p">,</span> <span class="sh">"</span><span class="s">What is the tallest mountain in the world?</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Result:</span><span class="sh">"</span><span class="p">,</span> <span class="n">result</span><span class="p">)</span>
    <span class="c1"># → Info Handler called
</span>    <span class="c1"># → "Information retrieved for: '...'"
</span>
<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="kn">import</span> <span class="n">nest_asyncio</span><span class="p">;</span> <span class="n">nest_asyncio</span><span class="p">.</span><span class="nf">apply</span><span class="p">()</span>
    <span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">main</span><span class="p">())</span>
</code></pre></div></div> <h3 id="adk-auto-flow-vs-explicit-routing">ADK Auto-Flow vs. Explicit Routing</h3> <p>Here’s what ADK does automatically that you write manually in LangChain:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User message
     ↓
Coordinator LLM reads:
  - The user's message
  - The description of each sub-agent
  - Its own instruction
     ↓
Decides: "This sounds like a booking. Delegate to Booker."
     ↓
Booker agent activates
     ↓
Booker LLM reads the request + its tools
     ↓
Calls booking_handler(request=...)
     ↓
Returns result to coordinator
     ↓
Coordinator returns final response to user
</code></pre></div></div> <p>The routing decision is embedded in the LLM’s reasoning — you don’t write it explicitly. This is more concise, but also harder to debug when it routes incorrectly.</p> <h2 id="langchain-vs-adk-which-to-use">LangChain vs. ADK: Which to Use</h2> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">LANGCHAIN vs ADK — WHICH TO USE</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-decision" style="max-width:260px;"><div class="ns-node-title">How complex is your routing?</div></div> <div class="ns-row" style="max-width:480px;margin-top:0.5rem;"> <div class="ns-branch"> <span class="ns-label-green" style="margin-bottom:0.35rem;">Simple, clear categories</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Use ADK Auto-Flow</div><div class="ns-node-sub">Less code · LLM decides</div></div> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node"><div class="ns-node-title">Coordinator + sub_agents</div><div class="ns-node-sub">Faster to build</div></div> </div> <div class="ns-branch"> <span class="ns-label-red" style="margin-bottom:0.35rem;">Complex, state-dependent</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-cyan"><div class="ns-node-title">Use LangChain / LangGraph</div><div class="ns-node-sub">Explicit control flow</div></div> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node"><div class="ns-node-title">RunnableBranch</div><div class="ns-node-sub">LangGraph conditional edges</div></div> </div> </div> </div> </div> <table> <thead> <tr> <th> </th> <th>LangChain + RunnableBranch</th> <th>Google ADK Auto-Flow</th> </tr> </thead> <tbody> <tr> <td><strong>Routing control</strong></td> <td>You write explicit conditions</td> <td>LLM decides based on descriptions</td> </tr> <tr> <td><strong>Debuggability</strong></td> <td>Easy — conditions are plain Python</td> <td>Harder — routing is inside LLM reasoning</td> </tr> <tr> <td><strong>Flexibility</strong></td> <td>Very high</td> <td>Medium</td> </tr> <tr> <td><strong>Code volume</strong></td> <td>More</td> <td>Less</td> </tr> <tr> <td><strong>Best for</strong></td> <td>Complex multi-step routing, state machines</td> <td>Simple agent delegation</td> </tr> </tbody> </table> <h2 id="key-takeaways">Key Takeaways</h2> <p><strong>What routing is.</strong> Routing adds conditional logic to an agent — instead of always following the same path, the agent evaluates the input and chooses the appropriate handler.</p> <p><strong>The four methods.</strong> LLM-based is most flexible. Rule-based is fastest. Embedding-based handles semantic variation. ML classifier is most accurate for known categories.</p> <p><strong>The mechanism.</strong> A router classifies the input → outputs a decision → the dispatcher picks the matching handler → the handler runs.</p> <p><strong>In LangChain:</strong> explicit routing with <code class="language-plaintext highlighter-rouge">RunnableBranch</code> + conditions. Full control. More code.</p> <p><strong>In ADK:</strong> implicit routing via <code class="language-plaintext highlighter-rouge">sub_agents</code> and agent descriptions. Less code. Less debuggable.</p> <blockquote> <p><strong>Rule of thumb:</strong> If you can write an <code class="language-plaintext highlighter-rouge">if</code> statement to describe the routing logic — use rules or LangChain. If the categories are fuzzy and overlap — use LLM-based or embedding routing.</p> </blockquote> <h2 id="references">References</h2> <ol> <li>LangGraph Documentation — <a href="https://www.langchain.com/">langchain.com</a></li> <li>Google ADK Documentation — <a href="https://google.github.io/adk-docs/">google.github.io/adk-docs</a></li> <li>LangChain RunnableBranch — <a href="https://python.langchain.com/">python.langchain.com</a></li> </ol>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="routing"/><summary type="html"><![CDATA[Prompt chains are predictable. The real world isn't. Routing gives agents the ability to make decisions — picking the right tool, sub-agent, or workflow based on what's actually in front of them.]]></summary></entry><entry><title type="html">Chapter 1: Prompt Chaining</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/prompt-chaining/" rel="alternate" type="text/html" title="Chapter 1: Prompt Chaining"/><published>2026-01-05T00:00:00+00:00</published><updated>2026-01-05T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/prompt-chaining</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/prompt-chaining/"><![CDATA[<h2 id="the-problem-with-one-big-prompt">The Problem with One Big Prompt</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>LLM (Large Language Model):</strong> A type of AI trained on enormous amounts of text (books, websites, code) that can generate human-like text in response to prompts. GPT-4, Gemini, Claude — these are all LLMs.</p> <p style="margin-top:0.5rem"><strong>Prompt:</strong> The text you send to an LLM. Think of it as your question or instruction. The quality of the prompt directly affects the quality of the response.</p> <p style="margin-top:0.5rem"><strong>Context window:</strong> The LLM's "working memory" — the maximum amount of text it can see at once. If you send a very long conversation, the model may "forget" things from early on because they fell outside the window.</p> <p style="margin-top:0.5rem"><strong>Hallucination:</strong> When an LLM confidently states something false. It doesn't know that it's wrong — it's just predicting plausible-sounding text based on patterns it learned. This is a core reliability problem that prompt chaining helps solve.</p> <p style="margin-top:0.5rem"><strong>Temperature:</strong> A setting (0 to 1) that controls how "creative" vs "predictable" the output is. Temperature 0 = always picks the most likely next word (deterministic). Temperature 1 = more random and creative. For factual tasks, use 0. For brainstorming, use higher values.</p> <p style="margin-top:0.5rem"><strong>API (Application Programming Interface):</strong> A way for programs to talk to each other. When you use LangChain to call GPT-4, it's sending your prompt to OpenAI's API over the internet and getting a response back.</p> </div> <style>.concept-box{border-left:3px solid #4fc97e;background:rgba(79,201,126,0.06);border-radius:0 8px 8px 0;padding:.85rem 1.1rem;margin:1.25rem 0}.concept-box .concept-label{font-size:.6rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:#4fc97e;display:block;margin-bottom:.4rem;font-family:monospace}.concept-box p{margin:0;font-size:.88rem;line-height:1.65;color:var(--global-text-color)}.concept-box code{font-size:.82em}.analogy-box{border-left:3px solid #e6a817;background:rgba(230,168,23,0.05);border-radius:0 8px 8px 0;padding:.85rem 1.1rem;margin:1.25rem 0}.analogy-box .analogy-label{font-size:.6rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:#e6a817;display:block;margin-bottom:.4rem;font-family:monospace}.analogy-box p{margin:0;font-size:.88rem;line-height:1.65;color:var(--global-text-color);font-style:italic}.warning-box{border-left:3px solid #ff6b6b;background:rgba(255,107,107,0.05);border-radius:0 8px 8px 0;padding:.85rem 1.1rem;margin:1.25rem 0}.warning-box .warning-label{font-size:.6rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:#ff6b6b;display:block;margin-bottom:.4rem;font-family:monospace}.warning-box p{margin:0;font-size:.88rem;line-height:1.65;color:var(--global-text-color)}</style> <p>Imagine asking a single intern to — in one go — read a 50-page report, extract key data, spot trends, write a summary, <em>and</em> draft an email to the CEO. All at once. No notes allowed.</p> <p>They’d probably do a few things okay and quietly drop the rest.</p> <p>That’s exactly what happens when you give a large language model (LLM) one massive, complicated instruction. The model gets overwhelmed. It forgets parts of the prompt. It loses track of what it was doing. It makes stuff up to fill in the gaps.</p> <p>This has a name: <strong>cognitive overload</strong>. And it’s the #1 reason LLM outputs go wrong on complex tasks.</p> <p>Here’s what failure looks like in practice:</p> <table> <thead> <tr> <th>What goes wrong</th> <th>What it means</th> </tr> </thead> <tbody> <tr> <td><strong>Instruction neglect</strong></td> <td>The model quietly ignores parts of your prompt</td> </tr> <tr> <td><strong>Contextual drift</strong></td> <td>It forgets the original goal halfway through</td> </tr> <tr> <td><strong>Error propagation</strong></td> <td>One early mistake snowballs into a broken answer</td> </tr> <tr> <td><strong>Hallucination</strong></td> <td>When unsure, it confidently makes things up</td> </tr> </tbody> </table> <h2 id="the-fix-chain-your-prompts">The Fix: Chain Your Prompts</h2> <p><strong>Prompt chaining</strong> solves this by doing what any good manager does — breaking the big task into smaller steps, where each step has exactly one job.</p> <p>Step 1 does its job. Passes its output to Step 2. Step 2 does its job. Passes to Step 3. And so on.</p> <p>Think of it like an assembly line. Each station handles one thing and does it well. The car doesn’t get assembled all at once by one person.</p> <div class="chain-demo-wrapper"> <div class="chain-demo-header"> <span class="chain-demo-label">INTERACTIVE DEMO — watch prompt chaining in action</span> <button class="chain-demo-btn" id="chainRunBtn">▶ Run Chain</button> </div> <div class="chain-demo-body"> <div class="chain-node chain-node--input" id="chainNode0"> <div class="chain-node-tag">RAW INPUT</div> <div class="chain-node-content"><em>"Market report: AI adoption in enterprise grew 340% in 2024. Healthcare led at 67%, finance at 52%. Top blockers: data privacy (43%) and integration costs (38%)."</em></div> </div> <div class="chain-connector" id="chainConn1"> <div class="chain-connector-pipe"></div> <div class="chain-connector-label">PROMPT 1 — Summarise the report</div> </div> <div class="chain-node" id="chainNode1"> <div class="chain-node-tag">STEP 1 — SUMMARISE</div> <div class="chain-node-content" id="chainOut1"><span class="chain-placeholder">Waiting…</span></div> </div> <div class="chain-connector" id="chainConn2"> <div class="chain-connector-pipe"></div> <div class="chain-connector-label">PROMPT 2 — Extract top trends as JSON</div> </div> <div class="chain-node" id="chainNode2"> <div class="chain-node-tag">STEP 2 — EXTRACT TRENDS</div> <div class="chain-node-content" id="chainOut2"><span class="chain-placeholder">Waiting…</span></div> </div> <div class="chain-connector" id="chainConn3"> <div class="chain-connector-pipe"></div> <div class="chain-connector-label">PROMPT 3 — Draft email from trends</div> </div> <div class="chain-node" id="chainNode3"> <div class="chain-node-tag">STEP 3 — DRAFT EMAIL</div> <div class="chain-node-content" id="chainOut3"><span class="chain-placeholder">Waiting…</span></div> </div> </div> <div class="chain-demo-status" id="chainStatus">Click <strong>Run Chain</strong> to see each step process and pass its output forward.</div> </div> <style>.chain-demo-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0;font-family:inherit}.chain-demo-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.25rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.06)}.chain-demo-label{font-size:.65rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:var(--global-text-color-light)}.chain-demo-btn{background:var(--global-theme-color);color:#fff;border:0;border-radius:6px;padding:.35rem .9rem;font-size:.78rem;font-weight:700;cursor:pointer;letter-spacing:.04em;transition:opacity .2s}.chain-demo-btn:hover{opacity:.85}.chain-demo-btn:disabled{opacity:.4;cursor:default}.chain-demo-body{padding:1.5rem 1.25rem;display:flex;flex-direction:column;gap:0}.chain-node{border:1px solid var(--global-divider-color);border-radius:8px;padding:.9rem 1.1rem;transition:border-color .3s,box-shadow .3s}.chain-node--input{background:rgba(128,128,128,0.04)}.chain-node.chain-node--active{border-color:var(--global-theme-color);box-shadow:0 0 0 1px var(--global-theme-color)}.chain-node.chain-node--done{border-color:rgba(38,152,186,0.4)}.chain-node-tag{font-size:.6rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color-light);margin-bottom:.5rem}.chain-node-content{font-size:.875rem;line-height:1.6;color:var(--global-text-color);min-height:1.5rem}.chain-placeholder{color:var(--global-text-color-light);opacity:.4;font-style:italic}.chain-connector{display:flex;flex-direction:column;align-items:center;gap:.2rem;padding:.4rem 0}.chain-connector-pipe{width:2px;height:16px;background:var(--global-divider-color);border-radius:1px}.chain-connector-label{font-size:.62rem;font-weight:600;letter-spacing:.08em;text-transform:uppercase;color:var(--global-text-color-light);opacity:.6}.chain-demo-status{padding:.65rem 1.25rem;border-top:1px solid var(--global-divider-color);font-size:.78rem;color:var(--global-text-color-light);background:rgba(128,128,128,0.04);min-height:2.2rem}.chain-cursor{display:inline-block;width:2px;height:1em;background:var(--global-theme-color);margin-left:1px;animation:blink .7s step-end infinite;vertical-align:text-bottom}@keyframes blink{0%,100%{opacity:1}50%{opacity:0}}pre.chain-json{background:rgba(128,128,128,0.1);border-radius:6px;padding:.75rem 1rem;font-size:.78rem;overflow-x:auto;margin:0;white-space:pre}</style> <script>
(function() {
  var steps = [
    {
      id: 'chainOut1',
      status: 'AI adoption in enterprise surged 340% in 2024. Healthcare (67%) and finance (52%) led the way. Key blockers remain: data privacy concerns (43%) and high integration costs (38%).',
      delay: 800
    },
    {
      id: 'chainOut2',
      status: null,
      isJson: true,
      json: '{\n  "trends": [\n    {\n      "trend": "Healthcare AI adoption",\n      "data": "67% of enterprise AI deployments"\n    },\n    {\n      "trend": "Finance AI adoption",\n      "data": "52% of enterprise AI deployments"\n    },\n    {\n      "trend": "Data privacy as blocker",\n      "data": "43% cite as top concern"\n    }\n  ]\n}',
      delay: 600
    },
    {
      id: 'chainOut3',
      status: 'Subject: AI Market Trends — Action Required\n\nHi team,\n\nQ2 data shows enterprise AI adoption up 340% YoY. Healthcare and finance are leading. Our two biggest growth blockers remain data privacy and integration cost — both addressable. I\'d like to discuss a targeted response by end of week.\n\n— Kohsheen',
      delay: 600
    }
  ];

  var nodeIds = ['chainNode1','chainNode2','chainNode3'];
  var connIds = ['chainConn1','chainConn2','chainConn3'];
  var running = false;

  function typeText(el, text, speed, cb) {
    el.innerHTML = '<span class="chain-cursor"></span>';
    var i = 0;
    var cursor = el.querySelector('.chain-cursor');
    function tick() {
      if (i < text.length) {
        el.insertBefore(document.createTextNode(text[i]), cursor);
        i++;
        setTimeout(tick, speed);
      } else {
        if (cursor) cursor.remove();
        if (cb) cb();
      }
    }
    setTimeout(tick, 100);
  }

  function setStatus(msg) {
    var s = document.getElementById('chainStatus');
    if (s) s.innerHTML = msg;
  }

  function activateConnector(id) {
    var c = document.getElementById(id);
    if (c) {
      var pipe = c.querySelector('.chain-connector-pipe');
      if (pipe) pipe.style.background = 'var(--global-theme-color)';
      var lbl = c.querySelector('.chain-connector-label');
      if (lbl) { lbl.style.opacity = '1'; lbl.style.color = 'var(--global-theme-color)'; }
    }
  }

  function runStep(idx, cb) {
    var step = steps[idx];
    var node = document.getElementById(nodeIds[idx]);
    var out = document.getElementById(step.id);
    if (!node || !out) { if (cb) cb(); return; }

    node.classList.add('chain-node--active');
    setStatus('Processing step ' + (idx + 1) + ' of 3…');

    setTimeout(function() {
      if (step.isJson) {
        var pre = document.createElement('pre');
        pre.className = 'chain-json';
        out.innerHTML = '';
        out.appendChild(pre);
        typeText(pre, step.json, 12, function() {
          node.classList.remove('chain-node--active');
          node.classList.add('chain-node--done');
          if (cb) cb();
        });
      } else {
        typeText(out, step.status, 18, function() {
          node.classList.remove('chain-node--active');
          node.classList.add('chain-node--done');
          if (cb) cb();
        });
      }
    }, step.delay);
  }

  function reset() {
    steps.forEach(function(s, i) {
      var out = document.getElementById(s.id);
      if (out) out.innerHTML = '<span class="chain-placeholder">Waiting…</span>';
      var node = document.getElementById(nodeIds[i]);
      if (node) { node.classList.remove('chain-node--active','chain-node--done'); }
      var conn = document.getElementById(connIds[i]);
      if (conn) {
        var pipe = conn.querySelector('.chain-connector-pipe');
        if (pipe) pipe.style.background = '';
        var lbl = conn.querySelector('.chain-connector-label');
        if (lbl) { lbl.style.opacity = ''; lbl.style.color = ''; }
      }
    });
    setStatus('Click <strong>Run Chain</strong> to see each step process and pass its output forward.');
  }

  document.addEventListener('DOMContentLoaded', function() {
    var btn = document.getElementById('chainRunBtn');
    if (!btn) return;

    btn.addEventListener('click', function() {
      if (running) return;
      running = true;
      btn.disabled = true;
      reset();

      setTimeout(function() {
        activateConnector('chainConn1');
        runStep(0, function() {
          activateConnector('chainConn2');
          runStep(1, function() {
            activateConnector('chainConn3');
            runStep(2, function() {
              setStatus('Chain complete. Each step\'s output fed directly into the next prompt.');
              running = false;
              btn.disabled = false;
              btn.textContent = '↺ Run Again';
            });
          });
        });
      }, 300);
    });
  });
})();
</script> <p>Here’s the key insight: <strong>each step only has one job</strong>. The summariser doesn’t worry about emails. The trend-extractor doesn’t worry about formatting. When every step is focused, every step does well.</p> <h2 id="how-to-make-the-chain-reliable">How to Make the Chain Reliable</h2> <p>There’s one thing that can break a chain: <strong>messy data between steps</strong>.</p> <p>If Step 1 outputs a rambling paragraph and Step 2 is trying to parse it as structured data — it fails. The output needs to be clean and predictable.</p> <p>The fix is <strong>structured output</strong>. You tell the LLM: “output as JSON, nothing else.” Like this:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"trends"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"trend"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Healthcare AI adoption"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"data"</span><span class="p">:</span><span class="w"> </span><span class="s2">"67% of enterprise deployments"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"trend"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Finance AI adoption"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"data"</span><span class="p">:</span><span class="w"> </span><span class="s2">"52% of enterprise deployments"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>JSON is machine-readable. It can be passed directly into the next prompt without any ambiguity. This is what makes chains robust at scale.</p> <h3 id="assign-a-role-at-each-step">Assign a Role at Each Step</h3> <p>One more trick: give the model a <strong>role</strong> at each step. It anchors the model’s tone and focus.</p> <ul> <li>Step 1 prompt starts with: <em>“You are a Market Analyst. Your sole task is to summarise…”</em></li> <li>Step 2 prompt starts with: <em>“You are a Data Analyst. Extract trends from…”</em></li> <li>Step 3 prompt starts with: <em>“You are a Business Writer. Draft a clear email…”</em></li> </ul> <p>It sounds silly, but it works. The model “picks up” the persona and stays consistent within that step.</p> <h2 id="where-prompt-chaining-applies">Where Prompt Chaining Applies</h2> <p>This pattern shows up everywhere once you recognise it.</p> <h3 id="information-processing">Information Processing</h3> <p>Raw data → clean → extract entities → search database → generate report. Each arrow is a prompt.</p> <h3 id="complex-questions">Complex Questions</h3> <p><em>“What caused the 1929 crash and how did government respond?”</em></p> <p>You can’t answer that well in one go. You split it:</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">PROMPT CHAINING PATTERN</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">Raw Input</div> <div class="ns-node-sub">Market report · unstructured text</div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">Step 1 — Summarise</div> <div class="ns-node-sub">One job: condense the report into key facts</div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">Step 2 — Extract Trends</div> <div class="ns-node-sub">One job: pull top 3 trends as structured JSON</div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">Step 3 — Draft Email</div> <div class="ns-node-sub">One job: write the CEO email from the trends</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Final Output</div> <div class="ns-node-sub">Polished email — each step focused, no overload</div> </div> </div> </div> <p>Steps 1 and 2 can even run <strong>in parallel</strong> — two LLM calls at the same time — then synthesise at the end. This is faster and more reliable than one giant prompt.</p> <h3 id="data-extraction-with-retry-logic">Data Extraction (with Retry Logic)</h3> <p>Prompt chaining isn’t always linear. You can add <strong>conditional steps</strong>:</p> <ol> <li>Try to extract fields from an invoice</li> <li>Check: did we get everything?</li> <li>If not → run another prompt specifically targeting the missing fields</li> <li>Repeat until valid</li> </ol> <p>This is how you build reliable OCR pipelines. The LLM extracts text, normalises it (“one thousand and fifty” → <code class="language-plaintext highlighter-rouge">1050</code>), then hands arithmetic to an external calculator. Each step does what it’s best at.</p> <h3 id="content-generation">Content Generation</h3> <p>Blog posts, reports, documentation — all follow a natural chain:</p> <ol> <li>Generate topic ideas</li> <li>Pick one, generate an outline</li> <li>Write each section (using previous section as context)</li> <li>Final review pass</li> </ol> <h3 id="conversations-with-memory">Conversations with Memory</h3> <p>Each conversation turn becomes a prompt that includes the previous exchange. The chain <em>is</em> the memory. This is how conversational AI maintains context across a long back-and-forth.</p> <h3 id="code-generation">Code Generation</h3> <ol> <li>Understand the request → write pseudocode</li> <li>Generate code from pseudocode</li> <li>Run linter/tests (this is a <strong>tool call</strong>, not an LLM call)</li> <li>If errors → fix them with another prompt</li> <li>Add docs</li> </ol> <p>The critical thing here: <strong>you can insert regular code between LLM calls</strong>. Run a linter. Call an API. Do a database lookup. The chain isn’t just LLM calls — it’s a proper pipeline.</p> <h2 id="the-code">The Code</h2> <p>We’ll build a two-step LangChain pipeline that:</p> <ol> <li>Reads a laptop spec in plain English</li> <li>Extracts the specs from it</li> <li>Formats them as a clean JSON object</li> </ol> <p>Think of each step as one worker doing one job, then passing a note to the next worker.</p> <h3 id="install">Install</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>langchain langchain-openai python-dotenv
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">langchain</code> is the framework. <code class="language-plaintext highlighter-rouge">langchain-openai</code> is the plugin for OpenAI models. <code class="language-plaintext highlighter-rouge">python-dotenv</code> loads your API key from a <code class="language-plaintext highlighter-rouge">.env</code> file so you don’t hardcode secrets.</p> <h3 id="set-up-your-api-key">Set Up Your API Key</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># .env file (never commit this)
</span><span class="n">OPENAI_API_KEY</span><span class="o">=</span><span class="n">sk</span><span class="o">-</span><span class="bp">...</span>
</code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">dotenv</span> <span class="kn">import</span> <span class="n">load_dotenv</span>
<span class="nf">load_dotenv</span><span class="p">()</span>  <span class="c1"># reads .env and puts OPENAI_API_KEY into the environment
</span></code></pre></div></div> <h3 id="initialise-the-model">Initialise the Model</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>

<span class="c1"># temperature=0 means deterministic — the model picks the most likely token
# every time, no randomness. Good for extraction tasks.
# temperature=1 = more creative/varied. Bad for structured data extraction.
</span><span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4o-mini</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">ChatOpenAI</code> is a wrapper around OpenAI’s chat API. Every call to <code class="language-plaintext highlighter-rouge">llm</code> sends a request to the API and returns a response object. We haven’t called it yet — we’re just configuring it.</p> <h3 id="define-the-prompts">Define the Prompts</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">ChatPromptTemplate</span>

<span class="c1"># Prompt 1: extract specs from the raw text
# {text_input} is a placeholder — gets replaced with actual text at runtime
</span><span class="n">prompt_extract</span> <span class="o">=</span> <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"""</span><span class="s">
You are a hardware spec extractor. Your only job is to pull out technical
specifications from the text below. Be precise. Output only what</span><span class="sh">'</span><span class="s">s in the text.

Text: {text_input}
</span><span class="sh">"""</span><span class="p">)</span>

<span class="c1"># Prompt 2: take the extracted specs and structure them as JSON
# {specifications} gets filled with the OUTPUT of prompt 1
</span><span class="n">prompt_transform</span> <span class="o">=</span> <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="sh">"""</span><span class="s">
You are a data formatter. Take the hardware specifications below and output
a JSON object with exactly three keys: </span><span class="sh">"</span><span class="s">cpu</span><span class="sh">"</span><span class="s">, </span><span class="sh">"</span><span class="s">memory</span><span class="sh">"</span><span class="s">, </span><span class="sh">"</span><span class="s">storage</span><span class="sh">"</span><span class="s">.
No extra text, no explanation — JSON only.

Specifications: {specifications}
</span><span class="sh">"""</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">ChatPromptTemplate.from_template()</code> builds a reusable prompt with named placeholders. When you call the chain, you pass in the values that fill those placeholders.</p> <p><strong>Why two prompts instead of one?</strong> Try asking the model to extract <em>and</em> format at once — it often formats wrong, or forgets to extract something. Splitting the jobs gives each step full attention.</p> <h3 id="wire-the-chain-with-lcel">Wire the Chain with LCEL</h3> <p>This is the key step. LangChain’s <strong>LCEL</strong> (LangChain Expression Language) uses the <code class="language-plaintext highlighter-rouge">|</code> pipe operator — same as Unix pipes — to connect components.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_core.output_parsers</span> <span class="kn">import</span> <span class="n">StrOutputParser</span>

<span class="c1"># StrOutputParser converts the model's response object → plain string
# Without it, you'd get an AIMessage object, not a string
</span>
<span class="c1"># --- Chain 1: extract specs ---
# Flow: prompt_extract fills {text_input}
#       → sends to llm → gets AI response
#       → StrOutputParser strips it to plain text
</span><span class="n">extraction_chain</span> <span class="o">=</span> <span class="n">prompt_extract</span> <span class="o">|</span> <span class="n">llm</span> <span class="o">|</span> <span class="nc">StrOutputParser</span><span class="p">()</span>

<span class="c1"># --- Chain 2: full pipeline ---
# {"specifications": extraction_chain} runs extraction_chain first,
# then puts the result into {specifications} of prompt_transform
</span><span class="n">full_chain</span> <span class="o">=</span> <span class="p">(</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">specifications</span><span class="sh">"</span><span class="p">:</span> <span class="n">extraction_chain</span><span class="p">}</span>  <span class="c1"># ← runs chain 1, stores result
</span>    <span class="o">|</span> <span class="n">prompt_transform</span>                     <span class="c1"># ← fills {specifications}
</span>    <span class="o">|</span> <span class="n">llm</span>                                  <span class="c1"># ← sends to model
</span>    <span class="o">|</span> <span class="nc">StrOutputParser</span><span class="p">()</span>                    <span class="c1"># ← strips to plain string
</span><span class="p">)</span>
</code></pre></div></div> <p>Read it left to right. Each <code class="language-plaintext highlighter-rouge">|</code> says “take the output of the left side and pass it as input to the right side.” The chain is lazy — nothing runs until you call <code class="language-plaintext highlighter-rouge">.invoke()</code>.</p> <h3 id="run-it">Run It</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">result</span> <span class="o">=</span> <span class="n">full_chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span>
    <span class="sh">"</span><span class="s">text_input</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">New laptop: 3.5 GHz octa-core CPU, 16GB RAM, 1TB NVMe SSD.</span><span class="sh">"</span>
<span class="p">})</span>

<span class="nf">print</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">.invoke()</code> triggers the whole pipeline. Under the hood, this is what happens:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. prompt_extract receives: text_input = "New laptop: 3.5 GHz..."
2. llm receives the filled prompt → returns: "CPU: 3.5 GHz octa-core..."
3. StrOutputParser strips it to a plain string
4. prompt_transform receives: specifications = "CPU: 3.5 GHz octa-core..."
5. llm receives the filled prompt → returns: {"cpu": "3.5 GHz octa-core"...}
6. StrOutputParser strips it → final output
</code></pre></div></div> <p><strong>Output:</strong></p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"cpu"</span><span class="p">:</span><span class="w"> </span><span class="s2">"3.5 GHz octa-core"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"memory"</span><span class="p">:</span><span class="w"> </span><span class="s2">"16GB"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"storage"</span><span class="p">:</span><span class="w"> </span><span class="s2">"1TB NVMe SSD"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h3 id="what-you-get-at-each-step">What You Get at Each Step</h3> <p>To see intermediate outputs (helpful for debugging), run each chain separately:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># See what extraction_chain produces before it hits the formatter
</span><span class="n">raw_extraction</span> <span class="o">=</span> <span class="n">extraction_chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span>
    <span class="sh">"</span><span class="s">text_input</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">New laptop: 3.5 GHz octa-core CPU, 16GB RAM, 1TB NVMe SSD.</span><span class="sh">"</span>
<span class="p">})</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">After step 1:</span><span class="sh">"</span><span class="p">,</span> <span class="n">raw_extraction</span><span class="p">)</span>
<span class="c1"># → "CPU: 3.5 GHz octa-core\nRAM: 16GB\nStorage: 1TB NVMe SSD"
</span>
<span class="c1"># Now run the full thing
</span><span class="n">final</span> <span class="o">=</span> <span class="n">full_chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">text_input</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">...</span><span class="sh">"</span><span class="p">})</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">After step 2:</span><span class="sh">"</span><span class="p">,</span> <span class="n">final</span><span class="p">)</span>
<span class="c1"># → {"cpu": "3.5 GHz octa-core", "memory": "16GB", "storage": "1TB NVMe SSD"}
</span></code></pre></div></div> <p>This is one of the best things about LCEL — any sub-chain is itself a runnable. You can test each step independently before connecting them.</p> <h3 id="why-this-beats-one-big-prompt">Why This Beats One Big Prompt</h3> <p>Here’s simulated data from running both approaches 60 times on 60 different laptop descriptions. The Y-axis is <strong>output quality</strong> (0 = completely broken JSON or wrong fields, 1 = perfect extraction).</p> <div class="chain-chart-wrapper"> <div class="chain-chart-header"> <span class="chain-chart-title">CHAIN vs. SINGLE-PROMPT OUTPUT QUALITY</span> <span class="chain-chart-subtitle">60 runs on varied laptop descriptions</span> </div> <div class="chain-chart-area"> <canvas id="chainQualityChart" width="680" height="300"></canvas> </div> <div class="chain-chart-controls"> <button class="chain-chart-play" id="chartPlayBtn" title="Play">&#9654;</button> <div class="chain-chart-scrubber-wrap"> <input type="range" id="chartScrubber" min="0" max="59" value="0" step="1" class="chain-chart-scrubber"/> </div> <span class="chain-chart-counter" id="chartCounter">0 / 60</span> </div> <div class="chain-chart-legend"> <span class="chain-chart-legend-item"><span class="legend-dot legend-dot--chain"></span> chained output</span> <span class="chain-chart-legend-item"><span class="legend-dot legend-dot--single"></span> single-prompt output</span> <span class="chain-chart-legend-item"><span class="legend-dash"></span> quality threshold (0.70)</span> </div> </div> <style>.chain-chart-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.chain-chart-header{padding:.85rem 1.25rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.chain-chart-title{font-size:.7rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color);display:block}.chain-chart-subtitle{font-size:.65rem;color:var(--global-text-color-light);opacity:.7}.chain-chart-area{padding:.5rem .5rem 0;overflow-x:auto}#chainQualityChart{display:block;max-width:100%;height:auto}.chain-chart-controls{display:flex;align-items:center;gap:.75rem;padding:.65rem 1.25rem;border-top:1px solid var(--global-divider-color)}.chain-chart-play{background:0;border:1px solid var(--global-divider-color);border-radius:50%;width:28px;height:28px;display:flex;align-items:center;justify-content:center;cursor:pointer;color:var(--global-text-color);font-size:.7rem;flex-shrink:0;transition:border-color .2s}.chain-chart-play:hover{border-color:var(--global-theme-color);color:var(--global-theme-color)}.chain-chart-scrubber-wrap{flex:1}.chain-chart-scrubber{width:100%;height:4px;-webkit-appearance:none;appearance:none;background:var(--global-divider-color);border-radius:2px;outline:0;cursor:pointer}.chain-chart-scrubber::-webkit-slider-thumb{-webkit-appearance:none;width:14px;height:14px;border-radius:50%;background:var(--global-text-color);cursor:pointer}.chain-chart-counter{font-size:.72rem;color:var(--global-text-color-light);white-space:nowrap;min-width:3.5rem;text-align:right}.chain-chart-legend{display:flex;align-items:center;gap:1.25rem;padding:.6rem 1.25rem;border-top:1px solid var(--global-divider-color);background:rgba(128,128,128,0.03);flex-wrap:wrap}.chain-chart-legend-item{display:flex;align-items:center;gap:.4rem;font-size:.72rem;color:var(--global-text-color-light)}.legend-dot{width:9px;height:9px;border-radius:50%;display:inline-block;flex-shrink:0}.legend-dot--chain{background:#2698ba}.legend-dot--single{background:#e05252}.legend-dash{width:18px;height:2px;border-top:2px dashed rgba(255,200,80,0.7);display:inline-block;flex-shrink:0}</style> <script>
(function() {
  // Fixed data: 60 experiments. chain = consistently above 0.70. single = noisy.
  var chainScores  = [0.72,0.68,0.75,0.80,0.63,0.78,0.74,0.69,0.76,0.73,0.71,0.77,0.82,0.65,0.74,0.76,0.79,0.69,0.75,0.78,0.73,0.71,0.74,0.76,0.81,0.72,0.68,0.75,0.77,0.73,0.74,0.70,0.76,0.78,0.72,0.75,0.73,0.79,0.83,0.74,0.71,0.76,0.73,0.78,0.77,0.72,0.74,0.76,0.81,0.73,0.71,0.75,0.77,0.74,0.72,0.78,0.80,0.73,0.75,0.77];
  var singleScores = [0.48,0.71,0.39,0.62,0.44,0.55,0.68,0.41,0.59,0.52,0.38,0.65,0.47,0.73,0.43,0.58,0.46,0.64,0.37,0.56,0.51,0.44,0.68,0.39,0.55,0.62,0.41,0.49,0.66,0.43,0.57,0.38,0.61,0.48,0.55,0.42,0.63,0.47,0.52,0.38,0.64,0.46,0.55,0.41,0.59,0.48,0.53,0.37,0.65,0.44,0.58,0.46,0.62,0.39,0.55,0.48,0.53,0.41,0.57,0.63];
  var TOTAL = chainScores.length;
  var currentFrame = 0;
  var playing = false;
  var animHandle = null;

  var canvas, ctx, scrubber, playBtn, counter;

  // Layout
  var ML = 52, MR = 20, MT = 28, MB = 44;
  var W, H, PW, PH;

  function setup() {
    canvas   = document.getElementById('chainQualityChart');
    scrubber = document.getElementById('chartScrubber');
    playBtn  = document.getElementById('chartPlayBtn');
    counter  = document.getElementById('chartCounter');
    if (!canvas) return;

    ctx = canvas.getContext('2d');
    resize();
    draw(currentFrame);

    scrubber.addEventListener('input', function() {
      currentFrame = parseInt(this.value, 10);
      draw(currentFrame);
      counter.textContent = currentFrame + ' / ' + TOTAL;
    });

    playBtn.addEventListener('click', function() {
      if (playing) { pause(); } else { play(); }
    });
  }

  function resize() {
    var rect = canvas.parentElement.getBoundingClientRect();
    var dpr = window.devicePixelRatio || 1;
    W = Math.min(680, rect.width - 16);
    H = 300;
    canvas.width  = W * dpr;
    canvas.height = H * dpr;
    canvas.style.width  = W + 'px';
    canvas.style.height = H + 'px';
    ctx.scale(dpr, dpr);
    PW = W - ML - MR;
    PH = H - MT - MB;
  }

  function xPos(i) { return ML + (i / (TOTAL - 1)) * PW; }
  function yPos(v) { return MT + (1 - (v - 0.3) / 0.6) * PH; }

  function getTheme() {
    var s = getComputedStyle(document.documentElement);
    return {
      text:    s.getPropertyValue('--global-text-color').trim()       || '#e0e0e0',
      muted:   s.getPropertyValue('--global-text-color-light').trim() || '#888',
      divider: s.getPropertyValue('--global-divider-color').trim()    || '#333',
    };
  }

  function draw(upTo) {
    ctx.clearRect(0, 0, W, H);
    var th = getTheme();

    // Grid lines + Y labels
    var yTicks = [0.30,0.40,0.50,0.60,0.70,0.80,0.90];
    ctx.font = '10px monospace';
    ctx.textAlign = 'right';
    yTicks.forEach(function(v) {
      var y = yPos(v);
      ctx.strokeStyle = th.divider;
      ctx.lineWidth = 0.5;
      ctx.setLineDash([]);
      ctx.beginPath(); ctx.moveTo(ML, y); ctx.lineTo(ML + PW, y); ctx.stroke();
      ctx.fillStyle = th.muted;
      ctx.fillText(v.toFixed(2), ML - 6, y + 3.5);
    });

    // X axis labels
    ctx.textAlign = 'center';
    ctx.fillStyle = th.muted;
    [0,10,20,30,40,50,60].forEach(function(v) {
      if (v >= TOTAL) return;
      var x = xPos(v === TOTAL ? TOTAL - 1 : v);
      ctx.fillText(v, x, H - MB + 16);
    });

    // Axis label
    ctx.save();
    ctx.translate(10, MT + PH / 2);
    ctx.rotate(-Math.PI / 2);
    ctx.textAlign = 'center';
    ctx.font = '10px monospace';
    ctx.fillStyle = th.muted;
    ctx.fillText('quality score', 0, 0);
    ctx.restore();
    ctx.textAlign = 'center';
    ctx.font = '10px monospace';
    ctx.fillStyle = th.muted;
    ctx.fillText('experiment', ML + PW / 2, H - 6);

    // Threshold line at 0.70
    var ty = yPos(0.70);
    ctx.strokeStyle = 'rgba(255,200,80,0.55)';
    ctx.lineWidth = 1.2;
    ctx.setLineDash([5, 4]);
    ctx.beginPath(); ctx.moveTo(ML, ty); ctx.lineTo(ML + PW, ty); ctx.stroke();
    ctx.setLineDash([]);
    ctx.font = '9px monospace';
    ctx.fillStyle = 'rgba(255,200,80,0.75)';
    ctx.textAlign = 'left';
    ctx.fillText('0.70 quality threshold', ML + 4, ty - 4);

    // Draw dots up to upTo
    for (var i = 0; i <= upTo && i < TOTAL; i++) {
      var x = xPos(i);
      var cs = chainScores[i];
      var ss = singleScores[i];

      // Single-prompt dot (drawn first, behind)
      ctx.beginPath();
      ctx.arc(x, yPos(ss), 4.5, 0, Math.PI * 2);
      ctx.fillStyle = ss >= 0.70 ? 'rgba(224,82,82,0.5)' : 'rgba(224,82,82,0.85)';
      ctx.fill();

      // Chain dot
      ctx.beginPath();
      ctx.arc(x, yPos(cs), 4.5, 0, Math.PI * 2);
      ctx.fillStyle = cs >= 0.70 ? '#2698ba' : 'rgba(38,152,186,0.55)';
      ctx.fill();

      // Tooltip on the last visible point
      if (i === upTo) {
        // Chain tooltip
        drawTooltip(x, yPos(cs), cs.toFixed(3), '#2698ba');
        // Single tooltip
        drawTooltip(x, yPos(ss), ss.toFixed(3), '#e05252');
      }
    }
  }

  function drawTooltip(x, y, label, color) {
    var TW = 44, TH = 18, TR = 3;
    var tx = x - TW / 2;
    var ty = y - TH - 6;
    // keep in bounds
    if (tx < ML) tx = ML;
    if (tx + TW > ML + PW) tx = ML + PW - TW;

    ctx.fillStyle = color;
    ctx.beginPath();
    ctx.roundRect ? ctx.roundRect(tx, ty, TW, TH, TR) : ctx.rect(tx, ty, TW, TH);
    ctx.fill();

    ctx.fillStyle = '#fff';
    ctx.font = 'bold 10px monospace';
    ctx.textAlign = 'center';
    ctx.fillText(label, tx + TW / 2, ty + 12);
  }

  function play() {
    playing = true;
    playBtn.innerHTML = '&#9646;&#9646;';
    if (currentFrame >= TOTAL - 1) { currentFrame = 0; }
    step();
  }

  function pause() {
    playing = false;
    playBtn.innerHTML = '&#9654;';
    if (animHandle) { clearTimeout(animHandle); animHandle = null; }
  }

  function step() {
    if (!playing) return;
    currentFrame = Math.min(currentFrame + 1, TOTAL - 1);
    scrubber.value = currentFrame;
    counter.textContent = currentFrame + ' / ' + TOTAL;
    draw(currentFrame);
    if (currentFrame < TOTAL - 1) {
      animHandle = setTimeout(step, 120);
    } else {
      pause();
    }
  }

  document.addEventListener('DOMContentLoaded', setup);
})();
</script> <p>Play through the 60 experiments. Notice:</p> <ul> <li><strong>Chain (cyan):</strong> consistently above the 0.70 threshold. Reliable.</li> <li><strong>Single-prompt (red):</strong> scattered. Many fall below acceptable quality.</li> </ul> <p>This gap is <strong>why chaining exists</strong>. It’s not magic — it’s just focus at each step.</p> <h2 id="context-engineering">Context Engineering</h2> <p>Here’s something most tutorials skip: <strong>what you put <em>around</em> the prompt matters as much as the prompt itself</strong>.</p> <p>There’s a discipline called <strong>Context Engineering</strong> — the art of giving the model a rich, complete picture before it generates a response.</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">CONTEXT ENGINEERING</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;flex-direction:row;align-items:stretch;gap:0.85rem;"> <div style="display:flex;flex-direction:column;gap:0.5rem;flex-shrink:0;min-width:190px;justify-content:center;"> <div class="ns-node ns-node-cyan"><div class="ns-node-title">System Prompt</div><div class="ns-node-sub">who the model is</div></div> <div class="ns-node ns-node-purple"><div class="ns-node-title">Retrieved Docs</div><div class="ns-node-sub">facts from your KB</div></div> <div class="ns-node ns-node-amber"><div class="ns-node-title">Tool Outputs</div><div class="ns-node-sub">API / calendar results</div></div> <div class="ns-node"><div class="ns-node-title">User History</div><div class="ns-node-sub">what they said before</div></div> </div> <div style="display:flex;align-items:center;flex-shrink:0;padding:0 0.25rem;"> <div style="width:2px;background:#2698ba;height:70%;align-self:center;"></div> <div style="font-size:1.4rem;color:#2698ba;line-height:1;">→</div> </div> <div style="display:flex;align-items:center;flex-shrink:0;"> <div class="ns-node ns-node-cyan" style="min-width:100px;text-align:center;"> <div class="ns-node-title" style="font-size:1rem;">Model</div> <div class="ns-node-sub">LLM</div> </div> </div> <div style="display:flex;align-items:center;flex-shrink:0;padding:0 0.25rem;"> <div style="font-size:1.4rem;color:#4fc97e;line-height:1;">→</div> </div> <div style="display:flex;align-items:center;flex-shrink:0;"> <div class="ns-node ns-node-green" style="min-width:110px;text-align:center;"> <div class="ns-node-title" style="font-size:1rem;">Good answer</div> <div class="ns-node-sub">grounded, relevant</div> </div> </div> </div> </div> <p>Prompt engineering asks: <em>“how do I phrase this question?”</em></p> <p>Context engineering asks: <em>“what does the model need to know before I even ask?”</em></p> <p>A basic LLM answers from its training data. A context-engineered agent:</p> <ul> <li>Checks your calendar before scheduling something</li> <li>Reads your previous emails before drafting a new one</li> <li>Fetches the latest docs before writing code</li> </ul> <p>The difference is the <em>environment</em> you build around the model — not the model itself. This is why two teams using the same LLM can get wildly different results.</p> <h2 id="key-takeaways">Key Takeaways</h2> <p><strong>The core idea.</strong> One complex prompt = one overwhelmed LLM. A chain of focused prompts = reliable, controllable output.</p> <p><strong>The mechanism.</strong> Each step outputs structured data (usually JSON) that feeds directly into the next step’s prompt. Clean handoffs between steps are everything.</p> <p><strong>The superpower.</strong> Between LLM calls, you can run real code — validators, APIs, calculators, databases. The chain is a pipeline, not just a list of prompts.</p> <p><strong>When to use it.</strong> Whenever your task has more than two stages, requires external data, or would benefit from independent verification at each step.</p> <blockquote> <p>If your task would make an intern ask “which part do you want me to do first?” — it needs a chain.</p> </blockquote> <h2 id="references">References</h2> <ol> <li>LangChain Expression Language — <a href="https://python.langchain.com/v0.2/docs/core_modules/expression_language/">python.langchain.com</a></li> <li>LangGraph — <a href="https://langchain-ai.github.io/langgraph/">langchain-ai.github.io/langgraph</a></li> <li>Prompt Chaining Guide — <a href="https://www.promptingguide.ai/techniques/chaining">promptingguide.ai</a></li> <li>Crew AI — <a href="https://docs.crewai.com/">docs.crewai.com</a></li> <li>Google Vertex AI Prompt Optimizer — <a href="https://cloud.google.com/vertex-ai/generative-ai/docs/learn/prompts/prompt-optimizer">cloud.google.com</a></li> </ol>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="prompt-engineering"/><summary type="html"><![CDATA[LLMs choke on complex tasks. Prompt chaining fixes that — by breaking one hard problem into a sequence of simple ones, each feeding the next.]]></summary></entry></feed>
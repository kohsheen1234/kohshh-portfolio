<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://kohsheen1234.github.io/kohshh-portfolio/feed.xml" rel="self" type="application/atom+xml"/><link href="https://kohsheen1234.github.io/kohshh-portfolio/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-10T07:35:08+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Chapter 16: Resource-Aware Optimization</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/resource-aware-optimization/" rel="alternate" type="text/html" title="Chapter 16: Resource-Aware Optimization"/><published>2026-03-06T00:00:00+00:00</published><updated>2026-03-06T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/resource-aware-optimization</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/resource-aware-optimization/"><![CDATA[<h2 id="the-overspending-problem">The Overspending Problem</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Token:</strong> The unit LLMs use for pricing and context length measurement. Roughly 4 characters of text = 1 token. "What is the capital of France?" is about 8 tokens. A 10-page document is roughly 2,500 tokens. Every LLM API call charges you for both input tokens (your prompt) and output tokens (the model's response).</p> <p style="margin-top:0.5rem"><strong>Model tiers:</strong> LLM providers offer multiple models at different price/capability points. "Flash" or "Mini" models are fast and cheap, designed for high-volume simple tasks. "Pro" or "Opus" models are powerful and expensive, designed for complex reasoning. The price difference can be 10–100× between tiers.</p> <p style="margin-top:0.5rem"><strong>Latency:</strong> The time between sending a request and receiving the full response. Cheaper, smaller models typically respond in 0.5–2 seconds. Larger, more capable models may take 5–30 seconds for complex reasoning. In real-time applications, latency directly affects user experience.</p> <p style="margin-top:0.5rem"><strong>Router agent:</strong> An agent whose sole job is to classify incoming requests and forward them to the appropriate downstream handler. The router itself typically uses a cheap, fast model — the routing decision must not cost more than the savings it generates.</p> <p style="margin-top:0.5rem"><strong>Critique agent:</strong> An agent that evaluates the quality of another agent's output. Used for quality assurance, self-correction loops, and identifying systematic failures in routing decisions (e.g., if the cheap model keeps producing bad results for certain query types).</p> <p style="margin-top:0.5rem"><strong>OpenRouter:</strong> A third-party service that provides a unified API endpoint for hundreds of AI models from different providers (OpenAI, Anthropic, Google, Meta, etc.). Instead of integrating each provider separately, you send all requests to OpenRouter with a model name, and it handles routing, billing, and failover.</p> <p style="margin-top:0.5rem"><strong>Graceful degradation:</strong> When a preferred system is unavailable, falling back to a less capable but still functional alternative rather than failing completely. (Covered in depth in Chapter 12.)</p> </div> <p>Imagine you run a customer support system powered by GPT-4 Pro. It handles 100,000 queries per day. Many of those queries are:</p> <ul> <li>“What are your business hours?” (trivial lookup)</li> <li>“How do I reset my password?” (template response)</li> <li>“Is my order shipped?” (database lookup)</li> <li>“What’s the return policy?” (static FAQ)</li> </ul> <p>These questions do not require sophisticated reasoning. GPT-4o-mini handles them perfectly. But if you’re routing everything to GPT-4 Pro, you’re paying 15–20× more per query than necessary — for zero quality improvement on simple questions.</p> <p>At 100,000 queries/day, 80% of which are simple:</p> <ul> <li><strong>Without optimization:</strong> 100,000 × $0.003/query = <strong>$300/day = $9,000/month</strong></li> <li><strong>With optimization:</strong> 80,000 × $0.0003 + 20,000 × $0.003 = <strong>$24 + $60 = $84/day = $2,520/month</strong></li> </ul> <p>That’s a <strong>70% cost reduction</strong> — same quality, same user experience, same output for the queries that matter.</p> <p><strong>Resource-aware optimization</strong> is the pattern that makes this happen systematically. It’s not just about cost — it’s about matching the computational resource to the task’s actual requirements across three dimensions: <strong>cost</strong> (API pricing), <strong>latency</strong> (response time), and <strong>quality</strong> (output accuracy and sophistication).</p> <hr/> <h2 id="the-model-tier-landscape">The Model Tier Landscape</h2> <p>Before building a resource-aware system, you need to understand the trade-offs across model tiers:</p> <div class="rao-model-compare-wrapper"> <div class="rao-mc-header"> <span class="rao-mc-title">MODEL TIER COMPARISON — hover to explore</span> <span class="rao-mc-sub">Relative values — exact pricing varies by provider and date</span> </div> <canvas id="modelTierChart" style="display:block;width:100%;max-width:600px;margin:0 auto;"></canvas> <div class="rao-mc-legend"> <div class="rao-mc-leg"><span class="rao-mc-dot" style="background:#4fc97e"></span>Flash / Mini (fast, cheap)</div> <div class="rao-mc-leg"><span class="rao-mc-dot" style="background:#e6a817"></span>Standard / Balanced</div> <div class="rao-mc-leg"><span class="rao-mc-dot" style="background:#ff6b6b"></span>Pro / Opus (powerful, expensive)</div> </div> <div class="rao-mc-tooltip" id="raoTooltip" style="display:none"></div> </div> <style>.rao-model-compare-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0;position:relative}.rao-mc-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.4rem}.rao-mc-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.rao-mc-sub{font-size:.65rem;color:var(--global-text-color-light)}.rao-mc-legend{display:flex;gap:1.25rem;padding:.6rem 1.1rem;border-top:1px solid var(--global-divider-color);flex-wrap:wrap}.rao-mc-leg{display:flex;align-items:center;gap:.4rem;font-size:.72rem;color:var(--global-text-color-light)}.rao-mc-dot{width:9px;height:9px;border-radius:50%;flex-shrink:0}.rao-mc-tooltip{position:fixed;background:var(--global-bg-color);border:1px solid var(--global-divider-color);border-radius:6px;padding:.5rem .75rem;font-size:.73rem;color:var(--global-text-color);pointer-events:none;z-index:200;max-width:230px;line-height:1.5;box-shadow:0 4px 16px rgba(0,0,0,0.35)}</style> <script>
(function(){
  var canvas = document.getElementById('modelTierChart');
  if (!canvas) return;
  var ctx = canvas.getContext('2d');
  var tooltip = document.getElementById('raoTooltip');
  var dpr = window.devicePixelRatio || 1;
  var W = Math.min(600, canvas.parentElement.getBoundingClientRect().width - 24);
  var H = 240;
  canvas.width = W*dpr; canvas.height = H*dpr;
  canvas.style.width = W+'px'; canvas.style.height = H+'px';
  ctx.scale(dpr, dpr);

  var dims = ['Cost/token', 'Speed', 'Reasoning', 'Context', 'Best for'];
  var tiers = [
    { name: 'Flash / Mini', color: '#4fc97e', scores: [1, 5, 2, 3],
      detail: 'e.g. Gemini Flash 2.5, GPT-4o-mini\nIdeal for: FAQ, classification, simple extraction, routing decisions' },
    { name: 'Standard', color: '#e6a817', scores: [3, 3, 4, 4],
      detail: 'e.g. GPT-4o, Gemini 1.5 Pro\nIdeal for: general tasks, coding, document analysis, customer support' },
    { name: 'Pro / Opus', color: '#ff6b6b', scores: [5, 2, 5, 5],
      detail: 'e.g. Gemini 2.5 Pro, Claude Opus 4\nIdeal for: complex reasoning, research synthesis, critical decisions' },
  ];

  var ML=90, MR=20, MT=24, MB=36;
  var PW=W-ML-MR, PH=H-MT-MB;
  var nDims=4, nTiers=tiers.length;
  var gW=PW/nDims, bW=(gW*0.65)/nTiers, bGap=(gW*0.35)/(nTiers+1);
  var hitRects=[];

  function getTheme(){ var s=getComputedStyle(document.documentElement); return {text:s.getPropertyValue('--global-text-color').trim()||'#e0e0e0',muted:s.getPropertyValue('--global-text-color-light').trim()||'#888',div:s.getPropertyValue('--global-divider-color').trim()||'#333'}; }

  function draw(hD, hT){
    ctx.clearRect(0,0,W,H); hitRects=[];
    var th=getTheme();
    for(var v=1;v<=5;v++){
      var gy=MT+(1-(v-1)/4)*PH;
      ctx.strokeStyle=th.div; ctx.lineWidth=0.5; ctx.setLineDash([3,3]);
      ctx.beginPath(); ctx.moveTo(ML,gy); ctx.lineTo(ML+PW,gy); ctx.stroke();
      ctx.setLineDash([]);
      ctx.fillStyle=th.muted; ctx.font='10px monospace'; ctx.textAlign='right';
      ctx.fillText(v, ML-5, gy+3.5);
    }
    for(var di=0;di<nDims;di++){
      var gx=ML+di*gW;
      for(var ti=0;ti<nTiers;ti++){
        var bx=gx+bGap*(ti+1)+bW*ti;
        var sc=tiers[ti].scores[di];
        var bh=(sc-1)/4*PH, by=MT+PH-bh;
        var isH=(hD===di&&hT===ti);
        var alpha=(hD===null)?1:(isH?1:0.22);
        ctx.globalAlpha=alpha; ctx.fillStyle=tiers[ti].color;
        ctx.fillRect(bx,by,bW,bh); ctx.globalAlpha=1;
        hitRects.push({x:bx,y:by,w:bW,h:bh,di:di,ti:ti,sc:sc});
      }
      ctx.fillStyle=th.muted; ctx.font='9px monospace'; ctx.textAlign='center';
      ctx.fillText(dims[di],gx+gW/2,H-MB+14);
    }
  }
  draw(null,null);

  canvas.addEventListener('mousemove',function(e){
    var rect=canvas.getBoundingClientRect(), mx=e.clientX-rect.left, my=e.clientY-rect.top, hit=null;
    for(var i=0;i<hitRects.length;i++){ var r=hitRects[i]; if(mx>=r.x&&mx<=r.x+r.w&&my>=r.y&&my<=r.y+r.h){hit=r;break;} }
    if(hit){
      draw(hit.di,hit.ti);
      tooltip.style.display='block'; tooltip.style.left=(e.clientX+12)+'px'; tooltip.style.top=(e.clientY-10)+'px';
      tooltip.innerHTML='<strong>'+tiers[hit.ti].name+'</strong><br><em>'+dims[hit.di]+'</em>: '+hit.sc+'/5<br><span style="color:var(--global-text-color-light)">'+tiers[hit.ti].detail.replace(/\n/g,'<br>')+'</span>';
      canvas.style.cursor='pointer';
    } else { draw(null,null); tooltip.style.display='none'; canvas.style.cursor='default'; }
  });
  canvas.addEventListener('mouseleave',function(){ draw(null,null); tooltip.style.display='none'; });
})();
</script> <p>The chart shows the fundamental trade-off: cost and speed move in opposite directions from quality. A Flash model is 5× faster and much cheaper, but scores lower on complex reasoning. A Pro model excels at reasoning but is slower and expensive. Resource-aware optimization exploits this: use the cheap model where it’s good enough, reserve the expensive one for where it’s actually needed.</p> <hr/> <h2 id="the-three-agent-architecture">The Three-Agent Architecture</h2> <p>Resource-aware optimization is typically implemented as a three-agent system:</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">RESOURCE-AWARE OPTIMIZATION ARCHITECTURE</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:300px;"> <div class="ns-node-title">Incoming Query</div> <div class="ns-node-sub">User question, task, or request — unknown complexity at arrival time</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:340px;"> <div class="ns-node-title">Router Agent (Flash model)</div> <div class="ns-node-sub">Classifies complexity: simple / reasoning / internet_search. Uses a cheap model — routing cost must be much less than the savings it generates.</div> </div> <div class="ns-arrow"></div> <div class="ns-row" style="max-width:540px;"> <div class="ns-node ns-node-green"> <div class="ns-node-title">Flash Model</div> <div class="ns-node-sub">Simple queries · Fast · Low cost · High volume capacity</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">Pro Model</div> <div class="ns-node-sub">Complex reasoning · Thorough · High cost · Reserved for hard problems</div> </div> <div class="ns-node"> <div class="ns-node-title">Search + Model</div> <div class="ns-node-sub">Live data needed · Retrieve first · Synthesize second</div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:340px;"> <div class="ns-node-title">Critique Agent (optional)</div> <div class="ns-node-sub">Evaluates output quality. Feeds back into router logic — if Flash keeps failing on certain query types, router learns to route those to Pro instead.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:300px;"> <div class="ns-node-title">Final Response</div> <div class="ns-node-sub">Correct answer · Delivered within budget · Appropriate latency for the query type</div> </div> </div> </div> <hr/> <h2 id="interactive-query-router-demo">Interactive: Query Router Demo</h2> <p>See how the router classifies different queries and routes them to the right model:</p> <div class="rao-demo-wrapper"> <div class="rao-demo-header"> <span class="rao-demo-title">QUERY ROUTER LIVE DEMO</span> <button class="rao-demo-btn" id="raoDemoRunBtn" disabled="">▶ Route Query</button> </div> <div class="rao-demo-queries"> <button class="rao-q-btn active" data-idx="0" onclick="raoSelect(0)">Simple fact</button> <button class="rao-q-btn" data-idx="1" onclick="raoSelect(1)">Complex reasoning</button> <button class="rao-q-btn" data-idx="2" onclick="raoSelect(2)">Current events</button> <button class="rao-q-btn" data-idx="3" onclick="raoSelect(3)">Math problem</button> </div> <div class="rao-demo-body"> <div class="rao-demo-query-display"> <span class="rao-q-label">QUERY</span> <span class="rao-q-text" id="raoQueryText">What is the capital of Australia?</span> </div> <div class="rao-demo-steps" id="raoDemoSteps"></div> </div> </div> <style>.rao-demo-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.rao-demo-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.rao-demo-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.rao-demo-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.rao-demo-btn:not(:disabled):hover{background:rgba(38,152,186,0.12);border-color:#2698ba;color:#2698ba}.rao-demo-btn:disabled{opacity:.4;cursor:default}.rao-demo-queries{display:flex;border-bottom:1px solid var(--global-divider-color);overflow-x:auto}.rao-q-btn{flex:1;min-width:100px;padding:.5rem;font-family:monospace;font-size:.68rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;white-space:nowrap}.rao-q-btn:last-child{border-right:0}.rao-q-btn.active{background:rgba(38,152,186,0.1);color:#2698ba;font-weight:700}.rao-demo-body{padding:1rem 1.1rem;display:flex;flex-direction:column;gap:.75rem}.rao-demo-query-display{border:1px solid var(--global-divider-color);border-radius:6px;padding:.6rem .9rem;display:flex;align-items:center;gap:.75rem;background:rgba(128,128,128,0.04)}.rao-q-label{font-size:.6rem;font-weight:700;letter-spacing:.1em;color:#2698ba;flex-shrink:0}.rao-q-text{font-size:.82rem;color:var(--global-text-color);font-family:monospace}.rao-demo-steps{display:flex;flex-direction:column;gap:.5rem}.rao-step{border:1px solid var(--global-divider-color);border-radius:7px;padding:.65rem .85rem;animation:raoIn .3s ease}@keyframes raoIn{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}.rao-step-label{font-size:.6rem;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:var(--global-text-color-light);margin-bottom:.3rem}.rao-step-content{font-size:.8rem;color:var(--global-text-color);line-height:1.55}.rao-step.classify{border-color:rgba(201,122,242,0.3);background:rgba(201,122,242,0.05)}.rao-step.route{border-color:rgba(230,168,23,0.3);background:rgba(230,168,23,0.05)}.rao-step.answer{border-color:rgba(79,201,126,0.3);background:rgba(79,201,126,0.07)}.rao-step-model{font-size:.65rem;font-family:monospace;padding:.1em .4em;border-radius:3px;margin-left:.4rem}.rao-model-flash{background:rgba(79,201,126,0.15);color:#4fc97e;border:1px solid rgba(79,201,126,0.3)}.rao-model-pro{background:rgba(255,107,107,0.12);color:#ff6b6b;border:1px solid rgba(255,107,107,0.3)}.rao-model-search{background:rgba(38,152,186,0.12);color:#7dcfff;border:1px solid rgba(38,152,186,0.3)}</style> <script>
var RAO_QUERIES = [
  {
    text: "What is the capital of Australia?",
    classification: "simple",
    reason: "Direct factual recall — no reasoning chain needed. Any small model has this memorized.",
    model: "gpt-4o-mini (Flash)",
    modelClass: "flash",
    cost: "~$0.0003",
    latency: "0.8s",
    answer: "The capital of Australia is Canberra, not Sydney as many people assume. Canberra was purpose-built as a compromise between Sydney and Melbourne when Australia federated in 1901."
  },
  {
    text: "Should a startup use microservices or a monolith for its first product?",
    classification: "reasoning",
    reason: "Multi-factor trade-off analysis requiring understanding of team size, deployment complexity, technical debt, scalability timing. Needs sophisticated reasoning.",
    model: "gemini-2.5-pro (Pro)",
    modelClass: "pro",
    cost: "~$0.015",
    latency: "8.2s",
    answer: "For a first product, almost always start with a monolith. Microservices add operational overhead (service discovery, network latency, distributed tracing) that slows down a small team. Migrate to microservices only after you understand the natural seam lines in your domain — typically when you have 5+ teams working on the same codebase."
  },
  {
    text: "What did the Fed announce about interest rates this week?",
    classification: "internet_search",
    reason: "Requires real-time information from this week — no training data can contain this. Must search the web first, then synthesize.",
    model: "Search → gpt-4o (Standard)",
    modelClass: "search",
    cost: "~$0.008",
    latency: "4.5s",
    answer: "[After web search] Based on recent news: The Federal Reserve held rates steady at 4.25–4.50% at their latest FOMC meeting, citing continued progress on inflation but noting uncertainty around trade policy impacts. (Source: Reuters, this week)"
  },
  {
    text: "If a train leaves Chicago at 9am going 80mph, and another leaves Boston at 10am going 65mph, when do they meet (1,000 miles apart)?",
    classification: "reasoning",
    reason: "Multi-step mathematical word problem requiring algebraic reasoning. Flash models often make arithmetic errors on word problems; Pro model handles with higher reliability.",
    model: "o4-mini (Reasoning)",
    modelClass: "pro",
    cost: "~$0.012",
    latency: "6.1s",
    answer: "Train A travels at 80mph from mile 0. Train B travels at 65mph from mile 1000 toward Train A. They approach at 80+65=145mph. Train B starts 1 hour later, so by 10am Train A is at mile 80. Remaining gap: 920 miles at 145mph combined = 6.34 hours. They meet at approximately 4:20pm, roughly 587 miles from Chicago."
  }
];

var raoIdx = 0;
var raoRunning = false;

function raoSelect(idx) {
  raoIdx = idx;
  document.querySelectorAll('.rao-q-btn').forEach(function(b){ b.classList.remove('active'); });
  document.querySelector('.rao-q-btn[data-idx="'+idx+'"]').classList.add('active');
  document.getElementById('raoQueryText').textContent = RAO_QUERIES[idx].text;
  document.getElementById('raoDemoSteps').innerHTML = '';
  var btn = document.getElementById('raoDemoRunBtn');
  btn.disabled = false;
  btn.textContent = '▶ Route Query';
}

document.addEventListener('DOMContentLoaded', function(){
  raoSelect(0);
  var btn = document.getElementById('raoDemoRunBtn');
  btn.addEventListener('click', async function(){
    if (raoRunning) return;
    raoRunning = true;
    btn.disabled = true;
    btn.textContent = '⏳ Routing…';

    var steps = document.getElementById('raoDemoSteps');
    steps.innerHTML = '';
    var q = RAO_QUERIES[raoIdx];

    await new Promise(function(r){ setTimeout(r, 500); });
    var s1 = document.createElement('div'); s1.className = 'rao-step classify';
    s1.innerHTML = '<div class="rao-step-label">01 — Classification (Router Agent)</div>' +
      '<div class="rao-step-content"><strong>Category: ' + q.classification + '</strong><br>' + q.reason + '</div>';
    steps.appendChild(s1);

    await new Promise(function(r){ setTimeout(r, 700); });
    var s2 = document.createElement('div'); s2.className = 'rao-step route';
    s2.innerHTML = '<div class="rao-step-label">02 — Model Selection</div>' +
      '<div class="rao-step-content">Selected: <strong>' + q.model + '</strong>' +
      '<span class="rao-step-model rao-model-' + q.modelClass + '">' + q.modelClass.toUpperCase() + '</span><br>' +
      'Estimated cost: <strong>' + q.cost + '</strong> &nbsp;·&nbsp; Expected latency: <strong>' + q.latency + '</strong></div>';
    steps.appendChild(s2);

    await new Promise(function(r){ setTimeout(r, 800); });
    var s3 = document.createElement('div'); s3.className = 'rao-step answer';
    s3.innerHTML = '<div class="rao-step-label">03 — Response</div>' +
      '<div class="rao-step-content">' + q.answer + '</div>';
    steps.appendChild(s3);

    raoRunning = false;
    btn.textContent = '↺ Try Again';
    btn.disabled = false;
  });
});
</script> <hr/> <h2 id="the-code-three-implementations">The Code: Three Implementations</h2> <h3 id="implementation-1-adk-with-model-tiers">Implementation 1: ADK with Model Tiers</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span>

<span class="c1"># Tier 1: Fast, cheap — for simple queries, routing, classification
</span><span class="n">gemini_flash_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">GeminiFlashAgent</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.5-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">A fast, cost-efficient agent for simple, well-defined queries.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Answer concisely. For factual questions, give the direct answer without elaboration. Be fast.</span><span class="sh">"</span>
<span class="p">)</span>

<span class="c1"># Tier 2: Powerful, expensive — for complex reasoning
</span><span class="n">gemini_pro_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">GeminiProAgent</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.5-pro</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">A highly capable agent for complex analytical and reasoning tasks.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Take your time to reason carefully. Show your thought process. Prioritize accuracy over brevity.</span><span class="sh">"</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why different instructions for different tiers?</strong> The instruction shapes the model’s behavior beyond just its capability. The Flash agent is told to be concise and fast — it shouldn’t pad its responses. The Pro agent is told to reason carefully and show work — it should use its full capability, even if that means longer responses. The instruction <em>activates</em> the tier’s strengths.</p> </blockquote> <h3 id="implementation-2-query-router-agent">Implementation 2: Query Router Agent</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">BaseAgent</span>
<span class="kn">from</span> <span class="n">google.adk.events</span> <span class="kn">import</span> <span class="n">Event</span>
<span class="kn">from</span> <span class="n">google.adk.agents.invocation_context</span> <span class="kn">import</span> <span class="n">InvocationContext</span>
<span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">AsyncGenerator</span>

<span class="k">class</span> <span class="nc">QueryRouterAgent</span><span class="p">(</span><span class="n">BaseAgent</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Routes incoming queries to the appropriate model tier based on complexity.</span><span class="sh">"""</span>
    <span class="n">name</span><span class="p">:</span>        <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">QueryRouter</span><span class="sh">"</span>
    <span class="n">description</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Routes queries to Flash (simple) or Pro (complex) based on analysis.</span><span class="sh">"</span>

    <span class="k">async</span> <span class="k">def</span> <span class="nf">_run_async_impl</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> <span class="n">context</span><span class="p">:</span> <span class="n">InvocationContext</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">AsyncGenerator</span><span class="p">[</span><span class="n">Event</span><span class="p">,</span> <span class="bp">None</span><span class="p">]:</span>
        <span class="n">user_query</span>    <span class="o">=</span> <span class="n">context</span><span class="p">.</span><span class="n">current_message</span><span class="p">.</span><span class="n">text</span>
        <span class="n">query_words</span>   <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">user_query</span><span class="p">.</span><span class="nf">split</span><span class="p">())</span>
        <span class="n">query_lower</span>   <span class="o">=</span> <span class="n">user_query</span><span class="p">.</span><span class="nf">lower</span><span class="p">()</span>

        <span class="c1"># Complexity signals
</span>        <span class="n">is_long_query</span>      <span class="o">=</span> <span class="n">query_words</span> <span class="o">&gt;</span> <span class="mi">20</span>
        <span class="n">needs_reasoning</span>    <span class="o">=</span> <span class="nf">any</span><span class="p">(</span><span class="n">kw</span> <span class="ow">in</span> <span class="n">query_lower</span> <span class="k">for</span> <span class="n">kw</span> <span class="ow">in</span>
                                <span class="p">[</span><span class="sh">'</span><span class="s">why</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">how</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">explain</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">compare</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">analyze</span><span class="sh">'</span><span class="p">,</span>
                                 <span class="sh">'</span><span class="s">evaluate</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">design</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">recommend</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">trade-off</span><span class="sh">'</span><span class="p">])</span>
        <span class="n">needs_current_data</span> <span class="o">=</span> <span class="nf">any</span><span class="p">(</span><span class="n">kw</span> <span class="ow">in</span> <span class="n">query_lower</span> <span class="k">for</span> <span class="n">kw</span> <span class="ow">in</span>
                                <span class="p">[</span><span class="sh">'</span><span class="s">today</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">this week</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">latest</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">current</span><span class="sh">'</span><span class="p">,</span>
                                 <span class="sh">'</span><span class="s">recent</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">now</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">2025</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">2026</span><span class="sh">'</span><span class="p">])</span>
        <span class="n">needs_math</span>         <span class="o">=</span> <span class="nf">any</span><span class="p">(</span><span class="n">kw</span> <span class="ow">in</span> <span class="n">query_lower</span> <span class="k">for</span> <span class="n">kw</span> <span class="ow">in</span>
                                <span class="p">[</span><span class="sh">'</span><span class="s">calculate</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">solve</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">equation</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">probability</span><span class="sh">'</span><span class="p">,</span>
                                 <span class="sh">'</span><span class="s">percent</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">if x then</span><span class="sh">'</span><span class="p">])</span>

        <span class="c1"># Routing decision
</span>        <span class="k">if</span> <span class="n">needs_current_data</span><span class="p">:</span>
            <span class="n">route</span> <span class="o">=</span> <span class="sh">"</span><span class="s">search_and_synthesize</span><span class="sh">"</span>
            <span class="n">model_used</span> <span class="o">=</span> <span class="sh">"</span><span class="s">google_search + gemini-2.5-flash</span><span class="sh">"</span>
        <span class="k">elif</span> <span class="n">needs_reasoning</span> <span class="ow">or</span> <span class="n">needs_math</span> <span class="ow">or</span> <span class="n">is_long_query</span><span class="p">:</span>
            <span class="n">route</span> <span class="o">=</span> <span class="sh">"</span><span class="s">complex</span><span class="sh">"</span>
            <span class="n">model_used</span> <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.5-pro</span><span class="sh">"</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">route</span> <span class="o">=</span> <span class="sh">"</span><span class="s">simple</span><span class="sh">"</span>
            <span class="n">model_used</span> <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.5-flash</span><span class="sh">"</span>

        <span class="k">yield</span> <span class="nc">Event</span><span class="p">(</span>
            <span class="n">author</span>  <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">name</span><span class="p">,</span>
            <span class="n">content</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Routing </span><span class="sh">'</span><span class="si">{</span><span class="n">user_query</span><span class="p">[</span><span class="si">:</span><span class="mi">50</span><span class="p">]</span><span class="si">}</span><span class="s">...</span><span class="sh">'</span><span class="s"> → </span><span class="si">{</span><span class="n">route</span><span class="si">}</span><span class="s"> (model: </span><span class="si">{</span><span class="n">model_used</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span>
        <span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Rule-based vs LLM-based routing.</strong> This implementation uses keyword matching — fast and predictable, but brittle. It will misclassify edge cases (e.g., “why is the sky blue?” is classified as complex because of “why” but is actually simple). A more sophisticated router would use an LLM to classify queries based on semantic understanding rather than keywords. The trade-off: LLM routing is ~$0.0001 per classification call but far more accurate. Use keyword routing for high-volume, cost-critical systems; use LLM routing when accuracy matters more than classification cost.</p> </blockquote> <blockquote> <p><strong>Why check <code class="language-plaintext highlighter-rouge">needs_current_data</code> first?</strong> The routing conditions are evaluated in priority order. If a query needs current data, that overrides complexity — even a simple question about today’s news requires search. By checking this first, you ensure real-time queries always get search capability, not just a model upgrade.</p> </blockquote> <h3 id="implementation-3-the-critique-agent">Implementation 3: The Critique Agent</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">CRITIC_SYSTEM_PROMPT</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">
You are the Critique Agent — the quality assurance layer of our multi-model system.
Your function: evaluate responses from other models and identify systematic failures.

For each response you review, assess:
1. ACCURACY: Is the factual content correct?
2. COMPLETENESS: Does it fully address the question?
3. APPROPRIATE DEPTH: Is the detail level right for the question</span><span class="sh">'</span><span class="s">s complexity?
4. ROUTING SIGNAL: Was this the right model for this query type?

Return structured feedback:
- verdict: </span><span class="sh">"</span><span class="s">correct</span><span class="sh">"</span><span class="s"> | </span><span class="sh">"</span><span class="s">needs_refinement</span><span class="sh">"</span><span class="s"> | </span><span class="sh">"</span><span class="s">wrong_model</span><span class="sh">"</span><span class="s">
- if wrong_model: suggest </span><span class="sh">"</span><span class="s">should_use_flash</span><span class="sh">"</span><span class="s"> | </span><span class="sh">"</span><span class="s">should_use_pro</span><span class="sh">"</span><span class="s">
- specific_issues: list of concrete problems found
- confidence: 0.0 to 1.0

Be constructive. Flag systematic misrouting — if Flash keeps struggling with
a category, that</span><span class="sh">'</span><span class="s">s a signal to update the router</span><span class="sh">'</span><span class="s">s classification rules.
</span><span class="sh">"""</span>
</code></pre></div></div> <blockquote> <p><strong>Why “wrong_model” is a routing signal, not just a quality signal.</strong> When the Critique Agent says “wrong_model: should_use_flash,” it’s identifying a case where the router sent a simple query to the Pro model unnecessarily — overspending. When it says “wrong_model: should_use_pro,” it’s identifying a case where the router sent a complex query to Flash — producing a poor result. Both are actionable: the first wastes money, the second wastes quality. Tracking these misrouting events over time reveals systematic biases in the router’s classification logic that can be corrected.</p> </blockquote> <hr/> <h2 id="openrouter-multi-provider-optimization">OpenRouter: Multi-Provider Optimization</h2> <p>OpenRouter is a third-party service that simplifies resource-aware optimization by providing:</p> <ul> <li>A single API endpoint for 200+ models from all major providers</li> <li>Automatic model selection (<code class="language-plaintext highlighter-rouge">openrouter/auto</code>)</li> <li>Sequential fallback chains</li> <li>Real-time pricing and latency data</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">requests</span><span class="p">,</span> <span class="n">json</span>

<span class="c1"># Simple request with automatic model selection
</span><span class="n">response</span> <span class="o">=</span> <span class="n">requests</span><span class="p">.</span><span class="nf">post</span><span class="p">(</span>
    <span class="n">url</span>     <span class="o">=</span> <span class="sh">"</span><span class="s">https://openrouter.ai/api/v1/chat/completions</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">headers</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">Authorization</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Bearer </span><span class="si">{</span><span class="n">OPENROUTER_API_KEY</span><span class="si">}</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">X-Title</span><span class="sh">"</span><span class="p">:</span>       <span class="sh">"</span><span class="s">My Agent App</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">},</span>
    <span class="n">data</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span>
        <span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">openrouter/auto</span><span class="sh">"</span><span class="p">,</span>     <span class="c1"># ← OpenRouter picks the best model for this query
</span>        <span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span>
            <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">What is the capital of France?</span><span class="sh">"</span><span class="p">}</span>
        <span class="p">]</span>
    <span class="p">})</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">"model": "openrouter/auto"</code></strong>: OpenRouter analyzes the prompt and selects the most cost-effective model that can handle it well. For a simple factual question, it might choose a Flash-tier model. For a complex reasoning question, it upgrades automatically. The selection considers: prompt complexity, available model performance data, current pricing, and latency requirements.</p> </blockquote> <h3 id="sequential-fallback-chain">Sequential Fallback Chain</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Fallback chain: try Claude 3.5 Sonnet first, fall back to a cheaper model if it fails
</span><span class="n">response</span> <span class="o">=</span> <span class="n">requests</span><span class="p">.</span><span class="nf">post</span><span class="p">(</span>
    <span class="n">url</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">https://openrouter.ai/api/v1/chat/completions</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">headers</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">Authorization</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Bearer </span><span class="si">{</span><span class="n">OPENROUTER_API_KEY</span><span class="si">}</span><span class="sh">"</span><span class="p">},</span>
    <span class="n">data</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span>
        <span class="sh">"</span><span class="s">models</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span>
            <span class="sh">"</span><span class="s">anthropic/claude-sonnet-4-5</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># Try first — preferred quality
</span>            <span class="sh">"</span><span class="s">openai/gpt-4o-mini</span><span class="sh">"</span><span class="p">,</span>           <span class="c1"># Fallback 1 — if Claude is unavailable
</span>            <span class="sh">"</span><span class="s">google/gemini-flash-1.5</span><span class="sh">"</span><span class="p">,</span>      <span class="c1"># Fallback 2 — cheapest option
</span>        <span class="p">],</span>
        <span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">:</span> <span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">user_query</span><span class="p">}]</span>
    <span class="p">})</span>
<span class="p">)</span>

<span class="c1"># The response includes which model was actually used
</span><span class="n">model_used</span> <span class="o">=</span> <span class="n">response</span><span class="p">.</span><span class="nf">json</span><span class="p">()[</span><span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">]</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Served by: </span><span class="si">{</span><span class="n">model_used</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why the fallback chain is valuable in production.</strong> LLM APIs have outages, rate limits, and regional availability issues. If you’re hard-coded to one model and it goes down, your entire application fails. The fallback chain provides automatic resilience: Claude is down → seamlessly serve from GPT-4o-mini → users never notice. This is <a href="/kohshh-portfolio/blog/2026/exception-handling/">graceful degradation</a> (Chapter 12) applied at the model layer.</p> </blockquote> <blockquote> <p><strong>The response always tells you which model was used.</strong> This is critical for cost tracking, quality auditing, and identifying when fallbacks fire more than expected. If you notice you’re serving from the fallback 30% of the time, that’s a signal your primary model has reliability issues you should address.</p> </blockquote> <hr/> <h2 id="implementation-4-full-three-tier-system-with-openai">Implementation 4: Full Three-Tier System with OpenAI</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span><span class="p">,</span> <span class="n">json</span><span class="p">,</span> <span class="n">requests</span>
<span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>
<span class="kn">from</span> <span class="n">dotenv</span> <span class="kn">import</span> <span class="n">load_dotenv</span>

<span class="nf">load_dotenv</span><span class="p">()</span>
<span class="n">client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">(</span><span class="n">api_key</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">"</span><span class="s">OPENAI_API_KEY</span><span class="sh">"</span><span class="p">))</span>

<span class="k">def</span> <span class="nf">classify_prompt</span><span class="p">(</span><span class="n">prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Use a cheap model to classify query complexity — the router itself must be cheap.</span><span class="sh">"""</span>
    <span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
        <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gpt-4o-mini</span><span class="sh">"</span><span class="p">,</span>   <span class="c1"># Cheapest capable classifier
</span>        <span class="n">temperature</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span>               <span class="c1"># Deterministic — routing must be consistent
</span>        <span class="n">messages</span>    <span class="o">=</span> <span class="p">[</span>
            <span class="p">{</span>
                <span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span>
                <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="p">(</span>
                    <span class="sh">"</span><span class="s">Classify the user prompt into exactly one category: </span><span class="sh">"</span>
                    <span class="sh">"</span><span class="s">simple, reasoning, or internet_search.</span><span class="se">\n\n</span><span class="sh">"</span>
                    <span class="sh">"</span><span class="s">- simple: direct factual questions answerable from training data</span><span class="se">\n</span><span class="sh">"</span>
                    <span class="sh">"</span><span class="s">- reasoning: logic, math, multi-step analysis, trade-off evaluation</span><span class="se">\n</span><span class="sh">"</span>
                    <span class="sh">"</span><span class="s">- internet_search: requires current data, real-time events, recent news</span><span class="se">\n\n</span><span class="sh">"</span>
                    <span class="sh">"</span><span class="s">Respond ONLY with JSON: {</span><span class="se">\"</span><span class="s">classification</span><span class="se">\"</span><span class="s">: </span><span class="se">\"</span><span class="s">&lt;category&gt;</span><span class="se">\"</span><span class="s">}</span><span class="sh">"</span>
                <span class="p">)</span>
            <span class="p">},</span>
            <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">prompt</span><span class="p">}</span>
        <span class="p">]</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">json</span><span class="p">.</span><span class="nf">loads</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">choices</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">message</span><span class="p">.</span><span class="n">content</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">temperature=0</code> for the classifier.</strong> The classification decision must be deterministic and consistent. If the same query is classified differently on each call, the system becomes unpredictable — the same query might get routed to Flash one time and Pro the next, producing different costs and quality. <code class="language-plaintext highlighter-rouge">temperature=0</code> ensures the same input always produces the same classification.</p> </blockquote> <blockquote> <p><strong>Why use <code class="language-plaintext highlighter-rouge">gpt-4o-mini</code> for classification, not a cheaper model?</strong> The classifier needs to reliably distinguish “simple,” “reasoning,” and “internet_search” — this requires semantic understanding beyond keyword matching. Using a model that’s too cheap might produce unreliable classifications, routing complex queries to Flash (wrong quality) or simple queries to Pro (wrong cost). The classification model should be the cheapest model that classifies accurately for your query distribution.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">generate_response</span><span class="p">(</span><span class="n">prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">classification</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">search_results</span><span class="o">=</span><span class="bp">None</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">tuple</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Route to the appropriate model based on classification.</span><span class="sh">"""</span>
    <span class="k">if</span> <span class="n">classification</span> <span class="o">==</span> <span class="sh">"</span><span class="s">simple</span><span class="sh">"</span><span class="p">:</span>
        <span class="n">model</span>      <span class="o">=</span> <span class="sh">"</span><span class="s">gpt-4o-mini</span><span class="sh">"</span>    <span class="c1"># $0.15/1M input, $0.60/1M output
</span>        <span class="n">full_prompt</span> <span class="o">=</span> <span class="n">prompt</span>

    <span class="k">elif</span> <span class="n">classification</span> <span class="o">==</span> <span class="sh">"</span><span class="s">reasoning</span><span class="sh">"</span><span class="p">:</span>
        <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">o4-mini</span><span class="sh">"</span>       <span class="c1"># Reasoning model with chain-of-thought
</span>        <span class="n">full_prompt</span> <span class="o">=</span> <span class="n">prompt</span>

    <span class="k">elif</span> <span class="n">classification</span> <span class="o">==</span> <span class="sh">"</span><span class="s">internet_search</span><span class="sh">"</span><span class="p">:</span>
        <span class="n">model</span> <span class="o">=</span> <span class="sh">"</span><span class="s">gpt-4o</span><span class="sh">"</span>              <span class="c1"># Standard model for synthesis with search context
</span>        <span class="k">if</span> <span class="n">search_results</span><span class="p">:</span>
            <span class="n">search_context</span> <span class="o">=</span> <span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">([</span>
                <span class="sa">f</span><span class="sh">"</span><span class="s">Title: </span><span class="si">{</span><span class="n">r</span><span class="p">[</span><span class="sh">'</span><span class="s">title</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="se">\n</span><span class="s">Snippet: </span><span class="si">{</span><span class="n">r</span><span class="p">[</span><span class="sh">'</span><span class="s">snippet</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="se">\n</span><span class="s">URL: </span><span class="si">{</span><span class="n">r</span><span class="p">[</span><span class="sh">'</span><span class="s">link</span><span class="sh">'</span><span class="p">]</span><span class="si">}</span><span class="sh">"</span>
                <span class="k">for</span> <span class="n">r</span> <span class="ow">in</span> <span class="n">search_results</span>
            <span class="p">])</span>
            <span class="n">full_prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"""</span><span class="s">Use ONLY these web results to answer. Cite sources.</span><span class="se">\n\n</span><span class="si">{</span><span class="n">search_context</span><span class="si">}</span><span class="se">\n\n</span><span class="s">Question: </span><span class="si">{</span><span class="n">prompt</span><span class="si">}</span><span class="sh">"""</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">full_prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Note: web search returned no results. Answer from training data with a caveat.</span><span class="se">\n\n</span><span class="si">{</span><span class="n">prompt</span><span class="si">}</span><span class="sh">"</span>

    <span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
        <span class="n">model</span>    <span class="o">=</span> <span class="n">model</span><span class="p">,</span>
        <span class="n">messages</span> <span class="o">=</span> <span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">full_prompt</span><span class="p">}],</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">response</span><span class="p">.</span><span class="n">choices</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">message</span><span class="p">.</span><span class="n">content</span><span class="p">,</span> <span class="n">model</span>
</code></pre></div></div> <blockquote> <p><strong>Why different models for different categories?</strong> Each model was chosen for the category it dominates:</p> <ul> <li><code class="language-plaintext highlighter-rouge">gpt-4o-mini</code> for “simple”: cheapest model that handles factual recall with high reliability</li> <li><code class="language-plaintext highlighter-rouge">o4-mini</code> for “reasoning”: the <code class="language-plaintext highlighter-rouge">o</code>-series models have explicit chain-of-thought reasoning built in, dramatically improving performance on math and multi-step logic vs standard models</li> <li><code class="language-plaintext highlighter-rouge">gpt-4o</code> for “search”: synthesis tasks require reading and integrating retrieved text, a task standard models handle well; the full <code class="language-plaintext highlighter-rouge">gpt-4o</code> is better than mini for this use case</li> </ul> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">handle_prompt</span><span class="p">(</span><span class="n">prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Orchestrate the full resource-aware pipeline.</span><span class="sh">"""</span>
    <span class="c1"># Step 1: Classify (cheap)
</span>    <span class="n">classification</span> <span class="o">=</span> <span class="nf">classify_prompt</span><span class="p">(</span><span class="n">prompt</span><span class="p">)[</span><span class="sh">"</span><span class="s">classification</span><span class="sh">"</span><span class="p">]</span>

    <span class="c1"># Step 2: Retrieve if needed (medium cost)
</span>    <span class="n">search_results</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="k">if</span> <span class="n">classification</span> <span class="o">==</span> <span class="sh">"</span><span class="s">internet_search</span><span class="sh">"</span><span class="p">:</span>
        <span class="n">search_results</span> <span class="o">=</span> <span class="nf">google_search</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>

    <span class="c1"># Step 3: Generate (model cost depends on classification)
</span>    <span class="n">answer</span><span class="p">,</span> <span class="n">model_used</span> <span class="o">=</span> <span class="nf">generate_response</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">classification</span><span class="p">,</span> <span class="n">search_results</span><span class="p">)</span>

    <span class="k">return</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">classification</span><span class="sh">"</span><span class="p">:</span> <span class="n">classification</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">model_used</span><span class="sh">"</span><span class="p">:</span>     <span class="n">model_used</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">response</span><span class="sh">"</span><span class="p">:</span>       <span class="n">answer</span><span class="p">,</span>
    <span class="p">}</span>
</code></pre></div></div> <blockquote> <p><strong>The pipeline’s cost structure.</strong> Every query pays the classification cost (cheap, ~$0.0001). Then the routing kicks in:</p> <ul> <li>Simple queries: pay cheap generation cost (~$0.0003) → total ~$0.0004</li> <li>Reasoning queries: pay Pro generation cost (~$0.015) → total ~$0.0151</li> <li>Search queries: pay search API cost (~$0.005) + standard generation (~$0.008) → total ~$0.0131</li> </ul> <p>The classification cost is negligible. The savings come from routing ~70-80% of queries to cheap models instead of expensive ones.</p> </blockquote> <hr/> <h2 id="the-nine-optimization-techniques">The Nine Optimization Techniques</h2> <p>Resource-aware optimization extends beyond model switching. Here’s the full spectrum:</p> <div class="rao-techniques-grid"> <div class="rao-tech-card"> <span class="rao-tech-num">01</span> <h4>Dynamic Model Switching</h4> <p>Route to different model tiers based on query complexity. The pattern we've been building throughout this chapter.</p> </div> <div class="rao-tech-card"> <span class="rao-tech-num">02</span> <h4>Adaptive Tool Selection</h4> <p>Choose between tools based on cost and speed. Use a cached database lookup before an expensive live API call. Prefer a local index before a web search.</p> </div> <div class="rao-tech-card"> <span class="rao-tech-num">03</span> <h4>Contextual Pruning</h4> <p>Summarize or truncate long conversation history before sending to the LLM. Fewer input tokens = lower cost. Prioritize the most recent and most relevant context.</p> </div> <div class="rao-tech-card"> <span class="rao-tech-num">04</span> <h4>Proactive Resource Prediction</h4> <p>Forecast expected query volume and provision resources in advance. Avoids cold-start latency and capacity shortfalls during peak traffic.</p> </div> <div class="rao-tech-card"> <span class="rao-tech-num">05</span> <h4>Cost-Sensitive Multi-Agent Coordination</h4> <p>Optimize communication costs between agents in addition to computation. Batch small messages, compress serialized state, avoid redundant cross-agent calls.</p> </div> <div class="rao-tech-card"> <span class="rao-tech-num">06</span> <h4>Energy-Efficient Deployment</h4> <p>For edge devices (IoT, mobile), minimize inference frequency and prefer quantized, compressed models. Important for battery-constrained environments.</p> </div> <div class="rao-tech-card"> <span class="rao-tech-num">07</span> <h4>Parallelization Awareness</h4> <p>When multiple independent sub-tasks exist, parallelize them (Chapter 3 pattern). Total time = max(T_tasks), not sum(T_tasks). Coordinate resource allocation across parallel branches.</p> </div> <div class="rao-tech-card"> <span class="rao-tech-num">08</span> <h4>Learned Resource Allocation</h4> <p>Use the Critique Agent's feedback over time to retrain the router. If Flash consistently fails on certain query patterns, update the classifier. The system improves from its own production traffic.</p> </div> <div class="rao-tech-card"> <span class="rao-tech-num">09</span> <h4>Graceful Degradation + Fallback</h4> <p>When primary resources are exhausted or unavailable, automatically fall back to cheaper/simpler alternatives. Serve a reduced-quality response rather than failing completely. (Chapter 12 pattern applied at the resource layer.)</p> </div> </div> <style>.rao-techniques-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:.85rem;margin:1.5rem 0}.rao-tech-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.rao-tech-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#e6a817}.rao-tech-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.rao-tech-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}</style> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="rao-summary-card"> <div class="rao-summary-col"> <div class="rao-summary-label">WHAT</div> <p>A pattern for dynamically matching computational resources to task requirements. Simple queries route to cheap, fast models. Complex queries route to powerful, expensive ones. A critique agent monitors quality and improves routing over time.</p> </div> <div class="rao-summary-divider"></div> <div class="rao-summary-col"> <div class="rao-summary-label">WHY</div> <p>Routing all queries to the most capable (and most expensive) model wastes 70-90% of API costs on queries that a much cheaper model handles equally well. Resource-aware optimization preserves quality where it matters while eliminating waste where it doesn't.</p> </div> <div class="rao-summary-divider"></div> <div class="rao-summary-col"> <div class="rao-summary-label">RULE OF THUMB</div> <p>Start by measuring your query distribution: what fraction is simple, complex, or search-requiring? If &gt;50% is simple, this pattern pays for itself immediately. Use OpenRouter for easy multi-model fallback; build custom routers when query distribution requires specialized classification.</p> </div> </div> <style>.rao-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.rao-summary-card{flex-direction:column}}.rao-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.rao-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.rao-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.rao-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#e6a817}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li> <p><strong>Over-provisioning is the default failure mode.</strong> Without resource optimization, developers pick one capable model and route everything to it. This works but wastes 70-90% of API budget on queries that didn’t need that capability.</p> </li> <li> <p><strong>The routing cost must be much less than the savings.</strong> If classifying a query costs $0.001 and the savings from routing to a cheaper model is $0.0005, routing costs more than it saves. Use the cheapest reliable classifier — <code class="language-plaintext highlighter-rouge">gpt-4o-mini</code>, Gemini Flash, or keyword matching.</p> </li> <li> <p><strong><code class="language-plaintext highlighter-rouge">temperature=0</code> for all routing and classification calls.</strong> The routing decision must be deterministic. Non-zero temperature introduces randomness into which model serves a query, making cost and quality unpredictable and unreproducible.</p> </li> <li> <p><strong>The three-tier classification (simple / reasoning / internet_search) covers 95% of cases.</strong> These three categories map cleanly to different computational requirements: factual recall, chain-of-thought reasoning, and real-time retrieval. Most production systems can start with just these three.</p> </li> <li> <p><strong>OpenRouter provides zero-code fallback chains.</strong> Instead of implementing retry/fallback logic yourself, the <code class="language-plaintext highlighter-rouge">"models": [list]</code> parameter in OpenRouter handles it automatically. The simplest resilient multi-model system is a one-line configuration change.</p> </li> <li> <p><strong>The Critique Agent converts production traffic into training data.</strong> Every query where the Critique Agent says “wrong_model” is a labeled example of router misclassification. Collect these, analyze the patterns, and update the router’s classification logic. The system improves itself from its own errors.</p> </li> <li> <p><strong>Model tiers are not fixed — the landscape changes fast.</strong> What’s “Pro” today becomes “Flash” next year. Gemini 2.5 Flash matches the capability of last year’s Pro models. Build your resource-aware system with abstraction between the routing logic and the specific model names — use configuration or constants, not hardcoded strings, so updating model choices requires changing one line not twenty.</p> </li> </ul>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="optimization"/><category term="cost"/><category term="routing"/><summary type="html"><![CDATA[Not every question needs a supercomputer. Resource-aware optimization routes simple queries to cheap, fast models and reserves expensive, powerful ones for genuinely hard problems — saving cost without sacrificing quality.]]></summary></entry><entry><title type="html">Chapter 15: Inter-Agent Communication (A2A)</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/a2a/" rel="alternate" type="text/html" title="Chapter 15: Inter-Agent Communication (A2A)"/><published>2026-03-02T00:00:00+00:00</published><updated>2026-03-02T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/a2a</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/a2a/"><![CDATA[<h2 id="the-missing-link-in-multi-agent-systems">The Missing Link in Multi-Agent Systems</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Interoperability:</strong> The ability of different systems — built by different companies, using different technologies — to work together without custom translation layers. USB is an interoperable standard. HTTP is an interoperable standard. A2A aims to be the interoperable standard for agent communication.</p> <p style="margin-top:0.5rem"><strong>Protocol:</strong> A set of agreed-upon rules for communication. (Covered in Chapter 10 — MCP.) A2A is a protocol specifically for agent-to-agent communication over HTTP.</p> <p style="margin-top:0.5rem"><strong>JSON-RPC 2.0:</strong> A protocol for calling functions remotely using JSON messages. Format: <code>{"jsonrpc":"2.0","method":"sendTask","params":{...},"id":1}</code>. A2A uses JSON-RPC 2.0 as its message format over HTTP/HTTPS.</p> <p style="margin-top:0.5rem"><strong>Server-Sent Events (SSE):</strong> A web standard where a server maintains a persistent HTTP connection and pushes data to the client over time, without the client needing to make repeated requests. Used in A2A for streaming updates from agent to agent.</p> <p style="margin-top:0.5rem"><strong>mTLS (Mutual TLS):</strong> A security protocol where BOTH sides of a connection authenticate themselves — not just the server proving its identity to the client, but the client also proving its identity to the server. Prevents unauthorized agents from connecting to your A2A server.</p> <p style="margin-top:0.5rem"><strong>Opaque system:</strong> A system where the internal implementation is hidden from the caller. An A2A server is "opaque" to its client — the client knows what the server can do (from the Agent Card) but not how it does it. This is essential for framework independence.</p> <p style="margin-top:0.5rem"><strong>Webhook:</strong> A URL that a server can call to notify another system of an event. In A2A, a client can register a webhook URL and the server will POST to it when a long-running task completes — no need to poll.</p> </div> <p>In <a href="/kohshh-portfolio/blog/2026/multi-agent/">Chapter 7</a>, we built multi-agent systems where multiple agents collaborate. In <a href="/kohshh-portfolio/blog/2026/mcp/">Chapter 10</a>, we standardized how agents connect to tools via MCP. But there’s a gap that neither pattern addresses:</p> <p><strong>What happens when an agent built in Google ADK needs to collaborate with an agent built in LangGraph, which needs to call an agent built in CrewAI?</strong></p> <p>Without a standard, each pair of collaborating agents needs a custom integration — agent A knows how to call agent B using B’s specific API format, agent B knows how to call C using C’s format, and so on. This is the same integration explosion problem that MCP solved for tools — but at the agent layer.</p> <p>The table below shows why the problem gets serious fast:</p> <table> <thead> <tr> <th>Agents</th> <th>Framework pairs</th> <th>Custom integrations needed (without A2A)</th> </tr> </thead> <tbody> <tr> <td>5</td> <td>10 pairs</td> <td>10 integrations</td> </tr> <tr> <td>10</td> <td>45 pairs</td> <td>45 integrations</td> </tr> <tr> <td>50</td> <td>1,225 pairs</td> <td>1,225 integrations</td> </tr> </tbody> </table> <p><strong>Agent2Agent (A2A)</strong> is Google’s open protocol, launched in April 2025, that solves this. It defines a universal communication standard for agents — any agent that speaks A2A can work with any other A2A-compliant agent, regardless of whether they’re built with ADK, LangGraph, CrewAI, Azure AI Foundry, or any other framework.</p> <hr/> <h2 id="a2a-vs-mcp-two-complementary-protocols">A2A vs MCP: Two Complementary Protocols</h2> <p>Before going deeper into A2A, the most important conceptual clarification:</p> <div class="a2a-comparison-split"> <div class="a2a-comp-card a2a-comp-mcp"> <div class="a2a-comp-icon">🔧</div> <div class="a2a-comp-name">Model Context Protocol (MCP)</div> <div class="a2a-comp-subtitle">Agent ↔ Tools / External Systems</div> <div class="a2a-comp-desc">MCP defines how an agent connects to external tools and data sources. When an agent needs to query a database, call a weather API, read a file, or execute code — it uses MCP to communicate with those resources.</div> <div class="a2a-comp-examples"> <div class="a2a-comp-ex">Agent → database (query)</div> <div class="a2a-comp-ex">Agent → file system (read/write)</div> <div class="a2a-comp-ex">Agent → REST API (call)</div> <div class="a2a-comp-ex">Agent → code interpreter (execute)</div> </div> <div class="a2a-comp-analogy">Analogy: How a person uses tools — a hammer, a computer, a calculator. The tools don't know about each other.</div> </div> <div class="a2a-comp-arrow">+</div> <div class="a2a-comp-card a2a-comp-a2a"> <div class="a2a-comp-icon">🤝</div> <div class="a2a-comp-name">Agent2Agent Protocol (A2A)</div> <div class="a2a-comp-subtitle">Agent ↔ Agent</div> <div class="a2a-comp-desc">A2A defines how agents communicate with each other. When an orchestrating agent needs to delegate a task to a specialist agent — possibly built by a different company in a different framework — it uses A2A to discover and interact with that remote agent.</div> <div class="a2a-comp-examples"> <div class="a2a-comp-ex">ADK agent → LangGraph agent (delegate task)</div> <div class="a2a-comp-ex">CrewAI agent → ADK agent (request analysis)</div> <div class="a2a-comp-ex">Orchestrator → specialist agents (parallel)</div> <div class="a2a-comp-ex">Any agent → any A2A-compliant agent</div> </div> <div class="a2a-comp-analogy">Analogy: How team members collaborate — a manager delegates to specialists. The specialists may have completely different backgrounds and use different methods.</div> </div> </div> <style>.a2a-comparison-split{display:flex;gap:.75rem;align-items:center;margin:1.5rem 0;flex-wrap:wrap}.a2a-comp-card{flex:1;min-width:240px;border:1px solid var(--global-divider-color);border-radius:8px;padding:1.1rem;display:flex;flex-direction:column;gap:.5rem}.a2a-comp-mcp{border-left:3px solid #2698ba;background:rgba(38,152,186,0.04)}.a2a-comp-a2a{border-left:3px solid #4fc97e;background:rgba(79,201,126,0.04)}.a2a-comp-arrow{font-size:1.5rem;font-weight:700;color:var(--global-text-color-light);flex-shrink:0}.a2a-comp-icon{font-size:1.2rem}.a2a-comp-name{font-size:.85rem;font-weight:700;color:var(--global-text-color)}.a2a-comp-subtitle{font-size:.72rem;font-family:monospace;color:#2698ba}.a2a-comp-a2a .a2a-comp-subtitle{color:#4fc97e}.a2a-comp-desc{font-size:.78rem;color:var(--global-text-color-light);line-height:1.55}.a2a-comp-examples{display:flex;flex-direction:column;gap:.2rem}.a2a-comp-ex{font-size:.7rem;font-family:monospace;color:var(--global-text-color-light)}.a2a-comp-analogy{font-size:.75rem;color:var(--global-text-color-light);font-style:italic;border-top:1px solid var(--global-divider-color);padding-top:.4rem;margin-top:auto}</style> <p><strong>The key insight:</strong> MCP and A2A are complementary. A single agent might use MCP to connect to its tools (databases, APIs, file systems) AND use A2A to collaborate with other agents. They solve different problems at different levels of the stack.</p> <hr/> <h2 id="the-three-core-actors">The Three Core Actors</h2> <p>Every A2A interaction involves three roles:</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">A2A ACTOR MODEL — three roles in every interaction</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="flex-direction:row;align-items:stretch;gap:0.75rem;padding:1.1rem 1.25rem;flex-wrap:wrap;"> <div class="ns-node ns-node-cyan" style="flex:1;min-width:150px;"> <div class="ns-node-title">User</div> <div class="ns-node-sub">The human initiating the request. Provides the high-level goal. Doesn't need to know which agents will collaborate to achieve it.</div> </div> <div style="display:flex;align-items:center;color:#4a5a6a;font-size:1.2rem;flex-shrink:0;">→</div> <div class="ns-node ns-node-purple" style="flex:1;min-width:150px;"> <div class="ns-node-title">A2A Client (Client Agent)</div> <div class="ns-node-sub">An AI agent acting on behalf of the user. Discovers remote agents via Agent Cards, formulates task requests, and coordinates the workflow. Could be ADK, LangGraph, CrewAI, or any framework.</div> </div> <div style="display:flex;align-items:center;color:#4a5a6a;font-size:1.2rem;flex-shrink:0;">→ HTTP →</div> <div class="ns-node ns-node-green" style="flex:1;min-width:150px;"> <div class="ns-node-title">A2A Server (Remote Agent)</div> <div class="ns-node-sub">An AI agent that exposes an HTTP endpoint for receiving tasks. Processes requests using its own internal logic (opaque to the client) and returns results. Can be any framework.</div> </div> </div> </div> <p><strong>Why “opaque”?</strong> The client agent doesn’t know whether the server agent uses GPT-4, Gemini, or Llama. It doesn’t know if the server uses LangGraph, CrewAI, or a custom implementation. All it knows is the Agent Card — what tasks the server can handle, what inputs it accepts, and what outputs it produces. This opaqueness is the key that makes framework independence possible.</p> <hr/> <h2 id="the-agent-card-a-digital-identity">The Agent Card: A Digital Identity</h2> <p>The Agent Card is the cornerstone of A2A. It’s a JSON file that describes everything another agent needs to know to interact with yours — without you needing to share your internal implementation.</p> <div class="a2a-agentcard-wrapper"> <div class="a2a-agentcard-header"> <span class="a2a-agentcard-title">AGENT CARD EXPLORER — click any field to see what it means</span> </div> <div class="a2a-agentcard-body"> <div class="a2a-card-left"> <pre class="a2a-card-json" id="agentCardJson">{
  <span class="acj-field" onclick="acField('name')">"name"</span>: "WeatherBot",
  <span class="acj-field" onclick="acField('description')">"description"</span>: "Provides weather forecasts",
  <span class="acj-field" onclick="acField('url')">"url"</span>: "http://weather.example.com/a2a",
  <span class="acj-field" onclick="acField('version')">"version"</span>: "1.0.0",
  <span class="acj-field" onclick="acField('capabilities')">"capabilities"</span>: {
    "streaming": true,
    "pushNotifications": false,
    "stateTransitionHistory": true
  },
  <span class="acj-field" onclick="acField('authentication')">"authentication"</span>: {
    "schemes": ["apiKey"]
  },
  <span class="acj-field" onclick="acField('skills')">"skills"</span>: [
    {
      "id": "get_current_weather",
      "name": "Get Current Weather",
      "description": "Real-time weather for any location",
      "inputModes": ["text"],
      "outputModes": ["text"],
      "examples": ["What's the weather in Paris?"],
      "tags": ["weather", "current", "real-time"]
    }
  ]
}</pre> </div> <div class="a2a-card-right"> <div class="a2a-card-explanation" id="acExplanation"> <div class="ace-title">Click any field name</div> <div class="ace-desc">to see what it means and why it matters for agent interoperability</div> </div> </div> </div> </div> <style>.a2a-agentcard-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}.a2a-agentcard-header{padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.a2a-agentcard-body{display:flex;gap:0;flex-wrap:wrap}.a2a-card-left{flex:1.2;min-width:260px;border-right:1px solid var(--global-divider-color)}.a2a-card-right{flex:1;min-width:220px;padding:1rem}.a2a-card-json{background:#080808;margin:0;padding:1rem;font-size:.72rem;line-height:1.7;color:#cdd6f4;border-radius:0;border:0;white-space:pre;overflow-x:auto}.acj-field{color:#7dcfff;cursor:pointer;text-decoration:underline;text-underline-offset:2px}.acj-field:hover{color:#2698ba}.a2a-card-explanation{display:flex;flex-direction:column;gap:.5rem}.ace-title{font-size:.85rem;font-weight:700;color:var(--global-text-color)}.ace-desc{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6}.ace-code{font-size:.72rem;font-family:monospace;color:#7dcfff;background:rgba(0,0,0,0.2);border-radius:4px;padding:.4rem .6rem}</style> <script>
var AC_EXPLANATIONS = {
  name: {
    title: '"name" — Human-readable identifier',
    desc: 'The display name of the agent. Used by client agents and discovery registries to present the agent. Should be unique and descriptive enough that another agent can distinguish it from similar agents.',
    code: '"name": "WeatherBot"'
  },
  description: {
    title: '"description" — What this agent does',
    desc: 'A natural-language description of the agent\'s purpose. Client agents (and the LLMs powering them) read this to decide whether to contact this agent for a given task. Write it as if explaining to a colleague, not just naming the agent.',
    code: '"description": "Provides accurate weather forecasts and historical data."'
  },
  url: {
    title: '"url" — The A2A endpoint',
    desc: 'The HTTP URL where this agent\'s A2A server is running. Client agents send task requests to this URL using JSON-RPC 2.0 over HTTPS. This is the "phone number" of the agent.',
    code: '"url": "http://weather-service.example.com/a2a"'
  },
  version: {
    title: '"version" — API version',
    desc: 'Semantic version (major.minor.patch). Client agents can use this to check compatibility. If a client agent was built against v1.x of this agent and the agent upgrades to v2.x with breaking changes, the version field signals incompatibility.',
    code: '"version": "1.0.0"'
  },
  capabilities: {
    title: '"capabilities" — Supported interaction modes',
    desc: '• streaming: true = supports Server-Sent Events for real-time incremental results\n• pushNotifications: false = does NOT support webhook callbacks for async completion\n• stateTransitionHistory: true = tracks and returns all task state changes\nClient agents use this to know which interaction pattern to use.',
    code: '"capabilities": {\n  "streaming": true,\n  "pushNotifications": false\n}'
  },
  authentication: {
    title: '"authentication" — How to authenticate',
    desc: 'Declares the authentication schemes this agent requires. "apiKey" = send an API key in the HTTP header. Other options: "bearer" (JWT token), "oauth2" (OAuth 2.0 flow). The client agent knows to include the right credentials before sending any request.',
    code: '"authentication": {"schemes": ["apiKey"]}'
  },
  skills: {
    title: '"skills" — Specific capabilities with examples',
    desc: 'An array of discrete capabilities the agent offers. Each skill has: id (machine-readable identifier), name (human-readable), description (what it does), inputModes/outputModes (text, audio, video), examples (few-shot demonstrations for LLMs), and tags (for discovery filtering). Skills allow fine-grained routing — a client can pick the exact skill it needs.',
    code: '"skills": [{"id": "get_current_weather", ...}]'
  }
};
function acField(key) {
  var ac = AC_EXPLANATIONS[key];
  if (!ac) return;
  var el = document.getElementById('acExplanation');
  el.innerHTML = '<div class="ace-title">' + ac.title + '</div>' +
    '<div class="ace-desc">' + ac.desc.replace(/\n/g, '<br>') + '</div>' +
    '<div class="ace-code">' + ac.code + '</div>';
}
</script> <p><strong>Where is the Agent Card served?</strong> By convention, agents serve their Agent Card at <code class="language-plaintext highlighter-rouge">/.well-known/agent.json</code> on their domain. This is the <strong>Well-Known URI</strong> discovery method — any A2A client can find your agent’s capabilities by fetching <code class="language-plaintext highlighter-rouge">https://your-agent-domain.com/.well-known/agent.json</code>. No prior coordination needed.</p> <hr/> <h2 id="the-four-interaction-mechanisms">The Four Interaction Mechanisms</h2> <p>A2A supports four distinct ways for agents to communicate, each suited to different latency and complexity requirements:</p> <div class="a2a-interact-wrapper"> <div class="a2a-interact-header"> <span class="a2a-interact-title">A2A INTERACTION MECHANISMS — select to compare</span> </div> <div class="a2a-interact-tabs"> <button class="a2a-itab active" data-idx="0" onclick="a2aTab(0)">Synchronous</button> <button class="a2a-itab" data-idx="1" onclick="a2aTab(1)">Async Polling</button> <button class="a2a-itab" data-idx="2" onclick="a2aTab(2)">Streaming (SSE)</button> <button class="a2a-itab" data-idx="3" onclick="a2aTab(3)">Webhooks</button> </div> <div class="a2a-interact-body"> <div class="a2a-icontent active" id="a2aIC0"> <div class="a2a-iname">Synchronous Request/Response</div> <div class="a2a-idesc">Client sends a request and waits — blocking — until the server returns a complete response. Simple and predictable. Suitable for tasks that complete within seconds.</div> <div class="a2a-iwhen"><strong>When to use:</strong> Quick lookups (exchange rates, weather, simple calculations) where the response is fast and the client can afford to wait.</div> <div class="a2a-iflow">Client → <code>POST /a2a</code> (sendTask) → blocks → Server processes → complete result → Client continues</div> <pre class="a2a-ijson">{
  "jsonrpc": "2.0",
  "method": "sendTask",
  "id": "1",
  "params": {
    "id": "task-001",
    "sessionId": "session-001",
    "message": {
      "role": "user",
      "parts": [{"type": "text", "text": "Exchange rate USD to EUR?"}]
    },
    "acceptedOutputModes": ["text/plain"]
  }
}</pre> </div> <div class="a2a-icontent" id="a2aIC1"> <div class="a2a-iname">Asynchronous Polling</div> <div class="a2a-idesc">Client sends request → server immediately returns "working" status + task ID → client goes off and does other work → client periodically polls for completion. Non-blocking for the client.</div> <div class="a2a-iwhen"><strong>When to use:</strong> Tasks that take 10+ seconds (research, document generation, complex analysis) where blocking the client thread would be wasteful.</div> <div class="a2a-iflow">Client → sendTask → "working" + taskId → [client does other things] → Client polls getTask(taskId) → eventually "completed" + result</div> <pre class="a2a-ijson">// Initial request → server returns: {"status": "working", "id": "task-001"}
// Later poll:
{
  "jsonrpc": "2.0",
  "method": "getTask",
  "params": {"id": "task-001"}
}
// Response: {"status": "completed", "result": {...}}</pre> </div> <div class="a2a-icontent" id="a2aIC2"> <div class="a2a-iname">Streaming (Server-Sent Events)</div> <div class="a2a-idesc">Client uses <code>sendTaskSubscribe</code> to open a persistent HTTP connection. The server pushes incremental results as they become available — like watching a document being written in real time. No polling needed.</div> <div class="a2a-iwhen"><strong>When to use:</strong> Long documents, live research, real-time analysis where showing progress matters more than waiting for final output. The agent declares <code>"streaming": true</code> in its Agent Card.</div> <div class="a2a-iflow">Client → sendTaskSubscribe → open SSE connection → Server pushes: chunk 1 → chunk 2 → chunk 3 → "completed"</div> <pre class="a2a-ijson">{
  "jsonrpc": "2.0",
  "method": "sendTaskSubscribe",
  "id": "2",
  "params": {
    "id": "task-002",
    "message": {
      "parts": [{"type": "text", "text": "Write a market analysis..."}]
    }
  }
}
// Server sends SSE events:
// data: {"type": "progress", "content": "Researching market trends..."}
// data: {"type": "artifact", "content": "## Market Analysis\n\n..."}
// data: {"type": "completed"}</pre> </div> <div class="a2a-icontent" id="a2aIC3"> <div class="a2a-iname">Push Notifications (Webhooks)</div> <div class="a2a-idesc">Client registers a webhook URL when submitting a task. The client disconnects entirely. When the server finishes (hours or days later), it POSTs the result to the webhook URL. No persistent connection, no polling.</div> <div class="a2a-iwhen"><strong>When to use:</strong> Very long-running tasks (overnight batch processing, multi-day research) where maintaining any connection would be wasteful. The agent declares <code>"pushNotifications": true</code> in its Agent Card.</div> <div class="a2a-iflow">Client → sendTask + webhookUrl → Server: "acknowledged" → [hours pass] → Server → POST webhookUrl → result delivered</div> <pre class="a2a-ijson">{
  "jsonrpc": "2.0",
  "method": "sendTask",
  "params": {
    "id": "task-003",
    "message": {"parts": [{"type": "text", "text": "Analyze all Q1 data..."}]},
    "pushNotification": {
      "url": "https://my-agent.com/webhook/task-003",
      "authentication": {"schemes": ["bearer"]}
    }
  }
}</pre> </div> </div> </div> <style>.a2a-interact-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.a2a-interact-header{padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.a2a-interact-tabs{display:flex;overflow-x:auto;border-bottom:1px solid var(--global-divider-color)}.a2a-itab{flex-shrink:0;padding:.5rem .9rem;font-family:monospace;font-size:.7rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s}.a2a-itab:last-child{border-right:0}.a2a-itab.active{background:rgba(79,201,126,0.1);color:#4fc97e;font-weight:700}.a2a-interact-body{padding:1.1rem}.a2a-icontent{display:none;flex-direction:column;gap:.65rem}.a2a-icontent.active{display:flex}.a2a-iname{font-size:.95rem;font-weight:700;color:var(--global-text-color)}.a2a-idesc{font-size:.83rem;color:var(--global-text-color);line-height:1.65}.a2a-iwhen{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6}.a2a-iflow{font-size:.75rem;font-family:monospace;color:#4fc97e;background:rgba(79,201,126,0.06);border-radius:5px;padding:.45rem .7rem;line-height:1.6}.a2a-ijson{background:#080808;margin:0;padding:.8rem 1rem;font-size:.7rem;line-height:1.65;color:#cdd6f4;border-radius:6px;border:1px solid rgba(255,255,255,0.07);white-space:pre;overflow-x:auto}</style> <script>
function a2aTab(idx) {
  document.querySelectorAll('.a2a-itab').forEach(function(t){ t.classList.remove('active'); });
  document.querySelectorAll('.a2a-icontent').forEach(function(c){ c.classList.remove('active'); });
  document.querySelector('.a2a-itab[data-idx="'+idx+'"]').classList.add('active');
  document.getElementById('a2aIC'+idx).classList.add('active');
}
</script> <hr/> <h2 id="the-task-lifecycle">The Task Lifecycle</h2> <p>Every piece of work in A2A is a <strong>Task</strong> — an asynchronous unit of work with a unique ID and a defined state machine:</p> <pre><code class="language-mermaid">graph LR
    SUB([submitted]) --&gt; WRK[working]
    WRK --&gt;|needs more info| INP[input-required]
    INP --&gt;|client provides info| WRK
    WRK --&gt; COMP([completed])
    WRK --&gt; FAIL([failed])
    WRK --&gt; CANC([cancelled])
    style SUB fill:#141b2d,stroke:#2698ba,color:#e0e0e0
    style WRK fill:#141b2d,stroke:#e6a817,color:#e0e0e0
    style INP fill:#141b2d,stroke:#c97af2,color:#e0e0e0
    style COMP fill:#141b2d,stroke:#4fc97e,color:#e0e0e0
    style FAIL fill:#141b2d,stroke:#ff6b6b,color:#e0e0e0
    style CANC fill:#141b2d,stroke:#4a5a6a,color:#e0e0e0
</code></pre> <p>The <code class="language-plaintext highlighter-rouge">input-required</code> state is particularly important for multi-turn interactions. A remote agent might partially complete a task, determine it needs more information from the client, and pause — waiting for the client to provide the missing data before continuing. This enables rich back-and-forth collaboration between agents, not just one-shot requests.</p> <hr/> <h2 id="the-code-building-an-a2a-calendar-agent">The Code: Building an A2A Calendar Agent</h2> <p>The A2A samples repository provides a complete example of a calendar agent built with ADK that exposes itself as an A2A server. Let’s walk through the key components.</p> <h3 id="part-1-the-adk-agent-with-calendar-tools">Part 1: The ADK Agent with Calendar Tools</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">datetime</span>
<span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LlmAgent</span>
<span class="kn">from</span> <span class="n">google.adk.tools.google_api_tool</span> <span class="kn">import</span> <span class="n">CalendarToolset</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">create_agent</span><span class="p">(</span><span class="n">client_id</span><span class="p">,</span> <span class="n">client_secret</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">LlmAgent</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Constructs the ADK calendar agent with Google Calendar access.</span><span class="sh">"""</span>
    <span class="c1"># Initialize the Google Calendar tool with OAuth credentials
</span>    <span class="n">toolset</span> <span class="o">=</span> <span class="nc">CalendarToolset</span><span class="p">(</span><span class="n">client_id</span><span class="o">=</span><span class="n">client_id</span><span class="p">,</span> <span class="n">client_secret</span><span class="o">=</span><span class="n">client_secret</span><span class="p">)</span>

    <span class="k">return</span> <span class="nc">LlmAgent</span><span class="p">(</span>
        <span class="n">model</span>       <span class="o">=</span> <span class="sh">'</span><span class="s">gemini-2.0-flash-001</span><span class="sh">'</span><span class="p">,</span>
        <span class="n">name</span>        <span class="o">=</span> <span class="sh">'</span><span class="s">calendar_agent</span><span class="sh">'</span><span class="p">,</span>
        <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">An agent that can help manage a user</span><span class="sh">'</span><span class="s">s calendar</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">instruction</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"""</span><span class="s">
You are an agent that can help manage a user</span><span class="sh">'</span><span class="s">s calendar.
Users will request information about their calendar or ask to make changes.
Use the provided tools for interacting with the Calendar API.

If not specified, assume the user wants their </span><span class="sh">'</span><span class="s">primary</span><span class="sh">'</span><span class="s"> calendar.
When using Calendar API tools, use well-formed RFC3339 timestamps.
Today is </span><span class="si">{</span><span class="n">datetime</span><span class="p">.</span><span class="n">datetime</span><span class="p">.</span><span class="nf">now</span><span class="p">()</span><span class="si">}</span><span class="s">.
</span><span class="sh">"""</span><span class="p">,</span>
        <span class="n">tools</span> <span class="o">=</span> <span class="k">await</span> <span class="n">toolset</span><span class="p">.</span><span class="nf">get_tools</span><span class="p">(),</span>
    <span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">CalendarToolset</code></strong>: A pre-built ADK toolset that wraps the Google Calendar API as MCP-compatible tools. It handles OAuth 2.0 authentication flow, token management, and API call formatting. <code class="language-plaintext highlighter-rouge">toolset.get_tools()</code> returns a list of tool objects that the agent can call to read/write calendar data.</p> </blockquote> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">f"Today is {datetime.datetime.now()}"</code> in the instruction</strong>: This is dynamic instruction injection — the current date is embedded at agent creation time. Without it, the LLM might calculate “tomorrow” relative to its training cutoff date rather than today. This small detail ensures all date calculations are anchored to real-world time.</p> </blockquote> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">async def create_agent()</code>?</strong> The <code class="language-plaintext highlighter-rouge">toolset.get_tools()</code> call requires async execution (it may need to make network calls to fetch the tool schema from the Calendar API). The entire function is marked <code class="language-plaintext highlighter-rouge">async</code> to enable this. The <code class="language-plaintext highlighter-rouge">await</code> keyword pauses execution until <code class="language-plaintext highlighter-rouge">get_tools()</code> completes before constructing the agent.</p> </blockquote> <h3 id="part-2-defining-the-agent-card-and-starting-the-a2a-server">Part 2: Defining the Agent Card and Starting the A2A Server</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">a2a.types</span> <span class="kn">import</span> <span class="n">AgentSkill</span><span class="p">,</span> <span class="n">AgentCard</span><span class="p">,</span> <span class="n">AgentCapabilities</span>
<span class="kn">from</span> <span class="n">a2a.server.apps</span> <span class="kn">import</span> <span class="n">A2AStarletteApplication</span>
<span class="kn">from</span> <span class="n">a2a.server.request_handlers</span> <span class="kn">import</span> <span class="n">DefaultRequestHandler</span>
<span class="kn">from</span> <span class="n">a2a.server.tasks</span> <span class="kn">import</span> <span class="n">InMemoryTaskStore</span>
<span class="kn">from</span> <span class="n">google.adk.runners</span> <span class="kn">import</span> <span class="n">Runner</span>
</code></pre></div></div> <blockquote> <p><strong>Why these imports?</strong> A2A provides a Python SDK (<code class="language-plaintext highlighter-rouge">a2a</code> package) that handles the protocol details — JSON-RPC framing, task state management, SSE streaming, etc. Without it, you’d need to implement the full A2A protocol specification manually. The ADK <code class="language-plaintext highlighter-rouge">Runner</code> manages the agent’s execution lifecycle, session management, and tool call routing.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">main</span><span class="p">(</span><span class="n">host</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">port</span><span class="p">:</span> <span class="nb">int</span><span class="p">):</span>
    <span class="c1"># 1. Define the agent's skill (what it can do)
</span>    <span class="n">skill</span> <span class="o">=</span> <span class="nc">AgentSkill</span><span class="p">(</span>
        <span class="nb">id</span>          <span class="o">=</span> <span class="sh">'</span><span class="s">check_availability</span><span class="sh">'</span><span class="p">,</span>
        <span class="n">name</span>        <span class="o">=</span> <span class="sh">'</span><span class="s">Check Availability</span><span class="sh">'</span><span class="p">,</span>
        <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Checks a user</span><span class="sh">'</span><span class="s">s availability for a time using their Google Calendar</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">tags</span>         <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">calendar</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">examples</span>     <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">Am I free from 10am to 11am tomorrow?</span><span class="sh">'</span><span class="p">],</span>
    <span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">AgentSkill</code> is the fine-grained capability descriptor.</strong> While the <code class="language-plaintext highlighter-rouge">AgentCard</code> describes the agent overall, each <code class="language-plaintext highlighter-rouge">AgentSkill</code> describes a specific capability. This enables precise routing: if an orchestrating agent needs to check calendar availability (and not, say, create events), it can specifically request the <code class="language-plaintext highlighter-rouge">check_availability</code> skill. This granularity makes multi-agent systems more precise.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># 2. Define the Agent Card (the agent's public identity)
</span>    <span class="n">agent_card</span> <span class="o">=</span> <span class="nc">AgentCard</span><span class="p">(</span>
        <span class="n">name</span>                <span class="o">=</span> <span class="sh">'</span><span class="s">Calendar Agent</span><span class="sh">'</span><span class="p">,</span>
        <span class="n">description</span>         <span class="o">=</span> <span class="sh">"</span><span class="s">An agent that can manage a user</span><span class="sh">'</span><span class="s">s calendar</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">url</span>                 <span class="o">=</span> <span class="sa">f</span><span class="sh">'</span><span class="s">http://</span><span class="si">{</span><span class="n">host</span><span class="si">}</span><span class="s">:</span><span class="si">{</span><span class="n">port</span><span class="si">}</span><span class="s">/</span><span class="sh">'</span><span class="p">,</span>
        <span class="n">version</span>             <span class="o">=</span> <span class="sh">'</span><span class="s">1.0.0</span><span class="sh">'</span><span class="p">,</span>
        <span class="n">defaultInputModes</span>   <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">text</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">defaultOutputModes</span>  <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">text</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">capabilities</span>        <span class="o">=</span> <span class="nc">AgentCapabilities</span><span class="p">(</span><span class="n">streaming</span><span class="o">=</span><span class="bp">True</span><span class="p">),</span>
        <span class="n">skills</span>              <span class="o">=</span> <span class="p">[</span><span class="n">skill</span><span class="p">],</span>
    <span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">AgentCard</code> is the agent’s published identity.</strong> This is what gets served at <code class="language-plaintext highlighter-rouge">/.well-known/agent.json</code>. Any A2A client — regardless of framework — can fetch this URL and immediately understand: what this agent does, where to send requests, what authentication is required, whether streaming is supported, and which specific skills are available. The Agent Card makes the agent self-describing and discoverable without any prior coordination.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># 3. Create the ADK agent with credentials from environment
</span>    <span class="n">adk_agent</span> <span class="o">=</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">create_agent</span><span class="p">(</span>
        <span class="n">client_id</span>     <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">'</span><span class="s">GOOGLE_CLIENT_ID</span><span class="sh">'</span><span class="p">),</span>
        <span class="n">client_secret</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">'</span><span class="s">GOOGLE_CLIENT_SECRET</span><span class="sh">'</span><span class="p">),</span>
    <span class="p">))</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">os.getenv()</code> for credentials?</strong> Never hardcode API keys or OAuth secrets in source code — they’d be committed to version control and exposed. Environment variables are the standard way to pass secrets to running processes without embedding them in code. In production, these would be set via secrets management services (AWS Secrets Manager, Google Secret Manager, etc.).</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># 4. Wire the ADK agent to the A2A execution infrastructure
</span>    <span class="n">runner</span> <span class="o">=</span> <span class="nc">Runner</span><span class="p">(</span>
        <span class="n">app_name</span>        <span class="o">=</span> <span class="n">agent_card</span><span class="p">.</span><span class="n">name</span><span class="p">,</span>
        <span class="n">agent</span>           <span class="o">=</span> <span class="n">adk_agent</span><span class="p">,</span>
        <span class="n">artifact_service</span> <span class="o">=</span> <span class="nc">InMemoryArtifactService</span><span class="p">(),</span>   <span class="c1"># stores file outputs
</span>        <span class="n">session_service</span>  <span class="o">=</span> <span class="nc">InMemorySessionService</span><span class="p">(),</span>    <span class="c1"># tracks conversation state
</span>        <span class="n">memory_service</span>   <span class="o">=</span> <span class="nc">InMemoryMemoryService</span><span class="p">(),</span>     <span class="c1"># long-term memory
</span>    <span class="p">)</span>

    <span class="n">agent_executor</span> <span class="o">=</span> <span class="nc">ADKAgentExecutor</span><span class="p">(</span><span class="n">runner</span><span class="p">,</span> <span class="n">agent_card</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">ADKAgentExecutor</code></strong> is the bridge between the A2A protocol layer and the ADK agent. When an A2A task request arrives (a JSON-RPC <code class="language-plaintext highlighter-rouge">sendTask</code> message), the executor: (1) extracts the message content, (2) calls <code class="language-plaintext highlighter-rouge">runner.run()</code> with the message, (3) collects the ADK agent’s responses and tool calls, and (4) formats them as A2A protocol responses. This adapter pattern means the ADK agent doesn’t need to know anything about the A2A protocol — the executor handles the translation.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="c1"># 5. Create the A2A web application
</span>    <span class="n">request_handler</span> <span class="o">=</span> <span class="nc">DefaultRequestHandler</span><span class="p">(</span>
        <span class="n">agent_executor</span> <span class="o">=</span> <span class="n">agent_executor</span><span class="p">,</span>
        <span class="n">task_store</span>     <span class="o">=</span> <span class="nc">InMemoryTaskStore</span><span class="p">()</span>  <span class="c1"># tracks task states
</span>    <span class="p">)</span>

    <span class="n">a2a_app</span> <span class="o">=</span> <span class="nc">A2AStarletteApplication</span><span class="p">(</span>
        <span class="n">agent_card</span>    <span class="o">=</span> <span class="n">agent_card</span><span class="p">,</span>
        <span class="n">http_handler</span>  <span class="o">=</span> <span class="n">request_handler</span>
    <span class="p">)</span>

    <span class="c1"># 6. Add OAuth callback route (needed for Google Calendar auth)
</span>    <span class="n">routes</span> <span class="o">=</span> <span class="n">a2a_app</span><span class="p">.</span><span class="nf">routes</span><span class="p">()</span>
    <span class="n">routes</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">Route</span><span class="p">(</span>
        <span class="n">path</span>     <span class="o">=</span> <span class="sh">'</span><span class="s">/authenticate</span><span class="sh">'</span><span class="p">,</span>
        <span class="n">methods</span>  <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">GET</span><span class="sh">'</span><span class="p">],</span>
        <span class="n">endpoint</span> <span class="o">=</span> <span class="n">handle_auth</span><span class="p">,</span>
    <span class="p">))</span>

    <span class="c1"># 7. Start the HTTP server
</span>    <span class="n">app</span> <span class="o">=</span> <span class="nc">Starlette</span><span class="p">(</span><span class="n">routes</span><span class="o">=</span><span class="n">routes</span><span class="p">)</span>
    <span class="n">uvicorn</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="n">app</span><span class="p">,</span> <span class="n">host</span><span class="o">=</span><span class="n">host</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="n">port</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">A2AStarletteApplication</code></strong>: Creates a Starlette web application (a Python async web framework) that: (1) serves the Agent Card at <code class="language-plaintext highlighter-rouge">/.well-known/agent.json</code>, (2) handles incoming A2A task requests, (3) manages task state, (4) sends streaming responses via SSE when <code class="language-plaintext highlighter-rouge">streaming: true</code>. The entire A2A HTTP server is encapsulated in this single object.</p> </blockquote> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">InMemoryTaskStore</code></strong>: Stores the state of all active tasks (submitted, working, completed, failed) in memory. In production, you’d replace this with a persistent store (database) so task state survives server restarts. For development, in-memory is sufficient.</p> </blockquote> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">uvicorn.run()</code></strong>: Starts the ASGI server that will serve your A2A agent over HTTP. Uvicorn is a fast Python async HTTP server. Your agent is now accessible at <code class="language-plaintext highlighter-rouge">http://host:port/</code> — any A2A-compliant client can discover it via <code class="language-plaintext highlighter-rouge">/.well-known/agent.json</code> and send tasks to <code class="language-plaintext highlighter-rouge">/</code>.</p> </blockquote> <hr/> <h2 id="the-complete-a2a-flow-visualized">The Complete A2A Flow Visualized</h2> <pre><code class="language-mermaid">graph TD
    U([User: "Plan a birthday party for March 15"]) --&gt; CA[Client Agent — Orchestrator]
    CA --&gt;|GET /.well-known/agent.json| DISC[Agent Discovery]
    DISC --&gt;|Agent Card: capabilities, skills| CA
    CA --&gt;|POST /a2a sendTask| CALSERV[Calendar Agent A2A Server]
    CALSERV --&gt; ADK[ADK Agent Executor]
    ADK --&gt;|tool call| GCAL[Google Calendar API]
    GCAL --&gt;|availability data| ADK
    ADK --&gt;|SSE streaming| CALSERV
    CALSERV --&gt;|incremental results| CA
    CA --&gt; RESP([Final response to user])
    style CA fill:#141b2d,stroke:#2698ba,color:#e0e0e0
    style CALSERV fill:#141b2d,stroke:#4fc97e,color:#e0e0e0
    style ADK fill:#141b2d,stroke:#c97af2,color:#e0e0e0
    style GCAL fill:#141b2d,stroke:#e6a817,color:#e0e0e0
</code></pre> <hr/> <h2 id="security-how-a2a-stays-safe">Security: How A2A Stays Safe</h2> <p>Security is built into A2A at multiple levels:</p> <div class="a2a-security-grid"> <div class="a2a-sec-card"> <div class="a2a-sec-icon">🔒</div> <h4>Mutual TLS (mTLS)</h4> <p>Both the A2A client and server authenticate themselves with TLS certificates. This prevents unauthorized agents from connecting and ensures both sides can trust the other's identity. Essential for enterprise deployments where you don't want random clients calling your agent.</p> </div> <div class="a2a-sec-card"> <div class="a2a-sec-icon">🔑</div> <h4>Credential Handling</h4> <p>Authentication credentials (API keys, OAuth tokens) are passed via HTTP Authorization headers — never in the URL or request body. The Agent Card declares which authentication scheme is required, so clients know what to send before making any requests.</p> </div> <div class="a2a-sec-card"> <div class="a2a-sec-icon">📋</div> <h4>Audit Logs</h4> <p>All inter-agent communications are logged — which agent sent what to which agent, when, and what was returned. This creates an immutable audit trail for accountability, debugging, security analysis, and compliance with regulations that require traceability of AI decisions.</p> </div> <div class="a2a-sec-card"> <div class="a2a-sec-icon">🪪</div> <h4>Agent Card Security</h4> <p>Even though Agent Cards contain capability descriptions (not secrets), they should be access-controlled. In enterprise environments, not everyone should be able to discover which agents exist and what they can do. Secure discovery endpoints with network restrictions or access tokens.</p> </div> </div> <style>.a2a-security-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:.85rem;margin:1.5rem 0}.a2a-sec-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.a2a-sec-icon{font-size:1.1rem}.a2a-sec-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.a2a-sec-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}</style> <hr/> <h2 id="practical-applications">Practical Applications</h2> <div class="a2a-usecases-grid"> <div class="a2a-uc-card"> <span class="a2a-uc-num">01</span> <h4>Multi-Framework Collaboration</h4> <p>An ADK orchestrator delegates research to a LangGraph specialist, analysis to a CrewAI analyst, and report generation to another ADK agent — all communicating through A2A regardless of framework differences.</p> </div> <div class="a2a-uc-card"> <span class="a2a-uc-num">02</span> <h4>Enterprise Workflow Automation</h4> <p>A master agent decomposes a complex business process: collect data → delegate to analyst agent → delegate to compliance agent → delegate to report agent. Each agent operates independently and communicates via A2A tasks.</p> </div> <div class="a2a-uc-card"> <span class="a2a-uc-num">03</span> <h4>Dynamic Capability Discovery</h4> <p>An orchestrator agent discovers available specialist agents at runtime by querying a registry of Agent Cards, selects the most appropriate one for each sub-task, and delegates — without any hardcoded agent references.</p> </div> <div class="a2a-uc-card"> <span class="a2a-uc-num">04</span> <h4>Cross-Organization Collaboration</h4> <p>Your company's AI agent collaborates with a partner company's AI agent via A2A. Neither side needs to expose internal implementation details — just the Agent Card interface. Inter-company AI integration becomes as easy as an API call.</p> </div> </div> <style>.a2a-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:.85rem;margin:1.5rem 0}.a2a-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.a2a-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#4fc97e}.a2a-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.a2a-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}</style> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="a2a-summary-card"> <div class="a2a-summary-col"> <div class="a2a-summary-label">WHAT</div> <p>An open, HTTP-based standard for agent-to-agent communication. A2A defines how agents discover each other (Agent Cards), how they exchange tasks (JSON-RPC 2.0), and how results are returned (sync, polling, streaming, webhooks) — regardless of the framework each agent uses.</p> </div> <div class="a2a-summary-divider"></div> <div class="a2a-summary-col"> <div class="a2a-summary-label">WHY</div> <p>Without A2A, integrating N agents from M frameworks requires N×M custom integrations. A2A reduces this to N+M standard implementations. It's the USB standard for agent collaboration — write once, work with any compliant agent.</p> </div> <div class="a2a-summary-divider"></div> <div class="a2a-summary-col"> <div class="a2a-summary-label">vs MCP</div> <p>MCP = how an agent connects to tools and external resources. A2A = how agents connect to each other. Both are needed in production systems: MCP gives individual agents their capabilities; A2A lets capable agents collaborate.</p> </div> </div> <style>.a2a-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.a2a-summary-card{flex-direction:column}}.a2a-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.a2a-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.a2a-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.a2a-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#4fc97e}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li> <p><strong>A2A is the missing standard between MCP and multi-agent systems.</strong> MCP connects agents to tools. A2A connects agents to each other. Together, they form a complete ecosystem: agents with tool access (MCP) collaborating with other agents (A2A).</p> </li> <li> <p><strong>The Agent Card is the protocol’s foundation.</strong> It makes agents self-describing and discoverable — any A2A client can learn an agent’s capabilities, endpoint, authentication requirements, and skills just by fetching its Agent Card JSON. This is what makes true interoperability possible.</p> </li> <li> <p><strong>Four interaction patterns cover every latency profile.</strong> Synchronous for fast tasks, polling for medium tasks, streaming SSE for incremental results, webhooks for overnight tasks. The Agent Card declares which patterns are supported — clients choose accordingly.</p> </li> <li> <p><strong>The <code class="language-plaintext highlighter-rouge">input-required</code> state enables multi-turn agent conversations.</strong> A remote agent can pause mid-task, request more information from the client, and resume once the information is provided. This supports complex collaborative workflows, not just one-shot requests.</p> </li> <li> <p><strong>A2A is opaque by design.</strong> The client agent doesn’t know or care what framework the server agent uses, what LLM it calls, or how it processes requests. This opacity is what makes framework independence real — you can replace the server-side implementation entirely without changing any client.</p> </li> <li> <p><strong>The A2A Python SDK encapsulates all protocol complexity.</strong> <code class="language-plaintext highlighter-rouge">A2AStarletteApplication</code>, <code class="language-plaintext highlighter-rouge">ADKAgentExecutor</code>, <code class="language-plaintext highlighter-rouge">DefaultRequestHandler</code>, and <code class="language-plaintext highlighter-rouge">InMemoryTaskStore</code> handle the JSON-RPC framing, task state machine, SSE streaming, and HTTP routing. You implement the agent logic; the SDK handles the protocol.</p> </li> <li> <p><strong>Security must be explicit.</strong> Declare authentication requirements in the Agent Card. Use mTLS for production. Log all inter-agent communications. Control who can discover your Agent Card. These aren’t optional features — they’re prerequisites for deploying A2A in enterprise environments.</p> </li> <li> <p><strong>Industry adoption is accelerating.</strong> Atlassian, Box, LangChain, MongoDB, Salesforce, SAP, ServiceNow, Microsoft (Azure AI Foundry), Auth0 — the A2A ecosystem is growing fast. Agents you build today to the A2A standard will be composable with agents from any of these platforms.</p> </li> </ul>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="a2a"/><category term="agent2agent"/><category term="inter-agent"/><summary type="html"><![CDATA[MCP connects agents to tools. A2A connects agents to agents. The Agent2Agent protocol is the missing standard that lets any AI agent — regardless of framework — collaborate with any other.]]></summary></entry><entry><title type="html">Chapter 14: Knowledge Retrieval (RAG)</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/rag/" rel="alternate" type="text/html" title="Chapter 14: Knowledge Retrieval (RAG)"/><published>2026-02-26T00:00:00+00:00</published><updated>2026-02-26T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/rag</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/rag/"><![CDATA[<h2 id="the-knowledge-cutoff-problem">The Knowledge Cutoff Problem</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Embedding:</strong> A numerical representation of text as a vector (list of numbers). An embedding converts "What is the capital of France?" into something like [0.23, -0.81, 0.44, ...] with hundreds or thousands of dimensions. The crucial property: texts with similar meanings get similar vectors. "What is the capital of France?" and "Which city leads France?" would have very similar embeddings even though they share only a few words.</p> <p style="margin-top:0.5rem"><strong>Vector:</strong> An ordered list of numbers. A 3D vector might be [1.5, -2.3, 0.7]. In embeddings, vectors typically have 768, 1,536, or 3,072 dimensions. The position in this high-dimensional space encodes meaning — similar concepts cluster together, different concepts are far apart.</p> <p style="margin-top:0.5rem"><strong>Cosine similarity:</strong> A measure of how similar two vectors are, regardless of their magnitude. Returns a value from -1 (opposite) to 1 (identical). Used in RAG to find which stored document chunks are most similar to the user's query. If the query embedding and a document chunk embedding have high cosine similarity, they're semantically related.</p> <p style="margin-top:0.5rem"><strong>Hallucination:</strong> When an LLM generates text that sounds confident and plausible but is factually incorrect. Happens when the model doesn't know the answer but generates something statistically likely based on context. RAG reduces hallucination by giving the model actual facts to work with, rather than relying on pattern completion.</p> <p style="margin-top:0.5rem"><strong>Chunking:</strong> Splitting large documents into smaller pieces for embedding and storage. A 50-page document becomes hundreds of 300-500 word chunks. Each chunk is embedded separately so you can retrieve just the relevant section rather than the whole document.</p> <p style="margin-top:0.5rem"><strong>Vector database:</strong> A specialized database built to store embeddings and perform fast similarity search across millions of them. Unlike SQL databases that search by exact value matches, vector databases search by semantic similarity — finding the closest vectors to a query vector. Examples: Pinecone, Weaviate, Chroma, Milvus, pgvector.</p> <p style="margin-top:0.5rem"><strong>Augmentation:</strong> In RAG, the process of adding retrieved document chunks to the LLM's prompt. The original query is "augmented" with relevant context before being sent to the model. This is why it's called Retrieval-<em>Augmented</em> Generation.</p> </div> <p>Every LLM in the world has a <strong>training cutoff date</strong> — the point at which its training data ended. GPT-4’s cutoff is early 2024. Gemini Pro’s is mid-2024. Whatever happened after that date simply doesn’t exist in the model’s knowledge.</p> <p>But the more important limitation isn’t the cutoff date — it’s what was <em>never in the training data at all</em>:</p> <ul> <li><strong>Your company’s internal documentation.</strong> The model has never seen your employee handbook, your product specs, your customer contracts, or your proprietary research.</li> <li><strong>Your customers’ specific situations.</strong> The model can’t look up customer #7823’s account history, their past support tickets, or their current subscription plan.</li> <li><strong>Real-time data.</strong> The model can’t check today’s stock price, last hour’s server logs, or this morning’s news.</li> </ul> <p>An LLM without access to this information can still answer general questions reasonably well — but for the specific, grounded, current answers that make AI agents genuinely useful, it will either guess (hallucinate) or admit ignorance.</p> <p><strong>Retrieval-Augmented Generation (RAG)</strong> is the solution. Instead of asking the LLM to recall knowledge it doesn’t have, RAG gives it a system to <em>look things up</em> — converting a closed-book test into an open-book one.</p> <hr/> <h2 id="how-rag-works-the-six-steps">How RAG Works: The Six Steps</h2> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">RAG PIPELINE — from query to grounded answer</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-phase" style="max-width:480px;"> <div class="ns-phase-title">OFFLINE PHASE — done once, before any queries</div> <div class="ns-phase-sub">Prepare the knowledge base for fast retrieval</div> <div class="ns-row"> <div class="ns-node ns-node-cyan"><div class="ns-node-title">Load Documents</div><div class="ns-node-sub">PDFs, wikis, databases, web pages</div></div> <div style="display:flex;align-items:center;color:#4a5a6a;font-size:1rem;flex-shrink:0;">→</div> <div class="ns-node ns-node-cyan"><div class="ns-node-title">Chunk</div><div class="ns-node-sub">Split into 300-500 word pieces</div></div> <div style="display:flex;align-items:center;color:#4a5a6a;font-size:1rem;flex-shrink:0;">→</div> <div class="ns-node ns-node-cyan"><div class="ns-node-title">Embed + Store</div><div class="ns-node-sub">Convert to vectors, save in vector DB</div></div> </div> </div> <div class="ns-arrow"></div> <div class="ns-phase" style="max-width:480px;"> <div class="ns-phase-title">ONLINE PHASE — at query time, for each user request</div> <div class="ns-phase-sub">Retrieve relevant context and generate grounded answer</div> <div class="ns-node ns-node-purple" style="max-width:none;"><div class="ns-node-title">01 · User Query Arrives</div><div class="ns-node-sub">"What is our remote work policy for contractors?"</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:none;"><div class="ns-node-title">02 · Embed the Query</div><div class="ns-node-sub">Convert query to a vector using the same embedding model used for documents</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:none;"><div class="ns-node-title">03 · Semantic Search</div><div class="ns-node-sub">Find the top-K most similar vectors in the database. Return those chunks' text.</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:none;"><div class="ns-node-title">04 · Augment the Prompt</div><div class="ns-node-sub">Combine: [Retrieved Chunks] + [Original Query] → enriched prompt</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:none;"><div class="ns-node-title">05 · Generate Grounded Answer</div><div class="ns-node-sub">LLM reads the retrieved context and generates a response based on actual documents, not training data guesses. Can cite sources.</div></div> </div> </div> </div> <p>The key insight: <strong>the LLM’s job is to reason and synthesize, not to memorize facts</strong>. Give it the relevant facts (retrieved documents), and it can synthesize an excellent answer. Without those facts, it either hallucinates or refuses to answer.</p> <hr/> <h2 id="the-four-core-concepts">The Four Core Concepts</h2> <h3 id="concept-1-embeddings--how-meaning-becomes-numbers">Concept 1: Embeddings — How Meaning Becomes Numbers</h3> <p>An embedding model is a neural network trained to convert text into vectors such that semantically similar texts produce geometrically close vectors. This sounds abstract, so let’s ground it.</p> <p>Imagine a 2D map where every word is a dot. Words that often appear in similar contexts end up near each other. “Doctor” and “physician” are close. “Cat” and “kitten” are close. “Bank” (financial) might be near “loan” and “interest,” while “bank” (river) is near “river” and “shore” — the same word appearing in two different clusters based on how it’s typically used.</p> <p>Real embedding models work in 768 or 1,536 dimensions rather than 2, allowing for vastly more nuanced representation. But the principle is identical: proximity in vector space = similarity in meaning.</p> <p><strong>Why does this enable semantic search?</strong> If you embed the query “What is our vacation policy?” and it produces a vector that’s close to the chunk “Employees are entitled to 15 days of paid leave annually, plus public holidays…” — even though “vacation” never appears in the chunk — the similarity will still be high because the concepts are related. This is impossible with keyword search.</p> <div class="rag-embedding-demo"> <div class="rag-demo-header"> <span class="rag-demo-title">EMBEDDING SIMILARITY — visualized in 2D (real embeddings are 1536D)</span> </div> <canvas id="embeddingCanvas" style="width:100%;max-width:560px;display:block;margin:0 auto;"></canvas> <div class="rag-embed-legend"> <span class="rag-embed-query">● Your query</span> <span class="rag-embed-close">● Semantically close chunks (retrieved)</span> <span class="rag-embed-far">● Unrelated chunks (not retrieved)</span> </div> </div> <style>.rag-embedding-demo{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}.rag-demo-header{padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.rag-embed-legend{display:flex;gap:1.25rem;padding:.6rem 1.1rem;border-top:1px solid var(--global-divider-color);flex-wrap:wrap;font-size:.72rem}.rag-embed-query{color:#e6a817;font-family:monospace}.rag-embed-close{color:#4fc97e;font-family:monospace}.rag-embed-far{color:rgba(128,128,128,0.5);font-family:monospace}</style> <script>
document.addEventListener('DOMContentLoaded', function(){
  var canvas = document.getElementById('embeddingCanvas');
  if (!canvas) return;
  var dpr = window.devicePixelRatio || 1;
  var W = Math.min(560, canvas.parentElement.getBoundingClientRect().width - 32);
  var H = 220;
  canvas.width = W * dpr; canvas.height = H * dpr;
  canvas.style.width = W + 'px'; canvas.style.height = H + 'px';
  var ctx = canvas.getContext('2d');
  ctx.scale(dpr, dpr);

  // Points: [x%, y%, label, type (query/close/far)]
  var points = [
    [0.42, 0.45, 'Your query:\n"remote work policy"', 'query'],
    [0.35, 0.38, 'Remote Work Policy 2025', 'close'],
    [0.48, 0.52, 'Work-from-Home Guidelines', 'close'],
    [0.38, 0.55, 'Contractor Arrangements', 'close'],
    [0.18, 0.75, 'Office Safety Procedures', 'far'],
    [0.72, 0.25, 'Q4 Financial Report', 'far'],
    [0.82, 0.65, 'Marketing Campaign Q1', 'far'],
    [0.65, 0.82, 'Product Roadmap 2026', 'far'],
    [0.15, 0.22, 'Employee Benefits Guide', 'far'],
  ];

  var bg = getComputedStyle(document.documentElement).getPropertyValue('--global-bg-color').trim() || '#111';
  ctx.fillStyle = '#0a0a0a';
  ctx.fillRect(0, 0, W, H);

  // Draw similarity circle around query
  var qx = points[0][0] * W, qy = points[0][1] * H;
  ctx.beginPath();
  ctx.arc(qx, qy, 80, 0, Math.PI * 2);
  ctx.strokeStyle = 'rgba(79,201,126,0.15)';
  ctx.lineWidth = 1.5;
  ctx.setLineDash([4, 3]);
  ctx.stroke();
  ctx.setLineDash([]);
  ctx.fillStyle = 'rgba(79,201,126,0.04)';
  ctx.fill();

  // Draw lines from query to close points
  points.slice(1, 4).forEach(function(p) {
    var px = p[0] * W, py = p[1] * H;
    ctx.beginPath();
    ctx.moveTo(qx, qy); ctx.lineTo(px, py);
    ctx.strokeStyle = 'rgba(79,201,126,0.3)';
    ctx.lineWidth = 1;
    ctx.stroke();
  });

  // Draw points
  points.forEach(function(p) {
    var px = p[0] * W, py = p[1] * H;
    var color = p[3] === 'query' ? '#e6a817' : (p[3] === 'close' ? '#4fc97e' : 'rgba(128,128,128,0.4)');
    var radius = p[3] === 'query' ? 7 : 5;
    ctx.beginPath(); ctx.arc(px, py, radius, 0, Math.PI * 2);
    ctx.fillStyle = color; ctx.fill();

    // Label
    ctx.fillStyle = p[3] === 'query' ? '#e6a817' : (p[3] === 'close' ? '#4fc97e' : 'rgba(128,128,128,0.5)');
    ctx.font = '9px monospace'; ctx.textAlign = 'center';
    var lines = p[2].split('\n');
    lines.forEach(function(line, i) {
      ctx.fillText(line, px, py - radius - 5 - (lines.length - 1 - i) * 11);
    });
  });

  // Label the circle
  ctx.fillStyle = 'rgba(79,201,126,0.5)';
  ctx.font = '9px monospace'; ctx.textAlign = 'left';
  ctx.fillText('similarity threshold', qx + 58, qy - 55);
});
</script> <h3 id="concept-2-chunking--breaking-documents-for-retrieval">Concept 2: Chunking — Breaking Documents for Retrieval</h3> <p>You can’t embed a 50-page document as a single unit and hope to retrieve relevant sections. The embedding of the entire document would average across all topics, making similarity search imprecise. Instead, you chunk documents into pieces that are:</p> <ul> <li><strong>Small enough</strong> that each chunk is topically coherent (not mixing multiple subjects)</li> <li><strong>Large enough</strong> that each chunk contains sufficient context to be useful</li> </ul> <p><strong>Common chunking strategies:</strong></p> <table> <thead> <tr> <th>Strategy</th> <th>How it works</th> <th>Best for</th> </tr> </thead> <tbody> <tr> <td>Fixed-size</td> <td>Split every N characters/words, with overlap</td> <td>Simple baseline</td> </tr> <tr> <td>Sentence</td> <td>Split at sentence boundaries</td> <td>Conversational content</td> </tr> <tr> <td>Paragraph</td> <td>Split at paragraph breaks</td> <td>Structured documents</td> </tr> <tr> <td>Semantic</td> <td>Split when topic changes (detected by embedding distance shift)</td> <td>Dense, topic-switching documents</td> </tr> <tr> <td>Recursive</td> <td>Try paragraph splits, fall back to sentence, then fixed-size</td> <td>General purpose</td> </tr> </tbody> </table> <p><strong>Chunk overlap</strong> is a critical parameter. If you split a document at every 500 words with no overlap, a sentence that spans the boundary between chunk 3 and chunk 4 may be split in half — losing meaning in both chunks. Overlapping chunks by 50-100 words ensures boundary context is preserved in adjacent chunks.</p> <h3 id="concept-3-retrieval-methods">Concept 3: Retrieval Methods</h3> <p>Once your knowledge base is embedded and indexed, how do you find the relevant chunks?</p> <p><strong>Vector/Semantic Search:</strong> Embed the query, find the K nearest vectors in the database by cosine similarity. This finds chunks that are semantically related even without keyword overlap. Fast implementations use approximate nearest neighbor algorithms like <strong>HNSW</strong> (Hierarchical Navigable Small World) — a graph-based structure that allows sub-linear search across millions of vectors.</p> <p><strong>BM25 (Best Match 25):</strong> A classical information retrieval algorithm that scores documents based on term frequency and inverse document frequency (TF-IDF with improvements). It finds documents that contain the same words as the query. Very precise for exact keyword matches but blind to synonyms.</p> <p><strong>Hybrid Search:</strong> Combines both approaches. BM25 catches exact keyword matches; semantic search catches conceptually related content. A fusion algorithm (like Reciprocal Rank Fusion) combines the two ranked lists. This is the production standard for most enterprise RAG systems.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Conceptual hybrid search
</span><span class="k">def</span> <span class="nf">hybrid_search</span><span class="p">(</span><span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">k</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">5</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">:</span>
    <span class="c1"># Get semantic candidates (top-20 by embedding similarity)
</span>    <span class="n">semantic_results</span> <span class="o">=</span> <span class="n">vector_db</span><span class="p">.</span><span class="nf">similarity_search</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">20</span><span class="p">)</span>

    <span class="c1"># Get keyword candidates (top-20 by BM25)
</span>    <span class="n">bm25_results</span> <span class="o">=</span> <span class="n">bm25_index</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">20</span><span class="p">)</span>

    <span class="c1"># Combine using Reciprocal Rank Fusion
</span>    <span class="n">scores</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="k">for</span> <span class="n">rank</span><span class="p">,</span> <span class="n">doc</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">semantic_results</span><span class="p">):</span>
        <span class="n">scores</span><span class="p">[</span><span class="n">doc</span><span class="p">.</span><span class="nb">id</span><span class="p">]</span> <span class="o">=</span> <span class="n">scores</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">doc</span><span class="p">.</span><span class="nb">id</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span> <span class="o">/</span> <span class="p">(</span><span class="mi">60</span> <span class="o">+</span> <span class="n">rank</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">rank</span><span class="p">,</span> <span class="n">doc</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">bm25_results</span><span class="p">):</span>
        <span class="n">scores</span><span class="p">[</span><span class="n">doc</span><span class="p">.</span><span class="nb">id</span><span class="p">]</span> <span class="o">=</span> <span class="n">scores</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">doc</span><span class="p">.</span><span class="nb">id</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span> <span class="o">/</span> <span class="p">(</span><span class="mi">60</span> <span class="o">+</span> <span class="n">rank</span><span class="p">)</span>

    <span class="c1"># Return top-K by combined score
</span>    <span class="k">return</span> <span class="nf">sorted</span><span class="p">(</span><span class="n">scores</span><span class="p">,</span> <span class="n">key</span><span class="o">=</span><span class="n">scores</span><span class="p">.</span><span class="n">get</span><span class="p">,</span> <span class="n">reverse</span><span class="o">=</span><span class="bp">True</span><span class="p">)[:</span><span class="n">k</span><span class="p">]</span>
</code></pre></div></div> <h3 id="concept-4-vector-databases">Concept 4: Vector Databases</h3> <p>A vector database is purpose-built for one operation: <strong>given a query vector, find the most similar stored vectors as fast as possible</strong>.</p> <p>Traditional SQL databases compare row by row using exact value matching. They can’t natively compute “which of these 10 million vectors is geometrically closest to this query vector?” — that would require computing a similarity score for every single stored vector, which is too slow.</p> <p>Vector databases solve this with specialized index structures:</p> <ul> <li><strong>HNSW</strong> (used by Weaviate, Qdrant): builds a hierarchical graph structure that enables navigating toward nearest neighbors without checking every vector</li> <li><strong>IVF (Inverted File Index)</strong> (used by Faiss): clusters vectors into groups; search checks only the most relevant clusters</li> <li><strong>LSH (Locality Sensitive Hashing)</strong>: maps similar vectors to the same hash buckets for fast filtering</li> </ul> <p>The result: searching 10 million vectors takes milliseconds rather than seconds.</p> <p>Popular options by category:</p> <table> <thead> <tr> <th>Category</th> <th>Examples</th> <th>Best for</th> </tr> </thead> <tbody> <tr> <td>Managed cloud</td> <td>Pinecone, Weaviate Cloud</td> <td>Production, no infra management</td> </tr> <tr> <td>Open source</td> <td>Chroma, Milvus, Qdrant</td> <td>Self-hosted, customizable</td> </tr> <tr> <td>Extensions</td> <td>pgvector (Postgres), Redis</td> <td>Adding vector search to existing DB</td> </tr> <tr> <td>Research libraries</td> <td>FAISS (Meta), ScaNN (Google)</td> <td>Maximum performance, embedded use</td> </tr> </tbody> </table> <hr/> <h2 id="the-rag-pipeline-live-demonstration">The RAG Pipeline: Live Demonstration</h2> <div class="rag-pipeline-demo"> <div class="rag-pipe-header"> <span class="rag-pipe-title">RAG PIPELINE WALKTHROUGH — step through a complete retrieval</span> <div class="rag-pipe-nav"> <button class="rag-pipe-btn" id="ragPrev" onclick="ragStep(-1)">← Prev</button> <span class="rag-pipe-counter" id="ragCounter">Step 1 / 6</span> <button class="rag-pipe-btn" id="ragNext" onclick="ragStep(1)">Next →</button> </div> </div> <div class="rag-pipe-body"> <div class="rag-pipe-step active" id="ragStep0"> <div class="rag-step-num">01</div> <div class="rag-step-title">User Asks a Question</div> <div class="rag-step-detail">A user asks: <em>"What is our company's policy on reimbursing home office equipment for remote employees?"</em><br/><br/>Without RAG, the LLM would have to answer from its training data — which contains no information about your company's specific policies. It would either refuse ("I don't have information about your company's policies") or hallucinate a plausible-sounding but invented policy.</div> <div class="rag-step-msg"><code>User query: "What is our company's policy on reimbursing home office equipment for remote employees?"</code></div> </div> <div class="rag-pipe-step" id="ragStep1"> <div class="rag-step-num">02</div> <div class="rag-step-title">Embed the Query</div> <div class="rag-step-detail">The query is passed through an embedding model (e.g., OpenAI's text-embedding-3-large or Google's textembedding-gecko). The model converts the query into a 1,536-dimensional vector. This vector captures the semantic meaning of the query in a way that can be compared to the vectors of stored document chunks.</div> <div class="rag-step-msg"><code>query_vector = embedding_model.encode("What is our company's policy...")<br/># → [0.23, -0.81, 0.44, 0.17, ... ] (1,536 numbers)</code></div> </div> <div class="rag-pipe-step" id="ragStep2"> <div class="rag-step-num">03</div> <div class="rag-step-title">Search the Vector Database</div> <div class="rag-step-detail">The query vector is compared against all stored document chunk vectors. The database uses HNSW to efficiently find the top-5 most similar chunks (by cosine similarity) without checking every single stored vector. The search takes ~5-20ms even across millions of chunks.</div> <div class="rag-step-msg"><code>results = vector_db.similarity_search(query_vector, k=5)<br/><br/># Returns chunks ranked by similarity:<br/># 1. HR Policy Manual, Section 4.2: "Remote Work Equipment" (similarity: 0.94)<br/># 2. Employee Benefits FAQ: "Home Office Setup" (similarity: 0.88)<br/># 3. IT Equipment Request Process (similarity: 0.72)<br/># 4. Remote Work Agreement Template (similarity: 0.68)<br/># 5. New Employee Onboarding Guide (similarity: 0.61)</code></div> </div> <div class="rag-pipe-step" id="ragStep3"> <div class="rag-step-num">04</div> <div class="rag-step-title">Retrieve Chunk Text</div> <div class="rag-step-detail">The text of the top-k chunks is fetched from the database. This is the actual content that will be passed to the LLM as context. The chunk text preserves the exact wording from the original documents — this is the ground truth that prevents hallucination.</div> <div class="rag-step-msg"><code>chunk_1_text = """Remote Work Equipment Reimbursement (Section 4.2):<br/>Full-time remote employees are eligible for a one-time home office<br/>equipment reimbursement of up to $1,500. This includes monitors,<br/>keyboards, mice, webcams, and ergonomic equipment. Contractors<br/>are not eligible for this benefit. Submit receipts within 30 days<br/>of purchase via the expense portal..."""</code></div> </div> <div class="rag-pipe-step" id="ragStep4"> <div class="rag-step-num">05</div> <div class="rag-step-title">Augment the Prompt</div> <div class="rag-step-detail">The retrieved chunks are combined with the original query into an enriched prompt. The LLM now has access to the specific policy information it needs. The prompt template instructs the LLM to answer based on the provided context and to cite the source.</div> <div class="rag-step-msg"><code>augmented_prompt = f"""<br/>You are a helpful HR assistant. Answer the user's question based ONLY<br/>on the provided context. If the answer isn't in the context, say so.<br/>Cite the specific document section you're referencing.<br/><br/>CONTEXT:<br/>{retrieved_chunks}<br/><br/>QUESTION: {user_query}<br/><br/>ANSWER:"""</code></div> </div> <div class="rag-pipe-step" id="ragStep5"> <div class="rag-step-num">06</div> <div class="rag-step-title">Generate Grounded Answer</div> <div class="rag-step-detail">The LLM reads the retrieved context and generates a response based on the actual policy documents. The answer is factually accurate (grounded in real policy text), specific (mentions the $1,500 limit and contractor exclusion), and citable (references Section 4.2 of the HR Policy Manual). No hallucination possible — the model is reading the answer, not inventing it.</div> <div class="rag-step-msg"><code>LLM Response:<br/>"According to Section 4.2 of the HR Policy Manual, full-time remote<br/>employees are eligible for a one-time home office equipment<br/>reimbursement of up to $1,500, covering monitors, keyboards,<br/>webcams, and ergonomic equipment. Note: contractors are not eligible<br/>for this benefit. Submit receipts within 30 days via the expense portal."</code></div> </div> </div> </div> <style>.rag-pipeline-demo{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.rag-pipe-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.5rem}.rag-pipe-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.rag-pipe-nav{display:flex;align-items:center;gap:.75rem}.rag-pipe-btn{font-family:monospace;font-size:.72rem;padding:.25rem .7rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer}.rag-pipe-counter{font-family:monospace;font-size:.72rem;color:var(--global-text-color-light);min-width:70px;text-align:center}.rag-pipe-body{padding:1.1rem}.rag-pipe-step{display:none;flex-direction:column;gap:.7rem;animation:ragIn .2s ease}.rag-pipe-step.active{display:flex}@keyframes ragIn{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}.rag-step-num{font-family:monospace;font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}.rag-step-title{font-size:1rem;font-weight:700;color:var(--global-text-color)}.rag-step-detail{font-size:.83rem;color:var(--global-text-color);line-height:1.65}.rag-step-msg{background:rgba(0,0,0,0.2);border-radius:6px;padding:.65rem .85rem;font-size:.72rem;color:#cdd6f4;font-family:monospace;line-height:1.6;border:1px solid var(--global-divider-color);white-space:pre-wrap}</style> <script>
var ragCurrentStep = 0, ragTotal = 6;
function ragStep(dir) {
  document.getElementById('ragStep'+ragCurrentStep).classList.remove('active');
  ragCurrentStep = Math.max(0, Math.min(ragTotal-1, ragCurrentStep + dir));
  document.getElementById('ragStep'+ragCurrentStep).classList.add('active');
  document.getElementById('ragCounter').textContent = 'Step ' + (ragCurrentStep+1) + ' / ' + ragTotal;
  document.getElementById('ragPrev').disabled = ragCurrentStep === 0;
  document.getElementById('ragNext').disabled = ragCurrentStep === ragTotal-1;
}
</script> <hr/> <h2 id="graphrag-when-relationships-matter">GraphRAG: When Relationships Matter</h2> <p>Standard RAG treats each document chunk independently. It retrieves relevant chunks and feeds them to the LLM. This works well for localized questions (“What is our vacation policy?”) but struggles with questions that require synthesizing relationships across multiple documents (“How does our acquisition of Company X affect our healthcare benefits for employees who transfer?”).</p> <p><strong>GraphRAG</strong> replaces the vector database with a <strong>knowledge graph</strong> — a network of entities (nodes) connected by typed relationships (edges).</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">KNOWLEDGE GRAPH STRUCTURE — entities and relationships</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="flex-direction:row;align-items:center;gap:0.75rem;padding:1.1rem 1.25rem;flex-wrap:nowrap;overflow-x:auto;"> <div class="ns-node ns-node-cyan" style="flex-shrink:0;min-width:100px;"><div class="ns-node-title">Company X</div><div class="ns-node-sub">Entity: Company</div></div> <div style="display:flex;flex-direction:column;align-items:center;gap:0.2rem;flex-shrink:0;font-size:0.65rem;color:#4a5a6a;"> <span style="color:#c97af2;font-family:monospace;">acquired_by →</span> </div> <div class="ns-node ns-node-purple" style="flex-shrink:0;min-width:100px;"><div class="ns-node-title">Our Company</div><div class="ns-node-sub">Entity: Company</div></div> <div style="display:flex;flex-direction:column;align-items:center;gap:0.2rem;flex-shrink:0;font-size:0.65rem;color:#4a5a6a;"> <span style="color:#e6a817;font-family:monospace;">has_policy →</span> </div> <div class="ns-node ns-node-amber" style="flex-shrink:0;min-width:110px;"><div class="ns-node-title">Benefits Policy v3</div><div class="ns-node-sub">Entity: Document</div></div> <div style="display:flex;flex-direction:column;align-items:center;gap:0.2rem;flex-shrink:0;font-size:0.65rem;color:#4a5a6a;"> <span style="color:#4fc97e;font-family:monospace;">covers →</span> </div> <div class="ns-node ns-node-green" style="flex-shrink:0;min-width:110px;"><div class="ns-node-title">Healthcare Benefits</div><div class="ns-node-sub">Entity: BenefitType</div></div> <div style="display:flex;flex-direction:column;align-items:center;gap:0.2rem;flex-shrink:0;font-size:0.65rem;color:#4a5a6a;"> <span style="color:#2698ba;font-family:monospace;">applies_to →</span> </div> <div class="ns-node ns-node-cyan" style="flex-shrink:0;min-width:110px;"><div class="ns-node-title">Transferred Employees</div><div class="ns-node-sub">Entity: EmployeeGroup</div></div> </div> </div> <p>When asked “How does the Company X acquisition affect transferred employees’ healthcare?”, GraphRAG:</p> <ol> <li>Finds the “Company X” node</li> <li>Traverses the <code class="language-plaintext highlighter-rouge">acquired_by</code> edge to “Our Company”</li> <li>Traverses the <code class="language-plaintext highlighter-rouge">has_policy</code> edge to “Benefits Policy v3”</li> <li>Traverses the <code class="language-plaintext highlighter-rouge">covers</code> edge to “Healthcare Benefits”</li> <li>Checks the <code class="language-plaintext highlighter-rouge">applies_to</code> relationship with “Transferred Employees”</li> <li>Retrieves the specific healthcare clauses that apply</li> </ol> <p>Standard RAG would struggle because no single document chunk contains this full chain of relationships. GraphRAG navigates the graph to synthesize it.</p> <p><strong>GraphRAG trade-offs:</strong></p> <table> <thead> <tr> <th> </th> <th>Standard RAG</th> <th>GraphRAG</th> </tr> </thead> <tbody> <tr> <td><strong>Setup cost</strong></td> <td>Low — just embed documents</td> <td>Very high — build and maintain the graph</td> </tr> <tr> <td><strong>Query speed</strong></td> <td>Fast (vector search)</td> <td>Slower (graph traversal)</td> </tr> <tr> <td><strong>Best for</strong></td> <td>Localized, factual questions</td> <td>Multi-hop, relationship questions</td> </tr> <tr> <td><strong>Maintenance</strong></td> <td>Re-embed changed docs</td> <td>Update graph schema + relationships</td> </tr> <tr> <td><strong>Use case</strong></td> <td>80% of enterprise RAG needs</td> <td>Complex financial, scientific, legal analysis</td> </tr> </tbody> </table> <hr/> <h2 id="agentic-rag-when-retrieval-needs-intelligence">Agentic RAG: When Retrieval Needs Intelligence</h2> <p>Standard RAG has a fundamental architectural limitation: it retrieves passively. Query comes in → retrieve K chunks → send to LLM. There’s no reasoning about whether the retrieved chunks are good, complete, or contradictory.</p> <p><strong>Agentic RAG</strong> adds a reasoning agent that acts as a critical evaluator and orchestrator of the retrieval process.</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">AGENTIC RAG — reasoning agent validates and enriches retrieval</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:300px;"><div class="ns-node-title">Complex User Query</div><div class="ns-node-sub">"How do our product's features compare to Competitor X's pricing?"</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:340px;"><div class="ns-node-title">Reasoning Agent — Decompose Query</div><div class="ns-node-sub">Breaks into sub-queries: (1) Our product features, (2) Our pricing, (3) Competitor X features, (4) Competitor X pricing</div></div> <div class="ns-arrow"></div> <div class="ns-phase" style="max-width:480px;"> <div class="ns-phase-title">Parallel Retrieval — runs all sub-queries simultaneously</div> <div class="ns-row"> <div class="ns-node"><div class="ns-node-title">Sub-query 1</div><div class="ns-node-sub">→ Product spec docs</div></div> <div class="ns-node"><div class="ns-node-title">Sub-query 2</div><div class="ns-node-sub">→ Pricing sheets</div></div> <div class="ns-node"><div class="ns-node-title">Sub-query 3+4</div><div class="ns-node-sub">→ Web search (live)</div></div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:360px;"><div class="ns-node-title">Agent Validates + Reconciles</div><div class="ns-node-sub">Checks dates (use 2025 spec sheet, not 2023). Resolves conflicts (final report overrides draft). Identifies gaps (no Competitor X pricing found → activates web search).</div></div> <div class="ns-arrow"></div> <div class="ns-decision" style="max-width:220px;"><div class="ns-node-title">Knowledge Complete?</div></div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:480px;"> <div class="ns-branch"> <span class="ns-label-red">Gaps remain</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"><div class="ns-node-title">Activate External Tools</div><div class="ns-node-sub">Web search, live API, human expert query</div></div> </div> <div class="ns-branch"> <span class="ns-label-green">Complete</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Generate Grounded Response</div><div class="ns-node-sub">LLM synthesizes validated context into comprehensive answer</div></div> </div> </div> </div> </div> <p><strong>The four capabilities Agentic RAG adds over standard RAG:</strong></p> <p><strong>1. Source validation and date-awareness.</strong> A standard RAG retrieves the highest-similarity chunks regardless of whether they’re a 2020 blog post or the 2025 official policy. An agent reads document metadata and prioritizes authoritative, current sources.</p> <p><strong>2. Conflict resolution.</strong> When two retrieved chunks disagree (initial proposal says €50K, final report says €65K), an agent reasons about which source is more authoritative rather than presenting both to the LLM and hoping it figures it out.</p> <p><strong>3. Multi-step decomposition.</strong> Complex questions that require combining information from multiple sources are decomposed into sub-queries, each retrieved separately, then synthesized.</p> <p><strong>4. Knowledge gap detection and tool activation.</strong> When the knowledge base doesn’t contain the answer (the internal knowledge base is updated weekly but the user asks about something that happened yesterday), the agent recognizes the gap and activates external tools (web search, live APIs) rather than producing an answer based on stale data.</p> <hr/> <h2 id="hands-on-code-three-implementations">Hands-On Code: Three Implementations</h2> <h3 id="implementation-1-google-search-as-rag-simplest">Implementation 1: Google Search as RAG (Simplest)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.tools</span> <span class="kn">import</span> <span class="n">google_search</span>
<span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span>

<span class="n">search_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">research_assistant</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">You help users research topics accurately.
When asked about current events, company information, or factual topics,
use the Google Search tool to retrieve current information.
Always cite the sources you found.</span><span class="sh">"""</span><span class="p">,</span>
    <span class="n">tools</span> <span class="o">=</span> <span class="p">[</span><span class="n">google_search</span><span class="p">]</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why is Google Search a form of RAG?</strong> It implements the core RAG pattern: retrieve relevant documents (web search results) → augment the prompt → generate response. The difference from a custom RAG system is that Google Search retrieves from the public web rather than a private knowledge base. Use this for public information that changes frequently (news, current events, general facts). Use a custom vector database RAG for private, proprietary, or domain-specific knowledge.</p> </blockquote> <h3 id="implementation-2-vertex-ai-rag-corpus">Implementation 2: Vertex AI RAG Corpus</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.memory</span> <span class="kn">import</span> <span class="n">VertexAiRagMemoryService</span>

<span class="c1"># Resource identifier for your Vertex AI RAG Corpus
</span><span class="n">RAG_CORPUS_RESOURCE_NAME</span> <span class="o">=</span> <span class="p">(</span>
    <span class="sh">"</span><span class="s">projects/your-gcp-project-id</span><span class="sh">"</span>
    <span class="sh">"</span><span class="s">/locations/us-central1</span><span class="sh">"</span>
    <span class="sh">"</span><span class="s">/ragCorpora/your-corpus-id</span><span class="sh">"</span>
<span class="p">)</span>

<span class="n">memory_service</span> <span class="o">=</span> <span class="nc">VertexAiRagMemoryService</span><span class="p">(</span>
    <span class="n">rag_corpus</span>                <span class="o">=</span> <span class="n">RAG_CORPUS_RESOURCE_NAME</span><span class="p">,</span>
    <span class="n">similarity_top_k</span>          <span class="o">=</span> <span class="mi">5</span><span class="p">,</span>    <span class="c1"># retrieve 5 most similar chunks
</span>    <span class="n">vector_distance_threshold</span> <span class="o">=</span> <span class="mf">0.7</span><span class="p">,</span>  <span class="c1"># only chunks with similarity &gt;= 0.7
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">similarity_top_k=5</code></strong>: Retrieve the 5 chunks with highest similarity to the query. More chunks = more context for the LLM, but more tokens consumed and potential for irrelevant information diluting relevant content. 3-7 is a typical range.</p> </blockquote> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">vector_distance_threshold=0.7</code></strong>: Only return chunks with cosine similarity at least 0.7 to the query. If no chunks meet this threshold, return nothing rather than returning irrelevant chunks. Without this threshold, RAG always returns K chunks even if none are relevant — which can cause the LLM to make up answers based on unrelated context (a subtle but common failure mode).</p> </blockquote> <blockquote> <p><strong>What is a Vertex AI RAG Corpus?</strong> A managed service that handles the full RAG pipeline infrastructure: document ingestion, chunking, embedding, and storage in Google’s vector search infrastructure. You upload documents, the service handles everything else. The <code class="language-plaintext highlighter-rouge">VertexAiRagMemoryService</code> in ADK connects your agent to this corpus for retrieval.</p> </blockquote> <h3 id="implementation-3-custom-langchain--langgraph-rag-pipeline">Implementation 3: Custom LangChain + LangGraph RAG Pipeline</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_community.document_loaders</span> <span class="kn">import</span> <span class="n">TextLoader</span>
<span class="kn">from</span> <span class="n">langchain_core.documents</span> <span class="kn">import</span> <span class="n">Document</span>
<span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">ChatPromptTemplate</span>
<span class="kn">from</span> <span class="n">langchain_core.output_parsers</span> <span class="kn">import</span> <span class="n">StrOutputParser</span>
<span class="kn">from</span> <span class="n">langchain_community.embeddings</span> <span class="kn">import</span> <span class="n">OpenAIEmbeddings</span>
<span class="kn">from</span> <span class="n">langchain_community.vectorstores</span> <span class="kn">import</span> <span class="n">Weaviate</span>
<span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>
<span class="kn">from</span> <span class="n">langchain.text_splitter</span> <span class="kn">import</span> <span class="n">CharacterTextSplitter</span>
<span class="kn">from</span> <span class="n">langgraph.graph</span> <span class="kn">import</span> <span class="n">StateGraph</span><span class="p">,</span> <span class="n">END</span>
<span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span><span class="p">,</span> <span class="n">TypedDict</span>
<span class="kn">import</span> <span class="n">weaviate</span>
<span class="kn">from</span> <span class="n">weaviate.embedded</span> <span class="kn">import</span> <span class="n">EmbeddedOptions</span>
</code></pre></div></div> <blockquote> <p><strong>Why LangGraph instead of a simple chain?</strong> A simple chain runs retrieval → generation sequentially with no flexibility. LangGraph defines the pipeline as a graph where each step is a node and transitions are explicit edges. This enables: conditional routing (if retrieval finds no relevant documents, take a different path), loops (retry with a refined query if the first retrieval was insufficient), and parallel branches (retrieve from multiple sources simultaneously). For production RAG, you almost always need this flexibility.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Step 1: Prepare the knowledge base (done once)
</span><span class="n">loader</span>    <span class="o">=</span> <span class="nc">TextLoader</span><span class="p">(</span><span class="sh">'</span><span class="s">./company_docs.txt</span><span class="sh">'</span><span class="p">)</span>
<span class="n">documents</span> <span class="o">=</span> <span class="n">loader</span><span class="p">.</span><span class="nf">load</span><span class="p">()</span>

<span class="n">text_splitter</span> <span class="o">=</span> <span class="nc">CharacterTextSplitter</span><span class="p">(</span>
    <span class="n">chunk_size</span>    <span class="o">=</span> <span class="mi">500</span><span class="p">,</span>  <span class="c1"># characters per chunk
</span>    <span class="n">chunk_overlap</span> <span class="o">=</span> <span class="mi">50</span>    <span class="c1"># overlap prevents losing context at boundaries
</span><span class="p">)</span>
<span class="n">chunks</span> <span class="o">=</span> <span class="n">text_splitter</span><span class="p">.</span><span class="nf">split_documents</span><span class="p">(</span><span class="n">documents</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">chunk_size=500</code> and <code class="language-plaintext highlighter-rouge">chunk_overlap=50</code>:</strong> These are critical tuning parameters. Smaller chunks (200-300 chars) give more precise retrieval but lose context. Larger chunks (1,000+ chars) preserve context but make retrieval less precise because each chunk covers multiple topics. The overlap of 50 characters ensures that a sentence split across chunk boundaries appears in both chunks — preventing the common failure where the most important sentence in a document is the first/last line of a chunk and loses its context.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Step 2: Embed and store in Weaviate
</span><span class="n">client</span> <span class="o">=</span> <span class="n">weaviate</span><span class="p">.</span><span class="nc">Client</span><span class="p">(</span><span class="n">embedded_options</span><span class="o">=</span><span class="nc">EmbeddedOptions</span><span class="p">())</span>
<span class="n">vectorstore</span> <span class="o">=</span> <span class="n">Weaviate</span><span class="p">.</span><span class="nf">from_documents</span><span class="p">(</span>
    <span class="n">client</span>    <span class="o">=</span> <span class="n">client</span><span class="p">,</span>
    <span class="n">documents</span> <span class="o">=</span> <span class="n">chunks</span><span class="p">,</span>
    <span class="n">embedding</span> <span class="o">=</span> <span class="nc">OpenAIEmbeddings</span><span class="p">(),</span>
    <span class="n">by_text</span>   <span class="o">=</span> <span class="bp">False</span>
<span class="p">)</span>
<span class="n">retriever</span> <span class="o">=</span> <span class="n">vectorstore</span><span class="p">.</span><span class="nf">as_retriever</span><span class="p">()</span>
<span class="n">llm</span>       <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model_name</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-3.5-turbo</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">EmbeddedOptions()</code></strong>: Runs Weaviate in-process (embedded mode) without needing a separate Weaviate server. Perfect for development and testing. For production, point to a Weaviate instance running as a separate service.</p> </blockquote> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">by_text=False</code></strong>: Use pre-computed embeddings rather than Weaviate’s built-in vectorization. This gives you explicit control over which embedding model is used, ensuring consistency between embedding during indexing and embedding at query time. Always use the same embedding model for both — mixing models produces meaningless similarity scores.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Step 3: Define graph state
</span><span class="k">class</span> <span class="nc">RAGGraphState</span><span class="p">(</span><span class="n">TypedDict</span><span class="p">):</span>
    <span class="n">question</span><span class="p">:</span>   <span class="nb">str</span>
    <span class="n">documents</span><span class="p">:</span>  <span class="n">List</span><span class="p">[</span><span class="n">Document</span><span class="p">]</span>
    <span class="n">generation</span><span class="p">:</span> <span class="nb">str</span>
</code></pre></div></div> <blockquote> <p><strong>Why a TypedDict for state?</strong> LangGraph passes state between nodes as a dictionary. Using <code class="language-plaintext highlighter-rouge">TypedDict</code> documents the expected keys and their types — it’s not strictly required but makes the graph’s data flow explicit and enables type checking. Each node function receives the full state dict and returns a partial update (only the keys it modifies).</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Step 4: Define nodes
</span><span class="k">def</span> <span class="nf">retrieve_documents_node</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">RAGGraphState</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">RAGGraphState</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Retrieves relevant document chunks for the user</span><span class="sh">'</span><span class="s">s question.</span><span class="sh">"""</span>
    <span class="n">documents</span> <span class="o">=</span> <span class="n">retriever</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">])</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">documents</span><span class="sh">"</span><span class="p">:</span> <span class="n">documents</span><span class="p">,</span> <span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">],</span> <span class="sh">"</span><span class="s">generation</span><span class="sh">"</span><span class="p">:</span> <span class="sh">""</span><span class="p">}</span>

<span class="k">def</span> <span class="nf">generate_response_node</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">RAGGraphState</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">RAGGraphState</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Generates an answer using retrieved context.</span><span class="sh">"""</span>
    <span class="n">template</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">Answer the following question based ONLY on the provided context.
If you cannot answer from the context, say </span><span class="sh">"</span><span class="s">I don</span><span class="sh">'</span><span class="s">t have enough information.</span><span class="sh">"</span><span class="s">

Context: {context}
Question: {question}
Answer:</span><span class="sh">"""</span>

    <span class="n">context</span> <span class="o">=</span> <span class="sh">"</span><span class="se">\n\n</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">([</span><span class="n">doc</span><span class="p">.</span><span class="n">page_content</span> <span class="k">for</span> <span class="n">doc</span> <span class="ow">in</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">documents</span><span class="sh">"</span><span class="p">]])</span>
    <span class="n">prompt</span>  <span class="o">=</span> <span class="n">ChatPromptTemplate</span><span class="p">.</span><span class="nf">from_template</span><span class="p">(</span><span class="n">template</span><span class="p">)</span>
    <span class="n">chain</span>   <span class="o">=</span> <span class="n">prompt</span> <span class="o">|</span> <span class="n">llm</span> <span class="o">|</span> <span class="nc">StrOutputParser</span><span class="p">()</span>
    <span class="n">generation</span> <span class="o">=</span> <span class="n">chain</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">context</span><span class="sh">"</span><span class="p">:</span> <span class="n">context</span><span class="p">,</span> <span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">]})</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">],</span> <span class="sh">"</span><span class="s">documents</span><span class="sh">"</span><span class="p">:</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">documents</span><span class="sh">"</span><span class="p">],</span> <span class="sh">"</span><span class="s">generation</span><span class="sh">"</span><span class="p">:</span> <span class="n">generation</span><span class="p">}</span>
</code></pre></div></div> <blockquote> <p><strong>“Answer ONLY from the context”</strong>: This instruction is critical for RAG correctness. Without it, the LLM might supplement retrieved context with its own training knowledge, producing a mixed answer that’s hard to attribute or verify. The strict instruction forces the LLM to either use the retrieved evidence or admit it doesn’t know — making the system more honest and auditable.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Step 5: Build and compile the graph
</span><span class="n">workflow</span> <span class="o">=</span> <span class="nc">StateGraph</span><span class="p">(</span><span class="n">RAGGraphState</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="sh">"</span><span class="s">retrieve</span><span class="sh">"</span><span class="p">,</span> <span class="n">retrieve_documents_node</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="sh">"</span><span class="s">generate</span><span class="sh">"</span><span class="p">,</span> <span class="n">generate_response_node</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">set_entry_point</span><span class="p">(</span><span class="sh">"</span><span class="s">retrieve</span><span class="sh">"</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="sh">"</span><span class="s">retrieve</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">generate</span><span class="sh">"</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="sh">"</span><span class="s">generate</span><span class="sh">"</span><span class="p">,</span> <span class="n">END</span><span class="p">)</span>
<span class="n">app</span> <span class="o">=</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">compile</span><span class="p">()</span>

<span class="c1"># Step 6: Run queries
</span><span class="n">result</span> <span class="o">=</span> <span class="n">app</span><span class="p">.</span><span class="nf">invoke</span><span class="p">({</span><span class="sh">"</span><span class="s">question</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">What is our company</span><span class="sh">'</span><span class="s">s vacation policy?</span><span class="sh">"</span><span class="p">})</span>
<span class="nf">print</span><span class="p">(</span><span class="n">result</span><span class="p">[</span><span class="sh">"</span><span class="s">generation</span><span class="sh">"</span><span class="p">])</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">app.invoke()</code> instead of calling functions directly?</strong> <code class="language-plaintext highlighter-rouge">invoke()</code> runs the full LangGraph state machine — it manages state initialization, passes state between nodes, handles the edge routing, and returns the final state. Calling functions directly would bypass the graph infrastructure, losing LangGraph’s benefits (streaming, checkpointing, conditional edges).</p> </blockquote> <hr/> <h2 id="rags-challenges-what-can-go-wrong">RAG’s Challenges: What Can Go Wrong</h2> <p><strong>Challenge 1: Multi-document synthesis.</strong> If the answer to a question spans 5 different documents, and retrieval only returns 3 of them, the LLM will produce an incomplete answer — worse, it may produce a confident but incomplete one without signaling that information is missing. Mitigation: increase <code class="language-plaintext highlighter-rouge">k</code>, use multi-query retrieval (generate multiple variations of the query and merge results), or use Agentic RAG to detect gaps.</p> <p><strong>Challenge 2: Chunk boundary problems.</strong> A crucial sentence that spans two chunk boundaries may appear incomplete in both chunks. Mitigation: chunk overlap, recursive chunking, or sentence-aware splitting.</p> <p><strong>Challenge 3: Retrieval noise.</strong> If irrelevant chunks are retrieved (false positives), they pollute the LLM’s context with confusing or contradictory information. The LLM may hallucinate to reconcile the conflict. Mitigation: use a distance threshold, re-rank retrieved chunks using a cross-encoder model before sending to LLM.</p> <p><strong>Challenge 4: Index staleness.</strong> Your knowledge base is outdated. A policy changed last week but the vector store hasn’t been re-indexed. The LLM confidently answers from the old policy. Mitigation: incremental indexing pipelines, document versioning, freshness metadata, Agentic RAG with web search fallback for time-sensitive queries.</p> <p><strong>Challenge 5: Query-document vocabulary mismatch.</strong> The user asks about “vacation days” but your policy document uses “annual leave.” Pure semantic search helps but isn’t perfect. Mitigation: hybrid search (BM25 + semantic), query expansion (generate synonyms and related terms before retrieval).</p> <p><strong>Challenge 6: Context window limits.</strong> Retrieving 10 long chunks may exceed the LLM’s context window. Mitigation: summarize chunks before passing to LLM, reduce k, use a model with a larger context window.</p> <hr/> <h2 id="four-practical-applications">Four Practical Applications</h2> <div class="rag-usecases-grid"> <div class="rag-uc-card"> <span class="rag-uc-num">01</span> <h4>Enterprise Knowledge Base Q&amp;A</h4> <p>HR chatbots answering policy questions, IT helpdesks troubleshooting from documentation, legal teams searching contracts for specific clauses — all powered by RAG over internal document stores.</p> <span class="rag-uc-domain">HR · IT · Legal · Operations</span> </div> <div class="rag-uc-card"> <span class="rag-uc-num">02</span> <h4>Customer Support Automation</h4> <p>Agents that answer product-specific questions from support tickets, FAQs, and product manuals. Unlike a fine-tuned model, RAG-based support agents can be updated by uploading new documentation without retraining.</p> <span class="rag-uc-domain">SaaS · E-commerce · Telecom</span> </div> <div class="rag-uc-card"> <span class="rag-uc-num">03</span> <h4>News and Research Summarization</h4> <p>LLMs connected to live news feeds can summarize recent developments on any topic. The RAG system retrieves today's articles and the LLM synthesizes them — combining recency with synthesis capability.</p> <span class="rag-uc-domain">Media · Finance · Research</span> </div> <div class="rag-uc-card"> <span class="rag-uc-num">04</span> <h4>Personalized Recommendations</h4> <p>Instead of keyword matching, RAG retrieves products/content that are semantically aligned with a user's expressed preferences, past behavior, and current context — enabling genuine semantic recommendations.</p> <span class="rag-uc-domain">E-commerce · Content · EdTech</span> </div> </div> <style>.rag-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:.85rem;margin:1.5rem 0}.rag-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.rag-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#2698ba}.rag-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.rag-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.rag-uc-domain{font-size:.65rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="rag-summary-card"> <div class="rag-summary-col"> <div class="rag-summary-label">WHAT</div> <p>A pattern that connects LLMs to external knowledge bases, enabling them to retrieve relevant information at query time rather than relying solely on training data. Converts an LLM from a closed-book test to an open-book one.</p> </div> <div class="rag-summary-divider"></div> <div class="rag-summary-col"> <div class="rag-summary-label">WHY</div> <p>LLM training data is static, doesn't include private knowledge, and has a cutoff date. RAG provides access to current, specific, and proprietary information — grounding outputs in verifiable facts and dramatically reducing hallucination.</p> </div> <div class="rag-summary-divider"></div> <div class="rag-summary-col"> <div class="rag-summary-label">RULE OF THUMB</div> <p>Use RAG whenever the LLM needs to answer from specific, private, or current information not in its training data. Start with simple vector search RAG; add hybrid search for better precision; add agentic reasoning for complex multi-source synthesis.</p> </div> </div> <style>.rag-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.rag-summary-card{flex-direction:column}}.rag-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.rag-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.rag-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.rag-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li> <p><strong>RAG is a closed-book to open-book transformation.</strong> The LLM stops guessing from training data and starts reading from actual documents. This is the single most effective intervention for reducing hallucination and increasing factual accuracy.</p> </li> <li> <p><strong>Embeddings encode meaning, not words.</strong> The same semantic concept expressed in different words produces similar vectors. This enables finding relevant content even when the user’s phrasing doesn’t match the document’s vocabulary.</p> </li> <li> <p><strong>Chunking strategy matters as much as retrieval strategy.</strong> Poorly chunked documents break context at wrong boundaries, produce retrieval noise, and lose the information the user needs. Use sentence/paragraph-aware chunking with overlap.</p> </li> <li> <p><strong>Hybrid search outperforms either BM25 or semantic search alone.</strong> BM25 catches exact keyword matches; semantic search catches conceptual relevance. Production systems use both, combined with Reciprocal Rank Fusion.</p> </li> <li> <p><strong>The <code class="language-plaintext highlighter-rouge">vector_distance_threshold</code> prevents confident wrong answers.</strong> Without a minimum similarity threshold, RAG always returns K chunks even when none are relevant — causing the LLM to generate answers from unrelated context. Better to say “I don’t have information on this” than to confidently answer from the wrong context.</p> </li> <li> <p><strong>GraphRAG handles relationship-heavy queries; standard RAG handles localized queries.</strong> If your questions are mostly “what does this policy say?”, standard RAG is sufficient. If they’re “how does X relate to Y across our organization?”, GraphRAG is worth the added complexity.</p> </li> <li> <p><strong>Agentic RAG adds source validation, conflict resolution, and gap detection.</strong> For high-stakes RAG applications (legal, medical, financial), the agent layer ensures retrieved context is current, authoritative, and complete before being passed to the LLM.</p> </li> <li> <p><strong>Vertex AI RAG Corpus handles infrastructure; LangGraph handles pipeline logic.</strong> Use managed services for the embedding/storage infrastructure. Use graph-based orchestration (LangGraph) for complex multi-step retrieval pipelines with conditional logic.</p> </li> </ul> <hr/> <p><em>This concludes the 14-chapter Agentic AI series. From the simplest prompt chain (Chapter 1) to the most sophisticated retrieval-augmented generation system (Chapter 14), you now have the complete map of how intelligent AI agents are built, deployed, and made reliable in the real world.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="rag"/><category term="retrieval"/><category term="vector-database"/><summary type="html"><![CDATA[LLMs know a lot — but their knowledge stopped the day training ended. RAG is the bridge between static model weights and the live, private, specific knowledge that makes agents actually useful.]]></summary></entry><entry><title type="html">Chapter 13: Human-in-the-Loop</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/human-in-the-loop/" rel="alternate" type="text/html" title="Chapter 13: Human-in-the-Loop"/><published>2026-02-22T00:00:00+00:00</published><updated>2026-02-22T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/human-in-the-loop</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/human-in-the-loop/"><![CDATA[<h2 id="the-autonomy-paradox">The Autonomy Paradox</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Autonomy:</strong> The ability to act independently without human intervention. A fully autonomous agent makes all decisions on its own. A fully human-controlled system requires a human to approve every action. Most production AI systems live somewhere in between.</p> <p style="margin-top:0.5rem"><strong>Escalation:</strong> The process of handing a task from an AI agent to a human when the agent reaches the limits of its reliable capability. Escalation policies define: what triggers escalation (complexity, uncertainty, risk level), what information is passed to the human, and what the human is expected to decide.</p> <p style="margin-top:0.5rem"><strong>Human-in-the-Loop (HITL):</strong> A design pattern where human judgment is deliberately integrated into the AI's workflow at specific decision points. The human is not just monitoring — they are an active participant in the decision-making process for certain actions.</p> <p style="margin-top:0.5rem"><strong>Human-on-the-Loop (HOTL):</strong> A related pattern where a human sets the rules and monitors for exceptions, but the AI handles day-to-day execution autonomously. The human is informed of what happened and can intervene — but doesn't approve each action in advance. Think: setting investment policy and letting the AI trade within that policy.</p> <p style="margin-top:0.5rem"><strong>Alignment:</strong> The property of an AI system acting in accordance with human values, intentions, and goals. A system that's technically capable but misaligned might achieve the stated objective while violating implicit constraints — like a customer service bot that resolves complaints by simply closing tickets without actually addressing the issue.</p> <p style="margin-top:0.5rem"><strong>Scalability (in this context):</strong> The ability to handle increasing volume. HITL has an inherent scalability limit — humans can't review a million transactions per day. This is the fundamental tension HITL must navigate: depth of oversight vs. breadth of coverage.</p> </div> <p>AI agents are getting remarkably capable. They can write code, analyze documents, execute complex multi-step workflows, and reason through nuanced problems. The natural next question is: why not just let them run autonomously?</p> <p>The answer isn’t “because the technology isn’t ready” — in many narrow domains, the technology is perfectly capable of autonomous operation. The answer is more fundamental: <strong>in certain decisions, the consequences of errors are so severe that the cost of human oversight is worth paying, regardless of how good the AI is</strong>.</p> <p>Consider:</p> <ul> <li>A loan approval system denies credit to 10,000 applicants. The AI was 95% accurate. That’s still 500 people incorrectly denied credit — potentially ruining financial futures.</li> <li>A medical diagnosis system recommends treatment. It’s wrong 2% of the time. That’s millions of misdiagnoses per year if deployed at scale.</li> <li>A legal AI recommends a prison sentence. Even a 1% error rate means hundreds of people jailed incorrectly.</li> </ul> <p>In these domains, “pretty good” is not good enough. The accountability, ethics, and legal requirements demand that a human be responsible for the decision — not because the AI can’t do it, but because the stakes require human judgment, human accountability, and human context.</p> <p><strong>Human-in-the-Loop (HITL)</strong> is the pattern that navigates this reality. It doesn’t reject AI autonomy — it directs autonomy to where it’s appropriate, and maintains human control where it’s necessary.</p> <hr/> <h2 id="the-spectrum-of-human-involvement">The Spectrum of Human Involvement</h2> <p>There is no single correct amount of human involvement in an AI system. The right level depends on the risk of errors, the volume of decisions, the expertise required to evaluate them, and the regulatory environment. The spectrum below shows the range:</p> <div class="hitl-spectrum-wrapper"> <div class="hitl-spectrum-header"> <span class="hitl-spectrum-title">HUMAN INVOLVEMENT SPECTRUM — click to explore each level</span> </div> <div class="hitl-spectrum-tabs"> <button class="hitl-tab active" data-idx="0" onclick="hitlTab(0)">Full Automation</button> <button class="hitl-tab" data-idx="1" onclick="hitlTab(1)">Human-on-Loop</button> <button class="hitl-tab" data-idx="2" onclick="hitlTab(2)">Human-in-Loop</button> <button class="hitl-tab" data-idx="3" onclick="hitlTab(3)">AI-Assisted</button> <button class="hitl-tab" data-idx="4" onclick="hitlTab(4)">Full Human</button> </div> <div class="hitl-spectrum-body"> <div class="hitl-spec-content active" id="hitlSpec0"> <div class="hitl-spec-level">Level 1 — Full Automation</div> <div class="hitl-spec-name">AI acts without any human involvement</div> <div class="hitl-spec-desc">The AI makes and executes decisions autonomously. No human reviews outputs or approves actions. Humans may monitor aggregate metrics but don't intervene on individual decisions.</div> <div class="hitl-spec-when"><strong>When appropriate:</strong> Decisions with very low risk, very high volume, and well-defined success criteria. Spam filtering, autocomplete suggestions, recommendation ranking, ad targeting.</div> <div class="hitl-spec-example">Spam filter automatically moves 10,000 emails to the spam folder. No human reviews each one. If the filter makes an occasional mistake, the cost is minor (missed newsletter) and easily corrected.</div> </div> <div class="hitl-spec-content" id="hitlSpec1"> <div class="hitl-spec-level">Level 2 — Human-on-the-Loop (HOTL)</div> <div class="hitl-spec-name">Human sets policy; AI executes autonomously within it</div> <div class="hitl-spec-desc">A human expert defines the rules, boundaries, and policies. The AI then handles all decisions that fall within those policies autonomously and at high speed. Humans monitor for anomalies and update policies when needed.</div> <div class="hitl-spec-when"><strong>When appropriate:</strong> High-volume decisions with well-defined policies, where the AI must act faster than humans can review. Algorithmic trading, call routing, network management.</div> <div class="hitl-spec-example">Financial manager defines: "Buy AAPL if RSI &lt; 30, sell if RSI &gt; 70, never invest more than 5% in any single stock." AI executes hundreds of trades per day within these rules. Manager reviews daily summary and updates policy monthly.</div> </div> <div class="hitl-spec-content" id="hitlSpec2"> <div class="hitl-spec-level">Level 3 — Human-in-the-Loop (HITL)</div> <div class="hitl-spec-name">Human reviews and approves specific high-stakes decisions</div> <div class="hitl-spec-desc">The AI handles routine cases autonomously. For cases that are high-risk, ambiguous, or outside the AI's confident range, it pauses and escalates to a human. The human's decision is final and is recorded for accountability.</div> <div class="hitl-spec-when"><strong>When appropriate:</strong> Mixed-risk environments where most decisions are routine (AI can handle) but some require human judgment (AI should escalate). Fraud detection, content moderation, medical triage.</div> <div class="hitl-spec-example">Fraud detection AI automatically blocks 95% of clearly fraudulent transactions. For transactions with medium confidence scores, it flags them for human analyst review. The analyst investigates and makes the final call.</div> </div> <div class="hitl-spec-content" id="hitlSpec3"> <div class="hitl-spec-level">Level 4 — AI-Assisted Decision Making</div> <div class="hitl-spec-name">Human decides; AI provides analysis and recommendations</div> <div class="hitl-spec-desc">The human always makes the decision. The AI's role is to assist — gathering relevant information, analyzing options, surfacing risks, generating recommendations. The human is fully in control but better informed.</div> <div class="hitl-spec-when"><strong>When appropriate:</strong> Complex, high-stakes decisions requiring human judgment, where AI can add value through analysis but shouldn't make the final call. Legal judgments, medical diagnoses, strategic business decisions.</div> <div class="hitl-spec-example">Doctor reviews AI analysis of patient's MRI scan: "AI detects pattern consistent with early-stage condition X (confidence: 78%). Similar patterns in 15 past cases: 12 confirmed, 3 not." Doctor makes diagnosis. AI informed the decision, didn't make it.</div> </div> <div class="hitl-spec-content" id="hitlSpec4"> <div class="hitl-spec-level">Level 5 — Full Human Control</div> <div class="hitl-spec-name">Human makes all decisions; AI only provides tools or information</div> <div class="hitl-spec-desc">The human is fully in charge. AI may provide data, formatting, or convenience — but no autonomous decision-making, no recommendations, no automation of consequential actions. Required in domains with strict regulatory or ethical constraints.</div> <div class="hitl-spec-when"><strong>When appropriate:</strong> Legal sentencing, criminal investigations, core civil liberties decisions, creative authorship, diplomatic negotiations.</div> <div class="hitl-spec-example">Judge uses AI only to access case law database and format documents. All judicial reasoning, interpretation of law, and sentencing decisions are made entirely by the judge. No AI recommendations on outcomes.</div> </div> </div> </div> <style>.hitl-spectrum-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.hitl-spectrum-header{padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.hitl-spectrum-tabs{display:flex;overflow-x:auto;border-bottom:1px solid var(--global-divider-color)}.hitl-tab{flex-shrink:0;padding:.5rem .85rem;font-family:monospace;font-size:.68rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s}.hitl-tab:last-child{border-right:0}.hitl-tab.active{background:rgba(38,152,186,0.1);color:#2698ba;font-weight:700}.hitl-spectrum-body{padding:1.1rem}.hitl-spec-content{display:none;flex-direction:column;gap:.65rem}.hitl-spec-content.active{display:flex}.hitl-spec-level{font-size:.65rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:#2698ba}.hitl-spec-name{font-size:.95rem;font-weight:700;color:var(--global-text-color)}.hitl-spec-desc{font-size:.83rem;color:var(--global-text-color);line-height:1.65}.hitl-spec-when{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6}.hitl-spec-example{font-size:.78rem;background:rgba(79,201,126,0.06);border-left:3px solid #4fc97e;padding:.55rem .75rem;border-radius:0 6px 6px 0;color:var(--global-text-color);line-height:1.6}</style> <script>
function hitlTab(idx) {
  document.querySelectorAll('.hitl-tab').forEach(function(t){ t.classList.remove('active'); });
  document.querySelectorAll('.hitl-spec-content').forEach(function(c){ c.classList.remove('active'); });
  document.querySelector('.hitl-tab[data-idx="'+idx+'"]').classList.add('active');
  document.getElementById('hitlSpec'+idx).classList.add('active');
}
</script> <hr/> <h2 id="how-hitl-works-the-pattern-architecture">How HITL Works: The Pattern Architecture</h2> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">HUMAN-IN-THE-LOOP PATTERN — escalation and review flow</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:300px;"> <div class="ns-node-title">Request Arrives</div> <div class="ns-node-sub">User query, system event, or incoming data that the agent must process</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:320px;"> <div class="ns-node-title">Agent Processes + Assesses Confidence</div> <div class="ns-node-sub">Analyzes request, takes action or generates recommendation. Simultaneously estimates: How confident am I? Is this within my reliable capability? What is the risk if I'm wrong?</div> </div> <div class="ns-arrow"></div> <div class="ns-decision" style="max-width:240px;"> <div class="ns-node-title">Escalation Threshold Met?</div> <div class="ns-node-sub">High risk, low confidence, complex context, flagged category?</div> </div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:540px;"> <div class="ns-branch"> <span class="ns-label-green">No — handle autonomously</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Execute Action</div> <div class="ns-node-sub">Agent acts. Logs decision, action, and confidence score for audit trail.</div> </div> </div> <div class="ns-branch"> <span class="ns-label-red">Yes — escalate to human</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">Human Review Queue</div> <div class="ns-node-sub">Agent presents: the request, its analysis, recommended action, confidence level, and reason for escalation. Human decides.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Human Decision</div> <div class="ns-node-sub">Approve agent recommendation · Modify and approve · Override with different action · Request more information</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Execute + Learn</div> <div class="ns-node-sub">Execute the human-approved action. Store the (situation, human_decision) pair as training data for improving future confidence calibration.</div> </div> </div> </div> </div> </div> <p><strong>The four key design decisions in any HITL implementation:</strong></p> <ol> <li><strong>Escalation trigger:</strong> What conditions send a case to human review? (Confidence below threshold, specific risk categories, dollar amounts above $X, regulatory requirements)</li> <li><strong>Information presented to human:</strong> What does the human see? The agent’s analysis should be complete but not overwhelming — include the key facts, the recommended action, and the specific reason for escalation.</li> <li><strong>Response options:</strong> What can the human do? (Approve, modify, override, defer, flag for training)</li> <li><strong>Feedback loop:</strong> How does the human’s decision improve the agent? Every escalated decision is a labeled training example — use it.</li> </ol> <hr/> <h2 id="designing-escalation-policies">Designing Escalation Policies</h2> <p>An escalation policy is a formal specification of when and how the agent hands off to a human. Without an explicit policy, escalation is ad hoc — inconsistent, unpredictable, and unable to be improved systematically.</p> <h3 id="what-triggers-escalation">What Triggers Escalation</h3> <div class="hitl-triggers-grid"> <div class="hitl-trigger-card"> <div class="hitl-trigger-icon">📊</div> <h4>Confidence Threshold</h4> <p>Escalate when the agent's confidence in its output falls below a defined threshold. "If classification confidence &lt; 80%, escalate." Requires the agent to produce calibrated confidence scores.</p> <span class="hitl-trigger-example">Content moderation: posts with borderline confidence scores go to human reviewers</span> </div> <div class="hitl-trigger-card"> <div class="hitl-trigger-icon">💰</div> <h4>Risk / Impact Threshold</h4> <p>Escalate when the consequence of an error exceeds a defined limit. "Any payment above $10,000 requires human approval." Independent of confidence — even a confident agent should get human sign-off on high-impact actions.</p> <span class="hitl-trigger-example">Financial systems: transactions above threshold always go to human review</span> </div> <div class="hitl-trigger-card"> <div class="hitl-trigger-icon">🏷️</div> <h4>Category Rules</h4> <p>Escalate when the request falls into a pre-defined category that always requires human judgment. "All medical advice, legal decisions, and politically sensitive content → human review."</p> <span class="hitl-trigger-example">Customer support: any mention of "lawsuit", "discrimination", or "harassment" routes to senior human agent</span> </div> <div class="hitl-trigger-card"> <div class="hitl-trigger-icon">🔄</div> <h4>Iteration Limit</h4> <p>Escalate when the agent has tried and failed to resolve a situation within N attempts. Prevents infinite retry loops that never resolve and ensures stuck cases get human attention.</p> <span class="hitl-trigger-example">Technical support: if troubleshooting tool hasn't resolved issue after 3 attempts, escalate to human specialist</span> </div> <div class="hitl-trigger-card"> <div class="hitl-trigger-icon">😤</div> <h4>Sentiment / Urgency Signal</h4> <p>Escalate when user signals indicate emotional distress, urgent need, or explicit request for a human. "If customer frustration score &gt; 8/10, transfer to human agent." Respects user autonomy and emotional context.</p> <span class="hitl-trigger-example">Call centers: tone analysis triggering transfer to empathetic human agent for distressed callers</span> </div> <div class="hitl-trigger-card"> <div class="hitl-trigger-icon">🚨</div> <h4>Novelty Detection</h4> <p>Escalate when the request is significantly different from anything the agent has seen in training. "Out-of-distribution input detected → escalate rather than confidently guess." The agent knows what it doesn't know.</p> <span class="hitl-trigger-example">Medical AI: unusual symptom combination outside training distribution flagged for physician review</span> </div> </div> <style>.hitl-triggers-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(210px,1fr));gap:.85rem;margin:1.5rem 0}.hitl-trigger-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.hitl-trigger-icon{font-size:1.1rem}.hitl-trigger-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.hitl-trigger-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.hitl-trigger-example{font-size:.68rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="the-code-technical-support-agent-with-hitl">The Code: Technical Support Agent with HITL</h2> <p>The ADK code example demonstrates HITL through an escalation tool — an agent that handles routine support cases autonomously but calls a human specialist for complex ones.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span>
<span class="kn">from</span> <span class="n">google.adk.tools.tool_context</span> <span class="kn">import</span> <span class="n">ToolContext</span>
<span class="kn">from</span> <span class="n">google.adk.callbacks</span> <span class="kn">import</span> <span class="n">CallbackContext</span>
<span class="kn">from</span> <span class="n">google.adk.models.llm</span> <span class="kn">import</span> <span class="n">LlmRequest</span>
<span class="kn">from</span> <span class="n">google.genai</span> <span class="kn">import</span> <span class="n">types</span>
<span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">Optional</span>
</code></pre></div></div> <blockquote> <p><strong>Why these imports?</strong> <code class="language-plaintext highlighter-rouge">Agent</code> is the core ADK agent class. <code class="language-plaintext highlighter-rouge">ToolContext</code> gives tools access to session state. <code class="language-plaintext highlighter-rouge">CallbackContext</code> and <code class="language-plaintext highlighter-rouge">LlmRequest</code> are used for the personalization callback — a hook that runs before every LLM call. <code class="language-plaintext highlighter-rouge">types</code> is Google’s Generative AI types library for creating structured content.</p> </blockquote> <h3 id="defining-the-tool-suite">Defining the Tool Suite</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">troubleshoot_issue</span><span class="p">(</span><span class="n">issue</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Analyzes a technical issue and provides step-by-step troubleshooting guidance.
    Use this for standard technical problems like connectivity issues, software errors,
    or device configuration problems.

    Args:
        issue: Description of the technical problem.
    Returns:
        dict with </span><span class="sh">'</span><span class="s">status</span><span class="sh">'</span><span class="s"> and </span><span class="sh">'</span><span class="s">report</span><span class="sh">'</span><span class="s"> containing troubleshooting steps.
    </span><span class="sh">"""</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">success</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">report</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Troubleshooting steps for </span><span class="si">{</span><span class="n">issue</span><span class="si">}</span><span class="s">.</span><span class="sh">"</span><span class="p">}</span>


<span class="k">def</span> <span class="nf">create_ticket</span><span class="p">(</span><span class="n">issue_type</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">details</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Creates a support ticket to log an unresolved issue for follow-up.
    Use this when basic troubleshooting hasn</span><span class="sh">'</span><span class="s">t resolved the issue.

    Args:
        issue_type: Category of the issue (hardware, software, connectivity, etc.)
        details: Full description of the problem and steps attempted so far.
    Returns:
        dict with </span><span class="sh">'</span><span class="s">status</span><span class="sh">'</span><span class="s"> and </span><span class="sh">'</span><span class="s">ticket_id</span><span class="sh">'</span><span class="s"> for tracking.
    </span><span class="sh">"""</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">success</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">ticket_id</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">TICKET123</span><span class="sh">"</span><span class="p">}</span>


<span class="k">def</span> <span class="nf">escalate_to_human</span><span class="p">(</span><span class="n">issue_type</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Escalates the customer</span><span class="sh">'</span><span class="s">s issue to a human specialist.
    Use this ONLY when:
    1. The issue is too complex for basic troubleshooting
    2. The customer is highly frustrated or explicitly requests a human
    3. The issue involves safety, legal, or financial implications
    4. Basic troubleshooting has been exhausted without resolution

    Args:
        issue_type: The category of issue requiring human expertise.
    Returns:
        dict confirming escalation with expected response time.
    </span><span class="sh">"""</span>
    <span class="k">return</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">success</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">message</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Transferred to human specialist for </span><span class="si">{</span><span class="n">issue_type</span><span class="si">}</span><span class="s">. Expected wait: 5 minutes.</span><span class="sh">"</span>
    <span class="p">}</span>
</code></pre></div></div> <blockquote> <p><strong>Why is <code class="language-plaintext highlighter-rouge">escalate_to_human</code> a tool?</strong> This is the key design insight. Escalation is treated as an <em>action the agent can choose to take</em> — just like using any other tool. The LLM decides when to call it based on the docstring criteria. This means escalation logic is expressed in natural language (the docstring) rather than hardcoded if/else rules. The agent reads: “Use this ONLY when…” and applies judgment to decide if those conditions are met.</p> </blockquote> <blockquote> <p><strong>Why such a detailed docstring for <code class="language-plaintext highlighter-rouge">escalate_to_human</code>?</strong> The LLM will only call this tool when its training suggests the description matches the current situation. A vague description like “transfers to a human” gives the agent no guidance on when escalation is appropriate. The four specific conditions in the docstring make escalation <em>selective</em> and <em>purposeful</em> — the agent escalates when it genuinely should, not reflexively.</p> </blockquote> <h3 id="the-agent-configuration">The Agent Configuration</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">technical_support_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">technical_support_specialist</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">
You are a technical support specialist for an electronics company.

FIRST, check if the user has a support history in state[</span><span class="sh">"</span><span class="s">customer_info</span><span class="sh">"</span><span class="s">][</span><span class="sh">"</span><span class="s">support_history</span><span class="sh">"</span><span class="s">].
If they do, reference this history in your responses — don</span><span class="sh">'</span><span class="s">t ask them to repeat information
they</span><span class="sh">'</span><span class="s">ve already provided in past interactions.

For technical issues, follow this workflow IN ORDER:
1. Use the troubleshoot_issue tool to analyze the problem.
2. Walk the user through the troubleshooting steps clearly, one at a time.
3. If the issue persists after troubleshooting, use create_ticket to log it for follow-up.

For complex issues beyond basic troubleshooting — or if the user is frustrated,
has tried multiple fixes already, or explicitly asks for a human — use escalate_to_human
to transfer them to a specialist who can help more effectively.

Maintain a professional but empathetic tone. Acknowledge that technical problems are
frustrating. Explain what you</span><span class="sh">'</span><span class="s">re doing at each step so the user understands the process.
</span><span class="sh">"""</span><span class="p">,</span>
    <span class="n">tools</span> <span class="o">=</span> <span class="p">[</span><span class="n">troubleshoot_issue</span><span class="p">,</span> <span class="n">create_ticket</span><span class="p">,</span> <span class="n">escalate_to_human</span><span class="p">]</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>The ordered workflow in the instruction matters.</strong> “Follow this workflow IN ORDER” prevents the agent from jumping straight to escalation for routine problems (over-escalation) or from endlessly troubleshooting without ever creating a ticket (under-escalation). The explicit ordering creates a sensible default workflow while still allowing judgment for exceptional cases.</p> </blockquote> <blockquote> <p><strong>Why “if the user is frustrated… use escalate_to_human”?</strong> This sentiment-based escalation rule reflects a real support design principle: when a customer is emotionally distressed, technical competence is secondary to empathy. A human agent can provide the emotional connection that an AI cannot. This condition in the instruction teaches the agent to recognize and respond to frustration signals.</p> </blockquote> <h3 id="personalization-via-callback">Personalization via Callback</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">personalization_callback</span><span class="p">(</span>
    <span class="n">callback_context</span><span class="p">:</span> <span class="n">CallbackContext</span><span class="p">,</span>
    <span class="n">llm_request</span><span class="p">:</span> <span class="n">LlmRequest</span>
<span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Optional</span><span class="p">[</span><span class="n">LlmRequest</span><span class="p">]:</span>
    <span class="sh">"""</span><span class="s">
    Injects customer-specific context before every LLM call.

    This callback runs automatically before each LLM request.
    It reads customer data from session state and injects it as a
    system message so the agent has personalized context.

    Returns None to continue with the (modified) request.
    Returns a complete LlmRequest object to replace it entirely.
    </span><span class="sh">"""</span>
    <span class="n">customer_info</span> <span class="o">=</span> <span class="n">callback_context</span><span class="p">.</span><span class="n">state</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">customer_info</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="n">customer_info</span><span class="p">:</span>
        <span class="k">return</span> <span class="bp">None</span>  <span class="c1"># No personalization data — continue without modification
</span>
    <span class="c1"># Extract relevant fields with safe defaults
</span>    <span class="n">customer_name</span>     <span class="o">=</span> <span class="n">customer_info</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">name</span><span class="sh">"</span><span class="p">,</span>             <span class="sh">"</span><span class="s">valued customer</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">customer_tier</span>     <span class="o">=</span> <span class="n">customer_info</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">tier</span><span class="sh">"</span><span class="p">,</span>             <span class="sh">"</span><span class="s">standard</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">recent_purchases</span>  <span class="o">=</span> <span class="n">customer_info</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">recent_purchases</span><span class="sh">"</span><span class="p">,</span> <span class="p">[])</span>
    <span class="n">support_history</span>   <span class="o">=</span> <span class="n">customer_info</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">support_history</span><span class="sh">"</span><span class="p">,</span>  <span class="p">[])</span>

    <span class="c1"># Build a structured personalization note
</span>    <span class="n">personalization_note</span> <span class="o">=</span> <span class="p">(</span>
        <span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">CUSTOMER CONTEXT:</span><span class="se">\n</span><span class="sh">"</span>
        <span class="sa">f</span><span class="sh">"</span><span class="s">  Name: </span><span class="si">{</span><span class="n">customer_name</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>
        <span class="sa">f</span><span class="sh">"</span><span class="s">  Support Tier: </span><span class="si">{</span><span class="n">customer_tier</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>
    <span class="p">)</span>

    <span class="k">if</span> <span class="n">recent_purchases</span><span class="p">:</span>
        <span class="n">personalization_note</span> <span class="o">+=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">  Recent Purchases: </span><span class="si">{</span><span class="sh">'</span><span class="s">, </span><span class="sh">'</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">recent_purchases</span><span class="p">)</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>

    <span class="k">if</span> <span class="n">support_history</span><span class="p">:</span>
        <span class="n">personalization_note</span> <span class="o">+=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">  Open Issues: </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">support_history</span><span class="p">)</span><span class="si">}</span><span class="s"> past support cases</span><span class="se">\n</span><span class="sh">"</span>
        <span class="c1"># Include the most recent case for immediate context
</span>        <span class="n">personalization_note</span> <span class="o">+=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">  Most Recent Case: </span><span class="si">{</span><span class="n">support_history</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span>

    <span class="c1"># Inject as system message at the start of the conversation
</span>    <span class="n">system_content</span> <span class="o">=</span> <span class="n">types</span><span class="p">.</span><span class="nc">Content</span><span class="p">(</span>
        <span class="n">role</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">parts</span> <span class="o">=</span> <span class="p">[</span><span class="n">types</span><span class="p">.</span><span class="nc">Part</span><span class="p">(</span><span class="n">text</span><span class="o">=</span><span class="n">personalization_note</span><span class="p">)]</span>
    <span class="p">)</span>

    <span class="k">if</span> <span class="n">llm_request</span><span class="p">.</span><span class="n">contents</span><span class="p">:</span>
        <span class="n">llm_request</span><span class="p">.</span><span class="n">contents</span><span class="p">.</span><span class="nf">insert</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">system_content</span><span class="p">)</span>

    <span class="k">return</span> <span class="bp">None</span>  <span class="c1"># Signal: continue with the modified request
</span></code></pre></div></div> <blockquote> <p><strong>What is a callback?</strong> A callback is a function that the framework calls automatically at specific points in the agent’s lifecycle. The <code class="language-plaintext highlighter-rouge">personalization_callback</code> is called <em>before every LLM request</em> — this is the “before-LLM” hook. Other callback types include after-LLM (to postprocess output), before-tool-call (to validate or modify tool calls), and after-tool-call (to process tool results). Callbacks let you inject cross-cutting logic without modifying the agent’s core instructions.</p> </blockquote> <blockquote> <p><strong>Why inject personalization as a system message rather than in the instruction?</strong> The instruction is static — it’s the same for every conversation. The personalization callback is dynamic — it reads the <em>current session’s</em> customer data and injects it fresh for each request. If you put customer-specific information in the instruction, you’d need a different agent instance per customer. The callback allows one agent definition to serve all customers with personalized context.</p> </blockquote> <blockquote> <p><strong>What is <code class="language-plaintext highlighter-rouge">callback_context.state</code>?</strong> This is the session state for the current conversation — the same <code class="language-plaintext highlighter-rouge">session.state</code> dictionary from Chapter 8 (Memory). The customer’s name, tier, and purchase history were stored there when the session was initialized (likely by a separate customer authentication step). The callback reads from it to enrich every LLM call.</p> </blockquote> <blockquote> <p><strong>Why return <code class="language-plaintext highlighter-rouge">None</code>?</strong> The callback return value controls flow: <code class="language-plaintext highlighter-rouge">None</code> means “continue with the (possibly modified) request.” Returning a complete <code class="language-plaintext highlighter-rouge">LlmRequest</code> object would <em>replace</em> the original request entirely. In most personalization cases, <code class="language-plaintext highlighter-rouge">None</code> is correct — you want to add context to the existing request, not replace it.</p> </blockquote> <hr/> <h2 id="the-human-on-the-loop-variation">The Human-on-the-Loop Variation</h2> <p>HITL requires a human to be present and responsive for every escalated case. This doesn’t scale to millisecond-latency systems. The <strong>Human-on-the-Loop (HOTL)</strong> pattern addresses this by letting humans operate at the <em>policy level</em> rather than the <em>decision level</em>.</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">HUMAN-ON-THE-LOOP (HOTL) — policy-driven, AI-executed</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-purple" style="max-width:340px;"> <div class="ns-node-title">Human Expert Defines Policy</div> <div class="ns-node-sub">Investment rules, routing criteria, response templates, escalation thresholds. Happens infrequently — weekly or monthly. No real-time involvement needed.</div> </div> <div class="ns-arrow"></div> <div class="ns-phase" style="max-width:440px;"> <div class="ns-phase-title">AI Execution Loop — runs continuously, at scale</div> <div class="ns-phase-sub">Executes within policy boundaries without per-case human approval</div> <div class="ns-node ns-node-cyan" style="max-width:none;"><div class="ns-node-title">Monitor + Classify Incoming</div><div class="ns-node-sub">Event, transaction, or request arrives</div></div> <div class="ns-arrow"></div> <div class="ns-decision" style="max-width:none;"><div class="ns-node-title">Within Policy Boundaries?</div></div> <div class="ns-arrow"></div> <div class="ns-row" style="max-width:none;"> <div class="ns-node ns-node-green"><div class="ns-node-title">Execute Autonomously</div><div class="ns-node-sub">Act immediately within policy. Log for audit.</div></div> <div class="ns-node ns-node-amber"><div class="ns-node-title">Flag + Queue</div><div class="ns-node-sub">Outside policy → queue for human review (not urgent)</div></div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:340px;"> <div class="ns-node-title">Human Reviews Aggregates + Exceptions</div> <div class="ns-node-sub">Reviews daily summaries, exception reports, and flagged edge cases. Updates policy based on patterns. Provides feedback on AI decisions.</div> </div> </div> </div> <p><strong>Two concrete examples of HOTL:</strong></p> <p><strong>Automated trading:</strong> A fund manager writes: “Maintain 70% tech stocks, 30% bonds. No more than 5% in any single company. Auto-sell any stock that drops 10% below purchase price.” The AI executes hundreds of trades per day within these constraints, at speeds no human could match. The manager reviews end-of-day summaries and adjusts strategy monthly.</p> <p><strong>Call center routing:</strong> A manager sets rules: “Route ‘service outage’ mentions to technical specialists. If tone analysis detects high frustration, offer immediate human transfer. Upsell opportunities only to Gold-tier customers.” The AI handles initial interaction with all customers simultaneously, applying these rules in real time. The manager reviews daily reports, adjusts routing rules weekly based on data.</p> <hr/> <h2 id="eight-practical-applications">Eight Practical Applications</h2> <div class="hitl-usecases-grid"> <div class="hitl-uc-card"> <span class="hitl-uc-num">01</span> <h4>Content Moderation</h4> <p>AI handles clear-cut violations (obvious spam, known illegal content) automatically. Borderline cases — satire vs. hate speech, context-dependent content — escalate to human moderators for nuanced judgment.</p> <span class="hitl-uc-domain">Trust &amp; Safety · Social platforms</span> </div> <div class="hitl-uc-card"> <span class="hitl-uc-num">02</span> <h4>Financial Fraud Detection</h4> <p>AI flags suspicious transactions by pattern. High-risk or ambiguous alerts go to human analysts who investigate further, contact customers, and make the final determination — preserving accountability.</p> <span class="hitl-uc-domain">FinTech · Banking · Insurance</span> </div> <div class="hitl-uc-card"> <span class="hitl-uc-num">03</span> <h4>Medical Diagnosis Support</h4> <p>AI analyzes imaging, lab results, and patient history to surface possible diagnoses with confidence scores. Physicians review the AI analysis and make the clinical decision — informed, not replaced.</p> <span class="hitl-uc-domain">Healthcare · Radiology · Pathology</span> </div> <div class="hitl-uc-card"> <span class="hitl-uc-num">04</span> <h4>Autonomous Driving</h4> <p>Self-driving systems handle normal driving conditions autonomously. Extreme weather, unusual road conditions, and ambiguous edge cases trigger handover to human driver — system knows its limits.</p> <span class="hitl-uc-domain">Transportation · AV systems</span> </div> <div class="hitl-uc-card"> <span class="hitl-uc-num">05</span> <h4>Customer Support Escalation</h4> <p>Chatbot handles routine queries. Complex issues, emotional distress, or explicit human requests trigger seamless handoff to a human support agent — with full conversation context transferred automatically.</p> <span class="hitl-uc-domain">Customer Success · E-commerce · SaaS</span> </div> <div class="hitl-uc-card"> <span class="hitl-uc-num">06</span> <h4>Legal Document Review</h4> <p>AI scans thousands of documents to identify relevant clauses, flag inconsistencies, and surface key passages. Legal professionals review AI findings for accuracy, context, and legal implications.</p> <span class="hitl-uc-domain">Legal · Compliance · M&amp;A</span> </div> <div class="hitl-uc-card"> <span class="hitl-uc-num">07</span> <h4>Data Labeling &amp; Annotation</h4> <p>Human annotators provide ground truth labels for training data. AI pre-labels obvious cases; humans review and correct. The human feedback directly shapes the model's future behavior.</p> <span class="hitl-uc-domain">ML training · Computer vision · NLP</span> </div> <div class="hitl-uc-card"> <span class="hitl-uc-num">08</span> <h4>Generative AI Refinement</h4> <p>AI generates creative content (marketing copy, design concepts, code). Human editors and designers review, refine, and approve final output — ensuring brand alignment, quality, and appropriateness.</p> <span class="hitl-uc-domain">Marketing · Creative · Engineering</span> </div> </div> <style>.hitl-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:.85rem;margin:1.5rem 0}.hitl-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.hitl-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#2698ba}.hitl-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.hitl-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.hitl-uc-domain{font-size:.65rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="the-caveats-what-hitl-cannot-solve">The Caveats: What HITL Cannot Solve</h2> <p>HITL is powerful, but treating it as a universal solution ignores serious structural limitations that must be addressed honestly.</p> <p><strong>Caveat 1: Fundamental scalability ceiling.</strong> A human reviewer can process perhaps 100-500 decisions per day with careful attention. An AI system can process millions. If 2% of all decisions require human review and you’re handling a million cases per day, you need 20,000 human reviews daily — impossible at most organizations. HITL works when the escalation rate is low enough that humans can genuinely engage with each case rather than rubber-stamping them to clear the queue. If reviewers are overwhelmed, HITL’s quality guarantee collapses.</p> <p><strong>Caveat 2: Reviewer expertise is not guaranteed.</strong> HITL produces quality outcomes only when human reviewers are genuinely more qualified than the AI for the escalated cases. A non-specialist approving medical diagnoses, a junior analyst making fraud determinations, or an untrained moderator ruling on complex speech cases can produce outcomes worse than fully autonomous AI. The expertise required for meaningful oversight must be explicitly resourced.</p> <p><strong>Caveat 3: Annotation quality is not automatic.</strong> When HITL is used to generate training data (humans label outputs to train the model), the quality of that training data depends entirely on the quality of the human annotation. Annotators may have inconsistent standards, implicit biases, or misunderstand the task. Training data is only as good as the annotation guidelines and the annotator training — both require significant investment.</p> <p><strong>Caveat 4: Privacy constraints complicate oversight.</strong> For human reviewers to exercise meaningful judgment, they need full context — which may include sensitive personal information. Medical records, financial history, private communications. Before a case can reach a human reviewer, sensitive data must often be anonymized or redacted — which may remove exactly the context needed to make a good decision. Privacy and effective oversight are in tension, and there is no easy solution.</p> <p><strong>Caveat 5: Reviewer bias affects model learning.</strong> Human reviewers have biases — cultural, demographic, professional. When their decisions become training data, those biases are learned by the model. HITL doesn’t eliminate bias; it changes whose bias shapes the system. Diverse reviewer panels, explicit bias auditing, and disagreement resolution protocols help — but the fundamental challenge remains.</p> <hr/> <h2 id="common-mistakes-in-hitl-design">Common Mistakes in HITL Design</h2> <p><strong>Mistake 1: Escalating too much.</strong> If 40% of cases are escalated, reviewers face burnout, decisions become rushed, and the human oversight becomes rubber-stamping rather than genuine review. Calibrate escalation thresholds to match reviewer capacity — if you have 100 reviewer-hours per day, your escalation rate should produce fewer than 100 meaningful cases to review.</p> <p><strong>Mistake 2: Escalating too little.</strong> If borderline cases that should go to human review are processed autonomously, the value of HITL is lost. The edge cases are exactly where human judgment matters most. Monitor the cases that were handled autonomously and then had poor outcomes — these might have been better escalated.</p> <p><strong>Mistake 3: Not closing the feedback loop.</strong> Every escalated human decision is valuable training data. If you don’t capture it and use it to retrain or re-calibrate the agent, you’re throwing away the most valuable signal in your system. Every human correction should update either the model, the escalation threshold, or the agent’s instructions.</p> <p><strong>Mistake 4: Poor information presentation to reviewers.</strong> The human reviewer sees only what the system presents. If the escalation interface shows only the raw AI output without the reasoning, context, and specific escalation reason, the reviewer can’t make an informed decision — they’re guessing. Invest in the reviewer interface as much as the agent logic.</p> <p><strong>Mistake 5: No audit trail.</strong> In high-stakes domains (medical, legal, financial), every decision must be traceable: who made it, when, based on what information, with what justification. If a human overrides an AI recommendation, that override and its rationale must be logged. Audit trails are not optional features — they’re core infrastructure for accountable AI.</p> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="hitl-summary-card"> <div class="hitl-summary-col"> <div class="hitl-summary-label">WHAT</div> <p>A pattern that deliberately integrates human judgment into AI workflows at high-stakes decision points — through escalation, review, feedback, and oversight — rather than operating fully autonomously or requiring humans to do everything manually.</p> </div> <div class="hitl-summary-divider"></div> <div class="hitl-summary-col"> <div class="hitl-summary-label">WHY</div> <p>In domains with high error costs (medical, legal, financial, safety-critical), technical AI capability is insufficient — accountability, ethics, and regulatory requirements demand human judgment and human responsibility for consequential decisions.</p> </div> <div class="hitl-summary-divider"></div> <div class="hitl-summary-col"> <div class="hitl-summary-label">RULE OF THUMB</div> <p>Use HITL when errors have significant safety, ethical, or financial consequences. Use Human-on-the-Loop when volume requires full automation but policy oversight is sufficient. Always design the feedback loop — human decisions are your best training data.</p> </div> </div> <style>.hitl-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.hitl-summary-card{flex-direction:column}}.hitl-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.hitl-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.hitl-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.hitl-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li> <p><strong>HITL is not about AI capability — it’s about accountability.</strong> Even if an AI can make a correct decision 99% of the time, certain decisions require a human to be accountable for them. Loan rejections, medical treatments, prison sentences — the person affected has a right to know a human was responsible.</p> </li> <li> <p><strong>The spectrum from full automation to full human control is continuous.</strong> Most systems should live somewhere in the middle, with the dial position determined by risk level, volume, expertise availability, and regulatory requirements — not by ideology.</p> </li> <li> <p><strong>Escalation triggers must be explicit and calibrated.</strong> Vague escalation logic (“escalate when unsure”) produces inconsistent results. Define specific thresholds: confidence scores, dollar amounts, category rules, iteration limits. Calibrate escalation rates to match reviewer capacity.</p> </li> <li> <p><strong>Escalation is a tool the agent calls.</strong> In ADK, <code class="language-plaintext highlighter-rouge">escalate_to_human</code> is a function with a docstring that tells the LLM <em>when</em> to call it. This means escalation logic is expressed in the same natural language interface as tool use — making it adjustable without code changes.</p> </li> <li> <p><strong>The personalization callback injects context before every LLM call.</strong> This is how the agent knows customer history, tier, and past cases without those details being hardcoded in every conversation. Callbacks are hooks that let you inject cross-cutting logic without modifying the agent’s core definition.</p> </li> <li> <p><strong>Human-on-the-Loop scales better than Human-in-the-Loop.</strong> HOTL lets humans set policy once and review aggregates periodically, rather than approving every individual decision. Use HOTL when decisions must happen at machine speed but human policy guidance is essential.</p> </li> <li> <p><strong>Every human decision is a training signal — capture it.</strong> The cases escalated to human review are the hardest cases, and the human’s decision is the highest-quality label you can get. If you’re not feeding these back into the model, you’re leaving the most valuable improvement signal unused.</p> </li> <li> <p><strong>HITL primary risk: scalability.</strong> A poorly calibrated HITL system where 30% of cases escalate creates overwhelmed reviewers who rubber-stamp decisions rather than genuinely evaluate them — worse than no oversight at all. The escalation rate must be manageable.</p> </li> </ul> <hr/> <p><em>This concludes the core agentic AI patterns series. From prompt chaining (Chapter 1) to human-in-the-loop (Chapter 13), you now have the complete toolkit for designing, building, and deploying intelligent AI agent systems — with the depth to understand not just how each pattern works, but why it exists and when to use it.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="human-in-the-loop"/><category term="hitl"/><category term="safety"/><summary type="html"><![CDATA[Full autonomy sounds ideal — but in high-stakes domains, the cost of a single AI error is too high. Human-in-the-Loop is the pattern that keeps humans in control of the decisions that matter most.]]></summary></entry><entry><title type="html">Chapter 12: Exception Handling and Recovery</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/exception-handling/" rel="alternate" type="text/html" title="Chapter 12: Exception Handling and Recovery"/><published>2026-02-18T00:00:00+00:00</published><updated>2026-02-18T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/exception-handling</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/exception-handling/"><![CDATA[<h2 id="why-agents-break-in-the-real-world">Why Agents Break in the Real World</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Exception:</strong> An unexpected event that disrupts the normal flow of a program. In Python, exceptions are objects that represent errors — <code>ConnectionError</code> means the network failed, <code>ValueError</code> means the data had the wrong format, <code>TimeoutError</code> means a request took too long. When an exception occurs and isn't handled, the program crashes.</p> <p style="margin-top:0.5rem"><strong>try/except (Python's error handling):</strong> A code structure that lets you attempt an operation and gracefully handle any errors it raises. Inside <code>try</code> you write the risky code. Inside <code>except</code> you write what to do if it fails. The program doesn't crash — it follows your fallback path instead.</p> <p style="margin-top:0.5rem"><strong>Exponential backoff:</strong> A retry strategy where each retry waits longer than the previous one. Wait 1 second, then 2, then 4, then 8. This prevents "thundering herd" — where thousands of clients all retry simultaneously, overwhelming an already-struggling server.</p> <p style="margin-top:0.5rem"><strong>Graceful degradation:</strong> Providing reduced functionality instead of complete failure. A chatbot that can't access the customer database might still answer general questions — reduced capability, but still useful. Better than refusing to respond at all.</p> <p style="margin-top:0.5rem"><strong>Idempotency:</strong> A property of operations where calling them multiple times has the same effect as calling them once. GET requests are idempotent (reading data doesn't change it). POST requests often aren't (calling "send_email" twice sends two emails). Important for retry logic — only retry idempotent operations automatically.</p> <p style="margin-top:0.5rem"><strong>Circuit breaker:</strong> A pattern that automatically stops calling a failing service after a threshold of failures. Like an electrical circuit breaker that trips when it detects an overload. Prevents cascading failures where one broken service causes the entire system to hang waiting for responses that will never come.</p> <p style="margin-top:0.5rem"><strong>Fallback:</strong> An alternative approach that activates when the primary approach fails. If the precise GPS lookup fails, fall back to city-level location data. If the payment processor is down, fall back to a backup processor. Fallbacks ensure some functionality is preserved even when components fail.</p> </div> <p>Every pattern in this series has operated in idealized conditions: tools work, APIs respond, data arrives in the expected format. But deploy an agent in production and reality is messier:</p> <ul> <li>The weather API returns a <code class="language-plaintext highlighter-rouge">503 Service Unavailable</code> during peak traffic</li> <li>The database query times out because another process is holding a lock</li> <li>The LLM returns JSON with a missing field your code expects</li> <li>The email service rejects the request because the recipient’s inbox is full</li> <li>A third-party service changes its response format without notice</li> <li>The user provides malformed input that breaks a tool’s parameter validation</li> <li>A network packet drops mid-request, leaving the connection in an ambiguous state</li> </ul> <p>None of these are bugs in your agent’s logic. They’re the normal chaos of distributed systems. And a well-designed agent handles all of them — not by pretending they won’t happen, but by anticipating them and planning responses.</p> <p>The <strong>Exception Handling and Recovery</strong> pattern is about building agents that are <strong>resilient</strong> — capable of detecting failures, responding to them appropriately, and restoring operation — rather than <strong>fragile</strong> agents that crash on first contact with an unexpected input.</p> <p>This distinction matters enormously for production deployment. An agent that handles failures gracefully is trustworthy. An agent that crashes unpredictably is a liability, regardless of how intelligent its core reasoning is.</p> <hr/> <h2 id="the-three-phases-of-exception-management">The Three Phases of Exception Management</h2> <p>Exception handling in agents follows a clear three-phase structure:</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">EXCEPTION HANDLING AND RECOVERY — three-phase structure</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:320px;"> <div class="ns-node-title">Agent Attempts Action</div> <div class="ns-node-sub">Tool call, API request, database query, LLM invocation — any operation that interacts with an external system can fail.</div> </div> <div class="ns-arrow"></div> <div class="ns-decision" style="max-width:220px;"> <div class="ns-node-title">Success?</div> </div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:540px;"> <div class="ns-branch"> <span class="ns-label-green">Yes</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Continue Normal Flow</div></div> </div> <div class="ns-branch"> <span class="ns-label-red">No — exception raised</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-phase" style="gap:0.4rem;"> <div class="ns-phase-title">PHASE 1 — Error Detection</div> <div class="ns-node ns-node-red" style="max-width:none;"> <div class="ns-node-title">Identify &amp; Classify the Error</div> <div class="ns-node-sub">What type? HTTP error, timeout, validation error, auth failure, data format error? Severity? Transient or permanent?</div> </div> <div class="ns-node ns-node-amber" style="max-width:none;"> <div class="ns-node-title">PHASE 2 — Error Handling</div> <div class="ns-node-sub">Log → Retry (if transient) → Fallback (if retry exhausted) → Degrade gracefully → Notify</div> </div> <div class="ns-node ns-node-purple" style="max-width:none;"> <div class="ns-node-title">PHASE 3 — Recovery</div> <div class="ns-node-sub">State rollback → Diagnosis → Self-correction / Replan → Escalate to human if unresolvable</div> </div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:280px;"> <div class="ns-node-title">Stable Operation Restored</div> <div class="ns-node-sub">Either with full functionality, reduced functionality, or having escalated to human — but always in a defined, controlled state.</div> </div> </div> </div> <hr/> <h2 id="phase-1-error-detection">Phase 1: Error Detection</h2> <p>You can’t handle what you don’t know about. Error detection is the surveillance layer — continuously monitoring for signals that indicate something has gone wrong.</p> <h3 id="types-of-errors-agents-encounter">Types of Errors Agents Encounter</h3> <div class="exc-types-wrapper"> <div class="exc-type-grid"> <div class="exc-type-card exc-type-network"> <div class="exc-type-icon">🌐</div> <div class="exc-type-name">Network Errors</div> <div class="exc-type-desc">Connection refused, DNS resolution failure, SSL certificate expired, request timeout. The external service is unreachable — not necessarily broken, just unavailable right now.</div> <div class="exc-type-codes"><code>ConnectionError</code> · <code>TimeoutError</code> · <code>SSLError</code></div> <div class="exc-type-strategy">Usually transient → retry with backoff</div> </div> <div class="exc-type-card exc-type-api"> <div class="exc-type-icon">⚡</div> <div class="exc-type-name">API / HTTP Errors</div> <div class="exc-type-desc">4xx errors (client mistakes: 400 Bad Request, 401 Unauthorized, 404 Not Found, 429 Rate Limited). 5xx errors (server problems: 500 Internal Error, 503 Unavailable). Each requires different handling.</div> <div class="exc-type-codes"><code>HTTPError</code> · status codes 4xx/5xx</div> <div class="exc-type-strategy">429/5xx → retry · 4xx → fix request or abort</div> </div> <div class="exc-type-card exc-type-data"> <div class="exc-type-icon">📋</div> <div class="exc-type-name">Data / Format Errors</div> <div class="exc-type-desc">The API returned valid JSON but with different field names. The LLM output wasn't parseable as JSON. A required field is null. The date format changed. These require validation and format handling.</div> <div class="exc-type-codes"><code>JSONDecodeError</code> · <code>KeyError</code> · <code>ValueError</code></div> <div class="exc-type-strategy">Validate → parse defensively → fallback schema</div> </div> <div class="exc-type-card exc-type-llm"> <div class="exc-type-icon">🤖</div> <div class="exc-type-name">LLM Output Errors</div> <div class="exc-type-desc">The LLM was asked for JSON but returned prose. The LLM generated a tool call with wrong parameter types. The output is logically incoherent. These are probabilistic failures — even with temperature=0, they occasionally occur.</div> <div class="exc-type-codes">Structural validation failures · Schema mismatch</div> <div class="exc-type-strategy">Validate output → retry with clarified prompt</div> </div> <div class="exc-type-card exc-type-logic"> <div class="exc-type-icon">⚠️</div> <div class="exc-type-name">Logic / Semantic Errors</div> <div class="exc-type-desc">The agent took an action that is technically valid but semantically wrong — deleting the wrong record, sending to the wrong recipient, reading the wrong file. These are the hardest to detect because no exception is raised.</div> <div class="exc-type-codes">No Python exception — requires semantic validation</div> <div class="exc-type-strategy">Confirmation steps · human review for irreversible actions</div> </div> <div class="exc-type-card exc-type-resource"> <div class="exc-type-icon">💾</div> <div class="exc-type-name">Resource Errors</div> <div class="exc-type-desc">Context window exceeded, rate limit hit, token budget exhausted, memory full, disk full. These are quota and capacity constraints, not bugs — but they require the same structured handling.</div> <div class="exc-type-codes"><code>ContextLengthError</code> · <code>RateLimitError</code></div> <div class="exc-type-strategy">Summarize context → reduce payload → queue request</div> </div> </div> </div> <style>.exc-type-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:.85rem;margin:1.5rem 0}.exc-type-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;display:flex;flex-direction:column;gap:.4rem;background:rgba(128,128,128,0.04)}.exc-type-network{border-left:3px solid #2698ba}.exc-type-api{border-left:3px solid #e6a817}.exc-type-data{border-left:3px solid #c97af2}.exc-type-llm{border-left:3px solid #4fc97e}.exc-type-logic{border-left:3px solid #ff6b6b}.exc-type-resource{border-left:3px solid #7dcfff}.exc-type-icon{font-size:1.1rem}.exc-type-name{font-size:.85rem;font-weight:700;color:var(--global-text-color)}.exc-type-desc{font-size:.78rem;color:var(--global-text-color-light);line-height:1.5}.exc-type-codes{font-size:.68rem;font-family:monospace;color:#7dcfff}.exc-type-strategy{font-size:.68rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.3rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="phase-2-error-handling-strategies">Phase 2: Error Handling Strategies</h2> <p>Once an error is detected, you need a playbook — a structured set of responses based on the error type and severity. The five strategies below cover the full range from “this will resolve itself” to “this needs a human.”</p> <h3 id="strategy-1-retry-with-exponential-backoff">Strategy 1: Retry with Exponential Backoff</h3> <p>The most common strategy for transient errors. The operation failed temporarily — wait a moment and try again.</p> <p><strong>Why exponential backoff?</strong> If your request failed because the server is overloaded and 10,000 other clients all retry at exactly the same time (1 second later), you’ve just made the overload worse. Exponential backoff spreads retries out: different clients wait different amounts before retrying, reducing the “retry storm” problem.</p> <div class="exc-backoff-wrapper"> <div class="exc-backoff-header"> <span class="exc-backoff-title">EXPONENTIAL BACKOFF VISUALIZATION</span> <button class="exc-backoff-btn" id="backoffRunBtn">▶ Simulate Retry</button> </div> <div class="exc-backoff-body"> <div class="exc-backoff-timeline" id="backoffTimeline"></div> <div class="exc-backoff-result" id="backoffResult" style="display:none"></div> </div> </div> <style>.exc-backoff-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}.exc-backoff-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.exc-backoff-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.exc-backoff-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer}.exc-backoff-btn:hover{background:rgba(38,152,186,0.12);border-color:#2698ba;color:#2698ba}.exc-backoff-body{padding:1.1rem;display:flex;flex-direction:column;gap:.5rem}.exc-backoff-timeline{display:flex;flex-direction:column;gap:.4rem}.exc-attempt{display:flex;align-items:center;gap:.75rem;padding:.5rem .75rem;border-radius:6px;border:1px solid var(--global-divider-color);background:rgba(128,128,128,0.03);animation:bofIn .3s ease;font-size:.8rem}@keyframes bofIn{from{opacity:0;transform:translateX(-6px)}to{opacity:1;transform:none}}.exc-attempt.fail{border-color:rgba(255,107,107,0.3);background:rgba(255,107,107,0.05)}.exc-attempt.success{border-color:rgba(79,201,126,0.3);background:rgba(79,201,126,0.07)}.exc-attempt-num{font-family:monospace;font-size:.65rem;font-weight:700;min-width:70px;flex-shrink:0;color:var(--global-text-color-light)}.exc-attempt-bar{height:6px;border-radius:3px;flex-shrink:0}.exc-attempt-wait{font-size:.72rem;font-family:monospace;color:var(--global-text-color-light);flex-shrink:0}.exc-attempt-status{font-size:.72rem;font-family:monospace;flex-shrink:0}.exc-attempt.fail .exc-attempt-status{color:#ff6b6b}.exc-attempt.success .exc-attempt-status{color:#4fc97e}.exc-backoff-result{padding:.55rem .85rem;border-radius:6px;font-size:.8rem;font-family:monospace;text-align:center}.exc-backoff-result.success{background:rgba(79,201,126,0.08);color:#4fc97e;border:1px solid rgba(79,201,126,0.2)}.exc-backoff-result.fail{background:rgba(255,107,107,0.08);color:#ff6b6b;border:1px solid rgba(255,107,107,0.2)}</style> <script>
document.addEventListener('DOMContentLoaded', function(){
  var btn = document.getElementById('backoffRunBtn');
  if (!btn) return;
  var running = false;

  // Simulate: attempts 1-3 fail (503), attempt 4 succeeds
  var ATTEMPTS = [
    { wait: '0s (immediate)', status: '503 Service Unavailable', fail: true,  barW: 20 },
    { wait: '1s wait',        status: '503 Service Unavailable', fail: true,  barW: 40 },
    { wait: '2s wait',        status: '503 Service Unavailable', fail: true,  barW: 70 },
    { wait: '4s wait',        status: '200 OK — Success!',       fail: false, barW: 100 },
  ];

  btn.addEventListener('click', async function(){
    if (running) return;
    running = true;
    btn.textContent = '⏳ Running…';
    btn.disabled = true;

    var timeline = document.getElementById('backoffTimeline');
    var result   = document.getElementById('backoffResult');
    timeline.innerHTML = '';
    result.style.display = 'none';

    for (var i = 0; i < ATTEMPTS.length; i++) {
      await new Promise(function(r){ setTimeout(r, 700); });
      var a = ATTEMPTS[i];
      var div = document.createElement('div');
      div.className = 'exc-attempt ' + (a.fail ? 'fail' : 'success');
      div.innerHTML =
        '<span class="exc-attempt-num">Attempt ' + (i+1) + '</span>' +
        '<div class="exc-attempt-bar" style="width:' + a.barW + 'px;background:' + (a.fail ? '#ff6b6b' : '#4fc97e') + ';"></div>' +
        '<span class="exc-attempt-wait">' + a.wait + '</span>' +
        '<span class="exc-attempt-status">' + a.status + '</span>';
      timeline.appendChild(div);
    }

    await new Promise(function(r){ setTimeout(r, 400); });
    result.className = 'exc-backoff-result success';
    result.textContent = 'Succeeded on attempt 4 after 7s total wait (0 + 1 + 2 + 4 = 7s). Exponential backoff formula: wait = base_delay × (2^attempt)';
    result.style.display = 'block';

    running = false;
    btn.textContent = '↺ Replay';
    btn.disabled = false;
  });
});
</script> <p>Here’s how to implement retry with exponential backoff in Python:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">time</span>
<span class="kn">import</span> <span class="n">random</span>

<span class="k">def</span> <span class="nf">retry_with_backoff</span><span class="p">(</span><span class="n">func</span><span class="p">,</span> <span class="n">max_retries</span><span class="o">=</span><span class="mi">4</span><span class="p">,</span> <span class="n">base_delay</span><span class="o">=</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">max_delay</span><span class="o">=</span><span class="mf">60.0</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Execute `func` with exponential backoff on failure.

    Args:
        func: The callable to retry. Should raise an exception on failure.
        max_retries: Maximum number of retry attempts (not counting first try).
        base_delay: Initial wait time in seconds.
        max_delay: Maximum wait time cap in seconds.
    Returns:
        The result of func() if it eventually succeeds.
    Raises:
        The last exception raised by func if all retries are exhausted.
    </span><span class="sh">"""</span>
    <span class="n">last_exception</span> <span class="o">=</span> <span class="bp">None</span>

    <span class="k">for</span> <span class="n">attempt</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_retries</span> <span class="o">+</span> <span class="mi">1</span><span class="p">):</span>  <span class="c1"># +1 for the initial try
</span>        <span class="k">try</span><span class="p">:</span>
            <span class="k">return</span> <span class="nf">func</span><span class="p">()</span>  <span class="c1"># Try the operation
</span>
        <span class="nf">except </span><span class="p">(</span><span class="nb">ConnectionError</span><span class="p">,</span> <span class="nb">TimeoutError</span><span class="p">)</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
            <span class="c1"># These are transient — worth retrying
</span>            <span class="n">last_exception</span> <span class="o">=</span> <span class="n">e</span>
            <span class="k">if</span> <span class="n">attempt</span> <span class="o">==</span> <span class="n">max_retries</span><span class="p">:</span>
                <span class="k">break</span>  <span class="c1"># No more retries — fall through to raise
</span>
            <span class="c1"># Exponential backoff with jitter
</span>            <span class="c1"># jitter = random noise to prevent synchronized retries from multiple clients
</span>            <span class="n">wait_time</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">base_delay</span> <span class="o">*</span> <span class="p">(</span><span class="mi">2</span> <span class="o">**</span> <span class="n">attempt</span><span class="p">),</span> <span class="n">max_delay</span><span class="p">)</span>
            <span class="n">jitter</span> <span class="o">=</span> <span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">wait_time</span> <span class="o">*</span> <span class="mf">0.1</span><span class="p">)</span>  <span class="c1"># ±10% randomness
</span>            <span class="n">actual_wait</span> <span class="o">=</span> <span class="n">wait_time</span> <span class="o">+</span> <span class="n">jitter</span>

            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Attempt </span><span class="si">{</span><span class="n">attempt</span> <span class="o">+</span> <span class="mi">1</span><span class="si">}</span><span class="s"> failed: </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="s">. Waiting </span><span class="si">{</span><span class="n">actual_wait</span><span class="si">:</span><span class="p">.</span><span class="mi">1</span><span class="n">f</span><span class="si">}</span><span class="s">s before retry...</span><span class="sh">"</span><span class="p">)</span>
            <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="n">actual_wait</span><span class="p">)</span>

        <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
            <span class="c1"># Non-transient errors (auth failures, invalid requests) — don't retry
</span>            <span class="k">raise</span>  <span class="c1"># Re-raise immediately, no retry
</span>
    <span class="k">raise</span> <span class="n">last_exception</span>  <span class="c1"># All retries exhausted — propagate final exception
</span></code></pre></div></div> <blockquote> <p><strong>Why separate <code class="language-plaintext highlighter-rouge">ConnectionError/TimeoutError</code> from the general <code class="language-plaintext highlighter-rouge">Exception</code>?</strong> Retrying makes sense for transient failures — the server is temporarily busy, the network had a hiccup. It makes no sense for permanent failures — a <code class="language-plaintext highlighter-rouge">401 Unauthorized</code> error won’t become authorized just because you waited and tried again. Separate exception types for different retry policies prevents wasting retries on errors that will never resolve themselves.</p> </blockquote> <blockquote> <p><strong>What is “jitter”?</strong> Without jitter, if 1,000 clients all hit a rate limit at the same moment and all back off for exactly 1 second, they all retry at exactly the same moment — still overwhelming the server. Adding random noise (±10% of the wait time) spreads their retries across a window, significantly reducing the “synchronized retry storm” problem.</p> </blockquote> <h3 id="strategy-2-fallback-mechanisms">Strategy 2: Fallback Mechanisms</h3> <p>When retries are exhausted, activate an alternative approach. Fallbacks are pre-planned alternatives that provide some value when the primary approach is unavailable.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_location_info</span><span class="p">(</span><span class="n">address</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Attempt precise location lookup, fall back to city-level if unavailable.
    </span><span class="sh">"""</span>
    <span class="c1"># Primary: precise geocoding API
</span>    <span class="k">try</span><span class="p">:</span>
        <span class="n">result</span> <span class="o">=</span> <span class="n">precise_location_api</span><span class="p">.</span><span class="nf">lookup</span><span class="p">(</span><span class="n">address</span><span class="p">)</span>
        <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">precision</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">precise</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">data</span><span class="sh">"</span><span class="p">:</span> <span class="n">result</span><span class="p">}</span>

    <span class="nf">except </span><span class="p">(</span><span class="n">APIError</span><span class="p">,</span> <span class="nb">TimeoutError</span><span class="p">)</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Precise lookup failed: </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="s">. Activating fallback...</span><span class="sh">"</span><span class="p">)</span>

        <span class="c1"># Fallback 1: city-level lookup from a different provider
</span>        <span class="k">try</span><span class="p">:</span>
            <span class="n">city</span> <span class="o">=</span> <span class="nf">extract_city_from_address</span><span class="p">(</span><span class="n">address</span><span class="p">)</span>
            <span class="n">result</span> <span class="o">=</span> <span class="n">city_lookup_api</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">city</span><span class="p">)</span>
            <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">precision</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">city-level</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">data</span><span class="sh">"</span><span class="p">:</span> <span class="n">result</span><span class="p">,</span> <span class="sh">"</span><span class="s">degraded</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span><span class="p">}</span>

        <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">fallback_error</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">City-level fallback also failed: </span><span class="si">{</span><span class="n">fallback_error</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

            <span class="c1"># Fallback 2: cached/static data
</span>            <span class="n">cached</span> <span class="o">=</span> <span class="n">location_cache</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">address</span><span class="p">)</span>
            <span class="k">if</span> <span class="n">cached</span><span class="p">:</span>
                <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">precision</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">cached</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">data</span><span class="sh">"</span><span class="p">:</span> <span class="n">cached</span><span class="p">,</span> <span class="sh">"</span><span class="s">degraded</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span><span class="p">,</span> <span class="sh">"</span><span class="s">stale</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span><span class="p">}</span>

            <span class="c1"># No fallback available
</span>            <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">precision</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">none</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">error</span><span class="sh">"</span><span class="p">:</span> <span class="nf">str</span><span class="p">(</span><span class="n">e</span><span class="p">),</span> <span class="sh">"</span><span class="s">degraded</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span><span class="p">}</span>
</code></pre></div></div> <blockquote> <p><strong>The fallback hierarchy.</strong> Good fallback design has multiple levels: (1) primary approach, (2) alternative service with full data, (3) service with reduced data, (4) cached data, (5) graceful failure message. Each level provides less value but more availability. The agent always has a path forward.</p> </blockquote> <blockquote> <p><strong>Mark degraded results clearly.</strong> The <code class="language-plaintext highlighter-rouge">"degraded": True</code> flag in the return value tells downstream code and the LLM that this result is less reliable than normal. The LLM can then communicate appropriately to the user: “I found city-level location data — detailed address lookup was temporarily unavailable.”</p> </blockquote> <h3 id="strategy-3-graceful-degradation">Strategy 3: Graceful Degradation</h3> <p>Provide partial functionality rather than complete failure. When one component fails, other components continue working.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">process_user_request</span><span class="p">(</span><span class="n">user_query</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">user_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Process request with graceful degradation of individual components.
    </span><span class="sh">"""</span>
    <span class="n">response</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">query</span><span class="sh">"</span><span class="p">:</span> <span class="n">user_query</span><span class="p">,</span> <span class="sh">"</span><span class="s">components_used</span><span class="sh">"</span><span class="p">:</span> <span class="p">[]}</span>

    <span class="c1"># Attempt 1: Personalization (nice-to-have, not critical)
</span>    <span class="k">try</span><span class="p">:</span>
        <span class="n">preferences</span> <span class="o">=</span> <span class="n">user_preference_db</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">user_id</span><span class="p">)</span>
        <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">personalization</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">preferences</span>
        <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">components_used</span><span class="sh">"</span><span class="p">].</span><span class="nf">append</span><span class="p">(</span><span class="sh">"</span><span class="s">personalization</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
        <span class="c1"># Personalization failed — continue without it
</span>        <span class="nf">log_warning</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Personalization unavailable for </span><span class="si">{</span><span class="n">user_id</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">personalization</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="bp">None</span>  <span class="c1"># Use defaults
</span>
    <span class="c1"># Attempt 2: Real-time data (important, but not blocking)
</span>    <span class="k">try</span><span class="p">:</span>
        <span class="n">live_data</span> <span class="o">=</span> <span class="n">market_data_api</span><span class="p">.</span><span class="nf">get_latest</span><span class="p">()</span>
        <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">market_data</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">live_data</span>
        <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">components_used</span><span class="sh">"</span><span class="p">].</span><span class="nf">append</span><span class="p">(</span><span class="sh">"</span><span class="s">real_time_data</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
        <span class="c1"># Fall back to cached data from last successful fetch
</span>        <span class="n">cached</span> <span class="o">=</span> <span class="n">cache</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">market_data</span><span class="sh">"</span><span class="p">,</span> <span class="n">max_age_seconds</span><span class="o">=</span><span class="mi">300</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">cached</span><span class="p">:</span>
            <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">market_data</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">cached</span>
            <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">data_freshness</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">cached (may be up to 5min old)</span><span class="sh">"</span>
            <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">components_used</span><span class="sh">"</span><span class="p">].</span><span class="nf">append</span><span class="p">(</span><span class="sh">"</span><span class="s">cached_data</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">market_data</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="bp">None</span>
            <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">data_freshness</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">unavailable</span><span class="sh">"</span>

    <span class="c1"># Core functionality — this must succeed
</span>    <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">answer</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span>
        <span class="n">query</span><span class="o">=</span><span class="n">user_query</span><span class="p">,</span>
        <span class="n">context</span><span class="o">=</span><span class="n">response</span>
    <span class="p">)</span>
    <span class="n">response</span><span class="p">[</span><span class="sh">"</span><span class="s">components_used</span><span class="sh">"</span><span class="p">].</span><span class="nf">append</span><span class="p">(</span><span class="sh">"</span><span class="s">llm_core</span><span class="sh">"</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">response</span>
</code></pre></div></div> <blockquote> <p><strong>Why not catch everything at the top level?</strong> A single <code class="language-plaintext highlighter-rouge">try/except</code> around the entire function would either catch everything (and lose detail about what failed) or re-raise on first failure (no degradation). Component-level try/except gives you surgical control: some components are optional (personalization), some have fallbacks (real-time data → cache), and some are required (the LLM call). Each component’s failure is handled exactly as its criticality demands.</p> </blockquote> <h3 id="strategy-4-circuit-breaker">Strategy 4: Circuit Breaker</h3> <p>The circuit breaker pattern prevents cascading failures by automatically stopping calls to a consistently failing service.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">dataclasses</span> <span class="kn">import</span> <span class="n">dataclass</span><span class="p">,</span> <span class="n">field</span>
<span class="kn">from</span> <span class="n">datetime</span> <span class="kn">import</span> <span class="n">datetime</span><span class="p">,</span> <span class="n">timedelta</span>
<span class="kn">from</span> <span class="n">enum</span> <span class="kn">import</span> <span class="n">Enum</span>

<span class="k">class</span> <span class="nc">CircuitState</span><span class="p">(</span><span class="n">Enum</span><span class="p">):</span>
    <span class="n">CLOSED</span>   <span class="o">=</span> <span class="sh">"</span><span class="s">closed</span><span class="sh">"</span>    <span class="c1"># Normal operation — calls pass through
</span>    <span class="n">OPEN</span>     <span class="o">=</span> <span class="sh">"</span><span class="s">open</span><span class="sh">"</span>      <span class="c1"># Failing — calls blocked immediately
</span>    <span class="n">HALF_OPEN</span> <span class="o">=</span> <span class="sh">"</span><span class="s">half_open</span><span class="sh">"</span> <span class="c1"># Testing — one call allowed to check recovery
</span>
<span class="nd">@dataclass</span>
<span class="k">class</span> <span class="nc">CircuitBreaker</span><span class="p">:</span>
    <span class="n">failure_threshold</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">5</span>         <span class="c1"># Open after this many consecutive failures
</span>    <span class="n">recovery_timeout</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">30</span>          <span class="c1"># Seconds to wait before half-open
</span>    <span class="n">state</span><span class="p">:</span> <span class="n">CircuitState</span> <span class="o">=</span> <span class="n">CircuitState</span><span class="p">.</span><span class="n">CLOSED</span>
    <span class="n">failure_count</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">last_failure_time</span><span class="p">:</span> <span class="n">datetime</span> <span class="o">=</span> <span class="bp">None</span>

    <span class="k">def</span> <span class="nf">call</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">func</span><span class="p">,</span> <span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">state</span> <span class="o">==</span> <span class="n">CircuitState</span><span class="p">.</span><span class="n">OPEN</span><span class="p">:</span>
            <span class="c1"># Check if recovery timeout has elapsed
</span>            <span class="k">if</span> <span class="n">datetime</span><span class="p">.</span><span class="nf">now</span><span class="p">()</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">last_failure_time</span> <span class="o">&gt;</span> <span class="nf">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">recovery_timeout</span><span class="p">):</span>
                <span class="n">self</span><span class="p">.</span><span class="n">state</span> <span class="o">=</span> <span class="n">CircuitState</span><span class="p">.</span><span class="n">HALF_OPEN</span>
                <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Circuit HALF-OPEN: testing if service recovered...</span><span class="sh">"</span><span class="p">)</span>
            <span class="k">else</span><span class="p">:</span>
                <span class="k">raise</span> <span class="nc">Exception</span><span class="p">(</span><span class="sh">"</span><span class="s">Circuit OPEN: service unavailable. Skipping call to prevent cascade.</span><span class="sh">"</span><span class="p">)</span>

        <span class="k">try</span><span class="p">:</span>
            <span class="n">result</span> <span class="o">=</span> <span class="nf">func</span><span class="p">(</span><span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">_on_success</span><span class="p">()</span>
            <span class="k">return</span> <span class="n">result</span>
        <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">_on_failure</span><span class="p">()</span>
            <span class="k">raise</span>

    <span class="k">def</span> <span class="nf">_on_success</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">failure_count</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">state</span> <span class="o">==</span> <span class="n">CircuitState</span><span class="p">.</span><span class="n">HALF_OPEN</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Circuit CLOSED: service has recovered.</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">state</span> <span class="o">=</span> <span class="n">CircuitState</span><span class="p">.</span><span class="n">CLOSED</span>

    <span class="k">def</span> <span class="nf">_on_failure</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">failure_count</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">self</span><span class="p">.</span><span class="n">last_failure_time</span> <span class="o">=</span> <span class="n">datetime</span><span class="p">.</span><span class="nf">now</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">failure_count</span> <span class="o">&gt;=</span> <span class="n">self</span><span class="p">.</span><span class="n">failure_threshold</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">state</span> <span class="o">=</span> <span class="n">CircuitState</span><span class="p">.</span><span class="n">OPEN</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Circuit OPENED after </span><span class="si">{</span><span class="n">self</span><span class="p">.</span><span class="n">failure_count</span><span class="si">}</span><span class="s"> failures. Will retry in </span><span class="si">{</span><span class="n">self</span><span class="p">.</span><span class="n">recovery_timeout</span><span class="si">}</span><span class="s">s.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why is the circuit breaker needed when you already have retry logic?</strong> Without a circuit breaker, every request retries independently — even if you <em>know</em> the service has been failing for the past 10 minutes. The circuit breaker creates shared knowledge: once enough requests have failed, <em>all</em> subsequent requests immediately fail fast rather than waiting for a timeout. This frees up resources and prevents the cascade where one slow service causes the entire system to pile up with waiting threads.</p> </blockquote> <blockquote> <p><strong>The three states explained:</strong> CLOSED = normal (all calls pass through). OPEN = failed (all calls immediately rejected, no actual call made). HALF_OPEN = recovery probe (one call allowed — if it succeeds, CLOSED; if it fails, back to OPEN with refreshed timeout).</p> </blockquote> <h3 id="strategy-5-logging-and-observability">Strategy 5: Logging and Observability</h3> <p>You cannot improve what you cannot see. Comprehensive logging transforms failures from mysteries into actionable data.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">logging</span>
<span class="kn">import</span> <span class="n">traceback</span>
<span class="kn">import</span> <span class="n">json</span>
<span class="kn">from</span> <span class="n">datetime</span> <span class="kn">import</span> <span class="n">datetime</span>

<span class="c1"># Configure structured logging
</span><span class="n">logger</span> <span class="o">=</span> <span class="n">logging</span><span class="p">.</span><span class="nf">getLogger</span><span class="p">(</span><span class="n">__name__</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">log_agent_error</span><span class="p">(</span>
    <span class="n">error</span><span class="p">:</span> <span class="nb">Exception</span><span class="p">,</span>
    <span class="n">agent_name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
    <span class="n">action</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
    <span class="n">inputs</span><span class="p">:</span> <span class="nb">dict</span><span class="p">,</span>
    <span class="n">attempt_number</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span>
    <span class="n">context</span><span class="p">:</span> <span class="nb">dict</span> <span class="o">=</span> <span class="bp">None</span>
<span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Log a structured error record for debugging and monitoring.
    </span><span class="sh">"""</span>
    <span class="n">error_record</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">timestamp</span><span class="sh">"</span><span class="p">:</span>      <span class="n">datetime</span><span class="p">.</span><span class="nf">utcnow</span><span class="p">().</span><span class="nf">isoformat</span><span class="p">(),</span>
        <span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">:</span>          <span class="n">agent_name</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">action</span><span class="sh">"</span><span class="p">:</span>         <span class="n">action</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">attempt</span><span class="sh">"</span><span class="p">:</span>        <span class="n">attempt_number</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">error_type</span><span class="sh">"</span><span class="p">:</span>     <span class="nf">type</span><span class="p">(</span><span class="n">error</span><span class="p">).</span><span class="n">__name__</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">error_message</span><span class="sh">"</span><span class="p">:</span>  <span class="nf">str</span><span class="p">(</span><span class="n">error</span><span class="p">),</span>
        <span class="sh">"</span><span class="s">error_traceback</span><span class="sh">"</span><span class="p">:</span> <span class="n">traceback</span><span class="p">.</span><span class="nf">format_exc</span><span class="p">(),</span>
        <span class="sh">"</span><span class="s">inputs</span><span class="sh">"</span><span class="p">:</span>         <span class="n">inputs</span><span class="p">,</span>    <span class="c1"># What was the agent trying to do?
</span>        <span class="sh">"</span><span class="s">context</span><span class="sh">"</span><span class="p">:</span>        <span class="n">context</span><span class="p">,</span>   <span class="c1"># What was the system state?
</span>    <span class="p">}</span>

    <span class="c1"># JSON format enables querying with log analytics tools (Splunk, Datadog, etc.)
</span>    <span class="n">logger</span><span class="p">.</span><span class="nf">error</span><span class="p">(</span><span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">(</span><span class="n">error_record</span><span class="p">))</span>
</code></pre></div></div> <blockquote> <p><strong>Why structured JSON logging instead of plain text?</strong> Plain text logs like <code class="language-plaintext highlighter-rouge">"Error: connection failed at 14:32"</code> are readable but not queryable. JSON logs like <code class="language-plaintext highlighter-rouge">{"error_type": "ConnectionError", "agent": "billing_agent", "attempt": 3}</code> can be filtered, aggregated, and alerted on by log management tools. You can ask: “Show me all <code class="language-plaintext highlighter-rouge">ConnectionError</code> events from <code class="language-plaintext highlighter-rouge">billing_agent</code> in the last hour with more than 2 retries” — impossible with plain text, trivial with structured JSON.</p> </blockquote> <hr/> <h2 id="phase-3-recovery-strategies">Phase 3: Recovery Strategies</h2> <p>Handling an error keeps the system running. Recovery restores it to full health.</p> <h3 id="state-rollback">State Rollback</h3> <p>When an agent performs multiple steps and fails partway through, incomplete actions can leave the system in a corrupted state. State rollback reverses completed steps.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">TransactionManager</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Manages multi-step agent operations with rollback on failure.
    Like a database transaction — either everything succeeds, or everything rolls back.
    </span><span class="sh">"""</span>

    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">completed_steps</span> <span class="o">=</span> <span class="p">[]</span>  <span class="c1"># Stack of (step_name, undo_function) pairs
</span>
    <span class="k">def</span> <span class="nf">execute_step</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">step_name</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">action</span><span class="p">,</span> <span class="n">undo_action</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">Execute one step and register its undo operation.</span><span class="sh">"""</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="n">result</span> <span class="o">=</span> <span class="nf">action</span><span class="p">()</span>
            <span class="n">self</span><span class="p">.</span><span class="n">completed_steps</span><span class="p">.</span><span class="nf">append</span><span class="p">((</span><span class="n">step_name</span><span class="p">,</span> <span class="n">undo_action</span><span class="p">))</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">✓ Step completed: </span><span class="si">{</span><span class="n">step_name</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
            <span class="k">return</span> <span class="n">result</span>
        <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">✗ Step failed: </span><span class="si">{</span><span class="n">step_name</span><span class="si">}</span><span class="s"> — </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">rollback_all</span><span class="p">()</span>
            <span class="k">raise</span>

    <span class="k">def</span> <span class="nf">rollback_all</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">Undo all completed steps in reverse order.</span><span class="sh">"""</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Rolling back </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">completed_steps</span><span class="p">)</span><span class="si">}</span><span class="s"> completed steps...</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">while</span> <span class="n">self</span><span class="p">.</span><span class="n">completed_steps</span><span class="p">:</span>
            <span class="n">step_name</span><span class="p">,</span> <span class="n">undo_fn</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">completed_steps</span><span class="p">.</span><span class="nf">pop</span><span class="p">()</span>
            <span class="k">try</span><span class="p">:</span>
                <span class="nf">undo_fn</span><span class="p">()</span>
                <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  ↩ Rolled back: </span><span class="si">{</span><span class="n">step_name</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
            <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
                <span class="c1"># Log rollback failure but continue trying other rollbacks
</span>                <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  ⚠ Rollback failed for </span><span class="si">{</span><span class="n">step_name</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>


<span class="c1"># Usage example: booking a travel package
</span><span class="k">def</span> <span class="nf">book_travel_package</span><span class="p">(</span><span class="n">flight_id</span><span class="p">,</span> <span class="n">hotel_id</span><span class="p">,</span> <span class="n">car_id</span><span class="p">):</span>
    <span class="n">tx</span> <span class="o">=</span> <span class="nc">TransactionManager</span><span class="p">()</span>
    <span class="k">try</span><span class="p">:</span>
        <span class="c1"># Each step: (action, undo_action)
</span>        <span class="n">tx</span><span class="p">.</span><span class="nf">execute_step</span><span class="p">(</span>
            <span class="sh">"</span><span class="s">book_flight</span><span class="sh">"</span><span class="p">,</span>
            <span class="n">action</span>   <span class="o">=</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">flight_api</span><span class="p">.</span><span class="nf">book</span><span class="p">(</span><span class="n">flight_id</span><span class="p">),</span>
            <span class="n">undo_action</span> <span class="o">=</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">flight_api</span><span class="p">.</span><span class="nf">cancel</span><span class="p">(</span><span class="n">flight_id</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="n">tx</span><span class="p">.</span><span class="nf">execute_step</span><span class="p">(</span>
            <span class="sh">"</span><span class="s">book_hotel</span><span class="sh">"</span><span class="p">,</span>
            <span class="n">action</span>   <span class="o">=</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">hotel_api</span><span class="p">.</span><span class="nf">book</span><span class="p">(</span><span class="n">hotel_id</span><span class="p">),</span>
            <span class="n">undo_action</span> <span class="o">=</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">hotel_api</span><span class="p">.</span><span class="nf">cancel</span><span class="p">(</span><span class="n">hotel_id</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="n">tx</span><span class="p">.</span><span class="nf">execute_step</span><span class="p">(</span>
            <span class="sh">"</span><span class="s">book_car</span><span class="sh">"</span><span class="p">,</span>
            <span class="n">action</span>   <span class="o">=</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">car_api</span><span class="p">.</span><span class="nf">book</span><span class="p">(</span><span class="n">car_id</span><span class="p">),</span>
            <span class="n">undo_action</span> <span class="o">=</span> <span class="k">lambda</span><span class="p">:</span> <span class="n">car_api</span><span class="p">.</span><span class="nf">cancel</span><span class="p">(</span><span class="n">car_id</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">✅ All bookings successful!</span><span class="sh">"</span><span class="p">)</span>

    <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">❌ Booking failed: </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="s">. All completed bookings have been cancelled.</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">raise</span>
</code></pre></div></div> <blockquote> <p><strong>Why reverse order for rollback?</strong> If you completed steps A → B → C and step D fails, you need to undo C before B, and B before A. Undoing in original order could leave dependencies in place that make the undo impossible. Think of building with Lego: you take apart the last piece first, not the first piece first.</p> </blockquote> <blockquote> <p><strong>Why continue rolling back even if a rollback step fails?</strong> Because the goal of rollback is to restore the most complete clean state possible. If hotel rollback fails but car rollback succeeds, you’ve at least recovered the car booking cost. Stopping at the first rollback failure would leave more resources locked.</p> </blockquote> <hr/> <h2 id="the-adk-implementation-sequentialagent-with-fallback">The ADK Implementation: SequentialAgent with Fallback</h2> <p>Google ADK’s <code class="language-plaintext highlighter-rouge">SequentialAgent</code> provides a natural structure for implementing the primary → fallback → response pattern using session state as the coordination mechanism.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span><span class="p">,</span> <span class="n">SequentialAgent</span>
</code></pre></div></div> <blockquote> <p><strong>Why use a SequentialAgent for exception handling?</strong> Each “handler” in the exception handling pipeline is a distinct responsibility: the primary handler tries the best approach, the fallback handler activates if needed, and the response handler presents whatever result was obtained. <code class="language-plaintext highlighter-rouge">SequentialAgent</code> guarantees these run in order and share state through <code class="language-plaintext highlighter-rouge">session.state</code> — making the coordination explicit and debuggable.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Agent 1: Attempts the primary approach — high precision
</span><span class="n">primary_handler</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">primary_handler</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">
Your job is to get precise location information.
Use the get_precise_location_info tool with the user</span><span class="sh">'</span><span class="s">s provided address.
If the tool succeeds, store the result in state[</span><span class="sh">"</span><span class="s">location_result</span><span class="sh">"</span><span class="s">].
If the tool fails for any reason, store True in state[</span><span class="sh">"</span><span class="s">primary_location_failed</span><span class="sh">"</span><span class="s">].
Always set state[</span><span class="sh">"</span><span class="s">primary_location_failed</span><span class="sh">"</span><span class="s">] to either True or False.
</span><span class="sh">"""</span><span class="p">,</span>
    <span class="n">tools</span> <span class="o">=</span> <span class="p">[</span><span class="n">get_precise_location_info</span><span class="p">]</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why store the failure signal in <code class="language-plaintext highlighter-rouge">state["primary_location_failed"]</code>?</strong> The secondary agent needs to know whether the primary succeeded or failed. Session state is the shared communication channel between sequential agents in ADK — it’s the equivalent of a shared variable that both agents can read and write. Without this explicit signal, the fallback agent would have no way to know whether to activate.</p> </blockquote> <blockquote> <p><strong>Why “Always set state[‘primary_location_failed’] to either True or False”?</strong> This is defensive instruction design. Without it, the agent might only set the flag when it fails, leaving it unset (and thus <code class="language-plaintext highlighter-rouge">None</code>) when it succeeds. The fallback agent then can’t reliably distinguish “succeeded” from “flag not set.” Explicit True/False handling is more reliable than absence/presence.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Agent 2: Conditional fallback — only activates if primary failed
</span><span class="n">fallback_handler</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">fallback_handler</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">
Check the value of state[</span><span class="sh">"</span><span class="s">primary_location_failed</span><span class="sh">"</span><span class="s">].

If it is True:
  - Extract the city name from the user</span><span class="sh">'</span><span class="s">s original query
  - Use the get_general_area_info tool with that city name
  - Store the result in state[</span><span class="sh">"</span><span class="s">location_result</span><span class="sh">"</span><span class="s">]
  - Store </span><span class="sh">"</span><span class="s">city-level (degraded)</span><span class="sh">"</span><span class="s"> in state[</span><span class="sh">"</span><span class="s">data_precision</span><span class="sh">"</span><span class="s">]

If it is False:
  - Do nothing. The primary handler already succeeded.
</span><span class="sh">"""</span><span class="p">,</span>
    <span class="n">tools</span> <span class="o">=</span> <span class="p">[</span><span class="n">get_general_area_info</span><span class="p">]</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why check state in the instruction rather than in code?</strong> The conditional logic (“if failed, activate; if not, do nothing”) is expressed in natural language because the agent’s LLM reads it and decides what to do. This is the ADK way — the LLM is the control flow mechanism for agent behavior. Alternative: pre-check in Python and skip the agent if not needed, which is also valid and more efficient for deterministic conditions.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Agent 3: Presents results regardless of which path succeeded
</span><span class="n">response_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">response_agent</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">
Review the location information in state[</span><span class="sh">"</span><span class="s">location_result</span><span class="sh">"</span><span class="s">].

Present this information clearly to the user.
If state[</span><span class="sh">"</span><span class="s">data_precision</span><span class="sh">"</span><span class="s">] is </span><span class="sh">"</span><span class="s">city-level (degraded)</span><span class="sh">"</span><span class="s">, mention that
you</span><span class="sh">'</span><span class="s">re showing city-level data because detailed lookup was temporarily unavailable.

If state[</span><span class="sh">"</span><span class="s">location_result</span><span class="sh">"</span><span class="s">] is empty or does not exist, apologize that
you could not retrieve location information and suggest trying again later.
</span><span class="sh">"""</span><span class="p">,</span>
    <span class="n">tools</span> <span class="o">=</span> <span class="p">[]</span>  <span class="c1"># This agent only reasons — no tool calls needed
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why does the response agent have <code class="language-plaintext highlighter-rouge">tools=[]</code>?</strong> The response agent’s job is purely to interpret and communicate the state — it doesn’t need to call any tools. Giving it an empty tool list makes this explicit and prevents the LLM from attempting unnecessary tool calls. It’s also more efficient — no tool schema is sent to the model.</p> </blockquote> <blockquote> <p><strong>Why have a separate response agent at all?</strong> The response format shouldn’t be the responsibility of either the primary or fallback handler — they’re focused on data retrieval. Separating presentation from retrieval means: if you want to change the response format (add more context, translate to another language, format as HTML), you only change the response agent without touching the retrieval logic.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Assemble: SequentialAgent ensures guaranteed execution order
</span><span class="n">robust_location_agent</span> <span class="o">=</span> <span class="nc">SequentialAgent</span><span class="p">(</span>
    <span class="n">name</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">robust_location_agent</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">sub_agents</span> <span class="o">=</span> <span class="p">[</span><span class="n">primary_handler</span><span class="p">,</span> <span class="n">fallback_handler</span><span class="p">,</span> <span class="n">response_agent</span><span class="p">]</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>What happens if even the fallback fails?</strong> The response agent is designed to handle this: <code class="language-plaintext highlighter-rouge">"If state['location_result'] is empty or does not exist, apologize..."</code> This is the final safety net — no matter what happens upstream, the response agent always runs and always produces a response that makes sense to the user. The user never sees an unhandled exception; they always get a coherent message.</p> </blockquote> <h3 id="the-complete-flow-visualized">The Complete Flow Visualized</h3> <pre><code class="language-mermaid">graph TD
    U([User: "Find info for 123 Main St"]) --&gt; PA[primary_handler]
    PA --&gt;|tool call| GPS[get_precise_location_info]
    GPS --&gt;|success| S1[state: location_result = precise data&lt;br&gt;primary_location_failed = False]
    GPS --&gt;|error: 503| S2[state: primary_location_failed = True]
    S1 --&gt; FB[fallback_handler]
    S2 --&gt; FB
    FB --&gt;|failed=False| SKIP[Do nothing — primary succeeded]
    FB --&gt;|failed=True| CITY[get_general_area_info tool]
    CITY --&gt; S3[state: location_result = city data&lt;br&gt;data_precision = city-level]
    SKIP --&gt; RA[response_agent]
    S3 --&gt; RA
    RA --&gt; OUT([User sees: location info with appropriate context])
    style PA fill:#141b2d,stroke:#2698ba,color:#e0e0e0
    style FB fill:#141b2d,stroke:#e6a817,color:#e0e0e0
    style RA fill:#141b2d,stroke:#4fc97e,color:#e0e0e0
    style OUT fill:#141b2d,stroke:#4fc97e,color:#e0e0e0
</code></pre> <hr/> <h2 id="live-failure-simulation">Live Failure Simulation</h2> <div class="exc-sim-wrapper"> <div class="exc-sim-header"> <span class="exc-sim-title">EXCEPTION HANDLING DEMO — simulate tool failure and recovery</span> <div style="display:flex;gap:0.5rem;"> <select class="exc-sim-select" id="excScenario"> <option value="0">Scenario: API timeout → retry → success</option> <option value="1">Scenario: Auth failure → abort immediately</option> <option value="2">Scenario: 3 retries exhausted → fallback activates</option> <option value="3">Scenario: Malformed JSON → retry with clarified prompt</option> </select> <button class="exc-sim-btn" id="excSimRunBtn">▶ Run</button> </div> </div> <div class="exc-sim-body" id="excSimBody"> <div class="exc-sim-hint">Select a scenario above and click Run to see how the agent handles it.</div> </div> </div> <style>.exc-sim-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.exc-sim-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.5rem}.exc-sim-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.exc-sim-select{font-family:monospace;font-size:.7rem;padding:.28rem .6rem;border-radius:4px;border:1px solid var(--global-divider-color);background:var(--global-bg-color);color:var(--global-text-color);cursor:pointer}.exc-sim-btn{font-family:monospace;font-size:.72rem;padding:.3rem .75rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer}.exc-sim-btn:hover{background:rgba(38,152,186,0.12);border-color:#2698ba;color:#2698ba}.exc-sim-body{padding:1rem 1.1rem;display:flex;flex-direction:column;gap:.45rem;min-height:80px}.exc-sim-hint{font-size:.8rem;color:var(--global-text-color-light);font-style:italic}.exc-sim-event{padding:.5rem .75rem;border-radius:5px;font-size:.78rem;border:1px solid var(--global-divider-color);animation:excEvIn .25s ease}@keyframes excEvIn{from{opacity:0}to{opacity:1}}.exc-ev-attempt{background:rgba(128,128,128,0.05)}.exc-ev-error{background:rgba(255,107,107,0.06);border-color:rgba(255,107,107,0.2)}.exc-ev-retry{background:rgba(230,168,23,0.06);border-color:rgba(230,168,23,0.2)}.exc-ev-fallback{background:rgba(201,122,242,0.06);border-color:rgba(201,122,242,0.2)}.exc-ev-success{background:rgba(79,201,126,0.07);border-color:rgba(79,201,126,0.2)}.exc-ev-abort{background:rgba(255,107,107,0.1);border-color:rgba(255,107,107,0.3)}</style> <script>
var EXC_SCENARIOS = [
  [
    { type:'attempt', text:'🔧 Calling weather_api.get(city="London")...' },
    { type:'error',   text:'❌ Error: TimeoutError — request exceeded 10s timeout' },
    { type:'retry',   text:'⏱ Transient error detected. Waiting 1s before retry 1/3...' },
    { type:'attempt', text:'🔧 Retry 1: Calling weather_api.get(city="London")...' },
    { type:'success', text:'✅ Success on retry 1. Response: {"temp": 15, "condition": "Cloudy"}' },
  ],
  [
    { type:'attempt', text:'🔧 Calling crm_api.get_customer(id="12345")...' },
    { type:'error',   text:'❌ Error: HTTP 401 Unauthorized — API key is invalid or expired' },
    { type:'abort',   text:'🚫 Permanent error (4xx) — retrying will not help. Aborting and alerting operator.' },
  ],
  [
    { type:'attempt', text:'🔧 Calling payment_processor.charge(amount=99.99)...' },
    { type:'error',   text:'❌ Error: HTTP 503 Service Unavailable' },
    { type:'retry',   text:'⏱ Waiting 1s before retry 1/3...' },
    { type:'attempt', text:'🔧 Retry 1: payment_processor.charge(amount=99.99)...' },
    { type:'error',   text:'❌ Error: HTTP 503 Service Unavailable' },
    { type:'retry',   text:'⏱ Waiting 2s before retry 2/3...' },
    { type:'attempt', text:'🔧 Retry 2: payment_processor.charge(amount=99.99)...' },
    { type:'error',   text:'❌ Error: HTTP 503 Service Unavailable' },
    { type:'retry',   text:'⏱ Waiting 4s before retry 3/3...' },
    { type:'attempt', text:'🔧 Retry 3: payment_processor.charge(amount=99.99)...' },
    { type:'error',   text:'❌ Error: HTTP 503 — Max retries exhausted' },
    { type:'fallback',text:'🔄 Activating fallback: backup_payment_processor...' },
    { type:'success', text:'✅ Fallback succeeded. Transaction ID: txn_backup_7821. User notified that backup processor was used.' },
  ],
  [
    { type:'attempt', text:'🤖 Requesting JSON output from LLM...' },
    { type:'error',   text:'❌ JSONDecodeError: LLM returned prose instead of JSON: "The answer is 42 and the unit is meters..."' },
    { type:'retry',   text:'🔧 Retry with clarified prompt: "You MUST respond with ONLY valid JSON. No prose. Example: {\"value\": 42, \"unit\": \"meters\"}"' },
    { type:'success', text:'✅ Valid JSON received on retry: {"value": 42, "unit": "meters", "confidence": 0.95}' },
  ]
];

document.addEventListener('DOMContentLoaded', function(){
  var runBtn = document.getElementById('excSimRunBtn');
  var select = document.getElementById('excScenario');
  if (!runBtn) return;
  var running = false;
  var TYPE_CLASS = { attempt:'exc-ev-attempt', error:'exc-ev-error', retry:'exc-ev-retry', fallback:'exc-ev-fallback', success:'exc-ev-success', abort:'exc-ev-abort' };

  runBtn.addEventListener('click', async function(){
    if (running) return;
    running = true;
    runBtn.textContent = '⏳'; runBtn.disabled = true;
    var body = document.getElementById('excSimBody');
    body.innerHTML = '';
    var scenario = EXC_SCENARIOS[parseInt(select.value)];
    for (var i = 0; i < scenario.length; i++) {
      await new Promise(function(r){ setTimeout(r, 500); });
      var e = scenario[i];
      var div = document.createElement('div');
      div.className = 'exc-sim-event exc-ev-' + e.type;
      div.textContent = e.text;
      body.appendChild(div);
    }
    running = false; runBtn.textContent = '↺ Run'; runBtn.disabled = false;
  });
});
</script> <hr/> <h2 id="common-mistakes-in-exception-handling">Common Mistakes in Exception Handling</h2> <p><strong>Mistake 1: Catching everything with a bare <code class="language-plaintext highlighter-rouge">except Exception</code>.</strong> A single catch-all handler loses all information about what went wrong. Different errors need different responses — a <code class="language-plaintext highlighter-rouge">401 Unauthorized</code> needs credentials fixed; a <code class="language-plaintext highlighter-rouge">503</code> needs a retry. Catch specific exception types and handle each appropriately.</p> <p><strong>Mistake 2: Retrying non-idempotent operations.</strong> If <code class="language-plaintext highlighter-rouge">send_email()</code> fails partway, should you retry it? Only if the email service guarantees idempotency (same message ID = only sent once). Otherwise, retrying sends duplicate emails. Always ask: “Is it safe to call this twice?” before adding retry logic.</p> <p><strong>Mistake 3: Swallowing exceptions silently.</strong> <code class="language-plaintext highlighter-rouge">except Exception: pass</code> is one of the most dangerous patterns in programming. The error is hidden from logs, from monitoring, from the developer, and from the user. The system appears to be running correctly while actually failing silently. Always log exceptions, even if you handle them gracefully.</p> <p><strong>Mistake 4: Logging after a failed rollback, not the original error.</strong> If your rollback fails, the error you log is the rollback failure — but the root cause is the original operation failure. Log the original error <em>first</em>, then attempt rollback, then log any rollback failures separately.</p> <p><strong>Mistake 5: No circuit breaker for external dependencies.</strong> Without circuit breakers, a slow or failed external service causes your agent to pile up with threads waiting for timeouts. Each request waits 10 seconds before failing — with 100 concurrent users, you suddenly have 1,000 seconds of accumulated wait time. Circuit breakers fail fast, freeing resources immediately.</p> <p><strong>Mistake 6: Not testing failure paths.</strong> Error handling code is only tested when things go wrong — which means in production, when you least want surprises. Deliberately inject failures in testing: mock the API to return 503, send malformed JSON, trigger timeouts. Ensure your error handling actually works before deploying.</p> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="exc-summary-card"> <div class="exc-summary-col"> <div class="exc-summary-label">WHAT</div> <p>A structured approach to detecting, handling, and recovering from operational failures in AI agents — covering network errors, API failures, data format errors, LLM output failures, and logic errors, with specific strategies for each.</p> </div> <div class="exc-summary-divider"></div> <div class="exc-summary-col"> <div class="exc-summary-label">WHY</div> <p>Real-world systems fail constantly. An agent without exception handling is fragile — one API error crashes the entire conversation. An agent with exception handling is resilient — it detects problems, recovers where possible, degrades gracefully where not, and always leaves the user informed.</p> </div> <div class="exc-summary-divider"></div> <div class="exc-summary-col"> <div class="exc-summary-label">RULE OF THUMB</div> <p>Use this pattern for any production agent that interacts with external systems — which means every production agent. The implementation complexity scales with the criticality of the application: more retries, more fallbacks, and more human escalation paths for higher-stakes systems.</p> </div> </div> <style>.exc-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.exc-summary-card{flex-direction:column}}.exc-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.exc-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.exc-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.exc-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#ff6b6b}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li> <p><strong>Expect failures, design for them.</strong> Production agents encounter network timeouts, API errors, malformed data, LLM output failures, and resource exhaustion constantly. These are not edge cases — they are normal operating conditions. Design for them explicitly, not as an afterthought.</p> </li> <li> <p><strong>Classify errors before handling them.</strong> Transient errors (network glitches, rate limits, server overload) warrant retries. Permanent errors (authentication failures, invalid requests, missing resources) should fail fast — retrying wastes time and resources. Know the difference.</p> </li> <li> <p><strong>Exponential backoff with jitter is the right default retry strategy.</strong> Linear backoff creates retry storms. Exponential backoff spreads load. Jitter (randomization) prevents synchronized retries from multiple clients. The formula: <code class="language-plaintext highlighter-rouge">wait = base × 2^attempt + random(0, base × 0.1)</code>.</p> </li> <li> <p><strong>Fallback hierarchies provide resilience in depth.</strong> Primary approach fails? Try alternative service. That fails? Use cached data. That fails? Return a graceful degradation message. Each level provides less value but more availability. Design the full hierarchy before deployment.</p> </li> <li> <p><strong>Circuit breakers prevent cascading failures.</strong> When a service is consistently failing, stop calling it immediately rather than making every request wait for a timeout. Fail fast, preserve resources, check periodically whether the service has recovered.</p> </li> <li> <p><strong>Log everything, swallow nothing.</strong> <code class="language-plaintext highlighter-rouge">except Exception: pass</code> is production debt. Every caught exception should be logged with structured context (agent name, action, inputs, error type, traceback). This data is essential for diagnosing production issues.</p> </li> <li> <p><strong>State rollback is critical for multi-step operations.</strong> If a 5-step workflow fails at step 3, steps 1 and 2 need to be undone — otherwise you have partial, corrupted state. Use the transaction pattern: register undo operations at each step, roll back in reverse order on failure.</p> </li> <li> <p><strong>In ADK, use SequentialAgent + session state for primary/fallback flows.</strong> The primary handler sets <code class="language-plaintext highlighter-rouge">state["failed"] = True/False</code>. The fallback handler reads this signal and activates only if needed. The response agent presents results regardless of which path was taken. Explicit state signaling makes the coordination debuggable.</p> </li> </ul> <hr/> <p><em>Next up — Chapter 13: Human-in-the-Loop, where agents pause at critical decision points, present their reasoning, and request human approval before taking irreversible actions.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="exception-handling"/><category term="resilience"/><summary type="html"><![CDATA[Real-world agents fail. Networks time out, APIs return errors, databases go down, data arrives malformed. This chapter is about building agents that handle every failure gracefully — detecting it, recovering from it, and keeping the user informed throughout.]]></summary></entry><entry><title type="html">Chapter 11: Goal Setting and Monitoring</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/goal-setting-monitoring/" rel="alternate" type="text/html" title="Chapter 11: Goal Setting and Monitoring"/><published>2026-02-14T00:00:00+00:00</published><updated>2026-02-14T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/goal-setting-monitoring</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/goal-setting-monitoring/"><![CDATA[<h2 id="why-goals-transform-agents">Why Goals Transform Agents</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Goal state:</strong> The desired end condition — what "done" or "success" looks like. "The customer's billing issue is resolved" is a goal state. "The agent has replied to the customer" is not — that's just an action, not a goal.</p> <p style="margin-top:0.5rem"><strong>Initial state:</strong> Where you start from. The current situation that the agent must change. Understanding the initial state is as important as understanding the goal — the plan is a path from one to the other.</p> <p style="margin-top:0.5rem"><strong>SMART goals:</strong> A framework for writing well-defined goals: Specific (clear about what), Measurable (you can determine if it's achieved), Achievable (within the agent's capabilities), Relevant (connected to actual user needs), Time-bound (has a defined deadline or iteration limit). A vague goal like "help the user" is not SMART. "Generate Python code that passes all provided unit tests within 5 iterations" is SMART.</p> <p style="margin-top:0.5rem"><strong>Feedback loop:</strong> A cycle where the output of a process is used as input to the same process in the next iteration. In goal monitoring, the agent's progress toward the goal is measured, and that measurement is fed back to influence the agent's next action. This is how agents self-correct.</p> <p style="margin-top:0.5rem"><strong>Stopping condition:</strong> The rule that determines when to exit the feedback loop. Without a stopping condition, a goal-driven agent can loop forever. Good stopping conditions include: goal achieved (success), maximum iterations reached (timeout), and no improvement detected over N iterations (stagnation).</p> <p style="margin-top:0.5rem"><strong>Self-evaluation:</strong> When the agent uses the same LLM (or a separate one) to judge whether its own output meets the stated goals. This is related to the Reflection pattern (Chapter 4), but specifically oriented around goal achievement rather than general quality.</p> </div> <p>Every agent in this series so far reacts to inputs. You send a message; it responds. You make a request; it executes. The input determines the output, and the process is complete. These are <strong>reactive systems</strong> — they have no persistent purpose beyond the current request.</p> <p>But many of the most valuable things we want AI agents to do are not single-turn reactions. They are <em>sustained pursuits of outcomes</em>:</p> <ul> <li>“Resolve this customer’s billing issue” — might require multiple tool calls, a database lookup, an email, and a confirmation check</li> <li>“Write code that passes all these tests” — requires iteration: write, test, fix, retest</li> <li>“Keep this project on track” — requires continuous monitoring of task statuses and deadlines</li> <li>“Maximize portfolio returns within risk tolerance” — requires ongoing evaluation of market conditions</li> </ul> <p>These require agents that don’t just respond to the current input, but maintain a goal state across multiple steps, monitor their own progress toward that state, and adapt when they’re not making sufficient progress.</p> <p>That’s the <strong>Goal Setting and Monitoring</strong> pattern.</p> <p>The analogy: planning a trip. You don’t just spontaneously appear at your destination. You define where you want to go (the goal state), assess where you currently are (the initial state), plan the steps (book tickets, pack, travel), and continuously monitor your progress (check departure board, track your flight, navigate to the hotel). If something goes wrong — flight delayed, hotel overbooked — you don’t abandon the goal; you replan the path to the same destination.</p> <hr/> <h2 id="the-anatomy-of-a-well-defined-agent-goal">The Anatomy of a Well-Defined Agent Goal</h2> <p>Not all goals are equal. A poorly specified goal produces an agent that achieves the wrong thing confidently. A well-specified goal produces an agent that achieves the right thing reliably. The difference is the <strong>SMART framework</strong> — a goal-writing discipline from project management that applies directly to AI agents.</p> <div class="goal-smart-wrapper"> <div class="goal-smart-header"> <span class="goal-smart-title">SMART GOALS FOR AI AGENTS — click each letter to explore</span> </div> <div class="goal-smart-tabs"> <button class="gs-tab active" data-idx="0" onclick="gsTab(0)">S — Specific</button> <button class="gs-tab" data-idx="1" onclick="gsTab(1)">M — Measurable</button> <button class="gs-tab" data-idx="2" onclick="gsTab(2)">A — Achievable</button> <button class="gs-tab" data-idx="3" onclick="gsTab(3)">R — Relevant</button> <button class="gs-tab" data-idx="4" onclick="gsTab(4)">T — Time-bound</button> </div> <div class="goal-smart-body"> <div class="gs-content active" id="gsContent0"> <div class="gs-letter">S</div> <div class="gs-left"> <div class="gs-title">Specific — The goal must be unambiguous</div> <div class="gs-desc">A specific goal tells the agent *exactly* what it needs to achieve. Vague goals lead to vague results — the agent will pursue the path of least resistance, which is rarely what you actually wanted.</div> <div class="gs-compare"> <div class="gs-bad"><span class="gs-badge gs-bad-badge">✗ Vague</span>"Help users with their code problems." — What kind of help? Debugging? Explanation? Rewriting? What qualifies as "help"? The agent has no way to know when it's done.</div> <div class="gs-good"><span class="gs-badge gs-good-badge">✓ Specific</span>"Generate a working Python function that solves the given problem, includes a docstring, and handles the specified edge cases." — Clear what's needed, clear what format, clear scope.</div> </div> </div> </div> <div class="gs-content" id="gsContent1"> <div class="gs-letter">M</div> <div class="gs-left"> <div class="gs-title">Measurable — Success must be detectable</div> <div class="gs-desc">The monitoring component requires that you can determine, at any point, whether the goal has been achieved. Without measurability, monitoring is impossible — you can't track progress toward something you can't measure.</div> <div class="gs-compare"> <div class="gs-bad"><span class="gs-badge gs-bad-badge">✗ Unmeasurable</span>"Write good code." — "Good" is subjective. How would the agent know when it's achieved "good"? How would you know?</div> <div class="gs-good"><span class="gs-badge gs-good-badge">✓ Measurable</span>"Write code that passes all 5 provided unit tests and has no functions longer than 20 lines." — Both criteria can be objectively checked by running the tests and measuring line counts.</div> </div> </div> </div> <div class="gs-content" id="gsContent2"> <div class="gs-letter">A</div> <div class="gs-left"> <div class="gs-title">Achievable — Within the agent's actual capabilities</div> <div class="gs-desc">A goal must be achievable given the agent's available tools, knowledge, and authority. Setting an unachievable goal creates an agent that loops forever without progress, consuming resources without producing results.</div> <div class="gs-compare"> <div class="gs-bad"><span class="gs-badge gs-bad-badge">✗ Unachievable</span>"Solve any customer complaint instantly." — "Any" is too broad. "Instantly" may conflict with API latency. Some complaints require human judgment the agent cannot provide.</div> <div class="gs-good"><span class="gs-badge gs-good-badge">✓ Achievable</span>"Resolve billing complaints that match known error patterns using the billing_update tool. Escalate to human agents for complex cases." — Bounded scope + clear escalation path.</div> </div> </div> </div> <div class="gs-content" id="gsContent3"> <div class="gs-letter">R</div> <div class="gs-left"> <div class="gs-title">Relevant — Connected to what actually matters</div> <div class="gs-desc">The goal must be connected to the real objective — optimizing for the wrong metric is one of the most dangerous failure modes in AI systems. An agent that maximizes its stated metric while missing the actual intent is worse than no agent at all.</div> <div class="gs-compare"> <div class="gs-bad"><span class="gs-badge gs-bad-badge">✗ Misaligned</span>"Maximize the number of customer support tickets closed per hour." — The agent might close tickets prematurely without actually resolving issues, just to inflate the metric.</div> <div class="gs-good"><span class="gs-badge gs-good-badge">✓ Relevant</span>"Resolve customer issues such that customers confirm resolution and don't reopen the ticket within 48 hours." — Measures actual resolution, not just ticket closure velocity.</div> </div> </div> </div> <div class="gs-content" id="gsContent4"> <div class="gs-letter">T</div> <div class="gs-left"> <div class="gs-title">Time-bound — With a defined stopping condition</div> <div class="gs-desc">Every goal-driven agent loop needs a stopping condition. Without one, the agent either runs forever (burning API credits) or waits indefinitely for a condition that may never come. Time-bound goals can be calendar-bounded ("by Friday") or iteration-bounded ("within 5 attempts").</div> <div class="gs-compare"> <div class="gs-bad"><span class="gs-badge gs-bad-badge">✗ Unbounded</span>"Keep refining the code until it's perfect." — "Perfect" is never achieved. The loop runs until you manually kill the process or exhaust your budget.</div> <div class="gs-good"><span class="gs-badge gs-good-badge">✓ Time-bound</span>"Refine the code for up to 5 iterations. Stop early if all goals are met. After 5 iterations, return the best version achieved so far with a note on remaining gaps." — Clear exit conditions for both success and timeout.</div> </div> </div> </div> </div> </div> <style>.goal-smart-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.goal-smart-header{padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.goal-smart-tabs{display:flex;border-bottom:1px solid var(--global-divider-color);overflow-x:auto}.gs-tab{flex-shrink:0;padding:.55rem .9rem;font-family:monospace;font-size:.72rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s}.gs-tab:last-child{border-right:0}.gs-tab.active{background:rgba(79,201,126,0.1);color:#4fc97e;font-weight:700}.goal-smart-body{padding:1.1rem}.gs-content{display:none;gap:.85rem;flex-direction:column}.gs-content.active{display:flex}.gs-letter{font-size:3rem;font-weight:900;color:#4fc97e;opacity:.2;line-height:1;float:right;margin-left:1rem}.gs-left{flex:1;display:flex;flex-direction:column;gap:.65rem}.gs-title{font-size:.95rem;font-weight:700;color:var(--global-text-color)}.gs-desc{font-size:.83rem;color:var(--global-text-color-light);line-height:1.65}.gs-compare{display:flex;flex-direction:column;gap:.5rem}.gs-bad,.gs-good{padding:.65rem .85rem;border-radius:6px;font-size:.8rem;line-height:1.55}.gs-bad{background:rgba(255,107,107,0.07);border:1px solid rgba(255,107,107,0.2);color:var(--global-text-color-light)}.gs-good{background:rgba(79,201,126,0.07);border:1px solid rgba(79,201,126,0.2);color:var(--global-text-color)}.gs-badge{font-size:.62rem;font-weight:700;padding:.1em .45em;border-radius:3px;margin-right:.5rem;font-family:monospace;letter-spacing:.06em}.gs-bad-badge{background:rgba(255,107,107,0.2);color:#ff6b6b}.gs-good-badge{background:rgba(79,201,126,0.2);color:#4fc97e}</style> <script>
function gsTab(idx) {
  document.querySelectorAll('.gs-tab').forEach(function(t){ t.classList.remove('active'); });
  document.querySelectorAll('.gs-content').forEach(function(c){ c.classList.remove('active'); });
  document.querySelector('.gs-tab[data-idx="'+idx+'"]').classList.add('active');
  document.getElementById('gsContent'+idx).classList.add('active');
}
</script> <hr/> <h2 id="the-monitoring-feedback-loop">The Monitoring Feedback Loop</h2> <p>Goals without monitoring are just aspirations. The monitoring component is what makes the goal operational — it continuously checks: “Are we there yet? Are we making progress? Do we need to change course?”</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">GOAL SETTING AND MONITORING PATTERN</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:320px;"> <div class="ns-node-title">Define Goal + Success Criteria</div> <div class="ns-node-sub">SMART goal: specific, measurable, achievable, relevant, time-bound. Define what "done" looks like in concrete, checkable terms.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:320px;"> <div class="ns-node-title">Execute Action Step</div> <div class="ns-node-sub">Agent takes the next action toward the goal: generates output, calls a tool, makes a decision, updates a state value.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:340px;"> <div class="ns-node-title">Monitor: Evaluate Progress</div> <div class="ns-node-sub">Check current state against goal criteria. Run tests, ask the LLM to judge output, query a metric, compare to a threshold. Answer: "Have we met the goal? Are we making progress?"</div> </div> <div class="ns-arrow"></div> <div class="ns-decision" style="max-width:220px;"> <div class="ns-node-title">Goal Met?</div> <div class="ns-node-sub">Or max iterations reached?</div> </div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:500px;"> <div class="ns-branch"> <span class="ns-label-red">Not yet met</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"> <div class="ns-node-title">Adapt Strategy</div> <div class="ns-node-sub">Use monitoring feedback to adjust: refine output, try a different tool, replan approach, request more information.</div> </div> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-dim"><div class="ns-node-title">↑ Back to Execute</div></div> </div> <div class="ns-branch"> <span class="ns-label-green">Goal achieved</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Deliver Result</div> <div class="ns-node-sub">Return the final output. Log achievement. Update any persistent state.</div> </div> </div> </div> </div> </div> <p><strong>The difference from Reflection (Chapter 4).</strong> The Reflection pattern evaluates output quality and improves it. The Goal Setting and Monitoring pattern evaluates progress toward a <em>specific, predefined objective</em>. The distinction:</p> <ul> <li>Reflection: “Is this output good?” → improve quality</li> <li>Goal Monitoring: “Has this output met the stated goal criteria?” → achieve the specific target</li> </ul> <p>In practice, both patterns are often combined: the agent uses reflection to improve individual outputs <em>and</em> goal monitoring to determine when those outputs finally satisfy the predefined success criteria.</p> <hr/> <h2 id="watch-the-goal-loop-in-action">Watch the Goal Loop in Action</h2> <div class="goal-demo-wrapper"> <div class="goal-demo-header"> <span class="goal-demo-title">GOAL-DRIVEN CODE AGENT — live iteration demo</span> <button class="goal-demo-btn" id="goalDemoRunBtn">▶ Run Agent</button> </div> <div class="goal-demo-config"> <div class="goal-config-row"> <span class="goal-config-label">GOAL</span> <span class="goal-config-val">Write a Python factorial function: simple, correct, handles edge cases (negative, float), includes docstring</span> </div> <div class="goal-config-row"> <span class="goal-config-label">MAX ITERATIONS</span> <span class="goal-config-val">5</span> </div> </div> <div class="goal-demo-iterations" id="goalIterContainer"></div> <div class="goal-demo-result" id="goalResult" style="display:none"> <span class="goal-result-icon">✓</span> <span>Goal achieved in <strong id="goalIterCount">0</strong> iterations. Final code saved.</span> </div> </div> <style>.goal-demo-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.goal-demo-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.goal-demo-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.goal-demo-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.goal-demo-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.goal-demo-config{padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);display:flex;flex-direction:column;gap:.35rem;background:rgba(128,128,128,0.03)}.goal-config-row{display:flex;align-items:baseline;gap:.75rem}.goal-config-label{font-size:.6rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:#2698ba;flex-shrink:0;min-width:90px}.goal-config-val{font-size:.78rem;color:var(--global-text-color-light)}.goal-demo-iterations{padding:.75rem 1.1rem;display:flex;flex-direction:column;gap:.65rem}.goal-iter{border:1px solid var(--global-divider-color);border-radius:7px;overflow:hidden;animation:goalIterIn .3s ease}@keyframes goalIterIn{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}.goal-iter-header{display:flex;align-items:center;gap:.65rem;padding:.55rem .85rem;background:rgba(128,128,128,0.05);border-bottom:1px solid var(--global-divider-color)}.goal-iter-num{font-family:monospace;font-size:.65rem;font-weight:700;color:var(--global-text-color-light)}.goal-iter-status{font-size:.68rem;font-family:monospace;padding:.1em .45em;border-radius:3px}.goal-iter-status.running{color:#2698ba;border:1px solid rgba(38,152,186,0.3);background:rgba(38,152,186,0.1)}.goal-iter-status.fail{color:#ff6b6b;border:1px solid rgba(255,107,107,0.3);background:rgba(255,107,107,0.1)}.goal-iter-status.pass{color:#4fc97e;border:1px solid rgba(79,201,126,0.3);background:rgba(79,201,126,0.1)}.goal-iter-body{padding:.65rem .85rem;display:flex;flex-direction:column;gap:.4rem}.goal-iter-section{font-size:.62rem;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:var(--global-text-color-light);margin-top:.2rem}.goal-iter-code{background:rgba(0,0,0,0.2);border-radius:5px;padding:.5rem .7rem;font-size:.72rem;font-family:monospace;color:#cdd6f4;line-height:1.6;white-space:pre;overflow-x:auto}.goal-iter-critique{font-size:.78rem;color:var(--global-text-color-light);line-height:1.55}.goal-iter-verdict{font-size:.72rem;font-family:monospace;font-weight:700}.goal-iter-verdict.fail{color:#ff6b6b}.goal-iter-verdict.pass{color:#4fc97e}.goal-demo-result{display:flex;align-items:center;gap:.6rem;padding:.65rem 1.1rem;border-top:1px solid var(--global-divider-color);background:rgba(79,201,126,0.07);font-size:.8rem;color:var(--global-text-color)}.goal-result-icon{color:#4fc97e;font-weight:700;font-size:1rem}</style> <script>
var GOAL_ITERATIONS = [
  {
    code: 'def factorial(n):\n    result = 1\n    for i in range(1, n):\n        result *= i\n    return result',
    critique: 'Issues found: (1) Off-by-one error — range(1, n) excludes n itself. factorial(5) returns 24, not 120. (2) No docstring. (3) No handling for n=0, negative numbers, or float inputs.',
    verdict: false
  },
  {
    code: 'def factorial(n):\n    """Calculate factorial of n."""\n    if n < 0:\n        raise ValueError("n must be non-negative")\n    result = 1\n    for i in range(1, n + 1):\n        result *= i\n    return result',
    critique: 'Off-by-one bug fixed. Docstring added. Negative handling added. Remaining issues: (1) No handling for float inputs — factorial(3.5) would silently return wrong result. (2) Docstring lacks Args/Returns documentation.',
    verdict: false
  },
  {
    code: 'def factorial(n: int) -> int:\n    """\n    Calculate the factorial of a non-negative integer.\n\n    Args:\n        n: A non-negative integer.\n    Returns:\n        The factorial of n (n!). Returns 1 when n is 0.\n    Raises:\n        ValueError: If n is negative or not an integer.\n    """\n    if not isinstance(n, int) or isinstance(n, bool):\n        raise ValueError(f"n must be an integer, got {type(n).__name__}")\n    if n < 0:\n        raise ValueError(f"n must be non-negative, got {n}")\n    if n == 0:\n        return 1\n    result = 1\n    for i in range(1, n + 1):\n        result *= i\n    return result',
    critique: 'All goals met: correct implementation with proper range, comprehensive docstring with Args/Returns/Raises, handles edge cases (negative, float, bool), explicit n=0 case documented.',
    verdict: true
  }
];

document.addEventListener('DOMContentLoaded', function(){
  var runBtn = document.getElementById('goalDemoRunBtn');
  if (!runBtn) return;
  var running = false;

  runBtn.addEventListener('click', async function(){
    if (running) return;
    running = true;
    runBtn.textContent = '⏳ Running…';
    runBtn.disabled = true;
    document.getElementById('goalIterContainer').innerHTML = '';
    document.getElementById('goalResult').style.display = 'none';

    for (var i = 0; i < GOAL_ITERATIONS.length; i++) {
      await new Promise(function(r){ setTimeout(r, 600); });
      var iter = GOAL_ITERATIONS[i];
      var div = document.createElement('div');
      div.className = 'goal-iter';
      div.innerHTML =
        '<div class="goal-iter-header">' +
          '<span class="goal-iter-num">ITERATION ' + (i+1) + '</span>' +
          '<span class="goal-iter-status running">Generating…</span>' +
        '</div>' +
        '<div class="goal-iter-body">' +
          '<div class="goal-iter-section">Generated Code</div>' +
          '<div class="goal-iter-code">' + iter.code.replace(/</g,'&lt;').replace(/>/g,'&gt;') + '</div>' +
        '</div>';
      document.getElementById('goalIterContainer').appendChild(div);

      await new Promise(function(r){ setTimeout(r, 800); });

      // Add critique
      var body = div.querySelector('.goal-iter-body');
      var critiqueEl = document.createElement('div');
      critiqueEl.innerHTML = '<div class="goal-iter-section">LLM Critique</div><div class="goal-iter-critique">' + iter.critique + '</div>';
      body.appendChild(critiqueEl);

      await new Promise(function(r){ setTimeout(r, 600); });

      // Update status + verdict
      var statusEl = div.querySelector('.goal-iter-status');
      var verdictEl = document.createElement('div');
      if (iter.verdict) {
        statusEl.textContent = 'PASS ✓';
        statusEl.className = 'goal-iter-status pass';
        verdictEl.innerHTML = '<div class="goal-iter-verdict pass">Goals met → True. Stopping loop.</div>';
      } else {
        statusEl.textContent = 'FAIL ✗';
        statusEl.className = 'goal-iter-status fail';
        verdictEl.innerHTML = '<div class="goal-iter-verdict fail">Goals not met → False. Preparing next iteration…</div>';
      }
      body.appendChild(verdictEl);

      if (iter.verdict) {
        await new Promise(function(r){ setTimeout(r, 400); });
        document.getElementById('goalIterCount').textContent = (i+1);
        document.getElementById('goalResult').style.display = 'flex';
        break;
      }
    }

    running = false;
    runBtn.textContent = '↺ Replay';
    runBtn.disabled = false;
  });
});
</script> <p>The demo shows three iterations. Notice:</p> <ul> <li><strong>Iteration 1:</strong> Classic off-by-one bug + no docstring + no edge cases → <code class="language-plaintext highlighter-rouge">False</code></li> <li><strong>Iteration 2:</strong> Bug fixed, docstring added, negative handled → but float inputs and docstring completeness still missing → <code class="language-plaintext highlighter-rouge">False</code></li> <li><strong>Iteration 3:</strong> All criteria met → <code class="language-plaintext highlighter-rouge">True</code> → loop exits</li> </ul> <p>This is the goal-setting and monitoring loop in action: generate → judge → refine → judge → success.</p> <hr/> <h2 id="the-code-a-goal-driven-code-generation-agent">The Code: A Goal-Driven Code Generation Agent</h2> <p>Now let’s look at how this pattern is implemented in Python with LangChain and OpenAI.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>
<span class="kn">import</span> <span class="n">random</span>
<span class="kn">import</span> <span class="n">re</span>
<span class="kn">from</span> <span class="n">pathlib</span> <span class="kn">import</span> <span class="n">Path</span>
<span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>
<span class="kn">from</span> <span class="n">dotenv</span> <span class="kn">import</span> <span class="n">load_dotenv</span>

<span class="nf">load_dotenv</span><span class="p">()</span>  <span class="c1"># loads OPENAI_API_KEY from .env file
</span><span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4o</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mf">0.1</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">temperature=0.1</code> instead of 0?</strong> For code generation, you want deterministic, focused output — not creative random variation. But <code class="language-plaintext highlighter-rouge">temperature=0</code> can sometimes be <em>too</em> rigid, producing identical outputs when asked to improve. <code class="language-plaintext highlighter-rouge">0.1</code> adds just enough randomness to explore slightly different approaches on each iteration while remaining focused.</p> </blockquote> <h3 id="setting-up-the-goal">Setting Up the Goal</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">run_code_agent</span><span class="p">(</span><span class="n">use_case</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">goals_input</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">max_iterations</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">5</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="c1"># Parse goals from a comma-separated string into a list
</span>    <span class="n">goals</span> <span class="o">=</span> <span class="p">[</span><span class="n">g</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span> <span class="k">for</span> <span class="n">g</span> <span class="ow">in</span> <span class="n">goals_input</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="sh">"</span><span class="s">,</span><span class="sh">"</span><span class="p">)]</span>

    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">🎯 Use Case: </span><span class="si">{</span><span class="n">use_case</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">🎯 Goals:</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">g</span> <span class="ow">in</span> <span class="n">goals</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  - </span><span class="si">{</span><span class="n">g</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why parse goals from a comma-separated string?</strong> This makes the function easy to call from a command line or web UI: <code class="language-plaintext highlighter-rouge">goals_input = "simple, tested, handles edge cases"</code>. Each goal becomes a separate item in the list. The agent then checks each one individually during evaluation.</p> </blockquote> <blockquote> <p><strong>The goals list is the core of the pattern.</strong> Everything else — the code generation, the critique, the iteration — serves the purpose of achieving every item on this list. If you have 5 goals, the loop continues until all 5 are met or <code class="language-plaintext highlighter-rouge">max_iterations</code> is exhausted.</p> </blockquote> <h3 id="generating-code">Generating Code</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">generate_prompt</span><span class="p">(</span><span class="n">use_case</span><span class="p">,</span> <span class="n">goals</span><span class="p">,</span> <span class="n">previous_code</span><span class="p">,</span> <span class="n">feedback</span><span class="p">):</span>
    <span class="n">goal_str</span> <span class="o">=</span> <span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">- </span><span class="si">{</span><span class="n">g</span><span class="si">}</span><span class="sh">"</span> <span class="k">for</span> <span class="n">g</span> <span class="ow">in</span> <span class="n">goals</span><span class="p">)</span>

    <span class="k">if</span> <span class="ow">not</span> <span class="n">previous_code</span><span class="p">:</span>
        <span class="c1"># First iteration: generate from scratch
</span>        <span class="k">return</span> <span class="sa">f</span><span class="sh">"""</span><span class="s">Write a Python function that solves the following problem:

Problem: </span><span class="si">{</span><span class="n">use_case</span><span class="si">}</span><span class="s">

Your code must meet ALL of these goals:
</span><span class="si">{</span><span class="n">goal_str</span><span class="si">}</span><span class="s">

Respond with ONLY the Python code. No explanations, no markdown fences, just the code.</span><span class="sh">"""</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="c1"># Subsequent iterations: refine based on critique
</span>        <span class="k">return</span> <span class="sa">f</span><span class="sh">"""</span><span class="s">Here is a previous attempt at solving this problem:

Problem: </span><span class="si">{</span><span class="n">use_case</span><span class="si">}</span><span class="s">

Goals to meet:
</span><span class="si">{</span><span class="n">goal_str</span><span class="si">}</span><span class="s">

Previous code:
</span><span class="si">{</span><span class="n">previous_code</span><span class="si">}</span><span class="s">

Critique of previous code:
</span><span class="si">{</span><span class="n">feedback</span><span class="si">}</span><span class="s">

Write an improved version that addresses ALL critique points. Respond with ONLY the Python code.</span><span class="sh">"""</span>
</code></pre></div></div> <blockquote> <p><strong>Why two different prompts (first vs subsequent iterations)?</strong> The first iteration has no prior context — we simply specify the goal and ask for a solution. Subsequent iterations have crucial additional context: the previous attempt and the specific critique of why it failed. Giving the LLM this context dramatically improves the refinement quality — it’s not just told “try again,” it’s told exactly what was wrong and why.</p> </blockquote> <blockquote> <p><strong>“Respond with ONLY the Python code”</strong> — this is critical. Without this instruction, the LLM might respond with: “Sure! Here’s the code: <code class="language-plaintext highlighter-rouge">python def factorial...</code>”. Then your code extraction logic has to parse markdown fences, prose, and potentially multiple code blocks. The explicit instruction eliminates this parsing complexity.</p> </blockquote> <h3 id="the-critique-monitoring-step">The Critique (Monitoring) Step</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_code_feedback</span><span class="p">(</span><span class="n">code</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">goals</span><span class="p">:</span> <span class="nb">list</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">object</span><span class="p">:</span>
    <span class="n">goal_str</span> <span class="o">=</span> <span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">- </span><span class="si">{</span><span class="n">g</span><span class="si">}</span><span class="sh">"</span> <span class="k">for</span> <span class="n">g</span> <span class="ow">in</span> <span class="n">goals</span><span class="p">)</span>
    <span class="n">critique_prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"""</span><span class="s">You are an expert code reviewer.

Review the following code against these goals:
</span><span class="si">{</span><span class="n">goal_str</span><span class="si">}</span><span class="s">

Code to review:
</span><span class="si">{</span><span class="n">code</span><span class="si">}</span><span class="s">

For each goal, state whether it is met and why.
Then provide specific, actionable feedback on any unmet goals.
Be precise — point to exact line numbers and specific issues.</span><span class="sh">"""</span>

    <span class="k">return</span> <span class="n">llm</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">critique_prompt</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>This is the monitoring step.</strong> The same LLM (or a different one) evaluates the generated code against the stated goals. The critique prompt forces the evaluator to go through each goal individually — this is important because a general “is this code good?” question would produce vague feedback. Goal-by-goal evaluation produces specific, actionable critiques that the generator can act on.</p> </blockquote> <h3 id="the-stopping-condition-goals-met-check">The Stopping Condition (Goals Met Check)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">goals_met</span><span class="p">(</span><span class="n">feedback_text</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">goals</span><span class="p">:</span> <span class="nb">list</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">bool</span><span class="p">:</span>
    <span class="c1"># Ask the LLM to make a binary judgment: are ALL goals met?
</span>    <span class="n">check_prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"""</span><span class="s">Given this code review:
</span><span class="si">{</span><span class="n">feedback_text</span><span class="si">}</span><span class="s">

And these goals:
</span><span class="si">{</span><span class="sh">'</span><span class="s">, </span><span class="sh">'</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">goals</span><span class="p">)</span><span class="si">}</span><span class="s">

Answer with a single word: True if ALL goals are fully met, False if any goal is not fully met.</span><span class="sh">"""</span>

    <span class="n">response</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">check_prompt</span><span class="p">)</span>
    <span class="k">return</span> <span class="sh">"</span><span class="s">true</span><span class="sh">"</span> <span class="ow">in</span> <span class="n">response</span><span class="p">.</span><span class="n">content</span><span class="p">.</span><span class="nf">strip</span><span class="p">().</span><span class="nf">lower</span><span class="p">()</span>
</code></pre></div></div> <blockquote> <p><strong>Why ask the LLM for a binary True/False judgment?</strong> Machine-readable output enables programmatic control. The loop condition is <code class="language-plaintext highlighter-rouge">if goals_met(...)</code>. If you asked the LLM to describe whether goals are met in prose, you’d have to parse sentiment and intent from a paragraph — much harder and more error-prone. The explicit <code class="language-plaintext highlighter-rouge">True</code>/<code class="language-plaintext highlighter-rouge">False</code> instruction makes the stopping condition reliable.</p> </blockquote> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">.strip().lower()</code>?</strong> The LLM might output <code class="language-plaintext highlighter-rouge">"True"</code>, <code class="language-plaintext highlighter-rouge">"true"</code>, <code class="language-plaintext highlighter-rouge">"TRUE"</code>, <code class="language-plaintext highlighter-rouge">"True."</code> (with a period), or even <code class="language-plaintext highlighter-rouge">"True, all goals are met."</code> (with a sentence). <code class="language-plaintext highlighter-rouge">"true" in response.content.strip().lower()</code> handles all of these variants safely — it checks if the string “true” appears anywhere in the lowercased response.</p> </blockquote> <blockquote> <p><strong>The limitation of self-evaluation.</strong> When the same LLM that generated the code also judges whether it meets goals, there’s a risk of the judge being too lenient on the generator’s own work. The model might rationalize why a buggy solution actually meets the goals. This is the same cognitive bias problem discussed in Chapter 4 (Reflection). The solution, discussed below, is to use a <em>separate</em> agent for the judging role.</p> </blockquote> <h3 id="the-main-loop">The Main Loop</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="n">previous_code</span> <span class="o">=</span> <span class="sh">""</span>
    <span class="n">feedback</span> <span class="o">=</span> <span class="sh">""</span>

    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_iterations</span><span class="p">):</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">=== 🔁 Iteration </span><span class="si">{</span><span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="si">}</span><span class="s"> of </span><span class="si">{</span><span class="n">max_iterations</span><span class="si">}</span><span class="s"> ===</span><span class="sh">"</span><span class="p">)</span>

        <span class="c1"># STEP 1: Generate (or refine) code
</span>        <span class="n">prompt</span> <span class="o">=</span> <span class="nf">generate_prompt</span><span class="p">(</span><span class="n">use_case</span><span class="p">,</span> <span class="n">goals</span><span class="p">,</span> <span class="n">previous_code</span><span class="p">,</span>
                                 <span class="n">feedback</span> <span class="k">if</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">feedback</span><span class="p">,</span> <span class="nb">str</span><span class="p">)</span> <span class="k">else</span> <span class="n">feedback</span><span class="p">.</span><span class="n">content</span><span class="p">)</span>
        <span class="n">code_response</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
        <span class="n">code</span> <span class="o">=</span> <span class="nf">clean_code_block</span><span class="p">(</span><span class="n">code_response</span><span class="p">.</span><span class="n">content</span><span class="p">.</span><span class="nf">strip</span><span class="p">())</span>

        <span class="c1"># STEP 2: Evaluate (monitoring)
</span>        <span class="n">feedback</span> <span class="o">=</span> <span class="nf">get_code_feedback</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="n">goals</span><span class="p">)</span>
        <span class="n">feedback_text</span> <span class="o">=</span> <span class="n">feedback</span><span class="p">.</span><span class="n">content</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span>

        <span class="c1"># STEP 3: Check stopping condition
</span>        <span class="k">if</span> <span class="nf">goals_met</span><span class="p">(</span><span class="n">feedback_text</span><span class="p">,</span> <span class="n">goals</span><span class="p">):</span>
            <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">✅ All goals met. Stopping.</span><span class="sh">"</span><span class="p">)</span>
            <span class="k">break</span>

        <span class="c1"># STEP 4: Prepare for next iteration (strategy adaptation)
</span>        <span class="n">previous_code</span> <span class="o">=</span> <span class="n">code</span>

    <span class="c1"># Return the final result, save to file
</span>    <span class="n">final_code</span> <span class="o">=</span> <span class="nf">add_comment_header</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="n">use_case</span><span class="p">)</span>
    <span class="k">return</span> <span class="nf">save_code_to_file</span><span class="p">(</span><span class="n">final_code</span><span class="p">,</span> <span class="n">use_case</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>The four-step loop maps directly to the monitoring pattern:</strong> Generate (execute action) → Get feedback (monitor) → Check if goals met (evaluate) → Update previous_code (adapt strategy). This is the fundamental goal monitoring cycle implemented in Python.</p> </blockquote> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">previous_code = code</code> at the end of each iteration.</strong> The next iteration’s generator receives the previous iteration’s output. Each refinement builds on the last — it’s not starting from scratch each time, it’s improving an existing solution. This is more efficient and produces better results than generating independently each time.</p> </blockquote> <blockquote> <p><strong>What if <code class="language-plaintext highlighter-rouge">max_iterations</code> is reached without meeting goals?</strong> The loop exits with <code class="language-plaintext highlighter-rouge">break</code> (goal met) or naturally when <code class="language-plaintext highlighter-rouge">range(max_iterations)</code> is exhausted. In both cases, the final <code class="language-plaintext highlighter-rouge">code</code> variable contains the last generated version. This is saved and returned — the best attempt, even if goals weren’t fully met, is still returned rather than nothing.</p> </blockquote> <h3 id="saving-the-result">Saving the Result</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">save_code_to_file</span><span class="p">(</span><span class="n">code</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">use_case</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="c1"># Generate a short filename from the use case description
</span>    <span class="n">summary_prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Summarize this use case in one lowercase word or phrase, max 10 chars, suitable for a Python filename:</span><span class="se">\n\n</span><span class="si">{</span><span class="n">use_case</span><span class="si">}</span><span class="sh">"</span>
    <span class="n">raw_summary</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">summary_prompt</span><span class="p">).</span><span class="n">content</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span>
    <span class="n">short_name</span> <span class="o">=</span> <span class="n">re</span><span class="p">.</span><span class="nf">sub</span><span class="p">(</span><span class="sa">r</span><span class="sh">"</span><span class="s">[^a-zA-Z0-9_]</span><span class="sh">"</span><span class="p">,</span> <span class="sh">""</span><span class="p">,</span> <span class="n">raw_summary</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sh">"</span><span class="s"> </span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">_</span><span class="sh">"</span><span class="p">).</span><span class="nf">lower</span><span class="p">())[:</span><span class="mi">10</span><span class="p">]</span>

    <span class="c1"># Add random suffix to avoid filename collisions
</span>    <span class="n">random_suffix</span> <span class="o">=</span> <span class="nf">str</span><span class="p">(</span><span class="n">random</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">1000</span><span class="p">,</span> <span class="mi">9999</span><span class="p">))</span>
    <span class="n">filename</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">short_name</span><span class="si">}</span><span class="s">_</span><span class="si">{</span><span class="n">random_suffix</span><span class="si">}</span><span class="s">.py</span><span class="sh">"</span>
    <span class="n">filepath</span> <span class="o">=</span> <span class="n">Path</span><span class="p">.</span><span class="nf">cwd</span><span class="p">()</span> <span class="o">/</span> <span class="n">filename</span>

    <span class="k">with</span> <span class="nf">open</span><span class="p">(</span><span class="n">filepath</span><span class="p">,</span> <span class="sh">"</span><span class="s">w</span><span class="sh">"</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
        <span class="n">f</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="n">code</span><span class="p">)</span>

    <span class="k">return</span> <span class="nf">str</span><span class="p">(</span><span class="n">filepath</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why generate the filename with an LLM?</strong> The use case description might be long and contain special characters. Asking the LLM to summarize it into a valid Python filename identifier is convenient. The <code class="language-plaintext highlighter-rouge">re.sub(r"[^a-zA-Z0-9_]", "")</code> then strips any remaining invalid characters as a safety net.</p> </blockquote> <blockquote> <p><strong>Why add <code class="language-plaintext highlighter-rouge">random_suffix</code>?</strong> If you run the agent multiple times with the same use case, you don’t want the second run to overwrite the first. The random 4-digit suffix ensures unique filenames per run.</p> </blockquote> <hr/> <h2 id="the-critical-limitation-one-llm-both-writes-and-judges">The Critical Limitation: One LLM Both Writes and Judges</h2> <p>The implementation above has an important structural weakness: the same LLM generates the code <em>and</em> evaluates whether it meets the goals. This creates a subtle but significant problem.</p> <p>When a language model generates code, it has already committed to an internal representation of what the code should look like. When that same model then evaluates whether the code meets goals, it reads the code through the lens of its prior commitment. It tends to be more lenient on its own work — recognizing its own intentions and reading them into the code even when they’re not fully implemented.</p> <p>This is exactly the same cognitive bias that makes human code authors poor reviewers of their own code — and exactly why Chapter 4 (Reflection) recommends using a separate Critic agent with a different system prompt.</p> <h3 id="the-multi-agent-solution">The Multi-Agent Solution</h3> <p>A more robust architecture uses specialized agents, each with a dedicated role:</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">MULTI-AGENT GOAL MONITORING — separation of concerns</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:320px;"> <div class="ns-node-title">Goal + Use Case Input</div> <div class="ns-node-sub">SMART goal definition with specific, measurable success criteria</div> </div> <div class="ns-arrow"></div> <div class="ns-row" style="max-width:520px;"> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Peer Programmer</div> <div class="ns-node-sub">Generates code. Focuses entirely on solving the problem. Does not evaluate its own output.</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">Code Reviewer</div> <div class="ns-node-sub">Evaluates code against the stated goals. Returns structured feedback + True/False verdict. Completely separate from the generator.</div> </div> </div> <div class="ns-arrow"></div> <div class="ns-row" style="max-width:520px;"> <div class="ns-node"> <div class="ns-node-title">Test Writer</div> <div class="ns-node-sub">Generates unit tests for the code. Provides objective, executable validation that complements the LLM reviewer's judgment.</div> </div> <div class="ns-node"> <div class="ns-node-title">Documenter</div> <div class="ns-node-sub">Ensures docstrings, comments, and README are complete. Checks documentation goals independently.</div> </div> </div> <div class="ns-arrow"></div> <div class="ns-decision" style="max-width:220px;"> <div class="ns-node-title">All Goals Met?</div> <div class="ns-node-sub">Reviewer + Test Writer both confirm</div> </div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:480px;"> <div class="ns-branch"> <span class="ns-label-red">Not met</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"><div class="ns-node-title">Feedback → Programmer</div></div> </div> <div class="ns-branch"> <span class="ns-label-green">All met</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Final Output</div></div> </div> </div> </div> </div> <p><strong>Why this is better.</strong> Each agent has a single, focused responsibility. The Code Reviewer’s system prompt is entirely dedicated to finding flaws — it has no stake in defending the code it’s reviewing (it didn’t write it). The Test Writer generates executable tests, providing an objective, deterministic validation layer that doesn’t rely on LLM judgment at all. The separation of concerns produces higher-quality, more objective evaluation.</p> <p>This architecture naturally maps to how senior engineering teams work: a developer writes code, a separate reviewer evaluates it against requirements, a QA engineer runs tests. The goal (ship working, documented, tested code) is monitored by multiple independent validators.</p> <hr/> <h2 id="practical-applications">Practical Applications</h2> <div class="goal-usecases-grid"> <div class="goal-uc-card"> <span class="goal-uc-num">01</span> <h4>Customer Support Automation</h4> <p>Goal: "Resolve customer's billing inquiry." Monitor: verify billing change in database, confirm user acknowledgment. Escalate if goal not achievable within 3 tool calls.</p> <span class="goal-uc-domain">Customer Success · SaaS platforms</span> </div> <div class="goal-uc-card"> <span class="goal-uc-num">02</span> <h4>Personalized Learning</h4> <p>Goal: "Student achieves 80%+ accuracy on algebra exercises." Monitor: track quiz scores per topic. Adapt teaching materials when performance falls below threshold.</p> <span class="goal-uc-domain">EdTech · Tutoring systems</span> </div> <div class="goal-uc-card"> <span class="goal-uc-num">03</span> <h4>Project Management</h4> <p>Goal: "Milestone X complete by date Y." Monitor: task completion status, team velocity, open blockers. Flag at-risk milestones before they miss deadlines.</p> <span class="goal-uc-domain">Enterprise · Agile teams</span> </div> <div class="goal-uc-card"> <span class="goal-uc-num">04</span> <h4>Automated Trading</h4> <p>Goal: "Maximize portfolio returns within defined risk tolerance." Monitor: portfolio value, volatility metrics, drawdown percentage. Halt trading if risk thresholds breached.</p> <span class="goal-uc-domain">FinTech · Algorithmic trading</span> </div> <div class="goal-uc-card"> <span class="goal-uc-num">05</span> <h4>Autonomous Vehicles</h4> <p>Goal: "Transport passengers from A to B safely." Monitor: environment (obstacles, signals), vehicle state (speed, fuel), route progress. Replan on deviation or hazard.</p> <span class="goal-uc-domain">Robotics · AV systems</span> </div> <div class="goal-uc-card"> <span class="goal-uc-num">06</span> <h4>Content Moderation</h4> <p>Goal: "Identify and remove harmful content with &lt;2% false positive rate." Monitor: classification confidence, human reviewer override rate. Adjust thresholds to maintain goal metrics.</p> <span class="goal-uc-domain">Trust &amp; Safety · Social platforms</span> </div> </div> <style>.goal-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:.85rem;margin:1.5rem 0}.goal-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.goal-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#4fc97e}.goal-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.goal-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.goal-uc-domain{font-size:.65rem;font-family:monospace;color:#2698ba;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="common-mistakes-when-setting-agent-goals">Common Mistakes When Setting Agent Goals</h2> <p><strong>Mistake 1: Metric misalignment — optimizing for the proxy, not the goal.</strong> You set the goal as “close 50 support tickets per day.” The agent closes tickets by providing generic responses and immediately marking them resolved. Ticket count is high; actual resolution rate is low. Always define goals in terms of the <em>outcome you care about</em>, not the <em>metric that’s easy to measure</em>.</p> <p><strong>Mistake 2: No stopping condition.</strong> The classic infinite loop. An agent tasked with “refine the report until it’s perfect” has no stopping condition. Perfect is never reached. Use explicit bounds: “up to 5 iterations” or “until no improvement is detected over 2 consecutive iterations.”</p> <p><strong>Mistake 3: Self-evaluation by the same LLM that generated the output.</strong> As discussed, the generator is biased toward approving its own work. Use a separate agent with a distinct reviewer persona, or better yet, use an automated test suite that doesn’t involve the LLM at all for the evaluation step.</p> <p><strong>Mistake 4: Too many simultaneous goals.</strong> An agent with 15 goals has a 15-item checklist it must satisfy simultaneously. Each additional goal makes it less likely all are met in any given iteration, and harder to identify which specific goal caused failure. Start with 3-5 well-defined goals. Add more only as you observe the agent consistently meeting the baseline set.</p> <p><strong>Mistake 5: Goals that conflict.</strong> “Maximize response speed” and “maximize response completeness” are in tension. “Minimize API calls” and “gather comprehensive information” conflict. When goals conflict, the agent can’t satisfy all of them simultaneously — it will make arbitrary trade-offs. Explicitly rank goals by priority, or resolve conflicts before handing them to the agent.</p> <p><strong>Mistake 6: No escalation path.</strong> If the goal isn’t achievable (missing tool access, ambiguous requirements, edge case outside training data), the agent loops until max_iterations. Include an explicit escalation: “If goal is not met within 3 iterations, return current best attempt with a description of remaining gaps and what human intervention is needed.”</p> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="goal-summary-card"> <div class="goal-summary-col"> <div class="goal-summary-label">WHAT</div> <p>Equipping agents with explicit, measurable objectives and feedback loops that track progress toward those objectives. The agent doesn't just execute actions — it pursues specific outcomes and self-corrects when off course.</p> </div> <div class="goal-summary-divider"></div> <div class="goal-summary-col"> <div class="goal-summary-label">WHY</div> <p>Reactive agents can't handle multi-step tasks that require sustained pursuit of an outcome. Goal setting and monitoring transforms agents from "answer this question" to "achieve this outcome" — enabling genuinely autonomous operation.</p> </div> <div class="goal-summary-divider"></div> <div class="goal-summary-col"> <div class="goal-summary-label">RULE OF THUMB</div> <p>Use when the agent must execute a multi-step process, adapt to dynamic conditions, and reliably achieve a specific high-level objective without constant human intervention. Always define SMART goals and include an explicit stopping condition.</p> </div> </div> <style>.goal-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.goal-summary-card{flex-direction:column}}.goal-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.goal-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.goal-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.goal-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#4fc97e}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li> <p><strong>Goals transform reactive agents into purposeful ones.</strong> Without goals, agents answer questions. With goals, agents pursue outcomes — a qualitative difference that enables genuine autonomy on complex, multi-step tasks.</p> </li> <li> <p><strong>SMART goals prevent the most common failure modes.</strong> Specific eliminates ambiguity. Measurable enables monitoring. Achievable prevents infinite loops on impossible objectives. Relevant ensures you’re optimizing for what actually matters. Time-bound prevents runaway resource consumption.</p> </li> <li> <p><strong>The monitoring feedback loop is the operational core.</strong> Execute → Evaluate → Adapt is the cycle. Every iteration makes measurable progress toward the goal or reveals that the approach needs to change. Without monitoring, you just have an agent that runs to completion without knowing if it succeeded.</p> </li> <li> <p><strong>Self-evaluation by the generator is biased.</strong> The same LLM that generated an output will be more lenient when judging that output. Use a separate agent with a distinct critic persona, or better, use automated tests that don’t involve the LLM for objective evaluation.</p> </li> <li> <p><strong>True/False stopping conditions are more reliable than prose.</strong> When you need a programmatic stopping condition, extract a binary signal from the LLM rather than parsing sentiment from a paragraph. <code class="language-plaintext highlighter-rouge">"true" in response.lower()</code> is simple, robust, and predictable.</p> </li> <li> <p><strong>Every goal system needs an escalation path.</strong> When the goal isn’t achievable within the iteration budget — due to missing tools, ambiguous requirements, or out-of-distribution inputs — the agent should return its best attempt with an explanation of what’s missing, not silently fail or loop forever.</p> </li> <li> <p><strong>Multi-agent architectures produce more objective monitoring.</strong> Separating the Generator (Peer Programmer), the Evaluator (Code Reviewer), and the Validator (Test Writer) into independent agents with distinct system prompts produces higher-quality, less biased evaluation than self-assessment.</p> </li> </ul> <hr/> <p><em>Next up — Chapter 12: Safety and Guardrails, where we examine how to build agents that are not just capable, but reliably safe — preventing harmful outputs, unauthorized actions, and runaway behavior.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="goal-setting"/><category term="monitoring"/><summary type="html"><![CDATA[Without goals, agents react. With goals, agents pursue. This chapter shows how to give AI agents specific objectives, measurable success criteria, and the feedback loops that keep them on track.]]></summary></entry><entry><title type="html">Chapter 10: Model Context Protocol (MCP)</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/mcp/" rel="alternate" type="text/html" title="Chapter 10: Model Context Protocol (MCP)"/><published>2026-02-10T00:00:00+00:00</published><updated>2026-02-10T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/mcp</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/mcp/"><![CDATA[<h2 id="the-integration-problem">The Integration Problem</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Protocol:</strong> A set of agreed-upon rules that two parties use to communicate. HTTP is a protocol for web browsers and servers. Bluetooth is a protocol for wireless devices. A protocol defines the exact format of messages, the order of communication, and how errors are handled. Without a protocol, two systems might both "speak" but use incompatible formats — like two people trying to have a conversation where one speaks French and the other speaks Japanese.</p> <p style="margin-top:0.5rem"><strong>Client-server architecture:</strong> A model where one program (the server) provides a service, and another program (the client) uses that service. A web browser is a client; the website's computer is a server. The client makes requests; the server fulfills them. One server can serve many clients simultaneously.</p> <p style="margin-top:0.5rem"><strong>API (Application Programming Interface):</strong> A defined way for two programs to communicate. When you use a weather app, it calls a weather service's API to get forecast data. The API specifies exactly what requests you can make and what format the responses will be in.</p> <p style="margin-top:0.5rem"><strong>JSON-RPC:</strong> A protocol for calling functions remotely using JSON (JavaScript Object Notation) as the message format. You send: <code>{"method": "get_weather", "params": {"city": "London"}, "id": 1}</code> and receive back a JSON response. MCP uses JSON-RPC as its underlying communication format.</p> <p style="margin-top:0.5rem"><strong>STDIO (Standard Input/Output):</strong> The basic input and output channels of a Unix/Linux/Mac process. Every program has stdin (where it reads input from), stdout (where it writes output to), and stderr (where it writes errors to). MCP uses STDIO for local communication — the client writes to the server's stdin, the server writes responses to its stdout.</p> <p style="margin-top:0.5rem"><strong>Schema:</strong> A formal, machine-readable description of what data should look like. A JSON schema for a weather API tool might say: "The input must have a 'city' field that is a string, and a 'units' field that is either 'celsius' or 'fahrenheit'." The LLM reads this schema to know how to call the tool correctly.</p> <p style="margin-top:0.5rem"><strong>Interoperability:</strong> The ability of different systems to work together seamlessly. USB is an interoperable standard — any USB cable works with any USB port, regardless of who made either. MCP aims to be the "USB of AI integrations."</p> </div> <p>In <a href="/kohshh-portfolio/blog/2026/tool-use/">Chapter 5</a>, we learned how agents call tools. An agent equipped with a <code class="language-plaintext highlighter-rouge">get_weather</code> function can check the weather. An agent equipped with <code class="language-plaintext highlighter-rouge">send_email</code> can send emails. This works beautifully — for a small, fixed set of tools that you’ve written yourself.</p> <p>But consider what happens as the system grows:</p> <ul> <li>Your company has 40 internal services: CRM, ticketing, HR system, code repository, document store, project management, financial reporting, calendar, messaging…</li> <li>Each service has its own API format, authentication mechanism, and data structure</li> <li>You have 5 different AI applications that need to access subsets of these services</li> <li>Each integration requires custom code: understand the service’s API, write a wrapper function, handle its specific error formats, manage its authentication</li> </ul> <p>That’s potentially <strong>40 × 5 = 200 custom integrations</strong>, each requiring code, testing, and maintenance. When a service updates its API, every integration that touches it breaks. When you add a new AI application, it can’t use any existing integrations — they were all written for the old applications. When you swap the underlying LLM (say, from GPT-4 to Gemini), every tool definition needs to be reformatted for the new provider’s requirements.</p> <p>This is the integration hell that <strong>Model Context Protocol (MCP)</strong> solves. MCP is an open standard, created by Anthropic in 2024, that defines <em>one</em> way for LLMs to communicate with <em>any</em> external system. Write an MCP server once for your CRM — any compliant LLM application can use it. Write an MCP client once in your AI application — it can connect to any MCP server. The integration graph goes from O(applications × services) custom integrations to O(applications + services) standard implementations.</p> <hr/> <h2 id="what-mcp-is-the-universal-adapter">What MCP Is: The Universal Adapter</h2> <p>The best analogy for MCP is the USB standard. Before USB, every device (keyboard, mouse, printer, camera) had its own connector — PS/2, serial, parallel, proprietary. Adding a new device meant finding the right port, often physically impossible if all ports were occupied. USB created a universal connector: one standard port, one standard protocol, any device from any manufacturer.</p> <p>MCP is the USB of AI integrations. Instead of every AI application needing custom code to talk to every service, MCP defines:</p> <ul> <li>A standard way for services to <strong>advertise</strong> their capabilities (what tools, data, and templates they offer)</li> <li>A standard way for AI applications to <strong>discover</strong> those capabilities at runtime</li> <li>A standard way to <strong>request</strong> actions and <strong>receive</strong> results</li> </ul> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">WITHOUT MCP vs WITH MCP — the integration problem and its solution</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-row" style="max-width:560px;align-items:flex-start;gap:1rem;"> <div class="ns-branch" style="flex:1;"> <span class="ns-label-red" style="margin-bottom:0.6rem;">Without MCP — N×M custom integrations</span> <div class="ns-phase" style="gap:0.4rem;"> <div class="ns-phase-title">AI App A</div> <div class="ns-node" style="max-width:none;padding:0.5rem;"><div class="ns-node-title" style="font-size:0.72rem;">custom CRM code</div></div> <div class="ns-node" style="max-width:none;padding:0.5rem;"><div class="ns-node-title" style="font-size:0.72rem;">custom DB code</div></div> <div class="ns-node" style="max-width:none;padding:0.5rem;"><div class="ns-node-title" style="font-size:0.72rem;">custom email code</div></div> </div> <div class="ns-phase" style="gap:0.4rem;margin-top:0.5rem;"> <div class="ns-phase-title">AI App B</div> <div class="ns-node" style="max-width:none;padding:0.5rem;"><div class="ns-node-title" style="font-size:0.72rem;">custom CRM code</div></div> <div class="ns-node" style="max-width:none;padding:0.5rem;"><div class="ns-node-title" style="font-size:0.72rem;">custom DB code</div></div> <div class="ns-node" style="max-width:none;padding:0.5rem;"><div class="ns-node-title" style="font-size:0.72rem;">custom email code</div></div> </div> <div class="ns-node ns-node-red" style="max-width:none;margin-top:0.5rem;"><div class="ns-node-title" style="font-size:0.75rem;">6 integrations for 2 apps + 3 services</div><div class="ns-node-sub">Each one custom. Each one fragile. Each one LLM-specific.</div></div> </div> <div class="ns-branch" style="flex:1;"> <span class="ns-label-green" style="margin-bottom:0.6rem;">With MCP — N+M standard implementations</span> <div class="ns-node ns-node-cyan" style="max-width:none;"><div class="ns-node-title" style="font-size:0.72rem;">AI App A (MCP Client)</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:none;"><div class="ns-node-title" style="font-size:0.72rem;">MCP Standard Interface</div><div class="ns-node-sub">Discovery · Request · Response</div></div> <div class="ns-arrow"></div> <div class="ns-row" style="gap:0.4rem;max-width:none;"> <div class="ns-node ns-node-green" style="padding:0.5rem;"><div class="ns-node-title" style="font-size:0.7rem;">CRM MCP Server</div></div> <div class="ns-node ns-node-green" style="padding:0.5rem;"><div class="ns-node-title" style="font-size:0.7rem;">DB MCP Server</div></div> <div class="ns-node ns-node-green" style="padding:0.5rem;"><div class="ns-node-title" style="font-size:0.7rem;">Email MCP Server</div></div> </div> <div class="ns-node ns-node-green" style="max-width:none;margin-top:0.5rem;"><div class="ns-node-title" style="font-size:0.75rem;">5 implementations for 2 apps + 3 services</div><div class="ns-node-sub">Any app works with any server. Any LLM works.</div></div> </div> </div> </div> </div> <hr/> <h2 id="the-three-mcp-primitives">The Three MCP Primitives</h2> <p>MCP defines exactly three types of things that a server can expose to clients. Understanding each one — and the clear distinction between them — is essential to using MCP correctly.</p> <div class="mcp-primitives-wrapper"> <div class="mcp-prim-tabs"> <button class="mcp-tab active" data-idx="0" onclick="mcpTab(0)">🔧 Tools</button> <button class="mcp-tab" data-idx="1" onclick="mcpTab(1)">📄 Resources</button> <button class="mcp-tab" data-idx="2" onclick="mcpTab(2)">💬 Prompts</button> </div> <div class="mcp-prim-body"> <div class="mcp-prim-content active" id="mcpPrim0"> <div class="mcp-prim-header"> <div class="mcp-prim-icon">🔧</div> <div> <div class="mcp-prim-name">Tools — Actions the Agent Can Take</div> <div class="mcp-prim-sub">Executable functions with side effects</div> </div> </div> <p class="mcp-prim-desc">A <strong>Tool</strong> is a function that the LLM can call to perform an action. Tools have inputs (parameters) and outputs (return values). Critically, tools typically have <em>side effects</em> — they change something in the world: sending an email, updating a database record, creating a file, making a payment, sending a notification.</p> <p class="mcp-prim-desc">The LLM decides when to call a tool based on its description (the docstring) and the current context. The MCP server executes the tool and returns the result.</p> <div class="mcp-prim-examples"> <div class="mcp-eg"><code>send_email(to, subject, body)</code> — sends an actual email</div> <div class="mcp-eg"><code>create_ticket(title, priority, description)</code> — creates a support ticket</div> <div class="mcp-eg"><code>query_database(sql)</code> — executes a SQL query</div> <div class="mcp-eg"><code>search_web(query)</code> — performs a live web search</div> </div> <div class="mcp-prim-key">Key property: Tools are <strong>dynamic and actionable</strong>. They do things. They change state. They return results based on real-time execution.</div> </div> <div class="mcp-prim-content" id="mcpPrim1"> <div class="mcp-prim-header"> <div class="mcp-prim-icon">📄</div> <div> <div class="mcp-prim-name">Resources — Data the Agent Can Read</div> <div class="mcp-prim-sub">Static or slowly-changing data made accessible</div> </div> </div> <p class="mcp-prim-desc">A <strong>Resource</strong> is a piece of data that the LLM can read — a file, a database record, a document, a configuration. Resources are read-only: the LLM (through MCP) can retrieve them, but not modify them directly (that would require a Tool).</p> <p class="mcp-prim-desc">Resources are identified by URIs (Uniform Resource Identifiers — the same concept as URLs in web browsers). The MCP server knows how to fetch and return the content at each URI in a format the LLM can understand.</p> <div class="mcp-prim-examples"> <div class="mcp-eg"><code>file:///reports/q4-2025.md</code> — the text content of a Markdown file</div> <div class="mcp-eg"><code>db://customers/id/12345</code> — a specific customer record</div> <div class="mcp-eg"><code>git://main/src/api.py</code> — the content of a source code file</div> <div class="mcp-eg"><code>config://app/settings</code> — application configuration data</div> </div> <div class="mcp-prim-key">Key property: Resources are <strong>passive and readable</strong>. They expose data. They don't change the world. They must return agent-friendly text (Markdown, plain text, JSON) — not binary formats like PDFs that the LLM can't read.</div> </div> <div class="mcp-prim-content" id="mcpPrim2"> <div class="mcp-prim-header"> <div class="mcp-prim-icon">💬</div> <div> <div class="mcp-prim-name">Prompts — Interaction Templates</div> <div class="mcp-prim-sub">Pre-built prompt templates for structured interactions</div> </div> </div> <p class="mcp-prim-desc">A <strong>Prompt</strong> is a pre-built template that guides the LLM in how to interact effectively with a specific resource or tool. Think of it as a "best practices guide" baked into the server — the server knows how to use its own tools best, and encodes that knowledge as prompt templates.</p> <p class="mcp-prim-desc">For example, a database MCP server might provide a prompt template that says: "When querying customer data, always filter by status='active' first, then limit results to 50, then sort by last_update. Describe any security-sensitive fields as [REDACTED] in your response." This guides the LLM to interact with the database in a safe, efficient way without the LLM having to figure it out from scratch.</p> <div class="mcp-prim-examples"> <div class="mcp-eg">Database query guidelines — how to write safe, efficient SQL through this server</div> <div class="mcp-eg">File system interaction format — how to navigate and read files safely</div> <div class="mcp-eg">Customer service response template — structured format for support tickets</div> <div class="mcp-eg">Code review checklist prompt — systematic questions for reviewing code changes</div> </div> <div class="mcp-prim-key">Key property: Prompts are <strong>instructional templates</strong>. They encode expert knowledge about how to use the server's tools and resources effectively. They make the agent smarter without requiring complex prompt engineering in the client application.</div> </div> </div> </div> <style>.mcp-primitives-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.mcp-prim-tabs{display:flex;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.04)}.mcp-tab{flex:1;padding:.6rem .5rem;font-family:monospace;font-size:.72rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s}.mcp-tab:last-child{border-right:0}.mcp-tab.active{background:rgba(38,152,186,0.1);color:#2698ba;font-weight:700}.mcp-prim-body{padding:1.1rem}.mcp-prim-content{display:none;flex-direction:column;gap:.7rem}.mcp-prim-content.active{display:flex}.mcp-prim-header{display:flex;align-items:center;gap:.75rem}.mcp-prim-icon{font-size:1.4rem;flex-shrink:0}.mcp-prim-name{font-size:.95rem;font-weight:700;color:var(--global-text-color)}.mcp-prim-sub{font-size:.72rem;color:var(--global-text-color-light);font-family:monospace}.mcp-prim-desc{font-size:.85rem;color:var(--global-text-color);line-height:1.65;margin:0}.mcp-prim-examples{display:flex;flex-direction:column;gap:.3rem;background:rgba(0,0,0,0.15);border-radius:6px;padding:.7rem .9rem}.mcp-eg{font-size:.78rem;color:var(--global-text-color-light)}.mcp-eg code{font-size:.78rem;color:#7dcfff}.mcp-prim-key{font-size:.8rem;color:var(--global-text-color);background:rgba(38,152,186,0.07);border-left:3px solid #2698ba;padding:.55rem .8rem;border-radius:0 6px 6px 0;line-height:1.55}</style> <script>
function mcpTab(idx) {
  document.querySelectorAll('.mcp-tab').forEach(function(t){ t.classList.remove('active'); });
  document.querySelectorAll('.mcp-prim-content').forEach(function(c){ c.classList.remove('active'); });
  document.querySelector('.mcp-tab[data-idx="'+idx+'"]').classList.add('active');
  document.getElementById('mcpPrim'+idx).classList.add('active');
}
</script> <hr/> <h2 id="mcp-vs-tool-function-calling-whats-the-difference">MCP vs. Tool Function Calling: What’s the Difference?</h2> <p>If you already have tool function calling (Chapter 5), why do you need MCP? This is the most important conceptual question to answer clearly.</p> <p><strong>Tool function calling</strong> is a <em>mechanism</em> — a specific way for an LLM to trigger the execution of a pre-defined Python function. You write <code class="language-plaintext highlighter-rouge">@tool</code> on a function, bind it to an agent, and the LLM can call it. This works perfectly within one application, with a fixed set of tools, talking to one LLM provider.</p> <p><strong>MCP</strong> is a <em>standard</em> — a universal protocol that defines how LLMs and external systems communicate, regardless of which LLM or which system is involved. It’s not just about calling functions; it’s about a discoverable, provider-agnostic ecosystem where tools can be shared, reused, and composed.</p> <div class="mcp-compare-wrapper"> <div class="mcp-compare-header"> <span class="mcp-compare-title">TOOL FUNCTION CALLING vs MODEL CONTEXT PROTOCOL</span> <span class="mcp-compare-sub">Hover a row to highlight</span> </div> <div class="mcp-compare-table"> <div class="mcp-compare-row mcp-compare-head"> <div class="mcp-col-feature">Feature</div> <div class="mcp-col-tool">Tool Function Calling</div> <div class="mcp-col-mcp">Model Context Protocol (MCP)</div> </div> <div class="mcp-compare-row"> <div class="mcp-col-feature">Standardization</div> <div class="mcp-col-tool mcp-col-neg">Proprietary — format differs across LLM providers. OpenAI, Anthropic, Google all have different schemas.</div> <div class="mcp-col-mcp mcp-col-pos">Open standard — same protocol works with any compliant LLM or tool.</div> </div> <div class="mcp-compare-row"> <div class="mcp-col-feature">Scope</div> <div class="mcp-col-tool mcp-col-neg">One-to-one: LLM calls a specific pre-defined function in the application's code.</div> <div class="mcp-col-mcp mcp-col-pos">Many-to-many: any MCP client can discover and use any MCP server's capabilities.</div> </div> <div class="mcp-compare-row"> <div class="mcp-col-feature">Discovery</div> <div class="mcp-col-tool mcp-col-neg">Static — tools must be explicitly listed in the LLM's context at startup. No runtime discovery.</div> <div class="mcp-col-mcp mcp-col-pos">Dynamic — agent queries the server at runtime: "What can you do?" Server responds with current capabilities.</div> </div> <div class="mcp-compare-row"> <div class="mcp-col-feature">Reusability</div> <div class="mcp-col-tool mcp-col-neg">Tightly coupled to the specific app and LLM. Reusing in another app requires rewriting.</div> <div class="mcp-col-mcp mcp-col-pos">MCP servers are standalone services. Any compliant client can use them without modification.</div> </div> <div class="mcp-compare-row"> <div class="mcp-col-feature">Architecture</div> <div class="mcp-col-tool mcp-col-neg">Monolithic — tools live inside the application. Scaling means scaling the whole app.</div> <div class="mcp-col-mcp mcp-col-pos">Distributed — servers run independently. Scale each one separately. Add new ones without redeploying anything else.</div> </div> <div class="mcp-compare-row"> <div class="mcp-col-feature">When to use</div> <div class="mcp-col-tool">Simple, fixed tool sets. Prototypes. Single-LLM applications.</div> <div class="mcp-col-mcp">Enterprise systems. Multi-LLM environments. Tool ecosystems. When interoperability matters.</div> </div> </div> </div> <style>.mcp-compare-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.mcp-compare-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.4rem}.mcp-compare-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.mcp-compare-sub{font-size:.65rem;color:var(--global-text-color-light)}.mcp-compare-table{display:flex;flex-direction:column}.mcp-compare-row{display:grid;grid-template-columns:140px 1fr 1fr;border-bottom:1px solid var(--global-divider-color);transition:background .15s}.mcp-compare-row:last-child{border-bottom:0}.mcp-compare-row:not(.mcp-compare-head):hover{background:rgba(128,128,128,0.04)}.mcp-compare-head{background:rgba(128,128,128,0.06);font-size:.7rem;font-weight:700;letter-spacing:.07em;text-transform:uppercase;color:var(--global-text-color-light)}.mcp-col-feature{padding:.7rem .9rem;font-size:.78rem;font-weight:600;color:var(--global-text-color);border-right:1px solid var(--global-divider-color)}.mcp-col-tool,.mcp-col-mcp{padding:.7rem .9rem;font-size:.78rem;line-height:1.5;border-right:1px solid var(--global-divider-color)}.mcp-col-mcp{border-right:0}.mcp-col-neg{color:var(--global-text-color-light)}.mcp-col-pos{color:var(--global-text-color)}@media(max-width:580px){.mcp-compare-row{grid-template-columns:1fr}.mcp-col-feature{border-right:0;font-weight:700;background:rgba(128,128,128,0.06)}}</style> <blockquote> <p><strong>The practical distinction:</strong> If you’re building a personal project with 3 fixed tools, function calling is simpler and completely sufficient. If you’re building an enterprise platform where 50 services need to be accessible to 10 different AI applications, MCP is the right architecture — you write 60 implementations instead of 500.</p> </blockquote> <hr/> <h2 id="the-mcp-architecture-how-it-works">The MCP Architecture: How It Works</h2> <p>MCP defines a precise four-component architecture with a specific five-step interaction flow. Understanding each component and each step is essential to implementing MCP correctly.</p> <h3 id="the-four-components">The Four Components</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">MCP ARCHITECTURE — four components, one standardized protocol</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-row" style="max-width:540px;align-items:stretch;gap:0.5rem;"> <div class="ns-node ns-node-purple" style="flex:1;"> <div class="ns-node-title">LLM</div> <div class="ns-node-sub">The reasoning core. Processes user requests, decides what information is needed or what action to take. Does NOT directly call tools — communicates through the MCP Client.</div> </div> <div style="display:flex;align-items:center;color:#4a5a6a;font-size:1.2rem;flex-shrink:0;">⟵→</div> <div class="ns-node ns-node-cyan" style="flex:1;"> <div class="ns-node-title">MCP Client</div> <div class="ns-node-sub">The intermediary layer. Translates the LLM's intent into standard MCP requests. Discovers available servers, manages connections, handles authentication. Usually part of the host application.</div> </div> </div> <div style="display:flex;justify-content:center;padding:0.4rem 0;color:#4a5a6a;font-size:1.3rem;">↕</div> <div class="ns-row" style="max-width:540px;align-items:stretch;gap:0.5rem;"> <div class="ns-node ns-node-amber" style="flex:1;"> <div class="ns-node-title">MCP Server</div> <div class="ns-node-sub">The capability gateway. Exposes tools, resources, and prompts in MCP-standard format. Authenticates clients, validates requests, executes actions. Handles one specific domain (e.g., email, database, file system).</div> </div> <div style="display:flex;align-items:center;color:#4a5a6a;font-size:1.2rem;flex-shrink:0;">⟵→</div> <div class="ns-node ns-node-green" style="flex:1;"> <div class="ns-node-title">3P Service / External System</div> <div class="ns-node-sub">The actual external system: a database, an email API, a file system, a SaaS platform. The MCP server manages the technical details of interacting with it. The LLM never talks to this directly.</div> </div> </div> </div> </div> <p><strong>Why this four-layer design?</strong> Each layer has a clear responsibility:</p> <ul> <li>The <strong>LLM</strong> focuses purely on reasoning — it doesn’t need to know how to authenticate with Salesforce or format a Postgres query</li> <li>The <strong>MCP Client</strong> focuses purely on protocol — translating LLM intent to standard requests and routing them to the right server</li> <li>The <strong>MCP Server</strong> focuses purely on the domain — it knows everything about the external system and handles its quirks</li> <li>The <strong>external system</strong> stays unchanged — you wrap it with an MCP server without modifying the underlying service</li> </ul> <p>This is called <strong>layered architecture</strong> — a fundamental pattern in software engineering where each layer only communicates with the layer directly adjacent to it.</p> <h3 id="the-five-step-interaction-flow">The Five-Step Interaction Flow</h3> <div class="mcp-flow-wrapper"> <div class="mcp-flow-header"> <span class="mcp-flow-title">MCP INTERACTION FLOW — step through a complete request cycle</span> <div class="mcp-flow-nav"> <button class="mcp-flow-btn" id="mcpPrev" onclick="mcpStep(-1)">← Prev</button> <span class="mcp-flow-counter" id="mcpCounter">Step 1 / 5</span> <button class="mcp-flow-btn" id="mcpNext" onclick="mcpStep(1)">Next →</button> </div> </div> <div class="mcp-flow-body"> <div class="mcp-flow-track"> <div class="mcp-flow-step active" id="mcpFlowStep0"> <div class="mcp-flow-num">01</div> <div class="mcp-flow-name">Discovery</div> <div class="mcp-flow-detail">The MCP Client asks the MCP Server: "What can you do?" The server responds with a <strong>capabilities manifest</strong> — a complete list of all available tools (with their names, descriptions, and parameter schemas), resources (with their URIs), and prompt templates. This happens automatically when the client first connects. The LLM then receives this manifest and knows what tools exist without you having to hardcode them.</div> <div class="mcp-flow-msg"><code>Client → Server: {"method": "tools/list", "id": 1}</code><br/><code>Server → Client: {"tools": [{"name": "send_email", "description": "...", "inputSchema": {...}}, ...]}</code></div> </div> <div class="mcp-flow-step" id="mcpFlowStep1"> <div class="mcp-flow-num">02</div> <div class="mcp-flow-name">Request Formulation</div> <div class="mcp-flow-detail">The user says "Send an email to Alice about tomorrow's meeting." The LLM reads the server's capability manifest (which includes the <code>send_email</code> tool with its description and parameters) and reasons: "I need to call send_email with these specific arguments." It formulates a structured request specifying exactly which tool to use and what arguments to pass. This reasoning happens inside the LLM based on the tool descriptions.</div> <div class="mcp-flow-msg"><em>LLM reasoning: "User wants to send email. I have send_email tool. I'll call it with to='alice@company.com', subject='Meeting tomorrow', body='...'"</em></div> </div> <div class="mcp-flow-step" id="mcpFlowStep2"> <div class="mcp-flow-num">03</div> <div class="mcp-flow-name">Client Communication</div> <div class="mcp-flow-detail">The MCP Client takes the LLM's formulated tool call and wraps it in a standardized JSON-RPC message. It sends this over the transport layer — either STDIO (if the server is a local process on the same machine) or HTTP/SSE (if the server is remote). The client handles authentication, message formatting, and connection management so the LLM doesn't have to think about any of these details.</div> <div class="mcp-flow-msg"><code>Client → Server: {"jsonrpc": "2.0", "method": "tools/call", "params": {"name": "send_email", "arguments": {"to": "alice@company.com", "subject": "Meeting tomorrow", "body": "..."}}, "id": 2}</code></div> </div> <div class="mcp-flow-step" id="mcpFlowStep3"> <div class="mcp-flow-num">04</div> <div class="mcp-flow-name">Server Execution</div> <div class="mcp-flow-detail">The MCP Server receives the request. It: (1) authenticates the client — is this client authorized to call this tool? (2) validates the parameters — are all required fields present? Are the types correct? (3) executes the action — calls the email service's API with the provided arguments. The server handles all the domain-specific complexity: OAuth tokens, retry logic, rate limiting, error translation.</div> <div class="mcp-flow-msg"><em>Server internal: Validates params → Calls email API → email_api.send(to='alice@company.com', ...) → success</em></div> </div> <div class="mcp-flow-step" id="mcpFlowStep4"> <div class="mcp-flow-num">05</div> <div class="mcp-flow-name">Response &amp; Context Update</div> <div class="mcp-flow-detail">The server returns a standardized response — success or error with structured details. The MCP Client passes this back to the LLM as new context. The LLM now knows the email was sent (or exactly why it failed) and can either respond to the user or continue with the next step in a multi-step workflow. The result becomes part of the conversation history, enabling coherent multi-turn interactions.</div> <div class="mcp-flow-msg"><code>Server → Client: {"result": {"content": [{"type": "text", "text": "Email sent successfully. Message ID: msg_abc123"}]}, "id": 2}</code></div> </div> </div> </div> </div> <style>.mcp-flow-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.mcp-flow-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.5rem}.mcp-flow-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.mcp-flow-nav{display:flex;align-items:center;gap:.75rem}.mcp-flow-btn{font-family:monospace;font-size:.72rem;padding:.25rem .7rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer}.mcp-flow-btn:hover{background:rgba(38,152,186,0.12);border-color:#2698ba;color:#2698ba}.mcp-flow-counter{font-family:monospace;font-size:.72rem;color:var(--global-text-color-light);min-width:70px;text-align:center}.mcp-flow-body{padding:1.1rem}.mcp-flow-track{position:relative}.mcp-flow-step{display:none;flex-direction:column;gap:.65rem;animation:mcpFadeIn .2s ease}.mcp-flow-step.active{display:flex}@keyframes mcpFadeIn{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}.mcp-flow-num{font-family:monospace;font-size:.6rem;font-weight:700;letter-spacing:.12em;color:#2698ba}.mcp-flow-name{font-size:1rem;font-weight:700;color:var(--global-text-color)}.mcp-flow-detail{font-size:.85rem;color:var(--global-text-color);line-height:1.65}.mcp-flow-msg{background:rgba(0,0,0,0.2);border-radius:6px;padding:.65rem .85rem;font-size:.74rem;color:#7dcfff;font-family:monospace;line-height:1.55;border:1px solid var(--global-divider-color)}.mcp-flow-msg em{color:var(--global-text-color-light);font-style:italic}</style> <script>
var mcpCurrentStep = 0;
var mcpTotalSteps = 5;
function mcpStep(dir) {
  document.getElementById('mcpFlowStep'+mcpCurrentStep).classList.remove('active');
  mcpCurrentStep = Math.max(0, Math.min(mcpTotalSteps-1, mcpCurrentStep + dir));
  document.getElementById('mcpFlowStep'+mcpCurrentStep).classList.add('active');
  document.getElementById('mcpCounter').textContent = 'Step ' + (mcpCurrentStep+1) + ' / ' + mcpTotalSteps;
  document.getElementById('mcpPrev').disabled = mcpCurrentStep === 0;
  document.getElementById('mcpNext').disabled = mcpCurrentStep === mcpTotalSteps-1;
}
</script> <hr/> <h2 id="transport-mechanisms-how-the-messages-actually-travel">Transport Mechanisms: How the Messages Actually Travel</h2> <p>MCP supports two transport mechanisms. The choice determines where the MCP server runs and how the client connects to it.</p> <div class="mcp-transport-grid"> <div class="mcp-transport-card"> <div class="mcp-transport-title">STDIO — Local Process Communication</div> <div class="mcp-transport-sub">For servers running on the same machine as the agent</div> <p>The MCP client starts the server as a subprocess (a child process). They communicate by reading and writing to standard input/output streams — the most fundamental Unix communication mechanism. The client writes JSON-RPC messages to the server's stdin; the server writes responses to its stdout.</p> <p>This is exactly what happens with the filesystem server example: <code>npx @modelcontextprotocol/server-filesystem /path/to/folder</code> starts a Node.js process, and the MCP client communicates with it via STDIO.</p> <p><strong>Advantages:</strong> Zero network overhead. Extremely fast. No authentication needed (same machine, same trust boundary). Data never leaves your machine — ideal for sensitive data.</p> <p><strong>Disadvantages:</strong> Can't be shared across machines or teams. Server lifecycle tied to client lifecycle. Can't scale horizontally.</p> <div class="mcp-transport-code"><code>StdioServerParameters(command='npx', args=['@modelcontextprotocol/server-filesystem', '/path'])</code></div> </div> <div class="mcp-transport-card"> <div class="mcp-transport-title">HTTP + SSE — Remote Communication</div> <div class="mcp-transport-sub">For servers running on separate machines or shared across teams</div> <p>The MCP server runs as a web service. Clients connect to it via HTTP (for sending requests) and Server-Sent Events (SSE, for receiving streaming responses). SSE is a web standard that lets a server push data to clients over a persistent HTTP connection — useful for long-running tool executions where the response might come in chunks.</p> <p>This is what FastMCP uses when you run <code>mcp_server.run(transport="http", host="127.0.0.1", port=8000)</code>. Any HTTP client that speaks MCP can connect to this server.</p> <p><strong>Advantages:</strong> Accessible from any machine on the network. Can be shared across teams and applications. Can be scaled independently. Can have its own authentication (API keys, OAuth).</p> <p><strong>Disadvantages:</strong> Network latency. Requires authentication. Data travels over the network — security considerations for sensitive data.</p> <div class="mcp-transport-code"><code>HttpServerParameters(url="http://localhost:8000")</code></div> </div> </div> <style>.mcp-transport-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:.85rem;margin:1.75rem 0}.mcp-transport-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1.1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.55rem}.mcp-transport-title{font-size:.8rem;font-weight:700;color:#2698ba}.mcp-transport-sub{font-size:.68rem;font-family:monospace;color:var(--global-text-color-light)}.mcp-transport-card p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:0}.mcp-transport-code{background:rgba(0,0,0,0.2);border-radius:5px;padding:.5rem .65rem;font-size:.72rem;color:#7dcfff;font-family:monospace;margin-top:auto}</style> <hr/> <h2 id="important-caveats-mcp-is-not-magic">Important Caveats: MCP Is Not Magic</h2> <p>Before diving into implementation, there are two critical limitations of MCP that the documentation often glosses over. Understanding these prevents costly architectural mistakes.</p> <h3 id="caveat-1-mcp-wrapping-a-bad-api-produces-a-bad-agent-integration">Caveat 1: MCP Wrapping a Bad API Produces a Bad Agent Integration</h3> <p>MCP is a standardized <em>communication</em> layer — it doesn’t improve the underlying API it wraps. If your ticketing system’s API only lets you retrieve tickets one at a time, an MCP server wrapping that API still only lets the agent retrieve tickets one at a time. The agent will be slow, expensive, and potentially inaccurate at scale.</p> <p><strong>Before building an MCP server, audit the underlying API for agent-friendliness:</strong></p> <ul> <li>Does it support filtering? (The agent should be able to say “get me high-priority tickets” rather than “get all tickets, the agent will filter”)</li> <li>Does it support pagination with reasonable page sizes?</li> <li>Does it support batch operations where sequential access would be too slow?</li> <li>Does it return aggregates where individual-record access would require too many calls?</li> </ul> <p>If the answer to any of these is “no,” the right solution is to improve the underlying API <em>before</em> wrapping it with MCP. MCP should be the last mile of standardization, not a band-aid over a poorly designed API.</p> <h3 id="caveat-2-mcp-requires-agent-readable-data-formats">Caveat 2: MCP Requires Agent-Readable Data Formats</h3> <p>MCP exposes data to LLMs — and LLMs can only read text. If your MCP resource returns a PDF file, the LLM cannot read it. If your resource returns a binary database blob, the LLM cannot process it. MCP itself does not perform format conversion — that’s your server’s responsibility.</p> <p><strong>The golden rule:</strong> Every resource endpoint in your MCP server must return text that a human could read and understand: plain text, Markdown, JSON, CSV, HTML. Never return binary formats (PDF, DOCX, images, compressed files) directly as resource content.</p> <p>If your underlying system stores PDFs, your MCP server should extract the text content (using a library like PyPDF2 or Marker) and return <em>that</em> to the LLM. If your system stores images, your MCP server should return a text description or metadata. The server is responsible for the translation — the LLM cannot do it.</p> <hr/> <h2 id="hands-on-filesystem-agent-with-adk--mcp">Hands-On: Filesystem Agent with ADK + MCP</h2> <p>Let’s build a real MCP integration: an ADK agent that can navigate and read your local file system through an MCP server.</p> <h3 id="step-1-understanding-the-components">Step 1: Understanding the Components</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">os</span>
<span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LlmAgent</span>
<span class="kn">from</span> <span class="n">google.adk.tools.mcp_tool.mcp_toolset</span> <span class="kn">import</span> <span class="n">MCPToolset</span><span class="p">,</span> <span class="n">StdioServerParameters</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">MCPToolset</code></strong>: This is ADK’s MCP client implementation. It manages the connection to an MCP server, handles the discovery protocol (asking the server what tools it offers), and translates ADK/LLM tool calls into properly formatted MCP requests. You use it as a “tool” in the agent’s tool list — but instead of a single function, it gives the agent access to everything the MCP server exposes.</p> </blockquote> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">StdioServerParameters</code></strong>: This tells MCPToolset how to start the MCP server process and communicate with it via STDIO. You provide the <code class="language-plaintext highlighter-rouge">command</code> (what to run) and <code class="language-plaintext highlighter-rouge">args</code> (the arguments to pass).</p> </blockquote> <h3 id="step-2-building-the-path-safely">Step 2: Building the Path Safely</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Build a reliable absolute path — critical for MCP servers
</span><span class="n">TARGET_FOLDER_PATH</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span>
    <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">dirname</span><span class="p">(</span><span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">abspath</span><span class="p">(</span><span class="n">__file__</span><span class="p">)),</span>  <span class="c1"># directory containing agent.py
</span>    <span class="sh">"</span><span class="s">mcp_managed_files</span><span class="sh">"</span>                           <span class="c1"># folder name
</span><span class="p">)</span>

<span class="c1"># Create the folder if it doesn't exist yet
</span><span class="n">os</span><span class="p">.</span><span class="nf">makedirs</span><span class="p">(</span><span class="n">TARGET_FOLDER_PATH</span><span class="p">,</span> <span class="n">exist_ok</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">os.path.abspath(__file__)</code>?</strong> The MCP filesystem server requires an <em>absolute</em> path — a full path like <code class="language-plaintext highlighter-rouge">/Users/kohsheen/projects/mcp_managed_files</code>, not a relative path like <code class="language-plaintext highlighter-rouge">./mcp_managed_files</code>. <code class="language-plaintext highlighter-rouge">__file__</code> gives you the current script’s path, <code class="language-plaintext highlighter-rouge">os.path.abspath</code> converts it to absolute, and <code class="language-plaintext highlighter-rouge">os.path.dirname</code> gets just the directory part. The <code class="language-plaintext highlighter-rouge">os.path.join</code> then appends <code class="language-plaintext highlighter-rouge">mcp_managed_files</code> to create the full target path.</p> </blockquote> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">exist_ok=True</code>?</strong> <code class="language-plaintext highlighter-rouge">os.makedirs</code> creates the directory and all missing parent directories. Without <code class="language-plaintext highlighter-rouge">exist_ok=True</code>, it would raise <code class="language-plaintext highlighter-rouge">FileExistsError</code> if the directory already exists. With it, the call is idempotent — safe to run multiple times.</p> </blockquote> <h3 id="step-3-configuring-the-agent-with-mcptoolset">Step 3: Configuring the Agent with MCPToolset</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">root_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">'</span><span class="s">gemini-2.0-flash</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">'</span><span class="s">filesystem_assistant_agent</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="p">(</span>
        <span class="sh">'</span><span class="s">Help the user manage their files. You can list files, read files, and write files. </span><span class="sh">'</span>
        <span class="sa">f</span><span class="sh">'</span><span class="s">You are operating in the following directory: </span><span class="si">{</span><span class="n">TARGET_FOLDER_PATH</span><span class="si">}</span><span class="sh">'</span>
    <span class="p">),</span>
    <span class="n">tools</span> <span class="o">=</span> <span class="p">[</span>
        <span class="nc">MCPToolset</span><span class="p">(</span>
            <span class="n">connection_params</span> <span class="o">=</span> <span class="nc">StdioServerParameters</span><span class="p">(</span>
                <span class="n">command</span> <span class="o">=</span> <span class="sh">'</span><span class="s">npx</span><span class="sh">'</span><span class="p">,</span>         <span class="c1"># use npx to run the server package
</span>                <span class="n">args</span>    <span class="o">=</span> <span class="p">[</span>
                    <span class="sh">'</span><span class="s">-y</span><span class="sh">'</span><span class="p">,</span>                                           <span class="c1"># auto-confirm npm install
</span>                    <span class="sh">'</span><span class="s">@modelcontextprotocol/server-filesystem</span><span class="sh">'</span><span class="p">,</span>      <span class="c1"># the MCP server package
</span>                    <span class="n">TARGET_FOLDER_PATH</span><span class="p">,</span>                            <span class="c1"># the allowed directory
</span>                <span class="p">],</span>
            <span class="p">),</span>
            <span class="c1"># tool_filter=['list_directory', 'read_file']  # Optional: restrict available tools
</span>        <span class="p">)</span>
    <span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>What does <code class="language-plaintext highlighter-rouge">npx</code> do?</strong> <code class="language-plaintext highlighter-rouge">npx</code> (Node Package Execute) is a tool that runs Node.js packages <em>without</em> installing them globally. When you run <code class="language-plaintext highlighter-rouge">npx @modelcontextprotocol/server-filesystem</code>, it downloads the package from npm (the Node.js package registry) if needed, then runs it. This is why you need Node.js installed — many community MCP servers are distributed as npm packages.</p> </blockquote> <blockquote> <p><strong>What does <code class="language-plaintext highlighter-rouge">@modelcontextprotocol/server-filesystem</code> do?</strong> This is an official MCP server that exposes filesystem operations as MCP tools. It provides tools like: <code class="language-plaintext highlighter-rouge">list_directory</code> (list files in a folder), <code class="language-plaintext highlighter-rouge">read_file</code> (read a file’s content), <code class="language-plaintext highlighter-rouge">write_file</code> (write to a file), <code class="language-plaintext highlighter-rouge">create_directory</code>, <code class="language-plaintext highlighter-rouge">move_file</code>, etc. By passing <code class="language-plaintext highlighter-rouge">TARGET_FOLDER_PATH</code> as the argument, you restrict the server to only operate within that directory — a critical security measure.</p> </blockquote> <blockquote> <p><strong>What is <code class="language-plaintext highlighter-rouge">tool_filter</code>?</strong> MCPToolset will expose <em>all</em> tools the server offers unless you restrict it with <code class="language-plaintext highlighter-rouge">tool_filter</code>. For a filesystem server, you might only want the agent to be able to <em>read</em> files (not write them) in a particular context. <code class="language-plaintext highlighter-rouge">tool_filter=['list_directory', 'read_file']</code> ensures only those two tools are passed to the LLM.</p> </blockquote> <blockquote> <p><strong>What is <code class="language-plaintext highlighter-rouge">MCPToolset</code> doing internally?</strong> When the agent is initialized, <code class="language-plaintext highlighter-rouge">MCPToolset</code> starts the subprocess (the npm package), sends the MCP discovery request (<code class="language-plaintext highlighter-rouge">tools/list</code>), receives the list of available tools with their schemas, and makes those tools available to the LLM. Every time the LLM wants to use a tool, <code class="language-plaintext highlighter-rouge">MCPToolset</code> sends a <code class="language-plaintext highlighter-rouge">tools/call</code> request to the subprocess and returns the result.</p> </blockquote> <h3 id="step-4-alternative-transport--python3-server">Step 4: Alternative Transport — Python3 Server</h3> <p>Instead of an npm package, you can run a Python MCP server:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">connection_params</span> <span class="o">=</span> <span class="nc">StdioServerParameters</span><span class="p">(</span>
    <span class="n">command</span> <span class="o">=</span> <span class="sh">'</span><span class="s">python3</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">args</span>    <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">./agent/mcp_server.py</span><span class="sh">'</span><span class="p">],</span>
    <span class="n">env</span>     <span class="o">=</span> <span class="p">{</span>
        <span class="sh">'</span><span class="s">SERVICE_ACCOUNT_PATH</span><span class="sh">'</span><span class="p">:</span> <span class="n">SERVICE_ACCOUNT_PATH</span><span class="p">,</span>
        <span class="sh">'</span><span class="s">DRIVE_FOLDER_ID</span><span class="sh">'</span><span class="p">:</span>      <span class="n">DRIVE_FOLDER_ID</span><span class="p">,</span>
    <span class="p">}</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why pass <code class="language-plaintext highlighter-rouge">env</code>?</strong> The subprocess (the MCP server) runs in its own process with its own environment variables. If your server needs credentials (a Google service account path, an API key, a folder ID), you pass them via the <code class="language-plaintext highlighter-rouge">env</code> dictionary. This is the secure way to pass credentials — not hardcoding them in the server’s source code.</p> </blockquote> <h3 id="step-5-alternative-transport--uvx-for-python-packages">Step 5: Alternative Transport — uvx for Python Packages</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">connection_params</span> <span class="o">=</span> <span class="nc">StdioServerParameters</span><span class="p">(</span>
    <span class="n">command</span> <span class="o">=</span> <span class="sh">'</span><span class="s">uvx</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">args</span>    <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">mcp-google-sheets@latest</span><span class="sh">'</span><span class="p">],</span>
    <span class="n">env</span>     <span class="o">=</span> <span class="p">{</span>
        <span class="sh">'</span><span class="s">SERVICE_ACCOUNT_PATH</span><span class="sh">'</span><span class="p">:</span> <span class="n">SERVICE_ACCOUNT_PATH</span><span class="p">,</span>
        <span class="sh">'</span><span class="s">DRIVE_FOLDER_ID</span><span class="sh">'</span><span class="p">:</span>      <span class="n">DRIVE_FOLDER_ID</span><span class="p">,</span>
    <span class="p">}</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>What is <code class="language-plaintext highlighter-rouge">uvx</code>?</strong> <code class="language-plaintext highlighter-rouge">uvx</code> is to Python packages what <code class="language-plaintext highlighter-rouge">npx</code> is to Node.js packages — it runs Python packages in temporary isolated environments without permanent installation. It uses <code class="language-plaintext highlighter-rouge">uv</code> (an extremely fast Python package manager) under the hood. <code class="language-plaintext highlighter-rouge">mcp-google-sheets@latest</code> is a PyPI package that exposes Google Sheets operations as MCP tools. The <code class="language-plaintext highlighter-rouge">@latest</code> suffix ensures you always get the current version.</p> </blockquote> <hr/> <h2 id="building-an-mcp-server-with-fastmcp">Building an MCP Server with FastMCP</h2> <p>FastMCP is a Python framework that dramatically simplifies building MCP servers. While the raw MCP protocol requires significant boilerplate code (handling JSON-RPC framing, schema generation, transport initialization), FastMCP lets you define tools with Python decorators and handles all the protocol complexity automatically.</p> <h3 id="why-fastmcp-exists">Why FastMCP Exists</h3> <p>The raw MCP SDK requires you to manually write JSON schemas for each tool, implement the full JSON-RPC server loop, handle transport initialization, and register tools explicitly. FastMCP reduces this to a decorator:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@mcp_server.tool</span>
<span class="k">def</span> <span class="nf">my_function</span><span class="p">(</span><span class="n">param</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Description for the LLM.</span><span class="sh">"""</span>
    <span class="k">return</span> <span class="n">result</span>
</code></pre></div></div> <p>FastMCP reads the type hints (<code class="language-plaintext highlighter-rouge">param: str</code>), docstring, and function name to automatically generate the complete JSON schema. What would take 50 lines of boilerplate becomes 8 lines.</p> <h3 id="building-a-server">Building a Server</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">fastmcp</span> <span class="kn">import</span> <span class="n">FastMCP</span>

<span class="c1"># Initialize the server instance
</span><span class="n">mcp_server</span> <span class="o">=</span> <span class="nc">FastMCP</span><span class="p">()</span>
</code></pre></div></div> <blockquote> <p><strong>What does <code class="language-plaintext highlighter-rouge">FastMCP()</code> create?</strong> A server object that handles all MCP protocol details: accepting connections, responding to capability discovery requests, routing tool calls to the right Python functions, serializing responses as valid MCP messages.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@mcp_server.tool</span>
<span class="k">def</span> <span class="nf">greet</span><span class="p">(</span><span class="n">name</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Generates a personalized greeting for a person.
    Use this when the user asks to greet someone or say hello to a person.

    Args:
        name: The name of the person to greet. Should be their first name.
    Returns:
        A friendly greeting string.
    </span><span class="sh">"""</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Hello, </span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="s">! Nice to meet you.</span><span class="sh">"</span>
</code></pre></div></div> <blockquote> <p><strong>What does <code class="language-plaintext highlighter-rouge">@mcp_server.tool</code> do?</strong> This is a Python decorator that registers the function as an MCP tool. FastMCP: (1) reads the function’s name (<code class="language-plaintext highlighter-rouge">greet</code>) and uses it as the tool name, (2) reads the type hint <code class="language-plaintext highlighter-rouge">name: str</code> and generates the JSON schema <code class="language-plaintext highlighter-rouge">{"type": "object", "properties": {"name": {"type": "string"}}, "required": ["name"]}</code>, (3) reads the docstring and uses it as the tool description that the LLM reads to decide when to call this tool. Every time an MCP client calls the <code class="language-plaintext highlighter-rouge">greet</code> tool, FastMCP calls this Python function with the provided arguments and returns the result.</p> </blockquote> <blockquote> <p><strong>Why is the docstring so important?</strong> The docstring is what the LLM reads to decide <em>when</em> to call this tool. Without “Use this when the user asks to greet someone,” the LLM might not know when greet is relevant. A good tool docstring answers: what does this do, when should it be called, what are the exact input requirements, and what does it return.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">mcp_server</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span>
        <span class="n">transport</span> <span class="o">=</span> <span class="sh">"</span><span class="s">http</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">host</span>      <span class="o">=</span> <span class="sh">"</span><span class="s">127.0.0.1</span><span class="sh">"</span><span class="p">,</span>   <span class="c1"># localhost only — not exposed to network
</span>        <span class="n">port</span>      <span class="o">=</span> <span class="mi">8000</span>
    <span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">transport="http"</code></strong>: Starts the server with HTTP+SSE transport, making it accessible via HTTP. The alternative is <code class="language-plaintext highlighter-rouge">transport="stdio"</code> for STDIO-based communication (when the server is started as a subprocess). For development and testing, HTTP is more convenient because you can test it with curl or a web browser.</p> </blockquote> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">host="127.0.0.1"</code></strong>: Binds to localhost only. The server is only accessible from the same machine. Never use <code class="language-plaintext highlighter-rouge">"0.0.0.0"</code> in development — that would expose the server to your entire network.</p> </blockquote> <h3 id="consuming-the-fastmcp-server-with-an-adk-agent">Consuming the FastMCP Server with an ADK Agent</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.tools.mcp_tool.mcp_toolset</span> <span class="kn">import</span> <span class="n">MCPToolset</span><span class="p">,</span> <span class="n">HttpServerParameters</span>

<span class="n">FASTMCP_SERVER_URL</span> <span class="o">=</span> <span class="sh">"</span><span class="s">http://localhost:8000</span><span class="sh">"</span>

<span class="n">root_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">'</span><span class="s">gemini-2.0-flash</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">'</span><span class="s">fastmcp_greeter_agent</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">'</span><span class="s">You are a friendly assistant that can greet people by name. Use the </span><span class="sh">"</span><span class="s">greet</span><span class="sh">"</span><span class="s"> tool.</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">tools</span> <span class="o">=</span> <span class="p">[</span>
        <span class="nc">MCPToolset</span><span class="p">(</span>
            <span class="n">connection_params</span> <span class="o">=</span> <span class="nc">HttpServerParameters</span><span class="p">(</span>
                <span class="n">url</span> <span class="o">=</span> <span class="n">FASTMCP_SERVER_URL</span><span class="p">,</span>
            <span class="p">),</span>
            <span class="n">tool_filter</span> <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">greet</span><span class="sh">'</span><span class="p">]</span>   <span class="c1"># only expose the greet tool to the LLM
</span>        <span class="p">)</span>
    <span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">HttpServerParameters(url=FASTMCP_SERVER_URL)</code></strong>: Instead of starting a subprocess (STDIO), this connects to an already-running server at the specified URL. The <code class="language-plaintext highlighter-rouge">MCPToolset</code> sends an HTTP request to discover capabilities, then makes HTTP calls for each tool invocation.</p> </blockquote> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">tool_filter=['greet']</code></strong>: Even though the FastMCP server might expose multiple tools in the future, we only want to give this specific agent access to the <code class="language-plaintext highlighter-rouge">greet</code> tool. This is the principle of <strong>least privilege</strong> — give components only the access they actually need, nothing more.</p> </blockquote> <h3 id="the-complete-picture">The Complete Picture</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User: "Greet John Doe"
  ↓
LLM (Gemini): reads tool manifest, sees greet tool with description
  ↓  
LLM decides: "I should call greet with name='John Doe'"
  ↓
MCPToolset (HTTP client): POST http://localhost:8000 → {"method": "tools/call", "params": {"name": "greet", "arguments": {"name": "John Doe"}}}
  ↓
FastMCP server: routes to greet() function, executes greet("John Doe")
  ↓
Python function: returns "Hello, John Doe! Nice to meet you."
  ↓
FastMCP server: serializes as MCP response → {"result": {"content": [{"type": "text", "text": "Hello, John Doe! Nice to meet you."}]}}
  ↓
MCPToolset: returns result to LLM as new context
  ↓
LLM: formulates final response to user
  ↓
User: "Hello, John Doe! Nice to meet you."
</code></pre></div></div> <hr/> <h2 id="nine-practical-applications-of-mcp">Nine Practical Applications of MCP</h2> <div class="mcp-usecases-grid"> <div class="mcp-uc-card"> <span class="mcp-uc-num">01</span> <h4>Database Integration</h4> <p>LLMs query structured data in natural language. The MCP server translates to SQL, runs the query, and returns results as readable text. Example: "Show me all customers who haven't ordered in 6 months" → SQL → formatted table.</p> <span class="mcp-uc-tool">MCP Toolbox for Databases → BigQuery, Postgres, MySQL</span> </div> <div class="mcp-uc-card"> <span class="mcp-uc-num">02</span> <h4>Generative Media Orchestration</h4> <p>Agents coordinate multi-modal content creation: generate an image (Imagen), create a voiceover (Chirp 3 HD), compose background music (Lyria), and compile into a video (Veo) — all through MCP tools.</p> <span class="mcp-uc-tool">MCP Tools for Genmedia → Imagen, Veo, Chirp 3 HD, Lyria</span> </div> <div class="mcp-uc-card"> <span class="mcp-uc-num">03</span> <h4>External API Interaction</h4> <p>Any external API can be wrapped in an MCP server: weather, stock prices, CRM systems, ticketing systems, payment processors. The agent interacts with all of them through the same standard protocol.</p> <span class="mcp-uc-tool">Custom MCP servers wrapping any REST API</span> </div> <div class="mcp-uc-card"> <span class="mcp-uc-num">04</span> <h4>Intelligent Document Search</h4> <p>Instead of returning entire documents, an MCP resource server extracts the specific clause, figure, or statement that answers the user's question. The LLM's reasoning capability enables precision retrieval beyond keyword search.</p> <span class="mcp-uc-tool">Document store MCP server + LLM reasoning</span> </div> <div class="mcp-uc-card"> <span class="mcp-uc-num">05</span> <h4>Complex Workflow Orchestration</h4> <p>Multi-step workflows across multiple systems: retrieve customer data → generate personalized content → draft tailored email → schedule send → log in CRM. Each step is a different MCP server, coordinated by one agent.</p> <span class="mcp-uc-tool">Multiple MCP servers + agent orchestration</span> </div> <div class="mcp-uc-card"> <span class="mcp-uc-num">06</span> <h4>Legacy System Modernization</h4> <p>Wrap existing legacy systems in MCP servers without rewriting them. The MCP layer makes a 1990s mainframe accessible to a 2025 LLM application. The legacy system runs unchanged; the MCP server translates.</p> <span class="mcp-uc-tool">MCP wrapper → any legacy API</span> </div> <div class="mcp-uc-card"> <span class="mcp-uc-num">07</span> <h4>IoT Device Control</h4> <p>Natural language control of physical devices: "Turn off the lights in the conference room" → MCP tool call → smart building API → device command. Agents can monitor and control entire IoT ecosystems.</p> <span class="mcp-uc-tool">IoT platform MCP server → physical devices</span> </div> <div class="mcp-uc-card"> <span class="mcp-uc-num">08</span> <h4>Financial Services Automation</h4> <p>Agents analyze market data, execute trades, generate personalized financial advice, and automate regulatory reporting — all through MCP connections to financial data sources and trading platforms.</p> <span class="mcp-uc-tool">Financial data MCP servers + compliance tools</span> </div> <div class="mcp-uc-card"> <span class="mcp-uc-num">09</span> <h4>Custom Internal Tool Ecosystems</h4> <p>Organizations build MCP servers for proprietary internal systems (code repos, incident management, deployment pipelines) and make them available to any AI application in the company — once, reused everywhere.</p> <span class="mcp-uc-tool">FastMCP → internal systems → company-wide reuse</span> </div> </div> <style>.mcp-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:.85rem;margin:1.5rem 0}.mcp-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.mcp-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#2698ba}.mcp-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.mcp-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.mcp-uc-tool{font-size:.65rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="common-mistakes-when-implementing-mcp">Common Mistakes When Implementing MCP</h2> <p><strong>Mistake 1: Returning binary formats from resource endpoints.</strong> The single most common mistake. Your document store contains PDFs. You create a resource endpoint that returns the raw PDF bytes. The LLM receives binary garbage. Instead: extract text from PDFs (use <code class="language-plaintext highlighter-rouge">pymupdf</code>, <code class="language-plaintext highlighter-rouge">marker</code>, or <code class="language-plaintext highlighter-rouge">pdfplumber</code>), return Markdown or plain text. Always ask: “Can a human read this response in a text editor?” If no, your MCP server needs to convert it.</p> <p><strong>Mistake 2: Wrapping bad APIs without improving them.</strong> You wrap a ticketing API that only supports retrieving one ticket at a time. The agent needs to summarize 200 high-priority tickets. It makes 200 sequential API calls — slow, expensive, often hitting rate limits. Instead: improve the underlying API to support filtering (<code class="language-plaintext highlighter-rouge">?status=high_priority&amp;limit=50</code>) before wrapping it. MCP is a presentation layer, not a performance layer.</p> <p><strong>Mistake 3: No tool_filter — exposing too many tools.</strong> A filesystem MCP server exposes <code class="language-plaintext highlighter-rouge">list_directory</code>, <code class="language-plaintext highlighter-rouge">read_file</code>, <code class="language-plaintext highlighter-rouge">write_file</code>, <code class="language-plaintext highlighter-rouge">delete_file</code>, <code class="language-plaintext highlighter-rouge">create_directory</code>, and <code class="language-plaintext highlighter-rouge">move_file</code>. In a read-only agent, you expose all of them. The LLM, trying to be helpful, accidentally deletes a file. Always apply <code class="language-plaintext highlighter-rouge">tool_filter</code> to restrict the agent to only the tools it actually needs.</p> <p><strong>Mistake 4: Using relative paths with STDIO servers.</strong> The STDIO server runs as a subprocess with its own working directory — often different from your agent script’s directory. Relative paths like <code class="language-plaintext highlighter-rouge">./data</code> resolve to different locations in the subprocess. Always use absolute paths when configuring STDIO-based MCP servers.</p> <p><strong>Mistake 5: Starting the FastMCP server on 0.0.0.0 in development.</strong> <code class="language-plaintext highlighter-rouge">host="0.0.0.0"</code> binds to all network interfaces — your MCP server becomes accessible to every device on your local network, potentially exposing sensitive tools. Always use <code class="language-plaintext highlighter-rouge">host="127.0.0.1"</code> for local development. Use proper authentication and access controls for remote deployment.</p> <p><strong>Mistake 6: Ignoring authentication.</strong> A production MCP server without authentication is an unauthenticated API endpoint that any program on the network can call. Implement API key verification or OAuth before deploying any MCP server that handles sensitive data or performs write operations.</p> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="mcp-summary-card"> <div class="mcp-summary-col"> <div class="mcp-summary-label">WHAT</div> <p>An open standard that defines how LLMs and external systems communicate — replacing N×M custom integrations with N+M standard implementations. MCP servers expose tools (actions), resources (data), and prompts (templates). MCP clients connect to any server through a standard protocol.</p> </div> <div class="mcp-summary-divider"></div> <div class="mcp-summary-col"> <div class="mcp-summary-label">WHY</div> <p>Custom tool integrations are brittle, non-reusable, and LLM-provider-specific. MCP creates an ecosystem where a tool built once works with any compliant LLM. Discovery is dynamic — agents learn what servers can do at runtime without being redeployed when capabilities change.</p> </div> <div class="mcp-summary-divider"></div> <div class="mcp-summary-col"> <div class="mcp-summary-label">RULE OF THUMB</div> <p>Use direct function calling for small, fixed tool sets in single applications. Use MCP when tools need to be shared across multiple AI applications, when you want LLM-agnostic integrations, or when you're building an enterprise system that will evolve over time.</p> </div> </div> <style>.mcp-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.mcp-summary-card{flex-direction:column}}.mcp-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.mcp-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.mcp-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.mcp-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li> <p><strong>MCP is a protocol, not a product.</strong> It defines the <em>rules</em> for how LLMs and external systems communicate. Like HTTP enables any browser to talk to any web server, MCP enables any compliant LLM client to talk to any compliant MCP server.</p> </li> <li> <p><strong>Three primitives, one standard.</strong> Every MCP server exposes some combination of: Tools (actions that change the world), Resources (data that can be read), and Prompts (templates that guide the LLM). Understand which category each capability belongs to before designing your server.</p> </li> <li> <p><strong>Dynamic discovery is MCP’s superpower.</strong> The LLM doesn’t need to know what tools exist at startup. It queries the MCP server at runtime — “what can you do?” — and the server responds with its current capabilities. Add new tools to the server and all connected agents immediately have access, without redeployment.</p> </li> <li> <p><strong>STDIO for local, HTTP+SSE for remote.</strong> Local servers (same machine) use STDIO — fast, simple, secure. Remote servers (across a network) use HTTP + Server-Sent Events — accessible, scalable, requires authentication.</p> </li> <li> <p><strong>MCPToolset in ADK is your client implementation.</strong> It manages the connection lifecycle, handles the discovery protocol, and makes MCP server tools available to the LLM exactly like any other ADK tool. The LLM can’t tell the difference between a locally-defined function and an MCP-connected tool.</p> </li> <li> <p><strong>FastMCP removes boilerplate.</strong> The decorator <code class="language-plaintext highlighter-rouge">@mcp_server.tool</code> converts any Python function into a fully compliant MCP tool — complete with JSON schema generation from type hints, docstring-based descriptions, and automatic protocol handling.</p> </li> <li> <p><strong>MCP is only as good as the underlying API.</strong> Wrapping a poorly designed API in MCP produces a poorly designed MCP server. Always improve the underlying API (add filtering, batch operations, agent-friendly response formats) before wrapping it. MCP standardizes communication — it doesn’t fix bad design.</p> </li> <li> <p><strong>Return text, always.</strong> Every resource and tool response must return agent-readable text. Binary formats (PDFs, images, compressed files) must be converted by the MCP server before being returned to the LLM. The server is responsible for the translation — not the LLM, not the client.</p> </li> </ul> <hr/> <p><em>Next up — Chapter 11: Goal Setting and Monitoring, where agents don’t just react to inputs but actively pursue defined objectives and measure their own progress.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="mcp"/><category term="model-context-protocol"/><summary type="html"><![CDATA[Every tool integration so far required custom code. MCP changes that — a universal standard that lets any LLM connect to any tool, database, or service without bespoke glue code for each pair.]]></summary></entry><entry><title type="html">Chapter 9: Learning and Adaptation</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/learning-adaptation/" rel="alternate" type="text/html" title="Chapter 9: Learning and Adaptation"/><published>2026-02-06T00:00:00+00:00</published><updated>2026-02-06T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/learning-adaptation</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/learning-adaptation/"><![CDATA[<h2 id="the-problem-with-static-agents">The Problem with Static Agents</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Training vs Inference:</strong> "Training" is when an AI model learns from data — this is the expensive, time-consuming phase where the model's parameters (weights) are updated. "Inference" is when a trained model is used to answer questions — this is fast and cheap. Most deployed agents only do inference. Learning and adaptation are about bringing some form of training into the deployed system.</p> <p style="margin-top:0.5rem"><strong>Parameters/Weights:</strong> The numbers inside a neural network that determine how it responds to inputs. GPT-4 has hundreds of billions of parameters. Training changes these numbers. Inference uses them without changing them.</p> <p style="margin-top:0.5rem"><strong>Policy:</strong> In reinforcement learning, the "policy" is the agent's strategy — the rule that maps situations (states) to actions. "When I see X, I do Y" is a policy. Learning in RL means finding a better policy.</p> <p style="margin-top:0.5rem"><strong>Reward function:</strong> A mathematical signal that tells an RL agent whether its action was good (+reward) or bad (-penalty). The agent learns to maximize cumulative reward over time. Designing the reward function well is one of the hardest parts of RL.</p> <p style="margin-top:0.5rem"><strong>Benchmark:</strong> A standardized test suite used to measure an AI system's performance. Examples: HumanEval (coding), MMLU (knowledge), SWE-bench (software engineering). Benchmarks provide comparable, reproducible measurements of capability.</p> <p style="margin-top:0.5rem"><strong>Fine-tuning:</strong> Taking a pre-trained model (like GPT-4) and continuing to train it on a smaller, specific dataset to specialize its behavior. Like taking a medical school graduate and doing a residency in cardiology — the base knowledge stays, domain-specific patterns are reinforced.</p> </div> <p>Every pattern in this series so far — chaining, routing, parallelization, reflection, tool use, planning, multi-agent, memory — shares a fundamental assumption: the agent’s core behavior stays constant. You design the system, deploy it, and it runs. If it makes errors, you as the developer must find them, diagnose them, and manually update the prompts or code.</p> <p>This is called a <strong>static agent</strong> — one that executes without improving.</p> <p>Static agents fail in three important ways:</p> <p><strong>They can’t handle distribution shift.</strong> When the environment changes — user behavior shifts, new topics emerge, the API they rely on changes its output format — static agents degrade silently. They keep running the old strategy in a world that no longer matches the world they were designed for.</p> <p><strong>They can’t personalize.</strong> Every user gets the same agent behavior. A trading bot that works well for a risk-tolerant investor behaves identically for a risk-averse retiree. A customer service agent that works well for technical questions handles casual questions with the same approach. Without learning, there is no adaptation to individual users.</p> <p><strong>They can’t improve from their own mistakes.</strong> A static agent that makes the same error 1,000 times will make it 1,001 times. There is no mechanism to recognize the pattern of failure and correct it.</p> <p><strong>Learning and adaptation</strong> are the patterns that solve all three of these problems. An agent that learns from experience — whether that means updating its parameters, refining its prompts, modifying its code, or building up a richer knowledge base — can improve autonomously without constant manual intervention.</p> <p>This chapter covers the complete spectrum: from classical machine learning approaches (reinforcement learning, supervised learning) to the frontier of agents that rewrite their own code and discover algorithms better than anything humans have designed.</p> <hr/> <h2 id="the-six-ways-agents-learn">The Six Ways Agents Learn</h2> <p>Not all learning is the same. Depending on what data is available, what the agent is trying to optimize, and how it interacts with its environment, different learning paradigms apply. Here are the six most relevant for agentic AI systems:</p> <div class="learn-types-wrapper"> <div class="learn-types-tabs"> <button class="lt-tab active" data-idx="0" onclick="ltSelect(0)">Reinforcement</button> <button class="lt-tab" data-idx="1" onclick="ltSelect(1)">Supervised</button> <button class="lt-tab" data-idx="2" onclick="ltSelect(2)">Unsupervised</button> <button class="lt-tab" data-idx="3" onclick="ltSelect(3)">Few/Zero-Shot</button> <button class="lt-tab" data-idx="4" onclick="ltSelect(4)">Online</button> <button class="lt-tab" data-idx="5" onclick="ltSelect(5)">Memory-Based</button> </div> <div class="learn-types-body" id="ltBody"> <div class="lt-content"> <div class="lt-left"> <div class="lt-name" id="ltName">Reinforcement Learning</div> <div class="lt-desc" id="ltDesc">The agent tries actions in an environment and receives rewards (positive) or penalties (negative) based on the outcome. Over many trials, it learns which actions lead to the best cumulative reward. No teacher provides labeled correct answers — the agent learns entirely from the feedback signal of consequences.</div> <div class="lt-detail" id="ltDetail"><strong>How it learns:</strong> Through trial and error, guided by a reward function. The agent explores actions, observes outcomes, and updates its policy to favor high-reward actions.</div> <div class="lt-when" id="ltWhen"><strong>When to use:</strong> Sequential decision-making where you can define a reward function. Robot control, game playing, resource allocation, trading strategies.</div> <div class="lt-example" id="ltExample">Real example: AlphaGo learned to play Go by playing millions of games against itself, receiving +1 for a win and -1 for a loss. No human demonstrated moves — it discovered superhuman strategies through RL.</div> </div> <div class="lt-right"> <div class="lt-visual" id="ltVisual"> <div class="lt-vis-item lt-vis-agent">Agent</div> <div class="lt-vis-arrow">→ action →</div> <div class="lt-vis-item lt-vis-env">Environment</div> <div class="lt-vis-arrow lt-vis-arrow-down">↓ reward + new state ↓</div> <div class="lt-vis-item lt-vis-policy">Update Policy</div> </div> </div> </div> </div> </div> <style>.learn-types-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.learn-types-tabs{display:flex;overflow-x:auto;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.04)}.lt-tab{flex-shrink:0;padding:.5rem .85rem;font-family:monospace;font-size:.68rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s}.lt-tab:last-child{border-right:0}.lt-tab.active{background:rgba(38,152,186,0.1);color:#2698ba;font-weight:700}.learn-types-body{padding:1.1rem}.lt-content{display:flex;gap:1.5rem;flex-wrap:wrap}.lt-left{flex:2;min-width:240px;display:flex;flex-direction:column;gap:.65rem}.lt-right{flex:1;min-width:160px;display:flex;align-items:center;justify-content:center}.lt-name{font-size:1rem;font-weight:700;color:var(--global-text-color)}.lt-desc{font-size:.83rem;color:var(--global-text-color);line-height:1.65}.lt-detail,.lt-when{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6}.lt-example{font-size:.78rem;color:#4fc97e;background:rgba(79,201,126,0.07);border-left:3px solid #4fc97e;padding:.5rem .75rem;border-radius:0 6px 6px 0;line-height:1.55}.lt-visual{display:flex;flex-direction:column;align-items:center;gap:.35rem;font-family:monospace;font-size:.72rem}.lt-vis-item{padding:.45rem .75rem;border-radius:6px;font-weight:700}.lt-vis-agent{background:rgba(38,152,186,0.15);color:#2698ba;border:1px solid rgba(38,152,186,0.3)}.lt-vis-env{background:rgba(201,122,242,0.12);color:#c97af2;border:1px solid rgba(201,122,242,0.3)}.lt-vis-policy{background:rgba(79,201,126,0.12);color:#4fc97e;border:1px solid rgba(79,201,126,0.3)}.lt-vis-arrow{color:var(--global-text-color-light);font-size:.7rem}.lt-vis-arrow-down{text-align:center}</style> <script>
var LT_DATA = [
  {
    name: "Reinforcement Learning",
    desc: "The agent tries actions in an environment and receives rewards (positive) or penalties (negative) based on the outcome. Over many trials, it learns which actions lead to the best cumulative reward. No teacher provides labeled correct answers — the agent learns entirely from the feedback signal of consequences.",
    detail: "<strong>How it learns:</strong> Through trial and error, guided by a reward function. The agent explores actions, observes outcomes, and updates its policy to favor high-reward actions.",
    when: "<strong>When to use:</strong> Sequential decision-making where you can define a reward function. Robot control, game playing, resource allocation, trading strategies.",
    example: "Real example: AlphaGo learned to play Go by playing millions of games against itself, receiving +1 for a win and -1 for a loss. No human demonstrated moves — it discovered superhuman strategies through RL.",
    visual: '<div class="lt-vis-item lt-vis-agent">Agent</div><div class="lt-vis-arrow">→ action →</div><div class="lt-vis-item lt-vis-env">Environment</div><div class="lt-vis-arrow">↓ reward + new state</div><div class="lt-vis-item lt-vis-policy">Update Policy</div>'
  },
  {
    name: "Supervised Learning",
    desc: "The agent learns from a labeled dataset where every input has a known correct output. A teacher provides thousands of (input, correct_answer) pairs, and the model learns the mapping. After training, it can predict outputs for inputs it has never seen before.",
    detail: "<strong>How it learns:</strong> By minimizing the error between its predictions and the correct labels across the training dataset. Uses backpropagation to adjust weights.",
    when: "<strong>When to use:</strong> When you have labeled data. Email classification, image recognition, sentiment analysis, spam detection, medical diagnosis from records.",
    example: "Real example: Gmail's spam filter was trained on millions of (email, spam/not-spam) labeled pairs. It learned what spam looks like and generalizes to new, never-before-seen spam emails.",
    visual: '<div class="lt-vis-item lt-vis-agent">Training Data</div><div class="lt-vis-arrow">↓ (input, label) pairs</div><div class="lt-vis-item lt-vis-env">Model trains</div><div class="lt-vis-arrow">↓ minimize error</div><div class="lt-vis-item lt-vis-policy">Learned Mapping</div>'
  },
  {
    name: "Unsupervised Learning",
    desc: "The agent finds hidden structure and patterns in data without any labels — no teacher, no correct answers. It discovers clusters, anomalies, and underlying representations on its own by identifying regularities in the data.",
    detail: "<strong>How it learns:</strong> By finding patterns that minimize some structural objective — grouping similar items (clustering), reducing dimensionality while preserving information (autoencoders), or detecting outliers (anomaly detection).",
    when: "<strong>When to use:</strong> When you have lots of data but no labels. Customer segmentation, anomaly detection, data exploration, recommendation systems, topic modeling.",
    example: "Real example: Spotify's music recommendation system uses unsupervised clustering to find groups of songs with similar acoustic features — it discovers musical genres without being told what a genre is.",
    visual: '<div class="lt-vis-item lt-vis-agent">Unlabeled Data</div><div class="lt-vis-arrow">↓ no labels</div><div class="lt-vis-item lt-vis-env">Find structure</div><div class="lt-vis-arrow">↓ patterns emerge</div><div class="lt-vis-item lt-vis-policy">Clusters / Insights</div>'
  },
  {
    name: "Few-Shot / Zero-Shot (LLM)",
    desc: "LLMs can adapt to new tasks with just a few examples (few-shot) or even zero examples (zero-shot) because they were trained on enormous amounts of diverse data. You don't retrain the model — you just describe the task in the prompt and the model generalizes from its training.",
    detail: "<strong>How it works:</strong> The LLM's pre-training on billions of text examples means it has seen many task formats. Providing 1-5 examples in the prompt activates the right pattern. Zero-shot relies on the model inferring the task purely from description.",
    when: "<strong>When to use:</strong> When you need rapid adaptation to new tasks without training data or compute. Novel text classification, translation to rare languages, domain-specific formatting, specialized analysis.",
    example: "Real example: GPT-4 can translate English to Swahili zero-shot despite Swahili being underrepresented in training data — it generalizes linguistic patterns from other languages it learned well.",
    visual: '<div class="lt-vis-item lt-vis-agent">Task Description</div><div class="lt-vis-arrow">+ 0-5 examples</div><div class="lt-vis-item lt-vis-env">LLM Pattern Match</div><div class="lt-vis-arrow">↓ generalize</div><div class="lt-vis-item lt-vis-policy">Task Completed</div>'
  },
  {
    name: "Online Learning",
    desc: "The agent continuously updates its knowledge with each new piece of data, rather than training once on a fixed dataset. Every new observation potentially changes the model. The agent is always adapting in real time as the world changes around it.",
    detail: "<strong>How it learns:</strong> After each new observation or small batch, the model parameters are immediately updated. No separate training phase — learning and deployment happen simultaneously.",
    when: "<strong>When to use:</strong> When the data distribution changes over time and you need immediate adaptation. Financial markets, click-through rate prediction, fraud detection, news recommendation.",
    example: "Real example: YouTube's recommendation algorithm is retrained continuously as new videos are uploaded and user behavior changes. A video trending today affects recommendations within hours.",
    visual: '<div class="lt-vis-item lt-vis-agent">New Data Arrives</div><div class="lt-vis-arrow">↓ immediately</div><div class="lt-vis-item lt-vis-env">Update Model</div><div class="lt-vis-arrow">↓ live</div><div class="lt-vis-item lt-vis-policy">Adapted Behavior</div>'
  },
  {
    name: "Memory-Based Learning",
    desc: "The agent stores specific past experiences and retrieves them when facing similar situations. Instead of compressing everything into parameters, it keeps an explicit memory bank and uses similarity matching to find relevant past cases. Often combined with RAG (Retrieval Augmented Generation).",
    detail: "<strong>How it learns:</strong> By accumulating experience in an external memory. When a new situation arises, it retrieves the K most similar past cases and uses them to inform its current decision — like a doctor who remembers similar past patients.",
    when: "<strong>When to use:</strong> When rare but important cases should never be forgotten. Customer support (past tickets), medical diagnosis (case history), legal research (precedents), personalized assistants.",
    example: "Real example: GitHub Copilot retrieves similar code snippets from its training corpus that match the current coding context — a form of memory-based retrieval to suggest relevant completions.",
    visual: '<div class="lt-vis-item lt-vis-agent">New Situation</div><div class="lt-vis-arrow">↓ semantic search</div><div class="lt-vis-item lt-vis-env">Memory Bank</div><div class="lt-vis-arrow">↓ K similar cases</div><div class="lt-vis-item lt-vis-policy">Informed Decision</div>'
  }
];

function ltSelect(idx) {
  document.querySelectorAll('.lt-tab').forEach(function(t){ t.classList.remove('active'); });
  document.querySelector('[data-idx="'+idx+'"]').classList.add('active');
  var d = LT_DATA[idx];
  document.getElementById('ltName').textContent = d.name;
  document.getElementById('ltDesc').textContent = d.desc;
  document.getElementById('ltDetail').innerHTML = d.detail;
  document.getElementById('ltWhen').innerHTML = d.when;
  document.getElementById('ltExample').textContent = d.example;
  document.getElementById('ltVisual').innerHTML = d.visual;
}
document.addEventListener('DOMContentLoaded', function(){ ltSelect(0); });
</script> <hr/> <h2 id="ppo-the-algorithm-behind-agent-training">PPO: The Algorithm Behind Agent Training</h2> <p>Proximal Policy Optimization (PPO) is the reinforcement learning algorithm that trained ChatGPT’s conversational behavior, powers OpenAI Five (the Dota-playing AI), and underlies most modern RL-trained AI systems. Understanding it is essential for understanding how agents learn from feedback.</p> <h3 id="what-problem-ppo-solves">What Problem PPO Solves</h3> <p>Before PPO, the standard RL approach was simple: try an action, observe the reward, update the policy to make that action more likely if the reward was positive. Repeat millions of times.</p> <p>The problem: <strong>large, unstable updates</strong>. If you update the policy too aggressively based on one batch of experience, you can “fall off a cliff” — the policy changes so dramatically that the agent now performs <em>worse</em> than before, and it’s very hard to recover. This is called <strong>catastrophic forgetting</strong> or <strong>policy collapse</strong>.</p> <p>Imagine teaching a dog a trick. If you reward it so enthusiastically that it changes its entire behavior pattern at once — not just “sit” but its whole approach to interacting with you — you might accidentally teach it something wrong. Small, consistent corrections work better than dramatic overhauls.</p> <p>PPO solves this with one elegant idea: <strong>clip the policy update</strong> so it can’t change too much in a single step.</p> <h3 id="the-ppo-algorithm-step-by-step">The PPO Algorithm, Step by Step</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">PPO TRAINING LOOP — collect, evaluate, clip, update</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:300px;"> <div class="ns-node-title">Step 1: Collect Experience</div> <div class="ns-node-sub">Agent runs in environment using current policy π. Records: (state, action, reward) for each step. This is the "data collection" phase — no learning yet.</div> </div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:340px;"> <div class="ns-node-title">Step 2: Compute Advantage Estimates</div> <div class="ns-node-sub">For each action taken, compute: "Was this better or worse than average?" An action with reward 100 when average is 60 has advantage +40. This tells us which actions were surprisingly good or bad.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:340px;"> <div class="ns-node-title">Step 3: The Clipped Objective (PPO's Key Innovation)</div> <div class="ns-node-sub">Calculate the ratio: π_new(a|s) / π_old(a|s). If this ratio is too large (policy changed too much), clip it to [1-ε, 1+ε] where ε ≈ 0.2. This prevents any single update from being too dramatic.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:300px;"> <div class="ns-node-title">Step 4: Update the Policy</div> <div class="ns-node-sub">Run gradient descent to maximize the clipped objective. The clip ensures the new policy stays close to the old one — the "trust region" that prevents catastrophic updates.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:300px;"> <div class="ns-node-title">Repeat Until Converged</div> <div class="ns-node-sub">Go back to Step 1 with the updated policy. Each cycle, the agent gets slightly better. Thousands of cycles later, the policy is well-optimized.</div> </div> </div> </div> <p><strong>The clipping mechanism explained for a first-year student.</strong> The ratio <code class="language-plaintext highlighter-rouge">π_new(a|s) / π_old(a|s)</code> tells you how much the probability of taking action <code class="language-plaintext highlighter-rouge">a</code> in state <code class="language-plaintext highlighter-rouge">s</code> has changed. If the new policy makes action <code class="language-plaintext highlighter-rouge">a</code> twice as likely as the old policy, the ratio is 2.0. If <code class="language-plaintext highlighter-rouge">ε = 0.2</code>, the clip restricts this to <code class="language-plaintext highlighter-rouge">[0.8, 1.2]</code> — the policy is only allowed to change action probabilities by ±20% per update. Any larger change is simply cut off.</p> <p>This is like a speed limiter on a car. No matter how hard you press the accelerator, the car won’t exceed 120 km/h. PPO’s clip is a policy change limiter — no matter how good an action seemed, the policy update is constrained.</p> <p><strong>Where PPO is used in practice.</strong> PPO was used to train the RLHF (Reinforcement Learning from Human Feedback) phase of ChatGPT and Claude. After initial pre-training on text data, human raters compared pairs of responses and rated which was better. This preference data trained a reward model. Then PPO was used to fine-tune the LLM to generate responses that scored highly on the reward model — effectively teaching it to be more helpful, harmless, and honest.</p> <hr/> <h2 id="dpo-the-simpler-path-to-human-alignment">DPO: The Simpler Path to Human Alignment</h2> <p>Direct Preference Optimization (DPO) is a more recent algorithm that achieves the same goal as PPO-based RLHF — aligning LLMs with human preferences — with dramatically less complexity.</p> <h3 id="why-ppo-is-hard-for-llm-alignment">Why PPO Is Hard for LLM Alignment</h3> <p>The traditional RLHF pipeline using PPO requires two separate training phases:</p> <ol> <li> <p><strong>Train a reward model:</strong> Collect thousands of (prompt, response_A, response_B, human_preference) tuples. Train a separate neural network (the reward model) to predict which response a human would prefer.</p> </li> <li> <p><strong>Fine-tune the LLM with PPO:</strong> Run PPO using the reward model as the “critic” that scores the LLM’s outputs. The LLM learns to generate responses that get high scores.</p> </li> </ol> <p>This two-phase approach has serious practical problems. The reward model is imperfect — it’s an approximation of human judgment. The LLM, being optimized by PPO, can discover “reward hacking” — generating responses that score highly on the reward model but are actually bad (e.g., by exploiting weaknesses in how the reward model was trained). The separate reward model also requires additional GPU memory, additional training time, and careful hyperparameter tuning.</p> <h3 id="how-dpo-works">How DPO Works</h3> <p>DPO (Ziegler et al., 2023) makes the insight that you don’t need a reward model at all. You can derive the optimal policy <em>directly</em> from the preference data, without the intermediary.</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">PPO-RLHF vs DPO — two paths to human preference alignment</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:320px;"> <div class="ns-node-title">Human Preference Data</div> <div class="ns-node-sub">(prompt, preferred_response, rejected_response) — collected from human raters comparing pairs of responses</div> </div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:560px;"> <div class="ns-branch"> <span class="ns-label-red">PPO Path (Complex)</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"> <div class="ns-node-title">Train Reward Model</div> <div class="ns-node-sub">Separate neural network learns to score responses. Additional GPU memory, training time, risk of reward hacking.</div> </div> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"> <div class="ns-node-title">Fine-tune LLM with PPO</div> <div class="ns-node-sub">Complex RL training loop. LLM maximizes reward model score. Can be unstable.</div> </div> </div> <div class="ns-branch"> <span class="ns-label-green">DPO Path (Simple)</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Direct Fine-tuning on Preferences</div> <div class="ns-node-sub">No reward model needed. Increases probability of preferred responses. Decreases probability of rejected responses. Standard gradient descent.</div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:320px;"> <div class="ns-node-title">Aligned LLM</div> <div class="ns-node-sub">Generates responses that match human preferences — more helpful, harmless, and honest</div> </div> </div> </div> <p><strong>The mathematical intuition.</strong> DPO uses a mathematical property: the optimal policy under a reward model can be expressed directly in terms of the likelihood ratio between the policy and a reference policy. This means you can skip the reward model entirely and directly train the LLM to:</p> <ul> <li><strong>Increase</strong> the log-probability of generating responses labeled as “preferred”</li> <li><strong>Decrease</strong> the log-probability of generating responses labeled as “rejected”</li> </ul> <p>The loss function looks like: <code class="language-plaintext highlighter-rouge">L_DPO = -E[log σ(β · (log π(y_w|x) - log π_ref(y_w|x)) - β · (log π(y_l|x) - log π_ref(y_l|x)))]</code></p> <p>Don’t worry about the formula — the intuition is: for each (preferred, rejected) pair, adjust the model to make the preferred response more likely and the rejected response less likely, relative to where you started (the reference policy <code class="language-plaintext highlighter-rouge">π_ref</code>). That’s it.</p> <p><strong>Why DPO is gaining adoption.</strong> DPO achieves comparable alignment quality to PPO-based RLHF but requires only one training phase instead of two, no separate reward model, standard supervised learning training loops (no RL), and significantly less GPU memory and compute. Several recent frontier models, including variants of Llama and Mistral, use DPO for their alignment phase.</p> <table> <thead> <tr> <th> </th> <th>PPO-RLHF</th> <th>DPO</th> </tr> </thead> <tbody> <tr> <td><strong>Phases</strong></td> <td>2 (reward model + RL fine-tune)</td> <td>1 (direct fine-tune)</td> </tr> <tr> <td><strong>Reward model needed?</strong></td> <td>Yes — separate training</td> <td>No</td> </tr> <tr> <td><strong>Training stability</strong></td> <td>Can be unstable</td> <td>Generally stable</td> </tr> <tr> <td><strong>Risk of reward hacking</strong></td> <td>Yes</td> <td>No</td> </tr> <tr> <td><strong>Implementation complexity</strong></td> <td>High</td> <td>Low</td> </tr> <tr> <td><strong>Performance</strong></td> <td>Slightly better on some benchmarks</td> <td>Competitive on most tasks</td> </tr> </tbody> </table> <hr/> <h2 id="when-agents-learn-without-retraining-few-shot-and-in-context-adaptation">When Agents Learn Without Retraining: Few-Shot and In-Context Adaptation</h2> <p>One of the most practically important forms of learning for deployed LLM agents doesn’t involve changing any model weights at all. <strong>In-context learning</strong> is when the LLM adapts its behavior based on examples or instructions provided in the current prompt.</p> <p>This is both the most accessible form of adaptation (no training required) and the most temporary (no persistence between sessions unless you explicitly manage it).</p> <p><strong>Few-shot learning example.</strong> Suppose you want your agent to classify customer support tickets into categories. Rather than fine-tuning a model, you include examples in the prompt:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Classify the following support ticket:

Example 1: "My order hasn't arrived after 2 weeks" → SHIPPING
Example 2: "The item broke after one day of use" → QUALITY
Example 3: "How do I return something?" → RETURN

Now classify: "My package says delivered but I haven't received it"
</code></pre></div></div> <p>The model learns the classification scheme from those three examples and applies it to the new ticket — without any gradient updates, without any training, just from the examples in the prompt.</p> <p><strong>Zero-shot learning.</strong> Even more powerfully, you can describe a task and the model can often do it with no examples at all: “Classify this support ticket into one of these categories: SHIPPING, QUALITY, RETURN, BILLING, OTHER. Ticket: …” The model generalizes from the category names and descriptions alone.</p> <p><strong>Why this works.</strong> During pre-training on trillions of tokens of text, the LLM has seen countless examples of few-shot task demonstrations, classification systems, and task descriptions. It has learned <em>the format</em> of task specification itself. When you provide a few examples, you’re activating that learned pattern-matching capability — you’re not teaching it a new skill, you’re reminding it of one it already has.</p> <p><strong>The limitation.</strong> Few-shot and zero-shot adaptation exists only within the context window. When the session ends, the adaptation is gone. For persistent adaptation — the agent consistently handling a new task type better over time — you need either fine-tuning (updating the weights) or memory-based approaches (storing successful interaction patterns for retrieval in future sessions).</p> <hr/> <h2 id="case-study-sica--the-self-improving-coding-agent">Case Study: SICA — The Self-Improving Coding Agent</h2> <p>The Self-Improving Coding Agent (SICA), developed by Maxime Robeyns, Laurence Aitchison, and Martin Szummer, represents one of the most striking demonstrations of agent-based learning: an agent that improves by modifying its own source code.</p> <p>This is fundamentally different from everything we’ve discussed so far. In reinforcement learning, the model’s <em>weights</em> change. In fine-tuning, the model’s <em>weights</em> change. In SICA, the model’s <em>code</em> changes — the actual Python files that implement the agent’s tools and behavior are rewritten by the agent itself.</p> <h3 id="the-architecture">The Architecture</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">SICA SELF-IMPROVEMENT CYCLE — meta-improvement through code modification</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:320px;"> <div class="ns-node-title">Archive of Past Versions</div> <div class="ns-node-sub">All previous agent versions with their benchmark scores: success rate, time taken, compute cost. Each version is a snapshot of the agent's codebase.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:340px;"> <div class="ns-node-title">Select Best Version</div> <div class="ns-node-sub">Pick the version with highest weighted utility score: U = success × w₁ - time × w₂ - compute × w₃. This "best agent so far" becomes the base for the next improvement cycle.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:360px;"> <div class="ns-node-title">Meta-Improvement Phase</div> <div class="ns-node-sub">The selected agent analyzes the archive, identifies what's limiting performance, and directly edits its own Python source files. It can add new tools, modify existing functions, or redesign data structures.</div> </div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:340px;"> <div class="ns-node-title">Benchmark Evaluation</div> <div class="ns-node-sub">The modified agent runs against standardized coding challenges. Its success rate, runtime, and compute usage are recorded. This produces the utility score for this new version.</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:320px;"> <div class="ns-node-title">Archive Updated</div> <div class="ns-node-sub">The new version + its scores join the archive. The cycle repeats — the next meta-improvement cycle starts from the best version in the updated archive.</div> </div> </div> </div> <h3 id="what-sica-actually-improved">What SICA Actually Improved</h3> <p>What makes SICA remarkable isn’t just that it can modify code — it’s <em>what</em> it chose to modify, without being told. Starting from a simple file-overwriting approach (replace the entire file to make any change), SICA independently invented increasingly sophisticated tools:</p> <p><strong>Generation 1 → 2: The Smart Editor.</strong> SICA recognized that overwriting entire files was wasteful — most of the file stays the same when you make a small change. It invented a more intelligent editing tool that could make targeted modifications to specific sections of code without touching the rest. This was entirely self-generated.</p> <p><strong>Generation 2 → 3: The Diff-Enhanced Smart Editor.</strong> SICA discovered that describing changes as “diffs” (the Unix <code class="language-plaintext highlighter-rouge">diff</code> format — showing only what changed) was more efficient than showing the entire modified file. It added diff-based editing to its toolkit, reducing context length and processing time.</p> <p><strong>Navigation innovation: The AST Symbol Locator.</strong> To edit code intelligently, you need to find the right location quickly. SICA independently invented a tool that parsed the code’s <strong>Abstract Syntax Tree (AST)</strong> — a structured representation of code that shows its logical structure rather than raw text — to navigate directly to function or class definitions.</p> <blockquote> <p><strong>What is an Abstract Syntax Tree (AST)?</strong> When Python (or any programming language) parses code, it builds a tree structure representing the program’s logical organization. For a function definition <code class="language-plaintext highlighter-rouge">def calculate(x, y): return x + y</code>, the AST would show: “FunctionDef named ‘calculate’ with parameters [‘x’, ‘y’] and a body containing Return(Add(Name(‘x’), Name(‘y’)))”. The AST is language-structure-aware, unlike simple text search which is just pattern matching on characters. Using an AST to navigate code is like using a map with labeled roads rather than aerial photography — you find what you’re looking for based on its logical type and name, not its visual appearance.</p> </blockquote> <h3 id="the-asynchronous-overseer">The Asynchronous Overseer</h3> <p>A critical architectural component in SICA is the <strong>asynchronous overseer</strong> — a separate LLM that runs concurrently with the main agent and monitors its behavior.</p> <p>The overseer receives a detailed view of SICA’s actions: a callgraph (which functions called which), a full log of LLM prompts and responses, and tool call records. It periodically evaluates whether SICA is:</p> <ul> <li><strong>Looping</strong> — repeating the same sequence of actions without progress</li> <li><strong>Stagnating</strong> — making attempts but not improving</li> <li><strong>Deviating</strong> — taking actions outside expected patterns that might indicate confusion or misalignment</li> </ul> <p>If it detects problems, the overseer can send a correction message to SICA or halt execution entirely.</p> <p>This is the <a href="/kohshh-portfolio/blog/2026/reflection/">Reflection</a> and <a href="/kohshh-portfolio/blog/2026/multi-agent/">Multi-Agent Collaboration</a> patterns combined and applied to the meta-level: the agent’s learning process itself is supervised by another agent.</p> <h3 id="safety-docker-containerization">Safety: Docker Containerization</h3> <p>An agent that can execute shell commands and edit its own code is potentially dangerous. If it makes a mistake, it could delete files, run infinite loops, or consume all system resources.</p> <p>SICA addresses this with <strong>Docker containerization</strong>: the agent runs inside a Docker container — an isolated environment that is completely separated from the host machine’s file system, network, and processes. The agent can only affect files and processes within the container. Even if it runs <code class="language-plaintext highlighter-rouge">rm -rf /</code> inside the container, the host machine is untouched.</p> <p>This is the same principle behind browser sandboxing (websites can’t access your file system) and cloud function isolation (each invocation gets its own isolated environment). Containerization is the industry-standard safety mechanism for running untrusted or potentially dangerous code.</p> <h3 id="sica-performance-results">SICA Performance Results</h3> <p>SICA’s utility score (a weighted combination of success rate, time, and compute cost) increased significantly across iterations, with key jumps corresponding to the invention of specific tools:</p> <ul> <li>Introducing the <strong>Smart Editor</strong> produced a large improvement in code editing efficiency</li> <li>The <strong>AST Symbol Locator</strong> enabled faster and more accurate navigation</li> <li>The <strong>Hybrid Symbol Locator</strong> (combining fast text search with AST verification) pushed performance higher still</li> <li><strong>Context-Sensitive Diff Minimization</strong> reduced context window usage, allowing more iterations before hitting limits</li> </ul> <p>The full performance trajectory shows the characteristic pattern of AI improvement: step-function jumps at innovation points, with gradual improvement between them as the new capability is refined.</p> <hr/> <h2 id="alphaevolve-discovering-algorithms-better-than-humans">AlphaEvolve: Discovering Algorithms Better Than Humans</h2> <p>AlphaEvolve, developed by Google DeepMind in 2025, takes a different approach to learning: rather than improving an agent’s behavior, it uses AI to <em>discover new algorithms</em> — mathematical procedures for solving problems.</p> <h3 id="the-architecture-1">The Architecture</h3> <p>AlphaEvolve combines three components:</p> <p><strong>1. LLM Ensemble (Gemini Flash + Pro).</strong> Gemini Flash generates a large, diverse set of candidate algorithm modifications — exploring widely across the solution space. Gemini Pro then analyzes the most promising candidates in depth, providing more careful refinement. The combination of breadth (Flash) and depth (Pro) mirrors how human research teams work: brainstormers and analysts.</p> <p><strong>2. Automated Evaluation.</strong> Every proposed algorithm modification is automatically tested against predefined criteria — speed, correctness, resource usage. This provides immediate, objective feedback without requiring human review of each proposal. The evaluation is deterministic: given the same algorithm, the score is always the same.</p> <p><strong>3. Evolutionary Selection.</strong> The best-performing algorithm variants are kept. Worse variants are discarded. The surviving variants are used as the “parents” for the next generation of modifications. This is evolutionary computing — the same mechanism that evolution uses to improve biological organisms over generations, applied to algorithms.</p> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ALPHAEVOLVE ARCHITECTURE — LLM-guided evolutionary algorithm discovery</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.25rem 1.5rem;"> <div class="ns-node ns-node-cyan" style="max-width:300px;"> <div class="ns-node-title">Initial Algorithm Population</div> <div class="ns-node-sub">Start with existing known algorithms as the initial generation. These are the "seed" solutions.</div> </div> <div class="ns-arrow"></div> <div class="ns-phase" style="max-width:480px;"> <div class="ns-phase-title">Evolutionary Loop — runs for many generations</div> <div class="ns-phase-sub">Each generation: propose → evaluate → select → repeat</div> <div class="ns-row"> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Gemini Flash</div> <div class="ns-node-sub">Generate many diverse algorithm variants. Breadth over depth.</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">Gemini Pro</div> <div class="ns-node-sub">Analyze + refine best candidates. Depth over breadth.</div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-cyan" style="max-width:360px;"> <div class="ns-node-title">Automated Evaluation</div> <div class="ns-node-sub">Run every candidate against objective metrics. Score assigned. No human review per candidate.</div> </div> <div class="ns-arrow"></div> <div class="ns-decision" style="max-width:240px;"> <div class="ns-node-title">Selection</div> <div class="ns-node-sub">Keep high-scorers, discard low-scorers</div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:320px;"> <div class="ns-node-title">Best Algorithm Discovered</div> <div class="ns-node-sub">After many generations, the surviving algorithm surpasses what human designers achieved.</div> </div> </div> </div> <h3 id="what-alphaevolve-discovered">What AlphaEvolve Discovered</h3> <p>The results from AlphaEvolve are extraordinary:</p> <p><strong>Data center scheduling:</strong> AlphaEvolve discovered a scheduling algorithm that reduced Google’s global compute resource usage by <strong>0.7%</strong>. On Google’s scale, this represents enormous savings — equivalent to taking hundreds of thousands of servers offline permanently.</p> <p><strong>Matrix multiplication:</strong> AlphaEvolve found a method for multiplying 4×4 complex-valued matrices using 48 scalar multiplications — fewer than the previously best-known algorithm (which required 49). Matrix multiplication is fundamental to virtually all neural network computation. Even a 2% improvement at this level has enormous downstream impact.</p> <p><strong>TPU design:</strong> AlphaEvolve suggested optimizations to Verilog code (the hardware description language) for Google’s next generation of Tensor Processing Units — AI chips. This is an AI system improving the hardware that will run future AI systems.</p> <p><strong>Gemini architecture:</strong> AlphaEvolve achieved a <strong>23% speed improvement</strong> in a core computational kernel within the Gemini model itself — a recursive self-improvement at the infrastructure level.</p> <p><strong>Why this matters beyond the specific results.</strong> AlphaEvolve demonstrates that AI systems can now participate in the <em>discovery of scientific knowledge</em> — not just applying what humans have already figured out, but finding genuinely new solutions that human mathematicians and engineers had not found. This is a qualitative shift in what AI can do.</p> <hr/> <h2 id="openevolve-the-open-source-version">OpenEvolve: The Open Source Version</h2> <p>OpenEvolve is an open-source evolutionary coding agent that applies the same principles as AlphaEvolve to general code optimization tasks. It is designed to evolve entire code files (not just individual functions) across multiple programming languages.</p> <h3 id="architecture">Architecture</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">openevolve</span> <span class="kn">import</span> <span class="n">OpenEvolve</span>

<span class="c1"># Initialize the evolutionary system
</span><span class="n">evolve</span> <span class="o">=</span> <span class="nc">OpenEvolve</span><span class="p">(</span>
    <span class="n">initial_program_path</span> <span class="o">=</span> <span class="sh">"</span><span class="s">path/to/initial_program.py</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">evaluation_file</span>      <span class="o">=</span> <span class="sh">"</span><span class="s">path/to/evaluator.py</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">config_path</span>          <span class="o">=</span> <span class="sh">"</span><span class="s">path/to/config.yaml</span><span class="sh">"</span>
<span class="p">)</span>

<span class="c1"># Run 1,000 generations of evolution
</span><span class="n">best_program</span> <span class="o">=</span> <span class="k">await</span> <span class="n">evolve</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="n">iterations</span><span class="o">=</span><span class="mi">1000</span><span class="p">)</span>

<span class="c1"># Inspect what the system found
</span><span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Best program metrics:</span><span class="sh">"</span><span class="p">)</span>
<span class="k">for</span> <span class="n">name</span><span class="p">,</span> <span class="n">value</span> <span class="ow">in</span> <span class="n">best_program</span><span class="p">.</span><span class="n">metrics</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  </span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="n">value</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p><strong><code class="language-plaintext highlighter-rouge">initial_program_path</code></strong>: The starting point — a working but possibly unoptimized Python program. This is like the “seed” organism in biological evolution.</p> <p><strong><code class="language-plaintext highlighter-rouge">evaluation_file</code></strong>: A Python script that takes a program and returns scores. This is the fitness function — it defines what “better” means for your specific optimization goal. For a sorting algorithm, the evaluator might measure speed and correctness. For a compression algorithm, it might measure compression ratio and decompression speed.</p> <p><strong><code class="language-plaintext highlighter-rouge">config_path</code></strong>: Settings for the evolution — which LLM to use, how many candidates to evaluate per generation, the selection pressure, mutation strategies.</p> <p><strong><code class="language-plaintext highlighter-rouge">evolve.run(iterations=1000)</code></strong>: Runs 1,000 generations of the evolutionary loop: generate variants → evaluate all → select survivors → repeat. Returns the highest-scoring program found across all generations.</p> <h3 id="the-four-core-components-openevolves-architecture">The Four Core Components (OpenEvolve’s Architecture)</h3> <p>OpenEvolve’s controller orchestrates four components:</p> <p><strong>Program Database:</strong> Stores all programs ever generated, along with their evaluation metrics. Programs that score well are more likely to be selected as “parents” for the next generation. The database also tracks program lineage — which programs were derived from which.</p> <p><strong>LLM Ensemble:</strong> One or more LLMs that generate code modifications. Given a parent program and the evaluation scores, the LLM proposes changes: “The loop on line 42 could be vectorized,” “Replace the dictionary with a more efficient data structure,” “This recursive function could be converted to iterative with a stack.”</p> <p><strong>Evaluator Pool:</strong> Multiple parallel evaluation workers that run candidate programs concurrently. Since evaluation can be computationally expensive, running evaluations in parallel (the <a href="/kohshh-portfolio/blog/2026/parallelization/">Parallelization</a> pattern from Chapter 3) significantly speeds up each generation.</p> <p><strong>Prompt Sampler:</strong> Constructs the prompts sent to the LLM ensemble. It selects representative programs from the database, formats them with their scores, and asks the LLM to generate improvements. The quality of these prompts directly affects the quality of the generated modifications.</p> <h3 id="where-openevolve-succeeds">Where OpenEvolve Succeeds</h3> <p>OpenEvolve has demonstrated impressive results on competitive programming problems, algorithm optimization benchmarks, and numerical computing tasks. It is particularly effective when:</p> <ol> <li><strong>There is a clear, automatable evaluation metric</strong> — you must be able to measure “better” without human judgment per candidate.</li> <li><strong>The solution space is large and complex</strong> — problems where human intuition has natural limits but LLMs can propose creative modifications.</li> <li><strong>Starting from a working solution</strong> — evolution refines; it doesn’t create from nothing. You need a baseline that already works.</li> </ol> <hr/> <h2 id="practical-applications">Practical Applications</h2> <div class="learn-usecases-grid"> <div class="learn-uc-card"> <span class="learn-uc-num">01</span> <h4>Personalized Assistants</h4> <p>Agents that learn individual user preferences over time — communication style, expertise level, preferred formats, recurring topics — and continuously refine their interaction to match each person.</p> <span class="learn-uc-method">Memory-Based + Online Learning</span> </div> <div class="learn-uc-card"> <span class="learn-uc-num">02</span> <h4>Trading Bots</h4> <p>Adaptive decision-making algorithms that adjust model parameters based on high-resolution real-time market data, changing strategy as market conditions evolve.</p> <span class="learn-uc-method">Reinforcement Learning (PPO)</span> </div> <div class="learn-uc-card"> <span class="learn-uc-num">03</span> <h4>Fraud Detection</h4> <p>Agents that continuously refine anomaly detection by learning from newly identified fraud patterns — the system gets better at catching fraud as new attack patterns emerge.</p> <span class="learn-uc-method">Online Learning + Supervised</span> </div> <div class="learn-uc-card"> <span class="learn-uc-num">04</span> <h4>Game AI</h4> <p>Dynamic opponents that adapt to each player's strategy in real time, providing an appropriate challenge level rather than a fixed difficulty preset.</p> <span class="learn-uc-method">Reinforcement Learning (PPO/DQN)</span> </div> <div class="learn-uc-card"> <span class="learn-uc-num">05</span> <h4>Robotic Navigation</h4> <p>Autonomous systems that improve navigation and obstacle avoidance through accumulated sensor data and historical action analysis across different environmental conditions.</p> <span class="learn-uc-method">RL + Memory-Based Learning</span> </div> <div class="learn-uc-card"> <span class="learn-uc-num">06</span> <h4>Algorithm Discovery</h4> <p>Systems like AlphaEvolve that discover novel algorithms for matrix operations, scheduling, and hardware design — surpassing solutions that human experts developed over decades.</p> <span class="learn-uc-method">Evolutionary + LLM Ensemble</span> </div> </div> <style>.learn-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:.85rem;margin:1.5rem 0}.learn-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.learn-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#c97af2}.learn-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.learn-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.learn-uc-method{font-size:.68rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="the-spectrum-of-learning-from-prompts-to-self-modification">The Spectrum of Learning: From Prompts to Self-Modification</h2> <p>It’s worth stepping back to see the full spectrum of how agents can learn and adapt, ordered from simplest to most powerful:</p> <table> <thead> <tr> <th>Level</th> <th>Mechanism</th> <th>Persistence</th> <th>Compute cost</th> <th>Example</th> </tr> </thead> <tbody> <tr> <td><strong>0</strong></td> <td>Prompt tuning</td> <td>None (session only)</td> <td>Zero</td> <td>Few-shot examples in prompt</td> </tr> <tr> <td><strong>1</strong></td> <td>Memory retrieval</td> <td>Sessions</td> <td>Low</td> <td>RAG with user preference store</td> </tr> <tr> <td><strong>2</strong></td> <td>Fine-tuning (supervised)</td> <td>Permanent</td> <td>Medium</td> <td>Domain adaptation on labeled data</td> </tr> <tr> <td><strong>3</strong></td> <td>RLHF with PPO</td> <td>Permanent</td> <td>High</td> <td>ChatGPT alignment training</td> </tr> <tr> <td><strong>4</strong></td> <td>DPO</td> <td>Permanent</td> <td>Medium</td> <td>Llama alignment training</td> </tr> <tr> <td><strong>5</strong></td> <td>Online learning</td> <td>Continuous updates</td> <td>Ongoing</td> <td>Trading bot parameter updates</td> </tr> <tr> <td><strong>6</strong></td> <td>Code self-modification</td> <td>Persistent agent improvement</td> <td>High</td> <td>SICA meta-improvement</td> </tr> <tr> <td><strong>7</strong></td> <td>Evolutionary algorithm discovery</td> <td>Permanent (new algorithms)</td> <td>Very high</td> <td>AlphaEvolve</td> </tr> </tbody> </table> <p>Each level up represents more autonomous adaptation, more compute investment, and more powerful outcomes. Most production agents operate at levels 0-2 because those are practical and cost-effective. Levels 3-7 represent the frontier of research and are increasingly entering production for organizations with the resources to invest in them.</p> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="learn-summary-card"> <div class="learn-summary-col"> <div class="learn-summary-label">WHAT</div> <p>Static agents execute without improving. Learning and adaptation are the mechanisms that allow agents to autonomously get better — changing their weights, prompts, code, or knowledge base based on experience and feedback.</p> </div> <div class="learn-summary-divider"></div> <div class="learn-summary-col"> <div class="learn-summary-label">WHY</div> <p>Real environments change. Users are individuals. Novel situations arise that weren't anticipated at design time. An agent that cannot learn cannot maintain performance in a dynamic world — it degrades until a human manually intervenes to update it.</p> </div> <div class="learn-summary-divider"></div> <div class="learn-summary-col"> <div class="learn-summary-label">RULE OF THUMB</div> <p>Use learning mechanisms when: the environment changes unpredictably, personalization is required, or the cost of manual updates outweighs the cost of automated adaptation. Start at the simplest level (prompts/memory) before investing in fine-tuning or RL.</p> </div> </div> <style>.learn-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.learn-summary-card{flex-direction:column}}.learn-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.learn-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.learn-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.learn-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#c97af2}</style> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li> <p><strong>Learning means changing</strong> — either the model’s weights (training), the agent’s code (SICA-style), the agent’s knowledge base (memory-based), or the agent’s behavior within a session (few-shot). Each has different persistence, cost, and power characteristics.</p> </li> <li> <p><strong>PPO is the workhorse RL algorithm.</strong> Its core insight — clip updates to prevent catastrophic policy changes — makes it stable enough to train complex agents. It’s behind ChatGPT’s alignment, AlphaGo, and countless production RL systems.</p> </li> <li> <p><strong>DPO is PPO’s simpler cousin for LLM alignment.</strong> By skipping the reward model entirely and directly optimizing on preference pairs, DPO achieves comparable alignment with less complexity, less compute, and less risk of reward hacking.</p> </li> <li> <p><strong>Few-shot in-context learning is the most accessible form.</strong> No training, no compute investment — just well-crafted examples in the prompt. The limitation is that it’s session-scoped: the adaptation disappears when the context ends.</p> </li> <li> <p><strong>SICA demonstrates self-modification is possible.</strong> An agent that edits its own code can invent tools (AST Symbol Locator, Smart Editor) that human designers didn’t build. The key enabling factor is an automated evaluation loop that provides objective feedback. Without measurable feedback, self-improvement is directionless.</p> </li> <li> <p><strong>Safety requires containerization.</strong> Any agent that can execute code or modify files must run in an isolated environment (Docker container) that prevents it from affecting the host system. This is non-negotiable for production self-modifying agents.</p> </li> <li> <p><strong>AlphaEvolve shows AI can participate in scientific discovery.</strong> The combination of LLM creativity (proposing modifications) with automated evaluation (objective scoring) and evolutionary selection (survival of the fittest) produces algorithms that surpass what human experts designed — including in matrix multiplication, chip design, and data center optimization.</p> </li> <li> <p><strong>OpenEvolve makes evolutionary code optimization accessible.</strong> The three-component architecture (LLM ensemble → evaluator pool → program database) is open source and generalizable to any programming task where you can define an automated evaluation metric.</p> </li> <li> <p><strong>The key condition for all learning systems: automated evaluation.</strong> Without a fast, reliable way to measure whether a change is an improvement, learning is impossible. Designing the evaluation function is often harder than building the learning system itself.</p> </li> </ul> <hr/> <p><em>Next up — Chapter 10: Model Context Protocol (MCP), where agents gain standardized ways to connect to any tool, server, or data source through a universal interface.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="learning"/><category term="reinforcement-learning"/><summary type="html"><![CDATA[Every pattern so far assumes the agent stays the same. Learning and adaptation break that assumption — agents that improve through experience, rewrite their own code, and discover better algorithms than humans designed.]]></summary></entry><entry><title type="html">Chapter 8: Memory Management</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/memory/" rel="alternate" type="text/html" title="Chapter 8: Memory Management"/><published>2026-02-02T00:00:00+00:00</published><updated>2026-02-02T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/memory</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/memory/"><![CDATA[<h2 id="the-stateless-agent-problem">The Stateless Agent Problem</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Stateless:</strong> A system with no memory between requests. Each call is independent — the system doesn't remember previous interactions. A basic LLM API call is stateless: call it twice with the same prompt and it has no idea the first call happened.</p> <p style="margin-top:0.5rem"><strong>Persistent vs ephemeral:</strong> Persistent data survives when the program stops (saved to disk/database). Ephemeral data exists only while the program runs (in RAM) and disappears when it stops. The context window is ephemeral — conversation history in a database is persistent.</p> <p style="margin-top:0.5rem"><strong>Vector database:</strong> A database that stores text as vectors (lists of numbers representing meaning) and supports "semantic search" — finding entries similar in <em>meaning</em>, not just matching keywords. Used for long-term agent memory because you want to find relevant past info by topic, not by exact words.</p> <p style="margin-top:0.5rem"><strong>Semantic search:</strong> Finding things by meaning rather than keywords. Searching "car" would semantically find "automobile" and "vehicle" too. Vector databases do this by comparing the mathematical distance between word/sentence embeddings.</p> <p style="margin-top:0.5rem"><strong>Session:</strong> One complete conversation thread between a user and the agent. Like a phone call — starts when the user first messages, ends when they're done. Multiple sessions with the same user build up long-term memory over time.</p> </div> <p>Every agent pattern in this series — <a href="/kohshh-portfolio/blog/2026/prompt-chaining/">chaining</a>, <a href="/kohshh-portfolio/blog/2026/routing/">routing</a>, <a href="/kohshh-portfolio/blog/2026/planning/">planning</a>, <a href="/kohshh-portfolio/blog/2026/multi-agent/">multi-agent</a> — has one silent assumption: the agent has context. It knows what the user said, what it did before, what tools it called.</p> <p>But where does that context live?</p> <p>Without explicit memory management, agents are stateless. Each call to the LLM starts fresh. Ask it “what’s my order status?” and it has no idea who you are, what you ordered, or what you discussed five messages ago. That’s not an agent — it’s a very expensive autocomplete.</p> <p><strong>Memory</strong> is what transforms a stateless LLM call into an agent that can maintain context, track progress, personalize responses, and learn from past interactions.</p> <p><strong>Why the context window isn’t enough.</strong> You might be thinking: “But the LLM already has a context window — isn’t that memory?” The context window <em>is</em> short-term memory within a single session. If you send 10 messages in a conversation and they all fit in the context window, the LLM sees all of them and can refer back to the first one. That’s working memory.</p> <p>The problems start at the <em>session boundary</em>. When the conversation ends (the user closes the app, the session times out, the server restarts), the context window contents disappear. The next time the user comes back, the LLM has no idea who they are, what they discussed before, or what preferences they expressed. Every session starts from zero.</p> <p>The second problem is <em>scale</em>. The context window has a fixed size. If a user has had 200 conversations with your agent, you can’t fit all of them into one context window call — it would be millions of tokens, prohibitively expensive and slow. You need a way to <em>selectively retrieve</em> the most relevant past information rather than including everything every time.</p> <p><strong>The two-tier memory architecture.</strong> The solution is a two-tier system:</p> <ul> <li><strong>Short-term memory (session context):</strong> Everything in the current conversation window. Fast, always available, but ephemeral.</li> <li><strong>Long-term memory (external storage):</strong> Key facts, preferences, and summaries extracted from past conversations. Persistent, semantic-searchable, but requires explicit retrieval.</li> </ul> <p>This mirrors how human memory works. You don’t consciously replay every past conversation before responding to someone — you just <em>know</em> relevant facts about them because they’ve been encoded into long-term memory over time. When something is relevant to the current conversation, it surfaces automatically.</p> <p>Building this two-tier system for AI agents is exactly what this chapter covers — using Google ADK’s Session/State/MemoryService architecture and LangChain’s ConversationBufferMemory and InMemoryStore.</p> <p>There are two fundamentally different problems:</p> <ol> <li><strong>Short-term</strong>: remember what was said <em>in this conversation</em></li> <li><strong>Long-term</strong>: remember what matters <em>across conversations</em></li> </ol> <p>Both require different mechanisms. Neither is optional for production agents.</p> <hr/> <h2 id="two-types-of-agent-memory">Two Types of Agent Memory</h2> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">MEMORY ARCHITECTURE</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">User Message</div> <div class="ns-node-sub">Arrives at the Runner · new conversation turn</div> </div> <div class="ns-arrow"></div> <div class="ns-phase"> <div class="ns-phase-title">Short-Term Memory — Session Scope</div> <div class="ns-phase-sub">Ephemeral · lost when session ends · lives in the LLM context window</div> <div class="ns-row"> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Events</div> <div class="ns-node-sub">Full message history for this conversation</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">State</div> <div class="ns-node-sub">Key-value scratchpad · task progress, preferences</div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">Agent Processes · Responds</div> <div class="ns-node-sub">LLM has full session context · generates reply</div> </div> <div class="ns-arrow"></div> <div class="ns-phase"> <div class="ns-phase-title">Long-Term Memory — Cross-Session Scope</div> <div class="ns-phase-sub">Persistent · survives session end · retrieved via semantic search</div> <div class="ns-row"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">MemoryService</div> <div class="ns-node-sub">Extracts facts from sessions · stores persistently</div> </div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Vector Database</div> <div class="ns-node-sub">Semantic search · retrieved into next session's context</div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Persistent Knowledge</div> <div class="ns-node-sub">User preferences · past interactions · learned behaviors</div> </div> </div> </div> <h3 id="short-term-memory">Short-Term Memory</h3> <p>Short-term memory is the <strong>context window</strong> — everything the model sees on a single call: the system prompt, the conversation history, tool results, agent thoughts.</p> <p>It’s fast, has zero retrieval cost, and is always 100% relevant to the current conversation. But it’s <strong>ephemeral</strong>: when the session ends, it’s gone. And it has capacity limits — even a 1M token window fills up eventually, and processing the full history on every call is expensive.</p> <h3 id="long-term-memory">Long-Term Memory</h3> <p>Long-term memory is <strong>external storage</strong> — databases, vector stores, knowledge graphs — that persists across sessions. When an agent needs something from the past, it queries the store, retrieves the relevant data, and injects it into the current context.</p> <p>Vector databases are the dominant storage type here because they support <strong>semantic search</strong>: the agent can find relevant memories by <em>meaning</em>, not just exact keyword match.</p> <hr/> <h2 id="interactive-watch-memory-work">Interactive: Watch Memory Work</h2> <div class="mem-demo-wrapper"> <div class="mem-demo-header"> <span class="mem-demo-title">MEMORY TRACKER — 3-TURN CONVERSATION</span> <button class="mem-demo-btn" id="memDemoPlayBtn">▶ Run Conversation</button> </div> <div class="mem-demo-body"> <div class="mem-demo-turns" id="memTurns"></div> <div class="mem-demo-state-panel" id="memStatePanel" style="display:none"> <div class="mem-panel mem-panel-short"> <div class="mem-panel-label">SHORT-TERM STATE (session.state)</div> <div class="mem-panel-content" id="memShortContent"></div> </div> <div class="mem-panel mem-panel-long"> <div class="mem-panel-label">LONG-TERM MEMORY (MemoryService)</div> <div class="mem-panel-content" id="memLongContent"></div> </div> </div> </div> </div> <style>.mem-demo-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.mem-demo-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.mem-demo-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.mem-demo-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.mem-demo-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.mem-demo-body{padding:1rem 1.1rem;display:flex;flex-direction:column;gap:.85rem}.mem-demo-turns{display:flex;flex-direction:column;gap:.6rem}.mem-turn{border:1px solid var(--global-divider-color);border-radius:7px;padding:.75rem .9rem;background:rgba(128,128,128,0.03);animation:memTurnIn .3s ease}@keyframes memTurnIn{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}.mem-turn-header{display:flex;align-items:center;gap:.6rem;margin-bottom:.45rem}.mem-turn-icon{font-size:.9rem}.mem-turn-role{font-size:.68rem;font-weight:700;letter-spacing:.07em;text-transform:uppercase;color:var(--global-text-color-light)}.mem-turn-text{font-size:.82rem;color:var(--global-text-color);line-height:1.55}.mem-turn-agent .mem-turn-text{color:#2698ba}.mem-turn-tags{display:flex;flex-wrap:wrap;gap:.35rem;margin-top:.4rem}.mem-tag{font-size:.62rem;font-family:monospace;padding:.1em .45em;border-radius:3px;border:1px solid var(--global-divider-color);color:var(--global-text-color-light)}.mem-tag-short{border-color:rgba(201,122,242,0.3);color:#c97af2;background:rgba(201,122,242,0.07)}.mem-tag-long{border-color:rgba(79,201,126,0.3);color:#4fc97e;background:rgba(79,201,126,0.07)}.mem-demo-state-panel{display:flex;gap:.75rem;flex-wrap:wrap}.mem-panel{flex:1;min-width:200px;border:1px solid var(--global-divider-color);border-radius:7px;overflow:hidden}.mem-panel-short{border-color:rgba(201,122,242,0.3)}.mem-panel-long{border-color:rgba(79,201,126,0.3)}.mem-panel-label{font-size:.6rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;padding:.4rem .75rem;border-bottom:1px solid var(--global-divider-color)}.mem-panel-short .mem-panel-label{color:#c97af2;background:rgba(201,122,242,0.06)}.mem-panel-long .mem-panel-label{color:#4fc97e;background:rgba(79,201,126,0.05)}.mem-panel-content{padding:.6rem .75rem;font-size:.73rem;color:var(--global-text-color-light);line-height:1.65;font-family:monospace}</style> <script>
var MEM_TURNS = [
  {
    role: 'user', icon: '👤',
    text: 'Hi! My name is Sarah and I work in Tokyo as a data scientist.',
    tags: [
      { label: 'stored in session.state → user:name = "Sarah"', type: 'short' },
      { label: 'LTM: extracted → name=Sarah, role=data scientist, location=Tokyo', type: 'long' },
    ],
    shortState: '{\n  "user:name": "Sarah",\n  "user:location": "Tokyo",\n  "user:role": "data scientist"\n}',
    longMem: '• Name: Sarah\n• Role: Data scientist\n• Location: Tokyo\n• (extracted from Turn 1)',
  },
  {
    role: 'user', icon: '👤',
    text: 'Can you recommend some AI meetups I should attend?',
    tags: [
      { label: 'short-term: full Turn 1 in context window', type: 'short' },
      { label: 'LTM retrieved: location=Tokyo → scopes recommendation', type: 'long' },
    ],
    shortState: '{\n  "user:name": "Sarah",\n  "user:location": "Tokyo",\n  "user:role": "data scientist",\n  "task:active": "meetup_search"\n}',
    longMem: '• Retrieved: location=Tokyo (similarity search)\n• Retrieved: role=data scientist\n→ Agent recommends Tokyo AI/ML meetups specifically',
  },
  {
    role: 'agent', icon: '🤖',
    text: 'Based on your location in Tokyo, Sarah, I\'d recommend the Tokyo AI Meetup, ML Tokyo, and the Data Science Japan group — all very active for data scientists in 2025.',
    tags: [
      { label: 'used session.state[user:name] for personalization', type: 'short' },
      { label: 'used LTM location recall to scope recommendations', type: 'long' },
    ],
    shortState: '{\n  "user:name": "Sarah",\n  "user:location": "Tokyo",\n  "user:role": "data scientist",\n  "task:active": "meetup_search",\n  "last_recommendation": "Tokyo AI Meetup"\n}',
    longMem: '• Updated: user interested in AI meetups\n• Updated: task_type=recommendation\n• Stored for future sessions',
  }
];

document.addEventListener('DOMContentLoaded', function(){
  var playBtn = document.getElementById('memDemoPlayBtn');
  if (!playBtn) return;
  var running = false;

  playBtn.addEventListener('click', async function(){
    if (running) return;
    running = true;
    playBtn.textContent = '⏳ Running…';
    playBtn.disabled = true;

    var turns = document.getElementById('memTurns');
    var statePanel = document.getElementById('memStatePanel');
    turns.innerHTML = '';
    statePanel.style.display = 'none';

    for (var i = 0; i < MEM_TURNS.length; i++) {
      await new Promise(function(r){ setTimeout(r, 600); });
      var t = MEM_TURNS[i];
      var div = document.createElement('div');
      div.className = 'mem-turn mem-turn-' + t.role;
      var tagsHTML = t.tags.map(function(tg){
        return '<span class="mem-tag mem-tag-'+tg.type+'">'+tg.label+'</span>';
      }).join('');
      div.innerHTML =
        '<div class="mem-turn-header">' +
          '<span class="mem-turn-icon">'+t.icon+'</span>' +
          '<span class="mem-turn-role">'+t.role+'</span>' +
        '</div>' +
        '<div class="mem-turn-text">'+t.text+'</div>' +
        '<div class="mem-turn-tags">'+tagsHTML+'</div>';
      turns.appendChild(div);

      // update state panels
      document.getElementById('memShortContent').textContent = t.shortState;
      document.getElementById('memLongContent').textContent  = t.longMem;
      statePanel.style.display = 'flex';
    }

    running = false;
    playBtn.textContent = '↺ Replay';
    playBtn.disabled = false;
  });
});
</script> <hr/> <h2 id="google-adk-session-state-and-memoryservice">Google ADK: Session, State, and MemoryService</h2> <p>The ADK structures memory into three explicit components with distinct responsibilities.</p> <h3 id="session-the-conversation-thread">Session: The Conversation Thread</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.sessions</span> <span class="kn">import</span> <span class="n">InMemorySessionService</span>

<span class="n">session_service</span> <span class="o">=</span> <span class="nc">InMemorySessionService</span><span class="p">()</span>
</code></pre></div></div> <blockquote> <p>A <strong><code class="language-plaintext highlighter-rouge">Session</code></strong> is one conversation thread. It holds:</p> <ul> <li><code class="language-plaintext highlighter-rouge">id</code> — unique identifier for this thread</li> <li><code class="language-plaintext highlighter-rouge">events</code> — ordered list of all messages, agent replies, and tool calls</li> <li><code class="language-plaintext highlighter-rouge">state</code> — temporary key-value data for this conversation</li> <li><code class="language-plaintext highlighter-rouge">last_update_time</code> — timestamp of the most recent activity</li> </ul> <p>You never create <code class="language-plaintext highlighter-rouge">Session</code> objects directly — the <code class="language-plaintext highlighter-rouge">SessionService</code> manages their lifecycle: create, retrieve, append events, delete.</p> </blockquote> <h3 id="three-sessionservice-implementations">Three SessionService Implementations</h3> <div class="mem-backends-grid"> <div class="mem-backend-card mem-card-dev"> <div class="mem-backend-tier">DEVELOPMENT</div> <div class="mem-backend-name">InMemorySessionService</div> <div class="mem-backend-code"> <code>from google.adk.sessions import InMemorySessionService<br/>session_service = InMemorySessionService()</code> </div> <ul class="mem-backend-props"> <li>✓ Zero setup — no database needed</li> <li>✓ Fast for testing</li> <li>✗ Data lost on app restart</li> <li>✗ Single process only</li> </ul> <span class="mem-backend-use">Use for: local development, unit tests</span> </div> <div class="mem-backend-card mem-card-prod"> <div class="mem-backend-tier">PRODUCTION</div> <div class="mem-backend-name">DatabaseSessionService</div> <div class="mem-backend-code"> <code>from google.adk.sessions import DatabaseSessionService<br/>session_service = DatabaseSessionService(db_url="sqlite:///./agent.db")</code> </div> <ul class="mem-backend-props"> <li>✓ Persistent across restarts</li> <li>✓ Supports SQLite, PostgreSQL, MySQL</li> <li>✓ You control the database</li> <li>✗ Requires infra management</li> </ul> <span class="mem-backend-use">Use for: production apps, self-hosted</span> </div> <div class="mem-backend-card mem-card-cloud"> <div class="mem-backend-tier">CLOUD SCALE</div> <div class="mem-backend-name">VertexAiSessionService</div> <div class="mem-backend-code"> <code>from google.adk.sessions import VertexAiSessionService<br/>session_service = VertexAiSessionService(project=PROJECT_ID, location="us-central1")</code> </div> <ul class="mem-backend-props"> <li>✓ Fully managed by Google Cloud</li> <li>✓ Scales automatically</li> <li>✓ Integrated with Reasoning Engine</li> <li>✗ Requires GCP setup</li> </ul> <span class="mem-backend-use">Use for: enterprise, high-traffic production</span> </div> </div> <style>.mem-backends-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:.85rem;margin:1.5rem 0}.mem-backend-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.5rem}.mem-card-dev{border-left:3px solid #e6a817}.mem-card-prod{border-left:3px solid #2698ba}.mem-card-cloud{border-left:3px solid #4fc97e}.mem-backend-tier{font-size:.58rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color-light)}.mem-backend-name{font-size:.85rem;font-weight:700;color:var(--global-text-color)}.mem-backend-code{background:rgba(0,0,0,0.2);border-radius:5px;padding:.5rem .6rem;font-size:.67rem;line-height:1.55;color:#7dcfff;overflow-x:auto;white-space:pre}.mem-backend-code code{background:0;border:0;color:inherit;font-size:inherit;padding:0}.mem-backend-props{list-style:none;padding:0;margin:0;font-size:.74rem;color:var(--global-text-color-light);display:flex;flex-direction:column;gap:.2rem;line-height:1.45}.mem-backend-use{font-size:.68rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <h3 id="state-the-sessions-scratchpad">State: The Session’s Scratchpad</h3> <p><code class="language-plaintext highlighter-rouge">session.state</code> is a dictionary that lives within a <code class="language-plaintext highlighter-rouge">Session</code>. Think of it as a whiteboard for one conversation — agents read from it and write to it throughout the session.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># The state has 4 key namespaces:
</span>
<span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">preference</span><span class="sh">"</span><span class="p">]</span>        <span class="c1"># session-scoped (cleared when session ends)
</span><span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">user:name</span><span class="sh">"</span><span class="p">]</span>         <span class="c1"># user-scoped (persists across this user's sessions)
</span><span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">app:config</span><span class="sh">"</span><span class="p">]</span>        <span class="c1"># app-scoped (shared across all users)
</span><span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">temp:validation</span><span class="sh">"</span><span class="p">]</span>   <span class="c1"># turn-scoped (cleared after each processing turn)
</span></code></pre></div></div> <blockquote> <p><strong>Key prefixes determine persistence.</strong> Without a prefix, data lives only in the current session. With <code class="language-plaintext highlighter-rouge">user:</code>, it follows the user across all their sessions. With <code class="language-plaintext highlighter-rouge">app:</code>, it’s global to the application. With <code class="language-plaintext highlighter-rouge">temp:</code>, it’s cleared after each processing turn — useful for ephemeral flags.</p> </blockquote> <p><strong>The right way to update state:</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ✓ Method 1: output_key on the agent (simplest — for agent text replies)
</span><span class="n">greeting_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">Greeter</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>      <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Generate a short, friendly greeting.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">output_key</span> <span class="o">=</span> <span class="sh">"</span><span class="s">last_greeting</span><span class="sh">"</span><span class="p">,</span>   <span class="c1"># runner auto-saves the reply to state["last_greeting"]
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Why <code class="language-plaintext highlighter-rouge">output_key</code>?</strong> The Runner intercepts the agent’s final response and writes it to <code class="language-plaintext highlighter-rouge">session.state["last_greeting"]</code> through the normal <code class="language-plaintext highlighter-rouge">append_event</code> flow. This means the change is recorded in the event history, properly persisted by the SessionService, and timestamped. It’s the least error-prone option.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ✓ Method 2: tool that updates state via ToolContext (for complex updates)
</span><span class="kn">from</span> <span class="n">google.adk.tools.tool_context</span> <span class="kn">import</span> <span class="n">ToolContext</span>
<span class="kn">import</span> <span class="n">time</span>

<span class="k">def</span> <span class="nf">log_user_login</span><span class="p">(</span><span class="n">tool_context</span><span class="p">:</span> <span class="n">ToolContext</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Tracks a user login. Updates login count, status, and timestamp.</span><span class="sh">"""</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">tool_context</span><span class="p">.</span><span class="n">state</span>

    <span class="n">login_count</span> <span class="o">=</span> <span class="n">state</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">user:login_count</span><span class="sh">"</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span>
    <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">user:login_count</span><span class="sh">"</span><span class="p">]</span>    <span class="o">=</span> <span class="n">login_count</span>
    <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">task_status</span><span class="sh">"</span><span class="p">]</span>          <span class="o">=</span> <span class="sh">"</span><span class="s">active</span><span class="sh">"</span>
    <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">user:last_login_ts</span><span class="sh">"</span><span class="p">]</span>   <span class="o">=</span> <span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">()</span>
    <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">temp:validation_needed</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span>

    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">success</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">message</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Login #</span><span class="si">{</span><span class="n">login_count</span><span class="si">}</span><span class="s"> tracked.</span><span class="sh">"</span><span class="p">}</span>
</code></pre></div></div> <blockquote> <p><strong>Why use a tool for state updates?</strong> Tools have access to <code class="language-plaintext highlighter-rouge">ToolContext</code>, which wraps <code class="language-plaintext highlighter-rouge">session.state</code> with proper event-tracking. Changes made through <code class="language-plaintext highlighter-rouge">tool_context.state</code> get recorded in the event log and properly persisted.</p> <p><strong>Never do this:</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">session</span> <span class="o">=</span> <span class="n">session_service</span><span class="p">.</span><span class="nf">get_session</span><span class="p">(</span><span class="n">app_name</span><span class="p">,</span> <span class="n">user_id</span><span class="p">,</span> <span class="n">session_id</span><span class="p">)</span>
<span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">key</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">value</span><span class="sh">"</span>  <span class="c1"># ← bypasses event tracking, may not persist
</span></code></pre></div> </div> <p>Direct dictionary writes bypass the <code class="language-plaintext highlighter-rouge">append_event</code> mechanism. They won’t be persisted by <code class="language-plaintext highlighter-rouge">DatabaseSessionService</code> or <code class="language-plaintext highlighter-rouge">VertexAiSessionService</code> and won’t appear in the event history.</p> </blockquote> <h3 id="memoryservice-long-term-knowledge">MemoryService: Long-Term Knowledge</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Development
</span><span class="kn">from</span> <span class="n">google.adk.memory</span> <span class="kn">import</span> <span class="n">InMemoryMemoryService</span>
<span class="n">memory_service</span> <span class="o">=</span> <span class="nc">InMemoryMemoryService</span><span class="p">()</span>

<span class="c1"># Production (Vertex AI RAG)
</span><span class="kn">from</span> <span class="n">google.adk.memory</span> <span class="kn">import</span> <span class="n">VertexAiRagMemoryService</span>
<span class="n">memory_service</span> <span class="o">=</span> <span class="nc">VertexAiRagMemoryService</span><span class="p">(</span>
    <span class="n">rag_corpus</span>              <span class="o">=</span> <span class="sh">"</span><span class="s">projects/my-project/locations/us-central1/ragCorpora/my-corpus</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">similarity_top_k</span>        <span class="o">=</span> <span class="mi">5</span><span class="p">,</span>      <span class="c1"># retrieve top 5 most relevant memories
</span>    <span class="n">vector_distance_threshold</span> <span class="o">=</span> <span class="mf">0.7</span><span class="p">,</span>  <span class="c1"># minimum similarity score to include
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">VertexAiRagMemoryService</code></strong> stores memories as vector embeddings in a Vertex AI RAG Corpus. When an agent searches memory, it sends a semantic query and gets back the most similar stored facts — not exact keyword matches, but conceptually related information.</p> <p><strong><code class="language-plaintext highlighter-rouge">similarity_top_k=5</code></strong>: retrieve at most 5 memories per query. Higher numbers give more context but consume more of the context window.</p> <p><strong><code class="language-plaintext highlighter-rouge">vector_distance_threshold=0.7</code></strong>: only return memories with at least 70% similarity to the query. This prevents irrelevant memories from polluting the context.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># After a session ends: extract facts and store in long-term memory
</span><span class="k">await</span> <span class="n">memory_service</span><span class="p">.</span><span class="nf">add_session_to_memory</span><span class="p">(</span><span class="n">session</span><span class="p">)</span>

<span class="c1"># When a new session starts: retrieve relevant past memories
</span><span class="n">relevant_memories</span> <span class="o">=</span> <span class="k">await</span> <span class="n">memory_service</span><span class="p">.</span><span class="nf">search_memory</span><span class="p">(</span>
    <span class="n">app_name</span> <span class="o">=</span> <span class="sh">"</span><span class="s">my_app</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">user_id</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">sarah_123</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">query</span>    <span class="o">=</span> <span class="sh">"</span><span class="s">user preferences and past interactions</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <h3 id="session-lifecycle-in-adk">Session lifecycle in ADK</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ADK SESSION LIFECYCLE — what happens on every message, start to finish</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:260px;"><div class="ns-node-title">New Message Arrives</div><div class="ns-node-sub">user sends a message to the agent</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple" style="max-width:280px;"><div class="ns-node-title">Runner</div><div class="ns-node-sub">the main executor · retrieves or creates the session for this user</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:300px;"><div class="ns-node-title">SessionService.get_or_create</div><div class="ns-node-sub">loads full conversation history + state so agent has full context</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-amber" style="max-width:300px;"><div class="ns-node-title">Agent Processes with Session Context</div><div class="ns-node-sub">LLM sees all past messages + current state · generates response</div></div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:300px;"><div class="ns-node-title">SessionService.append_event</div><div class="ns-node-sub">saves the agent's response + any state changes to persistent storage</div></div> <div class="ns-arrow"></div> <div class="ns-branch-row" style="max-width:480px;"> <div class="ns-branch"> <span class="ns-label-green">Conversation continues</span> <div class="ns-arrow ns-arrow-green"></div> <div class="ns-node ns-node-green"><div class="ns-node-title">Wait for Next Message</div><div class="ns-node-sub">session stays open · history grows each turn</div></div> </div> <div class="ns-branch"> <span class="ns-label-red">Session ends</span> <div class="ns-arrow ns-arrow-red"></div> <div class="ns-node ns-node-red"><div class="ns-node-title">MemoryService extracts facts</div><div class="ns-node-sub">key info saved to long-term store for future sessions</div></div> </div> </div> </div> </div> <hr/> <h2 id="langchain--langgraph-memory">LangChain &amp; LangGraph Memory</h2> <h3 id="short-term-conversationbuffermemory">Short-Term: ConversationBufferMemory</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain.memory</span> <span class="kn">import</span> <span class="n">ConversationBufferMemory</span>

<span class="n">memory</span> <span class="o">=</span> <span class="nc">ConversationBufferMemory</span><span class="p">(</span>
    <span class="n">memory_key</span>      <span class="o">=</span> <span class="sh">"</span><span class="s">chat_history</span><span class="sh">"</span><span class="p">,</span>   <span class="c1"># must match the variable name in your prompt
</span>    <span class="n">return_messages</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>              <span class="c1"># return list of message objects, not a string
</span><span class="p">)</span>

<span class="n">memory</span><span class="p">.</span><span class="nf">save_context</span><span class="p">(</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">input</span><span class="sh">"</span><span class="p">:</span>  <span class="sh">"</span><span class="s">What</span><span class="sh">'</span><span class="s">s the weather like?</span><span class="sh">"</span><span class="p">},</span>
    <span class="p">{</span><span class="sh">"</span><span class="s">output</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">It</span><span class="sh">'</span><span class="s">s sunny today.</span><span class="sh">"</span><span class="p">},</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">memory_key="chat_history"</code></strong> — this string must exactly match the placeholder in your <code class="language-plaintext highlighter-rouge">ChatPromptTemplate</code>. If your prompt has <code class="language-plaintext highlighter-rouge">MessagesPlaceholder(variable_name="chat_history")</code>, LangChain injects the conversation history there automatically.</p> <p><strong><code class="language-plaintext highlighter-rouge">return_messages=True</code></strong> — returns a list of <code class="language-plaintext highlighter-rouge">HumanMessage</code> / <code class="language-plaintext highlighter-rouge">AIMessage</code> objects instead of a single formatted string. Use this with chat models (ChatOpenAI, ChatGemini). The raw string format works for non-chat LLMs but loses role metadata.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_openai</span> <span class="kn">import</span> <span class="n">ChatOpenAI</span>
<span class="kn">from</span> <span class="n">langchain.chains</span> <span class="kn">import</span> <span class="n">LLMChain</span>
<span class="kn">from</span> <span class="n">langchain_core.prompts</span> <span class="kn">import</span> <span class="n">ChatPromptTemplate</span><span class="p">,</span> <span class="n">MessagesPlaceholder</span>

<span class="n">prompt</span> <span class="o">=</span> <span class="nc">ChatPromptTemplate</span><span class="p">(</span><span class="n">messages</span><span class="o">=</span><span class="p">[</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">You are a friendly travel assistant.</span><span class="sh">"</span><span class="p">),</span>
    <span class="nc">MessagesPlaceholder</span><span class="p">(</span><span class="n">variable_name</span><span class="o">=</span><span class="sh">"</span><span class="s">chat_history</span><span class="sh">"</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">human</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">{question}</span><span class="sh">"</span><span class="p">),</span>
<span class="p">])</span>

<span class="n">memory</span> <span class="o">=</span> <span class="nc">ConversationBufferMemory</span><span class="p">(</span><span class="n">memory_key</span><span class="o">=</span><span class="sh">"</span><span class="s">chat_history</span><span class="sh">"</span><span class="p">,</span> <span class="n">return_messages</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="n">conversation</span> <span class="o">=</span> <span class="nc">LLMChain</span><span class="p">(</span><span class="n">llm</span><span class="o">=</span><span class="nc">ChatOpenAI</span><span class="p">(),</span> <span class="n">prompt</span><span class="o">=</span><span class="n">prompt</span><span class="p">,</span> <span class="n">memory</span><span class="o">=</span><span class="n">memory</span><span class="p">)</span>

<span class="n">response</span> <span class="o">=</span> <span class="n">conversation</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">question</span><span class="o">=</span><span class="sh">"</span><span class="s">I want to book a flight.</span><span class="sh">"</span><span class="p">)</span>   <span class="c1"># turn 1
</span><span class="n">response</span> <span class="o">=</span> <span class="n">conversation</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">question</span><span class="o">=</span><span class="sh">"</span><span class="s">My name is Sam, by the way.</span><span class="sh">"</span><span class="p">)</span> <span class="c1"># turn 2
</span><span class="n">response</span> <span class="o">=</span> <span class="n">conversation</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">question</span><span class="o">=</span><span class="sh">"</span><span class="s">What was my name again?</span><span class="sh">"</span><span class="p">)</span>    <span class="c1"># turn 3 — knows it's Sam
</span></code></pre></div></div> <blockquote> <p>The <code class="language-plaintext highlighter-rouge">memory</code> object maintains the conversation buffer. On each <code class="language-plaintext highlighter-rouge">predict()</code> call, it: 1) loads the existing history into <code class="language-plaintext highlighter-rouge">{chat_history}</code>, 2) generates the response, 3) saves the new exchange to the buffer. Turn 3 works because turns 1 and 2 are in the buffer.</p> </blockquote> <h3 id="long-term-langgraph-inmemorystore">Long-Term: LangGraph InMemoryStore</h3> <p>LangGraph provides three long-term memory types, each mapped to a different human memory analogy:</p> <div class="mem-types-grid"> <div class="mem-type-card"> <div class="mem-type-icon">📖</div> <div class="mem-type-name">Semantic Memory</div> <div class="mem-type-sub">Remembering Facts</div> <div class="mem-type-desc">Stores facts and user preferences — a continuously updated profile or collection of factual documents. Used to ground responses with user-specific context.</div> <div class="mem-type-ex">user:name, location, preferences, domain knowledge</div> </div> <div class="mem-type-card"> <div class="mem-type-icon">🎬</div> <div class="mem-type-name">Episodic Memory</div> <div class="mem-type-sub">Remembering Experiences</div> <div class="mem-type-desc">Stores past events and successful interaction sequences. Implemented via few-shot examples — the agent learns from past task completions to do them better next time.</div> <div class="mem-type-ex">successful tool call patterns, past task solutions</div> </div> <div class="mem-type-card"> <div class="mem-type-icon">⚙</div> <div class="mem-type-name">Procedural Memory</div> <div class="mem-type-sub">Remembering Rules</div> <div class="mem-type-desc">The agent's core instructions and behaviors — often in the system prompt. Agents can update their own procedural memory through Reflection: reviewing past interactions and rewriting their own instructions.</div> <div class="mem-type-ex">system prompt rules, behavioral guidelines, self-updated instructions</div> </div> </div> <style>.mem-types-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:.85rem;margin:1.5rem 0}.mem-type-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.mem-type-icon{font-size:1.1rem}.mem-type-name{font-size:.85rem;font-weight:700;color:var(--global-text-color)}.mem-type-sub{font-size:.68rem;font-family:monospace;color:#c97af2}.mem-type-desc{font-size:.78rem;color:var(--global-text-color-light);line-height:1.5;margin:0}.mem-type-ex{font-size:.67rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <h3 id="langgraph-store-put-get-search">LangGraph Store: put, get, search</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langgraph.store.memory</span> <span class="kn">import</span> <span class="n">InMemoryStore</span>

<span class="k">def</span> <span class="nf">embed</span><span class="p">(</span><span class="n">texts</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">list</span><span class="p">[</span><span class="nb">float</span><span class="p">]]:</span>
    <span class="k">return</span> <span class="p">[[</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">2.0</span><span class="p">]</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="n">texts</span><span class="p">]</span>   <span class="c1"># replace with real embedding model in production
</span>
<span class="n">store</span> <span class="o">=</span> <span class="nc">InMemoryStore</span><span class="p">(</span><span class="n">index</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">embed</span><span class="sh">"</span><span class="p">:</span> <span class="n">embed</span><span class="p">,</span> <span class="sh">"</span><span class="s">dims</span><span class="sh">"</span><span class="p">:</span> <span class="mi">2</span><span class="p">})</span>

<span class="c1"># Namespace: (user_id, context_type) — like a folder structure
</span><span class="n">namespace</span> <span class="o">=</span> <span class="p">(</span><span class="sh">"</span><span class="s">sarah_123</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">preferences</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Store a memory
</span><span class="n">store</span><span class="p">.</span><span class="nf">put</span><span class="p">(</span>
    <span class="n">namespace</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">a-memory</span><span class="sh">"</span><span class="p">,</span>           <span class="c1"># key — like a filename
</span>    <span class="p">{</span>
        <span class="sh">"</span><span class="s">rules</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="sh">"</span><span class="s">User likes short, direct responses</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">User speaks English and Python</span><span class="sh">"</span><span class="p">],</span>
        <span class="sh">"</span><span class="s">timezone</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Asia/Tokyo</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">},</span>
<span class="p">)</span>

<span class="c1"># Retrieve by key
</span><span class="n">item</span> <span class="o">=</span> <span class="n">store</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">namespace</span><span class="p">,</span> <span class="sh">"</span><span class="s">a-memory</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Semantic search within the namespace
</span><span class="n">results</span> <span class="o">=</span> <span class="n">store</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span>
    <span class="n">namespace</span><span class="p">,</span>
    <span class="nb">filter</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">timezone</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Asia/Tokyo</span><span class="sh">"</span><span class="p">},</span>    <span class="c1"># metadata filter
</span>    <span class="n">query</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">language preferences</span><span class="sh">"</span><span class="p">,</span>        <span class="c1"># semantic similarity query
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Namespace = <code class="language-plaintext highlighter-rouge">(user_id, context_type)</code></strong> — think of it as a two-level folder structure. The first level is the user, the second is the type of memory (preferences, episodes, instructions). This prevents memory from one user bleeding into another’s.</p> <p><strong><code class="language-plaintext highlighter-rouge">store.put(namespace, key, data)</code></strong> — the <code class="language-plaintext highlighter-rouge">key</code> is like a filename. If a memory with that key already exists, it’s overwritten. Use descriptive keys like <code class="language-plaintext highlighter-rouge">"user-profile"</code> or <code class="language-plaintext highlighter-rouge">"booking-preferences"</code>.</p> <p><strong><code class="language-plaintext highlighter-rouge">store.search()</code> with both <code class="language-plaintext highlighter-rouge">filter</code> and <code class="language-plaintext highlighter-rouge">query</code></strong> — <code class="language-plaintext highlighter-rouge">filter</code> does exact metadata matching (deterministic); <code class="language-plaintext highlighter-rouge">query</code> does vector similarity search (semantic). Using both together is the most precise retrieval.</p> </blockquote> <h3 id="procedural-memory-self-updating-instructions">Procedural Memory: Self-Updating Instructions</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">update_instructions</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">State</span><span class="p">,</span> <span class="n">store</span><span class="p">:</span> <span class="n">BaseStore</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Reflection node: agent rewrites its own system prompt based on conversation.</span><span class="sh">"""</span>
    <span class="n">namespace</span> <span class="o">=</span> <span class="p">(</span><span class="sh">"</span><span class="s">instructions</span><span class="sh">"</span><span class="p">,)</span>
    <span class="n">current_instructions</span> <span class="o">=</span> <span class="n">store</span><span class="p">.</span><span class="nf">search</span><span class="p">(</span><span class="n">namespace</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>

    <span class="c1"># Ask the LLM to review its behavior and propose improvements
</span>    <span class="n">prompt</span> <span class="o">=</span> <span class="n">prompt_template</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span>
        <span class="n">instructions</span> <span class="o">=</span> <span class="n">current_instructions</span><span class="p">.</span><span class="n">value</span><span class="p">[</span><span class="sh">"</span><span class="s">instructions</span><span class="sh">"</span><span class="p">],</span>
        <span class="n">conversation</span> <span class="o">=</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">],</span>
    <span class="p">)</span>
    <span class="n">output</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
    <span class="n">new_instructions</span> <span class="o">=</span> <span class="n">output</span><span class="p">[</span><span class="sh">"</span><span class="s">new_instructions</span><span class="sh">"</span><span class="p">]</span>

    <span class="c1"># Overwrite stored instructions with the improved version
</span>    <span class="n">store</span><span class="p">.</span><span class="nf">put</span><span class="p">((</span><span class="sh">"</span><span class="s">agent_instructions</span><span class="sh">"</span><span class="p">,),</span> <span class="sh">"</span><span class="s">agent_a</span><span class="sh">"</span><span class="p">,</span> <span class="p">{</span><span class="sh">"</span><span class="s">instructions</span><span class="sh">"</span><span class="p">:</span> <span class="n">new_instructions</span><span class="p">})</span>
</code></pre></div></div> <blockquote> <p>This is the <a href="/kohshh-portfolio/blog/2026/reflection/">Reflection</a> pattern (Chapter 4) applied to procedural memory. The agent reads its own instructions, sees how the conversation went, and rewrites its prompt to do better next time. Each session, it gets slightly smarter.</p> </blockquote> <hr/> <h2 id="vertex-memory-bank">Vertex Memory Bank</h2> <p>For teams using Google Cloud, <strong>Memory Bank</strong> (part of Vertex AI Agent Engine) is a fully managed long-term memory service that works across frameworks — ADK, LangGraph, and CrewAI.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.memory</span> <span class="kn">import</span> <span class="n">VertexAiMemoryBankService</span>

<span class="n">memory_service</span> <span class="o">=</span> <span class="nc">VertexAiMemoryBankService</span><span class="p">(</span>
    <span class="n">project</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">my-gcp-project</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">location</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">us-central1</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">agent_engine_id</span> <span class="o">=</span> <span class="n">agent_engine_id</span><span class="p">,</span>
<span class="p">)</span>

<span class="c1"># After a session completes: extract and store memories
</span><span class="n">session</span> <span class="o">=</span> <span class="k">await</span> <span class="n">session_service</span><span class="p">.</span><span class="nf">get_session</span><span class="p">(</span><span class="n">app_name</span><span class="o">=</span><span class="n">app_name</span><span class="p">,</span> <span class="n">user_id</span><span class="o">=</span><span class="sh">"</span><span class="s">USER_ID</span><span class="sh">"</span><span class="p">,</span> <span class="n">session_id</span><span class="o">=</span><span class="n">session</span><span class="p">.</span><span class="nb">id</span><span class="p">)</span>
<span class="k">await</span> <span class="n">memory_service</span><span class="p">.</span><span class="nf">add_session_to_memory</span><span class="p">(</span><span class="n">session</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>How Memory Bank works:</strong> After <code class="language-plaintext highlighter-rouge">add_session_to_memory()</code>, Gemini asynchronously analyzes the conversation history, extracts key facts and user preferences, and stores them persistently. On the next session, the agent retrieves relevant memories via similarity search — getting only the facts that matter for the current conversation, not the entire history.</p> <p><strong>What makes it different from <code class="language-plaintext highlighter-rouge">VertexAiRagMemoryService</code>:</strong> Memory Bank actively uses Gemini to <em>understand</em> and <em>consolidate</em> memories — resolving contradictions, merging related facts, and updating existing preferences. It’s not just a vector store; it’s a managed memory intelligence layer.</p> </blockquote> <hr/> <h2 id="storage-backend-comparison">Storage Backend Comparison</h2> <div class="mem-compare-wrapper"> <div class="mem-compare-header"> <span class="mem-compare-title">STORAGE BACKENDS — CAPABILITY COMPARISON</span> <span class="mem-compare-sub">Hover a bar for details</span> </div> <div style="overflow-x:auto;padding:0.75rem;"> <canvas id="memCompareChart" width="620" height="260"></canvas> </div> <div class="mem-compare-legend"> <div class="mem-leg-item"><span class="mem-leg-dot" style="background:#e6a817"></span>InMemory (dev)</div> <div class="mem-leg-item"><span class="mem-leg-dot" style="background:#2698ba"></span>Database (prod)</div> <div class="mem-leg-item"><span class="mem-leg-dot" style="background:#4fc97e"></span>Vertex AI (cloud)</div> </div> <div class="mem-compare-tooltip" id="memTooltip" style="display:none"></div> </div> <style>.mem-compare-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0;position:relative}.mem-compare-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05);flex-wrap:wrap;gap:.4rem}.mem-compare-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.mem-compare-sub{font-size:.65rem;color:var(--global-text-color-light);opacity:.7}.mem-compare-legend{display:flex;gap:1.25rem;padding:.6rem 1.1rem;border-top:1px solid var(--global-divider-color);flex-wrap:wrap}.mem-leg-item{display:flex;align-items:center;gap:.4rem;font-size:.72rem;color:var(--global-text-color-light)}.mem-leg-dot{width:9px;height:9px;border-radius:2px;flex-shrink:0}.mem-compare-tooltip{position:fixed;background:var(--global-bg-color);border:1px solid var(--global-divider-color);border-radius:6px;padding:.5rem .75rem;font-size:.73rem;color:var(--global-text-color);pointer-events:none;z-index:200;max-width:240px;line-height:1.5;box-shadow:0 4px 16px rgba(0,0,0,0.35)}</style> <script>
(function(){
  var canvas = document.getElementById('memCompareChart');
  if (!canvas) return;
  var ctx = canvas.getContext('2d');
  var tooltip = document.getElementById('memTooltip');

  var dims    = ['Setup ease', 'Persistence', 'Scalability', 'Semantic search', 'Cost'];
  var backends = ['InMemory', 'Database', 'Vertex AI'];
  var colors   = ['#e6a817', '#2698ba', '#4fc97e'];
  var scores   = [
    [5, 3, 1], // Setup ease
    [1, 4, 5], // Persistence
    [1, 3, 5], // Scalability
    [2, 2, 5], // Semantic search
    [5, 3, 2], // Cost (inverse — free = 5)
  ];
  var descs = [
    ['Zero config, no dependencies.', 'Need DB URL, driver install.', 'Requires GCP project + auth.'],
    ['Lost on restart — dev only.', 'Survives restarts, SQL-backed.', 'Fully managed, durable.'],
    ['Single process only.', 'Vertical scale, connection pooling.', 'Auto-scales with GCP infra.'],
    ['None — exact key lookup only.', 'None natively — add pgvector.', 'Built-in RAG + embeddings.'],
    ['Free — no infra cost.', 'Your DB hosting cost.', 'GCP billing per query.'],
  ];

  var dpr=window.devicePixelRatio||1, W=Math.min(620,canvas.parentElement.getBoundingClientRect().width-24), H=260;
  canvas.width=W*dpr; canvas.height=H*dpr; canvas.style.width=W+'px'; canvas.style.height=H+'px';
  ctx.scale(dpr,dpr);

  var ML=90,MR=20,MT=22,MB=36, PW=W-ML-MR, PH=H-MT-MB;
  var nD=dims.length,nB=backends.length, gW=PW/nD, bW=(gW*0.7)/nB, bGap=(gW*0.3)/(nB+1);
  var hitRects=[];

  function getTheme(){ var s=getComputedStyle(document.documentElement); return {text:s.getPropertyValue('--global-text-color').trim()||'#e0e0e0',muted:s.getPropertyValue('--global-text-color-light').trim()||'#888',div:s.getPropertyValue('--global-divider-color').trim()||'#333'}; }

  function draw(hD,hB){
    ctx.clearRect(0,0,W,H); hitRects=[];
    var th=getTheme();
    for(var v=1;v<=5;v++){
      var gy=MT+(1-(v-1)/4)*PH;
      ctx.strokeStyle=th.div; ctx.lineWidth=0.5; ctx.setLineDash([3,3]);
      ctx.beginPath(); ctx.moveTo(ML,gy); ctx.lineTo(ML+PW,gy); ctx.stroke();
      ctx.setLineDash([]); ctx.fillStyle=th.muted; ctx.font='10px monospace'; ctx.textAlign='right';
      ctx.fillText(v,ML-5,gy+3.5);
    }
    for(var di=0;di<nD;di++){
      var gx=ML+di*gW;
      for(var bi=0;bi<nB;bi++){
        var bx=gx+bGap*(bi+1)+bW*bi, sc=scores[di][bi];
        var bh=(sc-1)/4*PH, by=MT+PH-bh;
        var isH=(hD===di&&hB===bi), alpha=(hD===null)?1:(isH?1:0.22);
        ctx.globalAlpha=alpha; ctx.fillStyle=colors[bi];
        ctx.fillRect(bx,by,bW,bh); ctx.globalAlpha=1;
        hitRects.push({x:bx,y:by,w:bW,h:bh,di:di,bi:bi,sc:sc});
      }
      ctx.fillStyle=th.muted; ctx.font='9px monospace'; ctx.textAlign='center';
      ctx.fillText(dims[di],gx+gW/2,H-MB+14);
    }
  }
  draw(null,null);

  canvas.addEventListener('mousemove',function(e){
    var rect=canvas.getBoundingClientRect(), mx=e.clientX-rect.left, my=e.clientY-rect.top, hit=null;
    for(var i=0;i<hitRects.length;i++){ var r=hitRects[i]; if(mx>=r.x&&mx<=r.x+r.w&&my>=r.y&&my<=r.y+r.h){hit=r;break;} }
    if(hit){
      draw(hit.di,hit.bi);
      tooltip.style.display='block'; tooltip.style.left=(e.clientX+12)+'px'; tooltip.style.top=(e.clientY-10)+'px';
      tooltip.innerHTML='<strong>'+backends[hit.bi]+' — '+dims[hit.di]+'</strong><br><span style="color:var(--global-text-color-light)">'+descs[hit.di][hit.bi]+'</span>';
      canvas.style.cursor='pointer';
    } else { draw(null,null); tooltip.style.display='none'; canvas.style.cursor='default'; }
  });
  canvas.addEventListener('mouseleave',function(){ draw(null,null); tooltip.style.display='none'; });
})();
</script> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="mem-summary-card"> <div class="mem-summary-col"> <div class="mem-summary-label">WHAT</div> <p>A dual-component system: short-term memory (session context window) for the current conversation, and long-term memory (external vector store) for knowledge that persists across sessions.</p> </div> <div class="mem-summary-divider"></div> <div class="mem-summary-col"> <div class="mem-summary-label">WHY</div> <p>Without memory, every conversation starts from zero. Agents can't maintain context, track progress, personalize responses, or learn from past interactions — making them useless for anything beyond single-turn Q&amp;A.</p> </div> <div class="mem-summary-divider"></div> <div class="mem-summary-col"> <div class="mem-summary-label">RULE OF THUMB</div> <p>Use short-term memory (Session + State) for any agent handling multi-turn conversations. Add long-term memory (MemoryService) when users expect personalization or continuity across sessions.</p> </div> </div> <style>.mem-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.mem-summary-card{flex-direction:column}}.mem-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.mem-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.mem-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.mem-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}</style> <hr/> <h2 id="why-vector-databases-are-used-for-long-term-memory">Why Vector Databases Are Used for Long-Term Memory</h2> <p>You might wonder: why not just store memories in a regular SQL database and retrieve them with SQL queries? The answer reveals something fundamental about how language and meaning work.</p> <p><strong>The keyword matching problem.</strong> SQL requires exact string matching. If a user once said “I prefer short answers,” you might store this under the key <code class="language-plaintext highlighter-rouge">"user_preferences"</code>. But what if next session you want to retrieve “things that affect response length”? The string “response length” doesn’t match “user_preferences” — so you’d miss it entirely.</p> <p>Language has meaning that transcends exact words. “I dislike verbose responses” and “please be concise” and “keep it brief” all mean the same thing, but share no keywords.</p> <p><strong>How vector databases solve this.</strong> Instead of storing raw text, vector databases store text as <em>embeddings</em> — lists of numbers (typically 1,536 or more numbers) that encode the <em>meaning</em> of the text in a high-dimensional space. Sentences with similar meanings end up with similar embeddings (similar numbers), regardless of which exact words they use.</p> <p>When you search a vector database with the query “things that affect response length,” the system converts your query to an embedding and finds the stored texts whose embeddings are <em>mathematically closest</em> to your query’s embedding — even if they share no keywords. This is called <strong>semantic search</strong> — search by meaning rather than by keywords.</p> <p><strong>How embeddings work intuitively.</strong> Think of placing words and sentences in a 3D space (in reality it’s 1,536-dimensional, but the concept is the same). Words with similar meanings are placed near each other. “Car,” “automobile,” and “vehicle” cluster together. “Dog” and “puppy” cluster together, not near “car.” Sentences with similar meanings also land near each other. When you embed “I prefer brief responses” and “keep answers short,” they land near each other in this space. A search for “response length preferences” also lands near both — the database returns them as relevant matches.</p> <p><strong>The practical flow in an agent.</strong> Here is how memory retrieval works in a real ADK agent with <code class="language-plaintext highlighter-rouge">VertexAiRagMemoryService</code>: (1) User starts a new session with no prior context. (2) Before processing the user’s first message, the system runs: <code class="language-plaintext highlighter-rouge">memories = await memory_service.search_memory(app_name, user_id, query=user_message)</code>. (3) The memory service converts <code class="language-plaintext highlighter-rouge">user_message</code> to an embedding and searches for semantically similar past memories. (4) The top-K most relevant memories are retrieved and injected into the agent’s context: “From past conversations: User prefers brief responses. User is a data scientist in Tokyo.” (5) The agent now has personalized context without the user having to repeat themselves. (6) After the session ends: <code class="language-plaintext highlighter-rouge">await memory_service.add_session_to_memory(session)</code> — key facts are extracted and stored for future retrieval.</p> <p>This is what makes agents feel “intelligent” about user preferences — they’re not actually remembering in the human sense, they’re retrieving relevant records from a database.</p> <h2 id="common-mistakes-when-implementing-memory">Common Mistakes When Implementing Memory</h2> <p><strong>Mistake 1: Storing everything in long-term memory.</strong> Not every message deserves long-term storage. “What time is it?” and “Tell me a joke” don’t contain information worth remembering across sessions. Use selective extraction — store only facts, preferences, and task-relevant context. Otherwise, your memory service fills with noise that makes semantic search less precise.</p> <p><strong>Mistake 2: Not scoping state keys correctly.</strong> Using <code class="language-plaintext highlighter-rouge">session.state["name"]</code> (no prefix) when you mean <code class="language-plaintext highlighter-rouge">session.state["user:name"]</code> (user-scoped) means the name disappears after the session ends. Always think carefully about the lifecycle you need: session-only, user-persistent, app-wide, or turn-only.</p> <p><strong>Mistake 3: Reading state directly instead of through events.</strong> <code class="language-plaintext highlighter-rouge">session.state["key"] = value</code> bypasses the event log and may not persist. Always update state through <code class="language-plaintext highlighter-rouge">output_key</code> on agents or through <code class="language-plaintext highlighter-rouge">EventActions.state_delta</code> when appending events. This ensures changes are logged, persisted, and timestamped correctly.</p> <p><strong>Mistake 4: Using <code class="language-plaintext highlighter-rouge">InMemorySessionService</code> in production.</strong> <code class="language-plaintext highlighter-rouge">InMemorySessionService</code> stores all session data in RAM. When the server restarts (which happens regularly in cloud deployments), all session data is lost. Use <code class="language-plaintext highlighter-rouge">DatabaseSessionService</code> or <code class="language-plaintext highlighter-rouge">VertexAiSessionService</code> for any production deployment where data persistence matters.</p> <p><strong>Mistake 5: No memory retrieval at session start.</strong> The memory service only helps if you actually query it at the beginning of each session and inject the results into the agent’s context. If you create the <code class="language-plaintext highlighter-rouge">MemoryService</code> but never call <code class="language-plaintext highlighter-rouge">search_memory</code>, you have a write-only memory — agents store information but never benefit from past sessions.</p> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li><strong>Memory has two fundamentally different problems:</strong> short-term (within a conversation) and long-term (across conversations). They require different storage mechanisms and have different access patterns.</li> <li><strong>ADK’s three primitives:</strong> <code class="language-plaintext highlighter-rouge">Session</code> tracks the conversation thread. <code class="language-plaintext highlighter-rouge">State</code> is the session’s temporary scratchpad (with namespace prefixes for scope). <code class="language-plaintext highlighter-rouge">MemoryService</code> is the searchable long-term knowledge store.</li> <li><strong>Update state through <code class="language-plaintext highlighter-rouge">append_event</code>, not direct dict writes.</strong> <code class="language-plaintext highlighter-rouge">session.state["key"] = value</code> bypasses event tracking and may not persist. Use <code class="language-plaintext highlighter-rouge">output_key</code> on agents or <code class="language-plaintext highlighter-rouge">EventActions.state_delta</code> in events.</li> <li><strong>State key prefixes determine persistence:</strong> <code class="language-plaintext highlighter-rouge">user:</code> persists across sessions for that user. <code class="language-plaintext highlighter-rouge">app:</code> is global. <code class="language-plaintext highlighter-rouge">temp:</code> clears each turn. No prefix = session-only.</li> <li><strong>Long-term memory uses vector search.</strong> Semantic retrieval finds relevant memories by meaning, not keyword. <code class="language-plaintext highlighter-rouge">similarity_top_k</code> and <code class="language-plaintext highlighter-rouge">vector_distance_threshold</code> control how much you retrieve and how relevant it must be.</li> <li><strong>LangChain’s <code class="language-plaintext highlighter-rouge">ConversationBufferMemory</code></strong> handles short-term automatically — loads history into the prompt, saves each turn. Use <code class="language-plaintext highlighter-rouge">return_messages=True</code> for chat models.</li> <li><strong>LangGraph’s <code class="language-plaintext highlighter-rouge">InMemoryStore</code></strong> supports three long-term memory types: Semantic (facts), Episodic (past task solutions), and Procedural (self-updating instructions via Reflection).</li> <li><strong>Vertex Memory Bank</strong> is the fully managed option — Gemini analyzes conversations, extracts facts, resolves contradictions, and provides personalized retrieval across ADK, LangGraph, and CrewAI.</li> </ul> <hr/> <p><em>Next up — Chapter 9: Human-in-the-Loop, where agents pause, ask for approval, and incorporate human judgment at critical decision points.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="memory"/><category term="session"/><category term="state"/><summary type="html"><![CDATA[Without memory, every conversation starts from zero. Memory management gives agents the ability to remember — short-term context within a session, long-term knowledge across sessions.]]></summary></entry><entry><title type="html">Chapter 7: Multi-Agent Collaboration</title><link href="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/multi-agent/" rel="alternate" type="text/html" title="Chapter 7: Multi-Agent Collaboration"/><published>2026-01-29T00:00:00+00:00</published><updated>2026-01-29T00:00:00+00:00</updated><id>https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/multi-agent</id><content type="html" xml:base="https://kohsheen1234.github.io/kohshh-portfolio/blog/2026/multi-agent/"><![CDATA[<h2 id="why-one-agent-isnt-enough">Why One Agent Isn’t Enough</h2> <div class="concept-box"> <span class="concept-label">Before You Start — Key Terms Explained</span> <p><strong>Specialization:</strong> Focusing on a narrow domain to become very good at it. A specialist cardiologist is better at heart problems than a general practitioner, even though the GP knows more topics overall. The same principle applies to AI agents.</p> <p style="margin-top:0.5rem"><strong>Orchestrator:</strong> The agent that manages and coordinates other agents. It receives the high-level goal, decomposes it, assigns sub-tasks, and aggregates results. Think of it as the project manager in a team.</p> <p style="margin-top:0.5rem"><strong>Sequential vs parallel:</strong> Sequential = one after another (like a queue at a counter). Parallel = simultaneously (like multiple checkout lanes open at once). Sequential is simpler to reason about; parallel is faster for independent tasks.</p> <p style="margin-top:0.5rem"><strong>Why does more roles = worse LLM performance?</strong> When you stuff too many instructions into one system prompt ("be a researcher AND a writer AND a fact-checker"), the model splits its "attention" across all goals. It tends to do each poorly. Separate, focused system prompts produce better results for each role.</p> </div> <p>Every chapter up to now has been about making a single agent smarter — chain it, route it, run it in parallel, give it tools, make it reflect, make it plan. All of these improve a single agent’s performance.</p> <p>But there’s a ceiling — and it’s a hard one.</p> <p>The ceiling shows up in several distinct ways:</p> <p><strong>Knowledge breadth vs. depth.</strong> A single agent trying to be a domain expert in multiple fields simultaneously is like a company that puts one person in charge of engineering, legal, marketing, and finance all at once. Each role gets diluted attention, and the person’s expertise in any one domain never reaches the depth needed to excel.</p> <p><strong>System prompt dilution.</strong> When you write a system prompt that tries to make one agent do many things — “You are a researcher AND a writer AND a code reviewer AND a fact-checker” — you’re asking the model to activate multiple behavioral patterns simultaneously. These patterns often have conflicting heuristics. A researcher’s instinct is to gather more information; a writer’s instinct is to synthesize and move on. Held in tension within one agent, they produce mediocre results in both directions.</p> <p><strong>Context window saturation.</strong> A complex multi-domain task accumulates a lot of context: research findings, intermediate drafts, tool outputs, conversation history. A single agent accumulates all of this in one context window, which fills up fast. Multiple specialized agents each have their own context window focused only on their domain.</p> <p><strong>Error propagation.</strong> When a single agent makes a mistake early in a long task, that mistake often propagates through every subsequent step. In a multi-agent system, errors are isolated — a mistake by the Research Agent affects only the research phase, and the Writer Agent can still produce a good draft given the flawed research (though the final output will reflect the research quality).</p> <p><strong>The multi-agent solution</strong> addresses all four of these ceilings simultaneously: specialization deepens expertise, dedicated context windows stay focused, errors are isolated, and coordination mechanisms ensure coherent final outputs.</p> <p>A single agent handling a complex research project needs to be: a domain expert, a search specialist, a statistical analyst, a fact-checker, and a polished writer — simultaneously. The more roles you pile onto one system prompt, the worse it performs at each of them.</p> <p>The same problem that prompted humans to build teams applies to agents: <strong>specialization beats generalism at scale</strong>.</p> <p>Multi-agent collaboration breaks a complex goal into sub-tasks, assigns each to an agent built specifically for that task, and coordinates their outputs into a unified result. The researcher does the research. The analyst runs the numbers. The writer assembles the draft. The reviewer catches errors. Each agent excels at its role. The system as a whole exceeds what any single agent could produce.</p> <hr/> <h2 id="what-multi-agent-collaboration-is">What Multi-Agent Collaboration Is</h2> <p>Multi-agent collaboration is a system design pattern where:</p> <ol> <li>A <strong>complex goal</strong> is decomposed into discrete sub-tasks</li> <li>Each sub-task is assigned to a <strong>specialized agent</strong> with the right tools, knowledge, or reasoning approach</li> <li>Agents <strong>coordinate</strong> through defined communication protocols — passing outputs, delegating tasks, sharing state</li> <li>A <strong>final output</strong> emerges from the coordinated work of the team</li> </ol> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">MULTI-AGENT COLLABORATION PATTERN</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">Complex Goal</div> <div class="ns-node-sub">Too large for any single agent</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Orchestrator Agent</div> <div class="ns-node-sub">Decomposes goal · delegates to specialists</div> </div> <div class="ns-arrow"></div> <div class="ns-phase"> <div class="ns-phase-title">Specialist Agents — each owns a distinct role</div> <div class="ns-phase-sub">Separate tools, knowledge, and system prompts per agent</div> <div class="ns-row"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">Research Agent</div> <div class="ns-node-sub">Search · retrieve</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">Analysis Agent</div> <div class="ns-node-sub">Process · compute</div> </div> <div class="ns-node ns-node-purple"> <div class="ns-node-title">Writer Agent</div> <div class="ns-node-sub">Draft · format</div> </div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node"> <div class="ns-node-title">Synthesis Agent</div> <div class="ns-node-sub">Merges all specialist outputs · resolves conflicts</div> </div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green"> <div class="ns-node-title">Final Result</div> <div class="ns-node-sub">Exceeds what any single agent could produce</div> </div> </div> </div> <p>The critical ingredient is <strong>inter-agent communication</strong>: a standardized way for agents to exchange data, delegate work, and signal completion. Without it, you just have isolated agents running in parallel — not a team.</p> <hr/> <h2 id="the-six-interaction-models">The Six Interaction Models</h2> <p>Agent teams can be structured in fundamentally different ways. The choice of structure changes autonomy, fault tolerance, complexity, and scalability.</p> <div class="mas-models-wrapper"> <div class="mas-models-tabs"> <button class="mas-tab active" data-model="0" onclick="masSelect(0)">Single</button> <button class="mas-tab" data-model="1" onclick="masSelect(1)">Network</button> <button class="mas-tab" data-model="2" onclick="masSelect(2)">Supervisor</button> <button class="mas-tab" data-model="3" onclick="masSelect(3)">Supervisor as Tool</button> <button class="mas-tab" data-model="4" onclick="masSelect(4)">Hierarchical</button> <button class="mas-tab" data-model="5" onclick="masSelect(5)">Custom</button> </div> <div class="mas-models-body"> <div class="mas-model-content" id="masContent"> <div class="mas-model-left"> <div class="mas-model-name" id="masName">Single Agent</div> <div class="mas-model-desc" id="masDesc">One agent handles everything autonomously. Simple to build and manage, but constrained by a single agent's scope and resources. No inter-agent communication overhead. Suitable only when the task is fully self-contained.</div> <div class="mas-model-props" id="masProps"> <div class="mas-prop"><span class="mas-prop-label">Autonomy</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:20%"></div></div></div> <div class="mas-prop"><span class="mas-prop-label">Fault tolerance</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:10%"></div></div></div> <div class="mas-prop"><span class="mas-prop-label">Scalability</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:15%"></div></div></div> <div class="mas-prop"><span class="mas-prop-label">Complexity</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:10%"></div></div></div> </div> <div class="mas-model-usecase" id="masUsecase">Best for: simple, self-contained tasks with no need for specialization</div> </div> <div class="mas-model-right"> <svg id="masSvg" viewBox="0 0 240 180" width="240" height="180"></svg> </div> </div> </div> </div> <style>.mas-models-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.mas-models-tabs{display:flex;overflow-x:auto;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.04)}.mas-tab{flex-shrink:0;padding:.5rem .85rem;font-family:monospace;font-size:.68rem;border:0;border-right:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color-light);cursor:pointer;transition:background .15s,color .15s;white-space:nowrap}.mas-tab:last-child{border-right:0}.mas-tab.active{background:rgba(38,152,186,0.1);color:#2698ba;font-weight:700}.mas-tab:hover:not(.active){background:rgba(128,128,128,0.08)}.mas-models-body{padding:1.1rem}.mas-model-content{display:flex;gap:1.5rem;align-items:flex-start;flex-wrap:wrap}.mas-model-left{flex:1;min-width:220px;display:flex;flex-direction:column;gap:.75rem}.mas-model-name{font-size:1rem;font-weight:700;color:var(--global-text-color)}.mas-model-desc{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6}.mas-model-props{display:flex;flex-direction:column;gap:.45rem}.mas-prop{display:flex;align-items:center;gap:.75rem}.mas-prop-label{font-size:.68rem;font-family:monospace;color:var(--global-text-color-light);width:100px;flex-shrink:0}.mas-prop-bar{flex:1;height:4px;background:rgba(128,128,128,0.15);border-radius:2px;overflow:hidden}.mas-prop-fill{height:100%;background:#2698ba;border-radius:2px;transition:width .4s ease}.mas-model-usecase{font-size:.7rem;font-family:monospace;color:#4fc97e;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}.mas-model-right{flex-shrink:0}#masSvg{display:block}</style> <script>
var MAS_MODELS = [
  {
    name: "Single Agent",
    desc: "One agent handles everything autonomously. Simple to build and manage, but constrained by a single agent's scope and resources. No inter-agent communication overhead. Suitable only when the task is fully self-contained.",
    props: [20, 10, 15, 10],
    usecase: "Best for: simple, self-contained tasks with no need for specialization",
    draw: function(svg) {
      svg.innerHTML = '';
      addNode(svg, 120, 90, 'Agent', '#2698ba', 40, 20);
    }
  },
  {
    name: "Network",
    desc: "Multiple agents interact peer-to-peer in a decentralized mesh. Agents share information, resources, and tasks directly. Resilient — no single point of failure. Challenging to coordinate at scale without explicit orchestration.",
    props: [80, 80, 60, 70],
    usecase: "Best for: distributed tasks where agents need to share information freely",
    draw: function(svg) {
      svg.innerHTML = '';
      var pts = [[120,40],[60,100],[180,100],[80,155],[160,155]];
      // Edges
      var edges = [[0,1],[0,2],[1,3],[2,4],[1,2],[3,4],[0,3],[0,4]];
      edges.forEach(function(e){
        addEdge(svg,pts[e[0]][0],pts[e[0]][1],pts[e[1]][0],pts[e[1]][1],'#4a5a6a');
      });
      pts.forEach(function(p,i){
        addNode(svg,p[0],p[1],'A'+(i+1),'#c97af2',26,14);
      });
    }
  },
  {
    name: "Supervisor",
    desc: "A central supervisor agent coordinates and delegates to subordinate workers. Clear authority and task allocation. The supervisor is a single point of failure and can become a bottleneck with many subordinates.",
    props: [50, 30, 70, 45],
    usecase: "Best for: tasks with clear decomposition and one central coordinator",
    draw: function(svg) {
      svg.innerHTML = '';
      var workers = [[50,140],[100,140],[150,140],[200,140]];
      workers.forEach(function(p){
        addEdge(svg,120,50,p[0],p[1],'#4a5a6a');
      });
      addNode(svg,120,50,'Supervisor','#2698ba',42,18);
      workers.forEach(function(p,i){
        addNode(svg,p[0],p[1],'W'+(i+1),'#c97af2',28,14);
      });
    }
  },
  {
    name: "Supervisor as Tool",
    desc: "The supervisor provides resources, guidance, or analytical capabilities to other agents rather than commanding them. Workers can query the supervisor for support without rigid top-down control.",
    props: [60, 40, 65, 50],
    usecase: "Best for: teams that need shared expertise without strict hierarchy",
    draw: function(svg) {
      svg.innerHTML = '';
      var workers = [[40,140],[120,140],[200,140]];
      workers.forEach(function(p){
        addEdgeDashed(svg,120,55,p[0],p[1],'#e6a817');
      });
      addNode(svg,120,50,'Supervisor\n(Tool)','#e6a817',52,20);
      workers.forEach(function(p,i){
        addNode(svg,p[0],p[1],'Agent '+(i+1),'#c97af2',36,14);
      });
    }
  },
  {
    name: "Hierarchical",
    desc: "Multiple layers of supervisors, each managing the layer below. Higher supervisors oversee lower ones; operational agents sit at the bottom. Handles complexity at scale. Requires careful boundary design at each layer.",
    props: [70, 50, 90, 80],
    usecase: "Best for: large-scale systems with natural sub-problem decomposition",
    draw: function(svg) {
      svg.innerHTML = '';
      addNode(svg,120,28,'Root','#2698ba',36,14);
      addEdge(svg,120,42,70,80,'#4a5a6a');
      addEdge(svg,120,42,170,80,'#4a5a6a');
      addNode(svg,70,85,'Mgr A','#e6a817',36,14);
      addNode(svg,170,85,'Mgr B','#e6a817',36,14);
      [[40,145],[100,145]].forEach(function(p){addEdge(svg,70,99,p[0],p[1],'#4a5a6a');addNode(svg,p[0],p[1],'W','#c97af2',22,12);});
      [[140,145],[200,145]].forEach(function(p){addEdge(svg,170,99,p[0],p[1],'#4a5a6a');addNode(svg,p[0],p[1],'W','#c97af2',22,12);});
    }
  },
  {
    name: "Custom",
    desc: "A bespoke structure combining elements of multiple models — or an entirely novel design. Optimized for specific metrics, highly dynamic environments, or domain-specific constraints. Requires the deepest understanding of multi-agent design principles.",
    props: [90, 70, 85, 95],
    usecase: "Best for: complex, unique domains where no standard model fits",
    draw: function(svg) {
      svg.innerHTML = '';
      // Mix of hierarchical + peer links
      addNode(svg,120,28,'Orch','#2698ba',34,14);
      addEdge(svg,120,42,60,90,'#4a5a6a');
      addEdge(svg,120,42,180,90,'#4a5a6a');
      addNode(svg,60,95,'Expert A','#c97af2',38,14);
      addNode(svg,180,95,'Expert B','#4fc97e',38,14);
      addEdgeDashed(svg,60,111,180,111,'#e6a817');
      addEdge(svg,120,42,120,150,'#4a5a6a');
      addNode(svg,120,155,'Tool Agent','#e6a817',42,14);
      addEdgeDashed(svg,60,111,120,143,'#4a5a6a');
      addEdgeDashed(svg,180,111,120,143,'#4a5a6a');
    }
  }
];

function addNode(svg, cx, cy, label, color, rx, ry) {
  rx = rx || 36; ry = ry || 16;
  var rect = document.createElementNS('http://www.w3.org/2000/svg','rect');
  rect.setAttribute('x', cx - rx); rect.setAttribute('y', cy - ry);
  rect.setAttribute('width', rx*2); rect.setAttribute('height', ry*2);
  rect.setAttribute('rx', 6); rect.setAttribute('fill', '#141b2d');
  rect.setAttribute('stroke', color); rect.setAttribute('stroke-width', '1.5');
  svg.appendChild(rect);
  var lines = label.split('\n');
  lines.forEach(function(line, i) {
    var text = document.createElementNS('http://www.w3.org/2000/svg','text');
    text.setAttribute('x', cx); text.setAttribute('y', cy + (i - (lines.length-1)/2) * 13 + 4);
    text.setAttribute('text-anchor','middle'); text.setAttribute('fill','#e0e0e0');
    text.setAttribute('font-size','10'); text.setAttribute('font-family','monospace');
    text.textContent = line;
    svg.appendChild(text);
  });
}
function addEdge(svg, x1, y1, x2, y2, color) {
  var line = document.createElementNS('http://www.w3.org/2000/svg','line');
  line.setAttribute('x1',x1);line.setAttribute('y1',y1);line.setAttribute('x2',x2);line.setAttribute('y2',y2);
  line.setAttribute('stroke',color);line.setAttribute('stroke-width','1.2');
  line.setAttribute('marker-end','url(#arr)');
  svg.appendChild(line);
  ensureArrow(svg);
}
function addEdgeDashed(svg, x1, y1, x2, y2, color) {
  var line = document.createElementNS('http://www.w3.org/2000/svg','line');
  line.setAttribute('x1',x1);line.setAttribute('y1',y1);line.setAttribute('x2',x2);line.setAttribute('y2',y2);
  line.setAttribute('stroke',color);line.setAttribute('stroke-width','1.2');
  line.setAttribute('stroke-dasharray','4 3');
  svg.appendChild(line);
}
function ensureArrow(svg) {
  if (svg.querySelector('#arr')) return;
  var defs = document.createElementNS('http://www.w3.org/2000/svg','defs');
  defs.innerHTML = '<marker id="arr" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto"><path d="M0,0 L0,6 L6,3 z" fill="#4a5a6a"/></marker>';
  svg.insertBefore(defs, svg.firstChild);
}

function masSelect(idx) {
  document.querySelectorAll('.mas-tab').forEach(function(t){t.classList.remove('active');});
  document.querySelector('[data-model="'+idx+'"]').classList.add('active');
  var m = MAS_MODELS[idx];
  document.getElementById('masName').textContent = m.name;
  document.getElementById('masDesc').textContent = m.desc;
  document.getElementById('masUsecase').textContent = m.usecase;
  var fills = document.querySelectorAll('.mas-prop-fill');
  var labels = ['Autonomy','Fault tolerance','Scalability','Complexity'];
  var propContainer = document.getElementById('masProps');
  propContainer.innerHTML = '';
  labels.forEach(function(lbl, i) {
    var div = document.createElement('div'); div.className = 'mas-prop';
    div.innerHTML = '<span class="mas-prop-label">'+lbl+'</span><div class="mas-prop-bar"><div class="mas-prop-fill" style="width:'+m.props[i]+'%"></div></div>';
    propContainer.appendChild(div);
  });
  var svg = document.getElementById('masSvg');
  m.draw(svg);
}
document.addEventListener('DOMContentLoaded', function(){ masSelect(0); });
</script> <hr/> <h2 id="the-five-collaboration-patterns">The Five Collaboration Patterns</h2> <p>Within multi-agent systems, agents interact through five fundamental patterns:</p> <div class="mas-patterns-grid"> <div class="mas-pattern-card"> <div class="mas-pattern-icon">→</div> <h4>Sequential Handoff</h4> <p>Agent A completes its task and passes its output directly to Agent B. Output of A becomes input to B. Clean pipeline with clear dependencies.</p> <span class="mas-pattern-ex">Research → Write → Edit → Publish</span> </div> <div class="mas-pattern-card"> <div class="mas-pattern-icon">⇉</div> <h4>Parallel Workstreams</h4> <p>Multiple agents work on independent sub-tasks simultaneously. Results are merged by a synthesizer. Reduces total latency for independent work.</p> <span class="mas-pattern-ex">News + Weather + Stocks → Report</span> </div> <div class="mas-pattern-card"> <div class="mas-pattern-icon">⇄</div> <h4>Debate &amp; Consensus</h4> <p>Agents with different perspectives evaluate options and discuss. The group reaches a more informed decision than any single agent could.</p> <span class="mas-pattern-ex">Pro + Con agents → Moderator → Verdict</span> </div> <div class="mas-pattern-card"> <div class="mas-pattern-icon">↕</div> <h4>Hierarchical Delegation</h4> <p>A manager agent dynamically assigns sub-tasks to worker agents based on their tool access. Workers report back, manager synthesizes.</p> <span class="mas-pattern-ex">Coordinator → Specialist A / B / C → Result</span> </div> <div class="mas-pattern-card"> <div class="mas-pattern-icon">✓✗</div> <h4>Critic-Reviewer</h4> <p>A creator agent produces output. A critic agent evaluates it for quality, compliance, or correctness. Creator revises based on critique.</p> <span class="mas-pattern-ex">Generator → Critic → Revised Output</span> </div> </div> <style>.mas-patterns-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(190px,1fr));gap:.85rem;margin:1.5rem 0}.mas-pattern-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.mas-pattern-icon{font-size:1.1rem;color:#2698ba;font-weight:700}.mas-pattern-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.mas-pattern-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}.mas-pattern-ex{font-size:.68rem;font-family:monospace;color:#4fc97e;margin-top:auto;padding-top:.35rem;border-top:1px solid var(--global-divider-color)}</style> <hr/> <h2 id="watch-a-two-agent-team-work">Watch a Two-Agent Team Work</h2> <p>A Researcher and a Writer collaborate on a blog post. Click <strong>Run Team</strong> to watch the sequential handoff:</p> <div class="mas-team-wrapper"> <div class="mas-team-header"> <span class="mas-team-title">MULTI-AGENT TEAM DEMO — Research + Write Pipeline</span> <button class="mas-team-btn" id="masTeamRunBtn">▶ Run Team</button> </div> <div class="mas-team-body"> <div class="mas-team-goal"> <span class="mas-tg-label">SHARED GOAL</span> <span class="mas-tg-text">Write a blog post on the top 3 AI trends in 2025</span> </div> <div class="mas-agents-row"> <div class="mas-agent-panel" id="masAgentA"> <div class="mas-agent-header"> <span class="mas-agent-icon">🔍</span> <div> <div class="mas-agent-name">Research Agent</div> <div class="mas-agent-role">Senior Research Analyst</div> </div> <span class="mas-agent-badge" id="masAgentABadge">Idle</span> </div> <div class="mas-agent-track"><div class="mas-agent-fill cyan" id="masAgentAFill"></div></div> <div class="mas-agent-output" id="masAgentAOut" style="display:none"> <strong>Research findings:</strong><br/> 1. <strong>Multimodal AI</strong> — models handling text, image, audio, video simultaneously (GPT-4o, Gemini 1.5).<br/> 2. <strong>Agentic systems</strong> — AI moving from Q&amp;A to autonomous multi-step task execution.<br/> 3. <strong>On-device inference</strong> — compact models running locally on phones and laptops (Phi-3, Gemma). </div> </div> <div class="mas-handoff-arrow" id="masHandoff">→</div> <div class="mas-agent-panel" id="masAgentB" style="opacity:0.35"> <div class="mas-agent-header"> <span class="mas-agent-icon">✍</span> <div> <div class="mas-agent-name">Writer Agent</div> <div class="mas-agent-role">Technical Content Writer</div> </div> <span class="mas-agent-badge" id="masAgentBBadge">Waiting</span> </div> <div class="mas-agent-track"><div class="mas-agent-fill purple" id="masAgentBFill"></div></div> <div class="mas-agent-output" id="masAgentBOut" style="display:none"> <strong>Blog post draft:</strong><br/><br/> <em>The AI landscape in 2025 is defined by three transformative forces. First, multimodal models now process text, images, and audio as a unified stream — eliminating the boundary between media types. Second, agentic AI has crossed from demo to deployment: systems now autonomously plan, use tools, and course-correct across multi-step workflows. Third, on-device inference is making capable AI private and offline-ready, removing the cloud as a prerequisite for intelligence.</em> </div> </div> </div> <div class="mas-team-result" id="masTeamResult" style="display:none"> <span class="mas-result-icon">✓</span> Two specialized agents. One coherent output. <strong>Sequential handoff complete.</strong> </div> </div> </div> <style>.mas-team-wrapper{border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:2rem 0}.mas-team-header{display:flex;align-items:center;justify-content:space-between;padding:.75rem 1.1rem;border-bottom:1px solid var(--global-divider-color);background:rgba(128,128,128,0.05)}.mas-team-title{font-size:.68rem;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:var(--global-text-color)}.mas-team-btn{font-family:monospace;font-size:.72rem;padding:.3rem .8rem;border-radius:4px;border:1px solid var(--global-divider-color);background:transparent;color:var(--global-text-color);cursor:pointer;transition:background .15s}.mas-team-btn:hover{background:rgba(38,152,186,0.15);border-color:#2698ba;color:#2698ba}.mas-team-body{padding:1rem 1.1rem;display:flex;flex-direction:column;gap:.85rem}.mas-team-goal{border:1px solid var(--global-divider-color);border-radius:6px;padding:.6rem .9rem;display:flex;align-items:center;gap:.75rem;background:rgba(128,128,128,0.04)}.mas-tg-label{font-size:.6rem;font-weight:700;letter-spacing:.1em;color:#2698ba;flex-shrink:0}.mas-tg-text{font-size:.82rem;color:var(--global-text-color);font-family:monospace}.mas-agents-row{display:flex;align-items:center;gap:.75rem;flex-wrap:wrap}.mas-agent-panel{flex:1;min-width:220px;border:1.5px solid var(--global-divider-color);border-radius:8px;padding:.85rem;background:rgba(128,128,128,0.03);display:flex;flex-direction:column;gap:.5rem;transition:border-color .3s,opacity .4s}.mas-agent-panel.active{border-color:#2698ba}.mas-agent-panel.done{border-color:#4fc97e;opacity:1!important}.mas-agent-header{display:flex;align-items:center;gap:.6rem}.mas-agent-icon{font-size:1.1rem}.mas-agent-name{font-size:.82rem;font-weight:700;color:var(--global-text-color)}.mas-agent-role{font-size:.65rem;color:var(--global-text-color-light);font-family:monospace}.mas-agent-badge{font-size:.6rem;font-family:monospace;padding:.1em .4em;border-radius:3px;border:1px solid var(--global-divider-color);color:var(--global-text-color-light);margin-left:auto;transition:all .3s}.mas-agent-badge.running{color:#2698ba;border-color:#2698ba;background:rgba(38,152,186,0.1)}.mas-agent-badge.done{color:#4fc97e;border-color:#4fc97e;background:rgba(79,201,126,0.1)}.mas-agent-track{height:5px;background:rgba(128,128,128,0.12);border-radius:3px;overflow:hidden}.mas-agent-fill{height:100%;width:0;border-radius:3px;transition:width .05s linear}.mas-agent-fill.cyan{background:#2698ba}.mas-agent-fill.purple{background:#c97af2}.mas-agent-output{font-size:.77rem;color:var(--global-text-color-light);line-height:1.6;border-top:1px solid var(--global-divider-color);padding-top:.5rem;animation:masOutIn .3s ease}@keyframes masOutIn{from{opacity:0}to{opacity:1}}.mas-handoff-arrow{font-size:1.4rem;color:var(--global-text-color-light);flex-shrink:0;transition:color .4s}.mas-handoff-arrow.active{color:#2698ba}.mas-team-result{display:flex;align-items:center;gap:.6rem;padding:.6rem .85rem;background:rgba(79,201,126,0.07);border:1px solid rgba(79,201,126,0.2);border-radius:6px;font-size:.8rem;color:var(--global-text-color);animation:masOutIn .3s ease}.mas-result-icon{color:#4fc97e;font-weight:700}</style> <script>
document.addEventListener('DOMContentLoaded', function(){
  var btn = document.getElementById('masTeamRunBtn');
  if (!btn) return;
  var running = false;

  btn.addEventListener('click', async function(){
    if (running) return;
    running = true;
    btn.textContent = '⏳ Running…';
    btn.disabled = true;

    // Reset
    ['masAgentA','masAgentB'].forEach(function(id){
      var p = document.getElementById(id);
      p.classList.remove('active','done');
      p.style.opacity = id === 'masAgentB' ? '0.35' : '1';
    });
    document.getElementById('masAgentAFill').style.width = '0%';
    document.getElementById('masAgentBFill').style.width = '0%';
    document.getElementById('masAgentAOut').style.display = 'none';
    document.getElementById('masAgentBOut').style.display = 'none';
    document.getElementById('masTeamResult').style.display = 'none';
    document.getElementById('masHandoff').classList.remove('active');
    ['masAgentABadge','masAgentBBadge'].forEach(function(id,i){
      var el = document.getElementById(id);
      el.textContent = i===0 ? 'Idle' : 'Waiting';
      el.className = 'mas-agent-badge';
    });

    await new Promise(function(r){setTimeout(r,300)});

    // Agent A: Research
    var pA = document.getElementById('masAgentA');
    var badgeA = document.getElementById('masAgentABadge');
    var fillA = document.getElementById('masAgentAFill');
    pA.classList.add('active');
    badgeA.textContent = 'Researching…'; badgeA.className = 'mas-agent-badge running';

    var startA = null;
    await new Promise(function(resolve){
      function animA(ts){ if(!startA) startA=ts; var pct=Math.min((ts-startA)/1600*100,100); fillA.style.width=pct+'%'; if(pct<100) requestAnimationFrame(animA); else resolve(); }
      requestAnimationFrame(animA);
    });
    pA.classList.remove('active'); pA.classList.add('done');
    badgeA.textContent='Done ✓'; badgeA.className='mas-agent-badge done';
    document.getElementById('masAgentAOut').style.display='block';

    await new Promise(function(r){setTimeout(r,400)});

    // Handoff arrow
    document.getElementById('masHandoff').classList.add('active');
    await new Promise(function(r){setTimeout(r,500)});

    // Agent B: Write
    var pB = document.getElementById('masAgentB');
    var badgeB = document.getElementById('masAgentBBadge');
    var fillB = document.getElementById('masAgentBFill');
    pB.style.opacity = '1';
    pB.classList.add('active');
    badgeB.textContent='Writing…'; badgeB.className='mas-agent-badge running';

    var startB = null;
    await new Promise(function(resolve){
      function animB(ts){ if(!startB) startB=ts; var pct=Math.min((ts-startB)/1800*100,100); fillB.style.width=pct+'%'; if(pct<100) requestAnimationFrame(animB); else resolve(); }
      requestAnimationFrame(animB);
    });
    pB.classList.remove('active'); pB.classList.add('done');
    badgeB.textContent='Done ✓'; badgeB.className='mas-agent-badge done';
    document.getElementById('masAgentBOut').style.display='block';
    document.getElementById('masTeamResult').style.display='flex';

    running = false;
    btn.textContent = '↺ Replay';
    btn.disabled = false;
  });
});
</script> <hr/> <h2 id="use-cases-across-domains">Use Cases Across Domains</h2> <div class="mas-usecases-grid"> <div class="mas-uc-card"><span class="mas-uc-num">01</span><h4>Complex Research</h4><p>Searcher agent finds sources, summarizer agent digests them, trend-spotter identifies patterns, synthesizer writes the final report.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">02</span><h4>Software Development</h4><p>Requirements analyst, code generator, test writer, and documentation agent collaborate — each handing off to the next in sequence.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">03</span><h4>Financial Analysis</h4><p>Agents fetch stock data, analyze news sentiment, run technical analysis, and generate investment recommendations in parallel.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">04</span><h4>Customer Support</h4><p>Front-line agent handles general queries. Specialist agents (billing, technical, escalation) are activated only when needed.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">05</span><h4>Supply Chain</h4><p>Agents representing suppliers, manufacturers, and distributors collaborate to optimize inventory and logistics in real time.</p></div> <div class="mas-uc-card"><span class="mas-uc-num">06</span><h4>Network Remediation</h4><p>Multiple triage agents work concurrently to pinpoint failures. Each integrates with existing ML tooling while contributing to a coordinated remediation plan.</p></div> </div> <style>.mas-usecases-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(185px,1fr));gap:.85rem;margin:1.5rem 0}.mas-uc-card{border:1px solid var(--global-divider-color);border-radius:8px;padding:1rem;background:rgba(128,128,128,0.04);display:flex;flex-direction:column;gap:.4rem}.mas-uc-num{font-family:monospace;font-size:.65rem;font-weight:700;letter-spacing:.1em;color:#2698ba}.mas-uc-card h4{font-size:.85rem;font-weight:700;margin:0;color:var(--global-text-color)}.mas-uc-card p{font-size:.78rem;color:var(--global-text-color-light);margin:0;line-height:1.5}</style> <hr/> <h2 id="crewai-researcher--writer-team">CrewAI: Researcher + Writer Team</h2> <p>CrewAI is designed for exactly this — defining a team of agents with distinct roles, assigning tasks, and orchestrating collaboration.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">crewai</span> <span class="kn">import</span> <span class="n">Agent</span><span class="p">,</span> <span class="n">Task</span><span class="p">,</span> <span class="n">Crew</span><span class="p">,</span> <span class="n">Process</span>
<span class="kn">from</span> <span class="n">langchain_google_genai</span> <span class="kn">import</span> <span class="n">ChatGoogleGenerativeAI</span>

<span class="n">llm</span> <span class="o">=</span> <span class="nc">ChatGoogleGenerativeAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="defining-the-agents">Defining the Agents</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">researcher</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">role</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Senior Research Analyst</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">goal</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Find and summarize the latest trends in AI.</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">backstory</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">You are an experienced research analyst with a knack for identifying key trends and synthesizing information.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">verbose</span>          <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
    <span class="n">allow_delegation</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">writer</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">role</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Technical Content Writer</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">goal</span>      <span class="o">=</span> <span class="sh">'</span><span class="s">Write a clear and engaging blog post based on research findings.</span><span class="sh">'</span><span class="p">,</span>
    <span class="n">backstory</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">You are a skilled writer who can translate complex technical topics into accessible content.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">verbose</span>          <span class="o">=</span> <span class="bp">True</span><span class="p">,</span>
    <span class="n">allow_delegation</span> <span class="o">=</span> <span class="bp">False</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Two agents, two roles, zero overlap.</strong> The researcher knows nothing about writing; the writer knows nothing about searching. This clean separation means each agent’s system prompt is focused — no role confusion, no competing instructions.</p> <p><strong><code class="language-plaintext highlighter-rouge">allow_delegation=False</code></strong> — prevents each specialist from spawning sub-agents on their own. In a controlled pipeline, you want the delegation logic to live in the Crew definition, not in individual agents.</p> </blockquote> <h3 id="defining-the-tasks-with-dependency">Defining the Tasks (with dependency)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">research_task</span> <span class="o">=</span> <span class="nc">Task</span><span class="p">(</span>
    <span class="n">description</span>      <span class="o">=</span> <span class="sh">"</span><span class="s">Research the top 3 emerging AI trends in 2024-2025. Focus on practical applications and impact.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">expected_output</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">A detailed summary of the top 3 AI trends with key points and sources.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">agent</span>            <span class="o">=</span> <span class="n">researcher</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">writing_task</span> <span class="o">=</span> <span class="nc">Task</span><span class="p">(</span>
    <span class="n">description</span>     <span class="o">=</span> <span class="sh">"</span><span class="s">Write a 500-word blog post based on the research findings. Engaging, accessible to a general audience.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">expected_output</span> <span class="o">=</span> <span class="sh">"</span><span class="s">A complete 500-word blog post about the latest AI trends.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">agent</span>           <span class="o">=</span> <span class="n">writer</span><span class="p">,</span>
    <span class="n">context</span>         <span class="o">=</span> <span class="p">[</span><span class="n">research_task</span><span class="p">],</span>   <span class="c1"># ← explicit dependency
</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">context=[research_task]</code></strong> is the sequential handoff mechanism. It tells CrewAI: “the writing task cannot begin until research_task is complete, and the research output should be available to the writer as context.”</p> <p>Without this, the writer would run without access to the researcher’s findings — generating generic content rather than building on real research.</p> </blockquote> <h3 id="assembling-the-crew">Assembling the Crew</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">blog_creation_crew</span> <span class="o">=</span> <span class="nc">Crew</span><span class="p">(</span>
    <span class="n">agents</span>  <span class="o">=</span> <span class="p">[</span><span class="n">researcher</span><span class="p">,</span> <span class="n">writer</span><span class="p">],</span>
    <span class="n">tasks</span>   <span class="o">=</span> <span class="p">[</span><span class="n">research_task</span><span class="p">,</span> <span class="n">writing_task</span><span class="p">],</span>
    <span class="n">process</span> <span class="o">=</span> <span class="n">Process</span><span class="p">.</span><span class="n">sequential</span><span class="p">,</span>
    <span class="n">llm</span>     <span class="o">=</span> <span class="n">llm</span><span class="p">,</span>
    <span class="n">verbose</span> <span class="o">=</span> <span class="mi">2</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">result</span> <span class="o">=</span> <span class="n">blog_creation_crew</span><span class="p">.</span><span class="nf">kickoff</span><span class="p">()</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">Process.sequential</code></strong> ensures tasks run in the order they’re listed — researcher first, writer second. The <code class="language-plaintext highlighter-rouge">context</code> dependency enforces this even if the process allows parallelism.</p> <p><strong><code class="language-plaintext highlighter-rouge">llm</code> at the Crew level</strong> sets a default model for all agents. Individual agents can still override with their own <code class="language-plaintext highlighter-rouge">llm=</code> parameter if specialization is needed — e.g., a more powerful model for the researcher, a faster one for the writer.</p> </blockquote> <h3 id="crewai-data-flow">CrewAI Data Flow</h3> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">CREWAI DATA FLOW — research output becomes writer input via context=[task]</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="flex-direction:row;align-items:center;gap:0.75rem;padding:1.1rem 1.25rem;flex-wrap:nowrap;"> <div class="ns-node ns-node-cyan" style="flex-shrink:0;max-width:180px;"><div class="ns-node-title">Topic</div><div class="ns-node-sub">"Top 3 AI trends 2025"</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-purple" style="flex-shrink:0;max-width:200px;"><div class="ns-node-title">Researcher Agent</div><div class="ns-node-sub">Senior Research Analyst · finds sources · summarizes trends</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-amber" style="flex-shrink:0;max-width:200px;"><div class="ns-node-title">Crew Context</div><div class="ns-node-sub">research output stored · context=[research_task] passes it automatically</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-cyan" style="flex-shrink:0;max-width:200px;"><div class="ns-node-title">Writer Agent</div><div class="ns-node-sub">Technical Content Writer · sees research output · writes blog post</div></div> <div style="color:#4a5a6a;font-size:1.2rem;">→</div> <div class="ns-node ns-node-green" style="flex-shrink:0;max-width:180px;"><div class="ns-node-title">500-word Blog Post</div></div> </div> </div> <hr/> <h2 id="google-adk-four-orchestration-patterns">Google ADK: Four Orchestration Patterns</h2> <p>The ADK provides explicit primitives for each collaboration structure. Here are the four most important.</p> <h3 id="1-hierarchical-parent-child-agents">1. Hierarchical: Parent-Child Agents</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LlmAgent</span><span class="p">,</span> <span class="n">BaseAgent</span>
<span class="kn">from</span> <span class="n">google.adk.agents.invocation_context</span> <span class="kn">import</span> <span class="n">InvocationContext</span>
<span class="kn">from</span> <span class="n">google.adk.events</span> <span class="kn">import</span> <span class="n">Event</span>
<span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">AsyncGenerator</span>

<span class="k">class</span> <span class="nc">TaskExecutor</span><span class="p">(</span><span class="n">BaseAgent</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">A specialized agent with custom, non-LLM behavior.</span><span class="sh">"""</span>
    <span class="n">name</span><span class="p">:</span>        <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">TaskExecutor</span><span class="sh">"</span>
    <span class="n">description</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Executes a predefined task.</span><span class="sh">"</span>

    <span class="k">async</span> <span class="k">def</span> <span class="nf">_run_async_impl</span><span class="p">(</span>
        <span class="n">self</span><span class="p">,</span> <span class="n">context</span><span class="p">:</span> <span class="n">InvocationContext</span>
    <span class="p">)</span> <span class="o">-&gt;</span> <span class="n">AsyncGenerator</span><span class="p">[</span><span class="n">Event</span><span class="p">,</span> <span class="bp">None</span><span class="p">]:</span>
        <span class="k">yield</span> <span class="nc">Event</span><span class="p">(</span><span class="n">author</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">content</span><span class="o">=</span><span class="sh">"</span><span class="s">Task finished successfully.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">BaseAgent</code> is the extension point</strong> for non-LLM agents. When you need deterministic logic, API calls, or custom execution — instead of LLM reasoning — subclass <code class="language-plaintext highlighter-rouge">BaseAgent</code> and implement <code class="language-plaintext highlighter-rouge">_run_async_impl</code>. It yields <code class="language-plaintext highlighter-rouge">Event</code> objects as output, integrating cleanly with the ADK event stream.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">greeter</span>     <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Greeter</span><span class="sh">"</span><span class="p">,</span> <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span> <span class="n">instruction</span><span class="o">=</span><span class="sh">"</span><span class="s">You are a friendly greeter.</span><span class="sh">"</span><span class="p">)</span>
<span class="n">task_doer</span>   <span class="o">=</span> <span class="nc">TaskExecutor</span><span class="p">()</span>

<span class="n">coordinator</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">Coordinator</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">When asked to greet, delegate to the Greeter. When asked to perform a task, delegate to the TaskExecutor.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">sub_agents</span>  <span class="o">=</span> <span class="p">[</span><span class="n">greeter</span><span class="p">,</span> <span class="n">task_doer</span><span class="p">],</span>
<span class="p">)</span>

<span class="c1"># ADK sets parent_agent automatically
</span><span class="k">assert</span> <span class="n">greeter</span><span class="p">.</span><span class="n">parent_agent</span>   <span class="o">==</span> <span class="n">coordinator</span>
<span class="k">assert</span> <span class="n">task_doer</span><span class="p">.</span><span class="n">parent_agent</span> <span class="o">==</span> <span class="n">coordinator</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">sub_agents</code></strong> establishes the parent-child relationship. The coordinator’s LLM reads both agents’ descriptions and decides which to delegate to. The <code class="language-plaintext highlighter-rouge">parent_agent</code> attribute is set automatically by the ADK framework — you don’t need to wire it manually.</p> </blockquote> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ADK HIERARCHICAL AGENTS — parent delegates to children via sub_agents</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-cyan" style="max-width:300px;"><div class="ns-node-title">Coordinator LlmAgent</div><div class="ns-node-sub">reads user query · decides which child to delegate to based on each agent's description</div></div> <div class="ns-arrow"></div> <div class="ns-row" style="max-width:440px;"> <div class="ns-node ns-node-purple"><div class="ns-node-title">Greeter LlmAgent</div><div class="ns-node-sub">activated when greeting is needed · LLM-powered · uses Gemini</div></div> <div class="ns-node ns-node-amber"><div class="ns-node-title">TaskExecutor BaseAgent</div><div class="ns-node-sub">custom logic · non-LLM · runs deterministic code</div></div> </div> </div> </div> <h3 id="2-loopagent-iterative-workflows">2. LoopAgent: Iterative Workflows</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LoopAgent</span><span class="p">,</span> <span class="n">LlmAgent</span><span class="p">,</span> <span class="n">BaseAgent</span>
<span class="kn">from</span> <span class="n">google.adk.events</span> <span class="kn">import</span> <span class="n">Event</span><span class="p">,</span> <span class="n">EventActions</span>

<span class="k">class</span> <span class="nc">ConditionChecker</span><span class="p">(</span><span class="n">BaseAgent</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Stops the loop when session state signals completion.</span><span class="sh">"""</span>
    <span class="n">name</span><span class="p">:</span>        <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">ConditionChecker</span><span class="sh">"</span>
    <span class="n">description</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Checks if a process is complete and signals the loop to stop.</span><span class="sh">"</span>

    <span class="k">async</span> <span class="k">def</span> <span class="nf">_run_async_impl</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">context</span><span class="p">:</span> <span class="n">InvocationContext</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">AsyncGenerator</span><span class="p">[</span><span class="n">Event</span><span class="p">,</span> <span class="bp">None</span><span class="p">]:</span>
        <span class="n">status</span>  <span class="o">=</span> <span class="n">context</span><span class="p">.</span><span class="n">session</span><span class="p">.</span><span class="n">state</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">pending</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">is_done</span> <span class="o">=</span> <span class="p">(</span><span class="n">status</span> <span class="o">==</span> <span class="sh">"</span><span class="s">completed</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">is_done</span><span class="p">:</span>
            <span class="k">yield</span> <span class="nc">Event</span><span class="p">(</span><span class="n">author</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">actions</span><span class="o">=</span><span class="nc">EventActions</span><span class="p">(</span><span class="n">escalate</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span>  <span class="c1"># stop loop
</span>        <span class="k">else</span><span class="p">:</span>
            <span class="k">yield</span> <span class="nc">Event</span><span class="p">(</span><span class="n">author</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">content</span><span class="o">=</span><span class="sh">"</span><span class="s">Condition not met, continuing loop.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">EventActions(escalate=True)</code></strong> is the loop-termination signal. When <code class="language-plaintext highlighter-rouge">ConditionChecker</code> yields this, the <code class="language-plaintext highlighter-rouge">LoopAgent</code> stops regardless of remaining iterations. Without it, the loop runs to <code class="language-plaintext highlighter-rouge">max_iterations</code>.</p> <p><strong><code class="language-plaintext highlighter-rouge">context.session.state</code></strong> is the shared key-value store accessible to all agents within a session. <code class="language-plaintext highlighter-rouge">ProcessingStep</code> writes <code class="language-plaintext highlighter-rouge">"status": "completed"</code> there; <code class="language-plaintext highlighter-rouge">ConditionChecker</code> reads it. This is how agents communicate state in ADK without direct message passing.</p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">process_step</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">ProcessingStep</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Perform your task. If you are the final step, set session state </span><span class="sh">'</span><span class="s">status</span><span class="sh">'</span><span class="s"> to </span><span class="sh">'</span><span class="s">completed</span><span class="sh">'</span><span class="s">.</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">poller</span> <span class="o">=</span> <span class="nc">LoopAgent</span><span class="p">(</span>
    <span class="n">name</span>           <span class="o">=</span> <span class="sh">"</span><span class="s">StatusPoller</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">max_iterations</span> <span class="o">=</span> <span class="mi">10</span><span class="p">,</span>
    <span class="n">sub_agents</span>     <span class="o">=</span> <span class="p">[</span><span class="n">process_step</span><span class="p">,</span> <span class="nc">ConditionChecker</span><span class="p">()],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">LoopAgent</code></strong> runs its <code class="language-plaintext highlighter-rouge">sub_agents</code> sequentially on each iteration. Each iteration: <code class="language-plaintext highlighter-rouge">ProcessingStep</code> executes → <code class="language-plaintext highlighter-rouge">ConditionChecker</code> evaluates → if not done, repeat. Maximum 10 iterations as a safety cap. Use this for polling, retry workflows, and iterative refinement loops.</p> </blockquote> <h3 id="3-sequentialagent-linear-pipelines">3. SequentialAgent: Linear Pipelines</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">SequentialAgent</span><span class="p">,</span> <span class="n">Agent</span>

<span class="n">step1</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Step1_Fetch</span><span class="sh">"</span><span class="p">,</span>   <span class="n">output_key</span><span class="o">=</span><span class="sh">"</span><span class="s">data</span><span class="sh">"</span><span class="p">)</span>
<span class="n">step2</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">Step2_Process</span><span class="sh">"</span><span class="p">,</span> <span class="n">instruction</span><span class="o">=</span><span class="sh">"</span><span class="s">Analyze the information in state[</span><span class="sh">'</span><span class="s">data</span><span class="sh">'</span><span class="s">] and provide a summary.</span><span class="sh">"</span><span class="p">)</span>

<span class="n">pipeline</span> <span class="o">=</span> <span class="nc">SequentialAgent</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">MyPipeline</span><span class="sh">"</span><span class="p">,</span> <span class="n">sub_agents</span><span class="o">=</span><span class="p">[</span><span class="n">step1</span><span class="p">,</span> <span class="n">step2</span><span class="p">])</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">output_key="data"</code></strong> — when <code class="language-plaintext highlighter-rouge">Step1_Fetch</code> completes, its output is automatically stored in <code class="language-plaintext highlighter-rouge">session.state["data"]</code>. <code class="language-plaintext highlighter-rouge">Step2_Process</code> reads it from state via its instruction. This is the ADK equivalent of CrewAI’s <code class="language-plaintext highlighter-rouge">context=[task]</code> — explicit state-passing between sequential steps.</p> </blockquote> <h3 id="4-parallelagent-concurrent-workers">4. ParallelAgent: Concurrent Workers</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span><span class="p">,</span> <span class="n">ParallelAgent</span>

<span class="n">weather_fetcher</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">weather_fetcher</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Fetch the weather for the given location and return only the report.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">output_key</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">weather_data</span><span class="sh">"</span><span class="p">,</span>   <span class="c1"># → session.state["weather_data"]
</span><span class="p">)</span>

<span class="n">news_fetcher</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">news_fetcher</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash-exp</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Fetch the top news story for the given topic and return only that story.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">output_key</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">news_data</span><span class="sh">"</span><span class="p">,</span>      <span class="c1"># → session.state["news_data"]
</span><span class="p">)</span>

<span class="n">data_gatherer</span> <span class="o">=</span> <span class="nc">ParallelAgent</span><span class="p">(</span>
    <span class="n">name</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">data_gatherer</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">sub_agents</span> <span class="o">=</span> <span class="p">[</span><span class="n">weather_fetcher</span><span class="p">,</span> <span class="n">news_fetcher</span><span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">ParallelAgent</code></strong> fires both sub-agents concurrently. Each writes to a different <code class="language-plaintext highlighter-rouge">output_key</code> in session state — <code class="language-plaintext highlighter-rouge">weather_data</code> and <code class="language-plaintext highlighter-rouge">news_data</code> — with no conflict since the keys are distinct. After both complete, a downstream agent (or your application code) can read both from state.</p> </blockquote> <div class="ns-diagram"> <div class="ns-diagram-header"> <span class="ns-diagram-label">ADK PARALLEL AGENT — two workers fire simultaneously, results in shared state</span> <button class="ns-expand-btn" onclick="openNsDiagram(this)"><svg width="11" height="11" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 5V1h4M11 7v4H7M1 5l4-4M11 7l-4 4"/></svg> Expand</button> </div> <div class="ns-diagram-body" style="padding:1.1rem 1.25rem;"> <div class="ns-node ns-node-purple" style="max-width:260px;"><div class="ns-node-title">ParallelAgent</div><div class="ns-node-sub">fires both sub-agents at the exact same time — doesn't wait for one before starting the other</div></div> <div class="ns-arrow"></div> <div class="ns-row" style="max-width:440px;"> <div class="ns-node ns-node-cyan"> <div class="ns-node-title">weather_fetcher</div> <div class="ns-node-sub">output_key: "weather_data" → written to Session State</div> </div> <div class="ns-node ns-node-amber"> <div class="ns-node-title">news_fetcher</div> <div class="ns-node-sub">output_key: "news_data" → written to Session State</div> </div> </div> <div class="ns-arrow"></div> <div class="ns-node" style="max-width:300px;"><div class="ns-node-title">Session State</div><div class="ns-node-sub">shared key-value store · both results land here · no conflict since keys are different</div></div> <div class="ns-arrow"></div> <div class="ns-node ns-node-green" style="max-width:260px;"><div class="ns-node-title">Synthesizer reads both</div><div class="ns-node-sub">downstream agent combines weather_data + news_data into final response</div></div> </div> </div> <h3 id="5-agent-as-a-tool">5. Agent as a Tool</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">LlmAgent</span>
<span class="kn">from</span> <span class="n">google.adk.tools</span> <span class="kn">import</span> <span class="n">agent_tool</span>

<span class="k">def</span> <span class="nf">generate_image</span><span class="p">(</span><span class="n">prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Generates an image from a textual prompt. Returns image bytes.</span><span class="sh">"""</span>
    <span class="n">mock_bytes</span> <span class="o">=</span> <span class="sa">b</span><span class="sh">"</span><span class="s">mock_image_data_for_a_cat_wearing_a_hat</span><span class="sh">"</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">success</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">image_bytes</span><span class="sh">"</span><span class="p">:</span> <span class="n">mock_bytes</span><span class="p">,</span> <span class="sh">"</span><span class="s">mime_type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">image/png</span><span class="sh">"</span><span class="p">}</span>

<span class="n">image_generator_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">ImageGen</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Generates an image based on a detailed text prompt.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Take the user</span><span class="sh">'</span><span class="s">s request and use the generate_image tool to create the image.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span>       <span class="o">=</span> <span class="p">[</span><span class="n">generate_image</span><span class="p">],</span>
<span class="p">)</span>

<span class="n">image_tool</span> <span class="o">=</span> <span class="n">agent_tool</span><span class="p">.</span><span class="nc">AgentTool</span><span class="p">(</span>
    <span class="n">agent</span>       <span class="o">=</span> <span class="n">image_generator_agent</span><span class="p">,</span>
    <span class="n">description</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Use this to generate an image. Input should be a descriptive prompt.</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>

<span class="n">artist_agent</span> <span class="o">=</span> <span class="nc">LlmAgent</span><span class="p">(</span>
    <span class="n">name</span>        <span class="o">=</span> <span class="sh">"</span><span class="s">Artist</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span>       <span class="o">=</span> <span class="sh">"</span><span class="s">gemini-2.0-flash</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Invent a creative image prompt. Then use the ImageGen tool to generate it.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span>       <span class="o">=</span> <span class="p">[</span><span class="n">image_tool</span><span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong><code class="language-plaintext highlighter-rouge">AgentTool</code></strong> wraps a sub-agent and makes it callable as a tool from a parent agent’s perspective. The <code class="language-plaintext highlighter-rouge">artist_agent</code> sees <code class="language-plaintext highlighter-rouge">image_tool</code> the same way it sees <code class="language-plaintext highlighter-rouge">generate_image</code> — as a tool it can call with arguments. The tool’s <code class="language-plaintext highlighter-rouge">description</code> is what the parent’s LLM reads to decide when to invoke it.</p> <p><strong>Why “agent as a tool” instead of <code class="language-plaintext highlighter-rouge">sub_agents</code>?</strong> With <code class="language-plaintext highlighter-rouge">sub_agents</code>, the coordinator’s LLM decides routing. With <code class="language-plaintext highlighter-rouge">AgentTool</code>, the parent agent explicitly invokes the sub-agent like a function call — passing specific arguments and receiving a return value. More precise, less autonomous.</p> </blockquote> <hr/> <h2 id="framework-comparison">Framework Comparison</h2> <table> <thead> <tr> <th>Feature</th> <th>CrewAI</th> <th>Google ADK</th> </tr> </thead> <tbody> <tr> <td><strong>Team definition</strong></td> <td><code class="language-plaintext highlighter-rouge">Agent</code> + <code class="language-plaintext highlighter-rouge">Task</code> + <code class="language-plaintext highlighter-rouge">Crew</code></td> <td><code class="language-plaintext highlighter-rouge">LlmAgent</code> / <code class="language-plaintext highlighter-rouge">BaseAgent</code> + orchestrators</td> </tr> <tr> <td><strong>Sequential flow</strong></td> <td><code class="language-plaintext highlighter-rouge">Process.sequential</code> + <code class="language-plaintext highlighter-rouge">context=[]</code></td> <td><code class="language-plaintext highlighter-rouge">SequentialAgent</code> + <code class="language-plaintext highlighter-rouge">output_key</code></td> </tr> <tr> <td><strong>Parallel flow</strong></td> <td>Multiple tasks with no <code class="language-plaintext highlighter-rouge">context</code> dependency</td> <td><code class="language-plaintext highlighter-rouge">ParallelAgent</code></td> </tr> <tr> <td><strong>Loop flow</strong></td> <td>Custom code / <code class="language-plaintext highlighter-rouge">while</code> loop</td> <td><code class="language-plaintext highlighter-rouge">LoopAgent</code> + <code class="language-plaintext highlighter-rouge">EventActions(escalate=True)</code></td> </tr> <tr> <td><strong>Hierarchical</strong></td> <td><code class="language-plaintext highlighter-rouge">allow_delegation=True</code></td> <td><code class="language-plaintext highlighter-rouge">sub_agents=[]</code> on <code class="language-plaintext highlighter-rouge">LlmAgent</code></td> </tr> <tr> <td><strong>Agent as tool</strong></td> <td>Not native (use LangChain tools)</td> <td><code class="language-plaintext highlighter-rouge">AgentTool</code> wrapper</td> </tr> <tr> <td><strong>State passing</strong></td> <td><code class="language-plaintext highlighter-rouge">context=[task]</code> in task definition</td> <td><code class="language-plaintext highlighter-rouge">output_key</code> → <code class="language-plaintext highlighter-rouge">session.state</code></td> </tr> <tr> <td><strong>Custom agent logic</strong></td> <td>Not supported natively</td> <td><code class="language-plaintext highlighter-rouge">BaseAgent._run_async_impl()</code></td> </tr> </tbody> </table> <hr/> <h2 id="at-a-glance">At a Glance</h2> <div class="mas-summary-card"> <div class="mas-summary-col"> <div class="mas-summary-label">WHAT</div> <p>A system of specialized agents, each owning a distinct role and set of tools, coordinating to achieve a complex shared goal that no single agent could handle effectively alone.</p> </div> <div class="mas-summary-divider"></div> <div class="mas-summary-col"> <div class="mas-summary-label">WHY</div> <p>Specialization beats generalism at scale. Decomposing a complex task into focused sub-tasks — each assigned to an agent built for it — produces higher quality, more reliable, and more scalable outcomes.</p> </div> <div class="mas-summary-divider"></div> <div class="mas-summary-col"> <div class="mas-summary-label">RULE OF THUMB</div> <p>Use when a task requires diverse expertise, multiple distinct phases, or benefits from parallel workstreams. If a single agent's system prompt is trying to do too many jobs — split it into a team.</p> </div> </div> <style>.mas-summary-card{display:flex;border:1px solid var(--global-divider-color);border-radius:10px;overflow:hidden;margin:1.5rem 0}@media(max-width:640px){.mas-summary-card{flex-direction:column}}.mas-summary-col{flex:1;padding:1.1rem;background:rgba(128,128,128,0.03)}.mas-summary-col p{font-size:.8rem;color:var(--global-text-color-light);line-height:1.6;margin:.4rem 0 0}.mas-summary-divider{width:1px;background:var(--global-divider-color);flex-shrink:0}.mas-summary-label{font-size:.62rem;font-weight:700;letter-spacing:.12em;color:#2698ba}</style> <hr/> <h2 id="how-multi-agent-communication-actually-works">How Multi-Agent Communication Actually Works</h2> <p>In both CrewAI and ADK, agents don’t “talk to each other” directly — they communicate through shared data structures managed by the framework.</p> <p><strong>CrewAI: Task context as the communication channel.</strong> When you write <code class="language-plaintext highlighter-rouge">context=[research_task]</code> in a writing task definition, CrewAI stores the research task’s output after it completes. When the writing task runs, CrewAI injects that stored output into the writer agent’s context. The writer doesn’t know or care that a researcher ran before it — it just receives text in its context that says “here is the research that was done.” This is called <strong>indirect communication</strong> — agents share information through a coordinator (CrewAI) rather than directly messaging each other.</p> <p><strong>ADK: Session state as the communication channel.</strong> In ADK, agents communicate through <code class="language-plaintext highlighter-rouge">session.state</code> — a shared dictionary that all agents in a session can read from and write to. When <code class="language-plaintext highlighter-rouge">researcher_agent_1</code> finishes, its output is stored as <code class="language-plaintext highlighter-rouge">session.state["renewable_energy_result"]</code> (because <code class="language-plaintext highlighter-rouge">output_key="renewable_energy_result"</code> was set). When <code class="language-plaintext highlighter-rouge">synthesis_agent</code> runs, its instruction template includes <code class="language-plaintext highlighter-rouge">{renewable_energy_result}</code> — ADK automatically fills this from session state.</p> <p><strong>Why indirect communication is better than direct messaging.</strong> Direct agent-to-agent messaging introduces tight coupling — agent A needs to know about agent B’s interface, location, and availability. If B changes or fails, A breaks. Indirect communication through shared state decouples them — A writes to a named key, B reads from a named key, neither knows about the other. You can swap out A or B independently without changing the other. You can add a new agent C that also reads from the same key. You can replay any agent’s behavior by pre-populating the key manually for debugging.</p> <p><strong>How the orchestrator decides which agent to call.</strong> In ADK’s hierarchical mode, the coordinator’s LLM reads every sub-agent’s <code class="language-plaintext highlighter-rouge">description</code> field and decides which one to invoke based on the user’s request. This is the same mechanism as tool routing: the sub-agent’s description is the “docstring” that the coordinator LLM reads to make its routing decision. Write sub-agent descriptions with the same precision you’d use for tool docstrings.</p> <h2 id="common-mistakes-when-building-multi-agent-systems">Common Mistakes When Building Multi-Agent Systems</h2> <p><strong>Mistake 1: Over-specialization.</strong> Creating 15 highly specialized agents when 3 would do. Each agent boundary adds communication overhead (API calls, context window usage, potential errors in handoffs). Start with fewer, broader agents and specialize only when a single agent clearly fails at a specific task.</p> <p><strong>Mistake 2: No shared vocabulary.</strong> If the Research Agent produces “EV market growth: 40% YoY” and the Writer Agent expects a structured JSON report, the handoff fails. Define the exact format each agent should produce and each agent should expect. Use <code class="language-plaintext highlighter-rouge">expected_output</code> in CrewAI tasks or explicit instructions in ADK agents.</p> <p><strong>Mistake 3: Parallel agents with conflicting writes.</strong> In ADK’s <code class="language-plaintext highlighter-rouge">ParallelAgent</code>, if two sub-agents write to the same <code class="language-plaintext highlighter-rouge">output_key</code>, the second one overwrites the first. Always give parallel agents unique <code class="language-plaintext highlighter-rouge">output_key</code> values. If you forget, you’ll lose data silently — no error is raised.</p> <p><strong>Mistake 4: Not handling agent failure gracefully.</strong> If one agent in a sequential pipeline fails (API timeout, invalid output, exception), the pipeline crashes. In production, wrap each agent call in error handling and decide: should the pipeline retry, skip the failed step, or abort entirely?</p> <p><strong>Mistake 5: Building multi-agent before single-agent.</strong> Don’t jump to multi-agent complexity until a single agent provably fails at the task. Multi-agent systems are harder to debug, more expensive to run, and introduce new failure modes. Always start simple and add agents only when you’ve demonstrated a clear need.</p> <h2 id="key-takeaways">Key Takeaways</h2> <ul> <li><strong>Specialization wins.</strong> A focused agent with a clear role outperforms a generalist agent trying to do everything. Split roles aggressively.</li> <li><strong>The six interaction models</strong> — Single, Network, Supervisor, Supervisor-as-Tool, Hierarchical, Custom — each trade off autonomy vs. control vs. complexity. Most production systems use Hierarchical or Supervisor.</li> <li><strong>CrewAI wires tasks, not agents.</strong> The <code class="language-plaintext highlighter-rouge">context=[task]</code> parameter is the key to sequential handoffs — it passes one agent’s output as context to the next without any manual wiring.</li> <li><strong>ADK provides explicit primitives:</strong> <code class="language-plaintext highlighter-rouge">SequentialAgent</code> for linear pipelines, <code class="language-plaintext highlighter-rouge">ParallelAgent</code> for concurrent work, <code class="language-plaintext highlighter-rouge">LoopAgent</code> for iterative workflows, <code class="language-plaintext highlighter-rouge">sub_agents</code> for hierarchy, <code class="language-plaintext highlighter-rouge">AgentTool</code> for invoking an agent like a function.</li> <li><strong><code class="language-plaintext highlighter-rouge">output_key</code> is ADK’s state bus.</strong> Every agent in an ADK session shares <code class="language-plaintext highlighter-rouge">session.state</code>. Writing to a key with <code class="language-plaintext highlighter-rouge">output_key</code> and reading from it in another agent’s instruction is the standard inter-agent communication mechanism.</li> <li><strong><code class="language-plaintext highlighter-rouge">EventActions(escalate=True)</code></strong> is how a <code class="language-plaintext highlighter-rouge">LoopAgent</code> stops. Always provide a clear stopping condition — and <code class="language-plaintext highlighter-rouge">max_iterations</code> as a safety cap.</li> <li><strong>Failure isolation is a feature.</strong> When one agent in a multi-agent system fails, the others can continue. Compare this to a monolithic agent where one failed step can derail the entire task.</li> </ul> <hr/> <p><em>Next up — Chapter 8: Memory, where agents stop starting from scratch on every turn and begin building persistent context across conversations and sessions.</em></p>]]></content><author><name>Kohsheen Tiku</name></author><category term="agentic-ai"/><category term="llm"/><category term="multi-agent"/><category term="collaboration"/><summary type="html"><![CDATA[One agent hits walls. A team of specialized agents doesn't. Multi-agent collaboration lets you decompose complex problems into coordinated workstreams — each agent doing what it does best.]]></summary></entry></feed>